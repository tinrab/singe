/* automatically generated by rust-bindgen 0.72.1 */

#[repr(C)]
#[derive(Copy, Clone, Debug, Default, Eq, Hash, Ord, PartialEq, PartialOrd)]
pub struct __BindgenBitfieldUnit<Storage> {
    storage: Storage,
}
impl<Storage> __BindgenBitfieldUnit<Storage> {
    #[inline]
    pub const fn new(storage: Storage) -> Self {
        Self { storage }
    }
}
impl<Storage> __BindgenBitfieldUnit<Storage>
where
    Storage: AsRef<[u8]> + AsMut<[u8]>,
{
    #[inline]
    fn extract_bit(byte: u8, index: usize) -> bool {
        let bit_index = if cfg!(target_endian = "big") {
            7 - (index % 8)
        } else {
            index % 8
        };
        let mask = 1 << bit_index;
        byte & mask == mask
    }
    #[inline]
    pub fn get_bit(&self, index: usize) -> bool {
        debug_assert!(index / 8 < self.storage.as_ref().len());
        let byte_index = index / 8;
        let byte = self.storage.as_ref()[byte_index];
        Self::extract_bit(byte, index)
    }
    #[inline]
    pub unsafe fn raw_get_bit(this: *const Self, index: usize) -> bool {
        debug_assert!(index / 8 < core::mem::size_of::< Storage > ());
        let byte_index = index / 8;
        let byte = unsafe {
            *(core::ptr::addr_of!((* this).storage) as *const u8)
                .offset(byte_index as isize)
        };
        Self::extract_bit(byte, index)
    }
    #[inline]
    fn change_bit(byte: u8, index: usize, val: bool) -> u8 {
        let bit_index = if cfg!(target_endian = "big") {
            7 - (index % 8)
        } else {
            index % 8
        };
        let mask = 1 << bit_index;
        if val { byte | mask } else { byte & !mask }
    }
    #[inline]
    pub fn set_bit(&mut self, index: usize, val: bool) {
        debug_assert!(index / 8 < self.storage.as_ref().len());
        let byte_index = index / 8;
        let byte = &mut self.storage.as_mut()[byte_index];
        *byte = Self::change_bit(*byte, index, val);
    }
    #[inline]
    pub unsafe fn raw_set_bit(this: *mut Self, index: usize, val: bool) {
        debug_assert!(index / 8 < core::mem::size_of::< Storage > ());
        let byte_index = index / 8;
        let byte = unsafe {
            (core::ptr::addr_of_mut!((* this).storage) as *mut u8)
                .offset(byte_index as isize)
        };
        unsafe { *byte = Self::change_bit(*byte, index, val) };
    }
    #[inline]
    pub fn get(&self, bit_offset: usize, bit_width: u8) -> u64 {
        debug_assert!(bit_width <= 64);
        debug_assert!(bit_offset / 8 < self.storage.as_ref().len());
        debug_assert!(
            (bit_offset + (bit_width as usize)) / 8 <= self.storage.as_ref().len()
        );
        let mut val = 0;
        for i in 0..(bit_width as usize) {
            if self.get_bit(i + bit_offset) {
                let index = if cfg!(target_endian = "big") {
                    bit_width as usize - 1 - i
                } else {
                    i
                };
                val |= 1 << index;
            }
        }
        val
    }
    #[inline]
    pub unsafe fn raw_get(this: *const Self, bit_offset: usize, bit_width: u8) -> u64 {
        debug_assert!(bit_width <= 64);
        debug_assert!(bit_offset / 8 < core::mem::size_of::< Storage > ());
        debug_assert!(
            (bit_offset + (bit_width as usize)) / 8 <= core::mem::size_of::< Storage > ()
        );
        let mut val = 0;
        for i in 0..(bit_width as usize) {
            if unsafe { Self::raw_get_bit(this, i + bit_offset) } {
                let index = if cfg!(target_endian = "big") {
                    bit_width as usize - 1 - i
                } else {
                    i
                };
                val |= 1 << index;
            }
        }
        val
    }
    #[inline]
    pub fn set(&mut self, bit_offset: usize, bit_width: u8, val: u64) {
        debug_assert!(bit_width <= 64);
        debug_assert!(bit_offset / 8 < self.storage.as_ref().len());
        debug_assert!(
            (bit_offset + (bit_width as usize)) / 8 <= self.storage.as_ref().len()
        );
        for i in 0..(bit_width as usize) {
            let mask = 1 << i;
            let val_bit_is_set = val & mask == mask;
            let index = if cfg!(target_endian = "big") {
                bit_width as usize - 1 - i
            } else {
                i
            };
            self.set_bit(index + bit_offset, val_bit_is_set);
        }
    }
    #[inline]
    pub unsafe fn raw_set(this: *mut Self, bit_offset: usize, bit_width: u8, val: u64) {
        debug_assert!(bit_width <= 64);
        debug_assert!(bit_offset / 8 < core::mem::size_of::< Storage > ());
        debug_assert!(
            (bit_offset + (bit_width as usize)) / 8 <= core::mem::size_of::< Storage > ()
        );
        for i in 0..(bit_width as usize) {
            let mask = 1 << i;
            let val_bit_is_set = val & mask == mask;
            let index = if cfg!(target_endian = "big") {
                bit_width as usize - 1 - i
            } else {
                i
            };
            unsafe { Self::raw_set_bit(this, index + bit_offset, val_bit_is_set) };
        }
    }
}
pub const CUDSS_ALLOCATOR_NAME_LEN: u32 = 64;
pub const CUDSS_VERSION_MAJOR: u32 = 0;
pub const CUDSS_VERSION_MINOR: u32 = 8;
pub const CUDSS_VERSION_PATCH: u32 = 0;
pub const CUDSS_VERSION: u32 = 800;
pub type size_t = ::core::ffi::c_ulong;
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct CUstream_st {
    _unused: [u8; 0],
}
pub type __uint64_t = ::core::ffi::c_ulong;
pub type __off_t = ::core::ffi::c_long;
pub type __off64_t = ::core::ffi::c_long;
pub type FILE = _IO_FILE;
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct _IO_marker {
    _unused: [u8; 0],
}
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct _IO_codecvt {
    _unused: [u8; 0],
}
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct _IO_wide_data {
    _unused: [u8; 0],
}
pub type _IO_lock_t = ::core::ffi::c_void;
#[repr(C)]
#[derive(Debug, Copy, Clone, Hash, PartialOrd, Ord, PartialEq, Eq)]
pub struct _IO_FILE {
    pub _flags: ::core::ffi::c_int,
    pub _IO_read_ptr: *mut ::core::ffi::c_char,
    pub _IO_read_end: *mut ::core::ffi::c_char,
    pub _IO_read_base: *mut ::core::ffi::c_char,
    pub _IO_write_base: *mut ::core::ffi::c_char,
    pub _IO_write_ptr: *mut ::core::ffi::c_char,
    pub _IO_write_end: *mut ::core::ffi::c_char,
    pub _IO_buf_base: *mut ::core::ffi::c_char,
    pub _IO_buf_end: *mut ::core::ffi::c_char,
    pub _IO_save_base: *mut ::core::ffi::c_char,
    pub _IO_backup_base: *mut ::core::ffi::c_char,
    pub _IO_save_end: *mut ::core::ffi::c_char,
    pub _markers: *mut _IO_marker,
    pub _chain: *mut _IO_FILE,
    pub _fileno: ::core::ffi::c_int,
    pub _bitfield_align_1: [u32; 0],
    pub _bitfield_1: __BindgenBitfieldUnit<[u8; 3usize]>,
    pub _short_backupbuf: [::core::ffi::c_char; 1usize],
    pub _old_offset: __off_t,
    pub _cur_column: ::core::ffi::c_ushort,
    pub _vtable_offset: ::core::ffi::c_schar,
    pub _shortbuf: [::core::ffi::c_char; 1usize],
    pub _lock: *mut _IO_lock_t,
    pub _offset: __off64_t,
    pub _codecvt: *mut _IO_codecvt,
    pub _wide_data: *mut _IO_wide_data,
    pub _freeres_list: *mut _IO_FILE,
    pub _freeres_buf: *mut ::core::ffi::c_void,
    pub _prevchain: *mut *mut _IO_FILE,
    pub _mode: ::core::ffi::c_int,
    pub _unused3: ::core::ffi::c_int,
    pub _total_written: __uint64_t,
    pub _unused2: [::core::ffi::c_char; 8usize],
}
impl Default for _IO_FILE {
    fn default() -> Self {
        let mut s = ::core::mem::MaybeUninit::<Self>::uninit();
        unsafe {
            ::core::ptr::write_bytes(s.as_mut_ptr(), 0, 1);
            s.assume_init()
        }
    }
}
impl _IO_FILE {
    #[inline]
    pub fn _flags2(&self) -> ::core::ffi::c_int {
        unsafe { ::core::mem::transmute(self._bitfield_1.get(0usize, 24u8) as u32) }
    }
    #[inline]
    pub fn set__flags2(&mut self, val: ::core::ffi::c_int) {
        unsafe {
            let val: u32 = ::core::mem::transmute(val);
            self._bitfield_1.set(0usize, 24u8, val as u64)
        }
    }
    #[inline]
    pub unsafe fn _flags2_raw(this: *const Self) -> ::core::ffi::c_int {
        unsafe {
            ::core::mem::transmute(
                <__BindgenBitfieldUnit<
                    [u8; 3usize],
                >>::raw_get(::core::ptr::addr_of!((* this)._bitfield_1), 0usize, 24u8)
                    as u32,
            )
        }
    }
    #[inline]
    pub unsafe fn set__flags2_raw(this: *mut Self, val: ::core::ffi::c_int) {
        unsafe {
            let val: u32 = ::core::mem::transmute(val);
            <__BindgenBitfieldUnit<
                [u8; 3usize],
            >>::raw_set(
                ::core::ptr::addr_of_mut!((* this)._bitfield_1),
                0usize,
                24u8,
                val as u64,
            )
        }
    }
    #[inline]
    pub fn new_bitfield_1(
        _flags2: ::core::ffi::c_int,
    ) -> __BindgenBitfieldUnit<[u8; 3usize]> {
        let mut __bindgen_bitfield_unit: __BindgenBitfieldUnit<[u8; 3usize]> = Default::default();
        __bindgen_bitfield_unit
            .set(
                0usize,
                24u8,
                {
                    let _flags2: u32 = unsafe { ::core::mem::transmute(_flags2) };
                    _flags2 as u64
                },
            );
        __bindgen_bitfield_unit
    }
}
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct cudssContext {
    _unused: [u8; 0],
}
/// The structure holds the cuDSS library context (device properties, system information, execution controls like [`cudaStream_t`], etc.).
///
/// The handle must be initialized prior to calling any other cuDSS API with [`cudssCreate`] or [`cudssCreateMg`].
/// The handle must be destroyed to free up resources after using cuDSS with [`cudssDestroy`].
pub type cudssHandle_t = *mut cudssContext;
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct cudssMatrix {
    _unused: [u8; 0],
}
/// The structure is a lightweight wrapper around standard dense/sparse matrix parameters and does not own any data arrays.
/// Matrix objects are used to pass matrix of the linear system, as well as solution and right-hand side (even if these are in fact vectors).
/// Currently, cuDSS matrix objects can have either one of the two underlying matrix formats: dense and 3-array CSR (sparse). Additionally,
/// they can represent non-uniform batches of matrices or distributed (in the MGMN mode).
///
/// Matrix objects should be created via [`cudssMatrixCreateDn`] (for dense matrices) or
/// [`cudssMatrixCreateBatchDn`] (for a batch of dense matrices) or
/// [`cudssMatrixCreateCsr`] (for sparse matrices in CSR format) or
/// [`cudssMatrixCreateBatchCsr`] (for a batch of sparse matrices in CSR format). After use, matrix objects should be destroyed
/// via [`cudssMatrixDestroy`]. For distributed matrices, one can additionally call [`cudssMatrixSetDistributionRow1d`] to define how the matrix is distributed.
///
/// Matrix objects can be modified after creation via [`cudssMatrixSetValues`] and [`cudssMatrixSetCsrPointers`] (and similar APIs for batches).
///
/// Information can be retrieved from a matrix object by calling [`cudssMatrixGetFormat`] followed by either [`cudssMatrixGetDn`] or [`cudssMatrixGetCsr`] depending on the format returned.
pub type cudssMatrix_t = *mut cudssMatrix;
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct cudssData {
    _unused: [u8; 0],
}
/// The structure holds internal data (e.g., factors related data structures) as well as pointers to user-provided data.
/// A single object of this type should be associated with solving a specific linear system.
/// If multiple systems with the same datatype(!) are solved consecutively the object can be re-used (all necessary internal buffers will be re-created per necessity).
///
/// Note: by default, the library allocates device memory required for performing LU factorization and storing the LU factors internally. All data buffers are of this kind are kept inside the data object.
/// To change this default behavior, one can set a [`cudssDeviceMemHandler_t`] which will then be used for allocating device memory inside the solver.
///
/// The object should be created via [`cudssDataCreate`] and destroyed via [`cudssDataDestroy`].
///
/// During execution of any of the stages [`cudssExecute`], configuration settings of the solver are read from [`cudssConfig_t`] and thus affect the execution and internal data stored in the [`cudssData_t`] object.
///
/// Data parameters can be updated or retrieved by calling [`cudssDataSet`] or [`cudssDataGet`] respectively.
pub type cudssData_t = *mut cudssData;
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct cudssConfig {
    _unused: [u8; 0],
}
/// The structure stores configuration settings for the solver. This object is a lightweight (host-side) wrapper around common solver settings. While it can be re-used for solving different linear systems, it is recommended to have one per linear system.
///
/// The object should be created via [`cudssConfigCreate`] and destroyed via [`cudssConfigDestroy`].
///
/// During execution of any of the stages [`cudssExecute`], configuration settings of the solver are read from [`cudssConfig_t`] and thus affect the execution.
///
/// Configuration settings can be updated or retrieved by calling [`cudssConfigSet`] or [`cudssConfigGet`] respectively.
/// Note: certain settings need to be set before a corresponding solver stage is executed (e.g., reordering algorithm must be set prior to the phase [`cudssPhase_t::CUDSS_PHASE_ANALYSIS`]).
pub type cudssConfig_t = *mut cudssConfig;
/// The enumerator specifies the data type of values, offsets and indices used in cuDSS matrix objects and API calls.
///
/// The enumerator values for real and complex floating-point types alias the corresponding [`cudaDataType_t`] values and are interchangeable with them.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudssDataType_t {
    /// Sentinel value indicating an uninitialized or unset data type. Not a valid value for user-facing API calls.
    CUDSS_DATA_TYPE_UNSET = 1024,
    /// 32-bit real single-precision floating-point (`float`).
    CUDSS_R_32F = 0,
    /// 64-bit real double-precision floating-point (`double`).
    CUDSS_R_64F = 1,
    /// 32-bit complex single-precision floating-point (`cuComplex`).
    CUDSS_C_32F = 4,
    /// 64-bit complex double-precision floating-point (`cuDoubleComplex`).
    CUDSS_C_64F = 5,
    /// 128-bit real double-double floating-point ([`cudss_fp64mp2_t`]). This extended-precision type stores a value as the unevaluated sum of two `double` values (hi + lo).
    CUDSS_R_64F_64F = 1026,
    /// 32-bit signed integer (`int32_t`). Used for index and offset arrays.
    CUDSS_R_32I = 10,
    /// 64-bit signed integer (`int64_t`). Used for index and offset arrays.
    CUDSS_R_64I = 24,
}
/// The enumerator specifies possible names of solver configuration settings. For each setting there is a matching type to be used in [`cudssConfigSet`] or [`cudssConfigGet`].
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudssConfigParam_t {
    /// Algorithm for the reordering phase. Supported options are:
    ///
    /// * [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_DEFAULT`] - let the library choose (typically nested dissection).
    /// * [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_NESTED_DISSECTION`] - a customized nested dissection algorithm based on METIS.
    /// * [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_BTF_COLAMD`] - a custom combination of block triangular reordering and COLAMD algorithms which can be used together with global pivoting to increase solution pivots.
    ///   When this option is used for reordering, cuDSS uses an appropriate custom factorization algorithm (without the need to change the factorization setting [`cudssConfigParam_t::CUDSS_CONFIG_FACTORIZATION_ALG`]).
    /// * [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_COLAMD`] - similar to [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_BTF_COLAMD`] in that it implies using a special factorization algorithm which is tailored for a block-triangular representation but uses a trivial block structure.
    /// * [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_AMD`] - an approximate minimum degree (AMD) reordering.
    /// * [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_NONE`] - uses natural (identity) order for the internal ordering when no user permutation is supplied.
    ///
    /// Note: [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_BTF_COLAMD`] and [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_COLAMD`] are only supported for general (non-symmetric or non-hermitian) matrices.
    ///
    /// Note: [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_BTF_COLAMD`] uses an upper bound on the number of non-zero entries in the factors.
    /// If this bound appears to be not sufficient during the factorization phase, a runtime device error is returned (which can be checked by synchronizing the stream and calling [`cudssDataGet`] with [`cudssDataParam_t::CUDSS_DATA_INFO`] which will output the device error).
    /// In order to set a non-default upper bound, one should call [`cudssConfigSet`] with [`cudssConfigParam_t::CUDSS_CONFIG_MAX_LU_NNZ`] setting.
    ///
    /// Note: [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_AMD`] does not support the matrix index type `int64_t`.
    ///
    /// Associated parameter type: [`cudssReorderingAlg_t`]
    ///
    /// Default value: [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_DEFAULT`].
    CUDSS_CONFIG_REORDERING_ALG = 0,
    /// Algorithm for the factorization phase. Supported options are:
    ///
    /// * [`cudssFactorizationAlg_t::CUDSS_FACTORIZATION_ALG_DEFAULT`] - the default factorization algorithm.
    ///   This option chooses the best fitting factorization algorithm based on the choice of the reordering algorithm and sparsity structure produced by it.
    ///   It is not recommended to use other options.
    /// * [`cudssFactorizationAlg_t::CUDSS_FACTORIZATION_ALG_MULTIBLOCK`] - multiblock factorization algorithm. Starting with cudss 0.8.0, this option is supported only for real double data type. Suggested for use with extremely sparse matrices.
    /// * [`cudssFactorizationAlg_t::CUDSS_FACTORIZATION_ALG_GENERAL`] - general factorization algorithm.
    ///
    /// Associated parameter type: [`cudssFactorizationAlg_t`]
    ///
    /// Default value: [`cudssFactorizationAlg_t::CUDSS_FACTORIZATION_ALG_DEFAULT`].
    CUDSS_CONFIG_FACTORIZATION_ALG = 1,
    /// Algorithm for the solving phase. Supported options are:
    ///
    /// * [`cudssSolveAlg_t::CUDSS_SOLVE_ALG_DEFAULT`] - the default solve algorithm.
    /// * [`cudssSolveAlg_t::CUDSS_SOLVE_ALG_GENERAL`] - triangular solve only (forward/back substitution using existing L/U factors, without modifying them).
    ///
    /// Associated parameter type: [`cudssSolveAlg_t`]
    ///
    /// Default value: [`cudssSolveAlg_t::CUDSS_SOLVE_ALG_DEFAULT`], currently equivalent to [`cudssSolveAlg_t::CUDSS_SOLVE_ALG_GENERAL`].
    CUDSS_CONFIG_SOLVE_ALG = 2,
    /// Matching is an optional preprocessing step which computes a (non-symmetric column) permutation to put larger values on the diagonal which often improves the accuracy of the solution.
    /// This permutation is then combined with the permutation from the reordering.
    /// Set to [`cudssMatchingAlg_t::CUDSS_MATCHING_ALG_NONE`] to disable matching; for any other value, matching is enabled.
    /// While matching often reduces the number of perturbed pivots and improves accuracy of the solution
    /// (especially for ill-conditioned and badly scaled matrices) there are no guarantees that the accuracy will improve.
    /// Enabling matching brings an overhead during the analysis step and changes the non-zero pattern of the factors and therefore, can slow down factorization and solve.
    ///
    /// Note: matching routines require their internal workspace size to fit into the limit of 32-bit integers. The workspace limit depend on the choice of the matching algorithm.
    /// The biggest requirement among them is to have `10 * nrows + nnz` &lt; `INT_MAX` (`nrows` and `nnz` for the input matrix without accounting for symmetry).
    ///
    /// Supported options are:
    ///
    /// * [`cudssMatchingAlg_t::CUDSS_MATCHING_ALG_NONE`] - matching is disabled.
    /// * [`cudssMatchingAlg_t::CUDSS_MATCHING_ALG_MAX_DIAG_COUNT`] - this option is based on `job = 1` from MC64 algorithm (HSL).
    ///   Computes a column permutation of the matrix so that the permuted matrix has as many entries on its diagonal as possible. The values on the diagonal are of arbitrary size.
    ///   Note: this option does not use matrix values and thus can actually make the accuracy worse. It is not recommended to use this algorithm for matching unless there is a justified need.
    /// * [`cudssMatchingAlg_t::CUDSS_MATCHING_ALG_MAX_MIN_DIAG`] - this option is based on `job = 2` from MC64 algorithm (HSL). Computes a column permutation of the matrix so that the smallest value on the diagonal of the permuted matrix is maximized.
    /// * [`cudssMatchingAlg_t::CUDSS_MATCHING_ALG_MAX_MIN_DIAG_ALT`] - this option is based on `job = 3` from MC64 algorithm (HSL). Computes a column permutation of the matrix so that the smallest value on the diagonal of the permuted matrix is maximized.
    ///   This algorithm is different from the one used for [`cudssMatchingAlg_t::CUDSS_MATCHING_ALG_MAX_MIN_DIAG`].
    /// * [`cudssMatchingAlg_t::CUDSS_MATCHING_ALG_MAX_DIAG_SUM`] - this option is based on `job = 4` from MC64 algorithm (HSL). Computes a column permutation of the matrix so that the sum of the diagonal entries of the permuted matrix is maximized.
    /// * [`cudssMatchingAlg_t::CUDSS_MATCHING_ALG_MAX_DIAG_PRODUCT`] - this option is based on `job = 5` from MC64 algorithm (HSL). Computes a column permutation of the matrix so that the product of the diagonal entries of the permuted matrix is maximized.
    ///   In addition, this algorithm computes the row/col scaling vectors which can further improve accuracy of the solution.
    /// * [`cudssMatchingAlg_t::CUDSS_MATCHING_ALG_AUTO`] - the library selects the alg to apply, currently the same as [`cudssMatchingAlg_t::CUDSS_MATCHING_ALG_MAX_DIAG_PRODUCT`] (matching with scaling, requires matrix values, the most robust option).
    ///
    /// Note: one of the options, [`cudssMatchingAlg_t::CUDSS_MATCHING_ALG_MAX_DIAG_PRODUCT`], computes scaling vectors in addition to the matching permutation. This option is considered to be the most impactful and it is recommended to use this option if accuracy of the solution needs to be improved.
    /// However, this option requires matrix values to be present during the analysis step.
    ///
    /// Note: matching is not supported for [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_BTF_COLAMD`] and [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_COLAMD`] reordering algorithms (which use global pivoting to make the solution more accurate) or for matrices with type [`cudssDataType_t::CUDSS_R_64F_64F`] or for distributed matrices.
    ///
    /// Associated parameter type: [`cudssMatchingAlg_t`]
    ///
    /// Default value: [`cudssMatchingAlg_t::CUDSS_MATCHING_ALG_NONE`] (matching disabled).
    CUDSS_CONFIG_MATCHING_ALG = 3,
    /// Potential modifier on the system matrix (e.g. transpose or conjugate transpose)
    ///
    /// Associated parameter type: `int`
    ///
    /// Default value: `0` (no modifier).
    ///
    /// Other values are not supported.
    CUDSS_CONFIG_SOLVE_MODE = 4,
    /// Maximum number of steps during the iterative refinement. Refer to [`cudssConfigParam_t::CUDSS_CONFIG_IR_TOL`] for the details.
    ///
    /// Associated parameter type: `int`
    ///
    /// Default value: `0`.
    CUDSS_CONFIG_IR_N_STEPS = 5,
    /// Iterative refinement tolerance.
    ///
    /// When users set both [`cudssConfigParam_t::CUDSS_CONFIG_IR_N_STEPS`] > 0 and [`cudssConfigParam_t::CUDSS_CONFIG_IR_TOL`] > 0,
    /// the solver performs iterative refinement with convergence checking.
    ///
    /// In each iteration, the relative residual norm is computed as $||r||_2 / ||b||_2$ where $r = b - Ax$.
    ///
    /// The refinement terminates early if the residual norm is less than the specified tolerance.
    ///
    /// If [`cudssConfigParam_t::CUDSS_CONFIG_IR_TOL`] = 0 and [`cudssConfigParam_t::CUDSS_CONFIG_IR_N_STEPS`] > 0,
    /// exactly [`cudssConfigParam_t::CUDSS_CONFIG_IR_N_STEPS`] iterations are performed without convergence checking.
    ///
    /// The actual number of iterations performed can be retrieved via [`cudssDataParam_t::CUDSS_DATA_IR_N_STEPS`].
    /// If the tolerance is not met after all iterations, [`cudssStatus_t::CUDSS_STATUS_IR_FAILED`] is returned.
    ///
    /// Associated parameter type: `double`
    ///
    /// Default value: `0.0` (tolerance checking disabled).
    CUDSS_CONFIG_IR_TOL = 6,
    /// Type of pivoting
    ///
    /// The exact meaning of this parameter depends on the choice of the reordering algorithm which can be changed through [`cudssConfigParam_t::CUDSS_CONFIG_REORDERING_ALG`].
    /// For [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_BTF_COLAMD`] and [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_COLAMD`] the parameter refers to the global pivoting, while for [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_NESTED_DISSECTION`] and others it refers to the partial 1x1 pivoting procedure.
    ///
    /// Note that in the latter case, if the matrix type is symmetric but not positive-definite, then the pivoting is also symmetric and the pivot is searched for on the diagonal of the block, if only the pivot type is not equal to [`cudssPivotType_t::CUDSS_PIVOT_NONE`].
    ///
    /// For more details, see the description of the [pivoting strategies](https://docs.nvidia.com/cuda/cudss/advanced_features.html#numerical-pivoting).
    ///
    /// Associated parameter type: [`cudssPivotType_t`]
    ///
    /// Default value: [`cudssPivotType_t::CUDSS_PIVOT_AUTO`].
    CUDSS_CONFIG_PIVOT_TYPE = 7,
    /// Pivoting threshold $p_{threshold}$ which is used to determine if diagonal element is subject to pivoting and will be swapped with the maximum element in the row (or column) depending on the [type of pivoting](https://docs.nvidia.com/cuda/cudss/types.html#cudsspivottype-t).
    /// The diagonal element will be swapped if:
    /// $p_{threshold} \cdot max_{(sub)row \\, or \\, col} |a_{ij}| \geq |a_{ii}|$
    ///
    /// For more details, see the description of the [pivoting strategies](https://docs.nvidia.com/cuda/cudss/advanced_features.html#numerical-pivoting).
    ///
    /// Note: this parameter is only supported when reordering algorithm is set to [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_BTF_COLAMD`] or [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_COLAMD`].
    ///
    /// Associated parameter type: `double`
    ///
    /// Default value: `1.0`.
    CUDSS_CONFIG_PIVOT_THRESHOLD = 8,
    /// Pivoting epsilon.
    ///
    /// By default, this is the absolute value to test and replace small diagonal elements encountered during numerical factorization.
    ///
    /// In case [`cudssConfigParam_t::CUDSS_CONFIG_PIVOT_EPSILON_ALG`] is set to [`cudssPivotEpsilonAlg_t::CUDSS_PIVOT_EPSILON_ALG_SCALED`], this value will be additionally scaled (see more details in the description of [`cudssConfigParam_t::CUDSS_CONFIG_PIVOT_EPSILON_ALG`]).
    ///
    /// Associated parameter type: `double`
    ///
    /// Default value: `1e-5` for single precision and `1e-13` for double precision.
    CUDSS_CONFIG_PIVOT_EPSILON = 9,
    /// Upper limit on the number of nonzero entries in LU factors. This is only relevant for non-symmetric matrices and reordering algorithm set to [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_BTF_COLAMD`] or [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_COLAMD`].
    /// If the number of non-zero entries in L and U exceeds the set limit, a runtime error happen. See also the note for [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_BTF_COLAMD`] in the table entry for [`cudssConfigParam_t::CUDSS_CONFIG_REORDERING_ALG`].
    ///
    /// Associated parameter type: `int64_t`
    ///
    /// Default value: `-1` (then the parameter value is ignored and cuDSS uses an estimate of `100 nnz` where `nnz` is the number of non-zero entries in the input matrix).
    CUDSS_CONFIG_MAX_LU_NNZ = 10,
    /// Memory mode: `0` (default = device-only) or `1` (hybrid = host/device).
    ///
    /// Note: Hybrid host/device memory mode should be enabled before the analysis phase ([`cudssExecute`] with [`cudssPhase_t::CUDSS_PHASE_ANALYSIS`]).
    /// If the decision to use the hybrid mode is done after the analysis phase, the hybrid memory mode should be enabled and analysis phase must be re-done (which is sub-optimal).
    ///
    /// Note: Unlike the hybrid host/device execution mode (see [`cudssConfigParam_t::CUDSS_CONFIG_HYBRID_EXECUTE_MODE`]) which controls where compute kernels are executed, the hybrid memory mode
    /// only allows cuDSS to keep a part of the factor values (internal data) on the host and always uses the GPU for factorization and solve.
    ///
    /// For more details, see [Hybrid host/device memory mode](https://docs.nvidia.com/cuda/cudss/advanced_features.html#hybrid-host-device-memory-mode) feature.
    ///
    /// Associated parameter type: `int`
    ///
    /// Default value: `0` (disabled).
    ///
    /// Currently not supported when [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_BTF_COLAMD`] or [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_COLAMD`] is used for reordering, or, when [`cudssFactorizationAlg_t::CUDSS_FACTORIZATION_ALG_MULTIBLOCK`] is used for the factorization.
    CUDSS_CONFIG_HYBRID_MEMORY_MODE = 11,
    /// User-defined device memory limit (number of bytes) for the hybrid memory mode.
    ///
    /// This setting only affects execution when the hybrid host/device memory mode is enabled.
    /// For more details, see [Hybrid host/device memory mode](https://docs.nvidia.com/cuda/cudss/advanced_features.html#hybrid-host-device-memory-mode) feature.
    ///
    /// Note: In case of multiple devices ([`cudssCreateMg`]), this value must be set for each device separately by calling `cudaSetDevice()` prior to [`cudssConfigSet`]
    ///
    /// Associated parameter type: `int64_t`
    ///
    /// Default value: `-1` (use the internal default heuristic).
    CUDSS_CONFIG_HYBRID_DEVICE_MEMORY_LIMIT = 12,
    /// A flag to enable or disable usage of `cudaHostRegister()` by cuDSS hybrid host/device memory mode. Since the hybrid memory mode of cuDSS uses host memory to store the factors,
    /// it can use `cudaHostRegister()` (if the HW supports it) to speedup associated host-to-device and device-to-host memory transfers.
    /// However, registering host memory has limitations and in some cases might lead to slowdowns. If the flag is set to `0`, cuDSS will not attempt to use `cudaHostRegister()` even if the HW supports it.
    /// This setting only affects execution when the hybrid memory mode is enabled. For more details, see [Hybrid host/device memory mode](https://docs.nvidia.com/cuda/cudss/advanced_features.html#hybrid-host-device-memory-mode) feature.
    ///
    /// Associated parameter type: `int`
    ///
    /// Default value: `1` (use `cudaHostRegister()` if the device supports it).
    CUDSS_CONFIG_USE_CUDA_REGISTER_MEMORY = 13,
    /// Number of threads to be used by cuDSS in [MT mode](https://docs.nvidia.com/cuda/cudss/advanced_features.html#multi-threaded-mt-mode).
    /// This setting only affects execution when the multi-threaded mode is enabled.
    ///
    /// Associated parameter type: `int`
    ///
    /// Default value: `-1` (use number of threads returned by `cudssGetMaxThreads()`).
    CUDSS_CONFIG_HOST_NTHREADS = 14,
    /// Execute mode: `0` (default = device-only) or `1` (hybrid = host/device).
    /// Hybrid execute mode allows cuDSS to perform calculations on both GPU and CPU.
    /// Currently it is used to speed up execution parts with low parallelization capacity.
    ///
    /// Note: Reordering part of the analysis step is performed on CPU regardless of execute mode value.
    ///
    /// Note: Hybrid execute mode should be enabled before the analysis phase ([`cudssExecute`] with [`cudssPhase_t::CUDSS_PHASE_ANALYSIS`]).
    ///
    /// Note: Unlike the hybrid execution mode which controls where compute kernels are executed
    /// (and allows greater flexibility in placement of the input data), the hybrid host/device memory mode [`cudssConfigParam_t::CUDSS_CONFIG_HYBRID_MEMORY_MODE`] only allows cuDSS to keep part of the factor values (internal data)
    /// on the host while only the GPU is used for factorization and solve.
    ///
    /// Note: If hybrid execute mode is enabled, input matrix, right hand side and solution can be host memory pointers.
    ///
    /// For more details, see the [Hybrid host/device execute mode](https://docs.nvidia.com/cuda/cudss/advanced_features.html#hybrid-host-device-execute-mode) feature.
    ///
    /// Associated parameter type: `int`
    ///
    /// Note: For `nrhs > 1`, any non-one value must be set before the analysis phase ([`cudssExecute`] with [`cudssPhase_t::CUDSS_PHASE_ANALYSIS`]); the exact value used during solve does not need to match.
    ///
    /// Note: It is strongly recommended to enable the threading layer ([`cudssSetThreadingLayer`]) when hybrid execute mode is active, as all analysis step computations are enhanced by multi-threading. Expect significant performance improvements, especially for small and medium-sized matrices.
    ///
    /// Default value: `0` (disabled). Currently not supported when [`cudssConfigParam_t::CUDSS_CONFIG_HYBRID_MEMORY_MODE`] is used.
    /// Also not supported when [`cudssConfigParam_t::CUDSS_CONFIG_DETERMINISTIC_MODE`] is enabled, or, when `batchCount` is greater than 1, or when right-hand side and/or solution have a non-trivial leading dimension.
    CUDSS_CONFIG_HYBRID_EXECUTE_MODE = 15,
    /// Algorithm for pivot epsilon calculation. When a pivot has magnitude smaller than the pivot epsilon threshold (see the description of [`cudssConfigParam_t::CUDSS_CONFIG_PIVOT_EPSILON`]), it is replaced by an appropriately signed value derived from the epsilon; the chosen algorithm controls how that value is computed.
    ///
    /// Supported options are:
    ///
    /// * [`cudssPivotEpsilonAlg_t::CUDSS_PIVOT_EPSILON_ALG_DEFAULT`] - Default behavior pre-selected by the library, currently equivalent to [`cudssPivotEpsilonAlg_t::CUDSS_PIVOT_EPSILON_ALG_STATIC`].
    /// * [`cudssPivotEpsilonAlg_t::CUDSS_PIVOT_EPSILON_ALG_SCALED`] - Pivots with magnitude smaller than epsilon will be replaced by the appropriately signed epsilon scaled by the maximum element in the corresponding row and column of the original matrix.
    /// * [`cudssPivotEpsilonAlg_t::CUDSS_PIVOT_EPSILON_ALG_STATIC`] - Pivots with magnitude smaller than epsilon will be replaced by the appropriately signed epsilon (no scaling).
    ///
    /// Note: [`cudssPivotEpsilonAlg_t::CUDSS_PIVOT_EPSILON_ALG_SCALED`] is not supported when [`cudssConfigParam_t::CUDSS_CONFIG_REORDERING_ALG`] is set to [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_BTF_COLAMD`] or [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_COLAMD`].
    ///
    /// Associated parameter type: [`cudssPivotEpsilonAlg_t`]
    ///
    /// Default value: [`cudssPivotEpsilonAlg_t::CUDSS_PIVOT_EPSILON_ALG_DEFAULT`].
    CUDSS_CONFIG_PIVOT_EPSILON_ALG = 16,
    /// Minimum number of levels for the nested dissection reordering. The value for this parameter should be a positive integer.
    /// Additionally, for the [MGMN mode](https://docs.nvidia.com/cuda/cudss/advanced_features.html#multi-gpu-multi-node-mgmn-mode) the number of levels will be automatically increased (if needed) to satisfy the requirement: $2^{n_{levels}-1} \geq n_{proc}$
    /// where $n_{proc}$ is the number of processes in the communicator. This setting only works when reordering algorithm is [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_NESTED_DISSECTION`] (which is also [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_DEFAULT`]).
    ///
    /// Note: This is considered an advanced performance knob and it is recommended to use a non-default value only when optimizing for performance. Typical values to try should not be too far from the default value, e.g. from the range 8 - 11.
    ///
    /// Associated parameter type: `int`
    ///
    /// Default value: `10`.
    CUDSS_CONFIG_ND_NLEVELS = 17,
    /// The number of matrices in a uniform batch of systems to be processed by cuDSS. Uniform batch of matrices is defined as a batch of matrices with the same non-zero pattern but potentially different values.
    /// To create a uniform batch of matrices (unlike a non-uniform batch) one can simply use the usual [`cudssMatrixCreateCsr`] or [`cudssMatrixCreateDn`]
    /// with the only change: as a pointer to the matrix values one should provide a buffer which holds values for all matrices in the batch.
    /// Thus, it should have `nnz * CUDSS_CONFIG_UBATCH_SIZE` elements
    /// for sparse matrices (`csr_values` only) and `nrows * ncols * CUDSS_CONFIG_UBATCH_SIZE` elements for dense matrices.
    ///
    /// Note: a single system can be viewed as a uniform batch with [`cudssConfigParam_t::CUDSS_CONFIG_UBATCH_SIZE`] set to 1.
    ///
    /// There are two ways how cuDSS can process a uniform batch based on the value of the [`cudssConfigParam_t::CUDSS_CONFIG_UBATCH_INDEX`]: either factorizing (or solving) all matrices at once or just one at a time.
    ///
    /// For details, see the description of [`cudssConfigParam_t::CUDSS_CONFIG_UBATCH_INDEX`].
    ///
    /// Note: [`cudssConfigParam_t::CUDSS_CONFIG_UBATCH_SIZE`] must be set before calling [`cudssPhase_t::CUDSS_PHASE_ANALYSIS`].
    ///
    /// Associated parameter type: `int`
    ///
    /// Default value: `1`.
    ///
    /// Currently not supported when either [`cudssConfigParam_t::CUDSS_CONFIG_HYBRID_MEMORY_MODE`] (see [here](https://docs.nvidia.com/cuda/cudss/advanced_features.html#hybrid-host-device-memory-mode)) or [`cudssConfigParam_t::CUDSS_CONFIG_HYBRID_EXECUTE_MODE`] (see [here](https://docs.nvidia.com/cuda/cudss/advanced_features.html#hybrid-host-device-execute-mode))
    /// are enabled, or [MGMN mode](https://docs.nvidia.com/cuda/cudss/advanced_features.html#multi-gpu-multi-node-mgmn-mode) is used, or with [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_BTF_COLAMD`] and [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_COLAMD`] reordering algorithms.
    CUDSS_CONFIG_UBATCH_SIZE = 18,
    /// `-1` or a 0-based index of matrix in a uniform batch which will be processed during factorization or solve phase.
    /// Special value of `-1` can be used to tell cuDSS to process all matrices in the uniform batch at once.
    ///
    /// Note: [`cudssConfigParam_t::CUDSS_CONFIG_UBATCH_INDEX`] must be less than [`cudssConfigParam_t::CUDSS_CONFIG_UBATCH_SIZE`].
    ///
    /// Note: if [`cudssConfigParam_t::CUDSS_CONFIG_UBATCH_INDEX`] is set then cuDSS will treat the input sparse matrix, right hand side and solution as regular matrices (with just one set of values).
    /// Thus, it allows to process a uniform batch of matrices one by one while re-using the result of the analysis and keeping corresponding factors for all matrices in a single [`cudssData_t`] object.
    ///
    /// Note: [`cudssConfigParam_t::CUDSS_CONFIG_UBATCH_INDEX`] can be set after [`cudssPhase_t::CUDSS_PHASE_FACTORIZATION`], in that case one could solve only one specific system
    ///
    /// Associated parameter type: `int`
    ///
    /// Default value: `-1`
    ///
    /// Currently not supported when either [`cudssConfigParam_t::CUDSS_CONFIG_HYBRID_MEMORY_MODE`] (see [here](https://docs.nvidia.com/cuda/cudss/advanced_features.html#hybrid-host-device-memory-mode)) or [`cudssConfigParam_t::CUDSS_CONFIG_HYBRID_EXECUTE_MODE`] (see [here](https://docs.nvidia.com/cuda/cudss/advanced_features.html#hybrid-host-device-execute-mode))
    /// are enabled, or [MGMN mode](https://docs.nvidia.com/cuda/cudss/advanced_features.html#multi-gpu-multi-node-mgmn-mode) is used, or with [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_BTF_COLAMD`] and [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_COLAMD`] reordering algorithms.
    CUDSS_CONFIG_UBATCH_INDEX = 19,
    /// Use superpanel optimization: `1` (default = enabled) or `0` (disabled).
    ///
    /// Note: superpanel optimization should be enabled before the analysis phase ([`cudssExecute`] with [`cudssPhase_t::CUDSS_PHASE_ANALYSIS`]).
    /// If the decision to use the superpanel optimization mode is done after the analysis phase, the superpanel optimization should be enabled and analysis phase must be re-done which is sub-optimal).
    ///
    /// Associated parameter type: `int`
    ///
    /// Default value: `1` (enabled).
    CUDSS_CONFIG_USE_SUPERPANELS = 20,
    /// Device count in case of multiple device (see [`cudssCreateMg`])
    ///
    /// Note: Maximum supported device count is 16
    ///
    /// Associated parameter type: `int`
    ///
    /// Default value: `1`.
    CUDSS_CONFIG_DEVICE_COUNT = 21,
    /// A list of device indices as an integer array.
    ///
    /// Note: The calling device must be equal to the first device index from `device_indices` or `0` (if `device_indices` were `NULL` in the required prior call to [`cudssCreateMg`])
    ///
    /// Associated parameter type: `int`
    ///
    /// Default value: `NULL` (cuDSS will use devices from 0 to `device_count - 1`).
    CUDSS_CONFIG_DEVICE_INDICES = 22,
    /// Schur complement mode: `0` (default = disabled) or `1` (enabled).
    ///
    /// For more details regarding the Schur complement feature, see [Schur complement feature](https://docs.nvidia.com/cuda/cudss/advanced_features.html#schur-complement).
    ///
    /// Associated parameter type: `int`
    ///
    /// Default value: `0` (disabled).
    ///
    /// Currently not supported when [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_BTF_COLAMD`] or [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_COLAMD`] is used for reordering, when [MGMN mode](https://docs.nvidia.com/cuda/cudss/advanced_features.html#multi-gpu-multi-node-mgmn-mode) or [multi-GPU mode](https://docs.nvidia.com/cuda/cudss/advanced_features.html#multi-gpu-mg-mode) is used,
    /// or, when [`cudssFactorizationAlg_t::CUDSS_FACTORIZATION_ALG_MULTIBLOCK`] is used for the factorization. It is also not supported for uniform and non-uniform batches, or when matching is enabled.
    CUDSS_CONFIG_SCHUR_MODE = 23,
    /// Enable deterministic mode.
    /// In deterministic mode, cuDSS is guaranteed to provide the bit-wise identical result at every run when executed on GPUs with the same architecture
    /// and the same number of SMs (assuming the input data and solver settings are also bit-wise identical).
    ///
    /// Note: in deterministic mode cuDSS uses a different set of kernels which often might be slower than the kernels used in the default mode.
    ///
    /// Associated parameter type: `int`
    ///
    /// Default value: `0` (disabled).
    ///
    /// Currently the feature is supported only for single-gpu, single rhs, and with both hybrid host/device memory mode ([`cudssConfigParam_t::CUDSS_CONFIG_HYBRID_MEMORY_MODE`]) and hybrid host/device execute mode ([`cudssConfigParam_t::CUDSS_CONFIG_HYBRID_EXECUTE_MODE`]) disabled.
    /// Also, this feature does not support iterative refinement or reordering algorithms [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_BTF_COLAMD`] and [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_COLAMD`].
    CUDSS_CONFIG_DETERMINISTIC_MODE = 24,
    /// Controls the balance of the nested dissection partition tree. The value represents the allowed percentage of imbalance between the two children at each level of the partition tree. Possible values are from 1 to 100.
    ///
    /// Note: Smaller values produce a more balanced partition tree. For [MGMN mode](https://docs.nvidia.com/cuda/cudss/advanced_features.html#multi-gpu-multi-node-mgmn-mode) and [MG mode](https://docs.nvidia.com/cuda/cudss/advanced_features.html#multi-gpu-mg-mode), it is recommended to use smaller values in the range 2 to 5.
    ///
    /// Associated parameter type: `int`
    ///
    /// Default value: `20`.
    CUDSS_CONFIG_ND_UBFACTOR = 25,
}
/// The enumerator specifies possible parameter names which can set or get in the [`cudssData_t`] object. For each parameter name
/// there is an associated type to be used in [`cudssDataSet`] or [`cudssDataGet`]. Each parameter
/// name is marked with “in” or “out” depending on whether a parameter can be only set, get or be involved in both.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudssDataParam_t {
    /// By default, this parameter can be used to detect device-side asynchronous errors (primarily, during factorization and solve).
    /// However, in the special case, when in [hybrid host/device execute mode](https://docs.nvidia.com/cuda/cudss/advanced_features.html#hybrid-host-device-execute-mode), factorization is done on the host at least partially,
    /// this parameter can be used to retrieve host-side errors. In case execution is successful, the value is `0`.
    ///
    /// Note: the error returned via [`cudssDataParam_t::CUDSS_DATA_INFO`] is independent from the host side status of type [`cudssStatus_t`] returned by all cuDSS routines, including [`cudssExecute`].
    /// One of the noticeable use cases is when a matrix of the system is passed with an `mtype` for positive-definite matrices but it appears to have non-positive minors (at least numerically).
    /// In this case, calling [`cudssDataGet`] with [`cudssDataParam_t::CUDSS_DATA_INFO`] after the factorization phase will return the 1-based index of the first encountered non-positive minor.
    /// Then one can set the device error back to zero via [`cudssDataSet`], and either change the matrix type or adjust the matrix values (to make the matrix positive-definite) and call the factorization phase again.
    ///
    /// Note that the returned index is 1-based and is for the reordered matrix.
    /// To get the corresponding original index, it should be combined with (inverse) permutation which can be extracted via [`cudssDataGet`] for [`cudssDataParam_t::CUDSS_DATA_PERM_REORDER_ROW`].
    ///
    /// Direction: out
    ///
    /// Memory: host
    ///
    /// Associated parameter type: `int`.
    CUDSS_DATA_INFO = 0,
    /// Number of non-zero entries in LU factors.
    ///
    /// Note: in case `batchCount` > 1 (non-uniform batch) [`cudssDataGet`] returns accumulated number over all
    /// matrices in the batch.
    ///
    /// Direction: out
    ///
    /// Memory: host
    ///
    /// Associated parameter type: `int64_t`.
    CUDSS_DATA_LU_NNZ = 1,
    /// Number of pivots encountered during factorization.
    ///
    /// Direction: out
    ///
    /// Memory: host
    ///
    /// Associated parameter type: same as for the indices of the sparse matrix of the system.
    CUDSS_DATA_NPIVOTS = 2,
    /// Positive and negative indices of inertia for the system matrix `A` (two integer values). Valid only for symmetric/Hermitian non positive-definite matrix types.
    ///
    /// Note: in case `batchCount` > 1 (non-uniform batch) [`cudssDataGet`] returns the accumulated number over all
    /// matrices in the batch.
    ///
    /// Direction: out
    ///
    /// Memory: host
    ///
    /// Associated parameter type: same as for the indices of the sparse matrix of the system.
    CUDSS_DATA_INERTIA = 3,
    /// Row permutation `P` after reordering such that `A\[P,Q\]` is factorized.
    ///
    /// Note: using this parameter in [`cudssDataGet`] in case `batchCount` > 1 is not supported.
    ///
    /// Direction: out
    ///
    /// Memory: host or device
    ///
    /// Associated parameter type: same as for the indices of the sparse matrix of the system.
    CUDSS_DATA_PERM_REORDER_ROW = 4,
    /// Column permutation `Q` after reordering such that `A\[P,Q\]` is factorized.
    ///
    /// Note: using this parameter in [`cudssDataGet`] in case `batchCount` > 1 is not supported.
    ///
    /// Direction: out
    ///
    /// Memory: host or device
    ///
    /// Associated parameter type: same as for the indices of the sparse matrix of the system.
    CUDSS_DATA_PERM_REORDER_COL = 5,
    /// Final row permutation P (includes effects of both reordering and pivoting) which is applied to the original right-hand side of the system in the form $b_{new} = b_{old} \circ P$
    ///
    /// Note: using this parameter in [`cudssDataGet`] in case `batchCount` > 1 is not supported.
    ///
    /// Direction: out
    ///
    /// Memory: host or device
    ///
    /// Associated parameter type: same as for the indices of the sparse matrix of the system
    ///
    /// Currently supported only when [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_BTF_COLAMD`] or [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_COLAMD`] is used for reordering.
    CUDSS_DATA_PERM_ROW = 6,
    /// Final column permutation Q (includes effects of both reordering and pivoting) which is applied to transform the solution of the permuted system into the original solution $x_{old} = x_{new} \circ Q^{-1}$
    ///
    /// Note: using this parameter in [`cudssDataGet`] in case `batchCount` > 1 is not supported.
    ///
    /// Direction: out
    ///
    /// Memory: host or device
    ///
    /// Associated parameter type: same as for the indices of the sparse matrix of the system
    ///
    /// Currently supported only when [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_BTF_COLAMD`] or [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_COLAMD`] is used for reordering.
    CUDSS_DATA_PERM_COL = 7,
    /// Diagonal of the factorized matrix. The diagonal is returned in the original matrix order, taking into account all permutations applied during matching, reordering, and pivoting.
    ///
    /// Note: in case `batchCount` > 1 (non-uniform batch) [`cudssDataGet`] returns the accumulated values over all
    /// matrices in the batch.
    ///
    /// Direction: out
    ///
    /// Memory: host or device
    ///
    /// Associated parameter type: same as for the values of the sparse matrix of the system
    ///
    /// Currently supported only when [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_BTF_COLAMD`] or [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_COLAMD`] is used for reordering.
    CUDSS_DATA_DIAG = 8,
    /// User permutation to be used instead of running the reordering algorithms.
    /// The user permutation can be disabled by providing a `NULL` with zero size with [`cudssDataSet`].
    /// The provided buffer should be an integer array of size `n`, where `n` is the number of rows/columns of the system matrix.
    /// The integer type must match the one used for the system matrix.
    /// The values should represent a valid permutation vector for the set $\\{ index base, ..., n - index base \\}$
    ///
    /// Note: after [`cudssDataSet`] is called with [`cudssDataParam_t::CUDSS_DATA_USER_PERM`], the data are copied into an internal buffer so that the user buffer can be deallocated or re-used.
    ///
    /// Note: using this parameter in [`cudssDataGet`] in case `batchCount` > 1 is not supported.
    ///
    /// Direction: in
    ///
    /// Memory: host or device
    ///
    /// Associated parameter type: same as for the indices of the sparse matrix of the system
    ///
    /// Currently not supported when [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_BTF_COLAMD`] or [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_COLAMD`] is used for reordering.
    CUDSS_DATA_USER_PERM = 9,
    /// Minimal amount of device memory (number of bytes) required in the hybrid memory mode. This query must be done after the analysis phase and will return status [`cudssStatus_t::CUDSS_STATUS_NOT_SUPPORTED`] if it cannot be processed.
    ///
    /// Note: In case of multiple devices ([`cudssCreateMg`]), minimal memory will be returned for the calling device only.
    /// Thus, `cudaSetDevice()` must be called prior to [`cudssDataGet`] to get minimal memory on corresponding device
    ///
    /// Direction: out
    ///
    /// Memory: host
    ///
    /// Associated parameter type: `int64_t`
    ///
    /// For more details, see the [Hybrid host/device memory mode](https://docs.nvidia.com/cuda/cudss/advanced_features.html#hybrid-host-device-memory-mode) feature.
    CUDSS_DATA_HYBRID_DEVICE_MEMORY_MIN = 10,
    /// Device communicator for [MGMN mode](https://docs.nvidia.com/cuda/cudss/advanced_features.html#multi-gpu-multi-node-mgmn-mode).
    /// The actual type of the communicator must match the device communication backend of the [communication layer](https://docs.nvidia.com/cuda/cudss/advanced_features.html#communication-layer-library-in-cudss) which must be set via calling [`cudssSetCommLayer`].
    /// This communicator is used for communication involving device memory buffers.
    ///
    /// Direction: in
    ///
    /// Memory: host
    ///
    /// Associated parameter type: `void*`
    ///
    /// See [MGMN mode](https://docs.nvidia.com/cuda/cudss/advanced_features.html#multi-gpu-multi-node-mgmn-mode) for further details.
    CUDSS_DATA_COMM_DEVICE = 11,
    /// Host communicator for [MGMN mode](https://docs.nvidia.com/cuda/cudss/advanced_features.html#multi-gpu-multi-node-mgmn-mode).
    /// The actual type of the communicator must match the host communication backend of the [communication layer](https://docs.nvidia.com/cuda/cudss/advanced_features.html#communication-layer-library-in-cudss) which must be set via calling [`cudssSetCommLayer`].
    /// This communicator is used for communication involving host memory buffers.
    ///
    /// Direction: in
    ///
    /// Memory: host
    ///
    /// Associated parameter type: `void*`
    ///
    /// See [MGMN mode](https://docs.nvidia.com/cuda/cudss/advanced_features.html#multi-gpu-multi-node-mgmn-mode) for further details.
    CUDSS_DATA_COMM_HOST = 12,
    /// Memory estimates (in bytes) for host and device memory required for the chosen memory mode.
    /// The chosen memory mode is defined as the memory mode detected during the last executed analysis phase for the current [`cudssData_t`] object and the state of the corresponding [`cudssConfigParam_t`] object during the call.
    ///
    /// See [`cudssConfigParam_t`] for more details.
    ///
    /// Note: the returned memory estimate depends not just on the settings from [`cudssConfig_t`] object but also on the input matrix and rhs (specifically, nrhs)
    /// so if the objects (or the config) change after the analysis phase, memory estimates might be not accurate anymore.
    ///
    /// Values returned in the output array at position:
    /// \* 0 - permanent device memory
    /// \* 1 - peak device memory
    /// \* 2 - permanent host memory
    /// \* 3 - peak host memory
    /// \* 4 - (if in hybrid memory mode) minimum device memory for the hybrid memory mode
    /// \* 5 - (if in hybrid memory mode) maximum host memory for the hybrid memory mode
    /// \* 6, … 15 - reserved for future use
    ///
    /// This query must be done after the analysis phase and will return status [`cudssStatus_t::CUDSS_STATUS_NOT_SUPPORTED`] if it cannot be processed.
    ///
    /// Note: In case of multiple devices ([`cudssCreateMg`]) memory estimate will be done for the calling device only.
    /// Thus, `cudaSetDevice()` must be called prior to [`cudssDataGet`] to get memory estimate on corresponded device
    ///
    /// Direction: out
    ///
    /// Memory: host
    ///
    /// Associated parameter type: `int64_t\[16\]`.
    CUDSS_DATA_MEMORY_ESTIMATES = 13,
    /// Matching (column) permutation `Q` such that `A\[:,Q\]` is reordered and then factorized.
    ///
    /// Direction: out
    ///
    /// Memory: host or device
    ///
    /// Associated parameter type: same as for the indices of the sparse matrix of the system.
    CUDSS_DATA_PERM_MATCHING = 14,
    /// Row scaling the factorized matrix (corresponding to the rows of the original matrix)
    ///
    /// Direction: out
    ///
    /// Memory: host or device
    ///
    /// Associated parameter type: floating point for the absolute values of the matrix of the system (i.e., always real, either float or double)
    ///
    /// Only supported when matching is enabled (i.e. [`cudssConfigParam_t::CUDSS_CONFIG_MATCHING_ALG`] is not [`cudssMatchingAlg_t::CUDSS_MATCHING_ALG_NONE`]) and the chosen matching algorithm computes the scaling.
    CUDSS_DATA_SCALE_ROW = 15,
    /// Column scaling the factorized matrix (corresponding to the columns of the original matrix).
    ///
    /// Direction: out
    ///
    /// Memory: host or device
    ///
    /// Associated parameter type: floating point for the absolute values of the matrix of the system (i.e., always real, either float or double). Only supported when matching is enabled (i.e. [`cudssConfigParam_t::CUDSS_CONFIG_MATCHING_ALG`] is not [`cudssMatchingAlg_t::CUDSS_MATCHING_ALG_NONE`]) and the chosen matching algorithm computes the scaling.
    CUDSS_DATA_SCALE_COL = 16,
    /// Number of superpanels in the matrix.
    ///
    /// Direction: out
    ///
    /// Memory: host
    ///
    /// Associated parameter type: same as for the indices of the sparse matrix of the system.
    CUDSS_DATA_NSUPERPANELS = 17,
    /// User-provided Schur complement indices. The provided buffer should be an integer array of size `n`, where `n` is the number of rows (columns) of the system matrix,
    /// and the integer type should match the one used for the system matrix.
    /// The values should be equal to `1` for the rows/columns which are part of the Schur complement and `0` for the rest.
    ///
    /// Note: after [`cudssDataSet`] is called with [`cudssDataParam_t::CUDSS_DATA_USER_SCHUR_INDICES`], the data are copied into an internal buffer so that the user buffer can be deallocated or re-used.
    ///
    /// Direction: in
    ///
    /// Memory: host or device
    ///
    /// Associated parameter type: same as for the indices of the sparse matrix of the system.
    CUDSS_DATA_USER_SCHUR_INDICES = 18,
    /// Shape of the Schur complement matrix as a triplet `(nrows, ncols, sparse nnz)`. The last entry is only relevant when the Schur complement needs to be exported as a sparse matrix
    /// By querying with [`cudssDataSet`] with [`cudssDataParam_t::CUDSS_DATA_SCHUR_SHAPE`], users can get the necessary allocation sizes for the Schur complement buffers
    /// which can then be used to create a [`cudssMatrix_t`] object and passed back to cuDSS to fill in the Schur complement data via [`cudssDataSet`] with [`cudssDataParam_t::CUDSS_DATA_SCHUR_MATRIX`].
    ///
    /// Direction: out
    ///
    /// Memory: host
    ///
    /// Associated parameter type: `int64_t\[3\]`.
    CUDSS_DATA_SCHUR_SHAPE = 19,
    /// Schur complement matrix as a [`cudssMatrix_t`] object.
    ///
    /// Direction: in
    ///
    /// Memory: host (with underlying buffers on host or device)
    ///
    /// Associated parameter type: [`cudssMatrix_t`]
    ///
    /// Note: the data type for matrix values, offsets and indices (if it is a sparse matrix) must be same as for the values, offsets and indices of the system matrix respectively.
    ///
    /// Note: Schur complement matrix cannot be a distributed matrix. Also, Schur complement cannot be computed for matrix batches currently. For the dense Schur complement,
    /// the layout must be column-major. For sparse Schur complement, the matrix type must be [`cudssMatrixType_t::CUDSS_MTYPE_GENERAL`] and the matrix view must be [`cudssMatrixViewType_t::CUDSS_MVIEW_FULL`].
    CUDSS_DATA_SCHUR_MATRIX = 20,
    /// User provided nested dissection (ND) partition tree information, which is used instead of running the reordering algorithm (therefore, saving runtime).
    /// It must be used in combination with [`cudssDataParam_t::CUDSS_DATA_USER_PERM`] to have an effect.
    ///
    /// See [`cudssDataParam_t::CUDSS_DATA_ND_PARTITION_TREE`] for the size and usage information. The user ND partition tree can be disabled by providing a `NULL` with zero size with [`cudssDataSet`].
    ///
    /// Note: While it is possible to construct this array outside cuDSS (e.g., extracting the so-called `sizes` array from METIS / ParMETIS), we recommend to only use this feature by extracting the ND partition tree from cuDSS,
    /// followed by passing it back in subsequent calls.
    ///
    /// Direction: in
    ///
    /// Memory: host or device
    ///
    /// Associated parameter type: same as for the indices of the sparse matrix of the system.
    CUDSS_DATA_USER_ND_PARTITION_TREE = 21,
    /// Nested Dissection (ND) partition tree information, also known as a separator tree or separator sizes, which are computed during the reordering phase and used for improving parallelization.
    /// The number of elements in this array is $2^{n_{levels}} - 1$ with
    /// $n_{levels}$ from [`cudssConfigParam_t::CUDSS_CONFIG_ND_NLEVELS`]. It can be used in combination with
    /// [`cudssDataParam_t::CUDSS_DATA_USER_PERM`] and [`cudssDataParam_t::CUDSS_DATA_USER_ND_PARTITION_TREE`] for a matrix with the same sparsity structure.
    /// This avoids regenerating the same reordering information while retaining the same performance for the factorization and solve phases.
    ///
    /// See the [Nested Dissection Partition Tree](https://docs.nvidia.com/cuda/cudss/advanced_features.html#user-provided-nested-dissection-nd-partition-tree-data-saving-reordering) section for more details.
    ///
    /// Direction: out
    ///
    /// Memory: host or device
    ///
    /// Associated parameter type: same as for the indices of the sparse matrix of the system.
    CUDSS_DATA_ND_PARTITION_TREE = 22,
    /// User-provided host interrupt pointer. Setting this pointer to a non-`NULL` value enables user interruption of cuDSS routines on the host.
    /// Specifically, in the calls to [`cudssExecute`] cuDSS will perform checks on the host for the value of the pointer.
    /// If it is non-zero, execution stops and cuDSS returns early (before the phase execution completes) with the status [`cudssStatus_t::CUDSS_STATUS_EXECUTION_FAILED`].
    /// To resume after an interruption, set the value at that location back to `0` and call again.
    /// To remove the (small) overhead of checking for the host interruption, the interruption can be completely disabled by setting [`cudssDataParam_t::CUDSS_DATA_USER_HOST_INTERRUPT`] to `NULL`.
    ///
    /// Note: since most of device kernels inside cuDSS are launched asynchronously,
    /// execution on the GPU may continue some time after the host execution has been interrupted.
    ///
    /// Direction: in
    ///
    /// Memory: host
    ///
    /// Default value: `NULL` (user interruption disabled)
    ///
    /// Associated parameter type: pointer to a 4-byte integer (e.g. `int32_t` or `int`).
    ///
    /// Note: When setting the flag from another thread, the user must use an atomic store with release semantics so the library observes the write. A plain write to a non-atomic object from another thread is a data race and undefined behavior.
    ///
    /// Note: `std::atomic&lt;int>` (C++) or `_Atomic(int)*` (C) may be used when the object representation is a single 4-byte integer; the library does not guarantee correct behavior for all atomic types.
    ///
    /// Note: The host interruption feature is not supported when [MGMN mode](https://docs.nvidia.com/cuda/cudss/advanced_features.html#multi-gpu-multi-node-mgmn-mode) or [MG mode](https://docs.nvidia.com/cuda/cudss/advanced_features.html#multi-gpu-mg-mode) is used.
    /// Also, reordering algorithms [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_BTF_COLAMD`] and [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_COLAMD`] do not support the user interrupt currently.
    CUDSS_DATA_USER_HOST_INTERRUPT = 23,
    /// Actual number of iterative refinement steps performed
    ///
    /// Direction: out
    ///
    /// Memory: host
    ///
    /// Associated parameter type: `int`
    ///
    /// This parameter stores the actual number of iterative refinement iterations performed during the solve phase.
    ///
    /// The value can be less than [`cudssConfigParam_t::CUDSS_CONFIG_IR_N_STEPS`] if user-specified convergence tolerance is reached.
    /// The value is 0 if iterative refinement is disabled or if the initial solution already satisfies the tolerance.
    CUDSS_DATA_IR_N_STEPS = 24,
    /// A bitmask specifying the subset of problems to be factorized or solved in the uniform batch case. Each bit position corresponds to the index of a matrix in the uniform batch; setting a bit to `1` includes that matrix in the operation.
    ///
    /// Note: [`cudssDataParam_t::CUDSS_DATA_UBATCH_MASK`] can be used as an alternative to [`cudssConfigParam_t::CUDSS_CONFIG_UBATCH_INDEX`] to select multiple specific problems from the batch in a single call.
    ///
    /// Note: [`cudssDataParam_t::CUDSS_DATA_UBATCH_MASK`] can be set after [`cudssPhase_t::CUDSS_PHASE_FACTORIZATION`], in that case one could solve only a specific subset of systems.
    ///
    /// Direction: in
    ///
    /// Memory: host
    ///
    /// Associated parameter type: `int64_t`
    ///
    /// Default value: `-1` (all problems selected).
    ///
    /// Currently not supported when either [`cudssConfigParam_t::CUDSS_CONFIG_HYBRID_MEMORY_MODE`] (see [here](https://docs.nvidia.com/cuda/cudss/advanced_features.html#hybrid-host-device-memory-mode)) or [`cudssConfigParam_t::CUDSS_CONFIG_HYBRID_EXECUTE_MODE`] (see [here](https://docs.nvidia.com/cuda/cudss/advanced_features.html#hybrid-host-device-execute-mode))
    /// are enabled, or [MGMN mode](https://docs.nvidia.com/cuda/cudss/advanced_features.html#multi-gpu-multi-node-mgmn-mode) is used, or with [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_BTF_COLAMD`] and [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_COLAMD`] reordering algorithms.
    CUDSS_DATA_UBATCH_MASK = 25,
    /// Floating-point operation count for the factorization phase.
    ///
    /// Note: the value is available after the [`cudssPhase_t::CUDSS_PHASE_ANALYSIS`] step.
    ///
    /// Direction: out
    ///
    /// Memory: host
    ///
    /// Associated parameter type: `int64_t`.
    CUDSS_DATA_FLOPS = 26,
}
/// The enumerator specifies the solver phases to be performed in the main cuDSS routine [`cudssExecute`].
/// Phases can be combined with the binary OR operator (`|`) to specify multiple phases at once: `CUDSS_PHASE_FACTORIZATION | CUDSS_PHASE_SOLVE`.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudssPhase_t {
    /// Reordering.
    CUDSS_PHASE_REORDERING = 1,
    /// Symbolic factorization
    ///
    /// Note: it is not allowed to call symbolic factorization twice without calling the reordering phase in-between.
    CUDSS_PHASE_SYMBOLIC_FACTORIZATION = 2,
    /// Reordering and symbolic factorization combined.
    CUDSS_PHASE_ANALYSIS = 3,
    /// Numerical factorization.
    CUDSS_PHASE_FACTORIZATION = 4,
    /// Numerical re-factorization.
    ///
    /// Note: For now it is only used if the reordering algorithm is set to [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_BTF_COLAMD`] or [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_COLAMD`]. Otherwise, it is the same as FACTORIZATION phase.
    CUDSS_PHASE_REFACTORIZATION = 8,
    /// Applying reordering permutation to the right hand side before the forward solve (forward substitution).
    ///
    /// Note: solve sub-phases are not supported when [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_BTF_COLAMD`] or [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_COLAMD`] are used for reordering.
    CUDSS_PHASE_SOLVE_FWD_PERM = 16,
    /// Forward substitution sub-step of the solving phase.
    ///
    /// Note: this phase includes the local permutation due to the partial pivoting.
    /// To remove this effect, if undesired, one can use a combination of reordering and final permutation (see [`cudssDataParam_t::CUDSS_DATA_PERM_REORDER_ROW`] and [`cudssDataParam_t::CUDSS_DATA_PERM_ROW`]).
    ///
    /// Note: solve sub-phases are not supported when [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_BTF_COLAMD`] or [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_COLAMD`] are used for reordering.
    CUDSS_PHASE_SOLVE_FWD = 32,
    /// Diagonal solve sub-step of the solving phase (only needed for symmetric/Hermitian matrix types).
    ///
    /// Note: solve sub-phases are not supported when [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_BTF_COLAMD`] or [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_COLAMD`] are used for reordering.
    CUDSS_PHASE_SOLVE_DIAG = 64,
    /// Backward substitution sub-step of the solving phase.
    ///
    /// Note: this phase includes the local permutation due to the partial pivoting.
    /// To remove this effect, if undesired, one can use a combination of reordering and final permutation (see [`cudssDataParam_t::CUDSS_DATA_PERM_REORDER_ROW`] and [`cudssDataParam_t::CUDSS_DATA_PERM_ROW`]).
    ///
    /// Note: solve sub-phases are not supported when [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_BTF_COLAMD`] or [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_COLAMD`] are used for reordering.
    CUDSS_PHASE_SOLVE_BWD = 128,
    /// Applying inverse reordering permutation to the intermediate solution after the backward (backward substitution).
    /// If matching (and scaling) is enabled, this phase also includes applying the inverse matching permutation and inverse scaling (as the matching permutation and scalings were used to modify the matrix before the factorization).
    ///
    /// Note: solve sub-phases are not supported when [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_BTF_COLAMD`] or [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_COLAMD`] are used for reordering.
    CUDSS_PHASE_SOLVE_BWD_PERM = 256,
    /// Iterative refinement (see configuration settings [`cudssConfigParam_t::CUDSS_CONFIG_IR_N_STEPS`] and [`cudssConfigParam_t::CUDSS_CONFIG_IR_TOL`]).
    ///
    /// Note: solve sub-phases are not supported when [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_BTF_COLAMD`] or [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_COLAMD`] are used for reordering.
    CUDSS_PHASE_SOLVE_REFINEMENT = 512,
    /// Full solving phase, combining all of the above (forward permutation + forward solve + diagonal solve + backward permutation + backward solve) and (optional) iterative refinement.
    ///
    /// Note: combining the solve sub-phases should be preferred to calling sub-phases separately.
    ///
    /// Note: Changing the sparse matrix for this phase is allowed but with restrictions, see limitations for [`cudssExecute`].
    CUDSS_PHASE_SOLVE = 1008,
}
/// The enumerator specifies possible status values (on the host) which can be returned from calls to cuDSS routines.
///
/// Note: device side failures are returned via [`cudssDataParam_t::CUDSS_DATA_INFO`] from [`cudssDataParam_t`].
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudssStatus_t {
    /// The operation completed successfully.
    CUDSS_STATUS_SUCCESS = 0,
    /// One of the input operands was not properly initialized prior to the call to a cuDSS routine. This can usually be one of the opaque objects like [`cudssHandle_t`], [`cudssData_t`] or others.
    CUDSS_STATUS_NOT_INITIALIZED = 1,
    /// Resource allocation failed inside the cuDSS library. This is usually caused by a device memory allocation (cudaMalloc()) or by a host memory allocation failure.
    CUDSS_STATUS_ALLOC_FAILED = 2,
    /// An incorrect value or parameter was passed to the function (a negative vector size, or a NULL pointer for a must-have buffer, for example).
    CUDSS_STATUS_INVALID_VALUE = 3,
    /// An unsupported (but otherwise reasonable) parameter was passed to the function.
    CUDSS_STATUS_NOT_SUPPORTED = 4,
    /// The GPU program failed to execute. This is often caused by a launch failure of the kernel on the GPU, which can be caused by multiple reasons.
    CUDSS_STATUS_EXECUTION_FAILED = 5,
    /// An internal cuDSS operation failed.
    CUDSS_STATUS_INTERNAL_ERROR = 6,
    /// Iterative refinement failed to meet the requested convergence tolerance. In this case, the iterations were performed and the refined solution is still accessible.
    CUDSS_STATUS_IR_FAILED = 7,
}
/// The enumerator specifies available matrix types for sparse matrices.
/// Matrix type should be used to describe the properties of the underlying matrix storage.
/// Matrix type affects the decision about what type of factorization is computed by the solver.
/// E.g, when matrix type is one of the positive-definite types, checks for singular values on the diagonal is not done.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudssMatrixType_t {
    /// General matrix \[default\] `LDU` factorization will be computed with optional local or global pivoting.
    CUDSS_MTYPE_GENERAL = 0,
    /// Real symmetric matrix. `LDL^T` factorization will be computed with optional local pivoting.
    CUDSS_MTYPE_SYMMETRIC = 1,
    /// Complex Hermitian matrix. `LDL^H` factorization will be computed with optional local pivoting.
    CUDSS_MTYPE_HERMITIAN = 2,
    /// Symmetric positive-definite matrix Cholesky factorization will be computed with optional local pivoting
    ///
    /// Note: if the matrix passed with this matrix type appears to have zero minors (at least numerically), one can get the 1-based index of the first encountered non-positive minor by
    /// calling [`cudssDataGet`] with [`cudssDataParam_t::CUDSS_DATA_INFO`]. As this would be a device-side error, the call to [`cudssExecute`] may still return [`cudssStatus_t::CUDSS_STATUS_SUCCESS`].
    CUDSS_MTYPE_SPD = 3,
    /// Hermitian positive-definite matrix Complex Cholesky factorization will be computed with optional local pivoting
    ///
    /// Note: if the matrix passed with this matrix type appears to have zero minors (at least numerically), one can get the 1-based index of the first non-positive minor by
    /// calling [`cudssDataGet`] with [`cudssDataParam_t::CUDSS_DATA_INFO`]. As this would be a device-side error, the call to [`cudssExecute`] may still return [`cudssStatus_t::CUDSS_STATUS_SUCCESS`].
    CUDSS_MTYPE_HPD = 4,
}
/// The enumerator specifies available matrix view types for sparse matrices. Matrix view defines how the matrix is treated by
/// the main cuDSS routine [`cudssExecute`]. E.g., to provide only upper-triangle data for a symmetric matrix
/// one can use as [`cudssMatrixType_t::CUDSS_MTYPE_SYMMETRIC`] as matrix type combined with [`cudssMatrixViewType_t::CUDSS_MVIEW_UPPER`] as the matrix view.
/// If the accompanying matrix type is [`cudssMatrixType_t::CUDSS_MTYPE_GENERAL`], the matrix view is ignored.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudssMatrixViewType_t {
    /// Full matrix \[default\].
    CUDSS_MVIEW_FULL = 0,
    /// Lower-triangular matrix (including the diagonal). All values above the main diagonal will be ignored.
    CUDSS_MVIEW_LOWER = 1,
    /// Upper-triangular matrix (including the diagonal). All values below the main diagonal will be ignored.
    CUDSS_MVIEW_UPPER = 2,
}
/// The enumerator specifies indexing base (0 or 1) for sparse matrix indices (row start/end offsets and column indices).
/// Once set for a sparse matrix, [`cudssExecute`]
/// will use the indexing base from the input sparse matrix for all index-related data (e.g. output from
/// [`cudssDataGet`] called with [`cudssDataParam_t::CUDSS_DATA_PERM_REORDER_ROW`] or [`cudssDataParam_t::CUDSS_DATA_PERM_REORDER_COL`]).
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudssIndexBase_t {
    /// Zero-based indexing \[default\].
    CUDSS_BASE_ZERO = 0,
    /// One-based indexing.
    CUDSS_BASE_ONE = 1,
}
/// The enumerator specifies dense matrix layout.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudssLayout_t {
    /// Column-major layout \[default\].
    CUDSS_LAYOUT_COL_MAJOR = 0,
    /// Row-major layout. Currently not supported.
    CUDSS_LAYOUT_ROW_MAJOR = 1,
}
/// The enumerator specifies the reordering algorithm for the analysis phase.
///
/// Used with [`cudssConfigParam_t::CUDSS_CONFIG_REORDERING_ALG`].
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudssReorderingAlg_t {
    /// The default algorithm for reordering (equivalent to
    /// [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_NESTED_DISSECTION`]).
    CUDSS_REORDERING_ALG_DEFAULT = 0,
    /// Block triangular form (BTF) combined with COLAMD. Supports global pivoting.
    CUDSS_REORDERING_ALG_BTF_COLAMD = 1,
    /// COLAMD with trivial block structure. Supports global pivoting.
    CUDSS_REORDERING_ALG_COLAMD = 2,
    /// Approximate minimum degree (AMD) reordering.
    CUDSS_REORDERING_ALG_AMD = 3,
    /// Nested dissection algorithm based on METIS.
    CUDSS_REORDERING_ALG_NESTED_DISSECTION = 4,
    /// Uses natural (identity) order for the internal ordering when no user permutation is supplied.
    CUDSS_REORDERING_ALG_NONE = 5,
}
/// The enumerator specifies the factorization algorithm for the numerical factorization phase.
///
/// Used with [`cudssConfigParam_t::CUDSS_CONFIG_FACTORIZATION_ALG`].
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudssFactorizationAlg_t {
    /// The default factorization algorithm (equivalent to [`cudssFactorizationAlg_t::CUDSS_FACTORIZATION_ALG_GENERAL`], recommended).
    CUDSS_FACTORIZATION_ALG_DEFAULT = 0,
    /// Multiblock factorization algorithm. Supported only for real double data type (CUDA_R_64F). Suggested for use with extremely sparse matrices.
    CUDSS_FACTORIZATION_ALG_MULTIBLOCK = 1,
    /// General factorization algorithm.
    CUDSS_FACTORIZATION_ALG_GENERAL = 2,
}
/// The enumerator specifies the algorithm for pivot epsilon calculation when replacing small pivots during numerical factorization.
///
/// Used with [`cudssConfigParam_t::CUDSS_CONFIG_PIVOT_EPSILON_ALG`].
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudssPivotEpsilonAlg_t {
    /// The default pivot epsilon algorithm (same as [`cudssPivotEpsilonAlg_t::CUDSS_PIVOT_EPSILON_ALG_STATIC`], recommended).
    CUDSS_PIVOT_EPSILON_ALG_DEFAULT = 0,
    /// Pivots below the threshold are replaced by the appropriately signed epsilon scaled by the maximum element in the corresponding row and column of the original matrix. Not supported with [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_BTF_COLAMD`] or [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_COLAMD`].
    CUDSS_PIVOT_EPSILON_ALG_SCALED = 1,
    /// Pivots below the threshold are replaced by the appropriately signed epsilon (no scaling). Recommended for most cases.
    CUDSS_PIVOT_EPSILON_ALG_STATIC = 2,
}
/// The enumerator specifies the algorithm for the solve phase.
///
/// Used with [`cudssConfigParam_t::CUDSS_CONFIG_SOLVE_ALG`].
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudssSolveAlg_t {
    /// Let the library choose the solve algorithm (recommended).
    CUDSS_SOLVE_ALG_DEFAULT = 0,
    /// Triangular solve only: forward/back substitution using the existing L/U factors without modifying them.
    CUDSS_SOLVE_ALG_GENERAL = 1,
}
/// The enumerator specifies the matching (column permutation) algorithm.
///
/// Used with [`cudssConfigParam_t::CUDSS_CONFIG_MATCHING_ALG`]. Except for [`cudssMatchingAlg_t::CUDSS_MATCHING_ALG_NONE`] and [`cudssMatchingAlg_t::CUDSS_MATCHING_ALG_AUTO`], all options are based on the MC64 algorithm (HSL).
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudssMatchingAlg_t {
    /// Matching is disabled.
    CUDSS_MATCHING_ALG_NONE = 0,
    /// Column permutation to maximize the number of diagonal entries (values arbitrary). MC64 JOB=1. Does not use matrix values; not recommended unless justified.
    CUDSS_MATCHING_ALG_MAX_DIAG_COUNT = 1,
    /// Column permutation to maximize the smallest value on the diagonal. MC64 JOB=2.
    CUDSS_MATCHING_ALG_MAX_MIN_DIAG = 2,
    /// Alternate algorithm to maximize the smallest value on the diagonal. MC64 JOB=3. May differ in performance from [`cudssMatchingAlg_t::CUDSS_MATCHING_ALG_MAX_MIN_DIAG`].
    CUDSS_MATCHING_ALG_MAX_MIN_DIAG_ALT = 3,
    /// Column permutation to maximize the sum of diagonal entries. MC64 JOB=4.
    CUDSS_MATCHING_ALG_MAX_DIAG_SUM = 4,
    /// Column permutation to maximize the product of diagonal entries; also computes row/column scaling so that nonzero diagonal entries are 1 in absolute value and off-diagonal entries are ≤ 1. MC64 JOB=5. Most impactful for accuracy; requires matrix values during analysis.
    CUDSS_MATCHING_ALG_MAX_DIAG_PRODUCT = 5,
    /// Same as [`cudssMatchingAlg_t::CUDSS_MATCHING_ALG_MAX_DIAG_PRODUCT`] (the most robust option). Matching with scaling; requires matrix values during analysis.
    CUDSS_MATCHING_ALG_AUTO = 6,
}
/// The enumerator specifies the pivoting strategy for numerical factorization.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudssPivotType_t {
    /// Automatic pivoting strategy \[default\]. Automatically selects the appropriate
    /// pivot type based on the reordering algorithm and matrix type:
    ///
    /// * For [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_NESTED_DISSECTION`] and [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_AMD`]:
    ///
    ///   + Symmetric/Hermitian indefinite matrices: Uses [`cudssPivotType_t::CUDSS_PIVOT_DIAGONAL`]
    ///   + General matrices: Uses [`cudssPivotType_t::CUDSS_PIVOT_LOCAL_BLOCK`]
    /// * For [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_BTF_COLAMD`] and [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_COLAMD`]:
    ///   Uses [`cudssPivotType_t::CUDSS_PIVOT_GLOBAL_COL`].
    CUDSS_PIVOT_AUTO = 0,
    /// No pivoting. Used only with reordering algorithms
    /// [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_NESTED_DISSECTION`] and
    /// [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_AMD`] (MetisND and AMD).
    CUDSS_PIVOT_NONE = 1,
    /// Global column pivoting. Used only with reordering algorithms
    /// [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_BTF_COLAMD`] and [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_COLAMD`].
    /// Searches for pivot elements within entire columns of the sub-matrix.
    CUDSS_PIVOT_GLOBAL_COL = 2,
    /// Global row pivoting. Used only with reordering algorithms
    /// [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_BTF_COLAMD`] and [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_COLAMD`].
    /// Searches for pivot elements within entire rows of the sub-matrix.
    CUDSS_PIVOT_GLOBAL_ROW = 3,
    /// Diagonal pivoting. Used only with reordering algorithms
    /// [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_NESTED_DISSECTION`] and
    /// [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_AMD`] (MetisND and AMD).
    /// For symmetric/Hermitian indefinite matrices, searches for pivot elements
    /// only within the diagonal of the supernode.
    CUDSS_PIVOT_DIAGONAL = 4,
    /// Complete block pivoting. Used only with reordering algorithms
    /// [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_NESTED_DISSECTION`] and
    /// [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_AMD`] (MetisND and AMD).
    /// For general matrices, searches for pivot elements within the entire
    /// diagonal block of the supernode.
    CUDSS_PIVOT_LOCAL_BLOCK = 5,
    /// Bunch-Kaufman pivoting. Reserved for future use. Currently not supported.
    CUDSS_PIVOT_BUNCH_KAUFMAN = 6,
}
/// The enumerator specifies the underlying matrix formats inside a cuDSS matrix object.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudssMatrixFormat_t {
    /// Matrix is dense (applied to a single matrix and a batch equally).
    CUDSS_MFORMAT_DENSE = 1,
    /// Matrix is in CSR format (applies to a single matrix and a batch equally)
    ///
    /// Note: Only 3-array CSR is supported.
    CUDSS_MFORMAT_CSR = 2,
    /// Matrix object represents a batch of matrices.
    CUDSS_MFORMAT_BATCH = 4,
    /// > Matrix object represents a distributed matrix (used in [MGMN mode](https://docs.nvidia.com/cuda/cudss/advanced_features.html#multi-gpu-multi-node-mgmn-mode))
    ///
    /// Note: The format flags can be combined. E.g., creating a [`cudssMatrix_t`]
    /// via [`cudssMatrixCreateBatchCsr`] would set both
    /// [`cudssMatrixFormat_t::CUDSS_MFORMAT_CSR`] and [`cudssMatrixFormat_t::CUDSS_MFORMAT_BATCH`] flags. One can check for a mixture of flags via bit-wise operations, e.g.
    /// `CUDSS_MFORMAT_CSR | CUDSS_MFORMAT_BATCH` for a batch of CSR matrices.
    CUDSS_MFORMAT_DISTRIBUTED = 8,
}
/// This structure holds information about the user-provided, stream-ordered device memory pool (mempool).
///
/// The object can be created by setting the struct members described below.
///
/// Once created, a device memory handler can be set for the cuDSS library handle via [`cudssSetDeviceMemHandler`].
///
/// Once set for the cuDSS library handle, information about the set device memory handler can be retrieved via [`cudssGetDeviceMemHandler`].
///
/// `char name\[CUDSS_ALLOCATOR_NAME_LEN\]`
///
/// The name of the provided mempool (must not exceed 64 characters).
#[repr(C)]
#[derive(Debug, Copy, Clone, Hash, PartialOrd, Ord, PartialEq, Eq)]
pub struct cudssDeviceMemHandler_t {
    /// A pointer to the user-owned mempool/context object.
    pub ctx: *mut ::core::ffi::c_void,
    /// A function pointer to the user-provided routine for allocating device memory of size on `stream`.
    ///
    /// The allocated memory should be made accessible to the current device (or more precisely, to the current CUDA context bound to the library handle).
    ///
    /// This interface supports any stream-ordered memory allocator `ctx`. Upon success, the allocated memory can be immediately used on the given stream by any operations enqueued/ordered on the same stream after this call.
    ///
    /// It is the caller’s responsibility to ensure a proper stream order is established.
    ///
    /// The allocated memory should be at least 256-byte aligned.
    pub device_alloc: ::core::option::Option<
        unsafe extern "C" fn(
            ctx: *mut ::core::ffi::c_void,
            ptr: *mut *mut ::core::ffi::c_void,
            size: size_t,
            stream: cudaStream_t,
        ) -> ::core::ffi::c_int,
    >,
    /// A function pointer to the user-provided routine for deallocating device memory of `size` on `stream`.
    ///
    /// This interface supports any stream-ordered memory allocator. Upon success, any subsequent accesses (of the memory pointed to by the pointer `ptr`)
    /// ordered after this call are undefined behaviors.
    ///
    /// It is the caller’s responsibility to ensure a proper stream order is established.
    ///
    /// If the arguments `ctx` and `size` are not the same as those passed to `device_alloc` to allocate the memory pointed to by `ptr`, the behavior is undefined.
    ///
    /// The argument `stream` need not be identical to the one used for allocating `ptr`, as long as the stream order is correctly established. The behavior is undefined if this assumption is not held.
    pub device_free: ::core::option::Option<
        unsafe extern "C" fn(
            ctx: *mut ::core::ffi::c_void,
            ptr: *mut ::core::ffi::c_void,
            size: size_t,
            stream: cudaStream_t,
        ) -> ::core::ffi::c_int,
    >,
    pub name: [::core::ffi::c_char; 64usize],
}
impl Default for cudssDeviceMemHandler_t {
    fn default() -> Self {
        let mut s = ::core::mem::MaybeUninit::<Self>::uninit();
        unsafe {
            ::core::ptr::write_bytes(s.as_mut_ptr(), 0, 1);
            s.assume_init()
        }
    }
}
/// The structure represents an extended-precision (double-double) floating-point value corresponding to [`cudssDataType_t::CUDSS_R_64F_64F`].
///
/// The value is stored as the unevaluated sum of two `double` components `hi + lo` and must be 16-byte aligned.
#[repr(C)]
#[repr(align(16))]
#[derive(Debug, Default, Copy, Clone, PartialOrd, PartialEq)]
pub struct cudss_fp64mp2_t {
    /// High-order part of the extended-precision value.
    pub hi: f64,
    /// Low-order part of the extended-precision value.
    pub lo: f64,
}
/// The enumerator specifies reduction operation to be used when calling
///
/// communication layer APIs `cudssReduce()` or `cudssAllreduce()`.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudssOpType_t {
    /// Reduced elements are added together.
    CUDSS_SUM = 0,
    /// Maximum element is found among the reduced elements.
    CUDSS_MAX = 1,
    /// Minimum element is found among the reduced elements.
    CUDSS_MIN = 2,
}
/// This struct defines all communication primitives which need to be implemented
/// in (any) implementation of cuDSS communication layer, see for more details
/// [communication layer](https://docs.nvidia.com/cuda/cudss/advanced_features.html#communication-layer-library-in-cudss) and [MGMN mode](https://docs.nvidia.com/cuda/cudss/advanced_features.html#multi-gpu-multi-node-mgmn-mode).
///
/// **Dual-backend structure**: Each communication primitive must be implemented **twice** —
/// once for device (GPU) memory buffers (function pointer name ending in `Device`) and once
/// for host (CPU) memory buffers (function pointer name ending in `Host`). Both variants have
/// identical signatures. cuDSS selects the appropriate variant based on the memory location of the
/// buffers at call time. For example, rank query has two function pointers: `cudssCommRankDevice`
/// and `cudssCommRankHost`.
///
/// Note: the `comm` argument to all functions is interpreted by the implementation based on the
/// underlying communication backend. E.g., for OpenMPI it should be treated as an `MPI_Comm`.
///
/// Note: functions with a `stream` argument must be stream-ordered. For GPU-aware backends like
/// OpenMPI, this implies performing an explicit `cudaStreamSynchronize()` inside the implementation.
#[repr(C)]
#[derive(Debug, Default, Copy, Clone, Hash, PartialOrd, Ord, PartialEq, Eq)]
pub struct cudssDistributedInterface_t {
    /// Function pointers to a routine which returns the rank of the process in a communicator.
    pub cudssCommRankDevice: ::core::option::Option<
        unsafe extern "C" fn(
            comm: *mut ::core::ffi::c_void,
            rank: *mut ::core::ffi::c_int,
        ) -> ::core::ffi::c_int,
    >,
    /// Function pointers to a routine which returns the number of processes in a communicator.
    pub cudssCommSizeDevice: ::core::option::Option<
        unsafe extern "C" fn(
            comm: *mut ::core::ffi::c_void,
            size: *mut ::core::ffi::c_int,
        ) -> ::core::ffi::c_int,
    >,
    /// Function pointers to a routine which performs a blocking send.
    pub cudssSendDevice: ::core::option::Option<
        unsafe extern "C" fn(
            buffer: *const ::core::ffi::c_void,
            count: ::core::ffi::c_int,
            datatype: cudssDataType_t,
            dest: ::core::ffi::c_int,
            tag: ::core::ffi::c_int,
            comm: *mut ::core::ffi::c_void,
            stream: cudaStream_t,
        ) -> ::core::ffi::c_int,
    >,
    /// Function pointers to a routine which performs a blocking receive for a message.
    pub cudssRecvDevice: ::core::option::Option<
        unsafe extern "C" fn(
            buffer: *mut ::core::ffi::c_void,
            count: ::core::ffi::c_int,
            datatype: cudssDataType_t,
            root: ::core::ffi::c_int,
            tag: ::core::ffi::c_int,
            comm: *mut ::core::ffi::c_void,
            stream: cudaStream_t,
        ) -> ::core::ffi::c_int,
    >,
    /// Function pointers to a routine which performs a broadcast from the `root` process
    ///
    /// to all other processes of the communicator.
    pub cudssBcastDevice: ::core::option::Option<
        unsafe extern "C" fn(
            buffer: *mut ::core::ffi::c_void,
            count: ::core::ffi::c_int,
            datatype: cudssDataType_t,
            root: ::core::ffi::c_int,
            comm: *mut ::core::ffi::c_void,
            stream: cudaStream_t,
        ) -> ::core::ffi::c_int,
    >,
    /// Function pointers to a routine which performs a reduction to a single value on the root process.
    pub cudssReduceDevice: ::core::option::Option<
        unsafe extern "C" fn(
            sendbuf: *const ::core::ffi::c_void,
            recvbuf: *mut ::core::ffi::c_void,
            count: ::core::ffi::c_int,
            datatype: cudssDataType_t,
            op: cudssOpType_t,
            root: ::core::ffi::c_int,
            comm: *mut ::core::ffi::c_void,
            stream: cudaStream_t,
        ) -> ::core::ffi::c_int,
    >,
    /// Function pointers to a routine which performs a reduction and distributes the result back to all processes.
    pub cudssAllreduceDevice: ::core::option::Option<
        unsafe extern "C" fn(
            sendbuf: *const ::core::ffi::c_void,
            recvbuf: *mut ::core::ffi::c_void,
            count: ::core::ffi::c_int,
            datatype: cudssDataType_t,
            op: cudssOpType_t,
            comm: *mut ::core::ffi::c_void,
            stream: cudaStream_t,
        ) -> ::core::ffi::c_int,
    >,
    /// Function pointers to a routine which scatters a buffer in parts to all processes in a communicator.
    pub cudssScattervDevice: ::core::option::Option<
        unsafe extern "C" fn(
            sendbuf: *const ::core::ffi::c_void,
            sendcounts: *const ::core::ffi::c_int,
            displs: *const ::core::ffi::c_int,
            sendtype: cudssDataType_t,
            recvbuf: *mut ::core::ffi::c_void,
            recvcount: ::core::ffi::c_int,
            recvtype: cudssDataType_t,
            root: ::core::ffi::c_int,
            comm: *mut ::core::ffi::c_void,
            stream: cudaStream_t,
        ) -> ::core::ffi::c_int,
    >,
    /// Function pointers to a routine which creates new communicators based on colors and keys.
    pub cudssCommSplitDevice: ::core::option::Option<
        unsafe extern "C" fn(
            comm: *const ::core::ffi::c_void,
            color: ::core::ffi::c_int,
            key: ::core::ffi::c_int,
            newcomm: *mut ::core::ffi::c_void,
        ) -> ::core::ffi::c_int,
    >,
    /// Function pointers to a routine which deallocates resources of a communicator.
    pub cudssCommFreeDevice: ::core::option::Option<
        unsafe extern "C" fn(comm: *mut ::core::ffi::c_void) -> ::core::ffi::c_int,
    >,
    pub cudssCommRankHost: ::core::option::Option<
        unsafe extern "C" fn(
            comm: *mut ::core::ffi::c_void,
            rank: *mut ::core::ffi::c_int,
        ) -> ::core::ffi::c_int,
    >,
    pub cudssCommSizeHost: ::core::option::Option<
        unsafe extern "C" fn(
            comm: *mut ::core::ffi::c_void,
            size: *mut ::core::ffi::c_int,
        ) -> ::core::ffi::c_int,
    >,
    pub cudssSendHost: ::core::option::Option<
        unsafe extern "C" fn(
            buffer: *const ::core::ffi::c_void,
            count: ::core::ffi::c_int,
            datatype: cudssDataType_t,
            dest: ::core::ffi::c_int,
            tag: ::core::ffi::c_int,
            comm: *mut ::core::ffi::c_void,
            stream: cudaStream_t,
        ) -> ::core::ffi::c_int,
    >,
    pub cudssRecvHost: ::core::option::Option<
        unsafe extern "C" fn(
            buffer: *mut ::core::ffi::c_void,
            count: ::core::ffi::c_int,
            datatype: cudssDataType_t,
            root: ::core::ffi::c_int,
            tag: ::core::ffi::c_int,
            comm: *mut ::core::ffi::c_void,
            stream: cudaStream_t,
        ) -> ::core::ffi::c_int,
    >,
    pub cudssBcastHost: ::core::option::Option<
        unsafe extern "C" fn(
            buffer: *mut ::core::ffi::c_void,
            count: ::core::ffi::c_int,
            datatype: cudssDataType_t,
            root: ::core::ffi::c_int,
            comm: *mut ::core::ffi::c_void,
            stream: cudaStream_t,
        ) -> ::core::ffi::c_int,
    >,
    pub cudssReduceHost: ::core::option::Option<
        unsafe extern "C" fn(
            sendbuf: *const ::core::ffi::c_void,
            recvbuf: *mut ::core::ffi::c_void,
            count: ::core::ffi::c_int,
            datatype: cudssDataType_t,
            op: cudssOpType_t,
            root: ::core::ffi::c_int,
            comm: *mut ::core::ffi::c_void,
            stream: cudaStream_t,
        ) -> ::core::ffi::c_int,
    >,
    pub cudssAllreduceHost: ::core::option::Option<
        unsafe extern "C" fn(
            sendbuf: *const ::core::ffi::c_void,
            recvbuf: *mut ::core::ffi::c_void,
            count: ::core::ffi::c_int,
            datatype: cudssDataType_t,
            op: cudssOpType_t,
            comm: *mut ::core::ffi::c_void,
            stream: cudaStream_t,
        ) -> ::core::ffi::c_int,
    >,
    pub cudssScattervHost: ::core::option::Option<
        unsafe extern "C" fn(
            sendbuf: *const ::core::ffi::c_void,
            sendcounts: *const ::core::ffi::c_int,
            displs: *const ::core::ffi::c_int,
            sendtype: cudssDataType_t,
            recvbuf: *mut ::core::ffi::c_void,
            recvcount: ::core::ffi::c_int,
            recvtype: cudssDataType_t,
            root: ::core::ffi::c_int,
            comm: *mut ::core::ffi::c_void,
            stream: cudaStream_t,
        ) -> ::core::ffi::c_int,
    >,
    pub cudssCommSplitHost: ::core::option::Option<
        unsafe extern "C" fn(
            comm: *const ::core::ffi::c_void,
            color: ::core::ffi::c_int,
            key: ::core::ffi::c_int,
            newcomm: *mut ::core::ffi::c_void,
        ) -> ::core::ffi::c_int,
    >,
    pub cudssCommFreeHost: ::core::option::Option<
        unsafe extern "C" fn(comm: *mut ::core::ffi::c_void) -> ::core::ffi::c_int,
    >,
    /// A function pointer to a routine which returns a versioning property of the communication layer library.
    ///
    /// Used by cuDSS to verify compatibility of a loaded communication layer.
    pub cudssDistributedGetProperty: ::core::option::Option<
        unsafe extern "C" fn(
            propertyType: libraryPropertyType,
            value: *mut ::core::ffi::c_int,
        ) -> ::core::ffi::c_int,
    >,
}
pub type cudss_thr_func_t = ::core::option::Option<
    unsafe extern "C" fn(task: ::core::ffi::c_int, ctx: *mut ::core::ffi::c_void),
>;
/// This struct defines all threading primitives which need to be implemented
///
/// in (any) implementation of cuDSS threading layer, see for more details
///
/// [threading layer](https://docs.nvidia.com/cuda/cudss/advanced_features.html#threading-layer-library-in-cudss) and [MT mode](https://docs.nvidia.com/cuda/cudss/advanced_features.html#multi-threaded-mt-mode).
#[repr(C)]
#[derive(Debug, Default, Copy, Clone, Hash, PartialOrd, Ord, PartialEq, Eq)]
pub struct cudssThreadingInterface_t {
    /// A function pointer to a routine which returns the maximum number of threads on the CPU
    /// that can be used by cuDSS for parallel execution.
    pub cudssGetMaxThreads: ::core::option::Option<
        unsafe extern "C" fn() -> ::core::ffi::c_int,
    >,
    /// A function pointer to a routine which opens a `parallel for` section with the requested number of
    /// threads and call cudss_thr_func_t (see `cudss_threading_interface.h` for details) `f` in
    /// the for loop with ntasks number of iterations.
    pub cudssParallelFor: ::core::option::Option<
        unsafe extern "C" fn(
            nthr_requested: ::core::ffi::c_int,
            ntasks: ::core::ffi::c_int,
            ctx: *mut ::core::ffi::c_void,
            f: cudss_thr_func_t,
        ),
    >,
    /// A function pointer to a routine which returns a versioning property of the threading layer library.
    /// Used by cuDSS to verify compatibility of a loaded threading layer.
    pub cudssThreadingGetProperty: ::core::option::Option<
        unsafe extern "C" fn(
            propertyType: libraryPropertyType,
            value: *mut ::core::ffi::c_int,
        ) -> ::core::ffi::c_int,
    >,
}
unsafe extern "C" {
    /// The function sets a parameter ([`cudssConfigParam_t`]) to the specified value passed by the pointer.
    ///
    /// # Parameters
    ///
    /// - `config`: \[host\] cuDSS config object.
    /// - `param`: \[host\] Parameter to be set.
    /// - `value`: \[host\] A pointer to the value to be set.
    /// - `sizeInBytes`: \[host\] Number of bytes to be read from the pointer.
    pub fn cudssConfigSet(
        config: cudssConfig_t,
        param: cudssConfigParam_t,
        value: *const ::core::ffi::c_void,
        sizeInBytes: size_t,
    ) -> cudssStatus_t;
}
unsafe extern "C" {
    /// The function retrieves value of a parameter ([`cudssConfigParam_t`]) and saves it to the specified memory location.
    ///
    /// # Parameters
    ///
    /// - `config`: \[host\] cuDSS config object.
    /// - `param`: \[host\] Parameter to be retrieved from the config.
    /// - `value`: \[host\] A pointer to the output memory.
    /// - `sizeInBytes`: \[host\] Number of bytes to be written (for verification).
    /// - `sizeWritten`: \[host\] Valid only when the return value is [`cudssStatus_t::CUDSS_STATUS_SUCCESS`]. If `sizeInBytes` is non-zero, then `sizeWritten` is the number of bytes actually written; if `sizeInBytes` is zero: `sizeWritten` is the number of bytes needed to write full contents.
    pub fn cudssConfigGet(
        config: cudssConfig_t,
        param: cudssConfigParam_t,
        value: *mut ::core::ffi::c_void,
        sizeInBytes: size_t,
        sizeWritten: *mut size_t,
    ) -> cudssStatus_t;
}
unsafe extern "C" {
    /// The function sets a parameter ([`cudssDataParam_t`]) to the specified value passed by the pointer.
    ///
    /// # Parameters
    ///
    /// - `handle`: \[host\] cuDSS library handle.
    /// - `data`: \[host\] cuDSS data object.
    /// - `param`: \[host\] Parameter to be set.
    /// - `value`: \[host\] A pointer to the value to be set.
    /// - `sizeInBytes`: \[host\] Number of bytes to be read from the pointer.
    pub fn cudssDataSet(
        handle: cudssHandle_t,
        data: cudssData_t,
        param: cudssDataParam_t,
        value: *const ::core::ffi::c_void,
        sizeInBytes: size_t,
    ) -> cudssStatus_t;
}
unsafe extern "C" {
    /// The function retrieves value of a parameter ([`cudssDataParam_t`]) and saves it to the specified memory location.
    ///
    /// The output memory buffer can be either on device or on host, a memory copy will be done if necessary.
    ///
    /// # Parameters
    ///
    /// - `handle`: \[host\] cuDSS library handle.
    /// - `data`: \[host\] cuDSS data object.
    /// - `param`: \[host\] Parameter to be retrieved from the data object.
    /// - `value`: \[host/device\] A pointer to the output memory.
    /// - `sizeInBytes`: \[host\] Number of bytes to be written (for verification).
    /// - `sizeWritten`: \[host\] Valid only when the return value is [`cudssStatus_t::CUDSS_STATUS_SUCCESS`]. If `sizeInBytes` is non-zero, then `sizeWritten` is the number of bytes actually written; if `sizeInBytes` is zero: `sizeWritten` is the number of bytes needed to write full contents.
    pub fn cudssDataGet(
        handle: cudssHandle_t,
        data: cudssData_t,
        param: cudssDataParam_t,
        value: *mut ::core::ffi::c_void,
        sizeInBytes: size_t,
        sizeWritten: *mut size_t,
    ) -> cudssStatus_t;
}
unsafe extern "C" {
    /// The function executes a phase of the solution process. Prior to calling [`cudssExecute`],
    /// all objects passed as parameters must already be created and properly initialized.
    ///
    /// The simplest possible solution process consists of three main phases,
    /// analysis, factorization, and solve, following one another. During the analysis phase, reordering and symbolic factorization
    /// (preparing the internal data structures) are done.
    /// During the factorization phase, numerical factorization is performed and during the solve phase, the factorization is
    /// used to find the solution to the linear system.
    ///
    /// The phases must always happen in the following order:
    /// [`cudssPhase_t::CUDSS_PHASE_REORDERING`] ->
    /// [`cudssPhase_t::CUDSS_PHASE_SYMBOLIC_FACTORIZATION`] ->
    /// [`cudssPhase_t::CUDSS_PHASE_FACTORIZATION`] -> (optional)
    /// [`cudssPhase_t::CUDSS_PHASE_REFACTORIZATION`]
    /// -> [`cudssPhase_t::CUDSS_PHASE_SOLVE`].
    /// The optional refactorization is usually skipped before the first solve.
    /// Re-using the analysis results is supported. Users can change matrix values and only need to run the (re-)factorization and solve phases.
    ///
    /// Note: Combining phases is supported as long as the order is followed. As an example, combining
    /// [`cudssPhase_t::CUDSS_PHASE_REORDERING`] | [`cudssPhase_t::CUDSS_PHASE_SOLVE`] will result in an error, while [`cudssPhase_t::CUDSS_PHASE_ANALYSIS`] | [`cudssPhase_t::CUDSS_PHASE_FACTORIZATION`] is
    /// allowed. Please review [`cudssPhase_t`] for the full list of phase parameters.
    ///
    /// > During the execution, the solver configuration properties are read from the `config` of type [`cudssConfig_t`].
    /// > The internal data structures necessary to keep all data required for solving the system (incl. the factors) are kept as a part of
    /// > `data` object of type [`cudssData_t`]. Users can change the configuration settings or provide additional data
    /// > parameters (e.g. a user permutation) or query extra information (like memory estimates or number of pivots) before/after the phases of the
    /// > solution process via [`cudssConfigSet`], or
    /// > [`cudssDataGet`], respectively.
    ///
    /// The data buffers in the matrix objects for the input matrix, solution and right-hand side matrices must hold device-visible data,
    /// unless the [hybrid host/device execution mode](https://docs.nvidia.com/cuda/cudss/advanced_features.html#hybrid-host-device-execute-mode) is enabled.
    /// For the hybrid execute mode, matrix, solution or right-hand side can be passed with host memory pointers.
    ///
    /// Note: The function has the following limitations on the [`cudssMatrix_t`] objects which can be used as call arguments,
    /// in addition to the limitations of the corresponding matrix creation routines (e.g., [`cudssMatrixCreateCsr`]):
    ///
    /// * The system matrix must be sparse (currently, only 3-array CSR format is supported).
    /// * Right-hand side and solution matrices must be dense, with column-major layout.
    /// * The input sparse matrix (in the batch case, each matrix in the batch) must have data consistent with its description
    ///   (incl. offsets and indices, indexing base, matrix type)
    /// * The input sparse matrix (in the batch case, each matrix in the batch) may have unsorted column indices but must not have repeating entries.
    /// * The input sparse matrix in CSR format must have its row offsets array start with the indexing base (0 or 1).
    /// * The input sparse matrix may change for the SOLVE phase (except when hybrid execution mode is used) but for distributed matrices, the range of rows (row distribution)
    ///   must remain the same.
    /// * The input sparse matrix, right hand side and solution must have the same datatypes for values and indices (if applicable).
    /// * For the non-uniform input batches, batches with varying shapes are supported. E.g., `nrows`, `ncols`, `nnz` can be different for each batch instance.
    ///   However, the aggregate number of rows and non-zeroes must not exceed the `INT_MAX` limit.
    /// * In MGMN mode all processes must have valid global `nrows`, `ncols`, `nnz` data for sparse matrices and
    ///   valid global `nrows`, `ncols` and local `ld` for the dense matrices. In the batch case, all processes must additionally have a valid `batchCount`.
    /// * If [`cudssMatrixSetDistributionRow1d`] is not used then in MGMN mode full matrix data
    ///   (for the system’s matrix, solution and right-hand side) must be present on the root process, i.e. process with
    ///   `rank` = 0 in the provided communicator. The other processes may have the data pointers set to `NULL`.
    ///
    ///   Otherwise the system’s matrix, solution and right-hand side must be distributed according to the corresponding call to [`cudssMatrixSetDistributionRow1d`].
    ///
    /// Note: using the [`cudssMatrixViewType_t`] parameter when creating the sparse input matrix (or a batch of those), one can pass only a
    /// triangular portion of the matrix without the need to explicitly change the underlying matrix data. E.g., if the `mview` is set to
    /// [`cudssMatrixViewType_t::CUDSS_MVIEW_UPPER`], then during the analysis phase, [`cudssExecute`] will ignore all the indices in the lower part of the matrix,
    /// even if the underlying matrix storage represents a full matrix.
    ///
    /// # Parameters
    ///
    /// - `handle`: \[host\] cuDSS library handle.
    /// - `phase`: \[host\] Execution phase(s).
    /// - `solution`: \[host\] Solution matrix.
    /// - `rhs`: \[host\] Right-hand side matrix.
    pub fn cudssExecute(
        handle: cudssHandle_t,
        phase: ::core::ffi::c_int,
        solverConfig: cudssConfig_t,
        solverData: cudssData_t,
        inputMatrix: cudssMatrix_t,
        solution: cudssMatrix_t,
        rhs: cudssMatrix_t,
    ) -> cudssStatus_t;
}
unsafe extern "C" {
    /// The function sets the stream to be used by the cuDSS library to execute its routines.
    ///
    /// # Parameters
    ///
    /// - `handle`: \[host\] cuDSS library handle.
    /// - `stream`: \[host\] The stream used by the library.
    pub fn cudssSetStream(handle: cudssHandle_t, stream: cudaStream_t) -> cudssStatus_t;
}
unsafe extern "C" {
    /// The function sets per-device streams to be used by the cuDSS library in multi-GPU mode.
    /// This function should be used when the library handle was created with [`cudssCreateMg`].
    ///
    /// Each stream in the array corresponds to a device specified in [`cudssCreateMg`].
    /// Specifically, `streams\[i\]` will be used for operations on `device_indices\[i\]`.
    ///
    /// The `stream_count` parameter must match the `device_count` used in [`cudssCreateMg`].
    ///
    /// **Note:** User is responsible for creating and destroying these streams. The streams must remain valid
    /// for the lifetime of the cuDSS handle. cuDSS will not destroy user-provided streams.
    ///
    /// **Note:** This function is optional. If not called, cuDSS will create and manage its own internal
    /// per-device streams for multi-GPU operations.
    ///
    /// # Parameters
    ///
    /// - `handle`: \[host\] cuDSS library handle created with [`cudssCreateMg`].
    /// - `streams`: \[host\] Array of CUDA streams, one per device.
    /// - `stream_count`: \[host\] Number of streams in the array (must match device_count from [`cudssCreateMg`]).
    pub fn cudssSetMgStreams(
        handle: cudssHandle_t,
        streams: *const cudaStream_t,
        stream_count: ::core::ffi::c_int,
    ) -> cudssStatus_t;
}
unsafe extern "C" {
    /// The function sets the communication layer to be used in [MGMN mode](https://docs.nvidia.com/cuda/cudss/advanced_features.html#multi-gpu-multi-node-mgmn-mode)
    ///
    /// of cuDSS. The set communication layer will be used for all MGMN mode operations where the
    ///
    /// modified library handle is involved.
    ///
    /// The communication layer must provide communication backends for both device and host memory buffers.
    ///
    /// After setting the communication layer, users should set the device communicator via [`cudssDataSet`]
    ///
    /// with [`cudssDataParam_t::CUDSS_DATA_COMM_DEVICE`] and the host communicator with [`cudssDataParam_t::CUDSS_DATA_COMM_HOST`].
    ///
    /// For more details of when and how this function should be used, see
    ///
    /// [MGMN mode](https://docs.nvidia.com/cuda/cudss/advanced_features.html#multi-gpu-multi-node-mgmn-mode).
    ///
    /// # Parameters
    ///
    /// - `handle`: \[host\] cuDSS library handle.
    /// - `commLibFileName`: \[host\] Library name or path (resolved by the platform dynamic loader, e.g. `dlopen` / `LoadLibrary`). If NULL, the name is read from [CUDSS_COMM_LIB](https://docs.nvidia.com/cuda/cudss/advanced_features.html#multi-gpu-multi-node-mgmn-mode).
    pub fn cudssSetCommLayer(
        handle: cudssHandle_t,
        commLibFileName: *const ::core::ffi::c_char,
    ) -> cudssStatus_t;
}
unsafe extern "C" {
    /// The function sets the threading layer to be used in [MT mode](https://docs.nvidia.com/cuda/cudss/advanced_features.html#multi-threaded-mt-mode)
    /// of cuDSS. The set threading layer will be used for all MT mode operations where the
    /// modified library handle is involved.
    /// For more details of when and how this function should be used, see [MT mode](https://docs.nvidia.com/cuda/cudss/advanced_features.html#multi-threaded-mt-mode).
    ///
    /// # Parameters
    ///
    /// - `handle`: \[host\] cuDSS library handle.
    /// - `thrLibFileName`: \[host\] Library name or path (resolved by the platform dynamic loader, e.g. `dlopen` / `LoadLibrary`). If NULL, the name is read from [CUDSS_THREADING_LIB](https://docs.nvidia.com/cuda/cudss/advanced_features.html#multi-threaded-mt-mode).
    pub fn cudssSetThreadingLayer(
        handle: cudssHandle_t,
        thrLibFileName: *const ::core::ffi::c_char,
    ) -> cudssStatus_t;
}
unsafe extern "C" {
    /// The function initializes the cuDSS config object ([`cudssConfig_t`]) which holds the settings of the solver related
    ///
    /// to solving a specific linear system. It allocates light resources on the host.
    ///
    /// To release the allocated memory, [`cudssConfigDestroy`] must be called.
    pub fn cudssConfigCreate(solverConfig: *mut cudssConfig_t) -> cudssStatus_t;
}
unsafe extern "C" {
    /// The function releases the host resources used by the cuDSS config object. Using the config object after this function call can lead to undefined behavior.
    pub fn cudssConfigDestroy(solverConfig: cudssConfig_t) -> cudssStatus_t;
}
unsafe extern "C" {
    /// The function initializes the cuDSS data object ([`cudssData_t`]) which holds the internal data (e.g., LU factors arrays)
    /// as well as pointers to user-provided data related to solving a specific linear system.
    /// To release the allocated memory, [`cudssDataDestroy`] must be called.
    ///
    /// # Parameters
    ///
    /// - `handle`: \[host\] cuDSS library handle.
    pub fn cudssDataCreate(
        handle: cudssHandle_t,
        solverData: *mut cudssData_t,
    ) -> cudssStatus_t;
}
unsafe extern "C" {
    /// The function releases the hardware resources used by the cuDSS data object. Using the data object after this function call can lead to undefined behavior.
    ///
    /// # Parameters
    ///
    /// - `handle`: \[host\] cuDSS library handle.
    pub fn cudssDataDestroy(
        handle: cudssHandle_t,
        solverData: cudssData_t,
    ) -> cudssStatus_t;
}
unsafe extern "C" {
    /// The function initializes the cuDSS library handle ([`cudssHandle_t`]) which holds the cuDSS library context.
    /// It allocates light hardware resources on the host, and must be called prior to making any other cuDSS library calls.
    /// Calling any cuDSS function which uses [`cudssHandle_t`] without a previous call of [`cudssCreate`] will return an error.
    /// The cuDSS library context is tied to the current CUDA device. To use the library on multiple devices, one cuDSS handle should be created for each device.
    ///
    /// # Parameters
    ///
    /// - `handle`: \[host\] cuDSS library handle.
    pub fn cudssCreate(handle: *mut cudssHandle_t) -> cudssStatus_t;
}
unsafe extern "C" {
    /// The function initializes the cuDSS library handle ([`cudssHandle_t`]) which holds the cuDSS library context for multiple devices.
    /// Calling any cuDSS function which uses [`cudssHandle_t`] without a previous call of [`cudssCreateMg`] will return an error.
    /// The cuDSS library context is tied to the CUDA devices defined by `device_count` number and `device_indices` array.
    /// If `device_indices` is `NULL`, cuDSS will take devices from `0` to `device_count - 1`.
    /// The calling device index must be equal to the first device number from `device_indices` or `0` (if `device_indices` is `NULL`)
    ///
    /// Note: Same `device_count` and `device_indices` must be passed to [`cudssConfig_t`] object
    /// by calling [`cudssConfigSet`] with [`cudssConfigParam_t::CUDSS_CONFIG_DEVICE_COUNT`] and [`cudssConfigParam_t::CUDSS_CONFIG_DEVICE_INDICES`].
    ///
    /// Limitations: Some of the features are not supported, see [MG mode](https://docs.nvidia.com/cuda/cudss/advanced_features.html#multi-gpu-mg-mode) for details.
    ///
    /// # Parameters
    ///
    /// - `device_count`: \[host\] Number of devices.
    /// - `device_indices`: \[host\] Integer array of size `device_count` which stores device indices. If set to `NULL`, device indices from `0` to `device_count - 1` are used.
    pub fn cudssCreateMg(
        handle_pt: *mut cudssHandle_t,
        device_count: ::core::ffi::c_int,
        device_indices: *const ::core::ffi::c_int,
    ) -> cudssStatus_t;
}
unsafe extern "C" {
    /// The function releases hardware resources used by the cuDSS library.
    /// This function is the last call with a particular handle to the cuDSS library.
    /// Calling any cuDSS function which uses [`cudssHandle_t`] after [`cudssDestroy`] will return an error.
    ///
    /// # Parameters
    ///
    /// - `handle`: \[host\] cuDSS library handle.
    pub fn cudssDestroy(handle: cudssHandle_t) -> cudssStatus_t;
}
unsafe extern "C" {
    /// The function returns the value of the requested property. Refer to [`libraryPropertyType`] for supported types.
    ///
    /// The enum [`libraryPropertyType`] is defined in `library_types.h` (standard CUDA header file).
    /// The supported subset of values for cuDSS is.
    ///
    /// # Parameters
    ///
    /// - `propertyType`: \[host\] Requested property.
    /// - `value`: \[host\] Value of the requested property.
    pub fn cudssGetProperty(
        propertyType: libraryPropertyType,
        value: *mut ::core::ffi::c_int,
    ) -> cudssStatus_t;
}
unsafe extern "C" {
    /// The function creates a matrix object wrapped around dense matrix data.
    /// The provided data buffer for the matrix values must hold device-visible data.
    ///
    /// Note: In MGMN mode all processes must have valid `nrows`, `ncols` and `ld`.
    ///
    /// See more limitations for using [`cudssMatrix_t`] objects in the documentation for the main routine, [`cudssExecute`].
    ///
    /// # Parameters
    ///
    /// - `matrix`: \[host\] Created matrix object.
    /// - `nrows`: \[host\] Number of rows.
    /// - `ncols`: \[host\] Number of columns.
    /// - `ld`: \[host\] Leading dimension.
    /// - `values`: \[device/host\] Values of the dense matrix.
    /// - `valueType`: \[host\] Data type of the matrix.
    /// - `layout`: \[host\] Memory layout.
    pub fn cudssMatrixCreateDn(
        matrix: *mut cudssMatrix_t,
        nrows: i64,
        ncols: i64,
        ld: i64,
        values: *const ::core::ffi::c_void,
        valueType: cudssDataType_t,
        layout: cudssLayout_t,
    ) -> cudssStatus_t;
}
unsafe extern "C" {
    /// The function creates a matrix object wrapped around sparse matrix data.
    /// The provided data buffers for `rowStart`, `rowEnd`, `colIndices` and `values` can hold either device or host memory pointers.
    /// Passing host memory pointers avoids redundant device-to-host data movements when the data resides on the host.
    ///
    /// Note: host memory input for the input matrix is not supported when `batchCount` > 1, when uniform batch options are enabled,
    /// or when [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_BTF_COLAMD`] or [`cudssReorderingAlg_t::CUDSS_REORDERING_ALG_COLAMD`] is used for reordering.
    ///
    /// Note: creating a cudssMatrix_t with CSR format does not perform any data consistency checks and thus currently it is
    /// the caller’s responsibility to have data description parameters matching the data.
    ///
    /// Note: In MGMN mode all processes must have valid `nrows`, `ncols`, `nnz` data (in case of distributed matrices,
    /// these should correspond to the global matrix)
    ///
    /// See more limitations for using [`cudssMatrix_t`] objects in the documentation for the main routine, [`cudssExecute`].
    ///
    /// # Parameters
    ///
    /// - `matrix`: \[host\] Created matrix object.
    /// - `nrows`: \[host\] Number of rows.
    /// - `ncols`: \[host\] Number of columns.
    /// - `nnz`: \[host\] Number of non-zeroes.
    /// - `rowStart`: \[device/host\] Row start offsets.
    /// - `rowEnd`: \[device/host\] Row end offsets.
    /// - `colIndices`: \[device/host\] Column indices of the matrix.
    /// - `values`: \[device/host\] Values of the matrix.
    /// - `offsetType`: \[host\] Offset type (`rowStart` and `rowEnd`) of the matrix.
    /// - `indexType`: \[host\] Index type of the matrix.
    /// - `valueType`: \[host\] Data type of the matrix.
    /// - `mtype`: \[host\] Matrix type of the matrix.
    /// - `mview`: \[host\] Matrix view of the matrix.
    /// - `indexBase`: \[host\] Indexing base.
    pub fn cudssMatrixCreateCsr(
        matrix: *mut cudssMatrix_t,
        nrows: i64,
        ncols: i64,
        nnz: i64,
        rowStart: *const ::core::ffi::c_void,
        rowEnd: *const ::core::ffi::c_void,
        colIndices: *const ::core::ffi::c_void,
        values: *const ::core::ffi::c_void,
        offsetType: cudssDataType_t,
        indexType: cudssDataType_t,
        valueType: cudssDataType_t,
        mtype: cudssMatrixType_t,
        mview: cudssMatrixViewType_t,
        indexBase: cudssIndexBase_t,
    ) -> cudssStatus_t;
}
unsafe extern "C" {
    /// The function creates a matrix object wrapped around a batch of dense matrices.
    /// The provided data buffer for the matrix values must contain device-visible pointers to device-visible data.
    ///
    /// Note: cuDSS supports non-uniform batches with varying shapes, e.g `nrows`, `ncols`, `ld` can be different for each batch instance.
    ///
    /// Note: MGMN mode does not support matrix batches.
    ///
    /// See more limitations for using [`cudssMatrix_t`] objects in the documentation for the main routine, [`cudssExecute`].
    ///
    /// # Parameters
    ///
    /// - `matrix`: \[host\] Created matrix object.
    /// - `batchCount`: \[host\] Size of the batch.
    /// - `nrows`: \[host\] Number of rows for each matrix in the batch.
    /// - `ncols`: \[host\] Number of columns for each matrix in the batch.
    /// - `ld`: \[host\] Leading dimension for each matrix in the batch.
    /// - `values`: \[device\] Pointer to values of each dense matrix in the batch.
    /// - `integerType`: \[host\] Integer type for scalar arrays (nrows, ncols, ld).
    /// - `valueType`: \[host\] Data type of the matrix.
    /// - `layout`: \[host\] Memory layout.
    pub fn cudssMatrixCreateBatchDn(
        matrix: *mut cudssMatrix_t,
        batchCount: i64,
        nrows: *const ::core::ffi::c_void,
        ncols: *const ::core::ffi::c_void,
        ld: *const ::core::ffi::c_void,
        values: *const *const ::core::ffi::c_void,
        integerType: cudssDataType_t,
        valueType: cudssDataType_t,
        layout: cudssLayout_t,
    ) -> cudssStatus_t;
}
unsafe extern "C" {
    /// The function creates a matrix object wrapped around a batch of sparse matrices (CSR format).
    /// The provided data buffer for `rowStart`, `rowEnd`, `colIndices` and `values` must contain device-visible pointers to device-visible data.
    ///
    /// Note: cuDSS supports non-uniform batches with varying shapes, e.g `nrows`, `ncols`, `nnz` can be different for each batch instance.
    ///
    /// Note: creating a cudssMatrix_t with a batch of matrices in CSR format does not perform any data consistency checks and thus currently it is
    /// the caller’s responsibility to have data description parameters matching the data.
    ///
    /// See more limitations for using [`cudssMatrix_t`] objects in the documentation for the main routine, [`cudssExecute`].
    ///
    /// # Parameters
    ///
    /// - `matrix`: \[host\] Created matrix object.
    /// - `batchCount`: \[host\] Size of the batch.
    /// - `nrows`: \[host\] Number of rows for each matrix in the batch.
    /// - `ncols`: \[host\] Number of columns for each matrix in the batch.
    /// - `nnz`: \[host\] Numbers of non-zeroes for each matrix in the batch.
    /// - `rowStart`: \[device\] Pointer to row start offsets for each matrix in the batch.
    /// - `rowEnd`: \[device\] Pointer to row end offsets for each matrix in the batch.
    /// - `colIndices`: \[device\] Pointer to column indices for each matrix in the batch.
    /// - `values`: \[device\] Pointer to values of each CSR matrix in the batch.
    /// - `offsetType`: \[host\] Offset type (`rowStart` and `rowEnd`) of the matrices in the batch.
    /// - `indexType`: \[host\] Index type of the matrices in the batch.
    /// - `valueType`: \[host\] Data type of the matrices in the batch.
    /// - `mtype`: \[host\] Matrix type of the matrix.
    /// - `mview`: \[host\] Matrix view of the matrix.
    /// - `indexBase`: \[host\] Indexing base.
    pub fn cudssMatrixCreateBatchCsr(
        matrix: *mut cudssMatrix_t,
        batchCount: i64,
        nrows: *const ::core::ffi::c_void,
        ncols: *const ::core::ffi::c_void,
        nnz: *const ::core::ffi::c_void,
        rowStart: *const *const ::core::ffi::c_void,
        rowEnd: *const *const ::core::ffi::c_void,
        colIndices: *const *const ::core::ffi::c_void,
        values: *const *const ::core::ffi::c_void,
        offsetType: cudssDataType_t,
        indexType: cudssDataType_t,
        valueType: cudssDataType_t,
        mtype: cudssMatrixType_t,
        mview: cudssMatrixViewType_t,
        indexBase: cudssIndexBase_t,
    ) -> cudssStatus_t;
}
unsafe extern "C" {
    /// The function releases memory associated with the matrix wrapper. As cuDSS matrix objects are only lightweight wrappers around the user data,
    /// the user data remains untouched.
    ///
    /// # Parameters
    ///
    /// - `matrix`: \[host\] cuDSS matrix object.
    pub fn cudssMatrixDestroy(matrix: cudssMatrix_t) -> cudssStatus_t;
}
unsafe extern "C" {
    /// The function retrieves dense matrix properties and data from a cuDSS matrix object which holds a dense matrix.
    /// If on input any of the pointers is NULL, it is ignored and the corresponding value is not returned.
    ///
    /// # Parameters
    ///
    /// - `matrix`: \[host\] cuDSS matrix object.
    /// - `nrows`: \[host\] Buffer for the number of rows.
    /// - `ncols`: \[host\] Buffer for the number of columns.
    /// - `ld`: \[host\] Buffer for the leading dimension.
    /// - `values`: \[device/host\] Buffer for the values of the matrix.
    /// - `layout`: \[host\] Buffer for the memory layout.
    pub fn cudssMatrixGetDn(
        matrix: cudssMatrix_t,
        nrows: *mut i64,
        ncols: *mut i64,
        ld: *mut i64,
        values: *mut *mut ::core::ffi::c_void,
        type_: *mut cudssDataType_t,
        layout: *mut cudssLayout_t,
    ) -> cudssStatus_t;
}
unsafe extern "C" {
    /// The function retrieves sparse matrix properties and data from a cuDSS matrix object which holds a CSR sparse matrix.
    /// If on input any of the pointers is NULL, it is ignored and the corresponding value is not returned.
    ///
    /// # Parameters
    ///
    /// - `matrix`: \[host\] Matrix object.
    /// - `nrows`: \[host\] Buffer for the number of rows.
    /// - `ncols`: \[host\] Buffer for the number of columns.
    /// - `nnz`: \[host\] Buffer for the number of non-zeroes.
    /// - `rowStart`: \[device/host\] Buffer for the row start offsets.
    /// - `rowEnd`: \[device/host\] Buffer for the row end offsets.
    /// - `colIndices`: \[device/host\] Buffer for the column indices of the matrix.
    /// - `values`: \[device/host\] Buffer for the values of the CSR matrix.
    /// - `offsetType`: \[host\] Buffer for the offset type (`rowStart` and `rowEnd`) of the matrix.
    /// - `indexType`: \[host\] Buffer for the index type of the matrix.
    /// - `valueType`: \[host\] Buffer for the data type of the matrix.
    /// - `mtype`: \[host\] Matrix type of the matrix.
    /// - `mview`: \[host\] Matrix view of the matrix.
    /// - `indexBase`: \[host\] Buffer for the indexing base.
    pub fn cudssMatrixGetCsr(
        matrix: cudssMatrix_t,
        nrows: *mut i64,
        ncols: *mut i64,
        nnz: *mut i64,
        rowStart: *mut *mut ::core::ffi::c_void,
        rowEnd: *mut *mut ::core::ffi::c_void,
        colIndices: *mut *mut ::core::ffi::c_void,
        values: *mut *mut ::core::ffi::c_void,
        offsetType: *mut cudssDataType_t,
        indexType: *mut cudssDataType_t,
        valueType: *mut cudssDataType_t,
        mtype: *mut cudssMatrixType_t,
        mview: *mut cudssMatrixViewType_t,
        indexBase: *mut cudssIndexBase_t,
    ) -> cudssStatus_t;
}
unsafe extern "C" {
    /// The function resets the pointer to values inside the cuDSS matrix object to the provided buffer.
    /// The provided data buffer must hold device-visible data.
    ///
    /// # Parameters
    ///
    /// - `matrix`: \[host\] cuDSS matrix object.
    /// - `values`: \[device/host\] Buffer with the new matrix values.
    pub fn cudssMatrixSetValues(
        matrix: cudssMatrix_t,
        values: *const ::core::ffi::c_void,
    ) -> cudssStatus_t;
}
unsafe extern "C" {
    /// The function resets the CSR pointers inside the cuDSS matrix object to the provided buffers.
    /// The provided data buffers must hold device-visible data.
    ///
    /// # Parameters
    ///
    /// - `matrix`: \[host\] cuDSS matrix object.
    /// - `rowEnd`: \[device/host\] Buffer with the new row end offsets.
    /// - `colIndices`: \[device/host\] Buffer with the new column indices.
    /// - `values`: \[device/host\] Buffer with the new matrix values.
    pub fn cudssMatrixSetCsrPointers(
        matrix: cudssMatrix_t,
        rowOffsets: *const ::core::ffi::c_void,
        rowEnd: *const ::core::ffi::c_void,
        colIndices: *const ::core::ffi::c_void,
        values: *const ::core::ffi::c_void,
    ) -> cudssStatus_t;
}
unsafe extern "C" {
    /// The function retrieves dense matrix properties and data from a cuDSS matrix object which holds a batch of dense matrices.
    /// If on input any of the pointers is NULL, it is ignored and the corresponding value is not returned.
    ///
    /// # Parameters
    ///
    /// - `matrix`: \[host\] cuDSS matrix object.
    /// - `batchCount`: \[host\] Buffer for the size of the batch.
    /// - `nrows`: \[host\] Pointer to the number of rows for each dense matrix in the batch.
    /// - `ncols`: \[host\] Pointer to the number of columns for dense matrix in the batch.
    /// - `ld`: \[host\] Pointer to the leading dimension for each dense matrix in the batch.
    /// - `values`: \[device\] Pointer to the values of each dense matrix in the batch.
    /// - `indexType`: \[host\] Buffer for the index type of the matrix.
    /// - `valueType`: \[host\] Buffer for the data type of the matrix.
    /// - `layout`: \[host\] Buffer for the memory layout.
    pub fn cudssMatrixGetBatchDn(
        matrix: cudssMatrix_t,
        batchCount: *mut i64,
        nrows: *mut *mut ::core::ffi::c_void,
        ncols: *mut *mut ::core::ffi::c_void,
        ld: *mut *mut ::core::ffi::c_void,
        values: *mut *mut *mut ::core::ffi::c_void,
        indexType: *mut cudssDataType_t,
        valueType: *mut cudssDataType_t,
        layout: *mut cudssLayout_t,
    ) -> cudssStatus_t;
}
unsafe extern "C" {
    /// The function retrieves sparse matrix properties and data from a cuDSS matrix object which holds a batch of CSR sparse matrices.
    /// If on input any of the pointers is NULL, it is ignored and the corresponding value is not returned.
    ///
    /// # Parameters
    ///
    /// - `matrix`: \[host\] Matrix object.
    /// - `batchCount`: \[host\] Buffer for the size of the batch.
    /// - `nrows`: \[host\] Pointer to the number of rows for each CSR matrix in the batch.
    /// - `ncols`: \[host\] Pointer to the number of columns for each CSR matrix in the batch.
    /// - `nnz`: \[host\] Pointer to the number of non-zeroes for each CSR matrix in the batch.
    /// - `rowStart`: \[device\] Pointer to the row start offsets for each CSR matrix in the batch.
    /// - `rowEnd`: \[device\] Pointer to the row end offsets for each CSR matrix in the batch.
    /// - `colIndices`: \[device\] Pointer to the column indices for each CSR matrix in the batch.
    /// - `values`: \[device\] Pointer to the values for each CSR matrix in the batch.
    /// - `offsetType`: \[host\] Buffer for the offset type (`rowStart` and `rowEnd`) of the matrices in the batch.
    /// - `indexType`: \[host\] Buffer for the index type of the matrices in the batch.
    /// - `valueType`: \[host\] Buffer for the data type of the matrices in the batch.
    /// - `mtype`: \[host\] Matrix type of the matrix.
    /// - `mview`: \[host\] Matrix view of the matrix.
    /// - `indexBase`: \[host\] Buffer for the indexing base.
    pub fn cudssMatrixGetBatchCsr(
        matrix: cudssMatrix_t,
        batchCount: *mut i64,
        nrows: *mut *mut ::core::ffi::c_void,
        ncols: *mut *mut ::core::ffi::c_void,
        nnz: *mut *mut ::core::ffi::c_void,
        rowStart: *mut *mut *mut ::core::ffi::c_void,
        rowEnd: *mut *mut *mut ::core::ffi::c_void,
        colIndices: *mut *mut *mut ::core::ffi::c_void,
        values: *mut *mut *mut ::core::ffi::c_void,
        offsetType: *mut cudssDataType_t,
        indexType: *mut cudssDataType_t,
        valueType: *mut cudssDataType_t,
        mtype: *mut cudssMatrixType_t,
        mview: *mut cudssMatrixViewType_t,
        indexBase: *mut cudssIndexBase_t,
    ) -> cudssStatus_t;
}
unsafe extern "C" {
    /// The function resets the pointer to values inside the cuDSS matrix object to the provided buffer.
    /// The provided data buffer for the matrix values must contain device-visible pointers to device-visible data.
    ///
    /// # Parameters
    ///
    /// - `matrix`: \[host\] cuDSS matrix object.
    /// - `values`: \[device/host\] Pointer to the new values for each dense matrix in the batch.
    pub fn cudssMatrixSetBatchValues(
        matrix: cudssMatrix_t,
        values: *const *const ::core::ffi::c_void,
    ) -> cudssStatus_t;
}
unsafe extern "C" {
    /// The function resets the CSR pointers inside the cuDSS matrix object to the provided buffers.
    /// The provided data buffer for `rowStart`, `rowEnd`, `colIndices` and `values` must contain device-visible pointers to device-visible data.
    ///
    /// # Parameters
    ///
    /// - `matrix`: \[host\] cuDSS matrix object.
    /// - `rowEnd`: \[device/host\] Pointer to the new row end offsets for each CSR matrix in the batch.
    /// - `colIndices`: \[device/host\] Pointer to the new column indices for each CSR matrix in the batch.
    /// - `values`: \[device/host\] Pointer to the new values for each CSR matrix in the batch.
    pub fn cudssMatrixSetBatchCsrPointers(
        matrix: cudssMatrix_t,
        rowOffsets: *const *const ::core::ffi::c_void,
        rowEnd: *const *const ::core::ffi::c_void,
        colIndices: *const *const ::core::ffi::c_void,
        values: *const *const ::core::ffi::c_void,
    ) -> cudssStatus_t;
}
unsafe extern "C" {
    /// The function returns into the provided buffer the matrix format (as an `int` which can be a combination of bit flags
    /// defined in [`cudssMatrixFormat_t`]) of the cuDSS matrix object.
    ///
    /// # Parameters
    ///
    /// - `matrix`: \[host\] cuDSS matrix object.
    /// - `format`: \[host\] Buffer for the returned matrix format.
    pub fn cudssMatrixGetFormat(
        matrix: cudssMatrix_t,
        format: *mut ::core::ffi::c_int,
    ) -> cudssStatus_t;
}
unsafe extern "C" {
    /// The function sets the 1D distribution for the matrix (CSR or Dense) for the MGMN mode.
    /// The provided `first_row` and `last_row` must be always 0-based and specify the first and the last (included) row indices of the local matrix on the calling process.
    /// Setting `first_row` > `last_row` means that the local matrix is empty for the calling process.
    ///
    /// > Note: input sparse matrix, right-hand-size and solution can have a different (from each other) distribution. For example, only
    /// > the sparse matrix can be distributed, while right-hand-size or solution are not.
    /// >
    /// > Note: if the sparse matrix or right-hand side are distributed with an overlap (between processes) then the overlapped part will be summed up, that is
    /// > the overlapped part will have a contribution from all related processes. if the solution is distributed with overlapping (between processes)
    /// > then the overlapped part will have the same values on corresponding processes.
    ///
    /// # Parameters
    ///
    /// - `matrix`: \[host\] cuDSS matrix object.
    /// - `first_row`: \[host\] first row index of the local matrix on the calling process.
    /// - `last_row`: \[host\] last row index of the local matrix on the calling process.
    pub fn cudssMatrixSetDistributionRow1d(
        matrix: cudssMatrix_t,
        first_row: i64,
        last_row: i64,
    ) -> cudssStatus_t;
}
unsafe extern "C" {
    /// The function retrieves 1D distribution boundaries (first and last row indices) from a cuDSS matrix object.
    /// For the non-MGMN mode the function returns `first_row` equals to 0 and `last_row` equal to `nrows - 1`.
    ///
    /// # Parameters
    ///
    /// - `matrix`: \[host\] cuDSS matrix object.
    /// - `first_row`: \[host\] first row index of the local matrix on the calling process.
    /// - `last_row`: \[host\] last row index of the local matrix on the calling process.
    pub fn cudssMatrixGetDistributionRow1d(
        matrix: cudssMatrix_t,
        first_row: *mut i64,
        last_row: *mut i64,
    ) -> cudssStatus_t;
}
unsafe extern "C" {
    /// Get the current device memory handler.
    ///
    /// # Parameters
    ///
    /// - `handle`: \[host\] cuDSS library handle.
    /// - `handler`: \[host\] A (deep) copy of the device memory handler that encapsulates the user’s mempool (if it was set) previously via calling [`cudssSetDeviceMemHandler`].
    pub fn cudssGetDeviceMemHandler(
        handle: cudssHandle_t,
        handler: *mut cudssDeviceMemHandler_t,
    ) -> cudssStatus_t;
}
unsafe extern "C" {
    /// Set the current device memory handler inside the library handle.
    ///
    /// If `handler` argument is set to NULL, the library handle will detach its existing memory handler from the library handle.
    /// In case device memory handler needs to be changed after it is set for the first time, previously set device memory handler needs to be detached.
    ///
    /// If a cuDSS API which needs to allocate device memory ([`cudssExecute`]) is called and there is no device memory handler
    /// attached to the library handle at the moment of the call, cuDSS will allocate device memory internally using a default memory handler.
    ///
    /// When the device memory handler is set, during calls to the main cuDSS routine [`cudssExecute`]
    /// the library will allocate the necessary device memory using the `device_alloc()` member of the device memory handler struct.
    /// The allocated memory will remain a part of the [`cudssData_t`] object used in the call until the allocated memory will be deallocated using the `device_free()`
    /// member of the struct when the corresponding [`cudssDataDestroy`] is called.
    /// As it follows, erroneous behavior is likely to occur if the device memory handler is changed during the lifespan of the [`cudssData_t`] objects which have
    /// used the library handle.
    /// See [`cudssDeviceMemHandler_t`] for further details about `device_alloc()` and `device_free()`.
    ///
    /// The internal stream order is established using the user-provided stream set via [`cudssSetStream`].
    ///
    /// Note: It is undefined behavior if the library handle is bound to a memory handler and subsequently to another handler (without detaching), or
    /// the library handle outlives the attached memory pool, or the memory pool is not stream-ordered.
    ///
    /// # Parameters
    ///
    /// - `handle`: \[host\] cuDSS library handle.
    /// - `handler`: \[host\] The device memory handler that encapsulates the user’s mempool. The struct content is copied internally.
    pub fn cudssSetDeviceMemHandler(
        handle: cudssHandle_t,
        handler: *const cudssDeviceMemHandler_t,
    ) -> cudssStatus_t;
}
/// A callback function pointer type for logging.
pub type cudssLoggerCallback_t = ::core::option::Option<
    unsafe extern "C" fn(
        logLevel: ::core::ffi::c_int,
        functionName: *const ::core::ffi::c_char,
        message: *const ::core::ffi::c_char,
    ),
>;
unsafe extern "C" {
    /// Sets a custom callback function to receive log messages.
    ///
    /// The callback is invoked synchronously from the calling thread and must not call any cuDSS API functions (may cause deadlocks).
    ///
    /// Note
    ///
    /// In [MGMN mode](https://docs.nvidia.com/cuda/cudss/advanced_features.html#multi-gpu-multi-node-mgmn-mode), each process has its own independent logger state and callback.
    /// The callback must be set in each process if needed.
    ///
    /// # Parameters
    ///
    /// - `callback`: \[host\] Pointer to callback function (see [`cudssLoggerCallback_t`]), or NULL to disable.
    pub fn cudssLoggerSetCallback(callback: cudssLoggerCallback_t) -> cudssStatus_t;
}
unsafe extern "C" {
    /// Redirects log output to an already-opened file handle.
    ///
    /// Pass `NULL` to disable log output entirely.
    ///
    /// Note
    ///
    /// The file handle must remain open until another file is set or logging is disabled.
    /// Passing `NULL` disables all log output until a valid file is set.
    ///
    /// Note
    ///
    /// In [MGMN mode](https://docs.nvidia.com/cuda/cudss/advanced_features.html#multi-gpu-multi-node-mgmn-mode), each process must use a different file handle to avoid
    /// corrupted or interleaved output. Multiple processes writing to the same file concurrently is not safe.
    ///
    /// # Parameters
    ///
    /// - `file`: \[host\] Pointer to open file with write permission, or `NULL` to disable logging.
    pub fn cudssLoggerSetFile(file: *mut FILE) -> cudssStatus_t;
}
unsafe extern "C" {
    /// Opens a file and redirects log output to it. The file is opened in write mode (“w”), truncating any existing content.
    ///
    /// Multiple calls automatically close the previous file. Parent directories must exist beforehand.
    ///
    /// Note
    ///
    /// In [MGMN mode](https://docs.nvidia.com/cuda/cudss/advanced_features.html#multi-gpu-multi-node-mgmn-mode), each process must open a different file to avoid
    /// corrupted or interleaved output. Multiple processes writing to the same file concurrently is not safe.
    ///
    /// # Parameters
    ///
    /// - `logFile`: \[host\] Path to the log file.
    pub fn cudssLoggerOpenFile(logFile: *const ::core::ffi::c_char) -> cudssStatus_t;
}
unsafe extern "C" {
    /// Sets the logging level to control which messages are output.
    ///
    /// # Parameters
    ///
    /// - `level`: \[host\] Log level. Refer to [cuDSS Logging Features](https://docs.nvidia.com/cuda/cudss/logging.html#cudss-logging-features) for specific logging level values.
    pub fn cudssLoggerSetLevel(level: ::core::ffi::c_int) -> cudssStatus_t;
}
unsafe extern "C" {
    /// Sets the logging mask for fine-grained control over which log types are output.
    ///
    /// # Parameters
    ///
    /// - `mask`: \[host\] Bitwise OR of masks. Refer to [cuDSS Logging Features](https://docs.nvidia.com/cuda/cudss/logging.html#cudss-logging-features) for specific logging mask values.
    pub fn cudssLoggerSetMask(mask: ::core::ffi::c_int) -> cudssStatus_t;
}
unsafe extern "C" {
    /// Permanently and irreversibly disables all logging for the entire process.
    ///
    /// Once called, logging cannot be re-enabled for the lifetime of the process.
    ///
    /// Note
    ///
    /// Once this API is called, disables all existing file sinks and registered callbacks immediately.
    /// This API takes precedence over all logging configurations (environment variables, [`cudssLoggerSetLevel`], [`cudssLoggerSetMask`], etc.).
    pub fn cudssLoggerForceDisable() -> cudssStatus_t;
}
