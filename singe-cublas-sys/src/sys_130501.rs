/* automatically generated by rust-bindgen 0.72.1 */

pub const CUBLAS_VER_MAJOR: u32 = 13;
pub const CUBLAS_VER_MINOR: u32 = 5;
pub const CUBLAS_VER_PATCH: u32 = 1;
pub const CUBLAS_VER_BUILD: u32 = 27;
pub const CUBLAS_VERSION: u32 = 130501;
#[repr(C)]
#[repr(align(8))]
#[derive(Debug, Default, Copy, Clone, PartialOrd, PartialEq)]
pub struct float2 {
    pub x: f32,
    pub y: f32,
}
#[repr(C)]
#[repr(align(16))]
#[derive(Debug, Default, Copy, Clone, PartialOrd, PartialEq)]
pub struct double2 {
    pub x: f64,
    pub y: f64,
}
pub type size_t = ::core::ffi::c_ulong;
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct CUstream_st {
    _unused: [u8; 0],
}
pub type cuFloatComplex = float2;
pub type cuDoubleComplex = double2;
pub type cuComplex = cuFloatComplex;
/// The type is used for function status returns. All cuBLAS library functions return their status, which can have the following values.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cublasStatus_t {
    /// The operation completed successfully.
    CUBLAS_STATUS_SUCCESS = 0,
    /// The cuBLAS library was not initialized. This is usually caused by the lack of a prior [`cublasCreate_v2`] call, an error in the CUDA Runtime API called by the cuBLAS routine, or an error in the hardware setup.
    ///
    /// To correct: call [`cublasCreate_v2`] before the function call; and check that the hardware, an appropriate version of the driver, and the cuBLAS library are correctly installed.
    CUBLAS_STATUS_NOT_INITIALIZED = 1,
    /// Resource allocation failed inside the cuBLAS library. This is usually caused by a `cudaMalloc()` failure.
    ///
    /// To correct: prior to the function call, deallocate previously allocated memory as much as possible.
    CUBLAS_STATUS_ALLOC_FAILED = 3,
    /// An unsupported value or parameter was passed to the function (a negative vector size, for example).
    ///
    /// To correct: ensure that all the parameters being passed have valid values.
    CUBLAS_STATUS_INVALID_VALUE = 7,
    /// The function requires a feature absent from the device architecture; usually caused by compute capability lower than 5.0.
    ///
    /// To correct: compile and run the application on a device with appropriate compute capability.
    CUBLAS_STATUS_ARCH_MISMATCH = 8,
    /// An access to GPU memory space failed, which is usually caused by a failure to bind a texture.
    ///
    /// To correct: before the function call, unbind any previously bound textures.
    CUBLAS_STATUS_MAPPING_ERROR = 11,
    /// The GPU program failed to execute. This is often caused by a launch failure of the kernel on the GPU, which can be caused by multiple reasons.
    ///
    /// To correct: check that the hardware, an appropriate version of the driver, and the cuBLAS library are correctly installed.
    CUBLAS_STATUS_EXECUTION_FAILED = 13,
    /// An internal cuBLAS operation failed. This error is usually caused by a `cudaMemcpyAsync()` failure.
    ///
    /// To correct: check that the hardware, an appropriate version of the driver, and the cuBLAS library are correctly installed. Also, check that the memory passed as a parameter to the routine is not being deallocated prior to the routine’s completion.
    CUBLAS_STATUS_INTERNAL_ERROR = 14,
    /// The functionality requested is not supported.
    CUBLAS_STATUS_NOT_SUPPORTED = 15,
    /// The functionality requested requires some license and an error was detected when trying to check the current licensing. This error can happen if the license is not present or is expired or if the environment variable NVIDIA_LICENSE_FILE is not set properly.
    CUBLAS_STATUS_LICENSE_ERROR = 16,
}
/// The type indicates which part (lower or upper) of the dense matrix was filled and consequently should be used by the function. Its values correspond to Fortran characters `L` or `l` (lower) and `U` or `u` (upper) that are often used as parameters to legacy BLAS implementations.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cublasFillMode_t {
    /// The lower part of the matrix is filled.
    CUBLAS_FILL_MODE_LOWER = 0,
    /// The upper part of the matrix is filled.
    CUBLAS_FILL_MODE_UPPER = 1,
    /// The full matrix is filled.
    CUBLAS_FILL_MODE_FULL = 2,
}
/// The type indicates whether the main diagonal of the dense matrix is unity and consequently should not be touched or modified by the function. Its values correspond to Fortran characters `‘N’` or `‘n’` (non-unit) and `‘U’` or `‘u’` (unit) that are often used as parameters to legacy BLAS implementations.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cublasDiagType_t {
    /// The matrix diagonal has non-unit elements.
    CUBLAS_DIAG_NON_UNIT = 0,
    /// The matrix diagonal has unit elements.
    CUBLAS_DIAG_UNIT = 1,
}
/// The type indicates whether the dense matrix is on the left or right side in the matrix equation solved by a particular function. Its values correspond to Fortran characters `‘L’` or `‘l’` (left) and `‘R’` or `‘r’` (right) that are often used as parameters to legacy BLAS implementations.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cublasSideMode_t {
    /// The matrix is on the left side in the equation.
    CUBLAS_SIDE_LEFT = 0,
    /// The matrix is on the right side in the equation.
    CUBLAS_SIDE_RIGHT = 1,
}
impl cublasOperation_t {
    pub const CUBLAS_OP_HERMITAN: cublasOperation_t = cublasOperation_t::CUBLAS_OP_C;
}
/// The [`cublasOperation_t`] type indicates which operation needs to be performed with the dense matrix. Its values correspond to Fortran characters `‘N’` or `‘n’` (non-transpose), `‘T’` or `‘t’` (transpose) and `‘C’` or `‘c’` (conjugate transpose) that are often used as parameters to legacy BLAS implementations.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cublasOperation_t {
    /// The non-transpose operation is selected.
    CUBLAS_OP_N = 0,
    /// The transpose operation is selected.
    CUBLAS_OP_T = 1,
    /// The conjugate transpose operation is selected.
    CUBLAS_OP_C = 2,
    CUBLAS_OP_CONJG = 3,
}
/// The [`cublasPointerMode_t`] type indicates whether the scalar values are passed by reference on the host or device. It is important to point out that if several scalar values are present in the function call, all of them must conform to the same single pointer mode. The pointer mode can be set and retrieved using [`cublasSetPointerMode_v2`] and [`cublasGetPointerMode_v2`] routines, respectively.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cublasPointerMode_t {
    /// The scalars are passed by reference on the host.
    CUBLAS_POINTER_MODE_HOST = 0,
    /// The scalars are passed by reference on the device.
    CUBLAS_POINTER_MODE_DEVICE = 1,
}
/// The type indicates whether cuBLAS routines which has an alternate implementation using atomics can be used. The atomics mode can be set and queried using [`cublasSetAtomicsMode`] and [`cublasGetAtomicsMode`] and routines, respectively.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cublasAtomicsMode_t {
    /// The usage of atomics is not allowed.
    CUBLAS_ATOMICS_NOT_ALLOWED = 0,
    /// The usage of atomics is allowed.
    CUBLAS_ATOMICS_ALLOWED = 1,
}
impl cublasGemmAlgo_t {
    pub const CUBLAS_GEMM_DEFAULT: cublasGemmAlgo_t = cublasGemmAlgo_t::CUBLAS_GEMM_DFALT;
}
impl cublasGemmAlgo_t {
    pub const CUBLAS_GEMM_DFALT_TENSOR_OP: cublasGemmAlgo_t = cublasGemmAlgo_t::CUBLAS_GEMM_DEFAULT_TENSOR_OP;
}
/// cublasGemmAlgo_t type is an enumerant to specify the algorithm for matrix-matrix multiplication on GPU architectures up to `sm_75`. On `sm_80` and newer GPU architectures, this enumerant has no effect. cuBLAS has the following algorithm options.
#[repr(i32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cublasGemmAlgo_t {
    CUBLAS_GEMM_DFALT = -1,
    /// Explicitly choose an Algorithm `0..23`. Note: Doesn’t have effect on NVIDIA Ampere architecture GPUs and newer.
    CUBLAS_GEMM_ALGO0 = 0,
    /// Explicitly choose an Algorithm `0..23`. Note: Doesn’t have effect on NVIDIA Ampere architecture GPUs and newer.
    CUBLAS_GEMM_ALGO1 = 1,
    /// Explicitly choose an Algorithm `0..23`. Note: Doesn’t have effect on NVIDIA Ampere architecture GPUs and newer.
    CUBLAS_GEMM_ALGO2 = 2,
    /// Explicitly choose an Algorithm `0..23`. Note: Doesn’t have effect on NVIDIA Ampere architecture GPUs and newer.
    CUBLAS_GEMM_ALGO3 = 3,
    /// Explicitly choose an Algorithm `0..23`. Note: Doesn’t have effect on NVIDIA Ampere architecture GPUs and newer.
    CUBLAS_GEMM_ALGO4 = 4,
    /// Explicitly choose an Algorithm `0..23`. Note: Doesn’t have effect on NVIDIA Ampere architecture GPUs and newer.
    CUBLAS_GEMM_ALGO5 = 5,
    /// Explicitly choose an Algorithm `0..23`. Note: Doesn’t have effect on NVIDIA Ampere architecture GPUs and newer.
    CUBLAS_GEMM_ALGO6 = 6,
    /// Explicitly choose an Algorithm `0..23`. Note: Doesn’t have effect on NVIDIA Ampere architecture GPUs and newer.
    CUBLAS_GEMM_ALGO7 = 7,
    /// Explicitly choose an Algorithm `0..23`. Note: Doesn’t have effect on NVIDIA Ampere architecture GPUs and newer.
    CUBLAS_GEMM_ALGO8 = 8,
    /// Explicitly choose an Algorithm `0..23`. Note: Doesn’t have effect on NVIDIA Ampere architecture GPUs and newer.
    CUBLAS_GEMM_ALGO9 = 9,
    /// Explicitly choose an Algorithm `0..23`. Note: Doesn’t have effect on NVIDIA Ampere architecture GPUs and newer.
    CUBLAS_GEMM_ALGO10 = 10,
    /// Explicitly choose an Algorithm `0..23`. Note: Doesn’t have effect on NVIDIA Ampere architecture GPUs and newer.
    CUBLAS_GEMM_ALGO11 = 11,
    /// Explicitly choose an Algorithm `0..23`. Note: Doesn’t have effect on NVIDIA Ampere architecture GPUs and newer.
    CUBLAS_GEMM_ALGO12 = 12,
    /// Explicitly choose an Algorithm `0..23`. Note: Doesn’t have effect on NVIDIA Ampere architecture GPUs and newer.
    CUBLAS_GEMM_ALGO13 = 13,
    /// Explicitly choose an Algorithm `0..23`. Note: Doesn’t have effect on NVIDIA Ampere architecture GPUs and newer.
    CUBLAS_GEMM_ALGO14 = 14,
    /// Explicitly choose an Algorithm `0..23`. Note: Doesn’t have effect on NVIDIA Ampere architecture GPUs and newer.
    CUBLAS_GEMM_ALGO15 = 15,
    /// Explicitly choose an Algorithm `0..23`. Note: Doesn’t have effect on NVIDIA Ampere architecture GPUs and newer.
    CUBLAS_GEMM_ALGO16 = 16,
    /// Explicitly choose an Algorithm `0..23`. Note: Doesn’t have effect on NVIDIA Ampere architecture GPUs and newer.
    CUBLAS_GEMM_ALGO17 = 17,
    /// Explicitly choose an Algorithm `0..23`. Note: Doesn’t have effect on NVIDIA Ampere architecture GPUs and newer.
    CUBLAS_GEMM_ALGO18 = 18,
    /// Explicitly choose an Algorithm `0..23`. Note: Doesn’t have effect on NVIDIA Ampere architecture GPUs and newer.
    CUBLAS_GEMM_ALGO19 = 19,
    /// Explicitly choose an Algorithm `0..23`. Note: Doesn’t have effect on NVIDIA Ampere architecture GPUs and newer.
    CUBLAS_GEMM_ALGO20 = 20,
    /// Explicitly choose an Algorithm `0..23`. Note: Doesn’t have effect on NVIDIA Ampere architecture GPUs and newer.
    CUBLAS_GEMM_ALGO21 = 21,
    /// Explicitly choose an Algorithm `0..23`. Note: Doesn’t have effect on NVIDIA Ampere architecture GPUs and newer.
    CUBLAS_GEMM_ALGO22 = 22,
    /// Explicitly choose an Algorithm `0..23`. Note: Doesn’t have effect on NVIDIA Ampere architecture GPUs and newer.
    CUBLAS_GEMM_ALGO23 = 23,
    #[deprecated]
    CUBLAS_GEMM_DEFAULT_TENSOR_OP = 99,
    /// Those values are deprecated and will be removed in a future release. Explicitly choose a Tensor core GEMM Algorithm `0..15`. Allows use of reduced precision CUBLAS_COMPUTE_32F_FAST_16F kernels (for backward compatibility). Note: Doesn’t have effect on NVIDIA Ampere architecture GPUs and newer.
    #[deprecated]
    CUBLAS_GEMM_ALGO0_TENSOR_OP = 100,
    /// Those values are deprecated and will be removed in a future release. Explicitly choose a Tensor core GEMM Algorithm `0..15`. Allows use of reduced precision CUBLAS_COMPUTE_32F_FAST_16F kernels (for backward compatibility). Note: Doesn’t have effect on NVIDIA Ampere architecture GPUs and newer.
    #[deprecated]
    CUBLAS_GEMM_ALGO1_TENSOR_OP = 101,
    /// Those values are deprecated and will be removed in a future release. Explicitly choose a Tensor core GEMM Algorithm `0..15`. Allows use of reduced precision CUBLAS_COMPUTE_32F_FAST_16F kernels (for backward compatibility). Note: Doesn’t have effect on NVIDIA Ampere architecture GPUs and newer.
    #[deprecated]
    CUBLAS_GEMM_ALGO2_TENSOR_OP = 102,
    /// Those values are deprecated and will be removed in a future release. Explicitly choose a Tensor core GEMM Algorithm `0..15`. Allows use of reduced precision CUBLAS_COMPUTE_32F_FAST_16F kernels (for backward compatibility). Note: Doesn’t have effect on NVIDIA Ampere architecture GPUs and newer.
    #[deprecated]
    CUBLAS_GEMM_ALGO3_TENSOR_OP = 103,
    /// Those values are deprecated and will be removed in a future release. Explicitly choose a Tensor core GEMM Algorithm `0..15`. Allows use of reduced precision CUBLAS_COMPUTE_32F_FAST_16F kernels (for backward compatibility). Note: Doesn’t have effect on NVIDIA Ampere architecture GPUs and newer.
    #[deprecated]
    CUBLAS_GEMM_ALGO4_TENSOR_OP = 104,
    /// Those values are deprecated and will be removed in a future release. Explicitly choose a Tensor core GEMM Algorithm `0..15`. Allows use of reduced precision CUBLAS_COMPUTE_32F_FAST_16F kernels (for backward compatibility). Note: Doesn’t have effect on NVIDIA Ampere architecture GPUs and newer.
    #[deprecated]
    CUBLAS_GEMM_ALGO5_TENSOR_OP = 105,
    /// Those values are deprecated and will be removed in a future release. Explicitly choose a Tensor core GEMM Algorithm `0..15`. Allows use of reduced precision CUBLAS_COMPUTE_32F_FAST_16F kernels (for backward compatibility). Note: Doesn’t have effect on NVIDIA Ampere architecture GPUs and newer.
    #[deprecated]
    CUBLAS_GEMM_ALGO6_TENSOR_OP = 106,
    /// Those values are deprecated and will be removed in a future release. Explicitly choose a Tensor core GEMM Algorithm `0..15`. Allows use of reduced precision CUBLAS_COMPUTE_32F_FAST_16F kernels (for backward compatibility). Note: Doesn’t have effect on NVIDIA Ampere architecture GPUs and newer.
    #[deprecated]
    CUBLAS_GEMM_ALGO7_TENSOR_OP = 107,
    /// Those values are deprecated and will be removed in a future release. Explicitly choose a Tensor core GEMM Algorithm `0..15`. Allows use of reduced precision CUBLAS_COMPUTE_32F_FAST_16F kernels (for backward compatibility). Note: Doesn’t have effect on NVIDIA Ampere architecture GPUs and newer.
    #[deprecated]
    CUBLAS_GEMM_ALGO8_TENSOR_OP = 108,
    /// Those values are deprecated and will be removed in a future release. Explicitly choose a Tensor core GEMM Algorithm `0..15`. Allows use of reduced precision CUBLAS_COMPUTE_32F_FAST_16F kernels (for backward compatibility). Note: Doesn’t have effect on NVIDIA Ampere architecture GPUs and newer.
    #[deprecated]
    CUBLAS_GEMM_ALGO9_TENSOR_OP = 109,
    /// Those values are deprecated and will be removed in a future release. Explicitly choose a Tensor core GEMM Algorithm `0..15`. Allows use of reduced precision CUBLAS_COMPUTE_32F_FAST_16F kernels (for backward compatibility). Note: Doesn’t have effect on NVIDIA Ampere architecture GPUs and newer.
    #[deprecated]
    CUBLAS_GEMM_ALGO10_TENSOR_OP = 110,
    /// Those values are deprecated and will be removed in a future release. Explicitly choose a Tensor core GEMM Algorithm `0..15`. Allows use of reduced precision CUBLAS_COMPUTE_32F_FAST_16F kernels (for backward compatibility). Note: Doesn’t have effect on NVIDIA Ampere architecture GPUs and newer.
    #[deprecated]
    CUBLAS_GEMM_ALGO11_TENSOR_OP = 111,
    /// Those values are deprecated and will be removed in a future release. Explicitly choose a Tensor core GEMM Algorithm `0..15`. Allows use of reduced precision CUBLAS_COMPUTE_32F_FAST_16F kernels (for backward compatibility). Note: Doesn’t have effect on NVIDIA Ampere architecture GPUs and newer.
    #[deprecated]
    CUBLAS_GEMM_ALGO12_TENSOR_OP = 112,
    /// Those values are deprecated and will be removed in a future release. Explicitly choose a Tensor core GEMM Algorithm `0..15`. Allows use of reduced precision CUBLAS_COMPUTE_32F_FAST_16F kernels (for backward compatibility). Note: Doesn’t have effect on NVIDIA Ampere architecture GPUs and newer.
    #[deprecated]
    CUBLAS_GEMM_ALGO13_TENSOR_OP = 113,
    /// Those values are deprecated and will be removed in a future release. Explicitly choose a Tensor core GEMM Algorithm `0..15`. Allows use of reduced precision CUBLAS_COMPUTE_32F_FAST_16F kernels (for backward compatibility). Note: Doesn’t have effect on NVIDIA Ampere architecture GPUs and newer.
    #[deprecated]
    CUBLAS_GEMM_ALGO14_TENSOR_OP = 114,
    /// Those values are deprecated and will be removed in a future release. Explicitly choose a Tensor core GEMM Algorithm `0..15`. Allows use of reduced precision CUBLAS_COMPUTE_32F_FAST_16F kernels (for backward compatibility). Note: Doesn’t have effect on NVIDIA Ampere architecture GPUs and newer.
    #[deprecated]
    CUBLAS_GEMM_ALGO15_TENSOR_OP = 115,
    /// \[EXPERIMENTAL\] The library will benchmark a number of available algorithms and choose the optimal one for the given problem configuration. Solution is cached in cublas handle so that next calls with the problem size will use the cached configuration. Note: To avoid overwriting the user’s data, the library will allocate the amount of memory corresponding to the size of the output. Note: The benchmarking is not supported during stream capture; [`cublasStatus_t::CUBLAS_STATUS_NOT_SUPPORTED`] will be returned under stream capture if no configuration was found in the cache for the given problem size.
    CUBLAS_GEMM_AUTOTUNE = 999,
}
/// [`cublasMath_t`] enumerate type is used in [`cublasSetMathMode`] to choose compute precision modes as defined in the following table. Since this setting does not directly control the use of Tensor Cores, the mode [`cublasMath_t::CUBLAS_TENSOR_OP_MATH`] is being deprecated, and will be removed in a future release.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cublasMath_t {
    /// This is the default and highest-performance mode that uses compute and intermediate storage precisions with at least the same number of mantissa and exponent bits as requested. Tensor Cores will be used whenever possible.
    CUBLAS_DEFAULT_MATH = 0,
    #[deprecated]
    CUBLAS_TENSOR_OP_MATH = 1,
    /// This mode uses the prescribed precision and standardized arithmetic for all phases of calculations and is primarily intended for numerical robustness studies, testing, and debugging. This mode might not be as performant as the other modes.
    CUBLAS_PEDANTIC_MATH = 2,
    /// Enable acceleration of single-precision routines using TF32 tensor cores. Note that input conversions round to nearest even.
    CUBLAS_TF32_TENSOR_OP_MATH = 3,
    /// Enable acceleration of single-precision routines using the BF16x9 algorithm. See Floating Point Emulation for more details. For single precision GEMM routines cuBLAS will use the [`cublasComputeType_t::CUBLAS_COMPUTE_32F_EMULATED_16BFX9`] compute type.
    CUBLAS_FP32_EMULATED_BF16X9_MATH = 4,
    /// Enable acceleration of double-precision routines using fixed-point emulation algorithms. See Floating Point Emulation for more details.
    CUBLAS_FP64_EMULATED_FIXEDPOINT_MATH = 8,
    /// Forces any reductions during matrix multiplications to use the accumulator type (that is, compute type) and not the output type in case of mixed precision routines where output type precision is less than the compute type precision. This is a flag that can be set (using a bitwise or operation) alongside any of the other values.
    CUBLAS_MATH_DISALLOW_REDUCED_PRECISION_REDUCTION = 16,
}
/// [`cublasComputeType_t`] enumerate type is used in [`cublasGemmEx`] and `cublasLtMatmul` (including all batched and strided batched variants) to choose compute precision modes as defined below.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cublasComputeType_t {
    CUBLAS_COMPUTE_16F = 64,
    CUBLAS_COMPUTE_16F_PEDANTIC = 65,
    CUBLAS_COMPUTE_32F = 68,
    CUBLAS_COMPUTE_32F_PEDANTIC = 69,
    CUBLAS_COMPUTE_32F_FAST_16F = 74,
    CUBLAS_COMPUTE_32F_FAST_16BF = 75,
    CUBLAS_COMPUTE_32F_FAST_TF32 = 77,
    CUBLAS_COMPUTE_32F_EMULATED_16BFX9 = 78,
    CUBLAS_COMPUTE_64F = 70,
    CUBLAS_COMPUTE_64F_PEDANTIC = 71,
    CUBLAS_COMPUTE_64F_EMULATED_FIXEDPOINT = 79,
    CUBLAS_COMPUTE_32I = 72,
    CUBLAS_COMPUTE_32I_PEDANTIC = 73,
}
/// [`cublasEmulationStrategy_t`] enumerate type is used in [`cublasSetEmulationStrategy`] to choose how to leverage floating point emulation algorithms.
#[repr(u32)]
#[derive(Debug, Copy, Clone, Hash, PartialOrd, Ord, PartialEq, Eq)]
pub enum cublasEmulationStrategy_t {
    /// This is the default emulation strategy and is equivalent to [`cublasEmulationStrategy_t::CUBLAS_EMULATION_STRATEGY_PERFORMANT`] unless the `CUBLAS_EMULATION_STRATEGY` environment variable is set.
    CUBLAS_EMULATION_STRATEGY_DEFAULT = 0,
    /// A strategy which utilizes emulation whenever it provides a performance benefit.
    CUBLAS_EMULATION_STRATEGY_PERFORMANT = 1,
    /// A strategy which utilizes emulation whenever possible.
    CUBLAS_EMULATION_STRATEGY_EAGER = 2,
}
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct cublasContext {
    _unused: [u8; 0],
}
/// The [`cublasHandle_t`] type is a pointer type to an opaque structure holding the cuBLAS library context. The cuBLAS library context must be initialized using [`cublasCreate_v2`] and the returned handle must be passed to all subsequent library function calls. The context should be destroyed at the end using [`cublasDestroy_v2`].
pub type cublasHandle_t = *mut cublasContext;
pub type cublasLogCallback = ::core::option::Option<
    unsafe extern "C" fn(msg: *const ::core::ffi::c_char),
>;
unsafe extern "C" {
    /// This function initializes the cuBLAS library and creates a handle to an opaque structure holding the cuBLAS library context. It allocates hardware resources on the host and device and must be called prior to making any other cuBLAS library calls.
    ///
    /// The cuBLAS library context is tied to the current CUDA device. To use the library on multiple devices, one cuBLAS handle needs to be created for each device. See also cuBLAS Context.
    ///
    /// For a given device, multiple cuBLAS handles with different configurations can be created. For multi-threaded applications that use the same device from different threads, the recommended programming model is to create one cuBLAS handle per thread and use that cuBLAS handle for the entire life of the thread.
    ///
    /// Because [`cublasCreate_v2`] allocates some internal resources and the release of those resources by calling [`cublasDestroy_v2`] will implicitly call `cudaDeviceSynchronize()`, it is recommended to minimize the number of times these functions are called.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_ALLOC_FAILED`]: The resources could not be allocated.
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: `handle` is NULL.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_INITIALIZED`]: The CUDA™ Runtime initialization failed.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The initialization succeeded.
    pub fn cublasCreate_v2(handle: *mut cublasHandle_t) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function releases hardware resources used by the cuBLAS library. This function is usually the last call with a particular handle to the cuBLAS library. Because [`cublasCreate_v2`] allocates some internal resources and the release of those resources by calling [`cublasDestroy_v2`] will implicitly call `cudaDeviceSynchronize()`, it is recommended to minimize the number of times these functions are called.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_INITIALIZED`]: the library was not initialized.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: the shut down succeeded.
    pub fn cublasDestroy_v2(handle: cublasHandle_t) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function returns the version number of the cuBLAS library.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: `version` is NULL.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The operation completed successfully.
    pub fn cublasGetVersion_v2(
        handle: cublasHandle_t,
        version: *mut ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function returns the value of the requested property in memory pointed to by value. Refer to [`libraryPropertyType`] for supported types.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: Invalid type or value
    ///   - If `type` has an invalid value, or if `value` is NULL.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The operation completed successfully.
    pub fn cublasGetProperty(
        type_: libraryPropertyType,
        value: *mut ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    pub fn cublasGetCudartVersion() -> size_t;
}
unsafe extern "C" {
    /// This function sets the cuBLAS library workspace to a user-owned device buffer, which will be used to execute all subsequent calls to the cuBLAS library functions (on the currently set stream). If the cuBLAS library workspace is not set, all kernels will use the default workspace pool allocated during the cuBLAS context creation. In particular, this routine can be used to change the workspace between kernel launches. The workspace pointer has to be aligned to at least 256 bytes, otherwise [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`] error is returned. The [`cublasSetStream_v2`] function unconditionally resets the cuBLAS library workspace back to the default workspace pool. Calling this function, including with `workspaceSizeInBytes` equal to 0, will prevent the cuBLAS library from utilizing the default workspace. Too small value of `workspaceSizeInBytes` may cause some routines to fail with [`cublasStatus_t::CUBLAS_STATUS_ALLOC_FAILED`] error returned or cause large regressions in performance. Workspace size equal to or larger than 16KiB is enough to prevent [`cublasStatus_t::CUBLAS_STATUS_ALLOC_FAILED`] error, while a larger workspace can provide performance benefits for some routines.
    ///
    /// The table below shows the recommended size of user-provided workspace.
    /// This is based on the cuBLAS default workspace pool size which is GPU architecture dependent.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: The `workspace` pointer wasn’t aligned to at least 256 bytes.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_INITIALIZED`]: The library was not initialized.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The stream was set successfully.
    pub fn cublasSetWorkspace_v2(
        handle: cublasHandle_t,
        workspace: *mut ::core::ffi::c_void,
        workspaceSizeInBytes: size_t,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function sets the cuBLAS library stream, which will be used to execute all subsequent calls to the cuBLAS library functions. If the cuBLAS library stream is not set, all kernels use the `default` NULL stream. In particular, this routine can be used to change the stream between kernel launches and then to reset the cuBLAS library stream back to NULL. Additionally this function unconditionally resets the cuBLAS library workspace back to the default workspace pool (see [`cublasSetWorkspace_v2`]).
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_INITIALIZED`]: the library was not initialized.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: the stream was set successfully.
    pub fn cublasSetStream_v2(
        handle: cublasHandle_t,
        streamId: cudaStream_t,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function gets the cuBLAS library stream, which is being used to execute all calls to the cuBLAS library functions. If the cuBLAS library stream is not set, all kernels use the `default` NULL stream.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: `streamId` is NULL.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_INITIALIZED`]: the library was not initialized.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: the stream was returned successfully.
    pub fn cublasGetStream_v2(
        handle: cublasHandle_t,
        streamId: *mut cudaStream_t,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function obtains the pointer mode used by the cuBLAS library. Please see the section on the [`cublasPointerMode_t`] type for more details.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: `mode` is NULL.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_INITIALIZED`]: The library was not initialized.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The pointer mode was obtained successfully.
    pub fn cublasGetPointerMode_v2(
        handle: cublasHandle_t,
        mode: *mut cublasPointerMode_t,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function sets the pointer mode used by the cuBLAS library. The `default` is for the values to be passed by reference on the host. Please see the section on the [`cublasPointerMode_t`] type for more details.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: `mode` is not [`cublasPointerMode_t::CUBLAS_POINTER_MODE_HOST`] or [`cublasPointerMode_t::CUBLAS_POINTER_MODE_DEVICE`].
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_INITIALIZED`]: The library was not initialized.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The pointer mode was set successfully.
    pub fn cublasSetPointerMode_v2(
        handle: cublasHandle_t,
        mode: cublasPointerMode_t,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function queries the atomic mode of a specific cuBLAS context.
    ///
    /// The default atomics mode of default initialized [`cublasHandle_t`] object is [`cublasAtomicsMode_t::CUBLAS_ATOMICS_NOT_ALLOWED`]. Please see the section on the type for more details.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: The argument `mode` is a NULL pointer.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_INITIALIZED`]: The library was not initialized.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The atomics mode was queried successfully.
    pub fn cublasGetAtomicsMode(
        handle: cublasHandle_t,
        mode: *mut cublasAtomicsMode_t,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// Some routines like cublas&lt;t>symv() and cublas&lt;t>hemv() have an alternate implementation that use atomics to cumulate results. This implementation is generally significantly faster but can generate results that are not strictly identical from one run to the others. Mathematically, those different results are not significant but when debugging those differences can be prejudicial.
    ///
    /// This function allows or disallows the usage of atomics in the cuBLAS library for all routines which have an alternate implementation. When not explicitly specified in the documentation of any cuBLAS routine, it means that this routine does not have an alternate implementation that use atomics. When atomics mode is disabled, each cuBLAS routine should produce the same results from one run to the other when called with identical parameters on the same Hardware.
    ///
    /// The default atomics mode of default initialized [`cublasHandle_t`] object is [`cublasAtomicsMode_t::CUBLAS_ATOMICS_NOT_ALLOWED`]. Please see the section on the type for more details.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_INITIALIZED`]: the library was not initialized.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: the atomics mode was set successfully.
    pub fn cublasSetAtomicsMode(
        handle: cublasHandle_t,
        mode: cublasAtomicsMode_t,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function returns the math mode used by the library routines.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: If `mode` is NULL.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_INITIALIZED`]: The library was not initialized.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The math type was returned successfully.
    pub fn cublasGetMathMode(
        handle: cublasHandle_t,
        mode: *mut cublasMath_t,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// The [`cublasSetMathMode`] function enables you to choose the compute precision modes as defined by [`cublasMath_t`]. Users are allowed to set the compute precision mode as a logical combination of them (except the deprecated [`cublasMath_t::CUBLAS_TENSOR_OP_MATH`]). For example, `cublasSetMathMode(handle, CUBLAS_DEFAULT_MATH | CUBLAS_MATH_DISALLOW_REDUCED_PRECISION_REDUCTION)`. Please note that the default math mode is [`cublasMath_t::CUBLAS_DEFAULT_MATH`].
    ///
    /// For matrix and compute precisions allowed for [`cublasGemmEx`] and `cublasLtMatmul` APIs and their strided variants please refer to: [`cublasGemmEx`], [`cublasGemmBatchedEx`], [`cublasGemmStridedBatchedEx`], and `cublasLtMatmul`.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: An invalid value for mode was specified.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_INITIALIZED`]: The library was not initialized.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The math mode was set successfully.
    pub fn cublasSetMathMode(
        handle: cublasHandle_t,
        mode: cublasMath_t,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function obtains the value previously programmed to the library handle.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: smCountTarget is NULL.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_INITIALIZED`]: the library was not initialized.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: SM count target was returned successfully.
    pub fn cublasGetSmCountTarget(
        handle: cublasHandle_t,
        smCountTarget: *mut ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// The [`cublasSetSmCountTarget`] function allows overriding the number of multiprocessors available to the library during kernels execution.
    ///
    /// This option can be used to improve the library performance when cuBLAS routines are known to run concurrently with other work on different CUDA streams. For example, on an NVIDIA A100 GPU, which has 108 multiprocessors, when there is a concurrent kenrel running with grid size of 8, one can use [`cublasSetSmCountTarget`] with `smCountTarget` set to `100` to override the library heuristics to optimize for running on the remaining 100 multiprocessors.
    ///
    /// When set to `0`, the library returns to its default behavior. The input value should not exceed the device’s multiprocessor count, which can be obtained using `cudaDeviceGetAttribute`. Negative values are not accepted.
    ///
    /// The user must ensure thread safety when modifying the library handle with this routine similar to when using [`cublasSetStream_v2`], etc.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: The value of `smCountTarget` outside of the allowed range.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_INITIALIZED`]: The library was not initialized.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: SM count target was set successfully.
    pub fn cublasSetSmCountTarget(
        handle: cublasHandle_t,
        smCountTarget: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function obtains the value previously programmed to the library handle.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: emulationStrategy is NULL.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_INITIALIZED`]: the library was not initialized.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: emulation strategy was returned successfully.
    pub fn cublasGetEmulationStrategy(
        handle: cublasHandle_t,
        emulationStrategy: *mut cublasEmulationStrategy_t,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// The [`cublasSetEmulationStrategy`] function enables you to select how the library should make use of floating point emulation. For more details, please see [`cublasEmulationStrategy_t`].
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: An invalid value for emulation strategy was specified.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_INITIALIZED`]: The library was not initialized.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The emulation strategy was set successfully.
    pub fn cublasSetEmulationStrategy(
        handle: cublasHandle_t,
        emulationStrategy: cublasEmulationStrategy_t,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function obtains the value previously programmed to the library handle.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: mask is NULL.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_INITIALIZED`]: the library was not initialized.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: emulation special values support was returned successfully.
    pub fn cublasGetEmulationSpecialValuesSupport(
        handle: cublasHandle_t,
        mask: *mut cudaEmulationSpecialValuesSupport,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function sets the value previously programmed to the library handle.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: mask is outside of the allowed range.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_INITIALIZED`]: the library was not initialized.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: emulation special values support was set successfully.
    pub fn cublasSetEmulationSpecialValuesSupport(
        handle: cublasHandle_t,
        mask: cudaEmulationSpecialValuesSupport,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function obtains the value previously programmed to the library handle.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: mantissaControl is NULL.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_INITIALIZED`]: the library was not initialized.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: fixed-point emulation mantissa control was returned successfully.
    pub fn cublasGetFixedPointEmulationMantissaControl(
        handle: cublasHandle_t,
        mantissaControl: *mut cudaEmulationMantissaControl,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function sets the value previously programmed to the library handle.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: mantissaControl is outside of the allowed range.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_INITIALIZED`]: the library was not initialized.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: fixed-point emulation mantissa control was set successfully.
    pub fn cublasSetFixedPointEmulationMantissaControl(
        handle: cublasHandle_t,
        mantissaControl: cudaEmulationMantissaControl,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function obtains the value previously programmed to the library handle.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: maxMantissaBitCount is NULL.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_INITIALIZED`]: the library was not initialized.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: maxMantissaBitCount was returned successfully.
    pub fn cublasGetFixedPointEmulationMaxMantissaBitCount(
        handle: cublasHandle_t,
        maxMantissaBitCount: *mut ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function sets the value previously programmed to the library handle.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: maxMantissaBitCount is outside of the allowed range.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_INITIALIZED`]: the library was not initialized.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: maxMantissaBitCount was set successfully.
    pub fn cublasSetFixedPointEmulationMaxMantissaBitCount(
        handle: cublasHandle_t,
        maxMantissaBitCount: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function obtains the value previously programmed to the library handle.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: mantissaBitOffset is NULL.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_INITIALIZED`]: the library was not initialized.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: mantissaBitOffset was returned successfully.
    pub fn cublasGetFixedPointEmulationMantissaBitOffset(
        handle: cublasHandle_t,
        mantissaBitOffset: *mut ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function sets the value previously programmed to the library handle.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: mantissaBitOffset is outside of the allowed range.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_INITIALIZED`]: the library was not initialized.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: mantissaBitOffset was set successfully.
    pub fn cublasSetFixedPointEmulationMantissaBitOffset(
        handle: cublasHandle_t,
        mantissaBitOffset: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function obtains the value previously programmed to the library handle.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: mantissaBitCount is NULL.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_INITIALIZED`]: the library was not initialized.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: mantissaBitCount was returned successfully.
    pub fn cublasGetFixedPointEmulationMantissaBitCountPointer(
        handle: cublasHandle_t,
        mantissaBitCount: *mut *mut ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function sets the value previously programmed to the library handle.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: mantissaBitCount is outside of the allowed range.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_INITIALIZED`]: the library was not initialized.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: mantissaBitCount was set successfully.
    pub fn cublasSetFixedPointEmulationMantissaBitCountPointer(
        handle: cublasHandle_t,
        mantissaBitCount: *mut ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function returns the string representation of a given status.
    ///
    /// # Return value
    ///
    /// - `NULL-terminated string`: The string representation of the `status`.
    pub fn cublasGetStatusName(status: cublasStatus_t) -> *const ::core::ffi::c_char;
}
unsafe extern "C" {
    /// This function returns the description string for a given status.
    ///
    /// # Return value
    ///
    /// - `NULL-terminated string`: The description of the `status`.
    pub fn cublasGetStatusString(status: cublasStatus_t) -> *const ::core::ffi::c_char;
}
unsafe extern "C" {
    /// This function configures logging during runtime. Besides this type of configuration, it is possible to configure logging with special environment variables which will be checked by libcublas.
    ///
    /// # Parameters
    ///
    /// - `logIsOn`: Turn on/off logging completely. By default is off, but is turned on by calling [`cublasSetLoggerCallback`] to user defined callback function.
    /// - `logToStdOut`: Turn on/off logging to standard output I/O stream. By default is off.
    /// - `logToStdErr`: Turn on/off logging to standard error I/O stream. By default is off.
    /// - `logFileName`: Turn on/off logging to file in filesystem specified by it’s name. [`cublasLoggerConfigure`] copies the content of `logFileName`. You should provide null pointer if you are not interested in this type of logging.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The operation completed successfully.
    pub fn cublasLoggerConfigure(
        logIsOn: ::core::ffi::c_int,
        logToStdOut: ::core::ffi::c_int,
        logToStdErr: ::core::ffi::c_int,
        logFileName: *const ::core::ffi::c_char,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function installs a custom user defined callback function via cublas C public API.
    ///
    /// # Parameters
    ///
    /// - `userCallback`: Pointer to user defined callback function.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The operation completed successfully.
    pub fn cublasSetLoggerCallback(userCallback: cublasLogCallback) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function retrieves function pointer to previously installed custom user defined callback function via [`cublasSetLoggerCallback`] or zero otherwise.
    ///
    /// # Parameters
    ///
    /// - `userCallback`: Pointer to user defined callback function.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: `userCallback` is NULL.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: the operation completed successfully.
    pub fn cublasGetLoggerCallback(
        userCallback: *mut cublasLogCallback,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function copies `n` elements from a vector `x` in host memory space to a vector `y` in GPU memory space. Elements in both vectors are assumed to have a size of `elemSize` bytes. The storage spacing between consecutive elements is given by `incx` for the source vector `x` and by `incy` for the destination vector `y`.
    ///
    /// Since column-major format for two-dimensional matrices is assumed, if a vector is part of a matrix, a vector increment equal to `1` accesses a (partial) column of that matrix. Similarly, using an increment equal to the leading dimension of the matrix results in accesses to a (partial) row of that matrix.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: The parameters `incx`, `incy`, or `elemSize` are not positive.
    /// - [`cublasStatus_t::CUBLAS_STATUS_MAPPING_ERROR`]: There was an error accessing GPU memory.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The operation completed successfully.
    pub fn cublasSetVector(
        n: ::core::ffi::c_int,
        elemSize: ::core::ffi::c_int,
        x: *const ::core::ffi::c_void,
        incx: ::core::ffi::c_int,
        devicePtr: *mut ::core::ffi::c_void,
        incy: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function copies `n` elements from a vector `x` in host memory space to a vector `y` in GPU memory space. Elements in both vectors are assumed to have a size of `elemSize` bytes. The storage spacing between consecutive elements is given by `incx` for the source vector `x` and by `incy` for the destination vector `y`.
    ///
    /// Since column-major format for two-dimensional matrices is assumed, if a vector is part of a matrix, a vector increment equal to `1` accesses a (partial) column of that matrix. Similarly, using an increment equal to the leading dimension of the matrix results in accesses to a (partial) row of that matrix.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: The parameters `incx`, `incy`, or `elemSize` are not positive.
    /// - [`cublasStatus_t::CUBLAS_STATUS_MAPPING_ERROR`]: There was an error accessing GPU memory.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The operation completed successfully.
    pub fn cublasSetVector_64(
        n: i64,
        elemSize: i64,
        x: *const ::core::ffi::c_void,
        incx: i64,
        devicePtr: *mut ::core::ffi::c_void,
        incy: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function copies `n` elements from a vector `x` in GPU memory space to a vector `y` in host memory space. Elements in both vectors are assumed to have a size of `elemSize` bytes. The storage spacing between consecutive elements is given by `incx` for the source vector and `incy` for the destination vector `y`.
    ///
    /// Since column-major format for two-dimensional matrices is assumed, if a vector is part of a matrix, a vector increment equal to `1` accesses a (partial) column of that matrix. Similarly, using an increment equal to the leading dimension of the matrix results in accesses to a (partial) row of that matrix.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: The parameters `incx`, `incy`, or `elemSize` are not positive.
    /// - [`cublasStatus_t::CUBLAS_STATUS_MAPPING_ERROR`]: There was an error accessing GPU memory.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The operation completed successfully.
    pub fn cublasGetVector(
        n: ::core::ffi::c_int,
        elemSize: ::core::ffi::c_int,
        x: *const ::core::ffi::c_void,
        incx: ::core::ffi::c_int,
        y: *mut ::core::ffi::c_void,
        incy: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function copies `n` elements from a vector `x` in GPU memory space to a vector `y` in host memory space. Elements in both vectors are assumed to have a size of `elemSize` bytes. The storage spacing between consecutive elements is given by `incx` for the source vector and `incy` for the destination vector `y`.
    ///
    /// Since column-major format for two-dimensional matrices is assumed, if a vector is part of a matrix, a vector increment equal to `1` accesses a (partial) column of that matrix. Similarly, using an increment equal to the leading dimension of the matrix results in accesses to a (partial) row of that matrix.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: The parameters `incx`, `incy`, or `elemSize` are not positive.
    /// - [`cublasStatus_t::CUBLAS_STATUS_MAPPING_ERROR`]: There was an error accessing GPU memory.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The operation completed successfully.
    pub fn cublasGetVector_64(
        n: i64,
        elemSize: i64,
        x: *const ::core::ffi::c_void,
        incx: i64,
        y: *mut ::core::ffi::c_void,
        incy: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function copies a tile of `rows x cols` elements from a matrix `A` in host memory space to a matrix `B` in GPU memory space. It is assumed that each element requires storage of `elemSize` bytes and that both matrices are stored in column-major format, with the leading dimension of the source matrix `A` and destination matrix `B` given in `lda` and `ldb`, respectively. The leading dimension indicates the number of rows of the allocated matrix, even if only a submatrix of it is being used.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: The parameters `rows` or `cols` are negative, or `elemSize`, `lda` `ldb` are not positive.
    /// - [`cublasStatus_t::CUBLAS_STATUS_MAPPING_ERROR`]: There was an error accessing GPU memory.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The operation completed successfully.
    pub fn cublasSetMatrix(
        rows: ::core::ffi::c_int,
        cols: ::core::ffi::c_int,
        elemSize: ::core::ffi::c_int,
        A: *const ::core::ffi::c_void,
        lda: ::core::ffi::c_int,
        B: *mut ::core::ffi::c_void,
        ldb: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function copies a tile of `rows x cols` elements from a matrix `A` in host memory space to a matrix `B` in GPU memory space. It is assumed that each element requires storage of `elemSize` bytes and that both matrices are stored in column-major format, with the leading dimension of the source matrix `A` and destination matrix `B` given in `lda` and `ldb`, respectively. The leading dimension indicates the number of rows of the allocated matrix, even if only a submatrix of it is being used.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: The parameters `rows` or `cols` are negative, or `elemSize`, `lda` `ldb` are not positive.
    /// - [`cublasStatus_t::CUBLAS_STATUS_MAPPING_ERROR`]: There was an error accessing GPU memory.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The operation completed successfully.
    pub fn cublasSetMatrix_64(
        rows: i64,
        cols: i64,
        elemSize: i64,
        A: *const ::core::ffi::c_void,
        lda: i64,
        B: *mut ::core::ffi::c_void,
        ldb: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function copies a tile of `rows x cols` elements from a matrix `A` in GPU memory space to a matrix `B` in host memory space. It is assumed that each element requires storage of `elemSize` bytes and that both matrices are stored in column-major format, with the leading dimension of the source matrix `A` and destination matrix `B` given in `lda` and `ldb`, respectively. The leading dimension indicates the number of rows of the allocated matrix, even if only a submatrix of it is being used.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: The parameters `rows` or `cols` are negative, or `elemSize`, `lda` `ldb` are not positive.
    /// - [`cublasStatus_t::CUBLAS_STATUS_MAPPING_ERROR`]: There was an error accessing GPU memory.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The operation completed successfully.
    pub fn cublasGetMatrix(
        rows: ::core::ffi::c_int,
        cols: ::core::ffi::c_int,
        elemSize: ::core::ffi::c_int,
        A: *const ::core::ffi::c_void,
        lda: ::core::ffi::c_int,
        B: *mut ::core::ffi::c_void,
        ldb: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function copies a tile of `rows x cols` elements from a matrix `A` in GPU memory space to a matrix `B` in host memory space. It is assumed that each element requires storage of `elemSize` bytes and that both matrices are stored in column-major format, with the leading dimension of the source matrix `A` and destination matrix `B` given in `lda` and `ldb`, respectively. The leading dimension indicates the number of rows of the allocated matrix, even if only a submatrix of it is being used.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: The parameters `rows` or `cols` are negative, or `elemSize`, `lda` `ldb` are not positive.
    /// - [`cublasStatus_t::CUBLAS_STATUS_MAPPING_ERROR`]: There was an error accessing GPU memory.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The operation completed successfully.
    pub fn cublasGetMatrix_64(
        rows: i64,
        cols: i64,
        elemSize: i64,
        A: *const ::core::ffi::c_void,
        lda: i64,
        B: *mut ::core::ffi::c_void,
        ldb: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function has the same functionality as [`cublasSetVector`], with the exception that the data transfer is done asynchronously (with respect to the host) using the given CUDA™ stream parameter.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: The parameters `incx`, `incy`, or `elemSize` are not positive.
    /// - [`cublasStatus_t::CUBLAS_STATUS_MAPPING_ERROR`]: There was an error accessing GPU memory.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The operation completed successfully.
    pub fn cublasSetVectorAsync(
        n: ::core::ffi::c_int,
        elemSize: ::core::ffi::c_int,
        hostPtr: *const ::core::ffi::c_void,
        incx: ::core::ffi::c_int,
        devicePtr: *mut ::core::ffi::c_void,
        incy: ::core::ffi::c_int,
        stream: cudaStream_t,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function has the same functionality as [`cublasSetVector`], with the exception that the data transfer is done asynchronously (with respect to the host) using the given CUDA™ stream parameter.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: The parameters `incx`, `incy`, or `elemSize` are not positive.
    /// - [`cublasStatus_t::CUBLAS_STATUS_MAPPING_ERROR`]: There was an error accessing GPU memory.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The operation completed successfully.
    pub fn cublasSetVectorAsync_64(
        n: i64,
        elemSize: i64,
        hostPtr: *const ::core::ffi::c_void,
        incx: i64,
        devicePtr: *mut ::core::ffi::c_void,
        incy: i64,
        stream: cudaStream_t,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function has the same functionality as [`cublasGetVector`], with the exception that the data transfer is done asynchronously (with respect to the host) using the given CUDA™ stream parameter.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: The parameters `incx`, `incy`, or `elemSize` are not positive.
    /// - [`cublasStatus_t::CUBLAS_STATUS_MAPPING_ERROR`]: There was an error accessing GPU memory.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The operation completed successfully.
    pub fn cublasGetVectorAsync(
        n: ::core::ffi::c_int,
        elemSize: ::core::ffi::c_int,
        devicePtr: *const ::core::ffi::c_void,
        incx: ::core::ffi::c_int,
        hostPtr: *mut ::core::ffi::c_void,
        incy: ::core::ffi::c_int,
        stream: cudaStream_t,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function has the same functionality as [`cublasGetVector`], with the exception that the data transfer is done asynchronously (with respect to the host) using the given CUDA™ stream parameter.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: The parameters `incx`, `incy`, or `elemSize` are not positive.
    /// - [`cublasStatus_t::CUBLAS_STATUS_MAPPING_ERROR`]: There was an error accessing GPU memory.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The operation completed successfully.
    pub fn cublasGetVectorAsync_64(
        n: i64,
        elemSize: i64,
        devicePtr: *const ::core::ffi::c_void,
        incx: i64,
        hostPtr: *mut ::core::ffi::c_void,
        incy: i64,
        stream: cudaStream_t,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function has the same functionality as [`cublasSetMatrix`], with the exception that the data transfer is done asynchronously (with respect to the host) using the given CUDA™ stream parameter.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: The parameters `rows` or `cols` are negative, or `elemSize`, `lda` `ldb` are not positive.
    /// - [`cublasStatus_t::CUBLAS_STATUS_MAPPING_ERROR`]: There was an error accessing GPU memory.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The operation completed successfully.
    pub fn cublasSetMatrixAsync(
        rows: ::core::ffi::c_int,
        cols: ::core::ffi::c_int,
        elemSize: ::core::ffi::c_int,
        A: *const ::core::ffi::c_void,
        lda: ::core::ffi::c_int,
        B: *mut ::core::ffi::c_void,
        ldb: ::core::ffi::c_int,
        stream: cudaStream_t,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function has the same functionality as [`cublasSetMatrix`], with the exception that the data transfer is done asynchronously (with respect to the host) using the given CUDA™ stream parameter.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: The parameters `rows` or `cols` are negative, or `elemSize`, `lda` `ldb` are not positive.
    /// - [`cublasStatus_t::CUBLAS_STATUS_MAPPING_ERROR`]: There was an error accessing GPU memory.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The operation completed successfully.
    pub fn cublasSetMatrixAsync_64(
        rows: i64,
        cols: i64,
        elemSize: i64,
        A: *const ::core::ffi::c_void,
        lda: i64,
        B: *mut ::core::ffi::c_void,
        ldb: i64,
        stream: cudaStream_t,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function has the same functionality as [`cublasGetMatrix`], with the exception that the data transfer is done asynchronously (with respect to the host) using the given CUDA™ stream parameter.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: The parameters `rows` or `cols` are negative, or `elemSize`, `lda` `ldb` are not positive.
    /// - [`cublasStatus_t::CUBLAS_STATUS_MAPPING_ERROR`]: There was an error accessing GPU memory.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The operation completed successfully.
    pub fn cublasGetMatrixAsync(
        rows: ::core::ffi::c_int,
        cols: ::core::ffi::c_int,
        elemSize: ::core::ffi::c_int,
        A: *const ::core::ffi::c_void,
        lda: ::core::ffi::c_int,
        B: *mut ::core::ffi::c_void,
        ldb: ::core::ffi::c_int,
        stream: cudaStream_t,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function has the same functionality as [`cublasGetMatrix`], with the exception that the data transfer is done asynchronously (with respect to the host) using the given CUDA™ stream parameter.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: The parameters `rows` or `cols` are negative, or `elemSize`, `lda` `ldb` are not positive.
    /// - [`cublasStatus_t::CUBLAS_STATUS_MAPPING_ERROR`]: There was an error accessing GPU memory.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The operation completed successfully.
    pub fn cublasGetMatrixAsync_64(
        rows: i64,
        cols: i64,
        elemSize: i64,
        A: *const ::core::ffi::c_void,
        lda: i64,
        B: *mut ::core::ffi::c_void,
        ldb: i64,
        stream: cudaStream_t,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    pub fn cublasXerbla(srName: *const ::core::ffi::c_char, info: ::core::ffi::c_int);
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function is an API generalization of the routine cublas&lt;t>nrm2() where input data, output data and compute type can be specified independently.
    ///
    /// This function computes the Euclidean norm of the vector `x`. The code uses a multiphase model of accumulation to avoid intermediate underflow and overflow, with the result being equivalent to $\sqrt{\sum_{i = 1}^{n}\left( {\mathbf{x}\lbrack j\rbrack \times \mathbf{x}\lbrack j\rbrack} \right)}$ where $j = 1 + \left( {i - 1} \right)\\*\text{incx}$ in exact arithmetic. Notice that the last equation reflects 1-based indexing used for compatibility with Fortran.
    ///
    /// The datatypes combinations currently supported for [`cublasNrm2Ex`] are listed below :
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [snrm2()](http://www.netlib.org/blas/snrm2.f90), [dnrm2()](http://www.netlib.org/blas/dnrm2.f90), [scnrm2()](http://www.netlib.org/blas/scnrm2.f90), [dznrm2()](http://www.netlib.org/blas/dznrm2.f90).
    ///
    /// # Parameters
    ///
    /// - `handle`: Handle to the cuBLAS library context.
    /// - `n`: Number of elements in the vector `x`.
    /// - `x`: &lt;`type`> vector with `n` elements.
    /// - `xType`: Enumerant specifying the datatype of vector `x`.
    /// - `incx`: Stride between consecutive elements of `x`.
    /// - `result`: The resulting norm, which is set to `0` if `n &lt;= 0` or `incx &lt;= 0`.
    /// - `resultType`: Enumerant specifying the datatype of the `result`.
    /// - `executionType`: Enumerant specifying the datatype in which the computation is executed.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_ALLOC_FAILED`]: The reduction buffer could not be allocated.
    /// - [`cublasStatus_t::CUBLAS_STATUS_EXECUTION_FAILED`]: The function failed to launch on the GPU.
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: If `xType` or `resultType` or `executionType` is not supported, or if `result` is NULL.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_INITIALIZED`]: The library was not initialized.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_SUPPORTED`]: The combination of the parameters `xType`, `resultType` and `executionType` is not supported.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The operation completed successfully.
    pub fn cublasNrm2Ex(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        x: *const ::core::ffi::c_void,
        xType: cudaDataType,
        incx: ::core::ffi::c_int,
        result: *mut ::core::ffi::c_void,
        resultType: cudaDataType,
        executionType: cudaDataType,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function is an API generalization of the routine cublas&lt;t>nrm2() where input data, output data and compute type can be specified independently.
    ///
    /// This function computes the Euclidean norm of the vector `x`. The code uses a multiphase model of accumulation to avoid intermediate underflow and overflow, with the result being equivalent to $\sqrt{\sum_{i = 1}^{n}\left( {\mathbf{x}\lbrack j\rbrack \times \mathbf{x}\lbrack j\rbrack} \right)}$ where $j = 1 + \left( {i - 1} \right)\\*\text{incx}$ in exact arithmetic. Notice that the last equation reflects 1-based indexing used for compatibility with Fortran.
    ///
    /// The datatypes combinations currently supported for [`cublasNrm2Ex`] are listed below :
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [snrm2()](http://www.netlib.org/blas/snrm2.f90), [dnrm2()](http://www.netlib.org/blas/dnrm2.f90), [scnrm2()](http://www.netlib.org/blas/scnrm2.f90), [dznrm2()](http://www.netlib.org/blas/dznrm2.f90).
    ///
    /// # Parameters
    ///
    /// - `handle`: Handle to the cuBLAS library context.
    /// - `n`: Number of elements in the vector `x`.
    /// - `x`: &lt;`type`> vector with `n` elements.
    /// - `xType`: Enumerant specifying the datatype of vector `x`.
    /// - `incx`: Stride between consecutive elements of `x`.
    /// - `result`: The resulting norm, which is set to `0` if `n &lt;= 0` or `incx &lt;= 0`.
    /// - `resultType`: Enumerant specifying the datatype of the `result`.
    /// - `executionType`: Enumerant specifying the datatype in which the computation is executed.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_ALLOC_FAILED`]: The reduction buffer could not be allocated.
    /// - [`cublasStatus_t::CUBLAS_STATUS_EXECUTION_FAILED`]: The function failed to launch on the GPU.
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: If `xType` or `resultType` or `executionType` is not supported, or if `result` is NULL.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_INITIALIZED`]: The library was not initialized.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_SUPPORTED`]: The combination of the parameters `xType`, `resultType` and `executionType` is not supported.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The operation completed successfully.
    pub fn cublasNrm2Ex_64(
        handle: cublasHandle_t,
        n: i64,
        x: *const ::core::ffi::c_void,
        xType: cudaDataType,
        incx: i64,
        result: *mut ::core::ffi::c_void,
        resultType: cudaDataType,
        executionType: cudaDataType,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function computes the Euclidean norm of the vector `x`. The code uses a multiphase model of accumulation to avoid intermediate underflow and overflow, with the result being equivalent to $\sqrt{\sum_{i = 1}^{n}\left( {\mathbf{x}\lbrack j\rbrack \times \mathbf{x}\lbrack j\rbrack} \right)}$ where $j = 1 + \left( {i - 1} \right)\\*\text{incx}$ in exact arithmetic. Notice that the last equation reflects 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [snrm2()](http://www.netlib.org/blas/snrm2.f90), [dnrm2()](http://www.netlib.org/blas/dnrm2.f90), [scnrm2()](http://www.netlib.org/blas/scnrm2.f90), [dznrm2()](http://www.netlib.org/blas/dznrm2.f90).
    pub fn cublasSnrm2_v2(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        x: *const f32,
        incx: ::core::ffi::c_int,
        result: *mut f32,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function computes the Euclidean norm of the vector `x`. The code uses a multiphase model of accumulation to avoid intermediate underflow and overflow, with the result being equivalent to $\sqrt{\sum_{i = 1}^{n}\left( {\mathbf{x}\lbrack j\rbrack \times \mathbf{x}\lbrack j\rbrack} \right)}$ where $j = 1 + \left( {i - 1} \right)\\*\text{incx}$ in exact arithmetic. Notice that the last equation reflects 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [snrm2()](http://www.netlib.org/blas/snrm2.f90), [dnrm2()](http://www.netlib.org/blas/dnrm2.f90), [scnrm2()](http://www.netlib.org/blas/scnrm2.f90), [dznrm2()](http://www.netlib.org/blas/dznrm2.f90).
    pub fn cublasSnrm2_v2_64(
        handle: cublasHandle_t,
        n: i64,
        x: *const f32,
        incx: i64,
        result: *mut f32,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function computes the Euclidean norm of the vector `x`. The code uses a multiphase model of accumulation to avoid intermediate underflow and overflow, with the result being equivalent to $\sqrt{\sum_{i = 1}^{n}\left( {\mathbf{x}\lbrack j\rbrack \times \mathbf{x}\lbrack j\rbrack} \right)}$ where $j = 1 + \left( {i - 1} \right)\\*\text{incx}$ in exact arithmetic. Notice that the last equation reflects 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [snrm2()](http://www.netlib.org/blas/snrm2.f90), [dnrm2()](http://www.netlib.org/blas/dnrm2.f90), [scnrm2()](http://www.netlib.org/blas/scnrm2.f90), [dznrm2()](http://www.netlib.org/blas/dznrm2.f90).
    pub fn cublasDnrm2_v2(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        x: *const f64,
        incx: ::core::ffi::c_int,
        result: *mut f64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function computes the Euclidean norm of the vector `x`. The code uses a multiphase model of accumulation to avoid intermediate underflow and overflow, with the result being equivalent to $\sqrt{\sum_{i = 1}^{n}\left( {\mathbf{x}\lbrack j\rbrack \times \mathbf{x}\lbrack j\rbrack} \right)}$ where $j = 1 + \left( {i - 1} \right)\\*\text{incx}$ in exact arithmetic. Notice that the last equation reflects 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [snrm2()](http://www.netlib.org/blas/snrm2.f90), [dnrm2()](http://www.netlib.org/blas/dnrm2.f90), [scnrm2()](http://www.netlib.org/blas/scnrm2.f90), [dznrm2()](http://www.netlib.org/blas/dznrm2.f90).
    pub fn cublasDnrm2_v2_64(
        handle: cublasHandle_t,
        n: i64,
        x: *const f64,
        incx: i64,
        result: *mut f64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function computes the Euclidean norm of the vector `x`. The code uses a multiphase model of accumulation to avoid intermediate underflow and overflow, with the result being equivalent to $\sqrt{\sum_{i = 1}^{n}\left( {\mathbf{x}\lbrack j\rbrack \times \mathbf{x}\lbrack j\rbrack} \right)}$ where $j = 1 + \left( {i - 1} \right)\\*\text{incx}$ in exact arithmetic. Notice that the last equation reflects 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [snrm2()](http://www.netlib.org/blas/snrm2.f90), [dnrm2()](http://www.netlib.org/blas/dnrm2.f90), [scnrm2()](http://www.netlib.org/blas/scnrm2.f90), [dznrm2()](http://www.netlib.org/blas/dznrm2.f90).
    pub fn cublasScnrm2_v2(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        x: *const cuComplex,
        incx: ::core::ffi::c_int,
        result: *mut f32,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function computes the Euclidean norm of the vector `x`. The code uses a multiphase model of accumulation to avoid intermediate underflow and overflow, with the result being equivalent to $\sqrt{\sum_{i = 1}^{n}\left( {\mathbf{x}\lbrack j\rbrack \times \mathbf{x}\lbrack j\rbrack} \right)}$ where $j = 1 + \left( {i - 1} \right)\\*\text{incx}$ in exact arithmetic. Notice that the last equation reflects 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [snrm2()](http://www.netlib.org/blas/snrm2.f90), [dnrm2()](http://www.netlib.org/blas/dnrm2.f90), [scnrm2()](http://www.netlib.org/blas/scnrm2.f90), [dznrm2()](http://www.netlib.org/blas/dznrm2.f90).
    pub fn cublasScnrm2_v2_64(
        handle: cublasHandle_t,
        n: i64,
        x: *const cuComplex,
        incx: i64,
        result: *mut f32,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function computes the Euclidean norm of the vector `x`. The code uses a multiphase model of accumulation to avoid intermediate underflow and overflow, with the result being equivalent to $\sqrt{\sum_{i = 1}^{n}\left( {\mathbf{x}\lbrack j\rbrack \times \mathbf{x}\lbrack j\rbrack} \right)}$ where $j = 1 + \left( {i - 1} \right)\\*\text{incx}$ in exact arithmetic. Notice that the last equation reflects 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [snrm2()](http://www.netlib.org/blas/snrm2.f90), [dnrm2()](http://www.netlib.org/blas/dnrm2.f90), [scnrm2()](http://www.netlib.org/blas/scnrm2.f90), [dznrm2()](http://www.netlib.org/blas/dznrm2.f90).
    pub fn cublasDznrm2_v2(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        x: *const cuDoubleComplex,
        incx: ::core::ffi::c_int,
        result: *mut f64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function computes the Euclidean norm of the vector `x`. The code uses a multiphase model of accumulation to avoid intermediate underflow and overflow, with the result being equivalent to $\sqrt{\sum_{i = 1}^{n}\left( {\mathbf{x}\lbrack j\rbrack \times \mathbf{x}\lbrack j\rbrack} \right)}$ where $j = 1 + \left( {i - 1} \right)\\*\text{incx}$ in exact arithmetic. Notice that the last equation reflects 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [snrm2()](http://www.netlib.org/blas/snrm2.f90), [dnrm2()](http://www.netlib.org/blas/dnrm2.f90), [scnrm2()](http://www.netlib.org/blas/scnrm2.f90), [dznrm2()](http://www.netlib.org/blas/dznrm2.f90).
    pub fn cublasDznrm2_v2_64(
        handle: cublasHandle_t,
        n: i64,
        x: *const cuDoubleComplex,
        incx: i64,
        result: *mut f64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// These functions support the 64-bit Integer Interface.
    ///
    /// These functions are an API generalization of the routines cublas&lt;t>dot() and cublas&lt;t>dotc() where input data, output data and compute type can be specified independently. Note: cublas&lt;t>dotc() is dot product conjugated, cublas&lt;t>dotu() is dot product unconjugated.
    ///
    /// This function computes the dot product of vectors `x` and `y`. Hence, the result is $\sum_{i = 1}^{n}\left( {\mathbf{x}\lbrack k\rbrack \times \mathbf{y}\lbrack j\rbrack} \right)$ where $k = 1 + \left( {i - 1} \right)\\*\text{incx}$ and $j = 1 + \left( {i - 1} \right)\\*\text{incy}$. Notice that in the first equation the conjugate of the element of vector x should be used if the function name ends in character ‘c’ and that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// The datatypes combinations currently supported for [`cublasDotEx`] and `cublasDotcEx`(cublasDotEx()) are listed below:
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sdot()](http://www.netlib.org/blas/sdot.f), [ddot()](http://www.netlib.org/blas/ddot.f), [cdotu()](http://www.netlib.org/blas/cdotu.f), [cdotc()](http://www.netlib.org/blas/cdotc.f), [zdotu()](http://www.netlib.org/blas/zdotu.f), [zdotc()](http://www.netlib.org/blas/zdotc.f).
    ///
    /// # Parameters
    ///
    /// - `handle`: Handle to the cuBLAS library context.
    /// - `n`: Number of elements in the vectors `x` and `y`.
    /// - `x`: &lt;`type`> vector with `n` elements.
    /// - `xType`: Enumerant specifying the datatype of vector `x`.
    /// - `incx`: Stride between consecutive elements of `x`.
    /// - `y`: &lt;`type`> vector with `n` elements.
    /// - `yType`: Enumerant specifying the datatype of vector `y`.
    /// - `incy`: Stride between consecutive elements of `y`.
    /// - `result`: The resulting dot product, which is set to `0` if `n &lt;= 0`.
    /// - `resultType`: Enumerant specifying the datatype of the `result`.
    /// - `executionType`: Enumerant specifying the datatype in which the computation is executed.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_ALLOC_FAILED`]: The reduction buffer could not be allocated.
    /// - [`cublasStatus_t::CUBLAS_STATUS_EXECUTION_FAILED`]: The function failed to launch on the GPU.
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: `xType` or `yType` or `resultType` or `executionType` is not supported.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_INITIALIZED`]: The library was not initialized.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_SUPPORTED`]: The combination of the parameters `xType`, `yType`, `resultType` and `executionType` is not supported.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The operation completed successfully.
    pub fn cublasDotEx(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        x: *const ::core::ffi::c_void,
        xType: cudaDataType,
        incx: ::core::ffi::c_int,
        y: *const ::core::ffi::c_void,
        yType: cudaDataType,
        incy: ::core::ffi::c_int,
        result: *mut ::core::ffi::c_void,
        resultType: cudaDataType,
        executionType: cudaDataType,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// These functions support the 64-bit Integer Interface.
    ///
    /// These functions are an API generalization of the routines cublas&lt;t>dot() and cublas&lt;t>dotc() where input data, output data and compute type can be specified independently. Note: cublas&lt;t>dotc() is dot product conjugated, cublas&lt;t>dotu() is dot product unconjugated.
    ///
    /// This function computes the dot product of vectors `x` and `y`. Hence, the result is $\sum_{i = 1}^{n}\left( {\mathbf{x}\lbrack k\rbrack \times \mathbf{y}\lbrack j\rbrack} \right)$ where $k = 1 + \left( {i - 1} \right)\\*\text{incx}$ and $j = 1 + \left( {i - 1} \right)\\*\text{incy}$. Notice that in the first equation the conjugate of the element of vector x should be used if the function name ends in character ‘c’ and that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// The datatypes combinations currently supported for [`cublasDotEx`] and `cublasDotcEx`(cublasDotEx()) are listed below:
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sdot()](http://www.netlib.org/blas/sdot.f), [ddot()](http://www.netlib.org/blas/ddot.f), [cdotu()](http://www.netlib.org/blas/cdotu.f), [cdotc()](http://www.netlib.org/blas/cdotc.f), [zdotu()](http://www.netlib.org/blas/zdotu.f), [zdotc()](http://www.netlib.org/blas/zdotc.f).
    ///
    /// # Parameters
    ///
    /// - `handle`: Handle to the cuBLAS library context.
    /// - `n`: Number of elements in the vectors `x` and `y`.
    /// - `x`: &lt;`type`> vector with `n` elements.
    /// - `xType`: Enumerant specifying the datatype of vector `x`.
    /// - `incx`: Stride between consecutive elements of `x`.
    /// - `y`: &lt;`type`> vector with `n` elements.
    /// - `yType`: Enumerant specifying the datatype of vector `y`.
    /// - `incy`: Stride between consecutive elements of `y`.
    /// - `result`: The resulting dot product, which is set to `0` if `n &lt;= 0`.
    /// - `resultType`: Enumerant specifying the datatype of the `result`.
    /// - `executionType`: Enumerant specifying the datatype in which the computation is executed.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_ALLOC_FAILED`]: The reduction buffer could not be allocated.
    /// - [`cublasStatus_t::CUBLAS_STATUS_EXECUTION_FAILED`]: The function failed to launch on the GPU.
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: `xType` or `yType` or `resultType` or `executionType` is not supported.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_INITIALIZED`]: The library was not initialized.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_SUPPORTED`]: The combination of the parameters `xType`, `yType`, `resultType` and `executionType` is not supported.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The operation completed successfully.
    pub fn cublasDotEx_64(
        handle: cublasHandle_t,
        n: i64,
        x: *const ::core::ffi::c_void,
        xType: cudaDataType,
        incx: i64,
        y: *const ::core::ffi::c_void,
        yType: cudaDataType,
        incy: i64,
        result: *mut ::core::ffi::c_void,
        resultType: cudaDataType,
        executionType: cudaDataType,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    pub fn cublasDotcEx(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        x: *const ::core::ffi::c_void,
        xType: cudaDataType,
        incx: ::core::ffi::c_int,
        y: *const ::core::ffi::c_void,
        yType: cudaDataType,
        incy: ::core::ffi::c_int,
        result: *mut ::core::ffi::c_void,
        resultType: cudaDataType,
        executionType: cudaDataType,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    pub fn cublasDotcEx_64(
        handle: cublasHandle_t,
        n: i64,
        x: *const ::core::ffi::c_void,
        xType: cudaDataType,
        incx: i64,
        y: *const ::core::ffi::c_void,
        yType: cudaDataType,
        incy: i64,
        result: *mut ::core::ffi::c_void,
        resultType: cudaDataType,
        executionType: cudaDataType,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function computes the dot product of vectors `x` and `y`. Hence, the result is $\sum_{i = 1}^{n}\left( {\mathbf{x}\lbrack k\rbrack \times \mathbf{y}\lbrack j\rbrack} \right)$ where $k = 1 + \left( {i - 1} \right)\\*\text{incx}$ and $j = 1 + \left( {i - 1} \right)\\*\text{incy}$. Notice that in the first equation the conjugate of the element of vector x should be used if the function name ends in character ‘c’ and that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sdot()](http://www.netlib.org/blas/sdot.f), [ddot()](http://www.netlib.org/blas/ddot.f), [cdotu()](http://www.netlib.org/blas/cdotu.f), [cdotc()](http://www.netlib.org/blas/cdotc.f), [zdotu()](http://www.netlib.org/blas/zdotu.f), [zdotc()](http://www.netlib.org/blas/zdotc.f).
    pub fn cublasSdot_v2(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        x: *const f32,
        incx: ::core::ffi::c_int,
        y: *const f32,
        incy: ::core::ffi::c_int,
        result: *mut f32,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function computes the dot product of vectors `x` and `y`. Hence, the result is $\sum_{i = 1}^{n}\left( {\mathbf{x}\lbrack k\rbrack \times \mathbf{y}\lbrack j\rbrack} \right)$ where $k = 1 + \left( {i - 1} \right)\\*\text{incx}$ and $j = 1 + \left( {i - 1} \right)\\*\text{incy}$. Notice that in the first equation the conjugate of the element of vector x should be used if the function name ends in character ‘c’ and that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sdot()](http://www.netlib.org/blas/sdot.f), [ddot()](http://www.netlib.org/blas/ddot.f), [cdotu()](http://www.netlib.org/blas/cdotu.f), [cdotc()](http://www.netlib.org/blas/cdotc.f), [zdotu()](http://www.netlib.org/blas/zdotu.f), [zdotc()](http://www.netlib.org/blas/zdotc.f).
    pub fn cublasSdot_v2_64(
        handle: cublasHandle_t,
        n: i64,
        x: *const f32,
        incx: i64,
        y: *const f32,
        incy: i64,
        result: *mut f32,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function computes the dot product of vectors `x` and `y`. Hence, the result is $\sum_{i = 1}^{n}\left( {\mathbf{x}\lbrack k\rbrack \times \mathbf{y}\lbrack j\rbrack} \right)$ where $k = 1 + \left( {i - 1} \right)\\*\text{incx}$ and $j = 1 + \left( {i - 1} \right)\\*\text{incy}$. Notice that in the first equation the conjugate of the element of vector x should be used if the function name ends in character ‘c’ and that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sdot()](http://www.netlib.org/blas/sdot.f), [ddot()](http://www.netlib.org/blas/ddot.f), [cdotu()](http://www.netlib.org/blas/cdotu.f), [cdotc()](http://www.netlib.org/blas/cdotc.f), [zdotu()](http://www.netlib.org/blas/zdotu.f), [zdotc()](http://www.netlib.org/blas/zdotc.f).
    pub fn cublasDdot_v2(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        x: *const f64,
        incx: ::core::ffi::c_int,
        y: *const f64,
        incy: ::core::ffi::c_int,
        result: *mut f64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function computes the dot product of vectors `x` and `y`. Hence, the result is $\sum_{i = 1}^{n}\left( {\mathbf{x}\lbrack k\rbrack \times \mathbf{y}\lbrack j\rbrack} \right)$ where $k = 1 + \left( {i - 1} \right)\\*\text{incx}$ and $j = 1 + \left( {i - 1} \right)\\*\text{incy}$. Notice that in the first equation the conjugate of the element of vector x should be used if the function name ends in character ‘c’ and that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sdot()](http://www.netlib.org/blas/sdot.f), [ddot()](http://www.netlib.org/blas/ddot.f), [cdotu()](http://www.netlib.org/blas/cdotu.f), [cdotc()](http://www.netlib.org/blas/cdotc.f), [zdotu()](http://www.netlib.org/blas/zdotu.f), [zdotc()](http://www.netlib.org/blas/zdotc.f).
    pub fn cublasDdot_v2_64(
        handle: cublasHandle_t,
        n: i64,
        x: *const f64,
        incx: i64,
        y: *const f64,
        incy: i64,
        result: *mut f64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    pub fn cublasCdotu_v2(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        x: *const cuComplex,
        incx: ::core::ffi::c_int,
        y: *const cuComplex,
        incy: ::core::ffi::c_int,
        result: *mut cuComplex,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    pub fn cublasCdotu_v2_64(
        handle: cublasHandle_t,
        n: i64,
        x: *const cuComplex,
        incx: i64,
        y: *const cuComplex,
        incy: i64,
        result: *mut cuComplex,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    pub fn cublasCdotc_v2(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        x: *const cuComplex,
        incx: ::core::ffi::c_int,
        y: *const cuComplex,
        incy: ::core::ffi::c_int,
        result: *mut cuComplex,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    pub fn cublasCdotc_v2_64(
        handle: cublasHandle_t,
        n: i64,
        x: *const cuComplex,
        incx: i64,
        y: *const cuComplex,
        incy: i64,
        result: *mut cuComplex,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    pub fn cublasZdotu_v2(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        x: *const cuDoubleComplex,
        incx: ::core::ffi::c_int,
        y: *const cuDoubleComplex,
        incy: ::core::ffi::c_int,
        result: *mut cuDoubleComplex,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    pub fn cublasZdotu_v2_64(
        handle: cublasHandle_t,
        n: i64,
        x: *const cuDoubleComplex,
        incx: i64,
        y: *const cuDoubleComplex,
        incy: i64,
        result: *mut cuDoubleComplex,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    pub fn cublasZdotc_v2(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        x: *const cuDoubleComplex,
        incx: ::core::ffi::c_int,
        y: *const cuDoubleComplex,
        incy: ::core::ffi::c_int,
        result: *mut cuDoubleComplex,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    pub fn cublasZdotc_v2_64(
        handle: cublasHandle_t,
        n: i64,
        x: *const cuDoubleComplex,
        incx: i64,
        y: *const cuDoubleComplex,
        incy: i64,
        result: *mut cuDoubleComplex,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function scales the vector `x` by the scalar $\alpha$ and overwrites it with the result. Hence, the performed operation is $\mathbf{x}\lbrack j\rbrack = \alpha \times \mathbf{x}\lbrack j\rbrack$ for $i = 1,\ldots,n$ and $j = 1 + \left( {i - 1} \right)\\*\text{incx}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// The datatypes combinations currently supported for [`cublasScalEx`] are listed below :
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sscal()](http://www.netlib.org/blas/sscal.f), [dscal()](http://www.netlib.org/blas/dscal.f), [csscal()](http://www.netlib.org/blas/csscal.f), [cscal()](http://www.netlib.org/blas/cscal.f), [zdscal()](http://www.netlib.org/blas/zdscal.f), [zscal()](http://www.netlib.org/blas/zscal.f).
    ///
    /// # Parameters
    ///
    /// - `handle`: Handle to the cuBLAS library context.
    /// - `n`: Number of elements in the vector `x`.
    /// - `alpha`: &lt;`type`> scalar used for multiplication.
    /// - `alphaType`: Enumerant specifying the datatype of scalar `alpha`.
    /// - `x`: &lt;`type`> vector with `n` elements.
    /// - `xType`: Enumerant specifying the datatype of vector `x`.
    /// - `incx`: Stride between consecutive elements of `x`.
    /// - `executionType`: Enumerant specifying the datatype in which the computation is executed.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_EXECUTION_FAILED`]: The function failed to launch on the GPU.
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: `alphaType` or `xType` or `executionType` is not supported.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_INITIALIZED`]: The library was not initialized.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_SUPPORTED`]: The combination of the parameters `xType` and `executionType` is not supported.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The operation completed successfully.
    pub fn cublasScalEx(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        alpha: *const ::core::ffi::c_void,
        alphaType: cudaDataType,
        x: *mut ::core::ffi::c_void,
        xType: cudaDataType,
        incx: ::core::ffi::c_int,
        executionType: cudaDataType,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function scales the vector `x` by the scalar $\alpha$ and overwrites it with the result. Hence, the performed operation is $\mathbf{x}\lbrack j\rbrack = \alpha \times \mathbf{x}\lbrack j\rbrack$ for $i = 1,\ldots,n$ and $j = 1 + \left( {i - 1} \right)\\*\text{incx}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// The datatypes combinations currently supported for [`cublasScalEx`] are listed below :
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sscal()](http://www.netlib.org/blas/sscal.f), [dscal()](http://www.netlib.org/blas/dscal.f), [csscal()](http://www.netlib.org/blas/csscal.f), [cscal()](http://www.netlib.org/blas/cscal.f), [zdscal()](http://www.netlib.org/blas/zdscal.f), [zscal()](http://www.netlib.org/blas/zscal.f).
    ///
    /// # Parameters
    ///
    /// - `handle`: Handle to the cuBLAS library context.
    /// - `n`: Number of elements in the vector `x`.
    /// - `alpha`: &lt;`type`> scalar used for multiplication.
    /// - `alphaType`: Enumerant specifying the datatype of scalar `alpha`.
    /// - `x`: &lt;`type`> vector with `n` elements.
    /// - `xType`: Enumerant specifying the datatype of vector `x`.
    /// - `incx`: Stride between consecutive elements of `x`.
    /// - `executionType`: Enumerant specifying the datatype in which the computation is executed.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_EXECUTION_FAILED`]: The function failed to launch on the GPU.
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: `alphaType` or `xType` or `executionType` is not supported.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_INITIALIZED`]: The library was not initialized.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_SUPPORTED`]: The combination of the parameters `xType` and `executionType` is not supported.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The operation completed successfully.
    pub fn cublasScalEx_64(
        handle: cublasHandle_t,
        n: i64,
        alpha: *const ::core::ffi::c_void,
        alphaType: cudaDataType,
        x: *mut ::core::ffi::c_void,
        xType: cudaDataType,
        incx: i64,
        executionType: cudaDataType,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function scales the vector `x` by the scalar $\alpha$ and overwrites it with the result. Hence, the performed operation is $\mathbf{x}\lbrack j\rbrack = \alpha \times \mathbf{x}\lbrack j\rbrack$ for $i = 1,\ldots,n$ and $j = 1 + \left( {i - 1} \right)\\*\text{incx}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sscal()](http://www.netlib.org/blas/sscal.f), [dscal()](http://www.netlib.org/blas/dscal.f), [csscal()](http://www.netlib.org/blas/csscal.f), [cscal()](http://www.netlib.org/blas/cscal.f), [zdscal()](http://www.netlib.org/blas/zdscal.f), [zscal()](http://www.netlib.org/blas/zscal.f).
    pub fn cublasSscal_v2(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        alpha: *const f32,
        x: *mut f32,
        incx: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function scales the vector `x` by the scalar $\alpha$ and overwrites it with the result. Hence, the performed operation is $\mathbf{x}\lbrack j\rbrack = \alpha \times \mathbf{x}\lbrack j\rbrack$ for $i = 1,\ldots,n$ and $j = 1 + \left( {i - 1} \right)\\*\text{incx}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sscal()](http://www.netlib.org/blas/sscal.f), [dscal()](http://www.netlib.org/blas/dscal.f), [csscal()](http://www.netlib.org/blas/csscal.f), [cscal()](http://www.netlib.org/blas/cscal.f), [zdscal()](http://www.netlib.org/blas/zdscal.f), [zscal()](http://www.netlib.org/blas/zscal.f).
    pub fn cublasSscal_v2_64(
        handle: cublasHandle_t,
        n: i64,
        alpha: *const f32,
        x: *mut f32,
        incx: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function scales the vector `x` by the scalar $\alpha$ and overwrites it with the result. Hence, the performed operation is $\mathbf{x}\lbrack j\rbrack = \alpha \times \mathbf{x}\lbrack j\rbrack$ for $i = 1,\ldots,n$ and $j = 1 + \left( {i - 1} \right)\\*\text{incx}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sscal()](http://www.netlib.org/blas/sscal.f), [dscal()](http://www.netlib.org/blas/dscal.f), [csscal()](http://www.netlib.org/blas/csscal.f), [cscal()](http://www.netlib.org/blas/cscal.f), [zdscal()](http://www.netlib.org/blas/zdscal.f), [zscal()](http://www.netlib.org/blas/zscal.f).
    pub fn cublasDscal_v2(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        alpha: *const f64,
        x: *mut f64,
        incx: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function scales the vector `x` by the scalar $\alpha$ and overwrites it with the result. Hence, the performed operation is $\mathbf{x}\lbrack j\rbrack = \alpha \times \mathbf{x}\lbrack j\rbrack$ for $i = 1,\ldots,n$ and $j = 1 + \left( {i - 1} \right)\\*\text{incx}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sscal()](http://www.netlib.org/blas/sscal.f), [dscal()](http://www.netlib.org/blas/dscal.f), [csscal()](http://www.netlib.org/blas/csscal.f), [cscal()](http://www.netlib.org/blas/cscal.f), [zdscal()](http://www.netlib.org/blas/zdscal.f), [zscal()](http://www.netlib.org/blas/zscal.f).
    pub fn cublasDscal_v2_64(
        handle: cublasHandle_t,
        n: i64,
        alpha: *const f64,
        x: *mut f64,
        incx: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function scales the vector `x` by the scalar $\alpha$ and overwrites it with the result. Hence, the performed operation is $\mathbf{x}\lbrack j\rbrack = \alpha \times \mathbf{x}\lbrack j\rbrack$ for $i = 1,\ldots,n$ and $j = 1 + \left( {i - 1} \right)\\*\text{incx}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sscal()](http://www.netlib.org/blas/sscal.f), [dscal()](http://www.netlib.org/blas/dscal.f), [csscal()](http://www.netlib.org/blas/csscal.f), [cscal()](http://www.netlib.org/blas/cscal.f), [zdscal()](http://www.netlib.org/blas/zdscal.f), [zscal()](http://www.netlib.org/blas/zscal.f).
    pub fn cublasCscal_v2(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        alpha: *const cuComplex,
        x: *mut cuComplex,
        incx: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function scales the vector `x` by the scalar $\alpha$ and overwrites it with the result. Hence, the performed operation is $\mathbf{x}\lbrack j\rbrack = \alpha \times \mathbf{x}\lbrack j\rbrack$ for $i = 1,\ldots,n$ and $j = 1 + \left( {i - 1} \right)\\*\text{incx}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sscal()](http://www.netlib.org/blas/sscal.f), [dscal()](http://www.netlib.org/blas/dscal.f), [csscal()](http://www.netlib.org/blas/csscal.f), [cscal()](http://www.netlib.org/blas/cscal.f), [zdscal()](http://www.netlib.org/blas/zdscal.f), [zscal()](http://www.netlib.org/blas/zscal.f).
    pub fn cublasCscal_v2_64(
        handle: cublasHandle_t,
        n: i64,
        alpha: *const cuComplex,
        x: *mut cuComplex,
        incx: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function scales the vector `x` by the scalar $\alpha$ and overwrites it with the result. Hence, the performed operation is $\mathbf{x}\lbrack j\rbrack = \alpha \times \mathbf{x}\lbrack j\rbrack$ for $i = 1,\ldots,n$ and $j = 1 + \left( {i - 1} \right)\\*\text{incx}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sscal()](http://www.netlib.org/blas/sscal.f), [dscal()](http://www.netlib.org/blas/dscal.f), [csscal()](http://www.netlib.org/blas/csscal.f), [cscal()](http://www.netlib.org/blas/cscal.f), [zdscal()](http://www.netlib.org/blas/zdscal.f), [zscal()](http://www.netlib.org/blas/zscal.f).
    pub fn cublasCsscal_v2(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        alpha: *const f32,
        x: *mut cuComplex,
        incx: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function scales the vector `x` by the scalar $\alpha$ and overwrites it with the result. Hence, the performed operation is $\mathbf{x}\lbrack j\rbrack = \alpha \times \mathbf{x}\lbrack j\rbrack$ for $i = 1,\ldots,n$ and $j = 1 + \left( {i - 1} \right)\\*\text{incx}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sscal()](http://www.netlib.org/blas/sscal.f), [dscal()](http://www.netlib.org/blas/dscal.f), [csscal()](http://www.netlib.org/blas/csscal.f), [cscal()](http://www.netlib.org/blas/cscal.f), [zdscal()](http://www.netlib.org/blas/zdscal.f), [zscal()](http://www.netlib.org/blas/zscal.f).
    pub fn cublasCsscal_v2_64(
        handle: cublasHandle_t,
        n: i64,
        alpha: *const f32,
        x: *mut cuComplex,
        incx: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function scales the vector `x` by the scalar $\alpha$ and overwrites it with the result. Hence, the performed operation is $\mathbf{x}\lbrack j\rbrack = \alpha \times \mathbf{x}\lbrack j\rbrack$ for $i = 1,\ldots,n$ and $j = 1 + \left( {i - 1} \right)\\*\text{incx}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sscal()](http://www.netlib.org/blas/sscal.f), [dscal()](http://www.netlib.org/blas/dscal.f), [csscal()](http://www.netlib.org/blas/csscal.f), [cscal()](http://www.netlib.org/blas/cscal.f), [zdscal()](http://www.netlib.org/blas/zdscal.f), [zscal()](http://www.netlib.org/blas/zscal.f).
    pub fn cublasZscal_v2(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        alpha: *const cuDoubleComplex,
        x: *mut cuDoubleComplex,
        incx: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function scales the vector `x` by the scalar $\alpha$ and overwrites it with the result. Hence, the performed operation is $\mathbf{x}\lbrack j\rbrack = \alpha \times \mathbf{x}\lbrack j\rbrack$ for $i = 1,\ldots,n$ and $j = 1 + \left( {i - 1} \right)\\*\text{incx}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sscal()](http://www.netlib.org/blas/sscal.f), [dscal()](http://www.netlib.org/blas/dscal.f), [csscal()](http://www.netlib.org/blas/csscal.f), [cscal()](http://www.netlib.org/blas/cscal.f), [zdscal()](http://www.netlib.org/blas/zdscal.f), [zscal()](http://www.netlib.org/blas/zscal.f).
    pub fn cublasZscal_v2_64(
        handle: cublasHandle_t,
        n: i64,
        alpha: *const cuDoubleComplex,
        x: *mut cuDoubleComplex,
        incx: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function scales the vector `x` by the scalar $\alpha$ and overwrites it with the result. Hence, the performed operation is $\mathbf{x}\lbrack j\rbrack = \alpha \times \mathbf{x}\lbrack j\rbrack$ for $i = 1,\ldots,n$ and $j = 1 + \left( {i - 1} \right)\\*\text{incx}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sscal()](http://www.netlib.org/blas/sscal.f), [dscal()](http://www.netlib.org/blas/dscal.f), [csscal()](http://www.netlib.org/blas/csscal.f), [cscal()](http://www.netlib.org/blas/cscal.f), [zdscal()](http://www.netlib.org/blas/zdscal.f), [zscal()](http://www.netlib.org/blas/zscal.f).
    pub fn cublasZdscal_v2(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        alpha: *const f64,
        x: *mut cuDoubleComplex,
        incx: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function scales the vector `x` by the scalar $\alpha$ and overwrites it with the result. Hence, the performed operation is $\mathbf{x}\lbrack j\rbrack = \alpha \times \mathbf{x}\lbrack j\rbrack$ for $i = 1,\ldots,n$ and $j = 1 + \left( {i - 1} \right)\\*\text{incx}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sscal()](http://www.netlib.org/blas/sscal.f), [dscal()](http://www.netlib.org/blas/dscal.f), [csscal()](http://www.netlib.org/blas/csscal.f), [cscal()](http://www.netlib.org/blas/cscal.f), [zdscal()](http://www.netlib.org/blas/zdscal.f), [zscal()](http://www.netlib.org/blas/zscal.f).
    pub fn cublasZdscal_v2_64(
        handle: cublasHandle_t,
        n: i64,
        alpha: *const f64,
        x: *mut cuDoubleComplex,
        incx: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function is an API generalization of the routine cublas&lt;t>axpy() where input data, output data and compute type can be specified independently.
    ///
    /// This function multiplies the vector `x` by the scalar $\alpha$ and adds it to the vector `y` overwriting the latest vector with the result. Hence, the performed operation is $\mathbf{y}\lbrack j\rbrack = \alpha \times \mathbf{x}\lbrack k\rbrack + \mathbf{y}\lbrack j\rbrack$ for $i = 1,\ldots,n$, $k = 1 + \left( {i - 1} \right)\\*\text{incx}$ and $j = 1 + \left( {i - 1} \right)\\*\text{incy}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// The datatypes combinations currently supported for [`cublasAxpyEx`] are listed in the following table:
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [saxpy()](http://www.netlib.org/blas/saxpy.f), [daxpy()](http://www.netlib.org/blas/daxpy.f), [caxpy()](http://www.netlib.org/blas/caxpy.f), [zaxpy()](http://www.netlib.org/blas/zaxpy.f).
    ///
    /// # Parameters
    ///
    /// - `handle`: Handle to the cuBLAS library context.
    /// - `n`: Number of elements in the vector `x` and `y`.
    /// - `alpha`: &lt;`type`> scalar used for multiplication.
    /// - `alphaType`: Enumerant specifying the datatype of scalar `alpha`.
    /// - `x`: &lt;`type`> vector with `n` elements.
    /// - `xType`: Enumerant specifying the datatype of vector `x`.
    /// - `incx`: Stride between consecutive elements of `x`.
    /// - `y`: &lt;`type`> vector with `n` elements.
    /// - `yType`: Enumerant specifying the datatype of vector `y`.
    /// - `incy`: Stride between consecutive elements of `y`.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_EXECUTION_FAILED`]: The function failed to launch on the GPU.
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: `alphaType` or `xType` or `yType` or `executionType` is not supported.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_INITIALIZED`]: The library was not initialized.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_SUPPORTED`]: The combination of the parameters `xType`, `yType`, and `executionType` is not supported.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The operation completed successfully.
    pub fn cublasAxpyEx(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        alpha: *const ::core::ffi::c_void,
        alphaType: cudaDataType,
        x: *const ::core::ffi::c_void,
        xType: cudaDataType,
        incx: ::core::ffi::c_int,
        y: *mut ::core::ffi::c_void,
        yType: cudaDataType,
        incy: ::core::ffi::c_int,
        executiontype: cudaDataType,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function is an API generalization of the routine cublas&lt;t>axpy() where input data, output data and compute type can be specified independently.
    ///
    /// This function multiplies the vector `x` by the scalar $\alpha$ and adds it to the vector `y` overwriting the latest vector with the result. Hence, the performed operation is $\mathbf{y}\lbrack j\rbrack = \alpha \times \mathbf{x}\lbrack k\rbrack + \mathbf{y}\lbrack j\rbrack$ for $i = 1,\ldots,n$, $k = 1 + \left( {i - 1} \right)\\*\text{incx}$ and $j = 1 + \left( {i - 1} \right)\\*\text{incy}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// The datatypes combinations currently supported for [`cublasAxpyEx`] are listed in the following table:
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [saxpy()](http://www.netlib.org/blas/saxpy.f), [daxpy()](http://www.netlib.org/blas/daxpy.f), [caxpy()](http://www.netlib.org/blas/caxpy.f), [zaxpy()](http://www.netlib.org/blas/zaxpy.f).
    ///
    /// # Parameters
    ///
    /// - `handle`: Handle to the cuBLAS library context.
    /// - `n`: Number of elements in the vector `x` and `y`.
    /// - `alpha`: &lt;`type`> scalar used for multiplication.
    /// - `alphaType`: Enumerant specifying the datatype of scalar `alpha`.
    /// - `x`: &lt;`type`> vector with `n` elements.
    /// - `xType`: Enumerant specifying the datatype of vector `x`.
    /// - `incx`: Stride between consecutive elements of `x`.
    /// - `y`: &lt;`type`> vector with `n` elements.
    /// - `yType`: Enumerant specifying the datatype of vector `y`.
    /// - `incy`: Stride between consecutive elements of `y`.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_EXECUTION_FAILED`]: The function failed to launch on the GPU.
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: `alphaType` or `xType` or `yType` or `executionType` is not supported.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_INITIALIZED`]: The library was not initialized.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_SUPPORTED`]: The combination of the parameters `xType`, `yType`, and `executionType` is not supported.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The operation completed successfully.
    pub fn cublasAxpyEx_64(
        handle: cublasHandle_t,
        n: i64,
        alpha: *const ::core::ffi::c_void,
        alphaType: cudaDataType,
        x: *const ::core::ffi::c_void,
        xType: cudaDataType,
        incx: i64,
        y: *mut ::core::ffi::c_void,
        yType: cudaDataType,
        incy: i64,
        executiontype: cudaDataType,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function multiplies the vector `x` by the scalar $\alpha$ and adds it to the vector `y` overwriting the latest vector with the result. Hence, the performed operation is $\mathbf{y}\lbrack j\rbrack = \alpha \times \mathbf{x}\lbrack k\rbrack + \mathbf{y}\lbrack j\rbrack$ for $i = 1,\ldots,n$, $k = 1 + \left( {i - 1} \right)\\*\text{incx}$ and $j = 1 + \left( {i - 1} \right)\\*\text{incy}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [saxpy()](http://www.netlib.org/blas/saxpy.f), [daxpy()](http://www.netlib.org/blas/daxpy.f), [caxpy()](http://www.netlib.org/blas/caxpy.f), [zaxpy()](http://www.netlib.org/blas/zaxpy.f).
    pub fn cublasSaxpy_v2(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        alpha: *const f32,
        x: *const f32,
        incx: ::core::ffi::c_int,
        y: *mut f32,
        incy: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function multiplies the vector `x` by the scalar $\alpha$ and adds it to the vector `y` overwriting the latest vector with the result. Hence, the performed operation is $\mathbf{y}\lbrack j\rbrack = \alpha \times \mathbf{x}\lbrack k\rbrack + \mathbf{y}\lbrack j\rbrack$ for $i = 1,\ldots,n$, $k = 1 + \left( {i - 1} \right)\\*\text{incx}$ and $j = 1 + \left( {i - 1} \right)\\*\text{incy}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [saxpy()](http://www.netlib.org/blas/saxpy.f), [daxpy()](http://www.netlib.org/blas/daxpy.f), [caxpy()](http://www.netlib.org/blas/caxpy.f), [zaxpy()](http://www.netlib.org/blas/zaxpy.f).
    pub fn cublasSaxpy_v2_64(
        handle: cublasHandle_t,
        n: i64,
        alpha: *const f32,
        x: *const f32,
        incx: i64,
        y: *mut f32,
        incy: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function multiplies the vector `x` by the scalar $\alpha$ and adds it to the vector `y` overwriting the latest vector with the result. Hence, the performed operation is $\mathbf{y}\lbrack j\rbrack = \alpha \times \mathbf{x}\lbrack k\rbrack + \mathbf{y}\lbrack j\rbrack$ for $i = 1,\ldots,n$, $k = 1 + \left( {i - 1} \right)\\*\text{incx}$ and $j = 1 + \left( {i - 1} \right)\\*\text{incy}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [saxpy()](http://www.netlib.org/blas/saxpy.f), [daxpy()](http://www.netlib.org/blas/daxpy.f), [caxpy()](http://www.netlib.org/blas/caxpy.f), [zaxpy()](http://www.netlib.org/blas/zaxpy.f).
    pub fn cublasDaxpy_v2(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        alpha: *const f64,
        x: *const f64,
        incx: ::core::ffi::c_int,
        y: *mut f64,
        incy: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function multiplies the vector `x` by the scalar $\alpha$ and adds it to the vector `y` overwriting the latest vector with the result. Hence, the performed operation is $\mathbf{y}\lbrack j\rbrack = \alpha \times \mathbf{x}\lbrack k\rbrack + \mathbf{y}\lbrack j\rbrack$ for $i = 1,\ldots,n$, $k = 1 + \left( {i - 1} \right)\\*\text{incx}$ and $j = 1 + \left( {i - 1} \right)\\*\text{incy}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [saxpy()](http://www.netlib.org/blas/saxpy.f), [daxpy()](http://www.netlib.org/blas/daxpy.f), [caxpy()](http://www.netlib.org/blas/caxpy.f), [zaxpy()](http://www.netlib.org/blas/zaxpy.f).
    pub fn cublasDaxpy_v2_64(
        handle: cublasHandle_t,
        n: i64,
        alpha: *const f64,
        x: *const f64,
        incx: i64,
        y: *mut f64,
        incy: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function multiplies the vector `x` by the scalar $\alpha$ and adds it to the vector `y` overwriting the latest vector with the result. Hence, the performed operation is $\mathbf{y}\lbrack j\rbrack = \alpha \times \mathbf{x}\lbrack k\rbrack + \mathbf{y}\lbrack j\rbrack$ for $i = 1,\ldots,n$, $k = 1 + \left( {i - 1} \right)\\*\text{incx}$ and $j = 1 + \left( {i - 1} \right)\\*\text{incy}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [saxpy()](http://www.netlib.org/blas/saxpy.f), [daxpy()](http://www.netlib.org/blas/daxpy.f), [caxpy()](http://www.netlib.org/blas/caxpy.f), [zaxpy()](http://www.netlib.org/blas/zaxpy.f).
    pub fn cublasCaxpy_v2(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        alpha: *const cuComplex,
        x: *const cuComplex,
        incx: ::core::ffi::c_int,
        y: *mut cuComplex,
        incy: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function multiplies the vector `x` by the scalar $\alpha$ and adds it to the vector `y` overwriting the latest vector with the result. Hence, the performed operation is $\mathbf{y}\lbrack j\rbrack = \alpha \times \mathbf{x}\lbrack k\rbrack + \mathbf{y}\lbrack j\rbrack$ for $i = 1,\ldots,n$, $k = 1 + \left( {i - 1} \right)\\*\text{incx}$ and $j = 1 + \left( {i - 1} \right)\\*\text{incy}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [saxpy()](http://www.netlib.org/blas/saxpy.f), [daxpy()](http://www.netlib.org/blas/daxpy.f), [caxpy()](http://www.netlib.org/blas/caxpy.f), [zaxpy()](http://www.netlib.org/blas/zaxpy.f).
    pub fn cublasCaxpy_v2_64(
        handle: cublasHandle_t,
        n: i64,
        alpha: *const cuComplex,
        x: *const cuComplex,
        incx: i64,
        y: *mut cuComplex,
        incy: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function multiplies the vector `x` by the scalar $\alpha$ and adds it to the vector `y` overwriting the latest vector with the result. Hence, the performed operation is $\mathbf{y}\lbrack j\rbrack = \alpha \times \mathbf{x}\lbrack k\rbrack + \mathbf{y}\lbrack j\rbrack$ for $i = 1,\ldots,n$, $k = 1 + \left( {i - 1} \right)\\*\text{incx}$ and $j = 1 + \left( {i - 1} \right)\\*\text{incy}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [saxpy()](http://www.netlib.org/blas/saxpy.f), [daxpy()](http://www.netlib.org/blas/daxpy.f), [caxpy()](http://www.netlib.org/blas/caxpy.f), [zaxpy()](http://www.netlib.org/blas/zaxpy.f).
    pub fn cublasZaxpy_v2(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        alpha: *const cuDoubleComplex,
        x: *const cuDoubleComplex,
        incx: ::core::ffi::c_int,
        y: *mut cuDoubleComplex,
        incy: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function multiplies the vector `x` by the scalar $\alpha$ and adds it to the vector `y` overwriting the latest vector with the result. Hence, the performed operation is $\mathbf{y}\lbrack j\rbrack = \alpha \times \mathbf{x}\lbrack k\rbrack + \mathbf{y}\lbrack j\rbrack$ for $i = 1,\ldots,n$, $k = 1 + \left( {i - 1} \right)\\*\text{incx}$ and $j = 1 + \left( {i - 1} \right)\\*\text{incy}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [saxpy()](http://www.netlib.org/blas/saxpy.f), [daxpy()](http://www.netlib.org/blas/daxpy.f), [caxpy()](http://www.netlib.org/blas/caxpy.f), [zaxpy()](http://www.netlib.org/blas/zaxpy.f).
    pub fn cublasZaxpy_v2_64(
        handle: cublasHandle_t,
        n: i64,
        alpha: *const cuDoubleComplex,
        x: *const cuDoubleComplex,
        incx: i64,
        y: *mut cuDoubleComplex,
        incy: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    pub fn cublasCopyEx(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        x: *const ::core::ffi::c_void,
        xType: cudaDataType,
        incx: ::core::ffi::c_int,
        y: *mut ::core::ffi::c_void,
        yType: cudaDataType,
        incy: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    pub fn cublasCopyEx_64(
        handle: cublasHandle_t,
        n: i64,
        x: *const ::core::ffi::c_void,
        xType: cudaDataType,
        incx: i64,
        y: *mut ::core::ffi::c_void,
        yType: cudaDataType,
        incy: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function copies the vector `x` into the vector `y`. Hence, the performed operation is $\mathbf{y}\lbrack j\rbrack = \mathbf{x}\lbrack k\rbrack$ for $i = 1,\ldots,n$, $k = 1 + \left( {i - 1} \right)\\*\text{incx}$ and $j = 1 + \left( {i - 1} \right)\\*\text{incy}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [scopy()](http://www.netlib.org/blas/scopy.f), [dcopy()](http://www.netlib.org/blas/dcopy.f), [ccopy()](http://www.netlib.org/blas/ccopy.f), [zcopy()](http://www.netlib.org/blas/zcopy.f).
    pub fn cublasScopy_v2(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        x: *const f32,
        incx: ::core::ffi::c_int,
        y: *mut f32,
        incy: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function copies the vector `x` into the vector `y`. Hence, the performed operation is $\mathbf{y}\lbrack j\rbrack = \mathbf{x}\lbrack k\rbrack$ for $i = 1,\ldots,n$, $k = 1 + \left( {i - 1} \right)\\*\text{incx}$ and $j = 1 + \left( {i - 1} \right)\\*\text{incy}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [scopy()](http://www.netlib.org/blas/scopy.f), [dcopy()](http://www.netlib.org/blas/dcopy.f), [ccopy()](http://www.netlib.org/blas/ccopy.f), [zcopy()](http://www.netlib.org/blas/zcopy.f).
    pub fn cublasScopy_v2_64(
        handle: cublasHandle_t,
        n: i64,
        x: *const f32,
        incx: i64,
        y: *mut f32,
        incy: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function copies the vector `x` into the vector `y`. Hence, the performed operation is $\mathbf{y}\lbrack j\rbrack = \mathbf{x}\lbrack k\rbrack$ for $i = 1,\ldots,n$, $k = 1 + \left( {i - 1} \right)\\*\text{incx}$ and $j = 1 + \left( {i - 1} \right)\\*\text{incy}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [scopy()](http://www.netlib.org/blas/scopy.f), [dcopy()](http://www.netlib.org/blas/dcopy.f), [ccopy()](http://www.netlib.org/blas/ccopy.f), [zcopy()](http://www.netlib.org/blas/zcopy.f).
    pub fn cublasDcopy_v2(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        x: *const f64,
        incx: ::core::ffi::c_int,
        y: *mut f64,
        incy: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function copies the vector `x` into the vector `y`. Hence, the performed operation is $\mathbf{y}\lbrack j\rbrack = \mathbf{x}\lbrack k\rbrack$ for $i = 1,\ldots,n$, $k = 1 + \left( {i - 1} \right)\\*\text{incx}$ and $j = 1 + \left( {i - 1} \right)\\*\text{incy}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [scopy()](http://www.netlib.org/blas/scopy.f), [dcopy()](http://www.netlib.org/blas/dcopy.f), [ccopy()](http://www.netlib.org/blas/ccopy.f), [zcopy()](http://www.netlib.org/blas/zcopy.f).
    pub fn cublasDcopy_v2_64(
        handle: cublasHandle_t,
        n: i64,
        x: *const f64,
        incx: i64,
        y: *mut f64,
        incy: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function copies the vector `x` into the vector `y`. Hence, the performed operation is $\mathbf{y}\lbrack j\rbrack = \mathbf{x}\lbrack k\rbrack$ for $i = 1,\ldots,n$, $k = 1 + \left( {i - 1} \right)\\*\text{incx}$ and $j = 1 + \left( {i - 1} \right)\\*\text{incy}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [scopy()](http://www.netlib.org/blas/scopy.f), [dcopy()](http://www.netlib.org/blas/dcopy.f), [ccopy()](http://www.netlib.org/blas/ccopy.f), [zcopy()](http://www.netlib.org/blas/zcopy.f).
    pub fn cublasCcopy_v2(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        x: *const cuComplex,
        incx: ::core::ffi::c_int,
        y: *mut cuComplex,
        incy: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function copies the vector `x` into the vector `y`. Hence, the performed operation is $\mathbf{y}\lbrack j\rbrack = \mathbf{x}\lbrack k\rbrack$ for $i = 1,\ldots,n$, $k = 1 + \left( {i - 1} \right)\\*\text{incx}$ and $j = 1 + \left( {i - 1} \right)\\*\text{incy}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [scopy()](http://www.netlib.org/blas/scopy.f), [dcopy()](http://www.netlib.org/blas/dcopy.f), [ccopy()](http://www.netlib.org/blas/ccopy.f), [zcopy()](http://www.netlib.org/blas/zcopy.f).
    pub fn cublasCcopy_v2_64(
        handle: cublasHandle_t,
        n: i64,
        x: *const cuComplex,
        incx: i64,
        y: *mut cuComplex,
        incy: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function copies the vector `x` into the vector `y`. Hence, the performed operation is $\mathbf{y}\lbrack j\rbrack = \mathbf{x}\lbrack k\rbrack$ for $i = 1,\ldots,n$, $k = 1 + \left( {i - 1} \right)\\*\text{incx}$ and $j = 1 + \left( {i - 1} \right)\\*\text{incy}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [scopy()](http://www.netlib.org/blas/scopy.f), [dcopy()](http://www.netlib.org/blas/dcopy.f), [ccopy()](http://www.netlib.org/blas/ccopy.f), [zcopy()](http://www.netlib.org/blas/zcopy.f).
    pub fn cublasZcopy_v2(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        x: *const cuDoubleComplex,
        incx: ::core::ffi::c_int,
        y: *mut cuDoubleComplex,
        incy: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function copies the vector `x` into the vector `y`. Hence, the performed operation is $\mathbf{y}\lbrack j\rbrack = \mathbf{x}\lbrack k\rbrack$ for $i = 1,\ldots,n$, $k = 1 + \left( {i - 1} \right)\\*\text{incx}$ and $j = 1 + \left( {i - 1} \right)\\*\text{incy}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [scopy()](http://www.netlib.org/blas/scopy.f), [dcopy()](http://www.netlib.org/blas/dcopy.f), [ccopy()](http://www.netlib.org/blas/ccopy.f), [zcopy()](http://www.netlib.org/blas/zcopy.f).
    pub fn cublasZcopy_v2_64(
        handle: cublasHandle_t,
        n: i64,
        x: *const cuDoubleComplex,
        incx: i64,
        y: *mut cuDoubleComplex,
        incy: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function interchanges the elements of vector `x` and `y`. Hence, the performed operation is $\left. \mathbf{y}\lbrack j\rbrack\Leftrightarrow\mathbf{x}\lbrack k\rbrack \right.$ for $i = 1,\ldots,n$, $k = 1 + \left( {i - 1} \right)\\*\text{incx}$ and $j = 1 + \left( {i - 1} \right)\\*\text{incy}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sswap()](http://www.netlib.org/blas/sswap.f), [dswap()](http://www.netlib.org/blas/dswap.f), [cswap()](http://www.netlib.org/blas/cswap.f), [zswap()](http://www.netlib.org/blas/zswap.f).
    pub fn cublasSswap_v2(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        x: *mut f32,
        incx: ::core::ffi::c_int,
        y: *mut f32,
        incy: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function interchanges the elements of vector `x` and `y`. Hence, the performed operation is $\left. \mathbf{y}\lbrack j\rbrack\Leftrightarrow\mathbf{x}\lbrack k\rbrack \right.$ for $i = 1,\ldots,n$, $k = 1 + \left( {i - 1} \right)\\*\text{incx}$ and $j = 1 + \left( {i - 1} \right)\\*\text{incy}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sswap()](http://www.netlib.org/blas/sswap.f), [dswap()](http://www.netlib.org/blas/dswap.f), [cswap()](http://www.netlib.org/blas/cswap.f), [zswap()](http://www.netlib.org/blas/zswap.f).
    pub fn cublasSswap_v2_64(
        handle: cublasHandle_t,
        n: i64,
        x: *mut f32,
        incx: i64,
        y: *mut f32,
        incy: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function interchanges the elements of vector `x` and `y`. Hence, the performed operation is $\left. \mathbf{y}\lbrack j\rbrack\Leftrightarrow\mathbf{x}\lbrack k\rbrack \right.$ for $i = 1,\ldots,n$, $k = 1 + \left( {i - 1} \right)\\*\text{incx}$ and $j = 1 + \left( {i - 1} \right)\\*\text{incy}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sswap()](http://www.netlib.org/blas/sswap.f), [dswap()](http://www.netlib.org/blas/dswap.f), [cswap()](http://www.netlib.org/blas/cswap.f), [zswap()](http://www.netlib.org/blas/zswap.f).
    pub fn cublasDswap_v2(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        x: *mut f64,
        incx: ::core::ffi::c_int,
        y: *mut f64,
        incy: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function interchanges the elements of vector `x` and `y`. Hence, the performed operation is $\left. \mathbf{y}\lbrack j\rbrack\Leftrightarrow\mathbf{x}\lbrack k\rbrack \right.$ for $i = 1,\ldots,n$, $k = 1 + \left( {i - 1} \right)\\*\text{incx}$ and $j = 1 + \left( {i - 1} \right)\\*\text{incy}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sswap()](http://www.netlib.org/blas/sswap.f), [dswap()](http://www.netlib.org/blas/dswap.f), [cswap()](http://www.netlib.org/blas/cswap.f), [zswap()](http://www.netlib.org/blas/zswap.f).
    pub fn cublasDswap_v2_64(
        handle: cublasHandle_t,
        n: i64,
        x: *mut f64,
        incx: i64,
        y: *mut f64,
        incy: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function interchanges the elements of vector `x` and `y`. Hence, the performed operation is $\left. \mathbf{y}\lbrack j\rbrack\Leftrightarrow\mathbf{x}\lbrack k\rbrack \right.$ for $i = 1,\ldots,n$, $k = 1 + \left( {i - 1} \right)\\*\text{incx}$ and $j = 1 + \left( {i - 1} \right)\\*\text{incy}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sswap()](http://www.netlib.org/blas/sswap.f), [dswap()](http://www.netlib.org/blas/dswap.f), [cswap()](http://www.netlib.org/blas/cswap.f), [zswap()](http://www.netlib.org/blas/zswap.f).
    pub fn cublasCswap_v2(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        x: *mut cuComplex,
        incx: ::core::ffi::c_int,
        y: *mut cuComplex,
        incy: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function interchanges the elements of vector `x` and `y`. Hence, the performed operation is $\left. \mathbf{y}\lbrack j\rbrack\Leftrightarrow\mathbf{x}\lbrack k\rbrack \right.$ for $i = 1,\ldots,n$, $k = 1 + \left( {i - 1} \right)\\*\text{incx}$ and $j = 1 + \left( {i - 1} \right)\\*\text{incy}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sswap()](http://www.netlib.org/blas/sswap.f), [dswap()](http://www.netlib.org/blas/dswap.f), [cswap()](http://www.netlib.org/blas/cswap.f), [zswap()](http://www.netlib.org/blas/zswap.f).
    pub fn cublasCswap_v2_64(
        handle: cublasHandle_t,
        n: i64,
        x: *mut cuComplex,
        incx: i64,
        y: *mut cuComplex,
        incy: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function interchanges the elements of vector `x` and `y`. Hence, the performed operation is $\left. \mathbf{y}\lbrack j\rbrack\Leftrightarrow\mathbf{x}\lbrack k\rbrack \right.$ for $i = 1,\ldots,n$, $k = 1 + \left( {i - 1} \right)\\*\text{incx}$ and $j = 1 + \left( {i - 1} \right)\\*\text{incy}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sswap()](http://www.netlib.org/blas/sswap.f), [dswap()](http://www.netlib.org/blas/dswap.f), [cswap()](http://www.netlib.org/blas/cswap.f), [zswap()](http://www.netlib.org/blas/zswap.f).
    pub fn cublasZswap_v2(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        x: *mut cuDoubleComplex,
        incx: ::core::ffi::c_int,
        y: *mut cuDoubleComplex,
        incy: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function interchanges the elements of vector `x` and `y`. Hence, the performed operation is $\left. \mathbf{y}\lbrack j\rbrack\Leftrightarrow\mathbf{x}\lbrack k\rbrack \right.$ for $i = 1,\ldots,n$, $k = 1 + \left( {i - 1} \right)\\*\text{incx}$ and $j = 1 + \left( {i - 1} \right)\\*\text{incy}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sswap()](http://www.netlib.org/blas/sswap.f), [dswap()](http://www.netlib.org/blas/dswap.f), [cswap()](http://www.netlib.org/blas/cswap.f), [zswap()](http://www.netlib.org/blas/zswap.f).
    pub fn cublasZswap_v2_64(
        handle: cublasHandle_t,
        n: i64,
        x: *mut cuDoubleComplex,
        incx: i64,
        y: *mut cuDoubleComplex,
        incy: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    pub fn cublasSwapEx(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        x: *mut ::core::ffi::c_void,
        xType: cudaDataType,
        incx: ::core::ffi::c_int,
        y: *mut ::core::ffi::c_void,
        yType: cudaDataType,
        incy: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    pub fn cublasSwapEx_64(
        handle: cublasHandle_t,
        n: i64,
        x: *mut ::core::ffi::c_void,
        xType: cudaDataType,
        incx: i64,
        y: *mut ::core::ffi::c_void,
        yType: cudaDataType,
        incy: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function finds the (smallest) index of the element of the maximum magnitude. Hence, the result is the first $i$ such that $\left| \mathbf{Im}\left( {x\lbrack j\rbrack} \right) \middle| + \middle| \mathbf{Re}\left( {x\lbrack j\rbrack} \right) \right|$ is maximum for $i = 1,\ldots,n$ and $j = 1 + \left( {i - 1} \right)\\*\text{ incx}$. Notice that the last equation reflects 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [isamax()](http://www.netlib.org/blas/isamax.f), [idamax()](http://www.netlib.org/blas/idamax.f), [icamax()](http://www.netlib.org/blas/icamax.f), [izamax()](http://www.netlib.org/blas/izamax.f).
    pub fn cublasIsamax_v2(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        x: *const f32,
        incx: ::core::ffi::c_int,
        result: *mut ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function finds the (smallest) index of the element of the maximum magnitude. Hence, the result is the first $i$ such that $\left| \mathbf{Im}\left( {x\lbrack j\rbrack} \right) \middle| + \middle| \mathbf{Re}\left( {x\lbrack j\rbrack} \right) \right|$ is maximum for $i = 1,\ldots,n$ and $j = 1 + \left( {i - 1} \right)\\*\text{ incx}$. Notice that the last equation reflects 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [isamax()](http://www.netlib.org/blas/isamax.f), [idamax()](http://www.netlib.org/blas/idamax.f), [icamax()](http://www.netlib.org/blas/icamax.f), [izamax()](http://www.netlib.org/blas/izamax.f).
    pub fn cublasIsamax_v2_64(
        handle: cublasHandle_t,
        n: i64,
        x: *const f32,
        incx: i64,
        result: *mut i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function finds the (smallest) index of the element of the maximum magnitude. Hence, the result is the first $i$ such that $\left| \mathbf{Im}\left( {x\lbrack j\rbrack} \right) \middle| + \middle| \mathbf{Re}\left( {x\lbrack j\rbrack} \right) \right|$ is maximum for $i = 1,\ldots,n$ and $j = 1 + \left( {i - 1} \right)\\*\text{ incx}$. Notice that the last equation reflects 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [isamax()](http://www.netlib.org/blas/isamax.f), [idamax()](http://www.netlib.org/blas/idamax.f), [icamax()](http://www.netlib.org/blas/icamax.f), [izamax()](http://www.netlib.org/blas/izamax.f).
    pub fn cublasIdamax_v2(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        x: *const f64,
        incx: ::core::ffi::c_int,
        result: *mut ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function finds the (smallest) index of the element of the maximum magnitude. Hence, the result is the first $i$ such that $\left| \mathbf{Im}\left( {x\lbrack j\rbrack} \right) \middle| + \middle| \mathbf{Re}\left( {x\lbrack j\rbrack} \right) \right|$ is maximum for $i = 1,\ldots,n$ and $j = 1 + \left( {i - 1} \right)\\*\text{ incx}$. Notice that the last equation reflects 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [isamax()](http://www.netlib.org/blas/isamax.f), [idamax()](http://www.netlib.org/blas/idamax.f), [icamax()](http://www.netlib.org/blas/icamax.f), [izamax()](http://www.netlib.org/blas/izamax.f).
    pub fn cublasIdamax_v2_64(
        handle: cublasHandle_t,
        n: i64,
        x: *const f64,
        incx: i64,
        result: *mut i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function finds the (smallest) index of the element of the maximum magnitude. Hence, the result is the first $i$ such that $\left| \mathbf{Im}\left( {x\lbrack j\rbrack} \right) \middle| + \middle| \mathbf{Re}\left( {x\lbrack j\rbrack} \right) \right|$ is maximum for $i = 1,\ldots,n$ and $j = 1 + \left( {i - 1} \right)\\*\text{ incx}$. Notice that the last equation reflects 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [isamax()](http://www.netlib.org/blas/isamax.f), [idamax()](http://www.netlib.org/blas/idamax.f), [icamax()](http://www.netlib.org/blas/icamax.f), [izamax()](http://www.netlib.org/blas/izamax.f).
    pub fn cublasIcamax_v2(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        x: *const cuComplex,
        incx: ::core::ffi::c_int,
        result: *mut ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function finds the (smallest) index of the element of the maximum magnitude. Hence, the result is the first $i$ such that $\left| \mathbf{Im}\left( {x\lbrack j\rbrack} \right) \middle| + \middle| \mathbf{Re}\left( {x\lbrack j\rbrack} \right) \right|$ is maximum for $i = 1,\ldots,n$ and $j = 1 + \left( {i - 1} \right)\\*\text{ incx}$. Notice that the last equation reflects 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [isamax()](http://www.netlib.org/blas/isamax.f), [idamax()](http://www.netlib.org/blas/idamax.f), [icamax()](http://www.netlib.org/blas/icamax.f), [izamax()](http://www.netlib.org/blas/izamax.f).
    pub fn cublasIcamax_v2_64(
        handle: cublasHandle_t,
        n: i64,
        x: *const cuComplex,
        incx: i64,
        result: *mut i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function finds the (smallest) index of the element of the maximum magnitude. Hence, the result is the first $i$ such that $\left| \mathbf{Im}\left( {x\lbrack j\rbrack} \right) \middle| + \middle| \mathbf{Re}\left( {x\lbrack j\rbrack} \right) \right|$ is maximum for $i = 1,\ldots,n$ and $j = 1 + \left( {i - 1} \right)\\*\text{ incx}$. Notice that the last equation reflects 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [isamax()](http://www.netlib.org/blas/isamax.f), [idamax()](http://www.netlib.org/blas/idamax.f), [icamax()](http://www.netlib.org/blas/icamax.f), [izamax()](http://www.netlib.org/blas/izamax.f).
    pub fn cublasIzamax_v2(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        x: *const cuDoubleComplex,
        incx: ::core::ffi::c_int,
        result: *mut ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function finds the (smallest) index of the element of the maximum magnitude. Hence, the result is the first $i$ such that $\left| \mathbf{Im}\left( {x\lbrack j\rbrack} \right) \middle| + \middle| \mathbf{Re}\left( {x\lbrack j\rbrack} \right) \right|$ is maximum for $i = 1,\ldots,n$ and $j = 1 + \left( {i - 1} \right)\\*\text{ incx}$. Notice that the last equation reflects 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [isamax()](http://www.netlib.org/blas/isamax.f), [idamax()](http://www.netlib.org/blas/idamax.f), [icamax()](http://www.netlib.org/blas/icamax.f), [izamax()](http://www.netlib.org/blas/izamax.f).
    pub fn cublasIzamax_v2_64(
        handle: cublasHandle_t,
        n: i64,
        x: *const cuDoubleComplex,
        incx: i64,
        result: *mut i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    pub fn cublasIamaxEx(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        x: *const ::core::ffi::c_void,
        xType: cudaDataType,
        incx: ::core::ffi::c_int,
        result: *mut ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    pub fn cublasIamaxEx_64(
        handle: cublasHandle_t,
        n: i64,
        x: *const ::core::ffi::c_void,
        xType: cudaDataType,
        incx: i64,
        result: *mut i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function finds the (smallest) index of the element of the minimum magnitude. Hence, the result is the first $i$ such that $\left| \mathbf{Im}\left( {x\lbrack j\rbrack} \right) \middle| + \middle| \mathbf{Re}\left( {x\lbrack j\rbrack} \right) \right|$ is minimum for $i = 1,\ldots,n$ and $j = 1 + \left( {i - 1} \right)\\*\text{incx}$ Notice that the last equation reflects 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [isamin()](http://www.netlib.org/scilib/blass.f).
    pub fn cublasIsamin_v2(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        x: *const f32,
        incx: ::core::ffi::c_int,
        result: *mut ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function finds the (smallest) index of the element of the minimum magnitude. Hence, the result is the first $i$ such that $\left| \mathbf{Im}\left( {x\lbrack j\rbrack} \right) \middle| + \middle| \mathbf{Re}\left( {x\lbrack j\rbrack} \right) \right|$ is minimum for $i = 1,\ldots,n$ and $j = 1 + \left( {i - 1} \right)\\*\text{incx}$ Notice that the last equation reflects 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [isamin()](http://www.netlib.org/scilib/blass.f).
    pub fn cublasIsamin_v2_64(
        handle: cublasHandle_t,
        n: i64,
        x: *const f32,
        incx: i64,
        result: *mut i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function finds the (smallest) index of the element of the minimum magnitude. Hence, the result is the first $i$ such that $\left| \mathbf{Im}\left( {x\lbrack j\rbrack} \right) \middle| + \middle| \mathbf{Re}\left( {x\lbrack j\rbrack} \right) \right|$ is minimum for $i = 1,\ldots,n$ and $j = 1 + \left( {i - 1} \right)\\*\text{incx}$ Notice that the last equation reflects 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [isamin()](http://www.netlib.org/scilib/blass.f).
    pub fn cublasIdamin_v2(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        x: *const f64,
        incx: ::core::ffi::c_int,
        result: *mut ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function finds the (smallest) index of the element of the minimum magnitude. Hence, the result is the first $i$ such that $\left| \mathbf{Im}\left( {x\lbrack j\rbrack} \right) \middle| + \middle| \mathbf{Re}\left( {x\lbrack j\rbrack} \right) \right|$ is minimum for $i = 1,\ldots,n$ and $j = 1 + \left( {i - 1} \right)\\*\text{incx}$ Notice that the last equation reflects 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [isamin()](http://www.netlib.org/scilib/blass.f).
    pub fn cublasIdamin_v2_64(
        handle: cublasHandle_t,
        n: i64,
        x: *const f64,
        incx: i64,
        result: *mut i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function finds the (smallest) index of the element of the minimum magnitude. Hence, the result is the first $i$ such that $\left| \mathbf{Im}\left( {x\lbrack j\rbrack} \right) \middle| + \middle| \mathbf{Re}\left( {x\lbrack j\rbrack} \right) \right|$ is minimum for $i = 1,\ldots,n$ and $j = 1 + \left( {i - 1} \right)\\*\text{incx}$ Notice that the last equation reflects 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [isamin()](http://www.netlib.org/scilib/blass.f).
    pub fn cublasIcamin_v2(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        x: *const cuComplex,
        incx: ::core::ffi::c_int,
        result: *mut ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function finds the (smallest) index of the element of the minimum magnitude. Hence, the result is the first $i$ such that $\left| \mathbf{Im}\left( {x\lbrack j\rbrack} \right) \middle| + \middle| \mathbf{Re}\left( {x\lbrack j\rbrack} \right) \right|$ is minimum for $i = 1,\ldots,n$ and $j = 1 + \left( {i - 1} \right)\\*\text{incx}$ Notice that the last equation reflects 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [isamin()](http://www.netlib.org/scilib/blass.f).
    pub fn cublasIcamin_v2_64(
        handle: cublasHandle_t,
        n: i64,
        x: *const cuComplex,
        incx: i64,
        result: *mut i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function finds the (smallest) index of the element of the minimum magnitude. Hence, the result is the first $i$ such that $\left| \mathbf{Im}\left( {x\lbrack j\rbrack} \right) \middle| + \middle| \mathbf{Re}\left( {x\lbrack j\rbrack} \right) \right|$ is minimum for $i = 1,\ldots,n$ and $j = 1 + \left( {i - 1} \right)\\*\text{incx}$ Notice that the last equation reflects 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [isamin()](http://www.netlib.org/scilib/blass.f).
    pub fn cublasIzamin_v2(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        x: *const cuDoubleComplex,
        incx: ::core::ffi::c_int,
        result: *mut ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function finds the (smallest) index of the element of the minimum magnitude. Hence, the result is the first $i$ such that $\left| \mathbf{Im}\left( {x\lbrack j\rbrack} \right) \middle| + \middle| \mathbf{Re}\left( {x\lbrack j\rbrack} \right) \right|$ is minimum for $i = 1,\ldots,n$ and $j = 1 + \left( {i - 1} \right)\\*\text{incx}$ Notice that the last equation reflects 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [isamin()](http://www.netlib.org/scilib/blass.f).
    pub fn cublasIzamin_v2_64(
        handle: cublasHandle_t,
        n: i64,
        x: *const cuDoubleComplex,
        incx: i64,
        result: *mut i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    pub fn cublasIaminEx(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        x: *const ::core::ffi::c_void,
        xType: cudaDataType,
        incx: ::core::ffi::c_int,
        result: *mut ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    pub fn cublasIaminEx_64(
        handle: cublasHandle_t,
        n: i64,
        x: *const ::core::ffi::c_void,
        xType: cudaDataType,
        incx: i64,
        result: *mut i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    pub fn cublasAsumEx(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        x: *const ::core::ffi::c_void,
        xType: cudaDataType,
        incx: ::core::ffi::c_int,
        result: *mut ::core::ffi::c_void,
        resultType: cudaDataType,
        executiontype: cudaDataType,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    pub fn cublasAsumEx_64(
        handle: cublasHandle_t,
        n: i64,
        x: *const ::core::ffi::c_void,
        xType: cudaDataType,
        incx: i64,
        result: *mut ::core::ffi::c_void,
        resultType: cudaDataType,
        executiontype: cudaDataType,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function computes the sum of the absolute values of the elements of vector `x`. Hence, the result is $\left. \sum_{i = 1}^{n} \middle| \mathbf{Im}\left( {x\lbrack j\rbrack} \right) \middle| + \middle| \mathbf{Re}\left( {x\lbrack j\rbrack} \right) \right|$ where $j = 1 + \left( {i - 1} \right)\\*\text{incx}$. Notice that the last equation reflects 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sasum()](http://www.netlib.org/blas/sasum.f), [dasum()](http://www.netlib.org/blas/dasum.f), [scasum()](http://www.netlib.org/blas/scasum.f), [dzasum()](http://www.netlib.org/blas/dzasum.f).
    pub fn cublasSasum_v2(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        x: *const f32,
        incx: ::core::ffi::c_int,
        result: *mut f32,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function computes the sum of the absolute values of the elements of vector `x`. Hence, the result is $\left. \sum_{i = 1}^{n} \middle| \mathbf{Im}\left( {x\lbrack j\rbrack} \right) \middle| + \middle| \mathbf{Re}\left( {x\lbrack j\rbrack} \right) \right|$ where $j = 1 + \left( {i - 1} \right)\\*\text{incx}$. Notice that the last equation reflects 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sasum()](http://www.netlib.org/blas/sasum.f), [dasum()](http://www.netlib.org/blas/dasum.f), [scasum()](http://www.netlib.org/blas/scasum.f), [dzasum()](http://www.netlib.org/blas/dzasum.f).
    pub fn cublasSasum_v2_64(
        handle: cublasHandle_t,
        n: i64,
        x: *const f32,
        incx: i64,
        result: *mut f32,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function computes the sum of the absolute values of the elements of vector `x`. Hence, the result is $\left. \sum_{i = 1}^{n} \middle| \mathbf{Im}\left( {x\lbrack j\rbrack} \right) \middle| + \middle| \mathbf{Re}\left( {x\lbrack j\rbrack} \right) \right|$ where $j = 1 + \left( {i - 1} \right)\\*\text{incx}$. Notice that the last equation reflects 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sasum()](http://www.netlib.org/blas/sasum.f), [dasum()](http://www.netlib.org/blas/dasum.f), [scasum()](http://www.netlib.org/blas/scasum.f), [dzasum()](http://www.netlib.org/blas/dzasum.f).
    pub fn cublasDasum_v2(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        x: *const f64,
        incx: ::core::ffi::c_int,
        result: *mut f64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function computes the sum of the absolute values of the elements of vector `x`. Hence, the result is $\left. \sum_{i = 1}^{n} \middle| \mathbf{Im}\left( {x\lbrack j\rbrack} \right) \middle| + \middle| \mathbf{Re}\left( {x\lbrack j\rbrack} \right) \right|$ where $j = 1 + \left( {i - 1} \right)\\*\text{incx}$. Notice that the last equation reflects 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sasum()](http://www.netlib.org/blas/sasum.f), [dasum()](http://www.netlib.org/blas/dasum.f), [scasum()](http://www.netlib.org/blas/scasum.f), [dzasum()](http://www.netlib.org/blas/dzasum.f).
    pub fn cublasDasum_v2_64(
        handle: cublasHandle_t,
        n: i64,
        x: *const f64,
        incx: i64,
        result: *mut f64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function computes the sum of the absolute values of the elements of vector `x`. Hence, the result is $\left. \sum_{i = 1}^{n} \middle| \mathbf{Im}\left( {x\lbrack j\rbrack} \right) \middle| + \middle| \mathbf{Re}\left( {x\lbrack j\rbrack} \right) \right|$ where $j = 1 + \left( {i - 1} \right)\\*\text{incx}$. Notice that the last equation reflects 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sasum()](http://www.netlib.org/blas/sasum.f), [dasum()](http://www.netlib.org/blas/dasum.f), [scasum()](http://www.netlib.org/blas/scasum.f), [dzasum()](http://www.netlib.org/blas/dzasum.f).
    pub fn cublasScasum_v2(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        x: *const cuComplex,
        incx: ::core::ffi::c_int,
        result: *mut f32,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function computes the sum of the absolute values of the elements of vector `x`. Hence, the result is $\left. \sum_{i = 1}^{n} \middle| \mathbf{Im}\left( {x\lbrack j\rbrack} \right) \middle| + \middle| \mathbf{Re}\left( {x\lbrack j\rbrack} \right) \right|$ where $j = 1 + \left( {i - 1} \right)\\*\text{incx}$. Notice that the last equation reflects 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sasum()](http://www.netlib.org/blas/sasum.f), [dasum()](http://www.netlib.org/blas/dasum.f), [scasum()](http://www.netlib.org/blas/scasum.f), [dzasum()](http://www.netlib.org/blas/dzasum.f).
    pub fn cublasScasum_v2_64(
        handle: cublasHandle_t,
        n: i64,
        x: *const cuComplex,
        incx: i64,
        result: *mut f32,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function computes the sum of the absolute values of the elements of vector `x`. Hence, the result is $\left. \sum_{i = 1}^{n} \middle| \mathbf{Im}\left( {x\lbrack j\rbrack} \right) \middle| + \middle| \mathbf{Re}\left( {x\lbrack j\rbrack} \right) \right|$ where $j = 1 + \left( {i - 1} \right)\\*\text{incx}$. Notice that the last equation reflects 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sasum()](http://www.netlib.org/blas/sasum.f), [dasum()](http://www.netlib.org/blas/dasum.f), [scasum()](http://www.netlib.org/blas/scasum.f), [dzasum()](http://www.netlib.org/blas/dzasum.f).
    pub fn cublasDzasum_v2(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        x: *const cuDoubleComplex,
        incx: ::core::ffi::c_int,
        result: *mut f64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function computes the sum of the absolute values of the elements of vector `x`. Hence, the result is $\left. \sum_{i = 1}^{n} \middle| \mathbf{Im}\left( {x\lbrack j\rbrack} \right) \middle| + \middle| \mathbf{Re}\left( {x\lbrack j\rbrack} \right) \right|$ where $j = 1 + \left( {i - 1} \right)\\*\text{incx}$. Notice that the last equation reflects 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sasum()](http://www.netlib.org/blas/sasum.f), [dasum()](http://www.netlib.org/blas/dasum.f), [scasum()](http://www.netlib.org/blas/scasum.f), [dzasum()](http://www.netlib.org/blas/dzasum.f).
    pub fn cublasDzasum_v2_64(
        handle: cublasHandle_t,
        n: i64,
        x: *const cuDoubleComplex,
        incx: i64,
        result: *mut f64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function applies Givens rotation matrix (i.e., rotation in the x,y plane counter-clockwise by angle defined by $cos(alpha) = c$, $sin(alpha) = s$):
    /// $$
    /// G = \begin{pmatrix}
    /// c & s \\\\
    /// {- s} & c \\\\
    /// \end{pmatrix}
    /// $$
    ///
    /// to vectors `x` and `y`.
    ///
    /// Hence, the result is $\mathbf{x}\lbrack k\rbrack = c \times \mathbf{x}\lbrack k\rbrack + s \times \mathbf{y}\lbrack j\rbrack$ and $\mathbf{y}\lbrack j\rbrack = - s \times \mathbf{x}\lbrack k\rbrack + c \times \mathbf{y}\lbrack j\rbrack$ where $k = 1 + \left( {i - 1} \right)\\*\text{incx}$ and $j = 1 + \left( {i - 1} \right)\\*\text{incy}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [srot()](http://www.netlib.org/blas/srot.f), [drot()](http://www.netlib.org/blas/drot.f), [crot()](http://www.netlib.org/lapack/lapack_routine/crot.f), [csrot()](http://www.netlib.org/blas/csrot.f), [zrot()](http://www.netlib.org/lapack/lapack_routine/zrot.f), [zdrot()](http://www.netlib.org/blas/zdrot.f).
    pub fn cublasSrot_v2(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        x: *mut f32,
        incx: ::core::ffi::c_int,
        y: *mut f32,
        incy: ::core::ffi::c_int,
        c: *const f32,
        s: *const f32,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function applies Givens rotation matrix (i.e., rotation in the x,y plane counter-clockwise by angle defined by $cos(alpha) = c$, $sin(alpha) = s$):
    /// $$
    /// G = \begin{pmatrix}
    /// c & s \\\\
    /// {- s} & c \\\\
    /// \end{pmatrix}
    /// $$
    ///
    /// to vectors `x` and `y`.
    ///
    /// Hence, the result is $\mathbf{x}\lbrack k\rbrack = c \times \mathbf{x}\lbrack k\rbrack + s \times \mathbf{y}\lbrack j\rbrack$ and $\mathbf{y}\lbrack j\rbrack = - s \times \mathbf{x}\lbrack k\rbrack + c \times \mathbf{y}\lbrack j\rbrack$ where $k = 1 + \left( {i - 1} \right)\\*\text{incx}$ and $j = 1 + \left( {i - 1} \right)\\*\text{incy}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [srot()](http://www.netlib.org/blas/srot.f), [drot()](http://www.netlib.org/blas/drot.f), [crot()](http://www.netlib.org/lapack/lapack_routine/crot.f), [csrot()](http://www.netlib.org/blas/csrot.f), [zrot()](http://www.netlib.org/lapack/lapack_routine/zrot.f), [zdrot()](http://www.netlib.org/blas/zdrot.f).
    pub fn cublasSrot_v2_64(
        handle: cublasHandle_t,
        n: i64,
        x: *mut f32,
        incx: i64,
        y: *mut f32,
        incy: i64,
        c: *const f32,
        s: *const f32,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function applies Givens rotation matrix (i.e., rotation in the x,y plane counter-clockwise by angle defined by $cos(alpha) = c$, $sin(alpha) = s$):
    /// $$
    /// G = \begin{pmatrix}
    /// c & s \\\\
    /// {- s} & c \\\\
    /// \end{pmatrix}
    /// $$
    ///
    /// to vectors `x` and `y`.
    ///
    /// Hence, the result is $\mathbf{x}\lbrack k\rbrack = c \times \mathbf{x}\lbrack k\rbrack + s \times \mathbf{y}\lbrack j\rbrack$ and $\mathbf{y}\lbrack j\rbrack = - s \times \mathbf{x}\lbrack k\rbrack + c \times \mathbf{y}\lbrack j\rbrack$ where $k = 1 + \left( {i - 1} \right)\\*\text{incx}$ and $j = 1 + \left( {i - 1} \right)\\*\text{incy}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [srot()](http://www.netlib.org/blas/srot.f), [drot()](http://www.netlib.org/blas/drot.f), [crot()](http://www.netlib.org/lapack/lapack_routine/crot.f), [csrot()](http://www.netlib.org/blas/csrot.f), [zrot()](http://www.netlib.org/lapack/lapack_routine/zrot.f), [zdrot()](http://www.netlib.org/blas/zdrot.f).
    pub fn cublasDrot_v2(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        x: *mut f64,
        incx: ::core::ffi::c_int,
        y: *mut f64,
        incy: ::core::ffi::c_int,
        c: *const f64,
        s: *const f64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function applies Givens rotation matrix (i.e., rotation in the x,y plane counter-clockwise by angle defined by $cos(alpha) = c$, $sin(alpha) = s$):
    /// $$
    /// G = \begin{pmatrix}
    /// c & s \\\\
    /// {- s} & c \\\\
    /// \end{pmatrix}
    /// $$
    ///
    /// to vectors `x` and `y`.
    ///
    /// Hence, the result is $\mathbf{x}\lbrack k\rbrack = c \times \mathbf{x}\lbrack k\rbrack + s \times \mathbf{y}\lbrack j\rbrack$ and $\mathbf{y}\lbrack j\rbrack = - s \times \mathbf{x}\lbrack k\rbrack + c \times \mathbf{y}\lbrack j\rbrack$ where $k = 1 + \left( {i - 1} \right)\\*\text{incx}$ and $j = 1 + \left( {i - 1} \right)\\*\text{incy}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [srot()](http://www.netlib.org/blas/srot.f), [drot()](http://www.netlib.org/blas/drot.f), [crot()](http://www.netlib.org/lapack/lapack_routine/crot.f), [csrot()](http://www.netlib.org/blas/csrot.f), [zrot()](http://www.netlib.org/lapack/lapack_routine/zrot.f), [zdrot()](http://www.netlib.org/blas/zdrot.f).
    pub fn cublasDrot_v2_64(
        handle: cublasHandle_t,
        n: i64,
        x: *mut f64,
        incx: i64,
        y: *mut f64,
        incy: i64,
        c: *const f64,
        s: *const f64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function applies Givens rotation matrix (i.e., rotation in the x,y plane counter-clockwise by angle defined by $cos(alpha) = c$, $sin(alpha) = s$):
    /// $$
    /// G = \begin{pmatrix}
    /// c & s \\\\
    /// {- s} & c \\\\
    /// \end{pmatrix}
    /// $$
    ///
    /// to vectors `x` and `y`.
    ///
    /// Hence, the result is $\mathbf{x}\lbrack k\rbrack = c \times \mathbf{x}\lbrack k\rbrack + s \times \mathbf{y}\lbrack j\rbrack$ and $\mathbf{y}\lbrack j\rbrack = - s \times \mathbf{x}\lbrack k\rbrack + c \times \mathbf{y}\lbrack j\rbrack$ where $k = 1 + \left( {i - 1} \right)\\*\text{incx}$ and $j = 1 + \left( {i - 1} \right)\\*\text{incy}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [srot()](http://www.netlib.org/blas/srot.f), [drot()](http://www.netlib.org/blas/drot.f), [crot()](http://www.netlib.org/lapack/lapack_routine/crot.f), [csrot()](http://www.netlib.org/blas/csrot.f), [zrot()](http://www.netlib.org/lapack/lapack_routine/zrot.f), [zdrot()](http://www.netlib.org/blas/zdrot.f).
    pub fn cublasCrot_v2(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        x: *mut cuComplex,
        incx: ::core::ffi::c_int,
        y: *mut cuComplex,
        incy: ::core::ffi::c_int,
        c: *const f32,
        s: *const cuComplex,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function applies Givens rotation matrix (i.e., rotation in the x,y plane counter-clockwise by angle defined by $cos(alpha) = c$, $sin(alpha) = s$):
    /// $$
    /// G = \begin{pmatrix}
    /// c & s \\\\
    /// {- s} & c \\\\
    /// \end{pmatrix}
    /// $$
    ///
    /// to vectors `x` and `y`.
    ///
    /// Hence, the result is $\mathbf{x}\lbrack k\rbrack = c \times \mathbf{x}\lbrack k\rbrack + s \times \mathbf{y}\lbrack j\rbrack$ and $\mathbf{y}\lbrack j\rbrack = - s \times \mathbf{x}\lbrack k\rbrack + c \times \mathbf{y}\lbrack j\rbrack$ where $k = 1 + \left( {i - 1} \right)\\*\text{incx}$ and $j = 1 + \left( {i - 1} \right)\\*\text{incy}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [srot()](http://www.netlib.org/blas/srot.f), [drot()](http://www.netlib.org/blas/drot.f), [crot()](http://www.netlib.org/lapack/lapack_routine/crot.f), [csrot()](http://www.netlib.org/blas/csrot.f), [zrot()](http://www.netlib.org/lapack/lapack_routine/zrot.f), [zdrot()](http://www.netlib.org/blas/zdrot.f).
    pub fn cublasCrot_v2_64(
        handle: cublasHandle_t,
        n: i64,
        x: *mut cuComplex,
        incx: i64,
        y: *mut cuComplex,
        incy: i64,
        c: *const f32,
        s: *const cuComplex,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function applies Givens rotation matrix (i.e., rotation in the x,y plane counter-clockwise by angle defined by $cos(alpha) = c$, $sin(alpha) = s$):
    /// $$
    /// G = \begin{pmatrix}
    /// c & s \\\\
    /// {- s} & c \\\\
    /// \end{pmatrix}
    /// $$
    ///
    /// to vectors `x` and `y`.
    ///
    /// Hence, the result is $\mathbf{x}\lbrack k\rbrack = c \times \mathbf{x}\lbrack k\rbrack + s \times \mathbf{y}\lbrack j\rbrack$ and $\mathbf{y}\lbrack j\rbrack = - s \times \mathbf{x}\lbrack k\rbrack + c \times \mathbf{y}\lbrack j\rbrack$ where $k = 1 + \left( {i - 1} \right)\\*\text{incx}$ and $j = 1 + \left( {i - 1} \right)\\*\text{incy}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [srot()](http://www.netlib.org/blas/srot.f), [drot()](http://www.netlib.org/blas/drot.f), [crot()](http://www.netlib.org/lapack/lapack_routine/crot.f), [csrot()](http://www.netlib.org/blas/csrot.f), [zrot()](http://www.netlib.org/lapack/lapack_routine/zrot.f), [zdrot()](http://www.netlib.org/blas/zdrot.f).
    pub fn cublasCsrot_v2(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        x: *mut cuComplex,
        incx: ::core::ffi::c_int,
        y: *mut cuComplex,
        incy: ::core::ffi::c_int,
        c: *const f32,
        s: *const f32,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function applies Givens rotation matrix (i.e., rotation in the x,y plane counter-clockwise by angle defined by $cos(alpha) = c$, $sin(alpha) = s$):
    /// $$
    /// G = \begin{pmatrix}
    /// c & s \\\\
    /// {- s} & c \\\\
    /// \end{pmatrix}
    /// $$
    ///
    /// to vectors `x` and `y`.
    ///
    /// Hence, the result is $\mathbf{x}\lbrack k\rbrack = c \times \mathbf{x}\lbrack k\rbrack + s \times \mathbf{y}\lbrack j\rbrack$ and $\mathbf{y}\lbrack j\rbrack = - s \times \mathbf{x}\lbrack k\rbrack + c \times \mathbf{y}\lbrack j\rbrack$ where $k = 1 + \left( {i - 1} \right)\\*\text{incx}$ and $j = 1 + \left( {i - 1} \right)\\*\text{incy}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [srot()](http://www.netlib.org/blas/srot.f), [drot()](http://www.netlib.org/blas/drot.f), [crot()](http://www.netlib.org/lapack/lapack_routine/crot.f), [csrot()](http://www.netlib.org/blas/csrot.f), [zrot()](http://www.netlib.org/lapack/lapack_routine/zrot.f), [zdrot()](http://www.netlib.org/blas/zdrot.f).
    pub fn cublasCsrot_v2_64(
        handle: cublasHandle_t,
        n: i64,
        x: *mut cuComplex,
        incx: i64,
        y: *mut cuComplex,
        incy: i64,
        c: *const f32,
        s: *const f32,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function applies Givens rotation matrix (i.e., rotation in the x,y plane counter-clockwise by angle defined by $cos(alpha) = c$, $sin(alpha) = s$):
    /// $$
    /// G = \begin{pmatrix}
    /// c & s \\\\
    /// {- s} & c \\\\
    /// \end{pmatrix}
    /// $$
    ///
    /// to vectors `x` and `y`.
    ///
    /// Hence, the result is $\mathbf{x}\lbrack k\rbrack = c \times \mathbf{x}\lbrack k\rbrack + s \times \mathbf{y}\lbrack j\rbrack$ and $\mathbf{y}\lbrack j\rbrack = - s \times \mathbf{x}\lbrack k\rbrack + c \times \mathbf{y}\lbrack j\rbrack$ where $k = 1 + \left( {i - 1} \right)\\*\text{incx}$ and $j = 1 + \left( {i - 1} \right)\\*\text{incy}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [srot()](http://www.netlib.org/blas/srot.f), [drot()](http://www.netlib.org/blas/drot.f), [crot()](http://www.netlib.org/lapack/lapack_routine/crot.f), [csrot()](http://www.netlib.org/blas/csrot.f), [zrot()](http://www.netlib.org/lapack/lapack_routine/zrot.f), [zdrot()](http://www.netlib.org/blas/zdrot.f).
    pub fn cublasZrot_v2(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        x: *mut cuDoubleComplex,
        incx: ::core::ffi::c_int,
        y: *mut cuDoubleComplex,
        incy: ::core::ffi::c_int,
        c: *const f64,
        s: *const cuDoubleComplex,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function applies Givens rotation matrix (i.e., rotation in the x,y plane counter-clockwise by angle defined by $cos(alpha) = c$, $sin(alpha) = s$):
    /// $$
    /// G = \begin{pmatrix}
    /// c & s \\\\
    /// {- s} & c \\\\
    /// \end{pmatrix}
    /// $$
    ///
    /// to vectors `x` and `y`.
    ///
    /// Hence, the result is $\mathbf{x}\lbrack k\rbrack = c \times \mathbf{x}\lbrack k\rbrack + s \times \mathbf{y}\lbrack j\rbrack$ and $\mathbf{y}\lbrack j\rbrack = - s \times \mathbf{x}\lbrack k\rbrack + c \times \mathbf{y}\lbrack j\rbrack$ where $k = 1 + \left( {i - 1} \right)\\*\text{incx}$ and $j = 1 + \left( {i - 1} \right)\\*\text{incy}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [srot()](http://www.netlib.org/blas/srot.f), [drot()](http://www.netlib.org/blas/drot.f), [crot()](http://www.netlib.org/lapack/lapack_routine/crot.f), [csrot()](http://www.netlib.org/blas/csrot.f), [zrot()](http://www.netlib.org/lapack/lapack_routine/zrot.f), [zdrot()](http://www.netlib.org/blas/zdrot.f).
    pub fn cublasZrot_v2_64(
        handle: cublasHandle_t,
        n: i64,
        x: *mut cuDoubleComplex,
        incx: i64,
        y: *mut cuDoubleComplex,
        incy: i64,
        c: *const f64,
        s: *const cuDoubleComplex,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function applies Givens rotation matrix (i.e., rotation in the x,y plane counter-clockwise by angle defined by $cos(alpha) = c$, $sin(alpha) = s$):
    /// $$
    /// G = \begin{pmatrix}
    /// c & s \\\\
    /// {- s} & c \\\\
    /// \end{pmatrix}
    /// $$
    ///
    /// to vectors `x` and `y`.
    ///
    /// Hence, the result is $\mathbf{x}\lbrack k\rbrack = c \times \mathbf{x}\lbrack k\rbrack + s \times \mathbf{y}\lbrack j\rbrack$ and $\mathbf{y}\lbrack j\rbrack = - s \times \mathbf{x}\lbrack k\rbrack + c \times \mathbf{y}\lbrack j\rbrack$ where $k = 1 + \left( {i - 1} \right)\\*\text{incx}$ and $j = 1 + \left( {i - 1} \right)\\*\text{incy}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [srot()](http://www.netlib.org/blas/srot.f), [drot()](http://www.netlib.org/blas/drot.f), [crot()](http://www.netlib.org/lapack/lapack_routine/crot.f), [csrot()](http://www.netlib.org/blas/csrot.f), [zrot()](http://www.netlib.org/lapack/lapack_routine/zrot.f), [zdrot()](http://www.netlib.org/blas/zdrot.f).
    pub fn cublasZdrot_v2(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        x: *mut cuDoubleComplex,
        incx: ::core::ffi::c_int,
        y: *mut cuDoubleComplex,
        incy: ::core::ffi::c_int,
        c: *const f64,
        s: *const f64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function applies Givens rotation matrix (i.e., rotation in the x,y plane counter-clockwise by angle defined by $cos(alpha) = c$, $sin(alpha) = s$):
    /// $$
    /// G = \begin{pmatrix}
    /// c & s \\\\
    /// {- s} & c \\\\
    /// \end{pmatrix}
    /// $$
    ///
    /// to vectors `x` and `y`.
    ///
    /// Hence, the result is $\mathbf{x}\lbrack k\rbrack = c \times \mathbf{x}\lbrack k\rbrack + s \times \mathbf{y}\lbrack j\rbrack$ and $\mathbf{y}\lbrack j\rbrack = - s \times \mathbf{x}\lbrack k\rbrack + c \times \mathbf{y}\lbrack j\rbrack$ where $k = 1 + \left( {i - 1} \right)\\*\text{incx}$ and $j = 1 + \left( {i - 1} \right)\\*\text{incy}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [srot()](http://www.netlib.org/blas/srot.f), [drot()](http://www.netlib.org/blas/drot.f), [crot()](http://www.netlib.org/lapack/lapack_routine/crot.f), [csrot()](http://www.netlib.org/blas/csrot.f), [zrot()](http://www.netlib.org/lapack/lapack_routine/zrot.f), [zdrot()](http://www.netlib.org/blas/zdrot.f).
    pub fn cublasZdrot_v2_64(
        handle: cublasHandle_t,
        n: i64,
        x: *mut cuDoubleComplex,
        incx: i64,
        y: *mut cuDoubleComplex,
        incy: i64,
        c: *const f64,
        s: *const f64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function is an extension to the routine cublas&lt;t>rot() where input data, output data, cosine/sine type, and compute type can be specified independently.
    ///
    /// This function applies Givens rotation matrix (i.e., rotation in the x,y plane counter-clockwise by angle defined by $cos(alpha) = c$, $sin(alpha) = s$):
    /// $$
    /// G = \begin{pmatrix}
    /// c & s \\\\
    /// {- s} & c \\\\
    /// \end{pmatrix}
    /// $$
    ///
    /// to vectors `x` and `y`.
    ///
    /// Hence, the result is $\mathbf{x}\lbrack k\rbrack = c \times \mathbf{x}\lbrack k\rbrack + s \times \mathbf{y}\lbrack j\rbrack$ and $\mathbf{y}\lbrack j\rbrack = - s \times \mathbf{x}\lbrack k\rbrack + c \times \mathbf{y}\lbrack j\rbrack$ where $k = 1 + \left( {i - 1} \right)\\*\text{incx}$ and $j = 1 + \left( {i - 1} \right)\\*\text{incy}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// The datatypes combinations currently supported for [`cublasRotEx`] are listed below :
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [srot()](http://www.netlib.org/blas/srot.f), [drot()](http://www.netlib.org/blas/drot.f), [crot()](http://www.netlib.org/lapack/lapack_routine/crot.f), [csrot()](http://www.netlib.org/blas/csrot.f), [zrot()](http://www.netlib.org/lapack/lapack_routine/zrot.f), [zdrot()](http://www.netlib.org/blas/zdrot.f).
    ///
    /// # Parameters
    ///
    /// - `handle`: Handle to the cuBLAS library context.
    /// - `n`: Number of elements in the vectors `x` and `y`.
    /// - `x`: &lt;`type`> vector with `n` elements.
    /// - `xType`: Enumerant specifying the datatype of vector `x`.
    /// - `incx`: Stride between consecutive elements of `x`.
    /// - `y`: &lt;`type`> vector with `n` elements.
    /// - `yType`: Enumerant specifying the datatype of vector `y`.
    /// - `incy`: Stride between consecutive elements of `y`.
    /// - `c`: Cosine element of the rotation matrix.
    /// - `s`: Sine element of the rotation matrix.
    /// - `csType`: Enumerant specifying the datatype of `c` and `s`.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_EXECUTION_FAILED`]: The function failed to launch on the GPU.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_INITIALIZED`]: The library was not initialized.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The operation completed successfully.
    pub fn cublasRotEx(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        x: *mut ::core::ffi::c_void,
        xType: cudaDataType,
        incx: ::core::ffi::c_int,
        y: *mut ::core::ffi::c_void,
        yType: cudaDataType,
        incy: ::core::ffi::c_int,
        c: *const ::core::ffi::c_void,
        s: *const ::core::ffi::c_void,
        csType: cudaDataType,
        executiontype: cudaDataType,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function is an extension to the routine cublas&lt;t>rot() where input data, output data, cosine/sine type, and compute type can be specified independently.
    ///
    /// This function applies Givens rotation matrix (i.e., rotation in the x,y plane counter-clockwise by angle defined by $cos(alpha) = c$, $sin(alpha) = s$):
    /// $$
    /// G = \begin{pmatrix}
    /// c & s \\\\
    /// {- s} & c \\\\
    /// \end{pmatrix}
    /// $$
    ///
    /// to vectors `x` and `y`.
    ///
    /// Hence, the result is $\mathbf{x}\lbrack k\rbrack = c \times \mathbf{x}\lbrack k\rbrack + s \times \mathbf{y}\lbrack j\rbrack$ and $\mathbf{y}\lbrack j\rbrack = - s \times \mathbf{x}\lbrack k\rbrack + c \times \mathbf{y}\lbrack j\rbrack$ where $k = 1 + \left( {i - 1} \right)\\*\text{incx}$ and $j = 1 + \left( {i - 1} \right)\\*\text{incy}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// The datatypes combinations currently supported for [`cublasRotEx`] are listed below :
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [srot()](http://www.netlib.org/blas/srot.f), [drot()](http://www.netlib.org/blas/drot.f), [crot()](http://www.netlib.org/lapack/lapack_routine/crot.f), [csrot()](http://www.netlib.org/blas/csrot.f), [zrot()](http://www.netlib.org/lapack/lapack_routine/zrot.f), [zdrot()](http://www.netlib.org/blas/zdrot.f).
    ///
    /// # Parameters
    ///
    /// - `handle`: Handle to the cuBLAS library context.
    /// - `n`: Number of elements in the vectors `x` and `y`.
    /// - `x`: &lt;`type`> vector with `n` elements.
    /// - `xType`: Enumerant specifying the datatype of vector `x`.
    /// - `incx`: Stride between consecutive elements of `x`.
    /// - `y`: &lt;`type`> vector with `n` elements.
    /// - `yType`: Enumerant specifying the datatype of vector `y`.
    /// - `incy`: Stride between consecutive elements of `y`.
    /// - `c`: Cosine element of the rotation matrix.
    /// - `s`: Sine element of the rotation matrix.
    /// - `csType`: Enumerant specifying the datatype of `c` and `s`.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_EXECUTION_FAILED`]: The function failed to launch on the GPU.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_INITIALIZED`]: The library was not initialized.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The operation completed successfully.
    pub fn cublasRotEx_64(
        handle: cublasHandle_t,
        n: i64,
        x: *mut ::core::ffi::c_void,
        xType: cudaDataType,
        incx: i64,
        y: *mut ::core::ffi::c_void,
        yType: cudaDataType,
        incy: i64,
        c: *const ::core::ffi::c_void,
        s: *const ::core::ffi::c_void,
        csType: cudaDataType,
        executiontype: cudaDataType,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function constructs the Givens rotation matrix:
    /// $$
    /// G = \begin{pmatrix}
    /// c & s \\\\
    /// {- s} & c \\\\
    /// \end{pmatrix}
    /// $$
    ///
    /// that zeros out the second entry of a $2 \times 1$ vector $\left( {a,b} \right)^{T}$.
    ///
    /// Then, for real numbers we can write:
    /// $$
    /// \begin{pmatrix}
    /// c & s \\\\
    /// {- s} & c \\\\
    /// \end{pmatrix}\begin{pmatrix}
    /// a \\\\
    /// b \\\\
    /// \end{pmatrix} = \begin{pmatrix}
    /// r \\\\
    /// 0 \\\\
    /// \end{pmatrix}
    /// $$
    ///
    /// where $c^{2} + s^{2} = 1$ and $r = \pm \sqrt{a^{2} + b^{2}}$. The parameters $a$ and $b$ are overwritten with $r$ and $z$, respectively. The value of $z$ is such that $c$ and $s$ may be recovered using the following rules:
    /// $$
    /// \left( {c,s} \right) = \begin{cases}
    /// \left( {\sqrt{1 - z^{2}},z} \right) & {\text{ if }\left| z \middle| &lt; 1 \right.} \\\\
    /// \left( {0.0,1.0} \right) & {\text{ if }\left| z \middle| = 1 \right.} \\\\
    /// \left( 1/z,\sqrt{1 - z^{2}} \right) & {\text{ if }\left| z \middle| > 1 \right.} \\\\
    /// \end{cases}
    /// $$
    ///
    /// For complex numbers we can write:
    /// $$
    /// \begin{pmatrix}
    /// c & s \\\\
    /// {- \bar{s}} & c \\\\
    /// \end{pmatrix}\begin{pmatrix}
    /// a \\\\
    /// b \\\\
    /// \end{pmatrix} = \begin{pmatrix}
    /// r \\\\
    /// 0 \\\\
    /// \end{pmatrix}
    /// $$
    ///
    /// where $c^{2} + \left( {\bar{s} \times s} \right) = 1$ and $r = \frac{a}{|a|} \times \parallel \left( {a,b} \right)^{T} \parallel_{2}$ with $\parallel \left( {a,b} \right)^{T} \parallel_{2} = \sqrt{\left| a|^{2} + \middle| B|^{2} \right.}$ for $a \neq 0$ and $r = b$ for $a = 0$. Finally, the parameter $a$ is overwritten with $r$ on exit.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [srotg()](http://www.netlib.org/blas/srotg.f90), [drotg()](http://www.netlib.org/blas/drotg.f90), [crotg()](http://www.netlib.org/blas/crotg.f90), [zrotg()](http://www.netlib.org/blas/zrotg.f90).
    pub fn cublasSrotg_v2(
        handle: cublasHandle_t,
        a: *mut f32,
        b: *mut f32,
        c: *mut f32,
        s: *mut f32,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function constructs the Givens rotation matrix:
    /// $$
    /// G = \begin{pmatrix}
    /// c & s \\\\
    /// {- s} & c \\\\
    /// \end{pmatrix}
    /// $$
    ///
    /// that zeros out the second entry of a $2 \times 1$ vector $\left( {a,b} \right)^{T}$.
    ///
    /// Then, for real numbers we can write:
    /// $$
    /// \begin{pmatrix}
    /// c & s \\\\
    /// {- s} & c \\\\
    /// \end{pmatrix}\begin{pmatrix}
    /// a \\\\
    /// b \\\\
    /// \end{pmatrix} = \begin{pmatrix}
    /// r \\\\
    /// 0 \\\\
    /// \end{pmatrix}
    /// $$
    ///
    /// where $c^{2} + s^{2} = 1$ and $r = \pm \sqrt{a^{2} + b^{2}}$. The parameters $a$ and $b$ are overwritten with $r$ and $z$, respectively. The value of $z$ is such that $c$ and $s$ may be recovered using the following rules:
    /// $$
    /// \left( {c,s} \right) = \begin{cases}
    /// \left( {\sqrt{1 - z^{2}},z} \right) & {\text{ if }\left| z \middle| &lt; 1 \right.} \\\\
    /// \left( {0.0,1.0} \right) & {\text{ if }\left| z \middle| = 1 \right.} \\\\
    /// \left( 1/z,\sqrt{1 - z^{2}} \right) & {\text{ if }\left| z \middle| > 1 \right.} \\\\
    /// \end{cases}
    /// $$
    ///
    /// For complex numbers we can write:
    /// $$
    /// \begin{pmatrix}
    /// c & s \\\\
    /// {- \bar{s}} & c \\\\
    /// \end{pmatrix}\begin{pmatrix}
    /// a \\\\
    /// b \\\\
    /// \end{pmatrix} = \begin{pmatrix}
    /// r \\\\
    /// 0 \\\\
    /// \end{pmatrix}
    /// $$
    ///
    /// where $c^{2} + \left( {\bar{s} \times s} \right) = 1$ and $r = \frac{a}{|a|} \times \parallel \left( {a,b} \right)^{T} \parallel_{2}$ with $\parallel \left( {a,b} \right)^{T} \parallel_{2} = \sqrt{\left| a|^{2} + \middle| B|^{2} \right.}$ for $a \neq 0$ and $r = b$ for $a = 0$. Finally, the parameter $a$ is overwritten with $r$ on exit.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [srotg()](http://www.netlib.org/blas/srotg.f90), [drotg()](http://www.netlib.org/blas/drotg.f90), [crotg()](http://www.netlib.org/blas/crotg.f90), [zrotg()](http://www.netlib.org/blas/zrotg.f90).
    pub fn cublasDrotg_v2(
        handle: cublasHandle_t,
        a: *mut f64,
        b: *mut f64,
        c: *mut f64,
        s: *mut f64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function constructs the Givens rotation matrix:
    /// $$
    /// G = \begin{pmatrix}
    /// c & s \\\\
    /// {- s} & c \\\\
    /// \end{pmatrix}
    /// $$
    ///
    /// that zeros out the second entry of a $2 \times 1$ vector $\left( {a,b} \right)^{T}$.
    ///
    /// Then, for real numbers we can write:
    /// $$
    /// \begin{pmatrix}
    /// c & s \\\\
    /// {- s} & c \\\\
    /// \end{pmatrix}\begin{pmatrix}
    /// a \\\\
    /// b \\\\
    /// \end{pmatrix} = \begin{pmatrix}
    /// r \\\\
    /// 0 \\\\
    /// \end{pmatrix}
    /// $$
    ///
    /// where $c^{2} + s^{2} = 1$ and $r = \pm \sqrt{a^{2} + b^{2}}$. The parameters $a$ and $b$ are overwritten with $r$ and $z$, respectively. The value of $z$ is such that $c$ and $s$ may be recovered using the following rules:
    /// $$
    /// \left( {c,s} \right) = \begin{cases}
    /// \left( {\sqrt{1 - z^{2}},z} \right) & {\text{ if }\left| z \middle| &lt; 1 \right.} \\\\
    /// \left( {0.0,1.0} \right) & {\text{ if }\left| z \middle| = 1 \right.} \\\\
    /// \left( 1/z,\sqrt{1 - z^{2}} \right) & {\text{ if }\left| z \middle| > 1 \right.} \\\\
    /// \end{cases}
    /// $$
    ///
    /// For complex numbers we can write:
    /// $$
    /// \begin{pmatrix}
    /// c & s \\\\
    /// {- \bar{s}} & c \\\\
    /// \end{pmatrix}\begin{pmatrix}
    /// a \\\\
    /// b \\\\
    /// \end{pmatrix} = \begin{pmatrix}
    /// r \\\\
    /// 0 \\\\
    /// \end{pmatrix}
    /// $$
    ///
    /// where $c^{2} + \left( {\bar{s} \times s} \right) = 1$ and $r = \frac{a}{|a|} \times \parallel \left( {a,b} \right)^{T} \parallel_{2}$ with $\parallel \left( {a,b} \right)^{T} \parallel_{2} = \sqrt{\left| a|^{2} + \middle| B|^{2} \right.}$ for $a \neq 0$ and $r = b$ for $a = 0$. Finally, the parameter $a$ is overwritten with $r$ on exit.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [srotg()](http://www.netlib.org/blas/srotg.f90), [drotg()](http://www.netlib.org/blas/drotg.f90), [crotg()](http://www.netlib.org/blas/crotg.f90), [zrotg()](http://www.netlib.org/blas/zrotg.f90).
    pub fn cublasCrotg_v2(
        handle: cublasHandle_t,
        a: *mut cuComplex,
        b: *mut cuComplex,
        c: *mut f32,
        s: *mut cuComplex,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function constructs the Givens rotation matrix:
    /// $$
    /// G = \begin{pmatrix}
    /// c & s \\\\
    /// {- s} & c \\\\
    /// \end{pmatrix}
    /// $$
    ///
    /// that zeros out the second entry of a $2 \times 1$ vector $\left( {a,b} \right)^{T}$.
    ///
    /// Then, for real numbers we can write:
    /// $$
    /// \begin{pmatrix}
    /// c & s \\\\
    /// {- s} & c \\\\
    /// \end{pmatrix}\begin{pmatrix}
    /// a \\\\
    /// b \\\\
    /// \end{pmatrix} = \begin{pmatrix}
    /// r \\\\
    /// 0 \\\\
    /// \end{pmatrix}
    /// $$
    ///
    /// where $c^{2} + s^{2} = 1$ and $r = \pm \sqrt{a^{2} + b^{2}}$. The parameters $a$ and $b$ are overwritten with $r$ and $z$, respectively. The value of $z$ is such that $c$ and $s$ may be recovered using the following rules:
    /// $$
    /// \left( {c,s} \right) = \begin{cases}
    /// \left( {\sqrt{1 - z^{2}},z} \right) & {\text{ if }\left| z \middle| &lt; 1 \right.} \\\\
    /// \left( {0.0,1.0} \right) & {\text{ if }\left| z \middle| = 1 \right.} \\\\
    /// \left( 1/z,\sqrt{1 - z^{2}} \right) & {\text{ if }\left| z \middle| > 1 \right.} \\\\
    /// \end{cases}
    /// $$
    ///
    /// For complex numbers we can write:
    /// $$
    /// \begin{pmatrix}
    /// c & s \\\\
    /// {- \bar{s}} & c \\\\
    /// \end{pmatrix}\begin{pmatrix}
    /// a \\\\
    /// b \\\\
    /// \end{pmatrix} = \begin{pmatrix}
    /// r \\\\
    /// 0 \\\\
    /// \end{pmatrix}
    /// $$
    ///
    /// where $c^{2} + \left( {\bar{s} \times s} \right) = 1$ and $r = \frac{a}{|a|} \times \parallel \left( {a,b} \right)^{T} \parallel_{2}$ with $\parallel \left( {a,b} \right)^{T} \parallel_{2} = \sqrt{\left| a|^{2} + \middle| B|^{2} \right.}$ for $a \neq 0$ and $r = b$ for $a = 0$. Finally, the parameter $a$ is overwritten with $r$ on exit.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [srotg()](http://www.netlib.org/blas/srotg.f90), [drotg()](http://www.netlib.org/blas/drotg.f90), [crotg()](http://www.netlib.org/blas/crotg.f90), [zrotg()](http://www.netlib.org/blas/zrotg.f90).
    pub fn cublasZrotg_v2(
        handle: cublasHandle_t,
        a: *mut cuDoubleComplex,
        b: *mut cuDoubleComplex,
        c: *mut f64,
        s: *mut cuDoubleComplex,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    pub fn cublasRotgEx(
        handle: cublasHandle_t,
        a: *mut ::core::ffi::c_void,
        b: *mut ::core::ffi::c_void,
        abType: cudaDataType,
        c: *mut ::core::ffi::c_void,
        s: *mut ::core::ffi::c_void,
        csType: cudaDataType,
        executiontype: cudaDataType,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function applies the modified Givens transformation:
    /// $$
    /// H = \begin{pmatrix}
    /// h_{11} & h_{12} \\\\
    /// h_{21} & h_{22} \\\\
    /// \end{pmatrix}
    /// $$
    ///
    /// to vectors `x` and `y`.
    ///
    /// Hence, the result is $\mathbf{x}\lbrack k\rbrack = h_{11} \times \mathbf{x}\lbrack k\rbrack + h_{12} \times \mathbf{y}\lbrack j\rbrack$ and $\mathbf{y}\lbrack j\rbrack = h_{21} \times \mathbf{x}\lbrack k\rbrack + h_{22} \times \mathbf{y}\lbrack j\rbrack$ where $k = 1 + \left( {i - 1} \right)\\*\text{incx}$ and $j = 1 + \left( {i - 1} \right)\\*\text{incy}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// The elements , , and of matrix $H$ are stored in `param\[1\]`, `param\[2\]`, `param\[3\]` and `param\[4\]`, respectively. The `flag = param\[0\]` defines the following predefined values for the matrix $H$ entries
    ///
    /// Notice that the values -1.0, 0.0 and 1.0 implied by the flag are not stored in param.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [srotm()](http://www.netlib.org/blas/srotm.f), [drotm()](http://www.netlib.org/blas/drotm.f).
    pub fn cublasSrotm_v2(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        x: *mut f32,
        incx: ::core::ffi::c_int,
        y: *mut f32,
        incy: ::core::ffi::c_int,
        param: *const f32,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function applies the modified Givens transformation:
    /// $$
    /// H = \begin{pmatrix}
    /// h_{11} & h_{12} \\\\
    /// h_{21} & h_{22} \\\\
    /// \end{pmatrix}
    /// $$
    ///
    /// to vectors `x` and `y`.
    ///
    /// Hence, the result is $\mathbf{x}\lbrack k\rbrack = h_{11} \times \mathbf{x}\lbrack k\rbrack + h_{12} \times \mathbf{y}\lbrack j\rbrack$ and $\mathbf{y}\lbrack j\rbrack = h_{21} \times \mathbf{x}\lbrack k\rbrack + h_{22} \times \mathbf{y}\lbrack j\rbrack$ where $k = 1 + \left( {i - 1} \right)\\*\text{incx}$ and $j = 1 + \left( {i - 1} \right)\\*\text{incy}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// The elements , , and of matrix $H$ are stored in `param\[1\]`, `param\[2\]`, `param\[3\]` and `param\[4\]`, respectively. The `flag = param\[0\]` defines the following predefined values for the matrix $H$ entries
    ///
    /// Notice that the values -1.0, 0.0 and 1.0 implied by the flag are not stored in param.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [srotm()](http://www.netlib.org/blas/srotm.f), [drotm()](http://www.netlib.org/blas/drotm.f).
    pub fn cublasSrotm_v2_64(
        handle: cublasHandle_t,
        n: i64,
        x: *mut f32,
        incx: i64,
        y: *mut f32,
        incy: i64,
        param: *const f32,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function applies the modified Givens transformation:
    /// $$
    /// H = \begin{pmatrix}
    /// h_{11} & h_{12} \\\\
    /// h_{21} & h_{22} \\\\
    /// \end{pmatrix}
    /// $$
    ///
    /// to vectors `x` and `y`.
    ///
    /// Hence, the result is $\mathbf{x}\lbrack k\rbrack = h_{11} \times \mathbf{x}\lbrack k\rbrack + h_{12} \times \mathbf{y}\lbrack j\rbrack$ and $\mathbf{y}\lbrack j\rbrack = h_{21} \times \mathbf{x}\lbrack k\rbrack + h_{22} \times \mathbf{y}\lbrack j\rbrack$ where $k = 1 + \left( {i - 1} \right)\\*\text{incx}$ and $j = 1 + \left( {i - 1} \right)\\*\text{incy}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// The elements , , and of matrix $H$ are stored in `param\[1\]`, `param\[2\]`, `param\[3\]` and `param\[4\]`, respectively. The `flag = param\[0\]` defines the following predefined values for the matrix $H$ entries
    ///
    /// Notice that the values -1.0, 0.0 and 1.0 implied by the flag are not stored in param.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [srotm()](http://www.netlib.org/blas/srotm.f), [drotm()](http://www.netlib.org/blas/drotm.f).
    pub fn cublasDrotm_v2(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        x: *mut f64,
        incx: ::core::ffi::c_int,
        y: *mut f64,
        incy: ::core::ffi::c_int,
        param: *const f64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function applies the modified Givens transformation:
    /// $$
    /// H = \begin{pmatrix}
    /// h_{11} & h_{12} \\\\
    /// h_{21} & h_{22} \\\\
    /// \end{pmatrix}
    /// $$
    ///
    /// to vectors `x` and `y`.
    ///
    /// Hence, the result is $\mathbf{x}\lbrack k\rbrack = h_{11} \times \mathbf{x}\lbrack k\rbrack + h_{12} \times \mathbf{y}\lbrack j\rbrack$ and $\mathbf{y}\lbrack j\rbrack = h_{21} \times \mathbf{x}\lbrack k\rbrack + h_{22} \times \mathbf{y}\lbrack j\rbrack$ where $k = 1 + \left( {i - 1} \right)\\*\text{incx}$ and $j = 1 + \left( {i - 1} \right)\\*\text{incy}$. Notice that the last two equations reflect 1-based indexing used for compatibility with Fortran.
    ///
    /// The elements , , and of matrix $H$ are stored in `param\[1\]`, `param\[2\]`, `param\[3\]` and `param\[4\]`, respectively. The `flag = param\[0\]` defines the following predefined values for the matrix $H$ entries
    ///
    /// Notice that the values -1.0, 0.0 and 1.0 implied by the flag are not stored in param.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [srotm()](http://www.netlib.org/blas/srotm.f), [drotm()](http://www.netlib.org/blas/drotm.f).
    pub fn cublasDrotm_v2_64(
        handle: cublasHandle_t,
        n: i64,
        x: *mut f64,
        incx: i64,
        y: *mut f64,
        incy: i64,
        param: *const f64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    pub fn cublasRotmEx(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        x: *mut ::core::ffi::c_void,
        xType: cudaDataType,
        incx: ::core::ffi::c_int,
        y: *mut ::core::ffi::c_void,
        yType: cudaDataType,
        incy: ::core::ffi::c_int,
        param: *const ::core::ffi::c_void,
        paramType: cudaDataType,
        executiontype: cudaDataType,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    pub fn cublasRotmEx_64(
        handle: cublasHandle_t,
        n: i64,
        x: *mut ::core::ffi::c_void,
        xType: cudaDataType,
        incx: i64,
        y: *mut ::core::ffi::c_void,
        yType: cudaDataType,
        incy: i64,
        param: *const ::core::ffi::c_void,
        paramType: cudaDataType,
        executiontype: cudaDataType,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function constructs the modified Givens transformation:
    /// $$
    /// H = \begin{pmatrix}
    /// h_{11} & h_{12} \\\\
    /// h_{21} & h_{22} \\\\
    /// \end{pmatrix}
    /// $$
    ///
    /// that zeros out the second entry of a $2 \times 1$ vector $\left( {\sqrt{d1}\\*x1,\sqrt{d2}\\*y1} \right)^{T}$.
    ///
    /// The `flag = param\[0\]` defines the following predefined values for the matrix $H$ entries
    ///
    /// Notice that the values -1.0, 0.0 and 1.0 implied by the flag are not stored in param.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [srotmg()](http://www.netlib.org/blas/srotmg.f), [drotmg()](http://www.netlib.org/blas/drotmg.f).
    pub fn cublasSrotmg_v2(
        handle: cublasHandle_t,
        d1: *mut f32,
        d2: *mut f32,
        x1: *mut f32,
        y1: *const f32,
        param: *mut f32,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function constructs the modified Givens transformation:
    /// $$
    /// H = \begin{pmatrix}
    /// h_{11} & h_{12} \\\\
    /// h_{21} & h_{22} \\\\
    /// \end{pmatrix}
    /// $$
    ///
    /// that zeros out the second entry of a $2 \times 1$ vector $\left( {\sqrt{d1}\\*x1,\sqrt{d2}\\*y1} \right)^{T}$.
    ///
    /// The `flag = param\[0\]` defines the following predefined values for the matrix $H$ entries
    ///
    /// Notice that the values -1.0, 0.0 and 1.0 implied by the flag are not stored in param.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [srotmg()](http://www.netlib.org/blas/srotmg.f), [drotmg()](http://www.netlib.org/blas/drotmg.f).
    pub fn cublasDrotmg_v2(
        handle: cublasHandle_t,
        d1: *mut f64,
        d2: *mut f64,
        x1: *mut f64,
        y1: *const f64,
        param: *mut f64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    pub fn cublasRotmgEx(
        handle: cublasHandle_t,
        d1: *mut ::core::ffi::c_void,
        d1Type: cudaDataType,
        d2: *mut ::core::ffi::c_void,
        d2Type: cudaDataType,
        x1: *mut ::core::ffi::c_void,
        x1Type: cudaDataType,
        y1: *const ::core::ffi::c_void,
        y1Type: cudaDataType,
        param: *mut ::core::ffi::c_void,
        paramType: cudaDataType,
        executiontype: cudaDataType,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-vector multiplication:
    /// $$
    /// \textbf{y} = \alpha\text{ op}(A)\textbf{x} + \beta\textbf{y}
    /// $$
    ///
    /// where $A$ is a $m \times n$ matrix stored in column-major format, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ and $\beta$ are scalars. Also, for matrix $A$:
    /// $$
    /// \text{ op}(A) = \begin{cases}
    /// A & \text{ if trans == $\mathrm{CUBLAS_OP_N}$} \\\\
    /// A^{T} & \text{ if trans == $\mathrm{CUBLAS_OP_T}$} \\\\
    /// A^{H} & \text{ if trans == $\mathrm{CUBLAS_OP_C}$} \\\\
    /// \end{cases}
    /// $$
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sgemv()](http://www.netlib.org/blas/sgemv.f), [dgemv()](http://www.netlib.org/blas/dgemv.f), [cgemv()](http://www.netlib.org/blas/cgemv.f), [zgemv()](http://www.netlib.org/blas/zgemv.f).
    pub fn cublasSgemv_v2(
        handle: cublasHandle_t,
        trans: cublasOperation_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        alpha: *const f32,
        A: *const f32,
        lda: ::core::ffi::c_int,
        x: *const f32,
        incx: ::core::ffi::c_int,
        beta: *const f32,
        y: *mut f32,
        incy: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-vector multiplication:
    /// $$
    /// \textbf{y} = \alpha\text{ op}(A)\textbf{x} + \beta\textbf{y}
    /// $$
    ///
    /// where $A$ is a $m \times n$ matrix stored in column-major format, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ and $\beta$ are scalars. Also, for matrix $A$:
    /// $$
    /// \text{ op}(A) = \begin{cases}
    /// A & \text{ if trans == $\mathrm{CUBLAS_OP_N}$} \\\\
    /// A^{T} & \text{ if trans == $\mathrm{CUBLAS_OP_T}$} \\\\
    /// A^{H} & \text{ if trans == $\mathrm{CUBLAS_OP_C}$} \\\\
    /// \end{cases}
    /// $$
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sgemv()](http://www.netlib.org/blas/sgemv.f), [dgemv()](http://www.netlib.org/blas/dgemv.f), [cgemv()](http://www.netlib.org/blas/cgemv.f), [zgemv()](http://www.netlib.org/blas/zgemv.f).
    pub fn cublasSgemv_v2_64(
        handle: cublasHandle_t,
        trans: cublasOperation_t,
        m: i64,
        n: i64,
        alpha: *const f32,
        A: *const f32,
        lda: i64,
        x: *const f32,
        incx: i64,
        beta: *const f32,
        y: *mut f32,
        incy: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-vector multiplication:
    /// $$
    /// \textbf{y} = \alpha\text{ op}(A)\textbf{x} + \beta\textbf{y}
    /// $$
    ///
    /// where $A$ is a $m \times n$ matrix stored in column-major format, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ and $\beta$ are scalars. Also, for matrix $A$:
    /// $$
    /// \text{ op}(A) = \begin{cases}
    /// A & \text{ if trans == $\mathrm{CUBLAS_OP_N}$} \\\\
    /// A^{T} & \text{ if trans == $\mathrm{CUBLAS_OP_T}$} \\\\
    /// A^{H} & \text{ if trans == $\mathrm{CUBLAS_OP_C}$} \\\\
    /// \end{cases}
    /// $$
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sgemv()](http://www.netlib.org/blas/sgemv.f), [dgemv()](http://www.netlib.org/blas/dgemv.f), [cgemv()](http://www.netlib.org/blas/cgemv.f), [zgemv()](http://www.netlib.org/blas/zgemv.f).
    pub fn cublasDgemv_v2(
        handle: cublasHandle_t,
        trans: cublasOperation_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        alpha: *const f64,
        A: *const f64,
        lda: ::core::ffi::c_int,
        x: *const f64,
        incx: ::core::ffi::c_int,
        beta: *const f64,
        y: *mut f64,
        incy: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-vector multiplication:
    /// $$
    /// \textbf{y} = \alpha\text{ op}(A)\textbf{x} + \beta\textbf{y}
    /// $$
    ///
    /// where $A$ is a $m \times n$ matrix stored in column-major format, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ and $\beta$ are scalars. Also, for matrix $A$:
    /// $$
    /// \text{ op}(A) = \begin{cases}
    /// A & \text{ if trans == $\mathrm{CUBLAS_OP_N}$} \\\\
    /// A^{T} & \text{ if trans == $\mathrm{CUBLAS_OP_T}$} \\\\
    /// A^{H} & \text{ if trans == $\mathrm{CUBLAS_OP_C}$} \\\\
    /// \end{cases}
    /// $$
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sgemv()](http://www.netlib.org/blas/sgemv.f), [dgemv()](http://www.netlib.org/blas/dgemv.f), [cgemv()](http://www.netlib.org/blas/cgemv.f), [zgemv()](http://www.netlib.org/blas/zgemv.f).
    pub fn cublasDgemv_v2_64(
        handle: cublasHandle_t,
        trans: cublasOperation_t,
        m: i64,
        n: i64,
        alpha: *const f64,
        A: *const f64,
        lda: i64,
        x: *const f64,
        incx: i64,
        beta: *const f64,
        y: *mut f64,
        incy: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-vector multiplication:
    /// $$
    /// \textbf{y} = \alpha\text{ op}(A)\textbf{x} + \beta\textbf{y}
    /// $$
    ///
    /// where $A$ is a $m \times n$ matrix stored in column-major format, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ and $\beta$ are scalars. Also, for matrix $A$:
    /// $$
    /// \text{ op}(A) = \begin{cases}
    /// A & \text{ if trans == $\mathrm{CUBLAS_OP_N}$} \\\\
    /// A^{T} & \text{ if trans == $\mathrm{CUBLAS_OP_T}$} \\\\
    /// A^{H} & \text{ if trans == $\mathrm{CUBLAS_OP_C}$} \\\\
    /// \end{cases}
    /// $$
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sgemv()](http://www.netlib.org/blas/sgemv.f), [dgemv()](http://www.netlib.org/blas/dgemv.f), [cgemv()](http://www.netlib.org/blas/cgemv.f), [zgemv()](http://www.netlib.org/blas/zgemv.f).
    pub fn cublasCgemv_v2(
        handle: cublasHandle_t,
        trans: cublasOperation_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        alpha: *const cuComplex,
        A: *const cuComplex,
        lda: ::core::ffi::c_int,
        x: *const cuComplex,
        incx: ::core::ffi::c_int,
        beta: *const cuComplex,
        y: *mut cuComplex,
        incy: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-vector multiplication:
    /// $$
    /// \textbf{y} = \alpha\text{ op}(A)\textbf{x} + \beta\textbf{y}
    /// $$
    ///
    /// where $A$ is a $m \times n$ matrix stored in column-major format, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ and $\beta$ are scalars. Also, for matrix $A$:
    /// $$
    /// \text{ op}(A) = \begin{cases}
    /// A & \text{ if trans == $\mathrm{CUBLAS_OP_N}$} \\\\
    /// A^{T} & \text{ if trans == $\mathrm{CUBLAS_OP_T}$} \\\\
    /// A^{H} & \text{ if trans == $\mathrm{CUBLAS_OP_C}$} \\\\
    /// \end{cases}
    /// $$
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sgemv()](http://www.netlib.org/blas/sgemv.f), [dgemv()](http://www.netlib.org/blas/dgemv.f), [cgemv()](http://www.netlib.org/blas/cgemv.f), [zgemv()](http://www.netlib.org/blas/zgemv.f).
    pub fn cublasCgemv_v2_64(
        handle: cublasHandle_t,
        trans: cublasOperation_t,
        m: i64,
        n: i64,
        alpha: *const cuComplex,
        A: *const cuComplex,
        lda: i64,
        x: *const cuComplex,
        incx: i64,
        beta: *const cuComplex,
        y: *mut cuComplex,
        incy: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-vector multiplication:
    /// $$
    /// \textbf{y} = \alpha\text{ op}(A)\textbf{x} + \beta\textbf{y}
    /// $$
    ///
    /// where $A$ is a $m \times n$ matrix stored in column-major format, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ and $\beta$ are scalars. Also, for matrix $A$:
    /// $$
    /// \text{ op}(A) = \begin{cases}
    /// A & \text{ if trans == $\mathrm{CUBLAS_OP_N}$} \\\\
    /// A^{T} & \text{ if trans == $\mathrm{CUBLAS_OP_T}$} \\\\
    /// A^{H} & \text{ if trans == $\mathrm{CUBLAS_OP_C}$} \\\\
    /// \end{cases}
    /// $$
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sgemv()](http://www.netlib.org/blas/sgemv.f), [dgemv()](http://www.netlib.org/blas/dgemv.f), [cgemv()](http://www.netlib.org/blas/cgemv.f), [zgemv()](http://www.netlib.org/blas/zgemv.f).
    pub fn cublasZgemv_v2(
        handle: cublasHandle_t,
        trans: cublasOperation_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        alpha: *const cuDoubleComplex,
        A: *const cuDoubleComplex,
        lda: ::core::ffi::c_int,
        x: *const cuDoubleComplex,
        incx: ::core::ffi::c_int,
        beta: *const cuDoubleComplex,
        y: *mut cuDoubleComplex,
        incy: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-vector multiplication:
    /// $$
    /// \textbf{y} = \alpha\text{ op}(A)\textbf{x} + \beta\textbf{y}
    /// $$
    ///
    /// where $A$ is a $m \times n$ matrix stored in column-major format, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ and $\beta$ are scalars. Also, for matrix $A$:
    /// $$
    /// \text{ op}(A) = \begin{cases}
    /// A & \text{ if trans == $\mathrm{CUBLAS_OP_N}$} \\\\
    /// A^{T} & \text{ if trans == $\mathrm{CUBLAS_OP_T}$} \\\\
    /// A^{H} & \text{ if trans == $\mathrm{CUBLAS_OP_C}$} \\\\
    /// \end{cases}
    /// $$
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sgemv()](http://www.netlib.org/blas/sgemv.f), [dgemv()](http://www.netlib.org/blas/dgemv.f), [cgemv()](http://www.netlib.org/blas/cgemv.f), [zgemv()](http://www.netlib.org/blas/zgemv.f).
    pub fn cublasZgemv_v2_64(
        handle: cublasHandle_t,
        trans: cublasOperation_t,
        m: i64,
        n: i64,
        alpha: *const cuDoubleComplex,
        A: *const cuDoubleComplex,
        lda: i64,
        x: *const cuDoubleComplex,
        incx: i64,
        beta: *const cuDoubleComplex,
        y: *mut cuDoubleComplex,
        incy: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the banded matrix-vector multiplication:
    /// $$
    /// \mathbf{y} = \alpha\text{ op}(A)\mathbf{x} + \beta\mathbf{y}
    /// $$
    ///
    /// where $A$ is a banded matrix with $kl$ subdiagonals and $ku$ superdiagonals, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ and $\beta$ are scalars. Also, for matrix $A$:
    /// $$
    /// \text{ op}(A) = \begin{cases}
    /// A & \text{ if trans == $\mathrm{CUBLAS_OP_N}$} \\\\
    /// A^{T} & \text{ if trans == $\mathrm{CUBLAS_OP_T}$} \\\\
    /// A^{H} & \text{ if trans == $\mathrm{CUBLAS_OP_C}$} \\\\
    /// \end{cases}
    /// $$
    ///
    /// The banded matrix $A$ is stored column by column, with the main diagonal stored in row $ku + 1$ (starting in first position), the first superdiagonal stored in row $ku$ (starting in second position), the first subdiagonal stored in row $ku + 2$ (starting in first position), etc. So that in general, the element $A\left( {i,j} \right)$ is stored in the memory location `A(ku+1+i-j,j)` for $j = 1,\ldots,n$ and $i \in \left\lbrack {\max\left( {1,j - ku} \right),\min\left( {m,j + kl} \right)} \right\rbrack$. Also, the elements in the array $A$ that do not conceptually correspond to the elements in the banded matrix (the top left $ku \times ku$ and bottom right $kl \times kl$ triangles) are not referenced.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sgbmv()](http://www.netlib.org/blas/sgbmv.f), [dgbmv()](http://www.netlib.org/blas/dgbmv.f), [cgbmv()](http://www.netlib.org/blas/cgbmv.f), [zgbmv()](http://www.netlib.org/blas/zgbmv.f).
    pub fn cublasSgbmv_v2(
        handle: cublasHandle_t,
        trans: cublasOperation_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        kl: ::core::ffi::c_int,
        ku: ::core::ffi::c_int,
        alpha: *const f32,
        A: *const f32,
        lda: ::core::ffi::c_int,
        x: *const f32,
        incx: ::core::ffi::c_int,
        beta: *const f32,
        y: *mut f32,
        incy: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the banded matrix-vector multiplication:
    /// $$
    /// \mathbf{y} = \alpha\text{ op}(A)\mathbf{x} + \beta\mathbf{y}
    /// $$
    ///
    /// where $A$ is a banded matrix with $kl$ subdiagonals and $ku$ superdiagonals, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ and $\beta$ are scalars. Also, for matrix $A$:
    /// $$
    /// \text{ op}(A) = \begin{cases}
    /// A & \text{ if trans == $\mathrm{CUBLAS_OP_N}$} \\\\
    /// A^{T} & \text{ if trans == $\mathrm{CUBLAS_OP_T}$} \\\\
    /// A^{H} & \text{ if trans == $\mathrm{CUBLAS_OP_C}$} \\\\
    /// \end{cases}
    /// $$
    ///
    /// The banded matrix $A$ is stored column by column, with the main diagonal stored in row $ku + 1$ (starting in first position), the first superdiagonal stored in row $ku$ (starting in second position), the first subdiagonal stored in row $ku + 2$ (starting in first position), etc. So that in general, the element $A\left( {i,j} \right)$ is stored in the memory location `A(ku+1+i-j,j)` for $j = 1,\ldots,n$ and $i \in \left\lbrack {\max\left( {1,j - ku} \right),\min\left( {m,j + kl} \right)} \right\rbrack$. Also, the elements in the array $A$ that do not conceptually correspond to the elements in the banded matrix (the top left $ku \times ku$ and bottom right $kl \times kl$ triangles) are not referenced.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sgbmv()](http://www.netlib.org/blas/sgbmv.f), [dgbmv()](http://www.netlib.org/blas/dgbmv.f), [cgbmv()](http://www.netlib.org/blas/cgbmv.f), [zgbmv()](http://www.netlib.org/blas/zgbmv.f).
    pub fn cublasSgbmv_v2_64(
        handle: cublasHandle_t,
        trans: cublasOperation_t,
        m: i64,
        n: i64,
        kl: i64,
        ku: i64,
        alpha: *const f32,
        A: *const f32,
        lda: i64,
        x: *const f32,
        incx: i64,
        beta: *const f32,
        y: *mut f32,
        incy: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the banded matrix-vector multiplication:
    /// $$
    /// \mathbf{y} = \alpha\text{ op}(A)\mathbf{x} + \beta\mathbf{y}
    /// $$
    ///
    /// where $A$ is a banded matrix with $kl$ subdiagonals and $ku$ superdiagonals, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ and $\beta$ are scalars. Also, for matrix $A$:
    /// $$
    /// \text{ op}(A) = \begin{cases}
    /// A & \text{ if trans == $\mathrm{CUBLAS_OP_N}$} \\\\
    /// A^{T} & \text{ if trans == $\mathrm{CUBLAS_OP_T}$} \\\\
    /// A^{H} & \text{ if trans == $\mathrm{CUBLAS_OP_C}$} \\\\
    /// \end{cases}
    /// $$
    ///
    /// The banded matrix $A$ is stored column by column, with the main diagonal stored in row $ku + 1$ (starting in first position), the first superdiagonal stored in row $ku$ (starting in second position), the first subdiagonal stored in row $ku + 2$ (starting in first position), etc. So that in general, the element $A\left( {i,j} \right)$ is stored in the memory location `A(ku+1+i-j,j)` for $j = 1,\ldots,n$ and $i \in \left\lbrack {\max\left( {1,j - ku} \right),\min\left( {m,j + kl} \right)} \right\rbrack$. Also, the elements in the array $A$ that do not conceptually correspond to the elements in the banded matrix (the top left $ku \times ku$ and bottom right $kl \times kl$ triangles) are not referenced.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sgbmv()](http://www.netlib.org/blas/sgbmv.f), [dgbmv()](http://www.netlib.org/blas/dgbmv.f), [cgbmv()](http://www.netlib.org/blas/cgbmv.f), [zgbmv()](http://www.netlib.org/blas/zgbmv.f).
    pub fn cublasDgbmv_v2(
        handle: cublasHandle_t,
        trans: cublasOperation_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        kl: ::core::ffi::c_int,
        ku: ::core::ffi::c_int,
        alpha: *const f64,
        A: *const f64,
        lda: ::core::ffi::c_int,
        x: *const f64,
        incx: ::core::ffi::c_int,
        beta: *const f64,
        y: *mut f64,
        incy: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the banded matrix-vector multiplication:
    /// $$
    /// \mathbf{y} = \alpha\text{ op}(A)\mathbf{x} + \beta\mathbf{y}
    /// $$
    ///
    /// where $A$ is a banded matrix with $kl$ subdiagonals and $ku$ superdiagonals, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ and $\beta$ are scalars. Also, for matrix $A$:
    /// $$
    /// \text{ op}(A) = \begin{cases}
    /// A & \text{ if trans == $\mathrm{CUBLAS_OP_N}$} \\\\
    /// A^{T} & \text{ if trans == $\mathrm{CUBLAS_OP_T}$} \\\\
    /// A^{H} & \text{ if trans == $\mathrm{CUBLAS_OP_C}$} \\\\
    /// \end{cases}
    /// $$
    ///
    /// The banded matrix $A$ is stored column by column, with the main diagonal stored in row $ku + 1$ (starting in first position), the first superdiagonal stored in row $ku$ (starting in second position), the first subdiagonal stored in row $ku + 2$ (starting in first position), etc. So that in general, the element $A\left( {i,j} \right)$ is stored in the memory location `A(ku+1+i-j,j)` for $j = 1,\ldots,n$ and $i \in \left\lbrack {\max\left( {1,j - ku} \right),\min\left( {m,j + kl} \right)} \right\rbrack$. Also, the elements in the array $A$ that do not conceptually correspond to the elements in the banded matrix (the top left $ku \times ku$ and bottom right $kl \times kl$ triangles) are not referenced.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sgbmv()](http://www.netlib.org/blas/sgbmv.f), [dgbmv()](http://www.netlib.org/blas/dgbmv.f), [cgbmv()](http://www.netlib.org/blas/cgbmv.f), [zgbmv()](http://www.netlib.org/blas/zgbmv.f).
    pub fn cublasDgbmv_v2_64(
        handle: cublasHandle_t,
        trans: cublasOperation_t,
        m: i64,
        n: i64,
        kl: i64,
        ku: i64,
        alpha: *const f64,
        A: *const f64,
        lda: i64,
        x: *const f64,
        incx: i64,
        beta: *const f64,
        y: *mut f64,
        incy: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the banded matrix-vector multiplication:
    /// $$
    /// \mathbf{y} = \alpha\text{ op}(A)\mathbf{x} + \beta\mathbf{y}
    /// $$
    ///
    /// where $A$ is a banded matrix with $kl$ subdiagonals and $ku$ superdiagonals, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ and $\beta$ are scalars. Also, for matrix $A$:
    /// $$
    /// \text{ op}(A) = \begin{cases}
    /// A & \text{ if trans == $\mathrm{CUBLAS_OP_N}$} \\\\
    /// A^{T} & \text{ if trans == $\mathrm{CUBLAS_OP_T}$} \\\\
    /// A^{H} & \text{ if trans == $\mathrm{CUBLAS_OP_C}$} \\\\
    /// \end{cases}
    /// $$
    ///
    /// The banded matrix $A$ is stored column by column, with the main diagonal stored in row $ku + 1$ (starting in first position), the first superdiagonal stored in row $ku$ (starting in second position), the first subdiagonal stored in row $ku + 2$ (starting in first position), etc. So that in general, the element $A\left( {i,j} \right)$ is stored in the memory location `A(ku+1+i-j,j)` for $j = 1,\ldots,n$ and $i \in \left\lbrack {\max\left( {1,j - ku} \right),\min\left( {m,j + kl} \right)} \right\rbrack$. Also, the elements in the array $A$ that do not conceptually correspond to the elements in the banded matrix (the top left $ku \times ku$ and bottom right $kl \times kl$ triangles) are not referenced.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sgbmv()](http://www.netlib.org/blas/sgbmv.f), [dgbmv()](http://www.netlib.org/blas/dgbmv.f), [cgbmv()](http://www.netlib.org/blas/cgbmv.f), [zgbmv()](http://www.netlib.org/blas/zgbmv.f).
    pub fn cublasCgbmv_v2(
        handle: cublasHandle_t,
        trans: cublasOperation_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        kl: ::core::ffi::c_int,
        ku: ::core::ffi::c_int,
        alpha: *const cuComplex,
        A: *const cuComplex,
        lda: ::core::ffi::c_int,
        x: *const cuComplex,
        incx: ::core::ffi::c_int,
        beta: *const cuComplex,
        y: *mut cuComplex,
        incy: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the banded matrix-vector multiplication:
    /// $$
    /// \mathbf{y} = \alpha\text{ op}(A)\mathbf{x} + \beta\mathbf{y}
    /// $$
    ///
    /// where $A$ is a banded matrix with $kl$ subdiagonals and $ku$ superdiagonals, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ and $\beta$ are scalars. Also, for matrix $A$:
    /// $$
    /// \text{ op}(A) = \begin{cases}
    /// A & \text{ if trans == $\mathrm{CUBLAS_OP_N}$} \\\\
    /// A^{T} & \text{ if trans == $\mathrm{CUBLAS_OP_T}$} \\\\
    /// A^{H} & \text{ if trans == $\mathrm{CUBLAS_OP_C}$} \\\\
    /// \end{cases}
    /// $$
    ///
    /// The banded matrix $A$ is stored column by column, with the main diagonal stored in row $ku + 1$ (starting in first position), the first superdiagonal stored in row $ku$ (starting in second position), the first subdiagonal stored in row $ku + 2$ (starting in first position), etc. So that in general, the element $A\left( {i,j} \right)$ is stored in the memory location `A(ku+1+i-j,j)` for $j = 1,\ldots,n$ and $i \in \left\lbrack {\max\left( {1,j - ku} \right),\min\left( {m,j + kl} \right)} \right\rbrack$. Also, the elements in the array $A$ that do not conceptually correspond to the elements in the banded matrix (the top left $ku \times ku$ and bottom right $kl \times kl$ triangles) are not referenced.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sgbmv()](http://www.netlib.org/blas/sgbmv.f), [dgbmv()](http://www.netlib.org/blas/dgbmv.f), [cgbmv()](http://www.netlib.org/blas/cgbmv.f), [zgbmv()](http://www.netlib.org/blas/zgbmv.f).
    pub fn cublasCgbmv_v2_64(
        handle: cublasHandle_t,
        trans: cublasOperation_t,
        m: i64,
        n: i64,
        kl: i64,
        ku: i64,
        alpha: *const cuComplex,
        A: *const cuComplex,
        lda: i64,
        x: *const cuComplex,
        incx: i64,
        beta: *const cuComplex,
        y: *mut cuComplex,
        incy: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the banded matrix-vector multiplication:
    /// $$
    /// \mathbf{y} = \alpha\text{ op}(A)\mathbf{x} + \beta\mathbf{y}
    /// $$
    ///
    /// where $A$ is a banded matrix with $kl$ subdiagonals and $ku$ superdiagonals, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ and $\beta$ are scalars. Also, for matrix $A$:
    /// $$
    /// \text{ op}(A) = \begin{cases}
    /// A & \text{ if trans == $\mathrm{CUBLAS_OP_N}$} \\\\
    /// A^{T} & \text{ if trans == $\mathrm{CUBLAS_OP_T}$} \\\\
    /// A^{H} & \text{ if trans == $\mathrm{CUBLAS_OP_C}$} \\\\
    /// \end{cases}
    /// $$
    ///
    /// The banded matrix $A$ is stored column by column, with the main diagonal stored in row $ku + 1$ (starting in first position), the first superdiagonal stored in row $ku$ (starting in second position), the first subdiagonal stored in row $ku + 2$ (starting in first position), etc. So that in general, the element $A\left( {i,j} \right)$ is stored in the memory location `A(ku+1+i-j,j)` for $j = 1,\ldots,n$ and $i \in \left\lbrack {\max\left( {1,j - ku} \right),\min\left( {m,j + kl} \right)} \right\rbrack$. Also, the elements in the array $A$ that do not conceptually correspond to the elements in the banded matrix (the top left $ku \times ku$ and bottom right $kl \times kl$ triangles) are not referenced.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sgbmv()](http://www.netlib.org/blas/sgbmv.f), [dgbmv()](http://www.netlib.org/blas/dgbmv.f), [cgbmv()](http://www.netlib.org/blas/cgbmv.f), [zgbmv()](http://www.netlib.org/blas/zgbmv.f).
    pub fn cublasZgbmv_v2(
        handle: cublasHandle_t,
        trans: cublasOperation_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        kl: ::core::ffi::c_int,
        ku: ::core::ffi::c_int,
        alpha: *const cuDoubleComplex,
        A: *const cuDoubleComplex,
        lda: ::core::ffi::c_int,
        x: *const cuDoubleComplex,
        incx: ::core::ffi::c_int,
        beta: *const cuDoubleComplex,
        y: *mut cuDoubleComplex,
        incy: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the banded matrix-vector multiplication:
    /// $$
    /// \mathbf{y} = \alpha\text{ op}(A)\mathbf{x} + \beta\mathbf{y}
    /// $$
    ///
    /// where $A$ is a banded matrix with $kl$ subdiagonals and $ku$ superdiagonals, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ and $\beta$ are scalars. Also, for matrix $A$:
    /// $$
    /// \text{ op}(A) = \begin{cases}
    /// A & \text{ if trans == $\mathrm{CUBLAS_OP_N}$} \\\\
    /// A^{T} & \text{ if trans == $\mathrm{CUBLAS_OP_T}$} \\\\
    /// A^{H} & \text{ if trans == $\mathrm{CUBLAS_OP_C}$} \\\\
    /// \end{cases}
    /// $$
    ///
    /// The banded matrix $A$ is stored column by column, with the main diagonal stored in row $ku + 1$ (starting in first position), the first superdiagonal stored in row $ku$ (starting in second position), the first subdiagonal stored in row $ku + 2$ (starting in first position), etc. So that in general, the element $A\left( {i,j} \right)$ is stored in the memory location `A(ku+1+i-j,j)` for $j = 1,\ldots,n$ and $i \in \left\lbrack {\max\left( {1,j - ku} \right),\min\left( {m,j + kl} \right)} \right\rbrack$. Also, the elements in the array $A$ that do not conceptually correspond to the elements in the banded matrix (the top left $ku \times ku$ and bottom right $kl \times kl$ triangles) are not referenced.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sgbmv()](http://www.netlib.org/blas/sgbmv.f), [dgbmv()](http://www.netlib.org/blas/dgbmv.f), [cgbmv()](http://www.netlib.org/blas/cgbmv.f), [zgbmv()](http://www.netlib.org/blas/zgbmv.f).
    pub fn cublasZgbmv_v2_64(
        handle: cublasHandle_t,
        trans: cublasOperation_t,
        m: i64,
        n: i64,
        kl: i64,
        ku: i64,
        alpha: *const cuDoubleComplex,
        A: *const cuDoubleComplex,
        lda: i64,
        x: *const cuDoubleComplex,
        incx: i64,
        beta: *const cuDoubleComplex,
        y: *mut cuDoubleComplex,
        incy: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the triangular matrix-vector multiplication:
    /// $$
    /// \textbf{x} = \text{op}(A)\textbf{x}
    /// $$
    ///
    /// where $A$ is a triangular matrix stored in lower or upper mode with or without the main diagonal, and $\mathbf{x}$ is a vector. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [strmv()](http://www.netlib.org/blas/strmv.f), [dtrmv()](http://www.netlib.org/blas/dtrmv.f), [ctrmv()](http://www.netlib.org/blas/ctrmv.f), [ztrmv()](http://www.netlib.org/blas/ztrmv.f).
    pub fn cublasStrmv_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        n: ::core::ffi::c_int,
        A: *const f32,
        lda: ::core::ffi::c_int,
        x: *mut f32,
        incx: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the triangular matrix-vector multiplication:
    /// $$
    /// \textbf{x} = \text{op}(A)\textbf{x}
    /// $$
    ///
    /// where $A$ is a triangular matrix stored in lower or upper mode with or without the main diagonal, and $\mathbf{x}$ is a vector. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [strmv()](http://www.netlib.org/blas/strmv.f), [dtrmv()](http://www.netlib.org/blas/dtrmv.f), [ctrmv()](http://www.netlib.org/blas/ctrmv.f), [ztrmv()](http://www.netlib.org/blas/ztrmv.f).
    pub fn cublasStrmv_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        n: i64,
        A: *const f32,
        lda: i64,
        x: *mut f32,
        incx: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the triangular matrix-vector multiplication:
    /// $$
    /// \textbf{x} = \text{op}(A)\textbf{x}
    /// $$
    ///
    /// where $A$ is a triangular matrix stored in lower or upper mode with or without the main diagonal, and $\mathbf{x}$ is a vector. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [strmv()](http://www.netlib.org/blas/strmv.f), [dtrmv()](http://www.netlib.org/blas/dtrmv.f), [ctrmv()](http://www.netlib.org/blas/ctrmv.f), [ztrmv()](http://www.netlib.org/blas/ztrmv.f).
    pub fn cublasDtrmv_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        n: ::core::ffi::c_int,
        A: *const f64,
        lda: ::core::ffi::c_int,
        x: *mut f64,
        incx: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the triangular matrix-vector multiplication:
    /// $$
    /// \textbf{x} = \text{op}(A)\textbf{x}
    /// $$
    ///
    /// where $A$ is a triangular matrix stored in lower or upper mode with or without the main diagonal, and $\mathbf{x}$ is a vector. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [strmv()](http://www.netlib.org/blas/strmv.f), [dtrmv()](http://www.netlib.org/blas/dtrmv.f), [ctrmv()](http://www.netlib.org/blas/ctrmv.f), [ztrmv()](http://www.netlib.org/blas/ztrmv.f).
    pub fn cublasDtrmv_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        n: i64,
        A: *const f64,
        lda: i64,
        x: *mut f64,
        incx: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the triangular matrix-vector multiplication:
    /// $$
    /// \textbf{x} = \text{op}(A)\textbf{x}
    /// $$
    ///
    /// where $A$ is a triangular matrix stored in lower or upper mode with or without the main diagonal, and $\mathbf{x}$ is a vector. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [strmv()](http://www.netlib.org/blas/strmv.f), [dtrmv()](http://www.netlib.org/blas/dtrmv.f), [ctrmv()](http://www.netlib.org/blas/ctrmv.f), [ztrmv()](http://www.netlib.org/blas/ztrmv.f).
    pub fn cublasCtrmv_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        n: ::core::ffi::c_int,
        A: *const cuComplex,
        lda: ::core::ffi::c_int,
        x: *mut cuComplex,
        incx: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the triangular matrix-vector multiplication:
    /// $$
    /// \textbf{x} = \text{op}(A)\textbf{x}
    /// $$
    ///
    /// where $A$ is a triangular matrix stored in lower or upper mode with or without the main diagonal, and $\mathbf{x}$ is a vector. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [strmv()](http://www.netlib.org/blas/strmv.f), [dtrmv()](http://www.netlib.org/blas/dtrmv.f), [ctrmv()](http://www.netlib.org/blas/ctrmv.f), [ztrmv()](http://www.netlib.org/blas/ztrmv.f).
    pub fn cublasCtrmv_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        n: i64,
        A: *const cuComplex,
        lda: i64,
        x: *mut cuComplex,
        incx: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the triangular matrix-vector multiplication:
    /// $$
    /// \textbf{x} = \text{op}(A)\textbf{x}
    /// $$
    ///
    /// where $A$ is a triangular matrix stored in lower or upper mode with or without the main diagonal, and $\mathbf{x}$ is a vector. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [strmv()](http://www.netlib.org/blas/strmv.f), [dtrmv()](http://www.netlib.org/blas/dtrmv.f), [ctrmv()](http://www.netlib.org/blas/ctrmv.f), [ztrmv()](http://www.netlib.org/blas/ztrmv.f).
    pub fn cublasZtrmv_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        n: ::core::ffi::c_int,
        A: *const cuDoubleComplex,
        lda: ::core::ffi::c_int,
        x: *mut cuDoubleComplex,
        incx: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the triangular matrix-vector multiplication:
    /// $$
    /// \textbf{x} = \text{op}(A)\textbf{x}
    /// $$
    ///
    /// where $A$ is a triangular matrix stored in lower or upper mode with or without the main diagonal, and $\mathbf{x}$ is a vector. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [strmv()](http://www.netlib.org/blas/strmv.f), [dtrmv()](http://www.netlib.org/blas/dtrmv.f), [ctrmv()](http://www.netlib.org/blas/ctrmv.f), [ztrmv()](http://www.netlib.org/blas/ztrmv.f).
    pub fn cublasZtrmv_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        n: i64,
        A: *const cuDoubleComplex,
        lda: i64,
        x: *mut cuDoubleComplex,
        incx: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the triangular banded matrix-vector multiplication:
    /// $$
    /// \textbf{x} = \text{op}(A)\textbf{x}
    /// $$
    ///
    /// where $A$ is a triangular banded matrix, and $\mathbf{x}$ is a vector. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the triangular banded matrix $A$ is stored column by column, with the main diagonal of the matrix stored in row `1`, the first subdiagonal in row `2` (starting at first position), the second subdiagonal in row `3` (starting at first position), etc. So that in general, the element $A(i,j)$ is stored in the memory location `A(1+i-j,j)` for $j = 1,\ldots,n$ and $i \in \lbrack j,\min(m,j + k)\rbrack$. Also, the elements in the array `A` that do not conceptually correspond to the elements in the banded matrix (the bottom right $k \times k$ triangle) are not referenced.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the triangular banded matrix $A$ is stored column by column, with the main diagonal of the matrix stored in row `k + 1`, the first superdiagonal in row `k` (starting at second position), the second superdiagonal in row `k-1` (starting at third position), etc. So that in general, the element $A(i,j)$ is stored in the memory location `A(1+k+i-j,j)` for $j = 1,\ldots,n$ and $i \in \lbrack\max(1,j - k,j)\rbrack$. Also, the elements in the array `A` that do not conceptually correspond to the elements in the banded matrix (the top left $k \times k$ triangle) are not referenced.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [stbmv()](http://www.netlib.org/blas/stbmv.f), [dtbmv()](http://www.netlib.org/blas/dtbmv.f), [ctbmv()](http://www.netlib.org/blas/ctbmv.f), [ztbmv()](http://www.netlib.org/blas/ztbmv.f).
    pub fn cublasStbmv_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        A: *const f32,
        lda: ::core::ffi::c_int,
        x: *mut f32,
        incx: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the triangular banded matrix-vector multiplication:
    /// $$
    /// \textbf{x} = \text{op}(A)\textbf{x}
    /// $$
    ///
    /// where $A$ is a triangular banded matrix, and $\mathbf{x}$ is a vector. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the triangular banded matrix $A$ is stored column by column, with the main diagonal of the matrix stored in row `1`, the first subdiagonal in row `2` (starting at first position), the second subdiagonal in row `3` (starting at first position), etc. So that in general, the element $A(i,j)$ is stored in the memory location `A(1+i-j,j)` for $j = 1,\ldots,n$ and $i \in \lbrack j,\min(m,j + k)\rbrack$. Also, the elements in the array `A` that do not conceptually correspond to the elements in the banded matrix (the bottom right $k \times k$ triangle) are not referenced.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the triangular banded matrix $A$ is stored column by column, with the main diagonal of the matrix stored in row `k + 1`, the first superdiagonal in row `k` (starting at second position), the second superdiagonal in row `k-1` (starting at third position), etc. So that in general, the element $A(i,j)$ is stored in the memory location `A(1+k+i-j,j)` for $j = 1,\ldots,n$ and $i \in \lbrack\max(1,j - k,j)\rbrack$. Also, the elements in the array `A` that do not conceptually correspond to the elements in the banded matrix (the top left $k \times k$ triangle) are not referenced.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [stbmv()](http://www.netlib.org/blas/stbmv.f), [dtbmv()](http://www.netlib.org/blas/dtbmv.f), [ctbmv()](http://www.netlib.org/blas/ctbmv.f), [ztbmv()](http://www.netlib.org/blas/ztbmv.f).
    pub fn cublasStbmv_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        n: i64,
        k: i64,
        A: *const f32,
        lda: i64,
        x: *mut f32,
        incx: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the triangular banded matrix-vector multiplication:
    /// $$
    /// \textbf{x} = \text{op}(A)\textbf{x}
    /// $$
    ///
    /// where $A$ is a triangular banded matrix, and $\mathbf{x}$ is a vector. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the triangular banded matrix $A$ is stored column by column, with the main diagonal of the matrix stored in row `1`, the first subdiagonal in row `2` (starting at first position), the second subdiagonal in row `3` (starting at first position), etc. So that in general, the element $A(i,j)$ is stored in the memory location `A(1+i-j,j)` for $j = 1,\ldots,n$ and $i \in \lbrack j,\min(m,j + k)\rbrack$. Also, the elements in the array `A` that do not conceptually correspond to the elements in the banded matrix (the bottom right $k \times k$ triangle) are not referenced.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the triangular banded matrix $A$ is stored column by column, with the main diagonal of the matrix stored in row `k + 1`, the first superdiagonal in row `k` (starting at second position), the second superdiagonal in row `k-1` (starting at third position), etc. So that in general, the element $A(i,j)$ is stored in the memory location `A(1+k+i-j,j)` for $j = 1,\ldots,n$ and $i \in \lbrack\max(1,j - k,j)\rbrack$. Also, the elements in the array `A` that do not conceptually correspond to the elements in the banded matrix (the top left $k \times k$ triangle) are not referenced.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [stbmv()](http://www.netlib.org/blas/stbmv.f), [dtbmv()](http://www.netlib.org/blas/dtbmv.f), [ctbmv()](http://www.netlib.org/blas/ctbmv.f), [ztbmv()](http://www.netlib.org/blas/ztbmv.f).
    pub fn cublasDtbmv_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        A: *const f64,
        lda: ::core::ffi::c_int,
        x: *mut f64,
        incx: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the triangular banded matrix-vector multiplication:
    /// $$
    /// \textbf{x} = \text{op}(A)\textbf{x}
    /// $$
    ///
    /// where $A$ is a triangular banded matrix, and $\mathbf{x}$ is a vector. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the triangular banded matrix $A$ is stored column by column, with the main diagonal of the matrix stored in row `1`, the first subdiagonal in row `2` (starting at first position), the second subdiagonal in row `3` (starting at first position), etc. So that in general, the element $A(i,j)$ is stored in the memory location `A(1+i-j,j)` for $j = 1,\ldots,n$ and $i \in \lbrack j,\min(m,j + k)\rbrack$. Also, the elements in the array `A` that do not conceptually correspond to the elements in the banded matrix (the bottom right $k \times k$ triangle) are not referenced.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the triangular banded matrix $A$ is stored column by column, with the main diagonal of the matrix stored in row `k + 1`, the first superdiagonal in row `k` (starting at second position), the second superdiagonal in row `k-1` (starting at third position), etc. So that in general, the element $A(i,j)$ is stored in the memory location `A(1+k+i-j,j)` for $j = 1,\ldots,n$ and $i \in \lbrack\max(1,j - k,j)\rbrack$. Also, the elements in the array `A` that do not conceptually correspond to the elements in the banded matrix (the top left $k \times k$ triangle) are not referenced.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [stbmv()](http://www.netlib.org/blas/stbmv.f), [dtbmv()](http://www.netlib.org/blas/dtbmv.f), [ctbmv()](http://www.netlib.org/blas/ctbmv.f), [ztbmv()](http://www.netlib.org/blas/ztbmv.f).
    pub fn cublasDtbmv_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        n: i64,
        k: i64,
        A: *const f64,
        lda: i64,
        x: *mut f64,
        incx: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the triangular banded matrix-vector multiplication:
    /// $$
    /// \textbf{x} = \text{op}(A)\textbf{x}
    /// $$
    ///
    /// where $A$ is a triangular banded matrix, and $\mathbf{x}$ is a vector. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the triangular banded matrix $A$ is stored column by column, with the main diagonal of the matrix stored in row `1`, the first subdiagonal in row `2` (starting at first position), the second subdiagonal in row `3` (starting at first position), etc. So that in general, the element $A(i,j)$ is stored in the memory location `A(1+i-j,j)` for $j = 1,\ldots,n$ and $i \in \lbrack j,\min(m,j + k)\rbrack$. Also, the elements in the array `A` that do not conceptually correspond to the elements in the banded matrix (the bottom right $k \times k$ triangle) are not referenced.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the triangular banded matrix $A$ is stored column by column, with the main diagonal of the matrix stored in row `k + 1`, the first superdiagonal in row `k` (starting at second position), the second superdiagonal in row `k-1` (starting at third position), etc. So that in general, the element $A(i,j)$ is stored in the memory location `A(1+k+i-j,j)` for $j = 1,\ldots,n$ and $i \in \lbrack\max(1,j - k,j)\rbrack$. Also, the elements in the array `A` that do not conceptually correspond to the elements in the banded matrix (the top left $k \times k$ triangle) are not referenced.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [stbmv()](http://www.netlib.org/blas/stbmv.f), [dtbmv()](http://www.netlib.org/blas/dtbmv.f), [ctbmv()](http://www.netlib.org/blas/ctbmv.f), [ztbmv()](http://www.netlib.org/blas/ztbmv.f).
    pub fn cublasCtbmv_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        A: *const cuComplex,
        lda: ::core::ffi::c_int,
        x: *mut cuComplex,
        incx: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the triangular banded matrix-vector multiplication:
    /// $$
    /// \textbf{x} = \text{op}(A)\textbf{x}
    /// $$
    ///
    /// where $A$ is a triangular banded matrix, and $\mathbf{x}$ is a vector. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the triangular banded matrix $A$ is stored column by column, with the main diagonal of the matrix stored in row `1`, the first subdiagonal in row `2` (starting at first position), the second subdiagonal in row `3` (starting at first position), etc. So that in general, the element $A(i,j)$ is stored in the memory location `A(1+i-j,j)` for $j = 1,\ldots,n$ and $i \in \lbrack j,\min(m,j + k)\rbrack$. Also, the elements in the array `A` that do not conceptually correspond to the elements in the banded matrix (the bottom right $k \times k$ triangle) are not referenced.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the triangular banded matrix $A$ is stored column by column, with the main diagonal of the matrix stored in row `k + 1`, the first superdiagonal in row `k` (starting at second position), the second superdiagonal in row `k-1` (starting at third position), etc. So that in general, the element $A(i,j)$ is stored in the memory location `A(1+k+i-j,j)` for $j = 1,\ldots,n$ and $i \in \lbrack\max(1,j - k,j)\rbrack$. Also, the elements in the array `A` that do not conceptually correspond to the elements in the banded matrix (the top left $k \times k$ triangle) are not referenced.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [stbmv()](http://www.netlib.org/blas/stbmv.f), [dtbmv()](http://www.netlib.org/blas/dtbmv.f), [ctbmv()](http://www.netlib.org/blas/ctbmv.f), [ztbmv()](http://www.netlib.org/blas/ztbmv.f).
    pub fn cublasCtbmv_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        n: i64,
        k: i64,
        A: *const cuComplex,
        lda: i64,
        x: *mut cuComplex,
        incx: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the triangular banded matrix-vector multiplication:
    /// $$
    /// \textbf{x} = \text{op}(A)\textbf{x}
    /// $$
    ///
    /// where $A$ is a triangular banded matrix, and $\mathbf{x}$ is a vector. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the triangular banded matrix $A$ is stored column by column, with the main diagonal of the matrix stored in row `1`, the first subdiagonal in row `2` (starting at first position), the second subdiagonal in row `3` (starting at first position), etc. So that in general, the element $A(i,j)$ is stored in the memory location `A(1+i-j,j)` for $j = 1,\ldots,n$ and $i \in \lbrack j,\min(m,j + k)\rbrack$. Also, the elements in the array `A` that do not conceptually correspond to the elements in the banded matrix (the bottom right $k \times k$ triangle) are not referenced.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the triangular banded matrix $A$ is stored column by column, with the main diagonal of the matrix stored in row `k + 1`, the first superdiagonal in row `k` (starting at second position), the second superdiagonal in row `k-1` (starting at third position), etc. So that in general, the element $A(i,j)$ is stored in the memory location `A(1+k+i-j,j)` for $j = 1,\ldots,n$ and $i \in \lbrack\max(1,j - k,j)\rbrack$. Also, the elements in the array `A` that do not conceptually correspond to the elements in the banded matrix (the top left $k \times k$ triangle) are not referenced.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [stbmv()](http://www.netlib.org/blas/stbmv.f), [dtbmv()](http://www.netlib.org/blas/dtbmv.f), [ctbmv()](http://www.netlib.org/blas/ctbmv.f), [ztbmv()](http://www.netlib.org/blas/ztbmv.f).
    pub fn cublasZtbmv_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        A: *const cuDoubleComplex,
        lda: ::core::ffi::c_int,
        x: *mut cuDoubleComplex,
        incx: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the triangular banded matrix-vector multiplication:
    /// $$
    /// \textbf{x} = \text{op}(A)\textbf{x}
    /// $$
    ///
    /// where $A$ is a triangular banded matrix, and $\mathbf{x}$ is a vector. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the triangular banded matrix $A$ is stored column by column, with the main diagonal of the matrix stored in row `1`, the first subdiagonal in row `2` (starting at first position), the second subdiagonal in row `3` (starting at first position), etc. So that in general, the element $A(i,j)$ is stored in the memory location `A(1+i-j,j)` for $j = 1,\ldots,n$ and $i \in \lbrack j,\min(m,j + k)\rbrack$. Also, the elements in the array `A` that do not conceptually correspond to the elements in the banded matrix (the bottom right $k \times k$ triangle) are not referenced.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the triangular banded matrix $A$ is stored column by column, with the main diagonal of the matrix stored in row `k + 1`, the first superdiagonal in row `k` (starting at second position), the second superdiagonal in row `k-1` (starting at third position), etc. So that in general, the element $A(i,j)$ is stored in the memory location `A(1+k+i-j,j)` for $j = 1,\ldots,n$ and $i \in \lbrack\max(1,j - k,j)\rbrack$. Also, the elements in the array `A` that do not conceptually correspond to the elements in the banded matrix (the top left $k \times k$ triangle) are not referenced.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [stbmv()](http://www.netlib.org/blas/stbmv.f), [dtbmv()](http://www.netlib.org/blas/dtbmv.f), [ctbmv()](http://www.netlib.org/blas/ctbmv.f), [ztbmv()](http://www.netlib.org/blas/ztbmv.f).
    pub fn cublasZtbmv_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        n: i64,
        k: i64,
        A: *const cuDoubleComplex,
        lda: i64,
        x: *mut cuDoubleComplex,
        incx: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the triangular packed matrix-vector multiplication:
    /// $$
    /// \textbf{x} = \text{op}(A)\textbf{x}
    /// $$
    ///
    /// where $A$ is a triangular matrix stored in packed format, and $\mathbf{x}$ is a vector. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the elements in the lower triangular part of the triangular matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+((2*n-j+1)*j)/2\]` for $j = 1,\ldots,n$ and $i \geq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the elements in the upper triangular part of the triangular matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+(j*(j+1))/2\]` for $A(i,j)$ and $i \leq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [stpmv()](http://www.netlib.org/blas/stpmv.f), [dtpmv()](http://www.netlib.org/blas/dtpmv.f), [ctpmv()](http://www.netlib.org/blas/ctpmv.f), [ztpmv()](http://www.netlib.org/blas/ztpmv.f).
    pub fn cublasStpmv_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        n: ::core::ffi::c_int,
        AP: *const f32,
        x: *mut f32,
        incx: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the triangular packed matrix-vector multiplication:
    /// $$
    /// \textbf{x} = \text{op}(A)\textbf{x}
    /// $$
    ///
    /// where $A$ is a triangular matrix stored in packed format, and $\mathbf{x}$ is a vector. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the elements in the lower triangular part of the triangular matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+((2*n-j+1)*j)/2\]` for $j = 1,\ldots,n$ and $i \geq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the elements in the upper triangular part of the triangular matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+(j*(j+1))/2\]` for $A(i,j)$ and $i \leq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [stpmv()](http://www.netlib.org/blas/stpmv.f), [dtpmv()](http://www.netlib.org/blas/dtpmv.f), [ctpmv()](http://www.netlib.org/blas/ctpmv.f), [ztpmv()](http://www.netlib.org/blas/ztpmv.f).
    pub fn cublasStpmv_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        n: i64,
        AP: *const f32,
        x: *mut f32,
        incx: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the triangular packed matrix-vector multiplication:
    /// $$
    /// \textbf{x} = \text{op}(A)\textbf{x}
    /// $$
    ///
    /// where $A$ is a triangular matrix stored in packed format, and $\mathbf{x}$ is a vector. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the elements in the lower triangular part of the triangular matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+((2*n-j+1)*j)/2\]` for $j = 1,\ldots,n$ and $i \geq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the elements in the upper triangular part of the triangular matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+(j*(j+1))/2\]` for $A(i,j)$ and $i \leq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [stpmv()](http://www.netlib.org/blas/stpmv.f), [dtpmv()](http://www.netlib.org/blas/dtpmv.f), [ctpmv()](http://www.netlib.org/blas/ctpmv.f), [ztpmv()](http://www.netlib.org/blas/ztpmv.f).
    pub fn cublasDtpmv_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        n: ::core::ffi::c_int,
        AP: *const f64,
        x: *mut f64,
        incx: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the triangular packed matrix-vector multiplication:
    /// $$
    /// \textbf{x} = \text{op}(A)\textbf{x}
    /// $$
    ///
    /// where $A$ is a triangular matrix stored in packed format, and $\mathbf{x}$ is a vector. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the elements in the lower triangular part of the triangular matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+((2*n-j+1)*j)/2\]` for $j = 1,\ldots,n$ and $i \geq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the elements in the upper triangular part of the triangular matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+(j*(j+1))/2\]` for $A(i,j)$ and $i \leq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [stpmv()](http://www.netlib.org/blas/stpmv.f), [dtpmv()](http://www.netlib.org/blas/dtpmv.f), [ctpmv()](http://www.netlib.org/blas/ctpmv.f), [ztpmv()](http://www.netlib.org/blas/ztpmv.f).
    pub fn cublasDtpmv_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        n: i64,
        AP: *const f64,
        x: *mut f64,
        incx: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the triangular packed matrix-vector multiplication:
    /// $$
    /// \textbf{x} = \text{op}(A)\textbf{x}
    /// $$
    ///
    /// where $A$ is a triangular matrix stored in packed format, and $\mathbf{x}$ is a vector. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the elements in the lower triangular part of the triangular matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+((2*n-j+1)*j)/2\]` for $j = 1,\ldots,n$ and $i \geq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the elements in the upper triangular part of the triangular matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+(j*(j+1))/2\]` for $A(i,j)$ and $i \leq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [stpmv()](http://www.netlib.org/blas/stpmv.f), [dtpmv()](http://www.netlib.org/blas/dtpmv.f), [ctpmv()](http://www.netlib.org/blas/ctpmv.f), [ztpmv()](http://www.netlib.org/blas/ztpmv.f).
    pub fn cublasCtpmv_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        n: ::core::ffi::c_int,
        AP: *const cuComplex,
        x: *mut cuComplex,
        incx: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the triangular packed matrix-vector multiplication:
    /// $$
    /// \textbf{x} = \text{op}(A)\textbf{x}
    /// $$
    ///
    /// where $A$ is a triangular matrix stored in packed format, and $\mathbf{x}$ is a vector. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the elements in the lower triangular part of the triangular matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+((2*n-j+1)*j)/2\]` for $j = 1,\ldots,n$ and $i \geq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the elements in the upper triangular part of the triangular matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+(j*(j+1))/2\]` for $A(i,j)$ and $i \leq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [stpmv()](http://www.netlib.org/blas/stpmv.f), [dtpmv()](http://www.netlib.org/blas/dtpmv.f), [ctpmv()](http://www.netlib.org/blas/ctpmv.f), [ztpmv()](http://www.netlib.org/blas/ztpmv.f).
    pub fn cublasCtpmv_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        n: i64,
        AP: *const cuComplex,
        x: *mut cuComplex,
        incx: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the triangular packed matrix-vector multiplication:
    /// $$
    /// \textbf{x} = \text{op}(A)\textbf{x}
    /// $$
    ///
    /// where $A$ is a triangular matrix stored in packed format, and $\mathbf{x}$ is a vector. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the elements in the lower triangular part of the triangular matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+((2*n-j+1)*j)/2\]` for $j = 1,\ldots,n$ and $i \geq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the elements in the upper triangular part of the triangular matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+(j*(j+1))/2\]` for $A(i,j)$ and $i \leq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [stpmv()](http://www.netlib.org/blas/stpmv.f), [dtpmv()](http://www.netlib.org/blas/dtpmv.f), [ctpmv()](http://www.netlib.org/blas/ctpmv.f), [ztpmv()](http://www.netlib.org/blas/ztpmv.f).
    pub fn cublasZtpmv_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        n: ::core::ffi::c_int,
        AP: *const cuDoubleComplex,
        x: *mut cuDoubleComplex,
        incx: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the triangular packed matrix-vector multiplication:
    /// $$
    /// \textbf{x} = \text{op}(A)\textbf{x}
    /// $$
    ///
    /// where $A$ is a triangular matrix stored in packed format, and $\mathbf{x}$ is a vector. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the elements in the lower triangular part of the triangular matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+((2*n-j+1)*j)/2\]` for $j = 1,\ldots,n$ and $i \geq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the elements in the upper triangular part of the triangular matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+(j*(j+1))/2\]` for $A(i,j)$ and $i \leq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [stpmv()](http://www.netlib.org/blas/stpmv.f), [dtpmv()](http://www.netlib.org/blas/dtpmv.f), [ctpmv()](http://www.netlib.org/blas/ctpmv.f), [ztpmv()](http://www.netlib.org/blas/ztpmv.f).
    pub fn cublasZtpmv_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        n: i64,
        AP: *const cuDoubleComplex,
        x: *mut cuDoubleComplex,
        incx: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function solves the triangular linear system with a single right-hand-side:
    /// $$
    /// \text{op}(A)\textbf{x} = \textbf{b}
    /// $$
    ///
    /// where $A$ is a triangular matrix stored in lower or upper mode with or without the main diagonal, and $\mathbf{x}$ and $\mathbf{b}$ are vectors. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// The solution $\mathbf{x}$ overwrites the right-hand-sides $\mathbf{b}$ on exit.
    ///
    /// No test for singularity or near-singularity is included in this function.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [strsv()](http://www.netlib.org/blas/strsv.f), [dtrsv()](http://www.netlib.org/blas/dtrsv.f), [ctrsv()](http://www.netlib.org/blas/ctrsv.f), [ztrsv()](http://www.netlib.org/blas/ztrsv.f).
    pub fn cublasStrsv_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        n: ::core::ffi::c_int,
        A: *const f32,
        lda: ::core::ffi::c_int,
        x: *mut f32,
        incx: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function solves the triangular linear system with a single right-hand-side:
    /// $$
    /// \text{op}(A)\textbf{x} = \textbf{b}
    /// $$
    ///
    /// where $A$ is a triangular matrix stored in lower or upper mode with or without the main diagonal, and $\mathbf{x}$ and $\mathbf{b}$ are vectors. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// The solution $\mathbf{x}$ overwrites the right-hand-sides $\mathbf{b}$ on exit.
    ///
    /// No test for singularity or near-singularity is included in this function.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [strsv()](http://www.netlib.org/blas/strsv.f), [dtrsv()](http://www.netlib.org/blas/dtrsv.f), [ctrsv()](http://www.netlib.org/blas/ctrsv.f), [ztrsv()](http://www.netlib.org/blas/ztrsv.f).
    pub fn cublasStrsv_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        n: i64,
        A: *const f32,
        lda: i64,
        x: *mut f32,
        incx: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function solves the triangular linear system with a single right-hand-side:
    /// $$
    /// \text{op}(A)\textbf{x} = \textbf{b}
    /// $$
    ///
    /// where $A$ is a triangular matrix stored in lower or upper mode with or without the main diagonal, and $\mathbf{x}$ and $\mathbf{b}$ are vectors. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// The solution $\mathbf{x}$ overwrites the right-hand-sides $\mathbf{b}$ on exit.
    ///
    /// No test for singularity or near-singularity is included in this function.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [strsv()](http://www.netlib.org/blas/strsv.f), [dtrsv()](http://www.netlib.org/blas/dtrsv.f), [ctrsv()](http://www.netlib.org/blas/ctrsv.f), [ztrsv()](http://www.netlib.org/blas/ztrsv.f).
    pub fn cublasDtrsv_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        n: ::core::ffi::c_int,
        A: *const f64,
        lda: ::core::ffi::c_int,
        x: *mut f64,
        incx: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function solves the triangular linear system with a single right-hand-side:
    /// $$
    /// \text{op}(A)\textbf{x} = \textbf{b}
    /// $$
    ///
    /// where $A$ is a triangular matrix stored in lower or upper mode with or without the main diagonal, and $\mathbf{x}$ and $\mathbf{b}$ are vectors. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// The solution $\mathbf{x}$ overwrites the right-hand-sides $\mathbf{b}$ on exit.
    ///
    /// No test for singularity or near-singularity is included in this function.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [strsv()](http://www.netlib.org/blas/strsv.f), [dtrsv()](http://www.netlib.org/blas/dtrsv.f), [ctrsv()](http://www.netlib.org/blas/ctrsv.f), [ztrsv()](http://www.netlib.org/blas/ztrsv.f).
    pub fn cublasDtrsv_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        n: i64,
        A: *const f64,
        lda: i64,
        x: *mut f64,
        incx: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function solves the triangular linear system with a single right-hand-side:
    /// $$
    /// \text{op}(A)\textbf{x} = \textbf{b}
    /// $$
    ///
    /// where $A$ is a triangular matrix stored in lower or upper mode with or without the main diagonal, and $\mathbf{x}$ and $\mathbf{b}$ are vectors. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// The solution $\mathbf{x}$ overwrites the right-hand-sides $\mathbf{b}$ on exit.
    ///
    /// No test for singularity or near-singularity is included in this function.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [strsv()](http://www.netlib.org/blas/strsv.f), [dtrsv()](http://www.netlib.org/blas/dtrsv.f), [ctrsv()](http://www.netlib.org/blas/ctrsv.f), [ztrsv()](http://www.netlib.org/blas/ztrsv.f).
    pub fn cublasCtrsv_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        n: ::core::ffi::c_int,
        A: *const cuComplex,
        lda: ::core::ffi::c_int,
        x: *mut cuComplex,
        incx: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function solves the triangular linear system with a single right-hand-side:
    /// $$
    /// \text{op}(A)\textbf{x} = \textbf{b}
    /// $$
    ///
    /// where $A$ is a triangular matrix stored in lower or upper mode with or without the main diagonal, and $\mathbf{x}$ and $\mathbf{b}$ are vectors. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// The solution $\mathbf{x}$ overwrites the right-hand-sides $\mathbf{b}$ on exit.
    ///
    /// No test for singularity or near-singularity is included in this function.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [strsv()](http://www.netlib.org/blas/strsv.f), [dtrsv()](http://www.netlib.org/blas/dtrsv.f), [ctrsv()](http://www.netlib.org/blas/ctrsv.f), [ztrsv()](http://www.netlib.org/blas/ztrsv.f).
    pub fn cublasCtrsv_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        n: i64,
        A: *const cuComplex,
        lda: i64,
        x: *mut cuComplex,
        incx: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function solves the triangular linear system with a single right-hand-side:
    /// $$
    /// \text{op}(A)\textbf{x} = \textbf{b}
    /// $$
    ///
    /// where $A$ is a triangular matrix stored in lower or upper mode with or without the main diagonal, and $\mathbf{x}$ and $\mathbf{b}$ are vectors. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// The solution $\mathbf{x}$ overwrites the right-hand-sides $\mathbf{b}$ on exit.
    ///
    /// No test for singularity or near-singularity is included in this function.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [strsv()](http://www.netlib.org/blas/strsv.f), [dtrsv()](http://www.netlib.org/blas/dtrsv.f), [ctrsv()](http://www.netlib.org/blas/ctrsv.f), [ztrsv()](http://www.netlib.org/blas/ztrsv.f).
    pub fn cublasZtrsv_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        n: ::core::ffi::c_int,
        A: *const cuDoubleComplex,
        lda: ::core::ffi::c_int,
        x: *mut cuDoubleComplex,
        incx: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function solves the triangular linear system with a single right-hand-side:
    /// $$
    /// \text{op}(A)\textbf{x} = \textbf{b}
    /// $$
    ///
    /// where $A$ is a triangular matrix stored in lower or upper mode with or without the main diagonal, and $\mathbf{x}$ and $\mathbf{b}$ are vectors. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// The solution $\mathbf{x}$ overwrites the right-hand-sides $\mathbf{b}$ on exit.
    ///
    /// No test for singularity or near-singularity is included in this function.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [strsv()](http://www.netlib.org/blas/strsv.f), [dtrsv()](http://www.netlib.org/blas/dtrsv.f), [ctrsv()](http://www.netlib.org/blas/ctrsv.f), [ztrsv()](http://www.netlib.org/blas/ztrsv.f).
    pub fn cublasZtrsv_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        n: i64,
        A: *const cuDoubleComplex,
        lda: i64,
        x: *mut cuDoubleComplex,
        incx: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function solves the packed triangular linear system with a single right-hand-side:
    /// $$
    /// \text{op}(A)\textbf{x} = \textbf{b}
    /// $$
    ///
    /// where $A$ is a triangular matrix stored in packed format, and $\mathbf{x}$ and $\mathbf{b}$ are vectors. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// The solution $\mathbf{x}$ overwrites the right-hand-sides $\mathbf{b}$ on exit.
    ///
    /// No test for singularity or near-singularity is included in this function.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the elements in the lower triangular part of the triangular matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+((2*n-j+1)*j)/2\]` for $j = 1,\ldots,n$ and $i \geq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the elements in the upper triangular part of the triangular matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+(j*(j+1))/2\]` for $j = 1,\ldots,n$ and $i \leq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [stpsv()](http://www.netlib.org/blas/stpsv.f), [dtpsv()](http://www.netlib.org/blas/dtpsv.f), [ctpsv()](http://www.netlib.org/blas/ctpsv.f), [ztpsv()](http://www.netlib.org/blas/ztpsv.f).
    pub fn cublasStpsv_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        n: ::core::ffi::c_int,
        AP: *const f32,
        x: *mut f32,
        incx: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function solves the packed triangular linear system with a single right-hand-side:
    /// $$
    /// \text{op}(A)\textbf{x} = \textbf{b}
    /// $$
    ///
    /// where $A$ is a triangular matrix stored in packed format, and $\mathbf{x}$ and $\mathbf{b}$ are vectors. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// The solution $\mathbf{x}$ overwrites the right-hand-sides $\mathbf{b}$ on exit.
    ///
    /// No test for singularity or near-singularity is included in this function.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the elements in the lower triangular part of the triangular matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+((2*n-j+1)*j)/2\]` for $j = 1,\ldots,n$ and $i \geq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the elements in the upper triangular part of the triangular matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+(j*(j+1))/2\]` for $j = 1,\ldots,n$ and $i \leq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [stpsv()](http://www.netlib.org/blas/stpsv.f), [dtpsv()](http://www.netlib.org/blas/dtpsv.f), [ctpsv()](http://www.netlib.org/blas/ctpsv.f), [ztpsv()](http://www.netlib.org/blas/ztpsv.f).
    pub fn cublasStpsv_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        n: i64,
        AP: *const f32,
        x: *mut f32,
        incx: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function solves the packed triangular linear system with a single right-hand-side:
    /// $$
    /// \text{op}(A)\textbf{x} = \textbf{b}
    /// $$
    ///
    /// where $A$ is a triangular matrix stored in packed format, and $\mathbf{x}$ and $\mathbf{b}$ are vectors. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// The solution $\mathbf{x}$ overwrites the right-hand-sides $\mathbf{b}$ on exit.
    ///
    /// No test for singularity or near-singularity is included in this function.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the elements in the lower triangular part of the triangular matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+((2*n-j+1)*j)/2\]` for $j = 1,\ldots,n$ and $i \geq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the elements in the upper triangular part of the triangular matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+(j*(j+1))/2\]` for $j = 1,\ldots,n$ and $i \leq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [stpsv()](http://www.netlib.org/blas/stpsv.f), [dtpsv()](http://www.netlib.org/blas/dtpsv.f), [ctpsv()](http://www.netlib.org/blas/ctpsv.f), [ztpsv()](http://www.netlib.org/blas/ztpsv.f).
    pub fn cublasDtpsv_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        n: ::core::ffi::c_int,
        AP: *const f64,
        x: *mut f64,
        incx: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function solves the packed triangular linear system with a single right-hand-side:
    /// $$
    /// \text{op}(A)\textbf{x} = \textbf{b}
    /// $$
    ///
    /// where $A$ is a triangular matrix stored in packed format, and $\mathbf{x}$ and $\mathbf{b}$ are vectors. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// The solution $\mathbf{x}$ overwrites the right-hand-sides $\mathbf{b}$ on exit.
    ///
    /// No test for singularity or near-singularity is included in this function.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the elements in the lower triangular part of the triangular matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+((2*n-j+1)*j)/2\]` for $j = 1,\ldots,n$ and $i \geq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the elements in the upper triangular part of the triangular matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+(j*(j+1))/2\]` for $j = 1,\ldots,n$ and $i \leq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [stpsv()](http://www.netlib.org/blas/stpsv.f), [dtpsv()](http://www.netlib.org/blas/dtpsv.f), [ctpsv()](http://www.netlib.org/blas/ctpsv.f), [ztpsv()](http://www.netlib.org/blas/ztpsv.f).
    pub fn cublasDtpsv_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        n: i64,
        AP: *const f64,
        x: *mut f64,
        incx: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function solves the packed triangular linear system with a single right-hand-side:
    /// $$
    /// \text{op}(A)\textbf{x} = \textbf{b}
    /// $$
    ///
    /// where $A$ is a triangular matrix stored in packed format, and $\mathbf{x}$ and $\mathbf{b}$ are vectors. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// The solution $\mathbf{x}$ overwrites the right-hand-sides $\mathbf{b}$ on exit.
    ///
    /// No test for singularity or near-singularity is included in this function.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the elements in the lower triangular part of the triangular matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+((2*n-j+1)*j)/2\]` for $j = 1,\ldots,n$ and $i \geq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the elements in the upper triangular part of the triangular matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+(j*(j+1))/2\]` for $j = 1,\ldots,n$ and $i \leq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [stpsv()](http://www.netlib.org/blas/stpsv.f), [dtpsv()](http://www.netlib.org/blas/dtpsv.f), [ctpsv()](http://www.netlib.org/blas/ctpsv.f), [ztpsv()](http://www.netlib.org/blas/ztpsv.f).
    pub fn cublasCtpsv_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        n: ::core::ffi::c_int,
        AP: *const cuComplex,
        x: *mut cuComplex,
        incx: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function solves the packed triangular linear system with a single right-hand-side:
    /// $$
    /// \text{op}(A)\textbf{x} = \textbf{b}
    /// $$
    ///
    /// where $A$ is a triangular matrix stored in packed format, and $\mathbf{x}$ and $\mathbf{b}$ are vectors. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// The solution $\mathbf{x}$ overwrites the right-hand-sides $\mathbf{b}$ on exit.
    ///
    /// No test for singularity or near-singularity is included in this function.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the elements in the lower triangular part of the triangular matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+((2*n-j+1)*j)/2\]` for $j = 1,\ldots,n$ and $i \geq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the elements in the upper triangular part of the triangular matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+(j*(j+1))/2\]` for $j = 1,\ldots,n$ and $i \leq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [stpsv()](http://www.netlib.org/blas/stpsv.f), [dtpsv()](http://www.netlib.org/blas/dtpsv.f), [ctpsv()](http://www.netlib.org/blas/ctpsv.f), [ztpsv()](http://www.netlib.org/blas/ztpsv.f).
    pub fn cublasCtpsv_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        n: i64,
        AP: *const cuComplex,
        x: *mut cuComplex,
        incx: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function solves the packed triangular linear system with a single right-hand-side:
    /// $$
    /// \text{op}(A)\textbf{x} = \textbf{b}
    /// $$
    ///
    /// where $A$ is a triangular matrix stored in packed format, and $\mathbf{x}$ and $\mathbf{b}$ are vectors. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// The solution $\mathbf{x}$ overwrites the right-hand-sides $\mathbf{b}$ on exit.
    ///
    /// No test for singularity or near-singularity is included in this function.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the elements in the lower triangular part of the triangular matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+((2*n-j+1)*j)/2\]` for $j = 1,\ldots,n$ and $i \geq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the elements in the upper triangular part of the triangular matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+(j*(j+1))/2\]` for $j = 1,\ldots,n$ and $i \leq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [stpsv()](http://www.netlib.org/blas/stpsv.f), [dtpsv()](http://www.netlib.org/blas/dtpsv.f), [ctpsv()](http://www.netlib.org/blas/ctpsv.f), [ztpsv()](http://www.netlib.org/blas/ztpsv.f).
    pub fn cublasZtpsv_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        n: ::core::ffi::c_int,
        AP: *const cuDoubleComplex,
        x: *mut cuDoubleComplex,
        incx: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function solves the packed triangular linear system with a single right-hand-side:
    /// $$
    /// \text{op}(A)\textbf{x} = \textbf{b}
    /// $$
    ///
    /// where $A$ is a triangular matrix stored in packed format, and $\mathbf{x}$ and $\mathbf{b}$ are vectors. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// The solution $\mathbf{x}$ overwrites the right-hand-sides $\mathbf{b}$ on exit.
    ///
    /// No test for singularity or near-singularity is included in this function.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the elements in the lower triangular part of the triangular matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+((2*n-j+1)*j)/2\]` for $j = 1,\ldots,n$ and $i \geq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the elements in the upper triangular part of the triangular matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+(j*(j+1))/2\]` for $j = 1,\ldots,n$ and $i \leq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [stpsv()](http://www.netlib.org/blas/stpsv.f), [dtpsv()](http://www.netlib.org/blas/dtpsv.f), [ctpsv()](http://www.netlib.org/blas/ctpsv.f), [ztpsv()](http://www.netlib.org/blas/ztpsv.f).
    pub fn cublasZtpsv_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        n: i64,
        AP: *const cuDoubleComplex,
        x: *mut cuDoubleComplex,
        incx: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function solves the triangular banded linear system with a single right-hand-side:
    /// $$
    /// \text{op}(A)\textbf{x} = \textbf{b}
    /// $$
    ///
    /// where $A$ is a triangular banded matrix, and $\mathbf{x}$ and $\mathbf{b}$ are vectors. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// The solution $\mathbf{x}$ overwrites the right-hand-sides $\mathbf{b}$ on exit.
    ///
    /// No test for singularity or near-singularity is included in this function.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the triangular banded matrix $A$ is stored column by column, with the main diagonal of the matrix stored in row `1`, the first subdiagonal in row `2` (starting at first position), the second subdiagonal in row `3` (starting at first position), etc. So that in general, the element $A(i,j)$ is stored in the memory location `A(1+i-j,j)` for $j = 1,\ldots,n$ and $i \in \lbrack j,\min(m,j + k)\rbrack$. Also, the elements in the array `A` that do not conceptually correspond to the elements in the banded matrix (the bottom right $k \times k$ triangle) are not referenced.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the triangular banded matrix $A$ is stored column by column, with the main diagonal of the matrix stored in row `k + 1`, the first superdiagonal in row `k` (starting at second position), the second superdiagonal in row `k-1` (starting at third position), etc. So that in general, the element $A(i,j)$ is stored in the memory location `A(1+k+i-j,j)` for $j = 1,\ldots,n$ and $i \in \lbrack\max(1,j - k,j)\rbrack$. Also, the elements in the array `A` that do not conceptually correspond to the elements in the banded matrix (the top left $k \times k$ triangle) are not referenced.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [stbsv()](http://www.netlib.org/blas/stbsv.f), [dtbsv()](http://www.netlib.org/blas/dtbsv.f), [ctbsv()](http://www.netlib.org/blas/ctbsv.f), [ztbsv()](http://www.netlib.org/blas/ztbsv.f).
    pub fn cublasStbsv_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        A: *const f32,
        lda: ::core::ffi::c_int,
        x: *mut f32,
        incx: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function solves the triangular banded linear system with a single right-hand-side:
    /// $$
    /// \text{op}(A)\textbf{x} = \textbf{b}
    /// $$
    ///
    /// where $A$ is a triangular banded matrix, and $\mathbf{x}$ and $\mathbf{b}$ are vectors. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// The solution $\mathbf{x}$ overwrites the right-hand-sides $\mathbf{b}$ on exit.
    ///
    /// No test for singularity or near-singularity is included in this function.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the triangular banded matrix $A$ is stored column by column, with the main diagonal of the matrix stored in row `1`, the first subdiagonal in row `2` (starting at first position), the second subdiagonal in row `3` (starting at first position), etc. So that in general, the element $A(i,j)$ is stored in the memory location `A(1+i-j,j)` for $j = 1,\ldots,n$ and $i \in \lbrack j,\min(m,j + k)\rbrack$. Also, the elements in the array `A` that do not conceptually correspond to the elements in the banded matrix (the bottom right $k \times k$ triangle) are not referenced.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the triangular banded matrix $A$ is stored column by column, with the main diagonal of the matrix stored in row `k + 1`, the first superdiagonal in row `k` (starting at second position), the second superdiagonal in row `k-1` (starting at third position), etc. So that in general, the element $A(i,j)$ is stored in the memory location `A(1+k+i-j,j)` for $j = 1,\ldots,n$ and $i \in \lbrack\max(1,j - k,j)\rbrack$. Also, the elements in the array `A` that do not conceptually correspond to the elements in the banded matrix (the top left $k \times k$ triangle) are not referenced.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [stbsv()](http://www.netlib.org/blas/stbsv.f), [dtbsv()](http://www.netlib.org/blas/dtbsv.f), [ctbsv()](http://www.netlib.org/blas/ctbsv.f), [ztbsv()](http://www.netlib.org/blas/ztbsv.f).
    pub fn cublasStbsv_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        n: i64,
        k: i64,
        A: *const f32,
        lda: i64,
        x: *mut f32,
        incx: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function solves the triangular banded linear system with a single right-hand-side:
    /// $$
    /// \text{op}(A)\textbf{x} = \textbf{b}
    /// $$
    ///
    /// where $A$ is a triangular banded matrix, and $\mathbf{x}$ and $\mathbf{b}$ are vectors. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// The solution $\mathbf{x}$ overwrites the right-hand-sides $\mathbf{b}$ on exit.
    ///
    /// No test for singularity or near-singularity is included in this function.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the triangular banded matrix $A$ is stored column by column, with the main diagonal of the matrix stored in row `1`, the first subdiagonal in row `2` (starting at first position), the second subdiagonal in row `3` (starting at first position), etc. So that in general, the element $A(i,j)$ is stored in the memory location `A(1+i-j,j)` for $j = 1,\ldots,n$ and $i \in \lbrack j,\min(m,j + k)\rbrack$. Also, the elements in the array `A` that do not conceptually correspond to the elements in the banded matrix (the bottom right $k \times k$ triangle) are not referenced.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the triangular banded matrix $A$ is stored column by column, with the main diagonal of the matrix stored in row `k + 1`, the first superdiagonal in row `k` (starting at second position), the second superdiagonal in row `k-1` (starting at third position), etc. So that in general, the element $A(i,j)$ is stored in the memory location `A(1+k+i-j,j)` for $j = 1,\ldots,n$ and $i \in \lbrack\max(1,j - k,j)\rbrack$. Also, the elements in the array `A` that do not conceptually correspond to the elements in the banded matrix (the top left $k \times k$ triangle) are not referenced.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [stbsv()](http://www.netlib.org/blas/stbsv.f), [dtbsv()](http://www.netlib.org/blas/dtbsv.f), [ctbsv()](http://www.netlib.org/blas/ctbsv.f), [ztbsv()](http://www.netlib.org/blas/ztbsv.f).
    pub fn cublasDtbsv_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        A: *const f64,
        lda: ::core::ffi::c_int,
        x: *mut f64,
        incx: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function solves the triangular banded linear system with a single right-hand-side:
    /// $$
    /// \text{op}(A)\textbf{x} = \textbf{b}
    /// $$
    ///
    /// where $A$ is a triangular banded matrix, and $\mathbf{x}$ and $\mathbf{b}$ are vectors. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// The solution $\mathbf{x}$ overwrites the right-hand-sides $\mathbf{b}$ on exit.
    ///
    /// No test for singularity or near-singularity is included in this function.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the triangular banded matrix $A$ is stored column by column, with the main diagonal of the matrix stored in row `1`, the first subdiagonal in row `2` (starting at first position), the second subdiagonal in row `3` (starting at first position), etc. So that in general, the element $A(i,j)$ is stored in the memory location `A(1+i-j,j)` for $j = 1,\ldots,n$ and $i \in \lbrack j,\min(m,j + k)\rbrack$. Also, the elements in the array `A` that do not conceptually correspond to the elements in the banded matrix (the bottom right $k \times k$ triangle) are not referenced.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the triangular banded matrix $A$ is stored column by column, with the main diagonal of the matrix stored in row `k + 1`, the first superdiagonal in row `k` (starting at second position), the second superdiagonal in row `k-1` (starting at third position), etc. So that in general, the element $A(i,j)$ is stored in the memory location `A(1+k+i-j,j)` for $j = 1,\ldots,n$ and $i \in \lbrack\max(1,j - k,j)\rbrack$. Also, the elements in the array `A` that do not conceptually correspond to the elements in the banded matrix (the top left $k \times k$ triangle) are not referenced.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [stbsv()](http://www.netlib.org/blas/stbsv.f), [dtbsv()](http://www.netlib.org/blas/dtbsv.f), [ctbsv()](http://www.netlib.org/blas/ctbsv.f), [ztbsv()](http://www.netlib.org/blas/ztbsv.f).
    pub fn cublasDtbsv_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        n: i64,
        k: i64,
        A: *const f64,
        lda: i64,
        x: *mut f64,
        incx: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function solves the triangular banded linear system with a single right-hand-side:
    /// $$
    /// \text{op}(A)\textbf{x} = \textbf{b}
    /// $$
    ///
    /// where $A$ is a triangular banded matrix, and $\mathbf{x}$ and $\mathbf{b}$ are vectors. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// The solution $\mathbf{x}$ overwrites the right-hand-sides $\mathbf{b}$ on exit.
    ///
    /// No test for singularity or near-singularity is included in this function.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the triangular banded matrix $A$ is stored column by column, with the main diagonal of the matrix stored in row `1`, the first subdiagonal in row `2` (starting at first position), the second subdiagonal in row `3` (starting at first position), etc. So that in general, the element $A(i,j)$ is stored in the memory location `A(1+i-j,j)` for $j = 1,\ldots,n$ and $i \in \lbrack j,\min(m,j + k)\rbrack$. Also, the elements in the array `A` that do not conceptually correspond to the elements in the banded matrix (the bottom right $k \times k$ triangle) are not referenced.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the triangular banded matrix $A$ is stored column by column, with the main diagonal of the matrix stored in row `k + 1`, the first superdiagonal in row `k` (starting at second position), the second superdiagonal in row `k-1` (starting at third position), etc. So that in general, the element $A(i,j)$ is stored in the memory location `A(1+k+i-j,j)` for $j = 1,\ldots,n$ and $i \in \lbrack\max(1,j - k,j)\rbrack$. Also, the elements in the array `A` that do not conceptually correspond to the elements in the banded matrix (the top left $k \times k$ triangle) are not referenced.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [stbsv()](http://www.netlib.org/blas/stbsv.f), [dtbsv()](http://www.netlib.org/blas/dtbsv.f), [ctbsv()](http://www.netlib.org/blas/ctbsv.f), [ztbsv()](http://www.netlib.org/blas/ztbsv.f).
    pub fn cublasCtbsv_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        A: *const cuComplex,
        lda: ::core::ffi::c_int,
        x: *mut cuComplex,
        incx: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function solves the triangular banded linear system with a single right-hand-side:
    /// $$
    /// \text{op}(A)\textbf{x} = \textbf{b}
    /// $$
    ///
    /// where $A$ is a triangular banded matrix, and $\mathbf{x}$ and $\mathbf{b}$ are vectors. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// The solution $\mathbf{x}$ overwrites the right-hand-sides $\mathbf{b}$ on exit.
    ///
    /// No test for singularity or near-singularity is included in this function.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the triangular banded matrix $A$ is stored column by column, with the main diagonal of the matrix stored in row `1`, the first subdiagonal in row `2` (starting at first position), the second subdiagonal in row `3` (starting at first position), etc. So that in general, the element $A(i,j)$ is stored in the memory location `A(1+i-j,j)` for $j = 1,\ldots,n$ and $i \in \lbrack j,\min(m,j + k)\rbrack$. Also, the elements in the array `A` that do not conceptually correspond to the elements in the banded matrix (the bottom right $k \times k$ triangle) are not referenced.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the triangular banded matrix $A$ is stored column by column, with the main diagonal of the matrix stored in row `k + 1`, the first superdiagonal in row `k` (starting at second position), the second superdiagonal in row `k-1` (starting at third position), etc. So that in general, the element $A(i,j)$ is stored in the memory location `A(1+k+i-j,j)` for $j = 1,\ldots,n$ and $i \in \lbrack\max(1,j - k,j)\rbrack$. Also, the elements in the array `A` that do not conceptually correspond to the elements in the banded matrix (the top left $k \times k$ triangle) are not referenced.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [stbsv()](http://www.netlib.org/blas/stbsv.f), [dtbsv()](http://www.netlib.org/blas/dtbsv.f), [ctbsv()](http://www.netlib.org/blas/ctbsv.f), [ztbsv()](http://www.netlib.org/blas/ztbsv.f).
    pub fn cublasCtbsv_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        n: i64,
        k: i64,
        A: *const cuComplex,
        lda: i64,
        x: *mut cuComplex,
        incx: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function solves the triangular banded linear system with a single right-hand-side:
    /// $$
    /// \text{op}(A)\textbf{x} = \textbf{b}
    /// $$
    ///
    /// where $A$ is a triangular banded matrix, and $\mathbf{x}$ and $\mathbf{b}$ are vectors. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// The solution $\mathbf{x}$ overwrites the right-hand-sides $\mathbf{b}$ on exit.
    ///
    /// No test for singularity or near-singularity is included in this function.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the triangular banded matrix $A$ is stored column by column, with the main diagonal of the matrix stored in row `1`, the first subdiagonal in row `2` (starting at first position), the second subdiagonal in row `3` (starting at first position), etc. So that in general, the element $A(i,j)$ is stored in the memory location `A(1+i-j,j)` for $j = 1,\ldots,n$ and $i \in \lbrack j,\min(m,j + k)\rbrack$. Also, the elements in the array `A` that do not conceptually correspond to the elements in the banded matrix (the bottom right $k \times k$ triangle) are not referenced.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the triangular banded matrix $A$ is stored column by column, with the main diagonal of the matrix stored in row `k + 1`, the first superdiagonal in row `k` (starting at second position), the second superdiagonal in row `k-1` (starting at third position), etc. So that in general, the element $A(i,j)$ is stored in the memory location `A(1+k+i-j,j)` for $j = 1,\ldots,n$ and $i \in \lbrack\max(1,j - k,j)\rbrack$. Also, the elements in the array `A` that do not conceptually correspond to the elements in the banded matrix (the top left $k \times k$ triangle) are not referenced.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [stbsv()](http://www.netlib.org/blas/stbsv.f), [dtbsv()](http://www.netlib.org/blas/dtbsv.f), [ctbsv()](http://www.netlib.org/blas/ctbsv.f), [ztbsv()](http://www.netlib.org/blas/ztbsv.f).
    pub fn cublasZtbsv_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        A: *const cuDoubleComplex,
        lda: ::core::ffi::c_int,
        x: *mut cuDoubleComplex,
        incx: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function solves the triangular banded linear system with a single right-hand-side:
    /// $$
    /// \text{op}(A)\textbf{x} = \textbf{b}
    /// $$
    ///
    /// where $A$ is a triangular banded matrix, and $\mathbf{x}$ and $\mathbf{b}$ are vectors. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// The solution $\mathbf{x}$ overwrites the right-hand-sides $\mathbf{b}$ on exit.
    ///
    /// No test for singularity or near-singularity is included in this function.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the triangular banded matrix $A$ is stored column by column, with the main diagonal of the matrix stored in row `1`, the first subdiagonal in row `2` (starting at first position), the second subdiagonal in row `3` (starting at first position), etc. So that in general, the element $A(i,j)$ is stored in the memory location `A(1+i-j,j)` for $j = 1,\ldots,n$ and $i \in \lbrack j,\min(m,j + k)\rbrack$. Also, the elements in the array `A` that do not conceptually correspond to the elements in the banded matrix (the bottom right $k \times k$ triangle) are not referenced.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the triangular banded matrix $A$ is stored column by column, with the main diagonal of the matrix stored in row `k + 1`, the first superdiagonal in row `k` (starting at second position), the second superdiagonal in row `k-1` (starting at third position), etc. So that in general, the element $A(i,j)$ is stored in the memory location `A(1+k+i-j,j)` for $j = 1,\ldots,n$ and $i \in \lbrack\max(1,j - k,j)\rbrack$. Also, the elements in the array `A` that do not conceptually correspond to the elements in the banded matrix (the top left $k \times k$ triangle) are not referenced.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [stbsv()](http://www.netlib.org/blas/stbsv.f), [dtbsv()](http://www.netlib.org/blas/dtbsv.f), [ctbsv()](http://www.netlib.org/blas/ctbsv.f), [ztbsv()](http://www.netlib.org/blas/ztbsv.f).
    pub fn cublasZtbsv_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        n: i64,
        k: i64,
        A: *const cuDoubleComplex,
        lda: i64,
        x: *mut cuDoubleComplex,
        incx: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric matrix-vector multiplication.
    ///
    /// $\textbf{y} = \alpha A\textbf{x} + \beta\textbf{y}$
    /// where $A$ is a $n \times n$ symmetric matrix stored in lower or upper mode, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ and $\beta$ are scalars.
    ///
    /// This function has an alternate faster implementation using atomics that can be enabled with [`cublasSetAtomicsMode`].
    ///
    /// Please see the section on the function [`cublasSetAtomicsMode`] for more details about the usage of atomics.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssymv()](http://www.netlib.org/blas/ssymv.f), [dsymv()](http://www.netlib.org/blas/dsymv.f).
    pub fn cublasSsymv_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: ::core::ffi::c_int,
        alpha: *const f32,
        A: *const f32,
        lda: ::core::ffi::c_int,
        x: *const f32,
        incx: ::core::ffi::c_int,
        beta: *const f32,
        y: *mut f32,
        incy: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric matrix-vector multiplication.
    ///
    /// $\textbf{y} = \alpha A\textbf{x} + \beta\textbf{y}$
    /// where $A$ is a $n \times n$ symmetric matrix stored in lower or upper mode, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ and $\beta$ are scalars.
    ///
    /// This function has an alternate faster implementation using atomics that can be enabled with [`cublasSetAtomicsMode`].
    ///
    /// Please see the section on the function [`cublasSetAtomicsMode`] for more details about the usage of atomics.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssymv()](http://www.netlib.org/blas/ssymv.f), [dsymv()](http://www.netlib.org/blas/dsymv.f).
    pub fn cublasSsymv_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: i64,
        alpha: *const f32,
        A: *const f32,
        lda: i64,
        x: *const f32,
        incx: i64,
        beta: *const f32,
        y: *mut f32,
        incy: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric matrix-vector multiplication.
    ///
    /// $\textbf{y} = \alpha A\textbf{x} + \beta\textbf{y}$
    /// where $A$ is a $n \times n$ symmetric matrix stored in lower or upper mode, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ and $\beta$ are scalars.
    ///
    /// This function has an alternate faster implementation using atomics that can be enabled with [`cublasSetAtomicsMode`].
    ///
    /// Please see the section on the function [`cublasSetAtomicsMode`] for more details about the usage of atomics.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssymv()](http://www.netlib.org/blas/ssymv.f), [dsymv()](http://www.netlib.org/blas/dsymv.f).
    pub fn cublasDsymv_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: ::core::ffi::c_int,
        alpha: *const f64,
        A: *const f64,
        lda: ::core::ffi::c_int,
        x: *const f64,
        incx: ::core::ffi::c_int,
        beta: *const f64,
        y: *mut f64,
        incy: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric matrix-vector multiplication.
    ///
    /// $\textbf{y} = \alpha A\textbf{x} + \beta\textbf{y}$
    /// where $A$ is a $n \times n$ symmetric matrix stored in lower or upper mode, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ and $\beta$ are scalars.
    ///
    /// This function has an alternate faster implementation using atomics that can be enabled with [`cublasSetAtomicsMode`].
    ///
    /// Please see the section on the function [`cublasSetAtomicsMode`] for more details about the usage of atomics.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssymv()](http://www.netlib.org/blas/ssymv.f), [dsymv()](http://www.netlib.org/blas/dsymv.f).
    pub fn cublasDsymv_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: i64,
        alpha: *const f64,
        A: *const f64,
        lda: i64,
        x: *const f64,
        incx: i64,
        beta: *const f64,
        y: *mut f64,
        incy: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric matrix-vector multiplication.
    ///
    /// $\textbf{y} = \alpha A\textbf{x} + \beta\textbf{y}$
    /// where $A$ is a $n \times n$ symmetric matrix stored in lower or upper mode, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ and $\beta$ are scalars.
    ///
    /// This function has an alternate faster implementation using atomics that can be enabled with [`cublasSetAtomicsMode`].
    ///
    /// Please see the section on the function [`cublasSetAtomicsMode`] for more details about the usage of atomics.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssymv()](http://www.netlib.org/blas/ssymv.f), [dsymv()](http://www.netlib.org/blas/dsymv.f).
    pub fn cublasCsymv_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: ::core::ffi::c_int,
        alpha: *const cuComplex,
        A: *const cuComplex,
        lda: ::core::ffi::c_int,
        x: *const cuComplex,
        incx: ::core::ffi::c_int,
        beta: *const cuComplex,
        y: *mut cuComplex,
        incy: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric matrix-vector multiplication.
    ///
    /// $\textbf{y} = \alpha A\textbf{x} + \beta\textbf{y}$
    /// where $A$ is a $n \times n$ symmetric matrix stored in lower or upper mode, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ and $\beta$ are scalars.
    ///
    /// This function has an alternate faster implementation using atomics that can be enabled with [`cublasSetAtomicsMode`].
    ///
    /// Please see the section on the function [`cublasSetAtomicsMode`] for more details about the usage of atomics.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssymv()](http://www.netlib.org/blas/ssymv.f), [dsymv()](http://www.netlib.org/blas/dsymv.f).
    pub fn cublasCsymv_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: i64,
        alpha: *const cuComplex,
        A: *const cuComplex,
        lda: i64,
        x: *const cuComplex,
        incx: i64,
        beta: *const cuComplex,
        y: *mut cuComplex,
        incy: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric matrix-vector multiplication.
    ///
    /// $\textbf{y} = \alpha A\textbf{x} + \beta\textbf{y}$
    /// where $A$ is a $n \times n$ symmetric matrix stored in lower or upper mode, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ and $\beta$ are scalars.
    ///
    /// This function has an alternate faster implementation using atomics that can be enabled with [`cublasSetAtomicsMode`].
    ///
    /// Please see the section on the function [`cublasSetAtomicsMode`] for more details about the usage of atomics.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssymv()](http://www.netlib.org/blas/ssymv.f), [dsymv()](http://www.netlib.org/blas/dsymv.f).
    pub fn cublasZsymv_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: ::core::ffi::c_int,
        alpha: *const cuDoubleComplex,
        A: *const cuDoubleComplex,
        lda: ::core::ffi::c_int,
        x: *const cuDoubleComplex,
        incx: ::core::ffi::c_int,
        beta: *const cuDoubleComplex,
        y: *mut cuDoubleComplex,
        incy: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric matrix-vector multiplication.
    ///
    /// $\textbf{y} = \alpha A\textbf{x} + \beta\textbf{y}$
    /// where $A$ is a $n \times n$ symmetric matrix stored in lower or upper mode, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ and $\beta$ are scalars.
    ///
    /// This function has an alternate faster implementation using atomics that can be enabled with [`cublasSetAtomicsMode`].
    ///
    /// Please see the section on the function [`cublasSetAtomicsMode`] for more details about the usage of atomics.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssymv()](http://www.netlib.org/blas/ssymv.f), [dsymv()](http://www.netlib.org/blas/dsymv.f).
    pub fn cublasZsymv_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: i64,
        alpha: *const cuDoubleComplex,
        A: *const cuDoubleComplex,
        lda: i64,
        x: *const cuDoubleComplex,
        incx: i64,
        beta: *const cuDoubleComplex,
        y: *mut cuDoubleComplex,
        incy: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the Hermitian matrix-vector multiplication:
    /// $$
    /// \textbf{y} = \alpha A\textbf{x} + \beta\textbf{y}
    /// $$
    ///
    /// where $A$ is a $n \times n$ Hermitian matrix stored in lower or upper mode, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ and $\beta$ are scalars.
    ///
    /// This function has an alternate faster implementation using atomics that can be enabled with
    ///
    /// Please see the section on the for more details about the usage of atomics
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [chemv()](http://www.netlib.org/blas/chemv.f), [zhemv()](http://www.netlib.org/blas/zhemv.f).
    pub fn cublasChemv_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: ::core::ffi::c_int,
        alpha: *const cuComplex,
        A: *const cuComplex,
        lda: ::core::ffi::c_int,
        x: *const cuComplex,
        incx: ::core::ffi::c_int,
        beta: *const cuComplex,
        y: *mut cuComplex,
        incy: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the Hermitian matrix-vector multiplication:
    /// $$
    /// \textbf{y} = \alpha A\textbf{x} + \beta\textbf{y}
    /// $$
    ///
    /// where $A$ is a $n \times n$ Hermitian matrix stored in lower or upper mode, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ and $\beta$ are scalars.
    ///
    /// This function has an alternate faster implementation using atomics that can be enabled with
    ///
    /// Please see the section on the for more details about the usage of atomics
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [chemv()](http://www.netlib.org/blas/chemv.f), [zhemv()](http://www.netlib.org/blas/zhemv.f).
    pub fn cublasChemv_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: i64,
        alpha: *const cuComplex,
        A: *const cuComplex,
        lda: i64,
        x: *const cuComplex,
        incx: i64,
        beta: *const cuComplex,
        y: *mut cuComplex,
        incy: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the Hermitian matrix-vector multiplication:
    /// $$
    /// \textbf{y} = \alpha A\textbf{x} + \beta\textbf{y}
    /// $$
    ///
    /// where $A$ is a $n \times n$ Hermitian matrix stored in lower or upper mode, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ and $\beta$ are scalars.
    ///
    /// This function has an alternate faster implementation using atomics that can be enabled with
    ///
    /// Please see the section on the for more details about the usage of atomics
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [chemv()](http://www.netlib.org/blas/chemv.f), [zhemv()](http://www.netlib.org/blas/zhemv.f).
    pub fn cublasZhemv_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: ::core::ffi::c_int,
        alpha: *const cuDoubleComplex,
        A: *const cuDoubleComplex,
        lda: ::core::ffi::c_int,
        x: *const cuDoubleComplex,
        incx: ::core::ffi::c_int,
        beta: *const cuDoubleComplex,
        y: *mut cuDoubleComplex,
        incy: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the Hermitian matrix-vector multiplication:
    /// $$
    /// \textbf{y} = \alpha A\textbf{x} + \beta\textbf{y}
    /// $$
    ///
    /// where $A$ is a $n \times n$ Hermitian matrix stored in lower or upper mode, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ and $\beta$ are scalars.
    ///
    /// This function has an alternate faster implementation using atomics that can be enabled with
    ///
    /// Please see the section on the for more details about the usage of atomics
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [chemv()](http://www.netlib.org/blas/chemv.f), [zhemv()](http://www.netlib.org/blas/zhemv.f).
    pub fn cublasZhemv_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: i64,
        alpha: *const cuDoubleComplex,
        A: *const cuDoubleComplex,
        lda: i64,
        x: *const cuDoubleComplex,
        incx: i64,
        beta: *const cuDoubleComplex,
        y: *mut cuDoubleComplex,
        incy: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric banded matrix-vector multiplication:
    /// $$
    /// \textbf{y} = \alpha A\textbf{x} + \beta\textbf{y}
    /// $$
    ///
    /// where $A$ is a $n \times n$ symmetric banded matrix with $k$ subdiagonals and superdiagonals, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ and $\beta$ are scalars.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the symmetric banded matrix $A$ is stored column by column, with the main diagonal of the matrix stored in row 1, the first subdiagonal in row 2 (starting at first position), the second subdiagonal in row 3 (starting at first position), etc. So that in general, the element $A(i,j)$ is stored in the memory location `A(1+i-j,j)` for $j = 1,\ldots,n$ and $i \in \lbrack j,\min(m,j + k)\rbrack$. Also, the elements in the array `A` that do not conceptually correspond to the elements in the banded matrix (the bottom right $k \times k$ triangle) are not referenced.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the symmetric banded matrix $A$ is stored column by column, with the main diagonal of the matrix stored in row `k + 1`, the first superdiagonal in row `k` (starting at second position), the second superdiagonal in row `k-1` (starting at third position), etc. So that in general, the element $A(i,j)$ is stored in the memory location `A(1+k+i-j,j)` for $j = 1,\ldots,n$ and $i \in \lbrack\max(1,j - k),j\rbrack$. Also, the elements in the array `A` that do not conceptually correspond to the elements in the banded matrix (the top left $k \times k$ triangle) are not referenced.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssbmv()](http://www.netlib.org/blas/ssbmv.f), [dsbmv()](http://www.netlib.org/blas/dsbmv.f).
    pub fn cublasSsbmv_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        alpha: *const f32,
        A: *const f32,
        lda: ::core::ffi::c_int,
        x: *const f32,
        incx: ::core::ffi::c_int,
        beta: *const f32,
        y: *mut f32,
        incy: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric banded matrix-vector multiplication:
    /// $$
    /// \textbf{y} = \alpha A\textbf{x} + \beta\textbf{y}
    /// $$
    ///
    /// where $A$ is a $n \times n$ symmetric banded matrix with $k$ subdiagonals and superdiagonals, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ and $\beta$ are scalars.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the symmetric banded matrix $A$ is stored column by column, with the main diagonal of the matrix stored in row 1, the first subdiagonal in row 2 (starting at first position), the second subdiagonal in row 3 (starting at first position), etc. So that in general, the element $A(i,j)$ is stored in the memory location `A(1+i-j,j)` for $j = 1,\ldots,n$ and $i \in \lbrack j,\min(m,j + k)\rbrack$. Also, the elements in the array `A` that do not conceptually correspond to the elements in the banded matrix (the bottom right $k \times k$ triangle) are not referenced.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the symmetric banded matrix $A$ is stored column by column, with the main diagonal of the matrix stored in row `k + 1`, the first superdiagonal in row `k` (starting at second position), the second superdiagonal in row `k-1` (starting at third position), etc. So that in general, the element $A(i,j)$ is stored in the memory location `A(1+k+i-j,j)` for $j = 1,\ldots,n$ and $i \in \lbrack\max(1,j - k),j\rbrack$. Also, the elements in the array `A` that do not conceptually correspond to the elements in the banded matrix (the top left $k \times k$ triangle) are not referenced.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssbmv()](http://www.netlib.org/blas/ssbmv.f), [dsbmv()](http://www.netlib.org/blas/dsbmv.f).
    pub fn cublasSsbmv_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: i64,
        k: i64,
        alpha: *const f32,
        A: *const f32,
        lda: i64,
        x: *const f32,
        incx: i64,
        beta: *const f32,
        y: *mut f32,
        incy: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric banded matrix-vector multiplication:
    /// $$
    /// \textbf{y} = \alpha A\textbf{x} + \beta\textbf{y}
    /// $$
    ///
    /// where $A$ is a $n \times n$ symmetric banded matrix with $k$ subdiagonals and superdiagonals, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ and $\beta$ are scalars.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the symmetric banded matrix $A$ is stored column by column, with the main diagonal of the matrix stored in row 1, the first subdiagonal in row 2 (starting at first position), the second subdiagonal in row 3 (starting at first position), etc. So that in general, the element $A(i,j)$ is stored in the memory location `A(1+i-j,j)` for $j = 1,\ldots,n$ and $i \in \lbrack j,\min(m,j + k)\rbrack$. Also, the elements in the array `A` that do not conceptually correspond to the elements in the banded matrix (the bottom right $k \times k$ triangle) are not referenced.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the symmetric banded matrix $A$ is stored column by column, with the main diagonal of the matrix stored in row `k + 1`, the first superdiagonal in row `k` (starting at second position), the second superdiagonal in row `k-1` (starting at third position), etc. So that in general, the element $A(i,j)$ is stored in the memory location `A(1+k+i-j,j)` for $j = 1,\ldots,n$ and $i \in \lbrack\max(1,j - k),j\rbrack$. Also, the elements in the array `A` that do not conceptually correspond to the elements in the banded matrix (the top left $k \times k$ triangle) are not referenced.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssbmv()](http://www.netlib.org/blas/ssbmv.f), [dsbmv()](http://www.netlib.org/blas/dsbmv.f).
    pub fn cublasDsbmv_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        alpha: *const f64,
        A: *const f64,
        lda: ::core::ffi::c_int,
        x: *const f64,
        incx: ::core::ffi::c_int,
        beta: *const f64,
        y: *mut f64,
        incy: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric banded matrix-vector multiplication:
    /// $$
    /// \textbf{y} = \alpha A\textbf{x} + \beta\textbf{y}
    /// $$
    ///
    /// where $A$ is a $n \times n$ symmetric banded matrix with $k$ subdiagonals and superdiagonals, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ and $\beta$ are scalars.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the symmetric banded matrix $A$ is stored column by column, with the main diagonal of the matrix stored in row 1, the first subdiagonal in row 2 (starting at first position), the second subdiagonal in row 3 (starting at first position), etc. So that in general, the element $A(i,j)$ is stored in the memory location `A(1+i-j,j)` for $j = 1,\ldots,n$ and $i \in \lbrack j,\min(m,j + k)\rbrack$. Also, the elements in the array `A` that do not conceptually correspond to the elements in the banded matrix (the bottom right $k \times k$ triangle) are not referenced.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the symmetric banded matrix $A$ is stored column by column, with the main diagonal of the matrix stored in row `k + 1`, the first superdiagonal in row `k` (starting at second position), the second superdiagonal in row `k-1` (starting at third position), etc. So that in general, the element $A(i,j)$ is stored in the memory location `A(1+k+i-j,j)` for $j = 1,\ldots,n$ and $i \in \lbrack\max(1,j - k),j\rbrack$. Also, the elements in the array `A` that do not conceptually correspond to the elements in the banded matrix (the top left $k \times k$ triangle) are not referenced.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssbmv()](http://www.netlib.org/blas/ssbmv.f), [dsbmv()](http://www.netlib.org/blas/dsbmv.f).
    pub fn cublasDsbmv_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: i64,
        k: i64,
        alpha: *const f64,
        A: *const f64,
        lda: i64,
        x: *const f64,
        incx: i64,
        beta: *const f64,
        y: *mut f64,
        incy: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the Hermitian banded matrix-vector multiplication:
    /// $$
    /// \textbf{y} = \alpha A\textbf{x} + \beta\textbf{y}
    /// $$
    ///
    /// where $A$ is a $n \times n$ Hermitian banded matrix with $k$ subdiagonals and superdiagonals, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ and $\beta$ are scalars.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the Hermitian banded matrix $A$ is stored column by column, with the main diagonal of the matrix stored in row `1`, the first subdiagonal in row `2` (starting at first position), the second subdiagonal in row `3` (starting at first position), etc. So that in general, the element $A(i,j)$ is stored in the memory location `A(1+i-j,j)` for $j = 1,\ldots,n$ and $i \in \lbrack j,\min(m,j + k)\rbrack$. Also, the elements in the array `A` that do not conceptually correspond to the elements in the banded matrix (the bottom right $k \times k$ triangle) are not referenced.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the Hermitian banded matrix $A$ is stored column by column, with the main diagonal of the matrix stored in row `k + 1`, the first superdiagonal in row `k` (starting at second position), the second superdiagonal in row `k-1` (starting at third position), etc. So that in general, the element $A(i,j)$ is stored in the memory location `A(1+k+i-j,j)` for $j = 1,\ldots,n$ and $i \in \lbrack\max(1,j - k),j\rbrack$. Also, the elements in the array `A` that do not conceptually correspond to the elements in the banded matrix (the top left $k \times k$ triangle) are not referenced.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [chbmv()](http://www.netlib.org/blas/chbmv.f), [zhbmv()](http://www.netlib.org/blas/zhbmv.f).
    pub fn cublasChbmv_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        alpha: *const cuComplex,
        A: *const cuComplex,
        lda: ::core::ffi::c_int,
        x: *const cuComplex,
        incx: ::core::ffi::c_int,
        beta: *const cuComplex,
        y: *mut cuComplex,
        incy: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the Hermitian banded matrix-vector multiplication:
    /// $$
    /// \textbf{y} = \alpha A\textbf{x} + \beta\textbf{y}
    /// $$
    ///
    /// where $A$ is a $n \times n$ Hermitian banded matrix with $k$ subdiagonals and superdiagonals, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ and $\beta$ are scalars.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the Hermitian banded matrix $A$ is stored column by column, with the main diagonal of the matrix stored in row `1`, the first subdiagonal in row `2` (starting at first position), the second subdiagonal in row `3` (starting at first position), etc. So that in general, the element $A(i,j)$ is stored in the memory location `A(1+i-j,j)` for $j = 1,\ldots,n$ and $i \in \lbrack j,\min(m,j + k)\rbrack$. Also, the elements in the array `A` that do not conceptually correspond to the elements in the banded matrix (the bottom right $k \times k$ triangle) are not referenced.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the Hermitian banded matrix $A$ is stored column by column, with the main diagonal of the matrix stored in row `k + 1`, the first superdiagonal in row `k` (starting at second position), the second superdiagonal in row `k-1` (starting at third position), etc. So that in general, the element $A(i,j)$ is stored in the memory location `A(1+k+i-j,j)` for $j = 1,\ldots,n$ and $i \in \lbrack\max(1,j - k),j\rbrack$. Also, the elements in the array `A` that do not conceptually correspond to the elements in the banded matrix (the top left $k \times k$ triangle) are not referenced.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [chbmv()](http://www.netlib.org/blas/chbmv.f), [zhbmv()](http://www.netlib.org/blas/zhbmv.f).
    pub fn cublasChbmv_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: i64,
        k: i64,
        alpha: *const cuComplex,
        A: *const cuComplex,
        lda: i64,
        x: *const cuComplex,
        incx: i64,
        beta: *const cuComplex,
        y: *mut cuComplex,
        incy: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the Hermitian banded matrix-vector multiplication:
    /// $$
    /// \textbf{y} = \alpha A\textbf{x} + \beta\textbf{y}
    /// $$
    ///
    /// where $A$ is a $n \times n$ Hermitian banded matrix with $k$ subdiagonals and superdiagonals, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ and $\beta$ are scalars.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the Hermitian banded matrix $A$ is stored column by column, with the main diagonal of the matrix stored in row `1`, the first subdiagonal in row `2` (starting at first position), the second subdiagonal in row `3` (starting at first position), etc. So that in general, the element $A(i,j)$ is stored in the memory location `A(1+i-j,j)` for $j = 1,\ldots,n$ and $i \in \lbrack j,\min(m,j + k)\rbrack$. Also, the elements in the array `A` that do not conceptually correspond to the elements in the banded matrix (the bottom right $k \times k$ triangle) are not referenced.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the Hermitian banded matrix $A$ is stored column by column, with the main diagonal of the matrix stored in row `k + 1`, the first superdiagonal in row `k` (starting at second position), the second superdiagonal in row `k-1` (starting at third position), etc. So that in general, the element $A(i,j)$ is stored in the memory location `A(1+k+i-j,j)` for $j = 1,\ldots,n$ and $i \in \lbrack\max(1,j - k),j\rbrack$. Also, the elements in the array `A` that do not conceptually correspond to the elements in the banded matrix (the top left $k \times k$ triangle) are not referenced.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [chbmv()](http://www.netlib.org/blas/chbmv.f), [zhbmv()](http://www.netlib.org/blas/zhbmv.f).
    pub fn cublasZhbmv_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        alpha: *const cuDoubleComplex,
        A: *const cuDoubleComplex,
        lda: ::core::ffi::c_int,
        x: *const cuDoubleComplex,
        incx: ::core::ffi::c_int,
        beta: *const cuDoubleComplex,
        y: *mut cuDoubleComplex,
        incy: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the Hermitian banded matrix-vector multiplication:
    /// $$
    /// \textbf{y} = \alpha A\textbf{x} + \beta\textbf{y}
    /// $$
    ///
    /// where $A$ is a $n \times n$ Hermitian banded matrix with $k$ subdiagonals and superdiagonals, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ and $\beta$ are scalars.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the Hermitian banded matrix $A$ is stored column by column, with the main diagonal of the matrix stored in row `1`, the first subdiagonal in row `2` (starting at first position), the second subdiagonal in row `3` (starting at first position), etc. So that in general, the element $A(i,j)$ is stored in the memory location `A(1+i-j,j)` for $j = 1,\ldots,n$ and $i \in \lbrack j,\min(m,j + k)\rbrack$. Also, the elements in the array `A` that do not conceptually correspond to the elements in the banded matrix (the bottom right $k \times k$ triangle) are not referenced.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the Hermitian banded matrix $A$ is stored column by column, with the main diagonal of the matrix stored in row `k + 1`, the first superdiagonal in row `k` (starting at second position), the second superdiagonal in row `k-1` (starting at third position), etc. So that in general, the element $A(i,j)$ is stored in the memory location `A(1+k+i-j,j)` for $j = 1,\ldots,n$ and $i \in \lbrack\max(1,j - k),j\rbrack$. Also, the elements in the array `A` that do not conceptually correspond to the elements in the banded matrix (the top left $k \times k$ triangle) are not referenced.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [chbmv()](http://www.netlib.org/blas/chbmv.f), [zhbmv()](http://www.netlib.org/blas/zhbmv.f).
    pub fn cublasZhbmv_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: i64,
        k: i64,
        alpha: *const cuDoubleComplex,
        A: *const cuDoubleComplex,
        lda: i64,
        x: *const cuDoubleComplex,
        incx: i64,
        beta: *const cuDoubleComplex,
        y: *mut cuDoubleComplex,
        incy: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric packed matrix-vector multiplication:
    /// $$
    /// \textbf{y} = \alpha A\textbf{x} + \beta\textbf{y}
    /// $$
    ///
    /// where $A$ is a $n \times n$ symmetric matrix stored in packed format, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ and $\beta$ are scalars.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the elements in the lower triangular part of the symmetric matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+((2*n-j+1)*j)/2\]` for $j = 1,\ldots,n$ and $i \geq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the elements in the upper triangular part of the symmetric matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+(j*(j+1))/2\]` for $j = 1,\ldots,n$ and $i \leq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sspmv()](http://www.netlib.org/blas/sspmv.f), [dspmv()](http://www.netlib.org/blas/dspmv.f).
    pub fn cublasSspmv_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: ::core::ffi::c_int,
        alpha: *const f32,
        AP: *const f32,
        x: *const f32,
        incx: ::core::ffi::c_int,
        beta: *const f32,
        y: *mut f32,
        incy: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric packed matrix-vector multiplication:
    /// $$
    /// \textbf{y} = \alpha A\textbf{x} + \beta\textbf{y}
    /// $$
    ///
    /// where $A$ is a $n \times n$ symmetric matrix stored in packed format, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ and $\beta$ are scalars.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the elements in the lower triangular part of the symmetric matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+((2*n-j+1)*j)/2\]` for $j = 1,\ldots,n$ and $i \geq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the elements in the upper triangular part of the symmetric matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+(j*(j+1))/2\]` for $j = 1,\ldots,n$ and $i \leq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sspmv()](http://www.netlib.org/blas/sspmv.f), [dspmv()](http://www.netlib.org/blas/dspmv.f).
    pub fn cublasSspmv_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: i64,
        alpha: *const f32,
        AP: *const f32,
        x: *const f32,
        incx: i64,
        beta: *const f32,
        y: *mut f32,
        incy: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric packed matrix-vector multiplication:
    /// $$
    /// \textbf{y} = \alpha A\textbf{x} + \beta\textbf{y}
    /// $$
    ///
    /// where $A$ is a $n \times n$ symmetric matrix stored in packed format, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ and $\beta$ are scalars.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the elements in the lower triangular part of the symmetric matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+((2*n-j+1)*j)/2\]` for $j = 1,\ldots,n$ and $i \geq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the elements in the upper triangular part of the symmetric matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+(j*(j+1))/2\]` for $j = 1,\ldots,n$ and $i \leq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sspmv()](http://www.netlib.org/blas/sspmv.f), [dspmv()](http://www.netlib.org/blas/dspmv.f).
    pub fn cublasDspmv_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: ::core::ffi::c_int,
        alpha: *const f64,
        AP: *const f64,
        x: *const f64,
        incx: ::core::ffi::c_int,
        beta: *const f64,
        y: *mut f64,
        incy: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric packed matrix-vector multiplication:
    /// $$
    /// \textbf{y} = \alpha A\textbf{x} + \beta\textbf{y}
    /// $$
    ///
    /// where $A$ is a $n \times n$ symmetric matrix stored in packed format, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ and $\beta$ are scalars.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the elements in the lower triangular part of the symmetric matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+((2*n-j+1)*j)/2\]` for $j = 1,\ldots,n$ and $i \geq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the elements in the upper triangular part of the symmetric matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+(j*(j+1))/2\]` for $j = 1,\ldots,n$ and $i \leq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sspmv()](http://www.netlib.org/blas/sspmv.f), [dspmv()](http://www.netlib.org/blas/dspmv.f).
    pub fn cublasDspmv_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: i64,
        alpha: *const f64,
        AP: *const f64,
        x: *const f64,
        incx: i64,
        beta: *const f64,
        y: *mut f64,
        incy: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the Hermitian packed matrix-vector multiplication:
    /// $$
    /// \textbf{y} = \alpha A\textbf{x} + \beta\textbf{y}
    /// $$
    ///
    /// where $A$ is a $n \times n$ Hermitian matrix stored in packed format, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ and $\beta$ are scalars.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the elements in the lower triangular part of the Hermitian matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+((2*n-j+1)*j)/2\]` for $j = 1,\ldots,n$ and $i \geq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the elements in the upper triangular part of the Hermitian matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+(j*(j+1))/2\]` for $j = 1,\ldots,n$ and $i \leq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [chpmv()](http://www.netlib.org/blas/chpmv.f), [zhpmv()](http://www.netlib.org/blas/zhpmv.f).
    pub fn cublasChpmv_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: ::core::ffi::c_int,
        alpha: *const cuComplex,
        AP: *const cuComplex,
        x: *const cuComplex,
        incx: ::core::ffi::c_int,
        beta: *const cuComplex,
        y: *mut cuComplex,
        incy: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the Hermitian packed matrix-vector multiplication:
    /// $$
    /// \textbf{y} = \alpha A\textbf{x} + \beta\textbf{y}
    /// $$
    ///
    /// where $A$ is a $n \times n$ Hermitian matrix stored in packed format, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ and $\beta$ are scalars.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the elements in the lower triangular part of the Hermitian matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+((2*n-j+1)*j)/2\]` for $j = 1,\ldots,n$ and $i \geq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the elements in the upper triangular part of the Hermitian matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+(j*(j+1))/2\]` for $j = 1,\ldots,n$ and $i \leq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [chpmv()](http://www.netlib.org/blas/chpmv.f), [zhpmv()](http://www.netlib.org/blas/zhpmv.f).
    pub fn cublasChpmv_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: i64,
        alpha: *const cuComplex,
        AP: *const cuComplex,
        x: *const cuComplex,
        incx: i64,
        beta: *const cuComplex,
        y: *mut cuComplex,
        incy: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the Hermitian packed matrix-vector multiplication:
    /// $$
    /// \textbf{y} = \alpha A\textbf{x} + \beta\textbf{y}
    /// $$
    ///
    /// where $A$ is a $n \times n$ Hermitian matrix stored in packed format, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ and $\beta$ are scalars.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the elements in the lower triangular part of the Hermitian matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+((2*n-j+1)*j)/2\]` for $j = 1,\ldots,n$ and $i \geq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the elements in the upper triangular part of the Hermitian matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+(j*(j+1))/2\]` for $j = 1,\ldots,n$ and $i \leq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [chpmv()](http://www.netlib.org/blas/chpmv.f), [zhpmv()](http://www.netlib.org/blas/zhpmv.f).
    pub fn cublasZhpmv_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: ::core::ffi::c_int,
        alpha: *const cuDoubleComplex,
        AP: *const cuDoubleComplex,
        x: *const cuDoubleComplex,
        incx: ::core::ffi::c_int,
        beta: *const cuDoubleComplex,
        y: *mut cuDoubleComplex,
        incy: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the Hermitian packed matrix-vector multiplication:
    /// $$
    /// \textbf{y} = \alpha A\textbf{x} + \beta\textbf{y}
    /// $$
    ///
    /// where $A$ is a $n \times n$ Hermitian matrix stored in packed format, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ and $\beta$ are scalars.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the elements in the lower triangular part of the Hermitian matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+((2*n-j+1)*j)/2\]` for $j = 1,\ldots,n$ and $i \geq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the elements in the upper triangular part of the Hermitian matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+(j*(j+1))/2\]` for $j = 1,\ldots,n$ and $i \leq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [chpmv()](http://www.netlib.org/blas/chpmv.f), [zhpmv()](http://www.netlib.org/blas/zhpmv.f).
    pub fn cublasZhpmv_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: i64,
        alpha: *const cuDoubleComplex,
        AP: *const cuDoubleComplex,
        x: *const cuDoubleComplex,
        incx: i64,
        beta: *const cuDoubleComplex,
        y: *mut cuDoubleComplex,
        incy: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the rank-1 update:
    /// $$
    /// A = \begin{cases}
    /// {\alpha\mathbf{xy}^{T} + A} & \text{if ger(),geru() is called} \\\\
    /// {\alpha\mathbf{xy}^{H} + A} & \text{if gerc() is called} \\\\
    /// \end{cases}
    /// $$
    ///
    /// where $A$ is a $m \times n$ matrix stored in column-major format, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ is a scalar.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sger()](http://www.netlib.org/blas/sger.f), [dger()](http://www.netlib.org/blas/dger.f), [cgeru()](http://www.netlib.org/blas/cgeru.f), [cgerc()](http://www.netlib.org/blas/cgerc.f), [zgeru()](http://www.netlib.org/blas/zgeru.f), [zgerc()](http://www.netlib.org/blas/zgerc.f).
    pub fn cublasSger_v2(
        handle: cublasHandle_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        alpha: *const f32,
        x: *const f32,
        incx: ::core::ffi::c_int,
        y: *const f32,
        incy: ::core::ffi::c_int,
        A: *mut f32,
        lda: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the rank-1 update:
    /// $$
    /// A = \begin{cases}
    /// {\alpha\mathbf{xy}^{T} + A} & \text{if ger(),geru() is called} \\\\
    /// {\alpha\mathbf{xy}^{H} + A} & \text{if gerc() is called} \\\\
    /// \end{cases}
    /// $$
    ///
    /// where $A$ is a $m \times n$ matrix stored in column-major format, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ is a scalar.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sger()](http://www.netlib.org/blas/sger.f), [dger()](http://www.netlib.org/blas/dger.f), [cgeru()](http://www.netlib.org/blas/cgeru.f), [cgerc()](http://www.netlib.org/blas/cgerc.f), [zgeru()](http://www.netlib.org/blas/zgeru.f), [zgerc()](http://www.netlib.org/blas/zgerc.f).
    pub fn cublasSger_v2_64(
        handle: cublasHandle_t,
        m: i64,
        n: i64,
        alpha: *const f32,
        x: *const f32,
        incx: i64,
        y: *const f32,
        incy: i64,
        A: *mut f32,
        lda: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the rank-1 update:
    /// $$
    /// A = \begin{cases}
    /// {\alpha\mathbf{xy}^{T} + A} & \text{if ger(),geru() is called} \\\\
    /// {\alpha\mathbf{xy}^{H} + A} & \text{if gerc() is called} \\\\
    /// \end{cases}
    /// $$
    ///
    /// where $A$ is a $m \times n$ matrix stored in column-major format, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ is a scalar.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sger()](http://www.netlib.org/blas/sger.f), [dger()](http://www.netlib.org/blas/dger.f), [cgeru()](http://www.netlib.org/blas/cgeru.f), [cgerc()](http://www.netlib.org/blas/cgerc.f), [zgeru()](http://www.netlib.org/blas/zgeru.f), [zgerc()](http://www.netlib.org/blas/zgerc.f).
    pub fn cublasDger_v2(
        handle: cublasHandle_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        alpha: *const f64,
        x: *const f64,
        incx: ::core::ffi::c_int,
        y: *const f64,
        incy: ::core::ffi::c_int,
        A: *mut f64,
        lda: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the rank-1 update:
    /// $$
    /// A = \begin{cases}
    /// {\alpha\mathbf{xy}^{T} + A} & \text{if ger(),geru() is called} \\\\
    /// {\alpha\mathbf{xy}^{H} + A} & \text{if gerc() is called} \\\\
    /// \end{cases}
    /// $$
    ///
    /// where $A$ is a $m \times n$ matrix stored in column-major format, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ is a scalar.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sger()](http://www.netlib.org/blas/sger.f), [dger()](http://www.netlib.org/blas/dger.f), [cgeru()](http://www.netlib.org/blas/cgeru.f), [cgerc()](http://www.netlib.org/blas/cgerc.f), [zgeru()](http://www.netlib.org/blas/zgeru.f), [zgerc()](http://www.netlib.org/blas/zgerc.f).
    pub fn cublasDger_v2_64(
        handle: cublasHandle_t,
        m: i64,
        n: i64,
        alpha: *const f64,
        x: *const f64,
        incx: i64,
        y: *const f64,
        incy: i64,
        A: *mut f64,
        lda: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    pub fn cublasCgeru_v2(
        handle: cublasHandle_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        alpha: *const cuComplex,
        x: *const cuComplex,
        incx: ::core::ffi::c_int,
        y: *const cuComplex,
        incy: ::core::ffi::c_int,
        A: *mut cuComplex,
        lda: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    pub fn cublasCgeru_v2_64(
        handle: cublasHandle_t,
        m: i64,
        n: i64,
        alpha: *const cuComplex,
        x: *const cuComplex,
        incx: i64,
        y: *const cuComplex,
        incy: i64,
        A: *mut cuComplex,
        lda: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    pub fn cublasCgerc_v2(
        handle: cublasHandle_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        alpha: *const cuComplex,
        x: *const cuComplex,
        incx: ::core::ffi::c_int,
        y: *const cuComplex,
        incy: ::core::ffi::c_int,
        A: *mut cuComplex,
        lda: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    pub fn cublasCgerc_v2_64(
        handle: cublasHandle_t,
        m: i64,
        n: i64,
        alpha: *const cuComplex,
        x: *const cuComplex,
        incx: i64,
        y: *const cuComplex,
        incy: i64,
        A: *mut cuComplex,
        lda: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    pub fn cublasZgeru_v2(
        handle: cublasHandle_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        alpha: *const cuDoubleComplex,
        x: *const cuDoubleComplex,
        incx: ::core::ffi::c_int,
        y: *const cuDoubleComplex,
        incy: ::core::ffi::c_int,
        A: *mut cuDoubleComplex,
        lda: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    pub fn cublasZgeru_v2_64(
        handle: cublasHandle_t,
        m: i64,
        n: i64,
        alpha: *const cuDoubleComplex,
        x: *const cuDoubleComplex,
        incx: i64,
        y: *const cuDoubleComplex,
        incy: i64,
        A: *mut cuDoubleComplex,
        lda: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    pub fn cublasZgerc_v2(
        handle: cublasHandle_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        alpha: *const cuDoubleComplex,
        x: *const cuDoubleComplex,
        incx: ::core::ffi::c_int,
        y: *const cuDoubleComplex,
        incy: ::core::ffi::c_int,
        A: *mut cuDoubleComplex,
        lda: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    pub fn cublasZgerc_v2_64(
        handle: cublasHandle_t,
        m: i64,
        n: i64,
        alpha: *const cuDoubleComplex,
        x: *const cuDoubleComplex,
        incx: i64,
        y: *const cuDoubleComplex,
        incy: i64,
        A: *mut cuDoubleComplex,
        lda: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric rank-1 update:
    /// $$
    /// A = \alpha\textbf{x}\textbf{x}^{T} + A
    /// $$
    ///
    /// where $A$ is a $n \times n$ symmetric matrix stored in column-major format, $\mathbf{x}$ is a vector, and $\alpha$ is a scalar.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssyr()](http://www.netlib.org/blas/ssyr.f), [dsyr()](http://www.netlib.org/blas/dsyr.f).
    pub fn cublasSsyr_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: ::core::ffi::c_int,
        alpha: *const f32,
        x: *const f32,
        incx: ::core::ffi::c_int,
        A: *mut f32,
        lda: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric rank-1 update:
    /// $$
    /// A = \alpha\textbf{x}\textbf{x}^{T} + A
    /// $$
    ///
    /// where $A$ is a $n \times n$ symmetric matrix stored in column-major format, $\mathbf{x}$ is a vector, and $\alpha$ is a scalar.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssyr()](http://www.netlib.org/blas/ssyr.f), [dsyr()](http://www.netlib.org/blas/dsyr.f).
    pub fn cublasSsyr_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: i64,
        alpha: *const f32,
        x: *const f32,
        incx: i64,
        A: *mut f32,
        lda: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric rank-1 update:
    /// $$
    /// A = \alpha\textbf{x}\textbf{x}^{T} + A
    /// $$
    ///
    /// where $A$ is a $n \times n$ symmetric matrix stored in column-major format, $\mathbf{x}$ is a vector, and $\alpha$ is a scalar.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssyr()](http://www.netlib.org/blas/ssyr.f), [dsyr()](http://www.netlib.org/blas/dsyr.f).
    pub fn cublasDsyr_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: ::core::ffi::c_int,
        alpha: *const f64,
        x: *const f64,
        incx: ::core::ffi::c_int,
        A: *mut f64,
        lda: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric rank-1 update:
    /// $$
    /// A = \alpha\textbf{x}\textbf{x}^{T} + A
    /// $$
    ///
    /// where $A$ is a $n \times n$ symmetric matrix stored in column-major format, $\mathbf{x}$ is a vector, and $\alpha$ is a scalar.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssyr()](http://www.netlib.org/blas/ssyr.f), [dsyr()](http://www.netlib.org/blas/dsyr.f).
    pub fn cublasDsyr_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: i64,
        alpha: *const f64,
        x: *const f64,
        incx: i64,
        A: *mut f64,
        lda: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric rank-1 update:
    /// $$
    /// A = \alpha\textbf{x}\textbf{x}^{T} + A
    /// $$
    ///
    /// where $A$ is a $n \times n$ symmetric matrix stored in column-major format, $\mathbf{x}$ is a vector, and $\alpha$ is a scalar.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssyr()](http://www.netlib.org/blas/ssyr.f), [dsyr()](http://www.netlib.org/blas/dsyr.f).
    pub fn cublasCsyr_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: ::core::ffi::c_int,
        alpha: *const cuComplex,
        x: *const cuComplex,
        incx: ::core::ffi::c_int,
        A: *mut cuComplex,
        lda: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric rank-1 update:
    /// $$
    /// A = \alpha\textbf{x}\textbf{x}^{T} + A
    /// $$
    ///
    /// where $A$ is a $n \times n$ symmetric matrix stored in column-major format, $\mathbf{x}$ is a vector, and $\alpha$ is a scalar.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssyr()](http://www.netlib.org/blas/ssyr.f), [dsyr()](http://www.netlib.org/blas/dsyr.f).
    pub fn cublasCsyr_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: i64,
        alpha: *const cuComplex,
        x: *const cuComplex,
        incx: i64,
        A: *mut cuComplex,
        lda: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric rank-1 update:
    /// $$
    /// A = \alpha\textbf{x}\textbf{x}^{T} + A
    /// $$
    ///
    /// where $A$ is a $n \times n$ symmetric matrix stored in column-major format, $\mathbf{x}$ is a vector, and $\alpha$ is a scalar.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssyr()](http://www.netlib.org/blas/ssyr.f), [dsyr()](http://www.netlib.org/blas/dsyr.f).
    pub fn cublasZsyr_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: ::core::ffi::c_int,
        alpha: *const cuDoubleComplex,
        x: *const cuDoubleComplex,
        incx: ::core::ffi::c_int,
        A: *mut cuDoubleComplex,
        lda: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric rank-1 update:
    /// $$
    /// A = \alpha\textbf{x}\textbf{x}^{T} + A
    /// $$
    ///
    /// where $A$ is a $n \times n$ symmetric matrix stored in column-major format, $\mathbf{x}$ is a vector, and $\alpha$ is a scalar.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssyr()](http://www.netlib.org/blas/ssyr.f), [dsyr()](http://www.netlib.org/blas/dsyr.f).
    pub fn cublasZsyr_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: i64,
        alpha: *const cuDoubleComplex,
        x: *const cuDoubleComplex,
        incx: i64,
        A: *mut cuDoubleComplex,
        lda: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the Hermitian rank-1 update:
    /// $$
    /// A = \alpha\textbf{x}\textbf{x}^{H} + A
    /// $$
    ///
    /// where $A$ is a $n \times n$ Hermitian matrix stored in column-major format, $\mathbf{x}$ is a vector, and $\alpha$ is a scalar.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [cher()](http://www.netlib.org/blas/cher.f), [zher()](http://www.netlib.org/blas/zher.f).
    pub fn cublasCher_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: ::core::ffi::c_int,
        alpha: *const f32,
        x: *const cuComplex,
        incx: ::core::ffi::c_int,
        A: *mut cuComplex,
        lda: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the Hermitian rank-1 update:
    /// $$
    /// A = \alpha\textbf{x}\textbf{x}^{H} + A
    /// $$
    ///
    /// where $A$ is a $n \times n$ Hermitian matrix stored in column-major format, $\mathbf{x}$ is a vector, and $\alpha$ is a scalar.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [cher()](http://www.netlib.org/blas/cher.f), [zher()](http://www.netlib.org/blas/zher.f).
    pub fn cublasCher_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: i64,
        alpha: *const f32,
        x: *const cuComplex,
        incx: i64,
        A: *mut cuComplex,
        lda: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the Hermitian rank-1 update:
    /// $$
    /// A = \alpha\textbf{x}\textbf{x}^{H} + A
    /// $$
    ///
    /// where $A$ is a $n \times n$ Hermitian matrix stored in column-major format, $\mathbf{x}$ is a vector, and $\alpha$ is a scalar.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [cher()](http://www.netlib.org/blas/cher.f), [zher()](http://www.netlib.org/blas/zher.f).
    pub fn cublasZher_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: ::core::ffi::c_int,
        alpha: *const f64,
        x: *const cuDoubleComplex,
        incx: ::core::ffi::c_int,
        A: *mut cuDoubleComplex,
        lda: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the Hermitian rank-1 update:
    /// $$
    /// A = \alpha\textbf{x}\textbf{x}^{H} + A
    /// $$
    ///
    /// where $A$ is a $n \times n$ Hermitian matrix stored in column-major format, $\mathbf{x}$ is a vector, and $\alpha$ is a scalar.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [cher()](http://www.netlib.org/blas/cher.f), [zher()](http://www.netlib.org/blas/zher.f).
    pub fn cublasZher_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: i64,
        alpha: *const f64,
        x: *const cuDoubleComplex,
        incx: i64,
        A: *mut cuDoubleComplex,
        lda: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the packed symmetric rank-1 update:
    /// $$
    /// A = \alpha\textbf{x}\textbf{x}^{T} + A
    /// $$
    ///
    /// where $A$ is a $n \times n$ symmetric matrix stored in packed format, $\mathbf{x}$ is a vector, and $\alpha$ is a scalar.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the elements in the lower triangular part of the symmetric matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+((2*n-j+1)*j)/2\]` for $j = 1,\ldots,n$ and $i \geq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the elements in the upper triangular part of the symmetric matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+(j*(j+1))/2\]` for $j = 1,\ldots,n$ and $i \leq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sspr()](http://www.netlib.org/blas/sspr.f), [dspr()](http://www.netlib.org/blas/dspr.f).
    pub fn cublasSspr_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: ::core::ffi::c_int,
        alpha: *const f32,
        x: *const f32,
        incx: ::core::ffi::c_int,
        AP: *mut f32,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the packed symmetric rank-1 update:
    /// $$
    /// A = \alpha\textbf{x}\textbf{x}^{T} + A
    /// $$
    ///
    /// where $A$ is a $n \times n$ symmetric matrix stored in packed format, $\mathbf{x}$ is a vector, and $\alpha$ is a scalar.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the elements in the lower triangular part of the symmetric matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+((2*n-j+1)*j)/2\]` for $j = 1,\ldots,n$ and $i \geq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the elements in the upper triangular part of the symmetric matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+(j*(j+1))/2\]` for $j = 1,\ldots,n$ and $i \leq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sspr()](http://www.netlib.org/blas/sspr.f), [dspr()](http://www.netlib.org/blas/dspr.f).
    pub fn cublasSspr_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: i64,
        alpha: *const f32,
        x: *const f32,
        incx: i64,
        AP: *mut f32,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the packed symmetric rank-1 update:
    /// $$
    /// A = \alpha\textbf{x}\textbf{x}^{T} + A
    /// $$
    ///
    /// where $A$ is a $n \times n$ symmetric matrix stored in packed format, $\mathbf{x}$ is a vector, and $\alpha$ is a scalar.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the elements in the lower triangular part of the symmetric matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+((2*n-j+1)*j)/2\]` for $j = 1,\ldots,n$ and $i \geq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the elements in the upper triangular part of the symmetric matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+(j*(j+1))/2\]` for $j = 1,\ldots,n$ and $i \leq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sspr()](http://www.netlib.org/blas/sspr.f), [dspr()](http://www.netlib.org/blas/dspr.f).
    pub fn cublasDspr_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: ::core::ffi::c_int,
        alpha: *const f64,
        x: *const f64,
        incx: ::core::ffi::c_int,
        AP: *mut f64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the packed symmetric rank-1 update:
    /// $$
    /// A = \alpha\textbf{x}\textbf{x}^{T} + A
    /// $$
    ///
    /// where $A$ is a $n \times n$ symmetric matrix stored in packed format, $\mathbf{x}$ is a vector, and $\alpha$ is a scalar.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the elements in the lower triangular part of the symmetric matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+((2*n-j+1)*j)/2\]` for $j = 1,\ldots,n$ and $i \geq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the elements in the upper triangular part of the symmetric matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+(j*(j+1))/2\]` for $j = 1,\ldots,n$ and $i \leq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sspr()](http://www.netlib.org/blas/sspr.f), [dspr()](http://www.netlib.org/blas/dspr.f).
    pub fn cublasDspr_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: i64,
        alpha: *const f64,
        x: *const f64,
        incx: i64,
        AP: *mut f64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the packed Hermitian rank-1 update:
    /// $$
    /// A = \alpha\textbf{x}\textbf{x}^{H} + A
    /// $$
    ///
    /// where $A$ is a $n \times n$ Hermitian matrix stored in packed format, $\mathbf{x}$ is a vector, and $\alpha$ is a scalar.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the elements in the lower triangular part of the Hermitian matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+((2*n-j+1)*j)/2\]` for $j = 1,\ldots,n$ and $i \geq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the elements in the upper triangular part of the Hermitian matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+(j*(j+1))/2\]` for $j = 1,\ldots,n$ and $i \leq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [chpr()](http://www.netlib.org/blas/chpr.f), [zhpr()](http://www.netlib.org/blas/zhpr.f).
    pub fn cublasChpr_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: ::core::ffi::c_int,
        alpha: *const f32,
        x: *const cuComplex,
        incx: ::core::ffi::c_int,
        AP: *mut cuComplex,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the packed Hermitian rank-1 update:
    /// $$
    /// A = \alpha\textbf{x}\textbf{x}^{H} + A
    /// $$
    ///
    /// where $A$ is a $n \times n$ Hermitian matrix stored in packed format, $\mathbf{x}$ is a vector, and $\alpha$ is a scalar.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the elements in the lower triangular part of the Hermitian matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+((2*n-j+1)*j)/2\]` for $j = 1,\ldots,n$ and $i \geq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the elements in the upper triangular part of the Hermitian matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+(j*(j+1))/2\]` for $j = 1,\ldots,n$ and $i \leq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [chpr()](http://www.netlib.org/blas/chpr.f), [zhpr()](http://www.netlib.org/blas/zhpr.f).
    pub fn cublasChpr_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: i64,
        alpha: *const f32,
        x: *const cuComplex,
        incx: i64,
        AP: *mut cuComplex,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the packed Hermitian rank-1 update:
    /// $$
    /// A = \alpha\textbf{x}\textbf{x}^{H} + A
    /// $$
    ///
    /// where $A$ is a $n \times n$ Hermitian matrix stored in packed format, $\mathbf{x}$ is a vector, and $\alpha$ is a scalar.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the elements in the lower triangular part of the Hermitian matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+((2*n-j+1)*j)/2\]` for $j = 1,\ldots,n$ and $i \geq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the elements in the upper triangular part of the Hermitian matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+(j*(j+1))/2\]` for $j = 1,\ldots,n$ and $i \leq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [chpr()](http://www.netlib.org/blas/chpr.f), [zhpr()](http://www.netlib.org/blas/zhpr.f).
    pub fn cublasZhpr_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: ::core::ffi::c_int,
        alpha: *const f64,
        x: *const cuDoubleComplex,
        incx: ::core::ffi::c_int,
        AP: *mut cuDoubleComplex,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the packed Hermitian rank-1 update:
    /// $$
    /// A = \alpha\textbf{x}\textbf{x}^{H} + A
    /// $$
    ///
    /// where $A$ is a $n \times n$ Hermitian matrix stored in packed format, $\mathbf{x}$ is a vector, and $\alpha$ is a scalar.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the elements in the lower triangular part of the Hermitian matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+((2*n-j+1)*j)/2\]` for $j = 1,\ldots,n$ and $i \geq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the elements in the upper triangular part of the Hermitian matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+(j*(j+1))/2\]` for $j = 1,\ldots,n$ and $i \leq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [chpr()](http://www.netlib.org/blas/chpr.f), [zhpr()](http://www.netlib.org/blas/zhpr.f).
    pub fn cublasZhpr_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: i64,
        alpha: *const f64,
        x: *const cuDoubleComplex,
        incx: i64,
        AP: *mut cuDoubleComplex,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric rank-2 update:
    /// $$
    /// A = \alpha\left( {\textbf{x}\textbf{y}^{T} + \textbf{y}\textbf{x}^{T}} \right) + A
    /// $$
    ///
    /// where $A$ is a $n \times n$ symmetric matrix stored in column-major format, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ is a scalar.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssyr2()](http://www.netlib.org/lapack/explore-html/db/d99/ssyr2_8f_source.html), [dsyr2()](http://www.netlib.org/lapack/explore-html/de/d41/dsyr2_8f_source.html).
    pub fn cublasSsyr2_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: ::core::ffi::c_int,
        alpha: *const f32,
        x: *const f32,
        incx: ::core::ffi::c_int,
        y: *const f32,
        incy: ::core::ffi::c_int,
        A: *mut f32,
        lda: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric rank-2 update:
    /// $$
    /// A = \alpha\left( {\textbf{x}\textbf{y}^{T} + \textbf{y}\textbf{x}^{T}} \right) + A
    /// $$
    ///
    /// where $A$ is a $n \times n$ symmetric matrix stored in column-major format, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ is a scalar.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssyr2()](http://www.netlib.org/lapack/explore-html/db/d99/ssyr2_8f_source.html), [dsyr2()](http://www.netlib.org/lapack/explore-html/de/d41/dsyr2_8f_source.html).
    pub fn cublasSsyr2_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: i64,
        alpha: *const f32,
        x: *const f32,
        incx: i64,
        y: *const f32,
        incy: i64,
        A: *mut f32,
        lda: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric rank-2 update:
    /// $$
    /// A = \alpha\left( {\textbf{x}\textbf{y}^{T} + \textbf{y}\textbf{x}^{T}} \right) + A
    /// $$
    ///
    /// where $A$ is a $n \times n$ symmetric matrix stored in column-major format, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ is a scalar.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssyr2()](http://www.netlib.org/lapack/explore-html/db/d99/ssyr2_8f_source.html), [dsyr2()](http://www.netlib.org/lapack/explore-html/de/d41/dsyr2_8f_source.html).
    pub fn cublasDsyr2_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: ::core::ffi::c_int,
        alpha: *const f64,
        x: *const f64,
        incx: ::core::ffi::c_int,
        y: *const f64,
        incy: ::core::ffi::c_int,
        A: *mut f64,
        lda: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric rank-2 update:
    /// $$
    /// A = \alpha\left( {\textbf{x}\textbf{y}^{T} + \textbf{y}\textbf{x}^{T}} \right) + A
    /// $$
    ///
    /// where $A$ is a $n \times n$ symmetric matrix stored in column-major format, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ is a scalar.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssyr2()](http://www.netlib.org/lapack/explore-html/db/d99/ssyr2_8f_source.html), [dsyr2()](http://www.netlib.org/lapack/explore-html/de/d41/dsyr2_8f_source.html).
    pub fn cublasDsyr2_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: i64,
        alpha: *const f64,
        x: *const f64,
        incx: i64,
        y: *const f64,
        incy: i64,
        A: *mut f64,
        lda: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric rank-2 update:
    /// $$
    /// A = \alpha\left( {\textbf{x}\textbf{y}^{T} + \textbf{y}\textbf{x}^{T}} \right) + A
    /// $$
    ///
    /// where $A$ is a $n \times n$ symmetric matrix stored in column-major format, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ is a scalar.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssyr2()](http://www.netlib.org/lapack/explore-html/db/d99/ssyr2_8f_source.html), [dsyr2()](http://www.netlib.org/lapack/explore-html/de/d41/dsyr2_8f_source.html).
    pub fn cublasCsyr2_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: ::core::ffi::c_int,
        alpha: *const cuComplex,
        x: *const cuComplex,
        incx: ::core::ffi::c_int,
        y: *const cuComplex,
        incy: ::core::ffi::c_int,
        A: *mut cuComplex,
        lda: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric rank-2 update:
    /// $$
    /// A = \alpha\left( {\textbf{x}\textbf{y}^{T} + \textbf{y}\textbf{x}^{T}} \right) + A
    /// $$
    ///
    /// where $A$ is a $n \times n$ symmetric matrix stored in column-major format, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ is a scalar.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssyr2()](http://www.netlib.org/lapack/explore-html/db/d99/ssyr2_8f_source.html), [dsyr2()](http://www.netlib.org/lapack/explore-html/de/d41/dsyr2_8f_source.html).
    pub fn cublasCsyr2_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: i64,
        alpha: *const cuComplex,
        x: *const cuComplex,
        incx: i64,
        y: *const cuComplex,
        incy: i64,
        A: *mut cuComplex,
        lda: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric rank-2 update:
    /// $$
    /// A = \alpha\left( {\textbf{x}\textbf{y}^{T} + \textbf{y}\textbf{x}^{T}} \right) + A
    /// $$
    ///
    /// where $A$ is a $n \times n$ symmetric matrix stored in column-major format, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ is a scalar.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssyr2()](http://www.netlib.org/lapack/explore-html/db/d99/ssyr2_8f_source.html), [dsyr2()](http://www.netlib.org/lapack/explore-html/de/d41/dsyr2_8f_source.html).
    pub fn cublasZsyr2_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: ::core::ffi::c_int,
        alpha: *const cuDoubleComplex,
        x: *const cuDoubleComplex,
        incx: ::core::ffi::c_int,
        y: *const cuDoubleComplex,
        incy: ::core::ffi::c_int,
        A: *mut cuDoubleComplex,
        lda: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric rank-2 update:
    /// $$
    /// A = \alpha\left( {\textbf{x}\textbf{y}^{T} + \textbf{y}\textbf{x}^{T}} \right) + A
    /// $$
    ///
    /// where $A$ is a $n \times n$ symmetric matrix stored in column-major format, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ is a scalar.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssyr2()](http://www.netlib.org/lapack/explore-html/db/d99/ssyr2_8f_source.html), [dsyr2()](http://www.netlib.org/lapack/explore-html/de/d41/dsyr2_8f_source.html).
    pub fn cublasZsyr2_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: i64,
        alpha: *const cuDoubleComplex,
        x: *const cuDoubleComplex,
        incx: i64,
        y: *const cuDoubleComplex,
        incy: i64,
        A: *mut cuDoubleComplex,
        lda: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the Hermitian rank-2 update:
    /// $$
    /// A = \alpha\textbf{x}\textbf{y}^{H} + \overset{ˉ}{\alpha}\textbf{y}\textbf{x}^{H} + A
    /// $$
    ///
    /// where $A$ is a $n \times n$ Hermitian matrix stored in column-major format, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ is a scalar.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [cher2()](http://www.netlib.org/blas/cher2.f), [zher2()](http://www.netlib.org/blas/zher2.f).
    pub fn cublasCher2_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: ::core::ffi::c_int,
        alpha: *const cuComplex,
        x: *const cuComplex,
        incx: ::core::ffi::c_int,
        y: *const cuComplex,
        incy: ::core::ffi::c_int,
        A: *mut cuComplex,
        lda: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the Hermitian rank-2 update:
    /// $$
    /// A = \alpha\textbf{x}\textbf{y}^{H} + \overset{ˉ}{\alpha}\textbf{y}\textbf{x}^{H} + A
    /// $$
    ///
    /// where $A$ is a $n \times n$ Hermitian matrix stored in column-major format, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ is a scalar.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [cher2()](http://www.netlib.org/blas/cher2.f), [zher2()](http://www.netlib.org/blas/zher2.f).
    pub fn cublasCher2_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: i64,
        alpha: *const cuComplex,
        x: *const cuComplex,
        incx: i64,
        y: *const cuComplex,
        incy: i64,
        A: *mut cuComplex,
        lda: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the Hermitian rank-2 update:
    /// $$
    /// A = \alpha\textbf{x}\textbf{y}^{H} + \overset{ˉ}{\alpha}\textbf{y}\textbf{x}^{H} + A
    /// $$
    ///
    /// where $A$ is a $n \times n$ Hermitian matrix stored in column-major format, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ is a scalar.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [cher2()](http://www.netlib.org/blas/cher2.f), [zher2()](http://www.netlib.org/blas/zher2.f).
    pub fn cublasZher2_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: ::core::ffi::c_int,
        alpha: *const cuDoubleComplex,
        x: *const cuDoubleComplex,
        incx: ::core::ffi::c_int,
        y: *const cuDoubleComplex,
        incy: ::core::ffi::c_int,
        A: *mut cuDoubleComplex,
        lda: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the Hermitian rank-2 update:
    /// $$
    /// A = \alpha\textbf{x}\textbf{y}^{H} + \overset{ˉ}{\alpha}\textbf{y}\textbf{x}^{H} + A
    /// $$
    ///
    /// where $A$ is a $n \times n$ Hermitian matrix stored in column-major format, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ is a scalar.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [cher2()](http://www.netlib.org/blas/cher2.f), [zher2()](http://www.netlib.org/blas/zher2.f).
    pub fn cublasZher2_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: i64,
        alpha: *const cuDoubleComplex,
        x: *const cuDoubleComplex,
        incx: i64,
        y: *const cuDoubleComplex,
        incy: i64,
        A: *mut cuDoubleComplex,
        lda: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the packed symmetric rank-2 update:
    /// $$
    /// A = \alpha\left( {\textbf{x}\textbf{y}^{T} + \textbf{y}\textbf{x}^{T}} \right) + A
    /// $$
    ///
    /// where $A$ is a $n \times n$ symmetric matrix stored in packed format, $\mathbf{x}$ is a vector, and $\alpha$ is a scalar.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the elements in the lower triangular part of the symmetric matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+((2*n-j+1)*j)/2\]` for $j = 1,\ldots,n$ and $i \geq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the elements in the upper triangular part of the symmetric matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+(j*(j+1))/2\]` for $j = 1,\ldots,n$ and $i \leq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sspr2()](http://www.netlib.org/blas/sspr2.f), [dspr2()](http://www.netlib.org/blas/dspr2.f).
    pub fn cublasSspr2_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: ::core::ffi::c_int,
        alpha: *const f32,
        x: *const f32,
        incx: ::core::ffi::c_int,
        y: *const f32,
        incy: ::core::ffi::c_int,
        AP: *mut f32,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the packed symmetric rank-2 update:
    /// $$
    /// A = \alpha\left( {\textbf{x}\textbf{y}^{T} + \textbf{y}\textbf{x}^{T}} \right) + A
    /// $$
    ///
    /// where $A$ is a $n \times n$ symmetric matrix stored in packed format, $\mathbf{x}$ is a vector, and $\alpha$ is a scalar.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the elements in the lower triangular part of the symmetric matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+((2*n-j+1)*j)/2\]` for $j = 1,\ldots,n$ and $i \geq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the elements in the upper triangular part of the symmetric matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+(j*(j+1))/2\]` for $j = 1,\ldots,n$ and $i \leq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sspr2()](http://www.netlib.org/blas/sspr2.f), [dspr2()](http://www.netlib.org/blas/dspr2.f).
    pub fn cublasSspr2_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: i64,
        alpha: *const f32,
        x: *const f32,
        incx: i64,
        y: *const f32,
        incy: i64,
        AP: *mut f32,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the packed symmetric rank-2 update:
    /// $$
    /// A = \alpha\left( {\textbf{x}\textbf{y}^{T} + \textbf{y}\textbf{x}^{T}} \right) + A
    /// $$
    ///
    /// where $A$ is a $n \times n$ symmetric matrix stored in packed format, $\mathbf{x}$ is a vector, and $\alpha$ is a scalar.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the elements in the lower triangular part of the symmetric matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+((2*n-j+1)*j)/2\]` for $j = 1,\ldots,n$ and $i \geq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the elements in the upper triangular part of the symmetric matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+(j*(j+1))/2\]` for $j = 1,\ldots,n$ and $i \leq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sspr2()](http://www.netlib.org/blas/sspr2.f), [dspr2()](http://www.netlib.org/blas/dspr2.f).
    pub fn cublasDspr2_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: ::core::ffi::c_int,
        alpha: *const f64,
        x: *const f64,
        incx: ::core::ffi::c_int,
        y: *const f64,
        incy: ::core::ffi::c_int,
        AP: *mut f64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the packed symmetric rank-2 update:
    /// $$
    /// A = \alpha\left( {\textbf{x}\textbf{y}^{T} + \textbf{y}\textbf{x}^{T}} \right) + A
    /// $$
    ///
    /// where $A$ is a $n \times n$ symmetric matrix stored in packed format, $\mathbf{x}$ is a vector, and $\alpha$ is a scalar.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the elements in the lower triangular part of the symmetric matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+((2*n-j+1)*j)/2\]` for $j = 1,\ldots,n$ and $i \geq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the elements in the upper triangular part of the symmetric matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+(j*(j+1))/2\]` for $j = 1,\ldots,n$ and $i \leq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sspr2()](http://www.netlib.org/blas/sspr2.f), [dspr2()](http://www.netlib.org/blas/dspr2.f).
    pub fn cublasDspr2_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: i64,
        alpha: *const f64,
        x: *const f64,
        incx: i64,
        y: *const f64,
        incy: i64,
        AP: *mut f64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the packed Hermitian rank-2 update:
    /// $$
    /// A = \alpha\textbf{x}\textbf{y}^{H} + \overset{ˉ}{\alpha}\textbf{y}\textbf{x}^{H} + A
    /// $$
    ///
    /// where $A$ is a $n \times n$ Hermitian matrix stored in packed format, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ is a scalar.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the elements in the lower triangular part of the Hermitian matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+((2*n-j+1)*j)/2\]` for $j = 1,\ldots,n$ and $i \geq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the elements in the upper triangular part of the Hermitian matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+(j*(j+1))/2\]` for $j = 1,\ldots,n$ and $i \leq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// chpr2, zhpr2.
    pub fn cublasChpr2_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: ::core::ffi::c_int,
        alpha: *const cuComplex,
        x: *const cuComplex,
        incx: ::core::ffi::c_int,
        y: *const cuComplex,
        incy: ::core::ffi::c_int,
        AP: *mut cuComplex,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the packed Hermitian rank-2 update:
    /// $$
    /// A = \alpha\textbf{x}\textbf{y}^{H} + \overset{ˉ}{\alpha}\textbf{y}\textbf{x}^{H} + A
    /// $$
    ///
    /// where $A$ is a $n \times n$ Hermitian matrix stored in packed format, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ is a scalar.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the elements in the lower triangular part of the Hermitian matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+((2*n-j+1)*j)/2\]` for $j = 1,\ldots,n$ and $i \geq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the elements in the upper triangular part of the Hermitian matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+(j*(j+1))/2\]` for $j = 1,\ldots,n$ and $i \leq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// chpr2, zhpr2.
    pub fn cublasChpr2_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: i64,
        alpha: *const cuComplex,
        x: *const cuComplex,
        incx: i64,
        y: *const cuComplex,
        incy: i64,
        AP: *mut cuComplex,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the packed Hermitian rank-2 update:
    /// $$
    /// A = \alpha\textbf{x}\textbf{y}^{H} + \overset{ˉ}{\alpha}\textbf{y}\textbf{x}^{H} + A
    /// $$
    ///
    /// where $A$ is a $n \times n$ Hermitian matrix stored in packed format, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ is a scalar.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the elements in the lower triangular part of the Hermitian matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+((2*n-j+1)*j)/2\]` for $j = 1,\ldots,n$ and $i \geq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the elements in the upper triangular part of the Hermitian matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+(j*(j+1))/2\]` for $j = 1,\ldots,n$ and $i \leq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// chpr2, zhpr2.
    pub fn cublasZhpr2_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: ::core::ffi::c_int,
        alpha: *const cuDoubleComplex,
        x: *const cuDoubleComplex,
        incx: ::core::ffi::c_int,
        y: *const cuDoubleComplex,
        incy: ::core::ffi::c_int,
        AP: *mut cuDoubleComplex,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the packed Hermitian rank-2 update:
    /// $$
    /// A = \alpha\textbf{x}\textbf{y}^{H} + \overset{ˉ}{\alpha}\textbf{y}\textbf{x}^{H} + A
    /// $$
    ///
    /// where $A$ is a $n \times n$ Hermitian matrix stored in packed format, $\mathbf{x}$ and $\mathbf{y}$ are vectors, and $\alpha$ is a scalar.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the elements in the lower triangular part of the Hermitian matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+((2*n-j+1)*j)/2\]` for $j = 1,\ldots,n$ and $i \geq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// If `uplo == CUBLAS_FILL_MODE_UPPER` then the elements in the upper triangular part of the Hermitian matrix $A$ are packed together column by column without gaps, so that the element $A(i,j)$ is stored in the memory location `AP\[i+(j*(j+1))/2\]` for $j = 1,\ldots,n$ and $i \leq j$. Consequently, the packed format requires only $\frac{n(n + 1)}{2}$ elements for storage.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// chpr2, zhpr2.
    pub fn cublasZhpr2_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: i64,
        alpha: *const cuDoubleComplex,
        x: *const cuDoubleComplex,
        incx: i64,
        y: *const cuDoubleComplex,
        incy: i64,
        AP: *mut cuDoubleComplex,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-vector multiplication of a batch of matrices and vectors. The batch is considered to be “uniform”, i.e. all instances have the same dimensions (m, n), leading dimension (lda), increments (incx, incy) and transposition (trans) for their respective A matrix, x and y vectors. The address of the input matrix and vector, and the output vector of each instance of the batch are read from arrays of pointers passed to the function by the caller.
    /// $$
    /// \textbf{y}\lbrack i\rbrack = \alpha\text{op}(A\lbrack i\rbrack)\textbf{x}\lbrack i\rbrack + \beta\textbf{y}\lbrack i\rbrack,\text{ for i} \in \lbrack 0,batchCount - 1\rbrack
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$ is an array of pointers to matrice $A\lbrack i\rbrack$ stored in column-major format with dimension $m \times n$, and $\textbf{x}$ and $\textbf{y}$ are arrays of pointers to vectors. Also, for matrix $A\lbrack i\rbrack$,
    /// $$
    /// \text{op}(A\lbrack i\rbrack) = \left\\{ \begin{matrix}
    /// {A\lbrack i\rbrack} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A\lbrack i\rbrack}^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// {A\lbrack i\rbrack}^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// On certain problem sizes, it might be advantageous to make multiple calls to cublas&lt;t>gemv() in different CUDA streams, rather than use this API.
    ///
    /// If math mode enables fast math modes when using [`cublasSgemvBatched`], pointers (not the pointer arrays) placed in the GPU memory must be properly aligned to avoid misaligned memory access errors. Ideally all pointers are aligned to at least 16 Bytes. Otherwise it is recommended that they meet the following rule.
    pub fn cublasSgemvBatched(
        handle: cublasHandle_t,
        trans: cublasOperation_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        alpha: *const f32,
        Aarray: *const *const f32,
        lda: ::core::ffi::c_int,
        xarray: *const *const f32,
        incx: ::core::ffi::c_int,
        beta: *const f32,
        yarray: *const *mut f32,
        incy: ::core::ffi::c_int,
        batchCount: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-vector multiplication of a batch of matrices and vectors. The batch is considered to be “uniform”, i.e. all instances have the same dimensions (m, n), leading dimension (lda), increments (incx, incy) and transposition (trans) for their respective A matrix, x and y vectors. The address of the input matrix and vector, and the output vector of each instance of the batch are read from arrays of pointers passed to the function by the caller.
    /// $$
    /// \textbf{y}\lbrack i\rbrack = \alpha\text{op}(A\lbrack i\rbrack)\textbf{x}\lbrack i\rbrack + \beta\textbf{y}\lbrack i\rbrack,\text{ for i} \in \lbrack 0,batchCount - 1\rbrack
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$ is an array of pointers to matrice $A\lbrack i\rbrack$ stored in column-major format with dimension $m \times n$, and $\textbf{x}$ and $\textbf{y}$ are arrays of pointers to vectors. Also, for matrix $A\lbrack i\rbrack$,
    /// $$
    /// \text{op}(A\lbrack i\rbrack) = \left\\{ \begin{matrix}
    /// {A\lbrack i\rbrack} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A\lbrack i\rbrack}^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// {A\lbrack i\rbrack}^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// On certain problem sizes, it might be advantageous to make multiple calls to cublas&lt;t>gemv() in different CUDA streams, rather than use this API.
    ///
    /// If math mode enables fast math modes when using [`cublasSgemvBatched`], pointers (not the pointer arrays) placed in the GPU memory must be properly aligned to avoid misaligned memory access errors. Ideally all pointers are aligned to at least 16 Bytes. Otherwise it is recommended that they meet the following rule.
    pub fn cublasSgemvBatched_64(
        handle: cublasHandle_t,
        trans: cublasOperation_t,
        m: i64,
        n: i64,
        alpha: *const f32,
        Aarray: *const *const f32,
        lda: i64,
        xarray: *const *const f32,
        incx: i64,
        beta: *const f32,
        yarray: *const *mut f32,
        incy: i64,
        batchCount: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-vector multiplication of a batch of matrices and vectors. The batch is considered to be “uniform”, i.e. all instances have the same dimensions (m, n), leading dimension (lda), increments (incx, incy) and transposition (trans) for their respective A matrix, x and y vectors. The address of the input matrix and vector, and the output vector of each instance of the batch are read from arrays of pointers passed to the function by the caller.
    /// $$
    /// \textbf{y}\lbrack i\rbrack = \alpha\text{op}(A\lbrack i\rbrack)\textbf{x}\lbrack i\rbrack + \beta\textbf{y}\lbrack i\rbrack,\text{ for i} \in \lbrack 0,batchCount - 1\rbrack
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$ is an array of pointers to matrice $A\lbrack i\rbrack$ stored in column-major format with dimension $m \times n$, and $\textbf{x}$ and $\textbf{y}$ are arrays of pointers to vectors. Also, for matrix $A\lbrack i\rbrack$,
    /// $$
    /// \text{op}(A\lbrack i\rbrack) = \left\\{ \begin{matrix}
    /// {A\lbrack i\rbrack} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A\lbrack i\rbrack}^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// {A\lbrack i\rbrack}^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// On certain problem sizes, it might be advantageous to make multiple calls to cublas&lt;t>gemv() in different CUDA streams, rather than use this API.
    ///
    /// If math mode enables fast math modes when using [`cublasSgemvBatched`], pointers (not the pointer arrays) placed in the GPU memory must be properly aligned to avoid misaligned memory access errors. Ideally all pointers are aligned to at least 16 Bytes. Otherwise it is recommended that they meet the following rule.
    pub fn cublasDgemvBatched(
        handle: cublasHandle_t,
        trans: cublasOperation_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        alpha: *const f64,
        Aarray: *const *const f64,
        lda: ::core::ffi::c_int,
        xarray: *const *const f64,
        incx: ::core::ffi::c_int,
        beta: *const f64,
        yarray: *const *mut f64,
        incy: ::core::ffi::c_int,
        batchCount: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-vector multiplication of a batch of matrices and vectors. The batch is considered to be “uniform”, i.e. all instances have the same dimensions (m, n), leading dimension (lda), increments (incx, incy) and transposition (trans) for their respective A matrix, x and y vectors. The address of the input matrix and vector, and the output vector of each instance of the batch are read from arrays of pointers passed to the function by the caller.
    /// $$
    /// \textbf{y}\lbrack i\rbrack = \alpha\text{op}(A\lbrack i\rbrack)\textbf{x}\lbrack i\rbrack + \beta\textbf{y}\lbrack i\rbrack,\text{ for i} \in \lbrack 0,batchCount - 1\rbrack
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$ is an array of pointers to matrice $A\lbrack i\rbrack$ stored in column-major format with dimension $m \times n$, and $\textbf{x}$ and $\textbf{y}$ are arrays of pointers to vectors. Also, for matrix $A\lbrack i\rbrack$,
    /// $$
    /// \text{op}(A\lbrack i\rbrack) = \left\\{ \begin{matrix}
    /// {A\lbrack i\rbrack} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A\lbrack i\rbrack}^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// {A\lbrack i\rbrack}^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// On certain problem sizes, it might be advantageous to make multiple calls to cublas&lt;t>gemv() in different CUDA streams, rather than use this API.
    ///
    /// If math mode enables fast math modes when using [`cublasSgemvBatched`], pointers (not the pointer arrays) placed in the GPU memory must be properly aligned to avoid misaligned memory access errors. Ideally all pointers are aligned to at least 16 Bytes. Otherwise it is recommended that they meet the following rule.
    pub fn cublasDgemvBatched_64(
        handle: cublasHandle_t,
        trans: cublasOperation_t,
        m: i64,
        n: i64,
        alpha: *const f64,
        Aarray: *const *const f64,
        lda: i64,
        xarray: *const *const f64,
        incx: i64,
        beta: *const f64,
        yarray: *const *mut f64,
        incy: i64,
        batchCount: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-vector multiplication of a batch of matrices and vectors. The batch is considered to be “uniform”, i.e. all instances have the same dimensions (m, n), leading dimension (lda), increments (incx, incy) and transposition (trans) for their respective A matrix, x and y vectors. The address of the input matrix and vector, and the output vector of each instance of the batch are read from arrays of pointers passed to the function by the caller.
    /// $$
    /// \textbf{y}\lbrack i\rbrack = \alpha\text{op}(A\lbrack i\rbrack)\textbf{x}\lbrack i\rbrack + \beta\textbf{y}\lbrack i\rbrack,\text{ for i} \in \lbrack 0,batchCount - 1\rbrack
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$ is an array of pointers to matrice $A\lbrack i\rbrack$ stored in column-major format with dimension $m \times n$, and $\textbf{x}$ and $\textbf{y}$ are arrays of pointers to vectors. Also, for matrix $A\lbrack i\rbrack$,
    /// $$
    /// \text{op}(A\lbrack i\rbrack) = \left\\{ \begin{matrix}
    /// {A\lbrack i\rbrack} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A\lbrack i\rbrack}^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// {A\lbrack i\rbrack}^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// On certain problem sizes, it might be advantageous to make multiple calls to cublas&lt;t>gemv() in different CUDA streams, rather than use this API.
    ///
    /// If math mode enables fast math modes when using [`cublasSgemvBatched`], pointers (not the pointer arrays) placed in the GPU memory must be properly aligned to avoid misaligned memory access errors. Ideally all pointers are aligned to at least 16 Bytes. Otherwise it is recommended that they meet the following rule.
    pub fn cublasCgemvBatched(
        handle: cublasHandle_t,
        trans: cublasOperation_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        alpha: *const cuComplex,
        Aarray: *const *const cuComplex,
        lda: ::core::ffi::c_int,
        xarray: *const *const cuComplex,
        incx: ::core::ffi::c_int,
        beta: *const cuComplex,
        yarray: *const *mut cuComplex,
        incy: ::core::ffi::c_int,
        batchCount: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-vector multiplication of a batch of matrices and vectors. The batch is considered to be “uniform”, i.e. all instances have the same dimensions (m, n), leading dimension (lda), increments (incx, incy) and transposition (trans) for their respective A matrix, x and y vectors. The address of the input matrix and vector, and the output vector of each instance of the batch are read from arrays of pointers passed to the function by the caller.
    /// $$
    /// \textbf{y}\lbrack i\rbrack = \alpha\text{op}(A\lbrack i\rbrack)\textbf{x}\lbrack i\rbrack + \beta\textbf{y}\lbrack i\rbrack,\text{ for i} \in \lbrack 0,batchCount - 1\rbrack
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$ is an array of pointers to matrice $A\lbrack i\rbrack$ stored in column-major format with dimension $m \times n$, and $\textbf{x}$ and $\textbf{y}$ are arrays of pointers to vectors. Also, for matrix $A\lbrack i\rbrack$,
    /// $$
    /// \text{op}(A\lbrack i\rbrack) = \left\\{ \begin{matrix}
    /// {A\lbrack i\rbrack} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A\lbrack i\rbrack}^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// {A\lbrack i\rbrack}^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// On certain problem sizes, it might be advantageous to make multiple calls to cublas&lt;t>gemv() in different CUDA streams, rather than use this API.
    ///
    /// If math mode enables fast math modes when using [`cublasSgemvBatched`], pointers (not the pointer arrays) placed in the GPU memory must be properly aligned to avoid misaligned memory access errors. Ideally all pointers are aligned to at least 16 Bytes. Otherwise it is recommended that they meet the following rule.
    pub fn cublasCgemvBatched_64(
        handle: cublasHandle_t,
        trans: cublasOperation_t,
        m: i64,
        n: i64,
        alpha: *const cuComplex,
        Aarray: *const *const cuComplex,
        lda: i64,
        xarray: *const *const cuComplex,
        incx: i64,
        beta: *const cuComplex,
        yarray: *const *mut cuComplex,
        incy: i64,
        batchCount: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-vector multiplication of a batch of matrices and vectors. The batch is considered to be “uniform”, i.e. all instances have the same dimensions (m, n), leading dimension (lda), increments (incx, incy) and transposition (trans) for their respective A matrix, x and y vectors. The address of the input matrix and vector, and the output vector of each instance of the batch are read from arrays of pointers passed to the function by the caller.
    /// $$
    /// \textbf{y}\lbrack i\rbrack = \alpha\text{op}(A\lbrack i\rbrack)\textbf{x}\lbrack i\rbrack + \beta\textbf{y}\lbrack i\rbrack,\text{ for i} \in \lbrack 0,batchCount - 1\rbrack
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$ is an array of pointers to matrice $A\lbrack i\rbrack$ stored in column-major format with dimension $m \times n$, and $\textbf{x}$ and $\textbf{y}$ are arrays of pointers to vectors. Also, for matrix $A\lbrack i\rbrack$,
    /// $$
    /// \text{op}(A\lbrack i\rbrack) = \left\\{ \begin{matrix}
    /// {A\lbrack i\rbrack} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A\lbrack i\rbrack}^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// {A\lbrack i\rbrack}^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// On certain problem sizes, it might be advantageous to make multiple calls to cublas&lt;t>gemv() in different CUDA streams, rather than use this API.
    ///
    /// If math mode enables fast math modes when using [`cublasSgemvBatched`], pointers (not the pointer arrays) placed in the GPU memory must be properly aligned to avoid misaligned memory access errors. Ideally all pointers are aligned to at least 16 Bytes. Otherwise it is recommended that they meet the following rule.
    pub fn cublasZgemvBatched(
        handle: cublasHandle_t,
        trans: cublasOperation_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        alpha: *const cuDoubleComplex,
        Aarray: *const *const cuDoubleComplex,
        lda: ::core::ffi::c_int,
        xarray: *const *const cuDoubleComplex,
        incx: ::core::ffi::c_int,
        beta: *const cuDoubleComplex,
        yarray: *const *mut cuDoubleComplex,
        incy: ::core::ffi::c_int,
        batchCount: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-vector multiplication of a batch of matrices and vectors. The batch is considered to be “uniform”, i.e. all instances have the same dimensions (m, n), leading dimension (lda), increments (incx, incy) and transposition (trans) for their respective A matrix, x and y vectors. The address of the input matrix and vector, and the output vector of each instance of the batch are read from arrays of pointers passed to the function by the caller.
    /// $$
    /// \textbf{y}\lbrack i\rbrack = \alpha\text{op}(A\lbrack i\rbrack)\textbf{x}\lbrack i\rbrack + \beta\textbf{y}\lbrack i\rbrack,\text{ for i} \in \lbrack 0,batchCount - 1\rbrack
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$ is an array of pointers to matrice $A\lbrack i\rbrack$ stored in column-major format with dimension $m \times n$, and $\textbf{x}$ and $\textbf{y}$ are arrays of pointers to vectors. Also, for matrix $A\lbrack i\rbrack$,
    /// $$
    /// \text{op}(A\lbrack i\rbrack) = \left\\{ \begin{matrix}
    /// {A\lbrack i\rbrack} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A\lbrack i\rbrack}^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// {A\lbrack i\rbrack}^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// On certain problem sizes, it might be advantageous to make multiple calls to cublas&lt;t>gemv() in different CUDA streams, rather than use this API.
    ///
    /// If math mode enables fast math modes when using [`cublasSgemvBatched`], pointers (not the pointer arrays) placed in the GPU memory must be properly aligned to avoid misaligned memory access errors. Ideally all pointers are aligned to at least 16 Bytes. Otherwise it is recommended that they meet the following rule.
    pub fn cublasZgemvBatched_64(
        handle: cublasHandle_t,
        trans: cublasOperation_t,
        m: i64,
        n: i64,
        alpha: *const cuDoubleComplex,
        Aarray: *const *const cuDoubleComplex,
        lda: i64,
        xarray: *const *const cuDoubleComplex,
        incx: i64,
        beta: *const cuDoubleComplex,
        yarray: *const *mut cuDoubleComplex,
        incy: i64,
        batchCount: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-vector multiplication of a batch of matrices and vectors. The batch is considered to be “uniform”, i.e. all instances have the same dimensions (m, n), leading dimension (lda), increments (incx, incy) and transposition (trans) for their respective A matrix, x and y vectors. Input matrix A and vector x, and output vector y for each instance of the batch are located at fixed offsets in number of elements from their locations in the previous instance. Pointers to A matrix, x and y vectors for the first instance are passed to the function by the user along with offsets in number of elements - strideA, stridex and stridey that determine the locations of input matrices and vectors, and output vectors in future instances.
    /// $$
    /// \textbf{y} + i\\*{stridey} = \alpha\text{op}(A + i\\*{strideA})(\textbf{x} + i\\*{stridex}) + \beta(\textbf{y} + i\\*{stridey}),\text{ for i } \in \lbrack 0,batchCount - 1\rbrack
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$ is an array of pointers to matrix stored in column-major format with dimension $A\lbrack i\rbrack$ $m \times n$, and $\textbf{x}$ and $\textbf{y}$ are arrays of pointers to vectors. Also, for matrix $A\lbrack i\rbrack$:
    /// $$
    /// \text{op}(A\lbrack i\rbrack) = \left\\{ \begin{matrix}
    /// {A\lbrack i\rbrack} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A\lbrack i\rbrack}^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// {A\lbrack i\rbrack}^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// On certain problem sizes, it might be advantageous to make multiple calls to cublas&lt;t>gemv() in different CUDA streams, rather than use this API.
    pub fn cublasSgemvStridedBatched(
        handle: cublasHandle_t,
        trans: cublasOperation_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        alpha: *const f32,
        A: *const f32,
        lda: ::core::ffi::c_int,
        strideA: ::core::ffi::c_longlong,
        x: *const f32,
        incx: ::core::ffi::c_int,
        stridex: ::core::ffi::c_longlong,
        beta: *const f32,
        y: *mut f32,
        incy: ::core::ffi::c_int,
        stridey: ::core::ffi::c_longlong,
        batchCount: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-vector multiplication of a batch of matrices and vectors. The batch is considered to be “uniform”, i.e. all instances have the same dimensions (m, n), leading dimension (lda), increments (incx, incy) and transposition (trans) for their respective A matrix, x and y vectors. Input matrix A and vector x, and output vector y for each instance of the batch are located at fixed offsets in number of elements from their locations in the previous instance. Pointers to A matrix, x and y vectors for the first instance are passed to the function by the user along with offsets in number of elements - strideA, stridex and stridey that determine the locations of input matrices and vectors, and output vectors in future instances.
    /// $$
    /// \textbf{y} + i\\*{stridey} = \alpha\text{op}(A + i\\*{strideA})(\textbf{x} + i\\*{stridex}) + \beta(\textbf{y} + i\\*{stridey}),\text{ for i } \in \lbrack 0,batchCount - 1\rbrack
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$ is an array of pointers to matrix stored in column-major format with dimension $A\lbrack i\rbrack$ $m \times n$, and $\textbf{x}$ and $\textbf{y}$ are arrays of pointers to vectors. Also, for matrix $A\lbrack i\rbrack$:
    /// $$
    /// \text{op}(A\lbrack i\rbrack) = \left\\{ \begin{matrix}
    /// {A\lbrack i\rbrack} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A\lbrack i\rbrack}^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// {A\lbrack i\rbrack}^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// On certain problem sizes, it might be advantageous to make multiple calls to cublas&lt;t>gemv() in different CUDA streams, rather than use this API.
    pub fn cublasSgemvStridedBatched_64(
        handle: cublasHandle_t,
        trans: cublasOperation_t,
        m: i64,
        n: i64,
        alpha: *const f32,
        A: *const f32,
        lda: i64,
        strideA: ::core::ffi::c_longlong,
        x: *const f32,
        incx: i64,
        stridex: ::core::ffi::c_longlong,
        beta: *const f32,
        y: *mut f32,
        incy: i64,
        stridey: ::core::ffi::c_longlong,
        batchCount: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-vector multiplication of a batch of matrices and vectors. The batch is considered to be “uniform”, i.e. all instances have the same dimensions (m, n), leading dimension (lda), increments (incx, incy) and transposition (trans) for their respective A matrix, x and y vectors. Input matrix A and vector x, and output vector y for each instance of the batch are located at fixed offsets in number of elements from their locations in the previous instance. Pointers to A matrix, x and y vectors for the first instance are passed to the function by the user along with offsets in number of elements - strideA, stridex and stridey that determine the locations of input matrices and vectors, and output vectors in future instances.
    /// $$
    /// \textbf{y} + i\\*{stridey} = \alpha\text{op}(A + i\\*{strideA})(\textbf{x} + i\\*{stridex}) + \beta(\textbf{y} + i\\*{stridey}),\text{ for i } \in \lbrack 0,batchCount - 1\rbrack
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$ is an array of pointers to matrix stored in column-major format with dimension $A\lbrack i\rbrack$ $m \times n$, and $\textbf{x}$ and $\textbf{y}$ are arrays of pointers to vectors. Also, for matrix $A\lbrack i\rbrack$:
    /// $$
    /// \text{op}(A\lbrack i\rbrack) = \left\\{ \begin{matrix}
    /// {A\lbrack i\rbrack} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A\lbrack i\rbrack}^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// {A\lbrack i\rbrack}^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// On certain problem sizes, it might be advantageous to make multiple calls to cublas&lt;t>gemv() in different CUDA streams, rather than use this API.
    pub fn cublasDgemvStridedBatched(
        handle: cublasHandle_t,
        trans: cublasOperation_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        alpha: *const f64,
        A: *const f64,
        lda: ::core::ffi::c_int,
        strideA: ::core::ffi::c_longlong,
        x: *const f64,
        incx: ::core::ffi::c_int,
        stridex: ::core::ffi::c_longlong,
        beta: *const f64,
        y: *mut f64,
        incy: ::core::ffi::c_int,
        stridey: ::core::ffi::c_longlong,
        batchCount: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-vector multiplication of a batch of matrices and vectors. The batch is considered to be “uniform”, i.e. all instances have the same dimensions (m, n), leading dimension (lda), increments (incx, incy) and transposition (trans) for their respective A matrix, x and y vectors. Input matrix A and vector x, and output vector y for each instance of the batch are located at fixed offsets in number of elements from their locations in the previous instance. Pointers to A matrix, x and y vectors for the first instance are passed to the function by the user along with offsets in number of elements - strideA, stridex and stridey that determine the locations of input matrices and vectors, and output vectors in future instances.
    /// $$
    /// \textbf{y} + i\\*{stridey} = \alpha\text{op}(A + i\\*{strideA})(\textbf{x} + i\\*{stridex}) + \beta(\textbf{y} + i\\*{stridey}),\text{ for i } \in \lbrack 0,batchCount - 1\rbrack
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$ is an array of pointers to matrix stored in column-major format with dimension $A\lbrack i\rbrack$ $m \times n$, and $\textbf{x}$ and $\textbf{y}$ are arrays of pointers to vectors. Also, for matrix $A\lbrack i\rbrack$:
    /// $$
    /// \text{op}(A\lbrack i\rbrack) = \left\\{ \begin{matrix}
    /// {A\lbrack i\rbrack} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A\lbrack i\rbrack}^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// {A\lbrack i\rbrack}^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// On certain problem sizes, it might be advantageous to make multiple calls to cublas&lt;t>gemv() in different CUDA streams, rather than use this API.
    pub fn cublasDgemvStridedBatched_64(
        handle: cublasHandle_t,
        trans: cublasOperation_t,
        m: i64,
        n: i64,
        alpha: *const f64,
        A: *const f64,
        lda: i64,
        strideA: ::core::ffi::c_longlong,
        x: *const f64,
        incx: i64,
        stridex: ::core::ffi::c_longlong,
        beta: *const f64,
        y: *mut f64,
        incy: i64,
        stridey: ::core::ffi::c_longlong,
        batchCount: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-vector multiplication of a batch of matrices and vectors. The batch is considered to be “uniform”, i.e. all instances have the same dimensions (m, n), leading dimension (lda), increments (incx, incy) and transposition (trans) for their respective A matrix, x and y vectors. Input matrix A and vector x, and output vector y for each instance of the batch are located at fixed offsets in number of elements from their locations in the previous instance. Pointers to A matrix, x and y vectors for the first instance are passed to the function by the user along with offsets in number of elements - strideA, stridex and stridey that determine the locations of input matrices and vectors, and output vectors in future instances.
    /// $$
    /// \textbf{y} + i\\*{stridey} = \alpha\text{op}(A + i\\*{strideA})(\textbf{x} + i\\*{stridex}) + \beta(\textbf{y} + i\\*{stridey}),\text{ for i } \in \lbrack 0,batchCount - 1\rbrack
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$ is an array of pointers to matrix stored in column-major format with dimension $A\lbrack i\rbrack$ $m \times n$, and $\textbf{x}$ and $\textbf{y}$ are arrays of pointers to vectors. Also, for matrix $A\lbrack i\rbrack$:
    /// $$
    /// \text{op}(A\lbrack i\rbrack) = \left\\{ \begin{matrix}
    /// {A\lbrack i\rbrack} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A\lbrack i\rbrack}^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// {A\lbrack i\rbrack}^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// On certain problem sizes, it might be advantageous to make multiple calls to cublas&lt;t>gemv() in different CUDA streams, rather than use this API.
    pub fn cublasCgemvStridedBatched(
        handle: cublasHandle_t,
        trans: cublasOperation_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        alpha: *const cuComplex,
        A: *const cuComplex,
        lda: ::core::ffi::c_int,
        strideA: ::core::ffi::c_longlong,
        x: *const cuComplex,
        incx: ::core::ffi::c_int,
        stridex: ::core::ffi::c_longlong,
        beta: *const cuComplex,
        y: *mut cuComplex,
        incy: ::core::ffi::c_int,
        stridey: ::core::ffi::c_longlong,
        batchCount: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-vector multiplication of a batch of matrices and vectors. The batch is considered to be “uniform”, i.e. all instances have the same dimensions (m, n), leading dimension (lda), increments (incx, incy) and transposition (trans) for their respective A matrix, x and y vectors. Input matrix A and vector x, and output vector y for each instance of the batch are located at fixed offsets in number of elements from their locations in the previous instance. Pointers to A matrix, x and y vectors for the first instance are passed to the function by the user along with offsets in number of elements - strideA, stridex and stridey that determine the locations of input matrices and vectors, and output vectors in future instances.
    /// $$
    /// \textbf{y} + i\\*{stridey} = \alpha\text{op}(A + i\\*{strideA})(\textbf{x} + i\\*{stridex}) + \beta(\textbf{y} + i\\*{stridey}),\text{ for i } \in \lbrack 0,batchCount - 1\rbrack
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$ is an array of pointers to matrix stored in column-major format with dimension $A\lbrack i\rbrack$ $m \times n$, and $\textbf{x}$ and $\textbf{y}$ are arrays of pointers to vectors. Also, for matrix $A\lbrack i\rbrack$:
    /// $$
    /// \text{op}(A\lbrack i\rbrack) = \left\\{ \begin{matrix}
    /// {A\lbrack i\rbrack} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A\lbrack i\rbrack}^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// {A\lbrack i\rbrack}^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// On certain problem sizes, it might be advantageous to make multiple calls to cublas&lt;t>gemv() in different CUDA streams, rather than use this API.
    pub fn cublasCgemvStridedBatched_64(
        handle: cublasHandle_t,
        trans: cublasOperation_t,
        m: i64,
        n: i64,
        alpha: *const cuComplex,
        A: *const cuComplex,
        lda: i64,
        strideA: ::core::ffi::c_longlong,
        x: *const cuComplex,
        incx: i64,
        stridex: ::core::ffi::c_longlong,
        beta: *const cuComplex,
        y: *mut cuComplex,
        incy: i64,
        stridey: ::core::ffi::c_longlong,
        batchCount: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-vector multiplication of a batch of matrices and vectors. The batch is considered to be “uniform”, i.e. all instances have the same dimensions (m, n), leading dimension (lda), increments (incx, incy) and transposition (trans) for their respective A matrix, x and y vectors. Input matrix A and vector x, and output vector y for each instance of the batch are located at fixed offsets in number of elements from their locations in the previous instance. Pointers to A matrix, x and y vectors for the first instance are passed to the function by the user along with offsets in number of elements - strideA, stridex and stridey that determine the locations of input matrices and vectors, and output vectors in future instances.
    /// $$
    /// \textbf{y} + i\\*{stridey} = \alpha\text{op}(A + i\\*{strideA})(\textbf{x} + i\\*{stridex}) + \beta(\textbf{y} + i\\*{stridey}),\text{ for i } \in \lbrack 0,batchCount - 1\rbrack
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$ is an array of pointers to matrix stored in column-major format with dimension $A\lbrack i\rbrack$ $m \times n$, and $\textbf{x}$ and $\textbf{y}$ are arrays of pointers to vectors. Also, for matrix $A\lbrack i\rbrack$:
    /// $$
    /// \text{op}(A\lbrack i\rbrack) = \left\\{ \begin{matrix}
    /// {A\lbrack i\rbrack} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A\lbrack i\rbrack}^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// {A\lbrack i\rbrack}^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// On certain problem sizes, it might be advantageous to make multiple calls to cublas&lt;t>gemv() in different CUDA streams, rather than use this API.
    pub fn cublasZgemvStridedBatched(
        handle: cublasHandle_t,
        trans: cublasOperation_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        alpha: *const cuDoubleComplex,
        A: *const cuDoubleComplex,
        lda: ::core::ffi::c_int,
        strideA: ::core::ffi::c_longlong,
        x: *const cuDoubleComplex,
        incx: ::core::ffi::c_int,
        stridex: ::core::ffi::c_longlong,
        beta: *const cuDoubleComplex,
        y: *mut cuDoubleComplex,
        incy: ::core::ffi::c_int,
        stridey: ::core::ffi::c_longlong,
        batchCount: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-vector multiplication of a batch of matrices and vectors. The batch is considered to be “uniform”, i.e. all instances have the same dimensions (m, n), leading dimension (lda), increments (incx, incy) and transposition (trans) for their respective A matrix, x and y vectors. Input matrix A and vector x, and output vector y for each instance of the batch are located at fixed offsets in number of elements from their locations in the previous instance. Pointers to A matrix, x and y vectors for the first instance are passed to the function by the user along with offsets in number of elements - strideA, stridex and stridey that determine the locations of input matrices and vectors, and output vectors in future instances.
    /// $$
    /// \textbf{y} + i\\*{stridey} = \alpha\text{op}(A + i\\*{strideA})(\textbf{x} + i\\*{stridex}) + \beta(\textbf{y} + i\\*{stridey}),\text{ for i } \in \lbrack 0,batchCount - 1\rbrack
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$ is an array of pointers to matrix stored in column-major format with dimension $A\lbrack i\rbrack$ $m \times n$, and $\textbf{x}$ and $\textbf{y}$ are arrays of pointers to vectors. Also, for matrix $A\lbrack i\rbrack$:
    /// $$
    /// \text{op}(A\lbrack i\rbrack) = \left\\{ \begin{matrix}
    /// {A\lbrack i\rbrack} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A\lbrack i\rbrack}^{T} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// {A\lbrack i\rbrack}^{H} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// On certain problem sizes, it might be advantageous to make multiple calls to cublas&lt;t>gemv() in different CUDA streams, rather than use this API.
    pub fn cublasZgemvStridedBatched_64(
        handle: cublasHandle_t,
        trans: cublasOperation_t,
        m: i64,
        n: i64,
        alpha: *const cuDoubleComplex,
        A: *const cuDoubleComplex,
        lda: i64,
        strideA: ::core::ffi::c_longlong,
        x: *const cuDoubleComplex,
        incx: i64,
        stridex: ::core::ffi::c_longlong,
        beta: *const cuDoubleComplex,
        y: *mut cuDoubleComplex,
        incy: i64,
        stridey: ::core::ffi::c_longlong,
        batchCount: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-matrix multiplication:
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(B) + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$, $B$ and $C$ are matrices stored in column-major format with dimensions $\text{op}(A)$ $m \times k$, $\text{op}(B)$ $k \times n$ and $C$ $m \times n$, respectively. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B)$ is defined similarly for matrix $B$.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sgemm()](http://www.netlib.org/blas/sgemm.f), [dgemm()](http://www.netlib.org/blas/dgemm.f), [cgemm()](http://www.netlib.org/blas/cgemm.f), [zgemm()](http://www.netlib.org/blas/zgemm.f).
    pub fn cublasSgemm_v2(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        alpha: *const f32,
        A: *const f32,
        lda: ::core::ffi::c_int,
        B: *const f32,
        ldb: ::core::ffi::c_int,
        beta: *const f32,
        C: *mut f32,
        ldc: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-matrix multiplication:
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(B) + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$, $B$ and $C$ are matrices stored in column-major format with dimensions $\text{op}(A)$ $m \times k$, $\text{op}(B)$ $k \times n$ and $C$ $m \times n$, respectively. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B)$ is defined similarly for matrix $B$.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sgemm()](http://www.netlib.org/blas/sgemm.f), [dgemm()](http://www.netlib.org/blas/dgemm.f), [cgemm()](http://www.netlib.org/blas/cgemm.f), [zgemm()](http://www.netlib.org/blas/zgemm.f).
    pub fn cublasSgemm_v2_64(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: i64,
        n: i64,
        k: i64,
        alpha: *const f32,
        A: *const f32,
        lda: i64,
        B: *const f32,
        ldb: i64,
        beta: *const f32,
        C: *mut f32,
        ldc: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-matrix multiplication:
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(B) + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$, $B$ and $C$ are matrices stored in column-major format with dimensions $\text{op}(A)$ $m \times k$, $\text{op}(B)$ $k \times n$ and $C$ $m \times n$, respectively. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B)$ is defined similarly for matrix $B$.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sgemm()](http://www.netlib.org/blas/sgemm.f), [dgemm()](http://www.netlib.org/blas/dgemm.f), [cgemm()](http://www.netlib.org/blas/cgemm.f), [zgemm()](http://www.netlib.org/blas/zgemm.f).
    pub fn cublasDgemm_v2(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        alpha: *const f64,
        A: *const f64,
        lda: ::core::ffi::c_int,
        B: *const f64,
        ldb: ::core::ffi::c_int,
        beta: *const f64,
        C: *mut f64,
        ldc: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-matrix multiplication:
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(B) + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$, $B$ and $C$ are matrices stored in column-major format with dimensions $\text{op}(A)$ $m \times k$, $\text{op}(B)$ $k \times n$ and $C$ $m \times n$, respectively. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B)$ is defined similarly for matrix $B$.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sgemm()](http://www.netlib.org/blas/sgemm.f), [dgemm()](http://www.netlib.org/blas/dgemm.f), [cgemm()](http://www.netlib.org/blas/cgemm.f), [zgemm()](http://www.netlib.org/blas/zgemm.f).
    pub fn cublasDgemm_v2_64(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: i64,
        n: i64,
        k: i64,
        alpha: *const f64,
        A: *const f64,
        lda: i64,
        B: *const f64,
        ldb: i64,
        beta: *const f64,
        C: *mut f64,
        ldc: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-matrix multiplication:
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(B) + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$, $B$ and $C$ are matrices stored in column-major format with dimensions $\text{op}(A)$ $m \times k$, $\text{op}(B)$ $k \times n$ and $C$ $m \times n$, respectively. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B)$ is defined similarly for matrix $B$.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sgemm()](http://www.netlib.org/blas/sgemm.f), [dgemm()](http://www.netlib.org/blas/dgemm.f), [cgemm()](http://www.netlib.org/blas/cgemm.f), [zgemm()](http://www.netlib.org/blas/zgemm.f).
    pub fn cublasCgemm_v2(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        alpha: *const cuComplex,
        A: *const cuComplex,
        lda: ::core::ffi::c_int,
        B: *const cuComplex,
        ldb: ::core::ffi::c_int,
        beta: *const cuComplex,
        C: *mut cuComplex,
        ldc: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-matrix multiplication:
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(B) + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$, $B$ and $C$ are matrices stored in column-major format with dimensions $\text{op}(A)$ $m \times k$, $\text{op}(B)$ $k \times n$ and $C$ $m \times n$, respectively. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B)$ is defined similarly for matrix $B$.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sgemm()](http://www.netlib.org/blas/sgemm.f), [dgemm()](http://www.netlib.org/blas/dgemm.f), [cgemm()](http://www.netlib.org/blas/cgemm.f), [zgemm()](http://www.netlib.org/blas/zgemm.f).
    pub fn cublasCgemm_v2_64(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: i64,
        n: i64,
        k: i64,
        alpha: *const cuComplex,
        A: *const cuComplex,
        lda: i64,
        B: *const cuComplex,
        ldb: i64,
        beta: *const cuComplex,
        C: *mut cuComplex,
        ldc: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the complex matrix-matrix multiplication, using Gauss complexity reduction algorithm. This can lead to an increase in performance up to 25%:
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(B) + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$, $B$ and $C$ are matrices stored in column-major format with dimensions $\text{op}(A)$ $m \times k$, $\text{op}(B)$ $k \times n$ and $C$ $m \times n$, respectively. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B)$ is defined similarly for matrix $B$.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [cgemm()](http://www.netlib.org/blas/cgemm.f), [zgemm()](http://www.netlib.org/blas/zgemm.f).
    pub fn cublasCgemm3m(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        alpha: *const cuComplex,
        A: *const cuComplex,
        lda: ::core::ffi::c_int,
        B: *const cuComplex,
        ldb: ::core::ffi::c_int,
        beta: *const cuComplex,
        C: *mut cuComplex,
        ldc: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the complex matrix-matrix multiplication, using Gauss complexity reduction algorithm. This can lead to an increase in performance up to 25%:
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(B) + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$, $B$ and $C$ are matrices stored in column-major format with dimensions $\text{op}(A)$ $m \times k$, $\text{op}(B)$ $k \times n$ and $C$ $m \times n$, respectively. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B)$ is defined similarly for matrix $B$.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [cgemm()](http://www.netlib.org/blas/cgemm.f), [zgemm()](http://www.netlib.org/blas/zgemm.f).
    pub fn cublasCgemm3m_64(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: i64,
        n: i64,
        k: i64,
        alpha: *const cuComplex,
        A: *const cuComplex,
        lda: i64,
        B: *const cuComplex,
        ldb: i64,
        beta: *const cuComplex,
        C: *mut cuComplex,
        ldc: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    pub fn cublasCgemm3mEx(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        alpha: *const cuComplex,
        A: *const ::core::ffi::c_void,
        Atype: cudaDataType,
        lda: ::core::ffi::c_int,
        B: *const ::core::ffi::c_void,
        Btype: cudaDataType,
        ldb: ::core::ffi::c_int,
        beta: *const cuComplex,
        C: *mut ::core::ffi::c_void,
        Ctype: cudaDataType,
        ldc: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    pub fn cublasCgemm3mEx_64(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: i64,
        n: i64,
        k: i64,
        alpha: *const cuComplex,
        A: *const ::core::ffi::c_void,
        Atype: cudaDataType,
        lda: i64,
        B: *const ::core::ffi::c_void,
        Btype: cudaDataType,
        ldb: i64,
        beta: *const cuComplex,
        C: *mut ::core::ffi::c_void,
        Ctype: cudaDataType,
        ldc: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-matrix multiplication:
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(B) + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$, $B$ and $C$ are matrices stored in column-major format with dimensions $\text{op}(A)$ $m \times k$, $\text{op}(B)$ $k \times n$ and $C$ $m \times n$, respectively. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B)$ is defined similarly for matrix $B$.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sgemm()](http://www.netlib.org/blas/sgemm.f), [dgemm()](http://www.netlib.org/blas/dgemm.f), [cgemm()](http://www.netlib.org/blas/cgemm.f), [zgemm()](http://www.netlib.org/blas/zgemm.f).
    pub fn cublasZgemm_v2(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        alpha: *const cuDoubleComplex,
        A: *const cuDoubleComplex,
        lda: ::core::ffi::c_int,
        B: *const cuDoubleComplex,
        ldb: ::core::ffi::c_int,
        beta: *const cuDoubleComplex,
        C: *mut cuDoubleComplex,
        ldc: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-matrix multiplication:
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(B) + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$, $B$ and $C$ are matrices stored in column-major format with dimensions $\text{op}(A)$ $m \times k$, $\text{op}(B)$ $k \times n$ and $C$ $m \times n$, respectively. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B)$ is defined similarly for matrix $B$.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sgemm()](http://www.netlib.org/blas/sgemm.f), [dgemm()](http://www.netlib.org/blas/dgemm.f), [cgemm()](http://www.netlib.org/blas/cgemm.f), [zgemm()](http://www.netlib.org/blas/zgemm.f).
    pub fn cublasZgemm_v2_64(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: i64,
        n: i64,
        k: i64,
        alpha: *const cuDoubleComplex,
        A: *const cuDoubleComplex,
        lda: i64,
        B: *const cuDoubleComplex,
        ldb: i64,
        beta: *const cuDoubleComplex,
        C: *mut cuDoubleComplex,
        ldc: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the complex matrix-matrix multiplication, using Gauss complexity reduction algorithm. This can lead to an increase in performance up to 25%:
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(B) + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$, $B$ and $C$ are matrices stored in column-major format with dimensions $\text{op}(A)$ $m \times k$, $\text{op}(B)$ $k \times n$ and $C$ $m \times n$, respectively. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B)$ is defined similarly for matrix $B$.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [cgemm()](http://www.netlib.org/blas/cgemm.f), [zgemm()](http://www.netlib.org/blas/zgemm.f).
    pub fn cublasZgemm3m(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        alpha: *const cuDoubleComplex,
        A: *const cuDoubleComplex,
        lda: ::core::ffi::c_int,
        B: *const cuDoubleComplex,
        ldb: ::core::ffi::c_int,
        beta: *const cuDoubleComplex,
        C: *mut cuDoubleComplex,
        ldc: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the complex matrix-matrix multiplication, using Gauss complexity reduction algorithm. This can lead to an increase in performance up to 25%:
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(B) + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$, $B$ and $C$ are matrices stored in column-major format with dimensions $\text{op}(A)$ $m \times k$, $\text{op}(B)$ $k \times n$ and $C$ $m \times n$, respectively. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B)$ is defined similarly for matrix $B$.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [cgemm()](http://www.netlib.org/blas/cgemm.f), [zgemm()](http://www.netlib.org/blas/zgemm.f).
    pub fn cublasZgemm3m_64(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: i64,
        n: i64,
        k: i64,
        alpha: *const cuDoubleComplex,
        A: *const cuDoubleComplex,
        lda: i64,
        B: *const cuDoubleComplex,
        ldb: i64,
        beta: *const cuDoubleComplex,
        C: *mut cuDoubleComplex,
        ldc: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function is an extension of cublas&lt;t>gemm(). In this function the input matrices and output matrices can have a lower precision but the computation is still done in the type `&lt;t>`. For example, in the type `float` for [`cublasSgemmEx`] and in the type [`cuComplex`] for [`cublasCgemmEx`].
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(B) + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$, $B$ and $C$ are matrices stored in column-major format with dimensions $\text{op}(A)$ $m \times k$, $\text{op}(B)$ $k \times n$ and $C$ $m \times n$, respectively. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B)$ is defined similarly for matrix $B$.
    ///
    /// The matrix types combinations supported for [`cublasSgemmEx`] are listed below:
    ///
    /// The matrix types combinations supported for [`cublasCgemmEx`] are listed below :
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sgemm()](http://www.netlib.org/blas/sgemm.f)
    ///
    /// For more information about the numerical behavior of some GEMM algorithms, refer to the GEMM Algorithms Numerical Behavior section.
    pub fn cublasSgemmEx(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        alpha: *const f32,
        A: *const ::core::ffi::c_void,
        Atype: cudaDataType,
        lda: ::core::ffi::c_int,
        B: *const ::core::ffi::c_void,
        Btype: cudaDataType,
        ldb: ::core::ffi::c_int,
        beta: *const f32,
        C: *mut ::core::ffi::c_void,
        Ctype: cudaDataType,
        ldc: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function is an extension of cublas&lt;t>gemm(). In this function the input matrices and output matrices can have a lower precision but the computation is still done in the type `&lt;t>`. For example, in the type `float` for [`cublasSgemmEx`] and in the type [`cuComplex`] for [`cublasCgemmEx`].
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(B) + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$, $B$ and $C$ are matrices stored in column-major format with dimensions $\text{op}(A)$ $m \times k$, $\text{op}(B)$ $k \times n$ and $C$ $m \times n$, respectively. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B)$ is defined similarly for matrix $B$.
    ///
    /// The matrix types combinations supported for [`cublasSgemmEx`] are listed below:
    ///
    /// The matrix types combinations supported for [`cublasCgemmEx`] are listed below :
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sgemm()](http://www.netlib.org/blas/sgemm.f)
    ///
    /// For more information about the numerical behavior of some GEMM algorithms, refer to the GEMM Algorithms Numerical Behavior section.
    pub fn cublasSgemmEx_64(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: i64,
        n: i64,
        k: i64,
        alpha: *const f32,
        A: *const ::core::ffi::c_void,
        Atype: cudaDataType,
        lda: i64,
        B: *const ::core::ffi::c_void,
        Btype: cudaDataType,
        ldb: i64,
        beta: *const f32,
        C: *mut ::core::ffi::c_void,
        Ctype: cudaDataType,
        ldc: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function is an extension of cublas&lt;t>gemm() that allows the user to individually specify the data types for each of the A, B and C matrices, the precision of computation and the GEMM algorithm to be run. Supported combinations of arguments are listed further down in this section.
    ///
    /// This function is only supported on devices with compute capability 5.0 or later.
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(B) + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$, $B$ and $C$ are matrices stored in column-major format with dimensions $\text{op}(A)$ $m \times k$, $\text{op}(B)$ $k \times n$ and $C$ $m \times n$, respectively. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B)$ is defined similarly for matrix $B$.
    ///
    /// [`cublasGemmEx`] supports the following Compute Type, Scale Type, Atype/Btype, and Ctype:
    ///
    /// Starting with release 11.2, using the typed functions instead of the extension functions (cublas\*\*Ex()) helps in reducing the binary size when linking to static cuBLAS Library.
    ///
    /// Also refer to: [sgemm.()](http://www.netlib.org/blas/sgemm.f)
    ///
    /// For more information about the numerical behavior of some GEMM algorithms, refer to the GEMM Algorithms Numerical Behavior section.
    ///
    /// # Parameters
    ///
    /// - `handle`: Handle to the cuBLAS library context.
    /// - `transa`: Operation op(`A`) that is non- or (conj.) transpose.
    /// - `transb`: Operation op(`B`) that is non- or (conj.) transpose.
    /// - `m`: Number of rows of matrix op(`A`) and `C`.
    /// - `n`: Number of columns of matrix op(`B`) and `C`.
    /// - `k`: Number of columns of op(`A`) and rows of op(`B`).
    /// - `alpha`: Scaling factor for A\*B of the type that corresponds to the computeType and Ctype, see the table below for details.
    /// - `A`: &lt;`type`> array of dimensions `lda x k` with `lda >= max(1, m)` if `transa == CUBLAS_OP_N` and `lda x m` with `lda >= max(1, k)` otherwise.
    /// - `Atype`: Enumerant specifying the datatype of matrix `A`.
    /// - `lda`: Leading dimension of two-dimensional array used to store the matrix `A`.
    /// - `B`: &lt;`type`> array of dimension `ldb x n` with `ldb >= max(1, k)` if `transb == CUBLAS_OP_N` and `ldb x k` with `ldb >= max(1,n)` otherwise.
    /// - `Btype`: Enumerant specifying the datatype of matrix `B`.
    /// - `ldb`: Leading dimension of two-dimensional array used to store matrix `B`.
    /// - `beta`: Scaling factor for C of the type that corresponds to the computeType and Ctype, see the table below for details. If `beta == 0`, `C` does not have to be a valid input.
    /// - `C`: &lt;`type`> array of dimensions `ldc x n` with `ldc >= max(1, m)`.
    /// - `Ctype`: Enumerant specifying the datatype of matrix `C`.
    /// - `ldc`: Leading dimension of a two-dimensional array used to store the matrix `C`.
    /// - `computeType`: Enumerant specifying the computation type.
    /// - `algo`: Enumerant specifying the algorithm. See [`cublasGemmAlgo_t`].
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_ARCH_MISMATCH`]: [`cublasGemmEx`] is only supported for GPU with architecture capabilities equal or greater than 5.0.
    /// - [`cublasStatus_t::CUBLAS_STATUS_EXECUTION_FAILED`]: The function failed to launch on the GPU.
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: If `m &lt; 0` or `n &lt; 0` or `k &lt; 0`, or if `transa` and `transb` are not one of [`cublasOperation_t::CUBLAS_OP_N`], [`cublasOperation_t::CUBLAS_OP_C`], [`cublasOperation_t::CUBLAS_OP_T`], or if `lda &lt; max(1, m)` when `transa == CUBLAS_OP_N` and `lda &lt; max(1, k)` otherwise, or if `ldb &lt; max(1, k)` when `transb == CUBLAS_OP_N` and `ldb &lt; max(1, n)` otherwise, or if `ldc &lt; max(1, m)`, or if `alpha` or `beta` are NULL, or if `C` is NULL when `beta` is not zero, or if `Atype` or `Btype` or `Ctype` or `algo` are not supported.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_INITIALIZED`]: The library was not initialized.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_SUPPORTED`]: The combination of the parameters `Atype`, `Btype` and `Ctype` or the algorithm, `algo` is not supported.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The operation completed successfully.
    pub fn cublasGemmEx(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        alpha: *const ::core::ffi::c_void,
        A: *const ::core::ffi::c_void,
        Atype: cudaDataType,
        lda: ::core::ffi::c_int,
        B: *const ::core::ffi::c_void,
        Btype: cudaDataType,
        ldb: ::core::ffi::c_int,
        beta: *const ::core::ffi::c_void,
        C: *mut ::core::ffi::c_void,
        Ctype: cudaDataType,
        ldc: ::core::ffi::c_int,
        computeType: cublasComputeType_t,
        algo: cublasGemmAlgo_t,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function is an extension of cublas&lt;t>gemm() that allows the user to individually specify the data types for each of the A, B and C matrices, the precision of computation and the GEMM algorithm to be run. Supported combinations of arguments are listed further down in this section.
    ///
    /// This function is only supported on devices with compute capability 5.0 or later.
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(B) + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$, $B$ and $C$ are matrices stored in column-major format with dimensions $\text{op}(A)$ $m \times k$, $\text{op}(B)$ $k \times n$ and $C$ $m \times n$, respectively. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B)$ is defined similarly for matrix $B$.
    ///
    /// [`cublasGemmEx`] supports the following Compute Type, Scale Type, Atype/Btype, and Ctype:
    ///
    /// Starting with release 11.2, using the typed functions instead of the extension functions (cublas\*\*Ex()) helps in reducing the binary size when linking to static cuBLAS Library.
    ///
    /// Also refer to: [sgemm.()](http://www.netlib.org/blas/sgemm.f)
    ///
    /// For more information about the numerical behavior of some GEMM algorithms, refer to the GEMM Algorithms Numerical Behavior section.
    ///
    /// # Parameters
    ///
    /// - `handle`: Handle to the cuBLAS library context.
    /// - `transa`: Operation op(`A`) that is non- or (conj.) transpose.
    /// - `transb`: Operation op(`B`) that is non- or (conj.) transpose.
    /// - `m`: Number of rows of matrix op(`A`) and `C`.
    /// - `n`: Number of columns of matrix op(`B`) and `C`.
    /// - `k`: Number of columns of op(`A`) and rows of op(`B`).
    /// - `alpha`: Scaling factor for A\*B of the type that corresponds to the computeType and Ctype, see the table below for details.
    /// - `A`: &lt;`type`> array of dimensions `lda x k` with `lda >= max(1, m)` if `transa == CUBLAS_OP_N` and `lda x m` with `lda >= max(1, k)` otherwise.
    /// - `Atype`: Enumerant specifying the datatype of matrix `A`.
    /// - `lda`: Leading dimension of two-dimensional array used to store the matrix `A`.
    /// - `B`: &lt;`type`> array of dimension `ldb x n` with `ldb >= max(1, k)` if `transb == CUBLAS_OP_N` and `ldb x k` with `ldb >= max(1,n)` otherwise.
    /// - `Btype`: Enumerant specifying the datatype of matrix `B`.
    /// - `ldb`: Leading dimension of two-dimensional array used to store matrix `B`.
    /// - `beta`: Scaling factor for C of the type that corresponds to the computeType and Ctype, see the table below for details. If `beta == 0`, `C` does not have to be a valid input.
    /// - `C`: &lt;`type`> array of dimensions `ldc x n` with `ldc >= max(1, m)`.
    /// - `Ctype`: Enumerant specifying the datatype of matrix `C`.
    /// - `ldc`: Leading dimension of a two-dimensional array used to store the matrix `C`.
    /// - `computeType`: Enumerant specifying the computation type.
    /// - `algo`: Enumerant specifying the algorithm. See [`cublasGemmAlgo_t`].
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_ARCH_MISMATCH`]: [`cublasGemmEx`] is only supported for GPU with architecture capabilities equal or greater than 5.0.
    /// - [`cublasStatus_t::CUBLAS_STATUS_EXECUTION_FAILED`]: The function failed to launch on the GPU.
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: If `m &lt; 0` or `n &lt; 0` or `k &lt; 0`, or if `transa` and `transb` are not one of [`cublasOperation_t::CUBLAS_OP_N`], [`cublasOperation_t::CUBLAS_OP_C`], [`cublasOperation_t::CUBLAS_OP_T`], or if `lda &lt; max(1, m)` when `transa == CUBLAS_OP_N` and `lda &lt; max(1, k)` otherwise, or if `ldb &lt; max(1, k)` when `transb == CUBLAS_OP_N` and `ldb &lt; max(1, n)` otherwise, or if `ldc &lt; max(1, m)`, or if `alpha` or `beta` are NULL, or if `C` is NULL when `beta` is not zero, or if `Atype` or `Btype` or `Ctype` or `algo` are not supported.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_INITIALIZED`]: The library was not initialized.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_SUPPORTED`]: The combination of the parameters `Atype`, `Btype` and `Ctype` or the algorithm, `algo` is not supported.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The operation completed successfully.
    pub fn cublasGemmEx_64(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: i64,
        n: i64,
        k: i64,
        alpha: *const ::core::ffi::c_void,
        A: *const ::core::ffi::c_void,
        Atype: cudaDataType,
        lda: i64,
        B: *const ::core::ffi::c_void,
        Btype: cudaDataType,
        ldb: i64,
        beta: *const ::core::ffi::c_void,
        C: *mut ::core::ffi::c_void,
        Ctype: cudaDataType,
        ldc: i64,
        computeType: cublasComputeType_t,
        algo: cublasGemmAlgo_t,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function is an extension of cublas&lt;t>gemm(). In this function the input matrices and output matrices can have a lower precision but the computation is still done in the type `&lt;t>`. For example, in the type `float` for [`cublasSgemmEx`] and in the type [`cuComplex`] for [`cublasCgemmEx`].
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(B) + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$, $B$ and $C$ are matrices stored in column-major format with dimensions $\text{op}(A)$ $m \times k$, $\text{op}(B)$ $k \times n$ and $C$ $m \times n$, respectively. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B)$ is defined similarly for matrix $B$.
    ///
    /// The matrix types combinations supported for [`cublasSgemmEx`] are listed below:
    ///
    /// The matrix types combinations supported for [`cublasCgemmEx`] are listed below :
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sgemm()](http://www.netlib.org/blas/sgemm.f)
    ///
    /// For more information about the numerical behavior of some GEMM algorithms, refer to the GEMM Algorithms Numerical Behavior section.
    pub fn cublasCgemmEx(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        alpha: *const cuComplex,
        A: *const ::core::ffi::c_void,
        Atype: cudaDataType,
        lda: ::core::ffi::c_int,
        B: *const ::core::ffi::c_void,
        Btype: cudaDataType,
        ldb: ::core::ffi::c_int,
        beta: *const cuComplex,
        C: *mut ::core::ffi::c_void,
        Ctype: cudaDataType,
        ldc: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function is an extension of cublas&lt;t>gemm(). In this function the input matrices and output matrices can have a lower precision but the computation is still done in the type `&lt;t>`. For example, in the type `float` for [`cublasSgemmEx`] and in the type [`cuComplex`] for [`cublasCgemmEx`].
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(B) + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$, $B$ and $C$ are matrices stored in column-major format with dimensions $\text{op}(A)$ $m \times k$, $\text{op}(B)$ $k \times n$ and $C$ $m \times n$, respectively. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B)$ is defined similarly for matrix $B$.
    ///
    /// The matrix types combinations supported for [`cublasSgemmEx`] are listed below:
    ///
    /// The matrix types combinations supported for [`cublasCgemmEx`] are listed below :
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sgemm()](http://www.netlib.org/blas/sgemm.f)
    ///
    /// For more information about the numerical behavior of some GEMM algorithms, refer to the GEMM Algorithms Numerical Behavior section.
    pub fn cublasCgemmEx_64(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: i64,
        n: i64,
        k: i64,
        alpha: *const cuComplex,
        A: *const ::core::ffi::c_void,
        Atype: cudaDataType,
        lda: i64,
        B: *const ::core::ffi::c_void,
        Btype: cudaDataType,
        ldb: i64,
        beta: *const cuComplex,
        C: *mut ::core::ffi::c_void,
        Ctype: cudaDataType,
        ldc: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric rank- $k$ update:
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(A)^{T} + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, $C$ is a symmetric matrix stored in lower or upper mode, and $A$ is a matrix with dimensions $\text{op}(A)$ $n \times k$. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssyrk()](http://www.netlib.org/blas/ssyrk.f), [dsyrk()](http://www.netlib.org/blas/dsyrk.f), [csyrk()](http://www.netlib.org/blas/csyrk.f), [zsyrk()](http://www.netlib.org/blas/zsyrk.f).
    pub fn cublasSsyrk_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        alpha: *const f32,
        A: *const f32,
        lda: ::core::ffi::c_int,
        beta: *const f32,
        C: *mut f32,
        ldc: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric rank- $k$ update:
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(A)^{T} + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, $C$ is a symmetric matrix stored in lower or upper mode, and $A$ is a matrix with dimensions $\text{op}(A)$ $n \times k$. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssyrk()](http://www.netlib.org/blas/ssyrk.f), [dsyrk()](http://www.netlib.org/blas/dsyrk.f), [csyrk()](http://www.netlib.org/blas/csyrk.f), [zsyrk()](http://www.netlib.org/blas/zsyrk.f).
    pub fn cublasSsyrk_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        n: i64,
        k: i64,
        alpha: *const f32,
        A: *const f32,
        lda: i64,
        beta: *const f32,
        C: *mut f32,
        ldc: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric rank- $k$ update:
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(A)^{T} + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, $C$ is a symmetric matrix stored in lower or upper mode, and $A$ is a matrix with dimensions $\text{op}(A)$ $n \times k$. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssyrk()](http://www.netlib.org/blas/ssyrk.f), [dsyrk()](http://www.netlib.org/blas/dsyrk.f), [csyrk()](http://www.netlib.org/blas/csyrk.f), [zsyrk()](http://www.netlib.org/blas/zsyrk.f).
    pub fn cublasDsyrk_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        alpha: *const f64,
        A: *const f64,
        lda: ::core::ffi::c_int,
        beta: *const f64,
        C: *mut f64,
        ldc: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric rank- $k$ update:
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(A)^{T} + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, $C$ is a symmetric matrix stored in lower or upper mode, and $A$ is a matrix with dimensions $\text{op}(A)$ $n \times k$. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssyrk()](http://www.netlib.org/blas/ssyrk.f), [dsyrk()](http://www.netlib.org/blas/dsyrk.f), [csyrk()](http://www.netlib.org/blas/csyrk.f), [zsyrk()](http://www.netlib.org/blas/zsyrk.f).
    pub fn cublasDsyrk_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        n: i64,
        k: i64,
        alpha: *const f64,
        A: *const f64,
        lda: i64,
        beta: *const f64,
        C: *mut f64,
        ldc: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric rank- $k$ update:
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(A)^{T} + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, $C$ is a symmetric matrix stored in lower or upper mode, and $A$ is a matrix with dimensions $\text{op}(A)$ $n \times k$. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssyrk()](http://www.netlib.org/blas/ssyrk.f), [dsyrk()](http://www.netlib.org/blas/dsyrk.f), [csyrk()](http://www.netlib.org/blas/csyrk.f), [zsyrk()](http://www.netlib.org/blas/zsyrk.f).
    pub fn cublasCsyrk_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        alpha: *const cuComplex,
        A: *const cuComplex,
        lda: ::core::ffi::c_int,
        beta: *const cuComplex,
        C: *mut cuComplex,
        ldc: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric rank- $k$ update:
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(A)^{T} + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, $C$ is a symmetric matrix stored in lower or upper mode, and $A$ is a matrix with dimensions $\text{op}(A)$ $n \times k$. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssyrk()](http://www.netlib.org/blas/ssyrk.f), [dsyrk()](http://www.netlib.org/blas/dsyrk.f), [csyrk()](http://www.netlib.org/blas/csyrk.f), [zsyrk()](http://www.netlib.org/blas/zsyrk.f).
    pub fn cublasCsyrk_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        n: i64,
        k: i64,
        alpha: *const cuComplex,
        A: *const cuComplex,
        lda: i64,
        beta: *const cuComplex,
        C: *mut cuComplex,
        ldc: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric rank- $k$ update:
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(A)^{T} + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, $C$ is a symmetric matrix stored in lower or upper mode, and $A$ is a matrix with dimensions $\text{op}(A)$ $n \times k$. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssyrk()](http://www.netlib.org/blas/ssyrk.f), [dsyrk()](http://www.netlib.org/blas/dsyrk.f), [csyrk()](http://www.netlib.org/blas/csyrk.f), [zsyrk()](http://www.netlib.org/blas/zsyrk.f).
    pub fn cublasZsyrk_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        alpha: *const cuDoubleComplex,
        A: *const cuDoubleComplex,
        lda: ::core::ffi::c_int,
        beta: *const cuDoubleComplex,
        C: *mut cuDoubleComplex,
        ldc: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric rank- $k$ update:
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(A)^{T} + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, $C$ is a symmetric matrix stored in lower or upper mode, and $A$ is a matrix with dimensions $\text{op}(A)$ $n \times k$. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssyrk()](http://www.netlib.org/blas/ssyrk.f), [dsyrk()](http://www.netlib.org/blas/dsyrk.f), [csyrk()](http://www.netlib.org/blas/csyrk.f), [zsyrk()](http://www.netlib.org/blas/zsyrk.f).
    pub fn cublasZsyrk_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        n: i64,
        k: i64,
        alpha: *const cuDoubleComplex,
        A: *const cuDoubleComplex,
        lda: i64,
        beta: *const cuDoubleComplex,
        C: *mut cuDoubleComplex,
        ldc: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function is an extension of [`cublasCsyrk_v2`] where the input matrix and output matrix can have a lower precision but the computation is still done in the type [`cuComplex`]
    ///
    /// This function performs the symmetric rank- $k$ update:
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(A)^{T} + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, $C$ is a symmetric matrix stored in lower or upper mode, and $A$ is a matrix with dimensions $\text{op}(A)$ $n \times k$. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// The matrix types combinations supported for [`cublasCsyrkEx`] are listed below:
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssyrk()](http://www.netlib.org/blas/ssyrk.f), [dsyrk()](http://www.netlib.org/blas/dsyrk.f), [csyrk()](http://www.netlib.org/blas/csyrk.f), [zsyrk()](http://www.netlib.org/blas/zsyrk.f).
    ///
    /// # Parameters
    ///
    /// - `handle`: Handle to the cuBLAS library context.
    /// - `uplo`: Indicates if matrix `C` lower or upper part is stored, the other symmetric part is not referenced and is inferred from the stored elements.
    /// - `trans`: Operation op(`A`) that is non- or transpose.
    /// - `n`: Number of rows of matrix op(`A`) and `C`.
    /// - `k`: Number of columns of matrix op(`A`).
    /// - `alpha`: &lt;`type`> scalar used for multiplication.
    /// - `A`: &lt;`type`> array of dimension `lda x k` with `lda >= max(1, n)` if `trans == CUBLAS_OP_N` and `lda x n` with `lda >= max(1, k)` otherwise.
    /// - `Atype`: Enumerant specifying the datatype of matrix `A`.
    /// - `lda`: Leading dimension of two-dimensional array used to store matrix A.
    /// - `beta`: &lt;`type`> scalar used for multiplication. If `beta == 0` then `C` does not have to be a valid input.
    /// - `C`: &lt;`type`> array of dimension `ldc x n`, with `ldc >= max(1, n)`.
    /// - `Ctype`: Enumerant specifying the datatype of matrix `C`.
    /// - `ldc`: Leading dimension of two-dimensional array used to store matrix `C`.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_ARCH_MISMATCH`]: The device has a compute capability lower than 5.0.
    /// - [`cublasStatus_t::CUBLAS_STATUS_EXECUTION_FAILED`]: The function failed to launch on the GPU.
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: If `n &lt; 0` or `k &lt; 0`, or if `uplo` is not one of [`cublasFillMode_t::CUBLAS_FILL_MODE_LOWER`] and [`cublasFillMode_t::CUBLAS_FILL_MODE_UPPER`], or if `trans` is not one of [`cublasOperation_t::CUBLAS_OP_N`], [`cublasOperation_t::CUBLAS_OP_T`] and [`cublasOperation_t::CUBLAS_OP_C`], or if `lda &lt; max(1, n)` if `trans == CUBLAS_OP_N` and `lda &lt; max(1, k)` otherwise, or if `ldc &lt; max(1, n)`, or if `Atype` or `Ctype` are not supported.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_INITIALIZED`]: The library was not initialized.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_SUPPORTED`]: The combination of the parameters `Atype` and `Ctype` is not supported.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The operation completed successfully.
    pub fn cublasCsyrkEx(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        alpha: *const cuComplex,
        A: *const ::core::ffi::c_void,
        Atype: cudaDataType,
        lda: ::core::ffi::c_int,
        beta: *const cuComplex,
        C: *mut ::core::ffi::c_void,
        Ctype: cudaDataType,
        ldc: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function is an extension of [`cublasCsyrk_v2`] where the input matrix and output matrix can have a lower precision but the computation is still done in the type [`cuComplex`]
    ///
    /// This function performs the symmetric rank- $k$ update:
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(A)^{T} + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, $C$ is a symmetric matrix stored in lower or upper mode, and $A$ is a matrix with dimensions $\text{op}(A)$ $n \times k$. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// The matrix types combinations supported for [`cublasCsyrkEx`] are listed below:
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssyrk()](http://www.netlib.org/blas/ssyrk.f), [dsyrk()](http://www.netlib.org/blas/dsyrk.f), [csyrk()](http://www.netlib.org/blas/csyrk.f), [zsyrk()](http://www.netlib.org/blas/zsyrk.f).
    ///
    /// # Parameters
    ///
    /// - `handle`: Handle to the cuBLAS library context.
    /// - `uplo`: Indicates if matrix `C` lower or upper part is stored, the other symmetric part is not referenced and is inferred from the stored elements.
    /// - `trans`: Operation op(`A`) that is non- or transpose.
    /// - `n`: Number of rows of matrix op(`A`) and `C`.
    /// - `k`: Number of columns of matrix op(`A`).
    /// - `alpha`: &lt;`type`> scalar used for multiplication.
    /// - `A`: &lt;`type`> array of dimension `lda x k` with `lda >= max(1, n)` if `trans == CUBLAS_OP_N` and `lda x n` with `lda >= max(1, k)` otherwise.
    /// - `Atype`: Enumerant specifying the datatype of matrix `A`.
    /// - `lda`: Leading dimension of two-dimensional array used to store matrix A.
    /// - `beta`: &lt;`type`> scalar used for multiplication. If `beta == 0` then `C` does not have to be a valid input.
    /// - `C`: &lt;`type`> array of dimension `ldc x n`, with `ldc >= max(1, n)`.
    /// - `Ctype`: Enumerant specifying the datatype of matrix `C`.
    /// - `ldc`: Leading dimension of two-dimensional array used to store matrix `C`.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_ARCH_MISMATCH`]: The device has a compute capability lower than 5.0.
    /// - [`cublasStatus_t::CUBLAS_STATUS_EXECUTION_FAILED`]: The function failed to launch on the GPU.
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: If `n &lt; 0` or `k &lt; 0`, or if `uplo` is not one of [`cublasFillMode_t::CUBLAS_FILL_MODE_LOWER`] and [`cublasFillMode_t::CUBLAS_FILL_MODE_UPPER`], or if `trans` is not one of [`cublasOperation_t::CUBLAS_OP_N`], [`cublasOperation_t::CUBLAS_OP_T`] and [`cublasOperation_t::CUBLAS_OP_C`], or if `lda &lt; max(1, n)` if `trans == CUBLAS_OP_N` and `lda &lt; max(1, k)` otherwise, or if `ldc &lt; max(1, n)`, or if `Atype` or `Ctype` are not supported.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_INITIALIZED`]: The library was not initialized.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_SUPPORTED`]: The combination of the parameters `Atype` and `Ctype` is not supported.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The operation completed successfully.
    pub fn cublasCsyrkEx_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        n: i64,
        k: i64,
        alpha: *const cuComplex,
        A: *const ::core::ffi::c_void,
        Atype: cudaDataType,
        lda: i64,
        beta: *const cuComplex,
        C: *mut ::core::ffi::c_void,
        Ctype: cudaDataType,
        ldc: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function is an extension of [`cublasCsyrk_v2`] where the input matrix and output matrix can have a lower precision but the computation is still done in the type [`cuComplex`]. This routine is implemented using the Gauss complexity reduction algorithm which can lead to an increase in performance up to 25%
    ///
    /// This function performs the symmetric rank- $k$ update:
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(A)^{T} + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, $C$ is a symmetric matrix stored in lower or upper mode, and $A$ is a matrix with dimensions $\text{op}(A)$ $n \times k$. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// The matrix types combinations supported for [`cublasCsyrk3mEx`] are listed below :
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssyrk()](http://www.netlib.org/blas/ssyrk.f), [dsyrk()](http://www.netlib.org/blas/dsyrk.f), [csyrk()](http://www.netlib.org/blas/csyrk.f), [zsyrk()](http://www.netlib.org/blas/zsyrk.f).
    ///
    /// # Parameters
    ///
    /// - `handle`: Handle to the cuBLAS library context.
    /// - `uplo`: Indicates if matrix `C` lower or upper part is stored, the other symmetric part is not referenced and is inferred from the stored elements.
    /// - `trans`: Operation op(`A`) that is non- or transpose.
    /// - `n`: Number of rows of matrix op(`A`) and `C`.
    /// - `k`: Number of columns of matrix op(`A`).
    /// - `alpha`: &lt;`type`> scalar used for multiplication.
    /// - `A`: &lt;`type`> array of dimension `lda x k` with `lda >= max(1, n)` if `trans == CUBLAS_OP_N` and `lda x n` with `lda >= max(1, k)` otherwise.
    /// - `Atype`: Enumerant specifying the datatype of matrix `A`.
    /// - `lda`: Leading dimension of two-dimensional array used to store matrix A.
    /// - `beta`: &lt;`type`> scalar used for multiplication. If `beta == 0` then `C` does not have to be a valid input.
    /// - `C`: &lt;`type`> array of dimension `ldc x n`, with `ldc >= max(1, n)`.
    /// - `Ctype`: Enumerant specifying the datatype of matrix `C`.
    /// - `ldc`: Leading dimension of two-dimensional array used to store matrix `C`.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_ARCH_MISMATCH`]: The device has a compute capability lower than 5.0.
    /// - [`cublasStatus_t::CUBLAS_STATUS_EXECUTION_FAILED`]: The function failed to launch on the GPU.
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: If `n &lt; 0` or `k &lt; 0`, or if `uplo` is not one of [`cublasFillMode_t::CUBLAS_FILL_MODE_LOWER`] and [`cublasFillMode_t::CUBLAS_FILL_MODE_UPPER`], or if `trans` is not one of [`cublasOperation_t::CUBLAS_OP_N`], [`cublasOperation_t::CUBLAS_OP_T`] and [`cublasOperation_t::CUBLAS_OP_C`], or if `lda &lt; max(1, n)` if `trans == CUBLAS_OP_N` and `lda &lt; max(1, k)` otherwise, or if `ldc &lt; max(1, n)`, or if `Atype` or `Ctype` are not supported.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_INITIALIZED`]: The library was not initialized.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_SUPPORTED`]: The combination of the parameters `Atype` and `Ctype` is not supported.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The operation completed successfully.
    pub fn cublasCsyrk3mEx(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        alpha: *const cuComplex,
        A: *const ::core::ffi::c_void,
        Atype: cudaDataType,
        lda: ::core::ffi::c_int,
        beta: *const cuComplex,
        C: *mut ::core::ffi::c_void,
        Ctype: cudaDataType,
        ldc: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function is an extension of [`cublasCsyrk_v2`] where the input matrix and output matrix can have a lower precision but the computation is still done in the type [`cuComplex`]. This routine is implemented using the Gauss complexity reduction algorithm which can lead to an increase in performance up to 25%
    ///
    /// This function performs the symmetric rank- $k$ update:
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(A)^{T} + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, $C$ is a symmetric matrix stored in lower or upper mode, and $A$ is a matrix with dimensions $\text{op}(A)$ $n \times k$. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// The matrix types combinations supported for [`cublasCsyrk3mEx`] are listed below :
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssyrk()](http://www.netlib.org/blas/ssyrk.f), [dsyrk()](http://www.netlib.org/blas/dsyrk.f), [csyrk()](http://www.netlib.org/blas/csyrk.f), [zsyrk()](http://www.netlib.org/blas/zsyrk.f).
    ///
    /// # Parameters
    ///
    /// - `handle`: Handle to the cuBLAS library context.
    /// - `uplo`: Indicates if matrix `C` lower or upper part is stored, the other symmetric part is not referenced and is inferred from the stored elements.
    /// - `trans`: Operation op(`A`) that is non- or transpose.
    /// - `n`: Number of rows of matrix op(`A`) and `C`.
    /// - `k`: Number of columns of matrix op(`A`).
    /// - `alpha`: &lt;`type`> scalar used for multiplication.
    /// - `A`: &lt;`type`> array of dimension `lda x k` with `lda >= max(1, n)` if `trans == CUBLAS_OP_N` and `lda x n` with `lda >= max(1, k)` otherwise.
    /// - `Atype`: Enumerant specifying the datatype of matrix `A`.
    /// - `lda`: Leading dimension of two-dimensional array used to store matrix A.
    /// - `beta`: &lt;`type`> scalar used for multiplication. If `beta == 0` then `C` does not have to be a valid input.
    /// - `C`: &lt;`type`> array of dimension `ldc x n`, with `ldc >= max(1, n)`.
    /// - `Ctype`: Enumerant specifying the datatype of matrix `C`.
    /// - `ldc`: Leading dimension of two-dimensional array used to store matrix `C`.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_ARCH_MISMATCH`]: The device has a compute capability lower than 5.0.
    /// - [`cublasStatus_t::CUBLAS_STATUS_EXECUTION_FAILED`]: The function failed to launch on the GPU.
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: If `n &lt; 0` or `k &lt; 0`, or if `uplo` is not one of [`cublasFillMode_t::CUBLAS_FILL_MODE_LOWER`] and [`cublasFillMode_t::CUBLAS_FILL_MODE_UPPER`], or if `trans` is not one of [`cublasOperation_t::CUBLAS_OP_N`], [`cublasOperation_t::CUBLAS_OP_T`] and [`cublasOperation_t::CUBLAS_OP_C`], or if `lda &lt; max(1, n)` if `trans == CUBLAS_OP_N` and `lda &lt; max(1, k)` otherwise, or if `ldc &lt; max(1, n)`, or if `Atype` or `Ctype` are not supported.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_INITIALIZED`]: The library was not initialized.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_SUPPORTED`]: The combination of the parameters `Atype` and `Ctype` is not supported.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The operation completed successfully.
    pub fn cublasCsyrk3mEx_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        n: i64,
        k: i64,
        alpha: *const cuComplex,
        A: *const ::core::ffi::c_void,
        Atype: cudaDataType,
        lda: i64,
        beta: *const cuComplex,
        C: *mut ::core::ffi::c_void,
        Ctype: cudaDataType,
        ldc: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the Hermitian rank- $k$ update:
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(A)^{H} + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, $C$ is a Hermitian matrix stored in lower or upper mode, and $A$ is a matrix with dimensions $\text{op}(A)$ $n \times k$. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [cherk()](http://www.netlib.org/blas/cherk.f), [zherk()](http://www.netlib.org/blas/zherk.f).
    pub fn cublasCherk_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        alpha: *const f32,
        A: *const cuComplex,
        lda: ::core::ffi::c_int,
        beta: *const f32,
        C: *mut cuComplex,
        ldc: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the Hermitian rank- $k$ update:
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(A)^{H} + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, $C$ is a Hermitian matrix stored in lower or upper mode, and $A$ is a matrix with dimensions $\text{op}(A)$ $n \times k$. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [cherk()](http://www.netlib.org/blas/cherk.f), [zherk()](http://www.netlib.org/blas/zherk.f).
    pub fn cublasCherk_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        n: i64,
        k: i64,
        alpha: *const f32,
        A: *const cuComplex,
        lda: i64,
        beta: *const f32,
        C: *mut cuComplex,
        ldc: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the Hermitian rank- $k$ update:
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(A)^{H} + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, $C$ is a Hermitian matrix stored in lower or upper mode, and $A$ is a matrix with dimensions $\text{op}(A)$ $n \times k$. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [cherk()](http://www.netlib.org/blas/cherk.f), [zherk()](http://www.netlib.org/blas/zherk.f).
    pub fn cublasZherk_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        alpha: *const f64,
        A: *const cuDoubleComplex,
        lda: ::core::ffi::c_int,
        beta: *const f64,
        C: *mut cuDoubleComplex,
        ldc: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the Hermitian rank- $k$ update:
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(A)^{H} + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, $C$ is a Hermitian matrix stored in lower or upper mode, and $A$ is a matrix with dimensions $\text{op}(A)$ $n \times k$. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [cherk()](http://www.netlib.org/blas/cherk.f), [zherk()](http://www.netlib.org/blas/zherk.f).
    pub fn cublasZherk_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        n: i64,
        k: i64,
        alpha: *const f64,
        A: *const cuDoubleComplex,
        lda: i64,
        beta: *const f64,
        C: *mut cuDoubleComplex,
        ldc: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function is an extension of [`cublasCherk_v2`] where the input matrix and output matrix can have a lower precision but the computation is still done in the type [`cuComplex`]
    ///
    /// This function performs the Hermitian rank- $k$ update:
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(A)^{H} + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, $C$ is a Hermitian matrix stored in lower or upper mode, and $A$ is a matrix with dimensions $\text{op}(A)$ $n \times k$. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// The matrix types combinations supported for [`cublasCherkEx`] are listed in the following table:
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [cherk()](http://www.netlib.org/blas/cherk.f).
    ///
    /// # Parameters
    ///
    /// - `handle`: Handle to the cuBLAS library context.
    /// - `uplo`: Indicates if matrix `C` lower or upper part is stored, the other Hermitian part is not referenced.
    /// - `trans`: Operation op(`A`) that is non- or (conj.) transpose.
    /// - `n`: Number of rows of matrix op(`A`) and `C`.
    /// - `k`: Number of columns of matrix op(`A`).
    /// - `alpha`: &lt;`type`> scalar used for multiplication.
    /// - `A`: &lt;`type`> array of dimension `lda x k` with `lda >= max(1, n)` if `transa == CUBLAS_OP_N` and `lda x n` with `lda >= max(1, k)` otherwise.
    /// - `Atype`: Enumerant specifying the datatype of matrix `A`.
    /// - `lda`: Leading dimension of two-dimensional array used to store matrix `A`.
    /// - `beta`: &lt;`type`> scalar used for multiplication. If `beta == 0` then `C` does not have to be a valid input.
    /// - `C`: &lt;`type`> array of dimension `ldc x n`, with `ldc >= max(1, n)`. The imaginary parts of the diagonal elements are assumed and set to zero.
    /// - `Ctype`: Enumerant specifying the datatype of matrix `C`.
    /// - `ldc`: Leading dimension of two-dimensional array used to store matrix `C`.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_ARCH_MISMATCH`]: The device has a compute capability lower than 5.0.
    /// - [`cublasStatus_t::CUBLAS_STATUS_EXECUTION_FAILED`]: The function failed to launch on the GPU.
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: If `n &lt; 0` or `k &lt; 0`, or if `uplo` is not one of [`cublasFillMode_t::CUBLAS_FILL_MODE_LOWER`] and [`cublasFillMode_t::CUBLAS_FILL_MODE_UPPER`], or if `trans` is not one of [`cublasOperation_t::CUBLAS_OP_N`], [`cublasOperation_t::CUBLAS_OP_T`] and [`cublasOperation_t::CUBLAS_OP_C`], or if `lda &lt; max(1, n)` if `trans == CUBLAS_OP_N` and `lda &lt; max(1, k)` otherwise, or if `ldc &lt; max(1, n)`, or if `Atype` or `Ctype` are not supported.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_INITIALIZED`]: The library was not initialized.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_SUPPORTED`]: The combination of the parameters `Atype` and `Ctype` is not supported.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The operation completed successfully.
    pub fn cublasCherkEx(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        alpha: *const f32,
        A: *const ::core::ffi::c_void,
        Atype: cudaDataType,
        lda: ::core::ffi::c_int,
        beta: *const f32,
        C: *mut ::core::ffi::c_void,
        Ctype: cudaDataType,
        ldc: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function is an extension of [`cublasCherk_v2`] where the input matrix and output matrix can have a lower precision but the computation is still done in the type [`cuComplex`]
    ///
    /// This function performs the Hermitian rank- $k$ update:
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(A)^{H} + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, $C$ is a Hermitian matrix stored in lower or upper mode, and $A$ is a matrix with dimensions $\text{op}(A)$ $n \times k$. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// The matrix types combinations supported for [`cublasCherkEx`] are listed in the following table:
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [cherk()](http://www.netlib.org/blas/cherk.f).
    ///
    /// # Parameters
    ///
    /// - `handle`: Handle to the cuBLAS library context.
    /// - `uplo`: Indicates if matrix `C` lower or upper part is stored, the other Hermitian part is not referenced.
    /// - `trans`: Operation op(`A`) that is non- or (conj.) transpose.
    /// - `n`: Number of rows of matrix op(`A`) and `C`.
    /// - `k`: Number of columns of matrix op(`A`).
    /// - `alpha`: &lt;`type`> scalar used for multiplication.
    /// - `A`: &lt;`type`> array of dimension `lda x k` with `lda >= max(1, n)` if `transa == CUBLAS_OP_N` and `lda x n` with `lda >= max(1, k)` otherwise.
    /// - `Atype`: Enumerant specifying the datatype of matrix `A`.
    /// - `lda`: Leading dimension of two-dimensional array used to store matrix `A`.
    /// - `beta`: &lt;`type`> scalar used for multiplication. If `beta == 0` then `C` does not have to be a valid input.
    /// - `C`: &lt;`type`> array of dimension `ldc x n`, with `ldc >= max(1, n)`. The imaginary parts of the diagonal elements are assumed and set to zero.
    /// - `Ctype`: Enumerant specifying the datatype of matrix `C`.
    /// - `ldc`: Leading dimension of two-dimensional array used to store matrix `C`.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_ARCH_MISMATCH`]: The device has a compute capability lower than 5.0.
    /// - [`cublasStatus_t::CUBLAS_STATUS_EXECUTION_FAILED`]: The function failed to launch on the GPU.
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: If `n &lt; 0` or `k &lt; 0`, or if `uplo` is not one of [`cublasFillMode_t::CUBLAS_FILL_MODE_LOWER`] and [`cublasFillMode_t::CUBLAS_FILL_MODE_UPPER`], or if `trans` is not one of [`cublasOperation_t::CUBLAS_OP_N`], [`cublasOperation_t::CUBLAS_OP_T`] and [`cublasOperation_t::CUBLAS_OP_C`], or if `lda &lt; max(1, n)` if `trans == CUBLAS_OP_N` and `lda &lt; max(1, k)` otherwise, or if `ldc &lt; max(1, n)`, or if `Atype` or `Ctype` are not supported.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_INITIALIZED`]: The library was not initialized.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_SUPPORTED`]: The combination of the parameters `Atype` and `Ctype` is not supported.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The operation completed successfully.
    pub fn cublasCherkEx_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        n: i64,
        k: i64,
        alpha: *const f32,
        A: *const ::core::ffi::c_void,
        Atype: cudaDataType,
        lda: i64,
        beta: *const f32,
        C: *mut ::core::ffi::c_void,
        Ctype: cudaDataType,
        ldc: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function is an extension of [`cublasCherk_v2`] where the input matrix and output matrix can have a lower precision but the computation is still done in the type [`cuComplex`]. This routine is implemented using the Gauss complexity reduction algorithm which can lead to an increase in performance up to 25%
    ///
    /// This function performs the Hermitian rank- $k$ update:
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(A)^{H} + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, $C$ is a Hermitian matrix stored in lower or upper mode, and $A$ is a matrix with dimensions $\text{op}(A)$ $n \times k$. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// The matrix types combinations supported for [`cublasCherk3mEx`] are listed in the following table:
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [cherk()](http://www.netlib.org/blas/cherk.f).
    ///
    /// # Parameters
    ///
    /// - `handle`: Handle to the cuBLAS library context.
    /// - `uplo`: Indicates if matrix `C` lower or upper part is stored, the other Hermitian part is not referenced.
    /// - `trans`: Operation op(`A`) that is non- or (conj.) transpose.
    /// - `n`: Number of rows of matrix op(`A`) and `C`.
    /// - `k`: Number of columns of matrix op(`A`).
    /// - `alpha`: &lt;`type`> scalar used for multiplication.
    /// - `A`: &lt;`type`> array of dimension `lda x k` with `lda >= max(1, n)` if `trans == CUBLAS_OP_N` and `lda x n` with `lda >= max(1, k)` otherwise.
    /// - `Atype`: Enumerant specifying the datatype of matrix `A`.
    /// - `lda`: Leading dimension of two-dimensional array used to store matrix `A`.
    /// - `beta`: &lt;`type`> scalar used for multiplication. If `beta == 0` then `C` does not have to be a valid input.
    /// - `C`: &lt;`type`> array of dimension `ldc x n`, with `ldc >= max(1, n)`. The imaginary parts of the diagonal elements are assumed and set to zero.
    /// - `Ctype`: Enumerant specifying the datatype of matrix `C`.
    /// - `ldc`: Leading dimension of two-dimensional array used to store matrix `C`.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_ARCH_MISMATCH`]: The device has a compute capability lower than 5.0.
    /// - [`cublasStatus_t::CUBLAS_STATUS_EXECUTION_FAILED`]: The function failed to launch on the GPU.
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: If `n &lt; 0` or `k &lt; 0`, or if `uplo` is not one of [`cublasFillMode_t::CUBLAS_FILL_MODE_LOWER`] and [`cublasFillMode_t::CUBLAS_FILL_MODE_UPPER`], or if `trans` is not one of [`cublasOperation_t::CUBLAS_OP_N`], [`cublasOperation_t::CUBLAS_OP_T`] and [`cublasOperation_t::CUBLAS_OP_C`], or if `lda &lt; max(1, n)` if `trans == CUBLAS_OP_N` and `lda &lt; max(1, k)` otherwise, or if `ldc &lt; max(1, n)`, or if `Atype` or `Ctype` are not supported.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_INITIALIZED`]: The library was not initialized.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_SUPPORTED`]: The combination of the parameters `Atype` and `Ctype` is not supported.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The operation completed successfully.
    pub fn cublasCherk3mEx(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        alpha: *const f32,
        A: *const ::core::ffi::c_void,
        Atype: cudaDataType,
        lda: ::core::ffi::c_int,
        beta: *const f32,
        C: *mut ::core::ffi::c_void,
        Ctype: cudaDataType,
        ldc: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function is an extension of [`cublasCherk_v2`] where the input matrix and output matrix can have a lower precision but the computation is still done in the type [`cuComplex`]. This routine is implemented using the Gauss complexity reduction algorithm which can lead to an increase in performance up to 25%
    ///
    /// This function performs the Hermitian rank- $k$ update:
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(A)^{H} + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, $C$ is a Hermitian matrix stored in lower or upper mode, and $A$ is a matrix with dimensions $\text{op}(A)$ $n \times k$. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// The matrix types combinations supported for [`cublasCherk3mEx`] are listed in the following table:
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [cherk()](http://www.netlib.org/blas/cherk.f).
    ///
    /// # Parameters
    ///
    /// - `handle`: Handle to the cuBLAS library context.
    /// - `uplo`: Indicates if matrix `C` lower or upper part is stored, the other Hermitian part is not referenced.
    /// - `trans`: Operation op(`A`) that is non- or (conj.) transpose.
    /// - `n`: Number of rows of matrix op(`A`) and `C`.
    /// - `k`: Number of columns of matrix op(`A`).
    /// - `alpha`: &lt;`type`> scalar used for multiplication.
    /// - `A`: &lt;`type`> array of dimension `lda x k` with `lda >= max(1, n)` if `trans == CUBLAS_OP_N` and `lda x n` with `lda >= max(1, k)` otherwise.
    /// - `Atype`: Enumerant specifying the datatype of matrix `A`.
    /// - `lda`: Leading dimension of two-dimensional array used to store matrix `A`.
    /// - `beta`: &lt;`type`> scalar used for multiplication. If `beta == 0` then `C` does not have to be a valid input.
    /// - `C`: &lt;`type`> array of dimension `ldc x n`, with `ldc >= max(1, n)`. The imaginary parts of the diagonal elements are assumed and set to zero.
    /// - `Ctype`: Enumerant specifying the datatype of matrix `C`.
    /// - `ldc`: Leading dimension of two-dimensional array used to store matrix `C`.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_ARCH_MISMATCH`]: The device has a compute capability lower than 5.0.
    /// - [`cublasStatus_t::CUBLAS_STATUS_EXECUTION_FAILED`]: The function failed to launch on the GPU.
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: If `n &lt; 0` or `k &lt; 0`, or if `uplo` is not one of [`cublasFillMode_t::CUBLAS_FILL_MODE_LOWER`] and [`cublasFillMode_t::CUBLAS_FILL_MODE_UPPER`], or if `trans` is not one of [`cublasOperation_t::CUBLAS_OP_N`], [`cublasOperation_t::CUBLAS_OP_T`] and [`cublasOperation_t::CUBLAS_OP_C`], or if `lda &lt; max(1, n)` if `trans == CUBLAS_OP_N` and `lda &lt; max(1, k)` otherwise, or if `ldc &lt; max(1, n)`, or if `Atype` or `Ctype` are not supported.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_INITIALIZED`]: The library was not initialized.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_SUPPORTED`]: The combination of the parameters `Atype` and `Ctype` is not supported.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The operation completed successfully.
    pub fn cublasCherk3mEx_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        n: i64,
        k: i64,
        alpha: *const f32,
        A: *const ::core::ffi::c_void,
        Atype: cudaDataType,
        lda: i64,
        beta: *const f32,
        C: *mut ::core::ffi::c_void,
        Ctype: cudaDataType,
        ldc: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric rank- $2k$ update:
    /// $$
    /// C = \alpha(\text{op}(A)\text{op}(B)^{T} + \text{op}(B)\text{op}(A)^{T}) + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, $C$ is a symmetric matrix stored in lower or upper mode, and $A$ and $B$ are matrices with dimensions $\text{op}(A)$ $n \times k$ and $\text{op}(B)$ $n \times k$, respectively. Also, for matrix $A$ and $B$:
    /// $$
    /// \text{op(}A\text{) and op(}B\text{)} = \left\\{ \begin{matrix}
    /// {A\text{ and }B} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A^{T}\text{ and }B^{T}} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssyr2k()](http://www.netlib.org/blas/ssyr2k.f), [dsyr2k()](http://www.netlib.org/blas/dsyr2k.f), [csyr2k()](http://www.netlib.org/blas/csyr2k.f), [zsyr2k()](http://www.netlib.org/blas/zsyr2k.f).
    pub fn cublasSsyr2k_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        alpha: *const f32,
        A: *const f32,
        lda: ::core::ffi::c_int,
        B: *const f32,
        ldb: ::core::ffi::c_int,
        beta: *const f32,
        C: *mut f32,
        ldc: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric rank- $2k$ update:
    /// $$
    /// C = \alpha(\text{op}(A)\text{op}(B)^{T} + \text{op}(B)\text{op}(A)^{T}) + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, $C$ is a symmetric matrix stored in lower or upper mode, and $A$ and $B$ are matrices with dimensions $\text{op}(A)$ $n \times k$ and $\text{op}(B)$ $n \times k$, respectively. Also, for matrix $A$ and $B$:
    /// $$
    /// \text{op(}A\text{) and op(}B\text{)} = \left\\{ \begin{matrix}
    /// {A\text{ and }B} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A^{T}\text{ and }B^{T}} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssyr2k()](http://www.netlib.org/blas/ssyr2k.f), [dsyr2k()](http://www.netlib.org/blas/dsyr2k.f), [csyr2k()](http://www.netlib.org/blas/csyr2k.f), [zsyr2k()](http://www.netlib.org/blas/zsyr2k.f).
    pub fn cublasSsyr2k_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        n: i64,
        k: i64,
        alpha: *const f32,
        A: *const f32,
        lda: i64,
        B: *const f32,
        ldb: i64,
        beta: *const f32,
        C: *mut f32,
        ldc: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric rank- $2k$ update:
    /// $$
    /// C = \alpha(\text{op}(A)\text{op}(B)^{T} + \text{op}(B)\text{op}(A)^{T}) + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, $C$ is a symmetric matrix stored in lower or upper mode, and $A$ and $B$ are matrices with dimensions $\text{op}(A)$ $n \times k$ and $\text{op}(B)$ $n \times k$, respectively. Also, for matrix $A$ and $B$:
    /// $$
    /// \text{op(}A\text{) and op(}B\text{)} = \left\\{ \begin{matrix}
    /// {A\text{ and }B} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A^{T}\text{ and }B^{T}} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssyr2k()](http://www.netlib.org/blas/ssyr2k.f), [dsyr2k()](http://www.netlib.org/blas/dsyr2k.f), [csyr2k()](http://www.netlib.org/blas/csyr2k.f), [zsyr2k()](http://www.netlib.org/blas/zsyr2k.f).
    pub fn cublasDsyr2k_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        alpha: *const f64,
        A: *const f64,
        lda: ::core::ffi::c_int,
        B: *const f64,
        ldb: ::core::ffi::c_int,
        beta: *const f64,
        C: *mut f64,
        ldc: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric rank- $2k$ update:
    /// $$
    /// C = \alpha(\text{op}(A)\text{op}(B)^{T} + \text{op}(B)\text{op}(A)^{T}) + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, $C$ is a symmetric matrix stored in lower or upper mode, and $A$ and $B$ are matrices with dimensions $\text{op}(A)$ $n \times k$ and $\text{op}(B)$ $n \times k$, respectively. Also, for matrix $A$ and $B$:
    /// $$
    /// \text{op(}A\text{) and op(}B\text{)} = \left\\{ \begin{matrix}
    /// {A\text{ and }B} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A^{T}\text{ and }B^{T}} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssyr2k()](http://www.netlib.org/blas/ssyr2k.f), [dsyr2k()](http://www.netlib.org/blas/dsyr2k.f), [csyr2k()](http://www.netlib.org/blas/csyr2k.f), [zsyr2k()](http://www.netlib.org/blas/zsyr2k.f).
    pub fn cublasDsyr2k_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        n: i64,
        k: i64,
        alpha: *const f64,
        A: *const f64,
        lda: i64,
        B: *const f64,
        ldb: i64,
        beta: *const f64,
        C: *mut f64,
        ldc: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric rank- $2k$ update:
    /// $$
    /// C = \alpha(\text{op}(A)\text{op}(B)^{T} + \text{op}(B)\text{op}(A)^{T}) + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, $C$ is a symmetric matrix stored in lower or upper mode, and $A$ and $B$ are matrices with dimensions $\text{op}(A)$ $n \times k$ and $\text{op}(B)$ $n \times k$, respectively. Also, for matrix $A$ and $B$:
    /// $$
    /// \text{op(}A\text{) and op(}B\text{)} = \left\\{ \begin{matrix}
    /// {A\text{ and }B} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A^{T}\text{ and }B^{T}} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssyr2k()](http://www.netlib.org/blas/ssyr2k.f), [dsyr2k()](http://www.netlib.org/blas/dsyr2k.f), [csyr2k()](http://www.netlib.org/blas/csyr2k.f), [zsyr2k()](http://www.netlib.org/blas/zsyr2k.f).
    pub fn cublasCsyr2k_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        alpha: *const cuComplex,
        A: *const cuComplex,
        lda: ::core::ffi::c_int,
        B: *const cuComplex,
        ldb: ::core::ffi::c_int,
        beta: *const cuComplex,
        C: *mut cuComplex,
        ldc: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric rank- $2k$ update:
    /// $$
    /// C = \alpha(\text{op}(A)\text{op}(B)^{T} + \text{op}(B)\text{op}(A)^{T}) + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, $C$ is a symmetric matrix stored in lower or upper mode, and $A$ and $B$ are matrices with dimensions $\text{op}(A)$ $n \times k$ and $\text{op}(B)$ $n \times k$, respectively. Also, for matrix $A$ and $B$:
    /// $$
    /// \text{op(}A\text{) and op(}B\text{)} = \left\\{ \begin{matrix}
    /// {A\text{ and }B} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A^{T}\text{ and }B^{T}} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssyr2k()](http://www.netlib.org/blas/ssyr2k.f), [dsyr2k()](http://www.netlib.org/blas/dsyr2k.f), [csyr2k()](http://www.netlib.org/blas/csyr2k.f), [zsyr2k()](http://www.netlib.org/blas/zsyr2k.f).
    pub fn cublasCsyr2k_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        n: i64,
        k: i64,
        alpha: *const cuComplex,
        A: *const cuComplex,
        lda: i64,
        B: *const cuComplex,
        ldb: i64,
        beta: *const cuComplex,
        C: *mut cuComplex,
        ldc: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric rank- $2k$ update:
    /// $$
    /// C = \alpha(\text{op}(A)\text{op}(B)^{T} + \text{op}(B)\text{op}(A)^{T}) + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, $C$ is a symmetric matrix stored in lower or upper mode, and $A$ and $B$ are matrices with dimensions $\text{op}(A)$ $n \times k$ and $\text{op}(B)$ $n \times k$, respectively. Also, for matrix $A$ and $B$:
    /// $$
    /// \text{op(}A\text{) and op(}B\text{)} = \left\\{ \begin{matrix}
    /// {A\text{ and }B} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A^{T}\text{ and }B^{T}} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssyr2k()](http://www.netlib.org/blas/ssyr2k.f), [dsyr2k()](http://www.netlib.org/blas/dsyr2k.f), [csyr2k()](http://www.netlib.org/blas/csyr2k.f), [zsyr2k()](http://www.netlib.org/blas/zsyr2k.f).
    pub fn cublasZsyr2k_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        alpha: *const cuDoubleComplex,
        A: *const cuDoubleComplex,
        lda: ::core::ffi::c_int,
        B: *const cuDoubleComplex,
        ldb: ::core::ffi::c_int,
        beta: *const cuDoubleComplex,
        C: *mut cuDoubleComplex,
        ldc: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric rank- $2k$ update:
    /// $$
    /// C = \alpha(\text{op}(A)\text{op}(B)^{T} + \text{op}(B)\text{op}(A)^{T}) + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, $C$ is a symmetric matrix stored in lower or upper mode, and $A$ and $B$ are matrices with dimensions $\text{op}(A)$ $n \times k$ and $\text{op}(B)$ $n \times k$, respectively. Also, for matrix $A$ and $B$:
    /// $$
    /// \text{op(}A\text{) and op(}B\text{)} = \left\\{ \begin{matrix}
    /// {A\text{ and }B} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A^{T}\text{ and }B^{T}} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssyr2k()](http://www.netlib.org/blas/ssyr2k.f), [dsyr2k()](http://www.netlib.org/blas/dsyr2k.f), [csyr2k()](http://www.netlib.org/blas/csyr2k.f), [zsyr2k()](http://www.netlib.org/blas/zsyr2k.f).
    pub fn cublasZsyr2k_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        n: i64,
        k: i64,
        alpha: *const cuDoubleComplex,
        A: *const cuDoubleComplex,
        lda: i64,
        B: *const cuDoubleComplex,
        ldb: i64,
        beta: *const cuDoubleComplex,
        C: *mut cuDoubleComplex,
        ldc: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the Hermitian rank- $2k$ update:
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(B)^{H} + \overset{ˉ}{\alpha}\text{op}(B)\text{op}(A)^{H} + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, $C$ is a Hermitian matrix stored in lower or upper mode, and $A$ and $B$ are matrices with dimensions $\text{op}(A)$ $n \times k$ and $\text{op}(B)$ $n \times k$, respectively. Also, for matrix $A$ and $B$:
    /// $$
    /// \text{op(}A\text{) and op(}B\text{)} = \left\\{ \begin{matrix}
    /// {A\text{ and }B} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A^{H}\text{ and }B^{H}} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [cher2k()](http://www.netlib.org/blas/cher2k.f), [zher2k()](http://www.netlib.org/blas/zher2k.f).
    pub fn cublasCher2k_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        alpha: *const cuComplex,
        A: *const cuComplex,
        lda: ::core::ffi::c_int,
        B: *const cuComplex,
        ldb: ::core::ffi::c_int,
        beta: *const f32,
        C: *mut cuComplex,
        ldc: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the Hermitian rank- $2k$ update:
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(B)^{H} + \overset{ˉ}{\alpha}\text{op}(B)\text{op}(A)^{H} + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, $C$ is a Hermitian matrix stored in lower or upper mode, and $A$ and $B$ are matrices with dimensions $\text{op}(A)$ $n \times k$ and $\text{op}(B)$ $n \times k$, respectively. Also, for matrix $A$ and $B$:
    /// $$
    /// \text{op(}A\text{) and op(}B\text{)} = \left\\{ \begin{matrix}
    /// {A\text{ and }B} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A^{H}\text{ and }B^{H}} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [cher2k()](http://www.netlib.org/blas/cher2k.f), [zher2k()](http://www.netlib.org/blas/zher2k.f).
    pub fn cublasCher2k_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        n: i64,
        k: i64,
        alpha: *const cuComplex,
        A: *const cuComplex,
        lda: i64,
        B: *const cuComplex,
        ldb: i64,
        beta: *const f32,
        C: *mut cuComplex,
        ldc: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the Hermitian rank- $2k$ update:
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(B)^{H} + \overset{ˉ}{\alpha}\text{op}(B)\text{op}(A)^{H} + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, $C$ is a Hermitian matrix stored in lower or upper mode, and $A$ and $B$ are matrices with dimensions $\text{op}(A)$ $n \times k$ and $\text{op}(B)$ $n \times k$, respectively. Also, for matrix $A$ and $B$:
    /// $$
    /// \text{op(}A\text{) and op(}B\text{)} = \left\\{ \begin{matrix}
    /// {A\text{ and }B} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A^{H}\text{ and }B^{H}} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [cher2k()](http://www.netlib.org/blas/cher2k.f), [zher2k()](http://www.netlib.org/blas/zher2k.f).
    pub fn cublasZher2k_v2(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        alpha: *const cuDoubleComplex,
        A: *const cuDoubleComplex,
        lda: ::core::ffi::c_int,
        B: *const cuDoubleComplex,
        ldb: ::core::ffi::c_int,
        beta: *const f64,
        C: *mut cuDoubleComplex,
        ldc: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the Hermitian rank- $2k$ update:
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(B)^{H} + \overset{ˉ}{\alpha}\text{op}(B)\text{op}(A)^{H} + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, $C$ is a Hermitian matrix stored in lower or upper mode, and $A$ and $B$ are matrices with dimensions $\text{op}(A)$ $n \times k$ and $\text{op}(B)$ $n \times k$, respectively. Also, for matrix $A$ and $B$:
    /// $$
    /// \text{op(}A\text{) and op(}B\text{)} = \left\\{ \begin{matrix}
    /// {A\text{ and }B} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A^{H}\text{ and }B^{H}} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [cher2k()](http://www.netlib.org/blas/cher2k.f), [zher2k()](http://www.netlib.org/blas/zher2k.f).
    pub fn cublasZher2k_v2_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        n: i64,
        k: i64,
        alpha: *const cuDoubleComplex,
        A: *const cuDoubleComplex,
        lda: i64,
        B: *const cuDoubleComplex,
        ldb: i64,
        beta: *const f64,
        C: *mut cuDoubleComplex,
        ldc: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs a variation of the symmetric rank- $k$ update:
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(B)^{T} + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, $C$ is a symmetric matrix stored in lower or upper mode, and $A$ and $B$ are matrices with dimensions $\text{op}(A)$ $n \times k$ and $\text{op}(B)$ $n \times k$, respectively. Also, for matrices $A$ and $B$:
    /// $$
    /// \text{op(}A\text{) and op(}B\text{)} = \left\\{ \begin{matrix}
    /// {A\text{ and }B} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A^{T}\text{ and }B^{T}} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// This routine can be used when B is in such way that the result is guaranteed to be symmetric. A usual example is when the matrix B is a scaled form of the matrix A: this is equivalent to B being the product of the matrix A and a diagonal matrix. For an efficient computation of the product of a regular matrix with a diagonal matrix, refer to the routine cublas&lt;t>dgmm().
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssyrk()](http://www.netlib.org/blas/ssyrk.f), [dsyrk()](http://www.netlib.org/blas/dsyrk.f), [csyrk()](http://www.netlib.org/blas/csyrk.f), [zsyrk()](http://www.netlib.org/blas/zsyrk.f) and
    ///
    /// [ssyr2k()](http://www.netlib.org/blas/ssyr2k.f), [dsyr2k()](http://www.netlib.org/blas/dsyr2k.f), [csyr2k()](http://www.netlib.org/blas/csyr2k.f), [zsyr2k()](http://www.netlib.org/blas/zsyr2k.f).
    pub fn cublasSsyrkx(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        alpha: *const f32,
        A: *const f32,
        lda: ::core::ffi::c_int,
        B: *const f32,
        ldb: ::core::ffi::c_int,
        beta: *const f32,
        C: *mut f32,
        ldc: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs a variation of the symmetric rank- $k$ update:
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(B)^{T} + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, $C$ is a symmetric matrix stored in lower or upper mode, and $A$ and $B$ are matrices with dimensions $\text{op}(A)$ $n \times k$ and $\text{op}(B)$ $n \times k$, respectively. Also, for matrices $A$ and $B$:
    /// $$
    /// \text{op(}A\text{) and op(}B\text{)} = \left\\{ \begin{matrix}
    /// {A\text{ and }B} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A^{T}\text{ and }B^{T}} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// This routine can be used when B is in such way that the result is guaranteed to be symmetric. A usual example is when the matrix B is a scaled form of the matrix A: this is equivalent to B being the product of the matrix A and a diagonal matrix. For an efficient computation of the product of a regular matrix with a diagonal matrix, refer to the routine cublas&lt;t>dgmm().
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssyrk()](http://www.netlib.org/blas/ssyrk.f), [dsyrk()](http://www.netlib.org/blas/dsyrk.f), [csyrk()](http://www.netlib.org/blas/csyrk.f), [zsyrk()](http://www.netlib.org/blas/zsyrk.f) and
    ///
    /// [ssyr2k()](http://www.netlib.org/blas/ssyr2k.f), [dsyr2k()](http://www.netlib.org/blas/dsyr2k.f), [csyr2k()](http://www.netlib.org/blas/csyr2k.f), [zsyr2k()](http://www.netlib.org/blas/zsyr2k.f).
    pub fn cublasSsyrkx_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        n: i64,
        k: i64,
        alpha: *const f32,
        A: *const f32,
        lda: i64,
        B: *const f32,
        ldb: i64,
        beta: *const f32,
        C: *mut f32,
        ldc: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs a variation of the symmetric rank- $k$ update:
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(B)^{T} + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, $C$ is a symmetric matrix stored in lower or upper mode, and $A$ and $B$ are matrices with dimensions $\text{op}(A)$ $n \times k$ and $\text{op}(B)$ $n \times k$, respectively. Also, for matrices $A$ and $B$:
    /// $$
    /// \text{op(}A\text{) and op(}B\text{)} = \left\\{ \begin{matrix}
    /// {A\text{ and }B} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A^{T}\text{ and }B^{T}} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// This routine can be used when B is in such way that the result is guaranteed to be symmetric. A usual example is when the matrix B is a scaled form of the matrix A: this is equivalent to B being the product of the matrix A and a diagonal matrix. For an efficient computation of the product of a regular matrix with a diagonal matrix, refer to the routine cublas&lt;t>dgmm().
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssyrk()](http://www.netlib.org/blas/ssyrk.f), [dsyrk()](http://www.netlib.org/blas/dsyrk.f), [csyrk()](http://www.netlib.org/blas/csyrk.f), [zsyrk()](http://www.netlib.org/blas/zsyrk.f) and
    ///
    /// [ssyr2k()](http://www.netlib.org/blas/ssyr2k.f), [dsyr2k()](http://www.netlib.org/blas/dsyr2k.f), [csyr2k()](http://www.netlib.org/blas/csyr2k.f), [zsyr2k()](http://www.netlib.org/blas/zsyr2k.f).
    pub fn cublasDsyrkx(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        alpha: *const f64,
        A: *const f64,
        lda: ::core::ffi::c_int,
        B: *const f64,
        ldb: ::core::ffi::c_int,
        beta: *const f64,
        C: *mut f64,
        ldc: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs a variation of the symmetric rank- $k$ update:
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(B)^{T} + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, $C$ is a symmetric matrix stored in lower or upper mode, and $A$ and $B$ are matrices with dimensions $\text{op}(A)$ $n \times k$ and $\text{op}(B)$ $n \times k$, respectively. Also, for matrices $A$ and $B$:
    /// $$
    /// \text{op(}A\text{) and op(}B\text{)} = \left\\{ \begin{matrix}
    /// {A\text{ and }B} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A^{T}\text{ and }B^{T}} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// This routine can be used when B is in such way that the result is guaranteed to be symmetric. A usual example is when the matrix B is a scaled form of the matrix A: this is equivalent to B being the product of the matrix A and a diagonal matrix. For an efficient computation of the product of a regular matrix with a diagonal matrix, refer to the routine cublas&lt;t>dgmm().
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssyrk()](http://www.netlib.org/blas/ssyrk.f), [dsyrk()](http://www.netlib.org/blas/dsyrk.f), [csyrk()](http://www.netlib.org/blas/csyrk.f), [zsyrk()](http://www.netlib.org/blas/zsyrk.f) and
    ///
    /// [ssyr2k()](http://www.netlib.org/blas/ssyr2k.f), [dsyr2k()](http://www.netlib.org/blas/dsyr2k.f), [csyr2k()](http://www.netlib.org/blas/csyr2k.f), [zsyr2k()](http://www.netlib.org/blas/zsyr2k.f).
    pub fn cublasDsyrkx_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        n: i64,
        k: i64,
        alpha: *const f64,
        A: *const f64,
        lda: i64,
        B: *const f64,
        ldb: i64,
        beta: *const f64,
        C: *mut f64,
        ldc: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs a variation of the symmetric rank- $k$ update:
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(B)^{T} + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, $C$ is a symmetric matrix stored in lower or upper mode, and $A$ and $B$ are matrices with dimensions $\text{op}(A)$ $n \times k$ and $\text{op}(B)$ $n \times k$, respectively. Also, for matrices $A$ and $B$:
    /// $$
    /// \text{op(}A\text{) and op(}B\text{)} = \left\\{ \begin{matrix}
    /// {A\text{ and }B} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A^{T}\text{ and }B^{T}} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// This routine can be used when B is in such way that the result is guaranteed to be symmetric. A usual example is when the matrix B is a scaled form of the matrix A: this is equivalent to B being the product of the matrix A and a diagonal matrix. For an efficient computation of the product of a regular matrix with a diagonal matrix, refer to the routine cublas&lt;t>dgmm().
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssyrk()](http://www.netlib.org/blas/ssyrk.f), [dsyrk()](http://www.netlib.org/blas/dsyrk.f), [csyrk()](http://www.netlib.org/blas/csyrk.f), [zsyrk()](http://www.netlib.org/blas/zsyrk.f) and
    ///
    /// [ssyr2k()](http://www.netlib.org/blas/ssyr2k.f), [dsyr2k()](http://www.netlib.org/blas/dsyr2k.f), [csyr2k()](http://www.netlib.org/blas/csyr2k.f), [zsyr2k()](http://www.netlib.org/blas/zsyr2k.f).
    pub fn cublasCsyrkx(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        alpha: *const cuComplex,
        A: *const cuComplex,
        lda: ::core::ffi::c_int,
        B: *const cuComplex,
        ldb: ::core::ffi::c_int,
        beta: *const cuComplex,
        C: *mut cuComplex,
        ldc: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs a variation of the symmetric rank- $k$ update:
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(B)^{T} + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, $C$ is a symmetric matrix stored in lower or upper mode, and $A$ and $B$ are matrices with dimensions $\text{op}(A)$ $n \times k$ and $\text{op}(B)$ $n \times k$, respectively. Also, for matrices $A$ and $B$:
    /// $$
    /// \text{op(}A\text{) and op(}B\text{)} = \left\\{ \begin{matrix}
    /// {A\text{ and }B} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A^{T}\text{ and }B^{T}} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// This routine can be used when B is in such way that the result is guaranteed to be symmetric. A usual example is when the matrix B is a scaled form of the matrix A: this is equivalent to B being the product of the matrix A and a diagonal matrix. For an efficient computation of the product of a regular matrix with a diagonal matrix, refer to the routine cublas&lt;t>dgmm().
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssyrk()](http://www.netlib.org/blas/ssyrk.f), [dsyrk()](http://www.netlib.org/blas/dsyrk.f), [csyrk()](http://www.netlib.org/blas/csyrk.f), [zsyrk()](http://www.netlib.org/blas/zsyrk.f) and
    ///
    /// [ssyr2k()](http://www.netlib.org/blas/ssyr2k.f), [dsyr2k()](http://www.netlib.org/blas/dsyr2k.f), [csyr2k()](http://www.netlib.org/blas/csyr2k.f), [zsyr2k()](http://www.netlib.org/blas/zsyr2k.f).
    pub fn cublasCsyrkx_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        n: i64,
        k: i64,
        alpha: *const cuComplex,
        A: *const cuComplex,
        lda: i64,
        B: *const cuComplex,
        ldb: i64,
        beta: *const cuComplex,
        C: *mut cuComplex,
        ldc: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs a variation of the symmetric rank- $k$ update:
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(B)^{T} + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, $C$ is a symmetric matrix stored in lower or upper mode, and $A$ and $B$ are matrices with dimensions $\text{op}(A)$ $n \times k$ and $\text{op}(B)$ $n \times k$, respectively. Also, for matrices $A$ and $B$:
    /// $$
    /// \text{op(}A\text{) and op(}B\text{)} = \left\\{ \begin{matrix}
    /// {A\text{ and }B} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A^{T}\text{ and }B^{T}} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// This routine can be used when B is in such way that the result is guaranteed to be symmetric. A usual example is when the matrix B is a scaled form of the matrix A: this is equivalent to B being the product of the matrix A and a diagonal matrix. For an efficient computation of the product of a regular matrix with a diagonal matrix, refer to the routine cublas&lt;t>dgmm().
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssyrk()](http://www.netlib.org/blas/ssyrk.f), [dsyrk()](http://www.netlib.org/blas/dsyrk.f), [csyrk()](http://www.netlib.org/blas/csyrk.f), [zsyrk()](http://www.netlib.org/blas/zsyrk.f) and
    ///
    /// [ssyr2k()](http://www.netlib.org/blas/ssyr2k.f), [dsyr2k()](http://www.netlib.org/blas/dsyr2k.f), [csyr2k()](http://www.netlib.org/blas/csyr2k.f), [zsyr2k()](http://www.netlib.org/blas/zsyr2k.f).
    pub fn cublasZsyrkx(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        alpha: *const cuDoubleComplex,
        A: *const cuDoubleComplex,
        lda: ::core::ffi::c_int,
        B: *const cuDoubleComplex,
        ldb: ::core::ffi::c_int,
        beta: *const cuDoubleComplex,
        C: *mut cuDoubleComplex,
        ldc: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs a variation of the symmetric rank- $k$ update:
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(B)^{T} + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, $C$ is a symmetric matrix stored in lower or upper mode, and $A$ and $B$ are matrices with dimensions $\text{op}(A)$ $n \times k$ and $\text{op}(B)$ $n \times k$, respectively. Also, for matrices $A$ and $B$:
    /// $$
    /// \text{op(}A\text{) and op(}B\text{)} = \left\\{ \begin{matrix}
    /// {A\text{ and }B} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A^{T}\text{ and }B^{T}} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// This routine can be used when B is in such way that the result is guaranteed to be symmetric. A usual example is when the matrix B is a scaled form of the matrix A: this is equivalent to B being the product of the matrix A and a diagonal matrix. For an efficient computation of the product of a regular matrix with a diagonal matrix, refer to the routine cublas&lt;t>dgmm().
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssyrk()](http://www.netlib.org/blas/ssyrk.f), [dsyrk()](http://www.netlib.org/blas/dsyrk.f), [csyrk()](http://www.netlib.org/blas/csyrk.f), [zsyrk()](http://www.netlib.org/blas/zsyrk.f) and
    ///
    /// [ssyr2k()](http://www.netlib.org/blas/ssyr2k.f), [dsyr2k()](http://www.netlib.org/blas/dsyr2k.f), [csyr2k()](http://www.netlib.org/blas/csyr2k.f), [zsyr2k()](http://www.netlib.org/blas/zsyr2k.f).
    pub fn cublasZsyrkx_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        n: i64,
        k: i64,
        alpha: *const cuDoubleComplex,
        A: *const cuDoubleComplex,
        lda: i64,
        B: *const cuDoubleComplex,
        ldb: i64,
        beta: *const cuDoubleComplex,
        C: *mut cuDoubleComplex,
        ldc: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs a variation of the Hermitian rank- $k$ update:
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(B)^{H} + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, $C$ is a Hermitian matrix stored in lower or upper mode, and $A$ and $B$ are matrices with dimensions $\text{op}(A)$ $n \times k$ and $\text{op}(B)$ $n \times k$, respectively. Also, for matrix $A$ and $B$:
    /// $$
    /// \text{op(}A\text{) and op(}B\text{)} = \left\\{ \begin{matrix}
    /// {A\text{ and }B} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A^{H}\text{ and }B^{H}} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// This routine can be used when the matrix B is in such way that the result is guaranteed to be hermitian. An usual example is when the matrix B is a scaled form of the matrix A: this is equivalent to B being the product of the matrix A and a diagonal matrix. For an efficient computation of the product of a regular matrix with a diagonal matrix, refer to the routine cublas&lt;t>dgmm().
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [cherk()](http://www.netlib.org/blas/cherk.f), [zherk()](http://www.netlib.org/blas/zherk.f) and
    ///
    /// [cher2k()](http://www.netlib.org/blas/cher2k.f), [zher2k()](http://www.netlib.org/blas/zher2k.f).
    pub fn cublasCherkx(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        alpha: *const cuComplex,
        A: *const cuComplex,
        lda: ::core::ffi::c_int,
        B: *const cuComplex,
        ldb: ::core::ffi::c_int,
        beta: *const f32,
        C: *mut cuComplex,
        ldc: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs a variation of the Hermitian rank- $k$ update:
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(B)^{H} + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, $C$ is a Hermitian matrix stored in lower or upper mode, and $A$ and $B$ are matrices with dimensions $\text{op}(A)$ $n \times k$ and $\text{op}(B)$ $n \times k$, respectively. Also, for matrix $A$ and $B$:
    /// $$
    /// \text{op(}A\text{) and op(}B\text{)} = \left\\{ \begin{matrix}
    /// {A\text{ and }B} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A^{H}\text{ and }B^{H}} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// This routine can be used when the matrix B is in such way that the result is guaranteed to be hermitian. An usual example is when the matrix B is a scaled form of the matrix A: this is equivalent to B being the product of the matrix A and a diagonal matrix. For an efficient computation of the product of a regular matrix with a diagonal matrix, refer to the routine cublas&lt;t>dgmm().
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [cherk()](http://www.netlib.org/blas/cherk.f), [zherk()](http://www.netlib.org/blas/zherk.f) and
    ///
    /// [cher2k()](http://www.netlib.org/blas/cher2k.f), [zher2k()](http://www.netlib.org/blas/zher2k.f).
    pub fn cublasCherkx_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        n: i64,
        k: i64,
        alpha: *const cuComplex,
        A: *const cuComplex,
        lda: i64,
        B: *const cuComplex,
        ldb: i64,
        beta: *const f32,
        C: *mut cuComplex,
        ldc: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs a variation of the Hermitian rank- $k$ update:
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(B)^{H} + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, $C$ is a Hermitian matrix stored in lower or upper mode, and $A$ and $B$ are matrices with dimensions $\text{op}(A)$ $n \times k$ and $\text{op}(B)$ $n \times k$, respectively. Also, for matrix $A$ and $B$:
    /// $$
    /// \text{op(}A\text{) and op(}B\text{)} = \left\\{ \begin{matrix}
    /// {A\text{ and }B} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A^{H}\text{ and }B^{H}} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// This routine can be used when the matrix B is in such way that the result is guaranteed to be hermitian. An usual example is when the matrix B is a scaled form of the matrix A: this is equivalent to B being the product of the matrix A and a diagonal matrix. For an efficient computation of the product of a regular matrix with a diagonal matrix, refer to the routine cublas&lt;t>dgmm().
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [cherk()](http://www.netlib.org/blas/cherk.f), [zherk()](http://www.netlib.org/blas/zherk.f) and
    ///
    /// [cher2k()](http://www.netlib.org/blas/cher2k.f), [zher2k()](http://www.netlib.org/blas/zher2k.f).
    pub fn cublasZherkx(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        alpha: *const cuDoubleComplex,
        A: *const cuDoubleComplex,
        lda: ::core::ffi::c_int,
        B: *const cuDoubleComplex,
        ldb: ::core::ffi::c_int,
        beta: *const f64,
        C: *mut cuDoubleComplex,
        ldc: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs a variation of the Hermitian rank- $k$ update:
    /// $$
    /// C = \alpha\text{op}(A)\text{op}(B)^{H} + \beta C
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, $C$ is a Hermitian matrix stored in lower or upper mode, and $A$ and $B$ are matrices with dimensions $\text{op}(A)$ $n \times k$ and $\text{op}(B)$ $n \times k$, respectively. Also, for matrix $A$ and $B$:
    /// $$
    /// \text{op(}A\text{) and op(}B\text{)} = \left\\{ \begin{matrix}
    /// {A\text{ and }B} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A^{H}\text{ and }B^{H}} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// This routine can be used when the matrix B is in such way that the result is guaranteed to be hermitian. An usual example is when the matrix B is a scaled form of the matrix A: this is equivalent to B being the product of the matrix A and a diagonal matrix. For an efficient computation of the product of a regular matrix with a diagonal matrix, refer to the routine cublas&lt;t>dgmm().
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [cherk()](http://www.netlib.org/blas/cherk.f), [zherk()](http://www.netlib.org/blas/zherk.f) and
    ///
    /// [cher2k()](http://www.netlib.org/blas/cher2k.f), [zher2k()](http://www.netlib.org/blas/zher2k.f).
    pub fn cublasZherkx_64(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        n: i64,
        k: i64,
        alpha: *const cuDoubleComplex,
        A: *const cuDoubleComplex,
        lda: i64,
        B: *const cuDoubleComplex,
        ldb: i64,
        beta: *const f64,
        C: *mut cuDoubleComplex,
        ldc: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric matrix-matrix multiplication:
    /// $$
    /// C = \left\\{ \begin{matrix}
    /// {\alpha AB + \beta C} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_LEFT}$}} \\\\
    /// {\alpha BA + \beta C} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_RIGHT}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $A$ is a symmetric matrix stored in lower or upper mode, $B$ and $C$ are $m \times n$ matrices, and $\alpha$ and $\beta$ are scalars.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssymm()](http://www.netlib.org/blas/ssymm.f), [dsymm()](http://www.netlib.org/blas/dsymm.f), [csymm()](http://www.netlib.org/blas/csymm.f), [zsymm()](http://www.netlib.org/blas/zsymm.f).
    pub fn cublasSsymm_v2(
        handle: cublasHandle_t,
        side: cublasSideMode_t,
        uplo: cublasFillMode_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        alpha: *const f32,
        A: *const f32,
        lda: ::core::ffi::c_int,
        B: *const f32,
        ldb: ::core::ffi::c_int,
        beta: *const f32,
        C: *mut f32,
        ldc: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric matrix-matrix multiplication:
    /// $$
    /// C = \left\\{ \begin{matrix}
    /// {\alpha AB + \beta C} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_LEFT}$}} \\\\
    /// {\alpha BA + \beta C} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_RIGHT}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $A$ is a symmetric matrix stored in lower or upper mode, $B$ and $C$ are $m \times n$ matrices, and $\alpha$ and $\beta$ are scalars.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssymm()](http://www.netlib.org/blas/ssymm.f), [dsymm()](http://www.netlib.org/blas/dsymm.f), [csymm()](http://www.netlib.org/blas/csymm.f), [zsymm()](http://www.netlib.org/blas/zsymm.f).
    pub fn cublasSsymm_v2_64(
        handle: cublasHandle_t,
        side: cublasSideMode_t,
        uplo: cublasFillMode_t,
        m: i64,
        n: i64,
        alpha: *const f32,
        A: *const f32,
        lda: i64,
        B: *const f32,
        ldb: i64,
        beta: *const f32,
        C: *mut f32,
        ldc: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric matrix-matrix multiplication:
    /// $$
    /// C = \left\\{ \begin{matrix}
    /// {\alpha AB + \beta C} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_LEFT}$}} \\\\
    /// {\alpha BA + \beta C} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_RIGHT}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $A$ is a symmetric matrix stored in lower or upper mode, $B$ and $C$ are $m \times n$ matrices, and $\alpha$ and $\beta$ are scalars.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssymm()](http://www.netlib.org/blas/ssymm.f), [dsymm()](http://www.netlib.org/blas/dsymm.f), [csymm()](http://www.netlib.org/blas/csymm.f), [zsymm()](http://www.netlib.org/blas/zsymm.f).
    pub fn cublasDsymm_v2(
        handle: cublasHandle_t,
        side: cublasSideMode_t,
        uplo: cublasFillMode_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        alpha: *const f64,
        A: *const f64,
        lda: ::core::ffi::c_int,
        B: *const f64,
        ldb: ::core::ffi::c_int,
        beta: *const f64,
        C: *mut f64,
        ldc: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric matrix-matrix multiplication:
    /// $$
    /// C = \left\\{ \begin{matrix}
    /// {\alpha AB + \beta C} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_LEFT}$}} \\\\
    /// {\alpha BA + \beta C} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_RIGHT}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $A$ is a symmetric matrix stored in lower or upper mode, $B$ and $C$ are $m \times n$ matrices, and $\alpha$ and $\beta$ are scalars.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssymm()](http://www.netlib.org/blas/ssymm.f), [dsymm()](http://www.netlib.org/blas/dsymm.f), [csymm()](http://www.netlib.org/blas/csymm.f), [zsymm()](http://www.netlib.org/blas/zsymm.f).
    pub fn cublasDsymm_v2_64(
        handle: cublasHandle_t,
        side: cublasSideMode_t,
        uplo: cublasFillMode_t,
        m: i64,
        n: i64,
        alpha: *const f64,
        A: *const f64,
        lda: i64,
        B: *const f64,
        ldb: i64,
        beta: *const f64,
        C: *mut f64,
        ldc: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric matrix-matrix multiplication:
    /// $$
    /// C = \left\\{ \begin{matrix}
    /// {\alpha AB + \beta C} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_LEFT}$}} \\\\
    /// {\alpha BA + \beta C} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_RIGHT}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $A$ is a symmetric matrix stored in lower or upper mode, $B$ and $C$ are $m \times n$ matrices, and $\alpha$ and $\beta$ are scalars.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssymm()](http://www.netlib.org/blas/ssymm.f), [dsymm()](http://www.netlib.org/blas/dsymm.f), [csymm()](http://www.netlib.org/blas/csymm.f), [zsymm()](http://www.netlib.org/blas/zsymm.f).
    pub fn cublasCsymm_v2(
        handle: cublasHandle_t,
        side: cublasSideMode_t,
        uplo: cublasFillMode_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        alpha: *const cuComplex,
        A: *const cuComplex,
        lda: ::core::ffi::c_int,
        B: *const cuComplex,
        ldb: ::core::ffi::c_int,
        beta: *const cuComplex,
        C: *mut cuComplex,
        ldc: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric matrix-matrix multiplication:
    /// $$
    /// C = \left\\{ \begin{matrix}
    /// {\alpha AB + \beta C} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_LEFT}$}} \\\\
    /// {\alpha BA + \beta C} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_RIGHT}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $A$ is a symmetric matrix stored in lower or upper mode, $B$ and $C$ are $m \times n$ matrices, and $\alpha$ and $\beta$ are scalars.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssymm()](http://www.netlib.org/blas/ssymm.f), [dsymm()](http://www.netlib.org/blas/dsymm.f), [csymm()](http://www.netlib.org/blas/csymm.f), [zsymm()](http://www.netlib.org/blas/zsymm.f).
    pub fn cublasCsymm_v2_64(
        handle: cublasHandle_t,
        side: cublasSideMode_t,
        uplo: cublasFillMode_t,
        m: i64,
        n: i64,
        alpha: *const cuComplex,
        A: *const cuComplex,
        lda: i64,
        B: *const cuComplex,
        ldb: i64,
        beta: *const cuComplex,
        C: *mut cuComplex,
        ldc: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric matrix-matrix multiplication:
    /// $$
    /// C = \left\\{ \begin{matrix}
    /// {\alpha AB + \beta C} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_LEFT}$}} \\\\
    /// {\alpha BA + \beta C} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_RIGHT}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $A$ is a symmetric matrix stored in lower or upper mode, $B$ and $C$ are $m \times n$ matrices, and $\alpha$ and $\beta$ are scalars.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssymm()](http://www.netlib.org/blas/ssymm.f), [dsymm()](http://www.netlib.org/blas/dsymm.f), [csymm()](http://www.netlib.org/blas/csymm.f), [zsymm()](http://www.netlib.org/blas/zsymm.f).
    pub fn cublasZsymm_v2(
        handle: cublasHandle_t,
        side: cublasSideMode_t,
        uplo: cublasFillMode_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        alpha: *const cuDoubleComplex,
        A: *const cuDoubleComplex,
        lda: ::core::ffi::c_int,
        B: *const cuDoubleComplex,
        ldb: ::core::ffi::c_int,
        beta: *const cuDoubleComplex,
        C: *mut cuDoubleComplex,
        ldc: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the symmetric matrix-matrix multiplication:
    /// $$
    /// C = \left\\{ \begin{matrix}
    /// {\alpha AB + \beta C} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_LEFT}$}} \\\\
    /// {\alpha BA + \beta C} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_RIGHT}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $A$ is a symmetric matrix stored in lower or upper mode, $B$ and $C$ are $m \times n$ matrices, and $\alpha$ and $\beta$ are scalars.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [ssymm()](http://www.netlib.org/blas/ssymm.f), [dsymm()](http://www.netlib.org/blas/dsymm.f), [csymm()](http://www.netlib.org/blas/csymm.f), [zsymm()](http://www.netlib.org/blas/zsymm.f).
    pub fn cublasZsymm_v2_64(
        handle: cublasHandle_t,
        side: cublasSideMode_t,
        uplo: cublasFillMode_t,
        m: i64,
        n: i64,
        alpha: *const cuDoubleComplex,
        A: *const cuDoubleComplex,
        lda: i64,
        B: *const cuDoubleComplex,
        ldb: i64,
        beta: *const cuDoubleComplex,
        C: *mut cuDoubleComplex,
        ldc: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the Hermitian matrix-matrix multiplication:
    /// $$
    /// C = \left\\{ \begin{matrix}
    /// {\alpha AB + \beta C} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_LEFT}$}} \\\\
    /// {\alpha BA + \beta C} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_RIGHT}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $A$ is a Hermitian matrix stored in lower or upper mode, $B$ and $C$ are $m \times n$ matrices, and $\alpha$ and $\beta$ are scalars.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [chemm()](http://www.netlib.org/blas/chemm.f), [zhemm()](http://www.netlib.org/blas/zhemm.f).
    pub fn cublasChemm_v2(
        handle: cublasHandle_t,
        side: cublasSideMode_t,
        uplo: cublasFillMode_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        alpha: *const cuComplex,
        A: *const cuComplex,
        lda: ::core::ffi::c_int,
        B: *const cuComplex,
        ldb: ::core::ffi::c_int,
        beta: *const cuComplex,
        C: *mut cuComplex,
        ldc: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the Hermitian matrix-matrix multiplication:
    /// $$
    /// C = \left\\{ \begin{matrix}
    /// {\alpha AB + \beta C} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_LEFT}$}} \\\\
    /// {\alpha BA + \beta C} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_RIGHT}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $A$ is a Hermitian matrix stored in lower or upper mode, $B$ and $C$ are $m \times n$ matrices, and $\alpha$ and $\beta$ are scalars.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [chemm()](http://www.netlib.org/blas/chemm.f), [zhemm()](http://www.netlib.org/blas/zhemm.f).
    pub fn cublasChemm_v2_64(
        handle: cublasHandle_t,
        side: cublasSideMode_t,
        uplo: cublasFillMode_t,
        m: i64,
        n: i64,
        alpha: *const cuComplex,
        A: *const cuComplex,
        lda: i64,
        B: *const cuComplex,
        ldb: i64,
        beta: *const cuComplex,
        C: *mut cuComplex,
        ldc: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the Hermitian matrix-matrix multiplication:
    /// $$
    /// C = \left\\{ \begin{matrix}
    /// {\alpha AB + \beta C} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_LEFT}$}} \\\\
    /// {\alpha BA + \beta C} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_RIGHT}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $A$ is a Hermitian matrix stored in lower or upper mode, $B$ and $C$ are $m \times n$ matrices, and $\alpha$ and $\beta$ are scalars.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [chemm()](http://www.netlib.org/blas/chemm.f), [zhemm()](http://www.netlib.org/blas/zhemm.f).
    pub fn cublasZhemm_v2(
        handle: cublasHandle_t,
        side: cublasSideMode_t,
        uplo: cublasFillMode_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        alpha: *const cuDoubleComplex,
        A: *const cuDoubleComplex,
        lda: ::core::ffi::c_int,
        B: *const cuDoubleComplex,
        ldb: ::core::ffi::c_int,
        beta: *const cuDoubleComplex,
        C: *mut cuDoubleComplex,
        ldc: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the Hermitian matrix-matrix multiplication:
    /// $$
    /// C = \left\\{ \begin{matrix}
    /// {\alpha AB + \beta C} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_LEFT}$}} \\\\
    /// {\alpha BA + \beta C} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_RIGHT}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $A$ is a Hermitian matrix stored in lower or upper mode, $B$ and $C$ are $m \times n$ matrices, and $\alpha$ and $\beta$ are scalars.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [chemm()](http://www.netlib.org/blas/chemm.f), [zhemm()](http://www.netlib.org/blas/zhemm.f).
    pub fn cublasZhemm_v2_64(
        handle: cublasHandle_t,
        side: cublasSideMode_t,
        uplo: cublasFillMode_t,
        m: i64,
        n: i64,
        alpha: *const cuDoubleComplex,
        A: *const cuDoubleComplex,
        lda: i64,
        B: *const cuDoubleComplex,
        ldb: i64,
        beta: *const cuDoubleComplex,
        C: *mut cuDoubleComplex,
        ldc: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function solves the triangular linear system with multiple right-hand-sides:
    /// $$
    /// \left\\{ \begin{matrix}
    /// {\text{op}(A)X = \alpha B} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_LEFT}$}} \\\\
    /// {X\text{op}(A) = \alpha B} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_RIGHT}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $A$ is a triangular matrix stored in lower or upper mode with or without the main diagonal, $X$ and $B$ are $m \times n$ matrices, and $\alpha$ is a scalar. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// The solution $X$ overwrites the right-hand-sides $B$ on exit.
    ///
    /// No test for singularity or near-singularity is included in this function.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [strsm()](http://www.netlib.org/blas/strsm.f), [dtrsm()](http://www.netlib.org/blas/dtrsm.f), [ctrsm()](http://www.netlib.org/blas/ctrsm.f), [ztrsm()](http://www.netlib.org/blas/ztrsm.f).
    pub fn cublasStrsm_v2(
        handle: cublasHandle_t,
        side: cublasSideMode_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        alpha: *const f32,
        A: *const f32,
        lda: ::core::ffi::c_int,
        B: *mut f32,
        ldb: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function solves the triangular linear system with multiple right-hand-sides:
    /// $$
    /// \left\\{ \begin{matrix}
    /// {\text{op}(A)X = \alpha B} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_LEFT}$}} \\\\
    /// {X\text{op}(A) = \alpha B} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_RIGHT}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $A$ is a triangular matrix stored in lower or upper mode with or without the main diagonal, $X$ and $B$ are $m \times n$ matrices, and $\alpha$ is a scalar. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// The solution $X$ overwrites the right-hand-sides $B$ on exit.
    ///
    /// No test for singularity or near-singularity is included in this function.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [strsm()](http://www.netlib.org/blas/strsm.f), [dtrsm()](http://www.netlib.org/blas/dtrsm.f), [ctrsm()](http://www.netlib.org/blas/ctrsm.f), [ztrsm()](http://www.netlib.org/blas/ztrsm.f).
    pub fn cublasStrsm_v2_64(
        handle: cublasHandle_t,
        side: cublasSideMode_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        m: i64,
        n: i64,
        alpha: *const f32,
        A: *const f32,
        lda: i64,
        B: *mut f32,
        ldb: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function solves the triangular linear system with multiple right-hand-sides:
    /// $$
    /// \left\\{ \begin{matrix}
    /// {\text{op}(A)X = \alpha B} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_LEFT}$}} \\\\
    /// {X\text{op}(A) = \alpha B} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_RIGHT}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $A$ is a triangular matrix stored in lower or upper mode with or without the main diagonal, $X$ and $B$ are $m \times n$ matrices, and $\alpha$ is a scalar. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// The solution $X$ overwrites the right-hand-sides $B$ on exit.
    ///
    /// No test for singularity or near-singularity is included in this function.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [strsm()](http://www.netlib.org/blas/strsm.f), [dtrsm()](http://www.netlib.org/blas/dtrsm.f), [ctrsm()](http://www.netlib.org/blas/ctrsm.f), [ztrsm()](http://www.netlib.org/blas/ztrsm.f).
    pub fn cublasDtrsm_v2(
        handle: cublasHandle_t,
        side: cublasSideMode_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        alpha: *const f64,
        A: *const f64,
        lda: ::core::ffi::c_int,
        B: *mut f64,
        ldb: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function solves the triangular linear system with multiple right-hand-sides:
    /// $$
    /// \left\\{ \begin{matrix}
    /// {\text{op}(A)X = \alpha B} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_LEFT}$}} \\\\
    /// {X\text{op}(A) = \alpha B} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_RIGHT}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $A$ is a triangular matrix stored in lower or upper mode with or without the main diagonal, $X$ and $B$ are $m \times n$ matrices, and $\alpha$ is a scalar. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// The solution $X$ overwrites the right-hand-sides $B$ on exit.
    ///
    /// No test for singularity or near-singularity is included in this function.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [strsm()](http://www.netlib.org/blas/strsm.f), [dtrsm()](http://www.netlib.org/blas/dtrsm.f), [ctrsm()](http://www.netlib.org/blas/ctrsm.f), [ztrsm()](http://www.netlib.org/blas/ztrsm.f).
    pub fn cublasDtrsm_v2_64(
        handle: cublasHandle_t,
        side: cublasSideMode_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        m: i64,
        n: i64,
        alpha: *const f64,
        A: *const f64,
        lda: i64,
        B: *mut f64,
        ldb: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function solves the triangular linear system with multiple right-hand-sides:
    /// $$
    /// \left\\{ \begin{matrix}
    /// {\text{op}(A)X = \alpha B} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_LEFT}$}} \\\\
    /// {X\text{op}(A) = \alpha B} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_RIGHT}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $A$ is a triangular matrix stored in lower or upper mode with or without the main diagonal, $X$ and $B$ are $m \times n$ matrices, and $\alpha$ is a scalar. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// The solution $X$ overwrites the right-hand-sides $B$ on exit.
    ///
    /// No test for singularity or near-singularity is included in this function.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [strsm()](http://www.netlib.org/blas/strsm.f), [dtrsm()](http://www.netlib.org/blas/dtrsm.f), [ctrsm()](http://www.netlib.org/blas/ctrsm.f), [ztrsm()](http://www.netlib.org/blas/ztrsm.f).
    pub fn cublasCtrsm_v2(
        handle: cublasHandle_t,
        side: cublasSideMode_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        alpha: *const cuComplex,
        A: *const cuComplex,
        lda: ::core::ffi::c_int,
        B: *mut cuComplex,
        ldb: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function solves the triangular linear system with multiple right-hand-sides:
    /// $$
    /// \left\\{ \begin{matrix}
    /// {\text{op}(A)X = \alpha B} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_LEFT}$}} \\\\
    /// {X\text{op}(A) = \alpha B} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_RIGHT}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $A$ is a triangular matrix stored in lower or upper mode with or without the main diagonal, $X$ and $B$ are $m \times n$ matrices, and $\alpha$ is a scalar. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// The solution $X$ overwrites the right-hand-sides $B$ on exit.
    ///
    /// No test for singularity or near-singularity is included in this function.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [strsm()](http://www.netlib.org/blas/strsm.f), [dtrsm()](http://www.netlib.org/blas/dtrsm.f), [ctrsm()](http://www.netlib.org/blas/ctrsm.f), [ztrsm()](http://www.netlib.org/blas/ztrsm.f).
    pub fn cublasCtrsm_v2_64(
        handle: cublasHandle_t,
        side: cublasSideMode_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        m: i64,
        n: i64,
        alpha: *const cuComplex,
        A: *const cuComplex,
        lda: i64,
        B: *mut cuComplex,
        ldb: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function solves the triangular linear system with multiple right-hand-sides:
    /// $$
    /// \left\\{ \begin{matrix}
    /// {\text{op}(A)X = \alpha B} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_LEFT}$}} \\\\
    /// {X\text{op}(A) = \alpha B} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_RIGHT}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $A$ is a triangular matrix stored in lower or upper mode with or without the main diagonal, $X$ and $B$ are $m \times n$ matrices, and $\alpha$ is a scalar. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// The solution $X$ overwrites the right-hand-sides $B$ on exit.
    ///
    /// No test for singularity or near-singularity is included in this function.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [strsm()](http://www.netlib.org/blas/strsm.f), [dtrsm()](http://www.netlib.org/blas/dtrsm.f), [ctrsm()](http://www.netlib.org/blas/ctrsm.f), [ztrsm()](http://www.netlib.org/blas/ztrsm.f).
    pub fn cublasZtrsm_v2(
        handle: cublasHandle_t,
        side: cublasSideMode_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        alpha: *const cuDoubleComplex,
        A: *const cuDoubleComplex,
        lda: ::core::ffi::c_int,
        B: *mut cuDoubleComplex,
        ldb: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function solves the triangular linear system with multiple right-hand-sides:
    /// $$
    /// \left\\{ \begin{matrix}
    /// {\text{op}(A)X = \alpha B} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_LEFT}$}} \\\\
    /// {X\text{op}(A) = \alpha B} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_RIGHT}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $A$ is a triangular matrix stored in lower or upper mode with or without the main diagonal, $X$ and $B$ are $m \times n$ matrices, and $\alpha$ is a scalar. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// The solution $X$ overwrites the right-hand-sides $B$ on exit.
    ///
    /// No test for singularity or near-singularity is included in this function.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [strsm()](http://www.netlib.org/blas/strsm.f), [dtrsm()](http://www.netlib.org/blas/dtrsm.f), [ctrsm()](http://www.netlib.org/blas/ctrsm.f), [ztrsm()](http://www.netlib.org/blas/ztrsm.f).
    pub fn cublasZtrsm_v2_64(
        handle: cublasHandle_t,
        side: cublasSideMode_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        m: i64,
        n: i64,
        alpha: *const cuDoubleComplex,
        A: *const cuDoubleComplex,
        lda: i64,
        B: *mut cuDoubleComplex,
        ldb: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the triangular matrix-matrix multiplication:
    /// $$
    /// C = \left\\{ \begin{matrix}
    /// {\alpha\text{op}(A)B} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_LEFT}$}} \\\\
    /// {\alpha B\text{op}(A)} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_RIGHT}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $A$ is a triangular matrix stored in lower or upper mode with or without the main diagonal, $B$ and $C$ are $m \times n$ matrix, and $\alpha$ is a scalar. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// Notice that in order to achieve better parallelism cuBLAS differs from the BLAS API only for this routine. The BLAS API assumes an in-place implementation (with results written back to B), while the cuBLAS API assumes an out-of-place implementation (with results written into C). The application can obtain the in-place functionality of BLAS in the cuBLAS API by passing the address of the matrix B in place of the matrix C. No other overlapping in the input parameters is supported.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [strmm()](http://www.netlib.org/blas/strmm.f), [dtrmm()](http://www.netlib.org/blas/dtrmm.f), [ctrmm()](http://www.netlib.org/blas/ctrmm.f), [ztrmm()](http://www.netlib.org/blas/ztrmm.f).
    pub fn cublasStrmm_v2(
        handle: cublasHandle_t,
        side: cublasSideMode_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        alpha: *const f32,
        A: *const f32,
        lda: ::core::ffi::c_int,
        B: *const f32,
        ldb: ::core::ffi::c_int,
        C: *mut f32,
        ldc: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the triangular matrix-matrix multiplication:
    /// $$
    /// C = \left\\{ \begin{matrix}
    /// {\alpha\text{op}(A)B} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_LEFT}$}} \\\\
    /// {\alpha B\text{op}(A)} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_RIGHT}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $A$ is a triangular matrix stored in lower or upper mode with or without the main diagonal, $B$ and $C$ are $m \times n$ matrix, and $\alpha$ is a scalar. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// Notice that in order to achieve better parallelism cuBLAS differs from the BLAS API only for this routine. The BLAS API assumes an in-place implementation (with results written back to B), while the cuBLAS API assumes an out-of-place implementation (with results written into C). The application can obtain the in-place functionality of BLAS in the cuBLAS API by passing the address of the matrix B in place of the matrix C. No other overlapping in the input parameters is supported.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [strmm()](http://www.netlib.org/blas/strmm.f), [dtrmm()](http://www.netlib.org/blas/dtrmm.f), [ctrmm()](http://www.netlib.org/blas/ctrmm.f), [ztrmm()](http://www.netlib.org/blas/ztrmm.f).
    pub fn cublasStrmm_v2_64(
        handle: cublasHandle_t,
        side: cublasSideMode_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        m: i64,
        n: i64,
        alpha: *const f32,
        A: *const f32,
        lda: i64,
        B: *const f32,
        ldb: i64,
        C: *mut f32,
        ldc: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the triangular matrix-matrix multiplication:
    /// $$
    /// C = \left\\{ \begin{matrix}
    /// {\alpha\text{op}(A)B} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_LEFT}$}} \\\\
    /// {\alpha B\text{op}(A)} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_RIGHT}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $A$ is a triangular matrix stored in lower or upper mode with or without the main diagonal, $B$ and $C$ are $m \times n$ matrix, and $\alpha$ is a scalar. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// Notice that in order to achieve better parallelism cuBLAS differs from the BLAS API only for this routine. The BLAS API assumes an in-place implementation (with results written back to B), while the cuBLAS API assumes an out-of-place implementation (with results written into C). The application can obtain the in-place functionality of BLAS in the cuBLAS API by passing the address of the matrix B in place of the matrix C. No other overlapping in the input parameters is supported.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [strmm()](http://www.netlib.org/blas/strmm.f), [dtrmm()](http://www.netlib.org/blas/dtrmm.f), [ctrmm()](http://www.netlib.org/blas/ctrmm.f), [ztrmm()](http://www.netlib.org/blas/ztrmm.f).
    pub fn cublasDtrmm_v2(
        handle: cublasHandle_t,
        side: cublasSideMode_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        alpha: *const f64,
        A: *const f64,
        lda: ::core::ffi::c_int,
        B: *const f64,
        ldb: ::core::ffi::c_int,
        C: *mut f64,
        ldc: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the triangular matrix-matrix multiplication:
    /// $$
    /// C = \left\\{ \begin{matrix}
    /// {\alpha\text{op}(A)B} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_LEFT}$}} \\\\
    /// {\alpha B\text{op}(A)} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_RIGHT}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $A$ is a triangular matrix stored in lower or upper mode with or without the main diagonal, $B$ and $C$ are $m \times n$ matrix, and $\alpha$ is a scalar. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// Notice that in order to achieve better parallelism cuBLAS differs from the BLAS API only for this routine. The BLAS API assumes an in-place implementation (with results written back to B), while the cuBLAS API assumes an out-of-place implementation (with results written into C). The application can obtain the in-place functionality of BLAS in the cuBLAS API by passing the address of the matrix B in place of the matrix C. No other overlapping in the input parameters is supported.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [strmm()](http://www.netlib.org/blas/strmm.f), [dtrmm()](http://www.netlib.org/blas/dtrmm.f), [ctrmm()](http://www.netlib.org/blas/ctrmm.f), [ztrmm()](http://www.netlib.org/blas/ztrmm.f).
    pub fn cublasDtrmm_v2_64(
        handle: cublasHandle_t,
        side: cublasSideMode_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        m: i64,
        n: i64,
        alpha: *const f64,
        A: *const f64,
        lda: i64,
        B: *const f64,
        ldb: i64,
        C: *mut f64,
        ldc: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the triangular matrix-matrix multiplication:
    /// $$
    /// C = \left\\{ \begin{matrix}
    /// {\alpha\text{op}(A)B} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_LEFT}$}} \\\\
    /// {\alpha B\text{op}(A)} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_RIGHT}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $A$ is a triangular matrix stored in lower or upper mode with or without the main diagonal, $B$ and $C$ are $m \times n$ matrix, and $\alpha$ is a scalar. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// Notice that in order to achieve better parallelism cuBLAS differs from the BLAS API only for this routine. The BLAS API assumes an in-place implementation (with results written back to B), while the cuBLAS API assumes an out-of-place implementation (with results written into C). The application can obtain the in-place functionality of BLAS in the cuBLAS API by passing the address of the matrix B in place of the matrix C. No other overlapping in the input parameters is supported.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [strmm()](http://www.netlib.org/blas/strmm.f), [dtrmm()](http://www.netlib.org/blas/dtrmm.f), [ctrmm()](http://www.netlib.org/blas/ctrmm.f), [ztrmm()](http://www.netlib.org/blas/ztrmm.f).
    pub fn cublasCtrmm_v2(
        handle: cublasHandle_t,
        side: cublasSideMode_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        alpha: *const cuComplex,
        A: *const cuComplex,
        lda: ::core::ffi::c_int,
        B: *const cuComplex,
        ldb: ::core::ffi::c_int,
        C: *mut cuComplex,
        ldc: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the triangular matrix-matrix multiplication:
    /// $$
    /// C = \left\\{ \begin{matrix}
    /// {\alpha\text{op}(A)B} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_LEFT}$}} \\\\
    /// {\alpha B\text{op}(A)} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_RIGHT}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $A$ is a triangular matrix stored in lower or upper mode with or without the main diagonal, $B$ and $C$ are $m \times n$ matrix, and $\alpha$ is a scalar. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// Notice that in order to achieve better parallelism cuBLAS differs from the BLAS API only for this routine. The BLAS API assumes an in-place implementation (with results written back to B), while the cuBLAS API assumes an out-of-place implementation (with results written into C). The application can obtain the in-place functionality of BLAS in the cuBLAS API by passing the address of the matrix B in place of the matrix C. No other overlapping in the input parameters is supported.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [strmm()](http://www.netlib.org/blas/strmm.f), [dtrmm()](http://www.netlib.org/blas/dtrmm.f), [ctrmm()](http://www.netlib.org/blas/ctrmm.f), [ztrmm()](http://www.netlib.org/blas/ztrmm.f).
    pub fn cublasCtrmm_v2_64(
        handle: cublasHandle_t,
        side: cublasSideMode_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        m: i64,
        n: i64,
        alpha: *const cuComplex,
        A: *const cuComplex,
        lda: i64,
        B: *const cuComplex,
        ldb: i64,
        C: *mut cuComplex,
        ldc: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the triangular matrix-matrix multiplication:
    /// $$
    /// C = \left\\{ \begin{matrix}
    /// {\alpha\text{op}(A)B} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_LEFT}$}} \\\\
    /// {\alpha B\text{op}(A)} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_RIGHT}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $A$ is a triangular matrix stored in lower or upper mode with or without the main diagonal, $B$ and $C$ are $m \times n$ matrix, and $\alpha$ is a scalar. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// Notice that in order to achieve better parallelism cuBLAS differs from the BLAS API only for this routine. The BLAS API assumes an in-place implementation (with results written back to B), while the cuBLAS API assumes an out-of-place implementation (with results written into C). The application can obtain the in-place functionality of BLAS in the cuBLAS API by passing the address of the matrix B in place of the matrix C. No other overlapping in the input parameters is supported.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [strmm()](http://www.netlib.org/blas/strmm.f), [dtrmm()](http://www.netlib.org/blas/dtrmm.f), [ctrmm()](http://www.netlib.org/blas/ctrmm.f), [ztrmm()](http://www.netlib.org/blas/ztrmm.f).
    pub fn cublasZtrmm_v2(
        handle: cublasHandle_t,
        side: cublasSideMode_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        alpha: *const cuDoubleComplex,
        A: *const cuDoubleComplex,
        lda: ::core::ffi::c_int,
        B: *const cuDoubleComplex,
        ldb: ::core::ffi::c_int,
        C: *mut cuDoubleComplex,
        ldc: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the triangular matrix-matrix multiplication:
    /// $$
    /// C = \left\\{ \begin{matrix}
    /// {\alpha\text{op}(A)B} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_LEFT}$}} \\\\
    /// {\alpha B\text{op}(A)} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_RIGHT}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $A$ is a triangular matrix stored in lower or upper mode with or without the main diagonal, $B$ and $C$ are $m \times n$ matrix, and $\alpha$ is a scalar. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// Notice that in order to achieve better parallelism cuBLAS differs from the BLAS API only for this routine. The BLAS API assumes an in-place implementation (with results written back to B), while the cuBLAS API assumes an out-of-place implementation (with results written into C). The application can obtain the in-place functionality of BLAS in the cuBLAS API by passing the address of the matrix B in place of the matrix C. No other overlapping in the input parameters is supported.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [strmm()](http://www.netlib.org/blas/strmm.f), [dtrmm()](http://www.netlib.org/blas/dtrmm.f), [ctrmm()](http://www.netlib.org/blas/ctrmm.f), [ztrmm()](http://www.netlib.org/blas/ztrmm.f).
    pub fn cublasZtrmm_v2_64(
        handle: cublasHandle_t,
        side: cublasSideMode_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        m: i64,
        n: i64,
        alpha: *const cuDoubleComplex,
        A: *const cuDoubleComplex,
        lda: i64,
        B: *const cuDoubleComplex,
        ldb: i64,
        C: *mut cuDoubleComplex,
        ldc: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-matrix multiplication of a batch of matrices. The batch is considered to be “uniform”, i.e. all instances have the same dimensions (m, n, k), leading dimensions (lda, ldb, ldc) and transpositions (transa, transb) for their respective A, B and C matrices. The address of the input matrices and the output matrix of each instance of the batch are read from arrays of pointers passed to the function by the caller.
    /// $$
    /// C\lbrack i\rbrack = \alpha\text{op}(A\lbrack i\rbrack)\text{op}(B\lbrack i\rbrack) + \beta C\lbrack i\rbrack,\text{ for i } \in \lbrack 0,batchCount - 1\rbrack
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$, $B$ and $C$ are arrays of pointers to matrices stored in column-major format with dimensions $\text{op}(A\lbrack i\rbrack)$ $m \times k$, $\text{op}(B\lbrack i\rbrack)$ $k \times n$ and $C\lbrack i\rbrack$ $m \times n$, respectively. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B\lbrack i\rbrack)$ is defined similarly for matrix $B\lbrack i\rbrack$.
    ///
    /// On certain problem sizes, it might be advantageous to make multiple calls to cublas&lt;t>gemm() in different CUDA streams, rather than use this API.
    ///
    /// If math mode enables fast math modes when using [`cublasSgemmBatched`], pointers (not the pointer arrays) placed in the GPU memory must be properly aligned to avoid misaligned memory access errors. Ideally all pointers are aligned to at least 16 Bytes. Otherwise it is recommended that they meet the following rule.
    pub fn cublasSgemmBatched(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        alpha: *const f32,
        Aarray: *const *const f32,
        lda: ::core::ffi::c_int,
        Barray: *const *const f32,
        ldb: ::core::ffi::c_int,
        beta: *const f32,
        Carray: *const *mut f32,
        ldc: ::core::ffi::c_int,
        batchCount: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-matrix multiplication of a batch of matrices. The batch is considered to be “uniform”, i.e. all instances have the same dimensions (m, n, k), leading dimensions (lda, ldb, ldc) and transpositions (transa, transb) for their respective A, B and C matrices. The address of the input matrices and the output matrix of each instance of the batch are read from arrays of pointers passed to the function by the caller.
    /// $$
    /// C\lbrack i\rbrack = \alpha\text{op}(A\lbrack i\rbrack)\text{op}(B\lbrack i\rbrack) + \beta C\lbrack i\rbrack,\text{ for i } \in \lbrack 0,batchCount - 1\rbrack
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$, $B$ and $C$ are arrays of pointers to matrices stored in column-major format with dimensions $\text{op}(A\lbrack i\rbrack)$ $m \times k$, $\text{op}(B\lbrack i\rbrack)$ $k \times n$ and $C\lbrack i\rbrack$ $m \times n$, respectively. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B\lbrack i\rbrack)$ is defined similarly for matrix $B\lbrack i\rbrack$.
    ///
    /// On certain problem sizes, it might be advantageous to make multiple calls to cublas&lt;t>gemm() in different CUDA streams, rather than use this API.
    ///
    /// If math mode enables fast math modes when using [`cublasSgemmBatched`], pointers (not the pointer arrays) placed in the GPU memory must be properly aligned to avoid misaligned memory access errors. Ideally all pointers are aligned to at least 16 Bytes. Otherwise it is recommended that they meet the following rule.
    pub fn cublasSgemmBatched_64(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: i64,
        n: i64,
        k: i64,
        alpha: *const f32,
        Aarray: *const *const f32,
        lda: i64,
        Barray: *const *const f32,
        ldb: i64,
        beta: *const f32,
        Carray: *const *mut f32,
        ldc: i64,
        batchCount: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-matrix multiplication of a batch of matrices. The batch is considered to be “uniform”, i.e. all instances have the same dimensions (m, n, k), leading dimensions (lda, ldb, ldc) and transpositions (transa, transb) for their respective A, B and C matrices. The address of the input matrices and the output matrix of each instance of the batch are read from arrays of pointers passed to the function by the caller.
    /// $$
    /// C\lbrack i\rbrack = \alpha\text{op}(A\lbrack i\rbrack)\text{op}(B\lbrack i\rbrack) + \beta C\lbrack i\rbrack,\text{ for i } \in \lbrack 0,batchCount - 1\rbrack
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$, $B$ and $C$ are arrays of pointers to matrices stored in column-major format with dimensions $\text{op}(A\lbrack i\rbrack)$ $m \times k$, $\text{op}(B\lbrack i\rbrack)$ $k \times n$ and $C\lbrack i\rbrack$ $m \times n$, respectively. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B\lbrack i\rbrack)$ is defined similarly for matrix $B\lbrack i\rbrack$.
    ///
    /// On certain problem sizes, it might be advantageous to make multiple calls to cublas&lt;t>gemm() in different CUDA streams, rather than use this API.
    ///
    /// If math mode enables fast math modes when using [`cublasSgemmBatched`], pointers (not the pointer arrays) placed in the GPU memory must be properly aligned to avoid misaligned memory access errors. Ideally all pointers are aligned to at least 16 Bytes. Otherwise it is recommended that they meet the following rule.
    pub fn cublasDgemmBatched(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        alpha: *const f64,
        Aarray: *const *const f64,
        lda: ::core::ffi::c_int,
        Barray: *const *const f64,
        ldb: ::core::ffi::c_int,
        beta: *const f64,
        Carray: *const *mut f64,
        ldc: ::core::ffi::c_int,
        batchCount: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-matrix multiplication of a batch of matrices. The batch is considered to be “uniform”, i.e. all instances have the same dimensions (m, n, k), leading dimensions (lda, ldb, ldc) and transpositions (transa, transb) for their respective A, B and C matrices. The address of the input matrices and the output matrix of each instance of the batch are read from arrays of pointers passed to the function by the caller.
    /// $$
    /// C\lbrack i\rbrack = \alpha\text{op}(A\lbrack i\rbrack)\text{op}(B\lbrack i\rbrack) + \beta C\lbrack i\rbrack,\text{ for i } \in \lbrack 0,batchCount - 1\rbrack
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$, $B$ and $C$ are arrays of pointers to matrices stored in column-major format with dimensions $\text{op}(A\lbrack i\rbrack)$ $m \times k$, $\text{op}(B\lbrack i\rbrack)$ $k \times n$ and $C\lbrack i\rbrack$ $m \times n$, respectively. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B\lbrack i\rbrack)$ is defined similarly for matrix $B\lbrack i\rbrack$.
    ///
    /// On certain problem sizes, it might be advantageous to make multiple calls to cublas&lt;t>gemm() in different CUDA streams, rather than use this API.
    ///
    /// If math mode enables fast math modes when using [`cublasSgemmBatched`], pointers (not the pointer arrays) placed in the GPU memory must be properly aligned to avoid misaligned memory access errors. Ideally all pointers are aligned to at least 16 Bytes. Otherwise it is recommended that they meet the following rule.
    pub fn cublasDgemmBatched_64(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: i64,
        n: i64,
        k: i64,
        alpha: *const f64,
        Aarray: *const *const f64,
        lda: i64,
        Barray: *const *const f64,
        ldb: i64,
        beta: *const f64,
        Carray: *const *mut f64,
        ldc: i64,
        batchCount: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-matrix multiplication of a batch of matrices. The batch is considered to be “uniform”, i.e. all instances have the same dimensions (m, n, k), leading dimensions (lda, ldb, ldc) and transpositions (transa, transb) for their respective A, B and C matrices. The address of the input matrices and the output matrix of each instance of the batch are read from arrays of pointers passed to the function by the caller.
    /// $$
    /// C\lbrack i\rbrack = \alpha\text{op}(A\lbrack i\rbrack)\text{op}(B\lbrack i\rbrack) + \beta C\lbrack i\rbrack,\text{ for i } \in \lbrack 0,batchCount - 1\rbrack
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$, $B$ and $C$ are arrays of pointers to matrices stored in column-major format with dimensions $\text{op}(A\lbrack i\rbrack)$ $m \times k$, $\text{op}(B\lbrack i\rbrack)$ $k \times n$ and $C\lbrack i\rbrack$ $m \times n$, respectively. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B\lbrack i\rbrack)$ is defined similarly for matrix $B\lbrack i\rbrack$.
    ///
    /// On certain problem sizes, it might be advantageous to make multiple calls to cublas&lt;t>gemm() in different CUDA streams, rather than use this API.
    ///
    /// If math mode enables fast math modes when using [`cublasSgemmBatched`], pointers (not the pointer arrays) placed in the GPU memory must be properly aligned to avoid misaligned memory access errors. Ideally all pointers are aligned to at least 16 Bytes. Otherwise it is recommended that they meet the following rule.
    pub fn cublasCgemmBatched(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        alpha: *const cuComplex,
        Aarray: *const *const cuComplex,
        lda: ::core::ffi::c_int,
        Barray: *const *const cuComplex,
        ldb: ::core::ffi::c_int,
        beta: *const cuComplex,
        Carray: *const *mut cuComplex,
        ldc: ::core::ffi::c_int,
        batchCount: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-matrix multiplication of a batch of matrices. The batch is considered to be “uniform”, i.e. all instances have the same dimensions (m, n, k), leading dimensions (lda, ldb, ldc) and transpositions (transa, transb) for their respective A, B and C matrices. The address of the input matrices and the output matrix of each instance of the batch are read from arrays of pointers passed to the function by the caller.
    /// $$
    /// C\lbrack i\rbrack = \alpha\text{op}(A\lbrack i\rbrack)\text{op}(B\lbrack i\rbrack) + \beta C\lbrack i\rbrack,\text{ for i } \in \lbrack 0,batchCount - 1\rbrack
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$, $B$ and $C$ are arrays of pointers to matrices stored in column-major format with dimensions $\text{op}(A\lbrack i\rbrack)$ $m \times k$, $\text{op}(B\lbrack i\rbrack)$ $k \times n$ and $C\lbrack i\rbrack$ $m \times n$, respectively. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B\lbrack i\rbrack)$ is defined similarly for matrix $B\lbrack i\rbrack$.
    ///
    /// On certain problem sizes, it might be advantageous to make multiple calls to cublas&lt;t>gemm() in different CUDA streams, rather than use this API.
    ///
    /// If math mode enables fast math modes when using [`cublasSgemmBatched`], pointers (not the pointer arrays) placed in the GPU memory must be properly aligned to avoid misaligned memory access errors. Ideally all pointers are aligned to at least 16 Bytes. Otherwise it is recommended that they meet the following rule.
    pub fn cublasCgemmBatched_64(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: i64,
        n: i64,
        k: i64,
        alpha: *const cuComplex,
        Aarray: *const *const cuComplex,
        lda: i64,
        Barray: *const *const cuComplex,
        ldb: i64,
        beta: *const cuComplex,
        Carray: *const *mut cuComplex,
        ldc: i64,
        batchCount: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    pub fn cublasCgemm3mBatched(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        alpha: *const cuComplex,
        Aarray: *const *const cuComplex,
        lda: ::core::ffi::c_int,
        Barray: *const *const cuComplex,
        ldb: ::core::ffi::c_int,
        beta: *const cuComplex,
        Carray: *const *mut cuComplex,
        ldc: ::core::ffi::c_int,
        batchCount: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    pub fn cublasCgemm3mBatched_64(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: i64,
        n: i64,
        k: i64,
        alpha: *const cuComplex,
        Aarray: *const *const cuComplex,
        lda: i64,
        Barray: *const *const cuComplex,
        ldb: i64,
        beta: *const cuComplex,
        Carray: *const *mut cuComplex,
        ldc: i64,
        batchCount: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-matrix multiplication of a batch of matrices. The batch is considered to be “uniform”, i.e. all instances have the same dimensions (m, n, k), leading dimensions (lda, ldb, ldc) and transpositions (transa, transb) for their respective A, B and C matrices. The address of the input matrices and the output matrix of each instance of the batch are read from arrays of pointers passed to the function by the caller.
    /// $$
    /// C\lbrack i\rbrack = \alpha\text{op}(A\lbrack i\rbrack)\text{op}(B\lbrack i\rbrack) + \beta C\lbrack i\rbrack,\text{ for i } \in \lbrack 0,batchCount - 1\rbrack
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$, $B$ and $C$ are arrays of pointers to matrices stored in column-major format with dimensions $\text{op}(A\lbrack i\rbrack)$ $m \times k$, $\text{op}(B\lbrack i\rbrack)$ $k \times n$ and $C\lbrack i\rbrack$ $m \times n$, respectively. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B\lbrack i\rbrack)$ is defined similarly for matrix $B\lbrack i\rbrack$.
    ///
    /// On certain problem sizes, it might be advantageous to make multiple calls to cublas&lt;t>gemm() in different CUDA streams, rather than use this API.
    ///
    /// If math mode enables fast math modes when using [`cublasSgemmBatched`], pointers (not the pointer arrays) placed in the GPU memory must be properly aligned to avoid misaligned memory access errors. Ideally all pointers are aligned to at least 16 Bytes. Otherwise it is recommended that they meet the following rule.
    pub fn cublasZgemmBatched(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        alpha: *const cuDoubleComplex,
        Aarray: *const *const cuDoubleComplex,
        lda: ::core::ffi::c_int,
        Barray: *const *const cuDoubleComplex,
        ldb: ::core::ffi::c_int,
        beta: *const cuDoubleComplex,
        Carray: *const *mut cuDoubleComplex,
        ldc: ::core::ffi::c_int,
        batchCount: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-matrix multiplication of a batch of matrices. The batch is considered to be “uniform”, i.e. all instances have the same dimensions (m, n, k), leading dimensions (lda, ldb, ldc) and transpositions (transa, transb) for their respective A, B and C matrices. The address of the input matrices and the output matrix of each instance of the batch are read from arrays of pointers passed to the function by the caller.
    /// $$
    /// C\lbrack i\rbrack = \alpha\text{op}(A\lbrack i\rbrack)\text{op}(B\lbrack i\rbrack) + \beta C\lbrack i\rbrack,\text{ for i } \in \lbrack 0,batchCount - 1\rbrack
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$, $B$ and $C$ are arrays of pointers to matrices stored in column-major format with dimensions $\text{op}(A\lbrack i\rbrack)$ $m \times k$, $\text{op}(B\lbrack i\rbrack)$ $k \times n$ and $C\lbrack i\rbrack$ $m \times n$, respectively. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B\lbrack i\rbrack)$ is defined similarly for matrix $B\lbrack i\rbrack$.
    ///
    /// On certain problem sizes, it might be advantageous to make multiple calls to cublas&lt;t>gemm() in different CUDA streams, rather than use this API.
    ///
    /// If math mode enables fast math modes when using [`cublasSgemmBatched`], pointers (not the pointer arrays) placed in the GPU memory must be properly aligned to avoid misaligned memory access errors. Ideally all pointers are aligned to at least 16 Bytes. Otherwise it is recommended that they meet the following rule.
    pub fn cublasZgemmBatched_64(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: i64,
        n: i64,
        k: i64,
        alpha: *const cuDoubleComplex,
        Aarray: *const *const cuDoubleComplex,
        lda: i64,
        Barray: *const *const cuDoubleComplex,
        ldb: i64,
        beta: *const cuDoubleComplex,
        Carray: *const *mut cuDoubleComplex,
        ldc: i64,
        batchCount: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-matrix multiplication of a batch of matrices. The batch is considered to be “uniform”, i.e. all instances have the same dimensions (m, n, k), leading dimensions (lda, ldb, ldc) and transpositions (transa, transb) for their respective A, B and C matrices. Input matrices A, B and output matrix C for each instance of the batch are located at fixed offsets in number of elements from their locations in the previous instance. Pointers to A, B and C matrices for the first instance are passed to the function by the user along with offsets in number of elements - strideA, strideB and strideC that determine the locations of input and output matrices in future instances.
    /// $$
    /// C + i\\*{strideC} = \alpha\text{op}(A + i\\*{strideA})\text{op}(B + i\\*{strideB}) + \beta(C + i\\*{strideC}),\text{ for i } \in \lbrack 0,batchCount - 1\rbrack
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$, $B$ and $C$ are arrays of pointers to matrices stored in column-major format with dimensions $\text{op}(A\lbrack i\rbrack)$ $m \times k$, $\text{op}(B\lbrack i\rbrack)$ $k \times n$ and $C\lbrack i\rbrack$ $m \times n$, respectively. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B\lbrack i\rbrack)$ is defined similarly for matrix $B\lbrack i\rbrack$.
    ///
    /// On certain problem sizes, it might be advantageous to make multiple calls to cublas&lt;t>gemm() in different CUDA streams, rather than use this API.
    pub fn cublasSgemmStridedBatched(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        alpha: *const f32,
        A: *const f32,
        lda: ::core::ffi::c_int,
        strideA: ::core::ffi::c_longlong,
        B: *const f32,
        ldb: ::core::ffi::c_int,
        strideB: ::core::ffi::c_longlong,
        beta: *const f32,
        C: *mut f32,
        ldc: ::core::ffi::c_int,
        strideC: ::core::ffi::c_longlong,
        batchCount: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-matrix multiplication of a batch of matrices. The batch is considered to be “uniform”, i.e. all instances have the same dimensions (m, n, k), leading dimensions (lda, ldb, ldc) and transpositions (transa, transb) for their respective A, B and C matrices. Input matrices A, B and output matrix C for each instance of the batch are located at fixed offsets in number of elements from their locations in the previous instance. Pointers to A, B and C matrices for the first instance are passed to the function by the user along with offsets in number of elements - strideA, strideB and strideC that determine the locations of input and output matrices in future instances.
    /// $$
    /// C + i\\*{strideC} = \alpha\text{op}(A + i\\*{strideA})\text{op}(B + i\\*{strideB}) + \beta(C + i\\*{strideC}),\text{ for i } \in \lbrack 0,batchCount - 1\rbrack
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$, $B$ and $C$ are arrays of pointers to matrices stored in column-major format with dimensions $\text{op}(A\lbrack i\rbrack)$ $m \times k$, $\text{op}(B\lbrack i\rbrack)$ $k \times n$ and $C\lbrack i\rbrack$ $m \times n$, respectively. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B\lbrack i\rbrack)$ is defined similarly for matrix $B\lbrack i\rbrack$.
    ///
    /// On certain problem sizes, it might be advantageous to make multiple calls to cublas&lt;t>gemm() in different CUDA streams, rather than use this API.
    pub fn cublasSgemmStridedBatched_64(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: i64,
        n: i64,
        k: i64,
        alpha: *const f32,
        A: *const f32,
        lda: i64,
        strideA: ::core::ffi::c_longlong,
        B: *const f32,
        ldb: i64,
        strideB: ::core::ffi::c_longlong,
        beta: *const f32,
        C: *mut f32,
        ldc: i64,
        strideC: ::core::ffi::c_longlong,
        batchCount: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-matrix multiplication of a batch of matrices. The batch is considered to be “uniform”, i.e. all instances have the same dimensions (m, n, k), leading dimensions (lda, ldb, ldc) and transpositions (transa, transb) for their respective A, B and C matrices. Input matrices A, B and output matrix C for each instance of the batch are located at fixed offsets in number of elements from their locations in the previous instance. Pointers to A, B and C matrices for the first instance are passed to the function by the user along with offsets in number of elements - strideA, strideB and strideC that determine the locations of input and output matrices in future instances.
    /// $$
    /// C + i\\*{strideC} = \alpha\text{op}(A + i\\*{strideA})\text{op}(B + i\\*{strideB}) + \beta(C + i\\*{strideC}),\text{ for i } \in \lbrack 0,batchCount - 1\rbrack
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$, $B$ and $C$ are arrays of pointers to matrices stored in column-major format with dimensions $\text{op}(A\lbrack i\rbrack)$ $m \times k$, $\text{op}(B\lbrack i\rbrack)$ $k \times n$ and $C\lbrack i\rbrack$ $m \times n$, respectively. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B\lbrack i\rbrack)$ is defined similarly for matrix $B\lbrack i\rbrack$.
    ///
    /// On certain problem sizes, it might be advantageous to make multiple calls to cublas&lt;t>gemm() in different CUDA streams, rather than use this API.
    pub fn cublasDgemmStridedBatched(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        alpha: *const f64,
        A: *const f64,
        lda: ::core::ffi::c_int,
        strideA: ::core::ffi::c_longlong,
        B: *const f64,
        ldb: ::core::ffi::c_int,
        strideB: ::core::ffi::c_longlong,
        beta: *const f64,
        C: *mut f64,
        ldc: ::core::ffi::c_int,
        strideC: ::core::ffi::c_longlong,
        batchCount: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-matrix multiplication of a batch of matrices. The batch is considered to be “uniform”, i.e. all instances have the same dimensions (m, n, k), leading dimensions (lda, ldb, ldc) and transpositions (transa, transb) for their respective A, B and C matrices. Input matrices A, B and output matrix C for each instance of the batch are located at fixed offsets in number of elements from their locations in the previous instance. Pointers to A, B and C matrices for the first instance are passed to the function by the user along with offsets in number of elements - strideA, strideB and strideC that determine the locations of input and output matrices in future instances.
    /// $$
    /// C + i\\*{strideC} = \alpha\text{op}(A + i\\*{strideA})\text{op}(B + i\\*{strideB}) + \beta(C + i\\*{strideC}),\text{ for i } \in \lbrack 0,batchCount - 1\rbrack
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$, $B$ and $C$ are arrays of pointers to matrices stored in column-major format with dimensions $\text{op}(A\lbrack i\rbrack)$ $m \times k$, $\text{op}(B\lbrack i\rbrack)$ $k \times n$ and $C\lbrack i\rbrack$ $m \times n$, respectively. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B\lbrack i\rbrack)$ is defined similarly for matrix $B\lbrack i\rbrack$.
    ///
    /// On certain problem sizes, it might be advantageous to make multiple calls to cublas&lt;t>gemm() in different CUDA streams, rather than use this API.
    pub fn cublasDgemmStridedBatched_64(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: i64,
        n: i64,
        k: i64,
        alpha: *const f64,
        A: *const f64,
        lda: i64,
        strideA: ::core::ffi::c_longlong,
        B: *const f64,
        ldb: i64,
        strideB: ::core::ffi::c_longlong,
        beta: *const f64,
        C: *mut f64,
        ldc: i64,
        strideC: ::core::ffi::c_longlong,
        batchCount: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-matrix multiplication of a batch of matrices. The batch is considered to be “uniform”, i.e. all instances have the same dimensions (m, n, k), leading dimensions (lda, ldb, ldc) and transpositions (transa, transb) for their respective A, B and C matrices. Input matrices A, B and output matrix C for each instance of the batch are located at fixed offsets in number of elements from their locations in the previous instance. Pointers to A, B and C matrices for the first instance are passed to the function by the user along with offsets in number of elements - strideA, strideB and strideC that determine the locations of input and output matrices in future instances.
    /// $$
    /// C + i\\*{strideC} = \alpha\text{op}(A + i\\*{strideA})\text{op}(B + i\\*{strideB}) + \beta(C + i\\*{strideC}),\text{ for i } \in \lbrack 0,batchCount - 1\rbrack
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$, $B$ and $C$ are arrays of pointers to matrices stored in column-major format with dimensions $\text{op}(A\lbrack i\rbrack)$ $m \times k$, $\text{op}(B\lbrack i\rbrack)$ $k \times n$ and $C\lbrack i\rbrack$ $m \times n$, respectively. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B\lbrack i\rbrack)$ is defined similarly for matrix $B\lbrack i\rbrack$.
    ///
    /// On certain problem sizes, it might be advantageous to make multiple calls to cublas&lt;t>gemm() in different CUDA streams, rather than use this API.
    pub fn cublasCgemmStridedBatched(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        alpha: *const cuComplex,
        A: *const cuComplex,
        lda: ::core::ffi::c_int,
        strideA: ::core::ffi::c_longlong,
        B: *const cuComplex,
        ldb: ::core::ffi::c_int,
        strideB: ::core::ffi::c_longlong,
        beta: *const cuComplex,
        C: *mut cuComplex,
        ldc: ::core::ffi::c_int,
        strideC: ::core::ffi::c_longlong,
        batchCount: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-matrix multiplication of a batch of matrices. The batch is considered to be “uniform”, i.e. all instances have the same dimensions (m, n, k), leading dimensions (lda, ldb, ldc) and transpositions (transa, transb) for their respective A, B and C matrices. Input matrices A, B and output matrix C for each instance of the batch are located at fixed offsets in number of elements from their locations in the previous instance. Pointers to A, B and C matrices for the first instance are passed to the function by the user along with offsets in number of elements - strideA, strideB and strideC that determine the locations of input and output matrices in future instances.
    /// $$
    /// C + i\\*{strideC} = \alpha\text{op}(A + i\\*{strideA})\text{op}(B + i\\*{strideB}) + \beta(C + i\\*{strideC}),\text{ for i } \in \lbrack 0,batchCount - 1\rbrack
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$, $B$ and $C$ are arrays of pointers to matrices stored in column-major format with dimensions $\text{op}(A\lbrack i\rbrack)$ $m \times k$, $\text{op}(B\lbrack i\rbrack)$ $k \times n$ and $C\lbrack i\rbrack$ $m \times n$, respectively. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B\lbrack i\rbrack)$ is defined similarly for matrix $B\lbrack i\rbrack$.
    ///
    /// On certain problem sizes, it might be advantageous to make multiple calls to cublas&lt;t>gemm() in different CUDA streams, rather than use this API.
    pub fn cublasCgemmStridedBatched_64(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: i64,
        n: i64,
        k: i64,
        alpha: *const cuComplex,
        A: *const cuComplex,
        lda: i64,
        strideA: ::core::ffi::c_longlong,
        B: *const cuComplex,
        ldb: i64,
        strideB: ::core::ffi::c_longlong,
        beta: *const cuComplex,
        C: *mut cuComplex,
        ldc: i64,
        strideC: ::core::ffi::c_longlong,
        batchCount: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    pub fn cublasCgemm3mStridedBatched(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        alpha: *const cuComplex,
        A: *const cuComplex,
        lda: ::core::ffi::c_int,
        strideA: ::core::ffi::c_longlong,
        B: *const cuComplex,
        ldb: ::core::ffi::c_int,
        strideB: ::core::ffi::c_longlong,
        beta: *const cuComplex,
        C: *mut cuComplex,
        ldc: ::core::ffi::c_int,
        strideC: ::core::ffi::c_longlong,
        batchCount: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    pub fn cublasCgemm3mStridedBatched_64(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: i64,
        n: i64,
        k: i64,
        alpha: *const cuComplex,
        A: *const cuComplex,
        lda: i64,
        strideA: ::core::ffi::c_longlong,
        B: *const cuComplex,
        ldb: i64,
        strideB: ::core::ffi::c_longlong,
        beta: *const cuComplex,
        C: *mut cuComplex,
        ldc: i64,
        strideC: ::core::ffi::c_longlong,
        batchCount: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-matrix multiplication of a batch of matrices. The batch is considered to be “uniform”, i.e. all instances have the same dimensions (m, n, k), leading dimensions (lda, ldb, ldc) and transpositions (transa, transb) for their respective A, B and C matrices. Input matrices A, B and output matrix C for each instance of the batch are located at fixed offsets in number of elements from their locations in the previous instance. Pointers to A, B and C matrices for the first instance are passed to the function by the user along with offsets in number of elements - strideA, strideB and strideC that determine the locations of input and output matrices in future instances.
    /// $$
    /// C + i\\*{strideC} = \alpha\text{op}(A + i\\*{strideA})\text{op}(B + i\\*{strideB}) + \beta(C + i\\*{strideC}),\text{ for i } \in \lbrack 0,batchCount - 1\rbrack
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$, $B$ and $C$ are arrays of pointers to matrices stored in column-major format with dimensions $\text{op}(A\lbrack i\rbrack)$ $m \times k$, $\text{op}(B\lbrack i\rbrack)$ $k \times n$ and $C\lbrack i\rbrack$ $m \times n$, respectively. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B\lbrack i\rbrack)$ is defined similarly for matrix $B\lbrack i\rbrack$.
    ///
    /// On certain problem sizes, it might be advantageous to make multiple calls to cublas&lt;t>gemm() in different CUDA streams, rather than use this API.
    pub fn cublasZgemmStridedBatched(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        alpha: *const cuDoubleComplex,
        A: *const cuDoubleComplex,
        lda: ::core::ffi::c_int,
        strideA: ::core::ffi::c_longlong,
        B: *const cuDoubleComplex,
        ldb: ::core::ffi::c_int,
        strideB: ::core::ffi::c_longlong,
        beta: *const cuDoubleComplex,
        C: *mut cuDoubleComplex,
        ldc: ::core::ffi::c_int,
        strideC: ::core::ffi::c_longlong,
        batchCount: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-matrix multiplication of a batch of matrices. The batch is considered to be “uniform”, i.e. all instances have the same dimensions (m, n, k), leading dimensions (lda, ldb, ldc) and transpositions (transa, transb) for their respective A, B and C matrices. Input matrices A, B and output matrix C for each instance of the batch are located at fixed offsets in number of elements from their locations in the previous instance. Pointers to A, B and C matrices for the first instance are passed to the function by the user along with offsets in number of elements - strideA, strideB and strideC that determine the locations of input and output matrices in future instances.
    /// $$
    /// C + i\\*{strideC} = \alpha\text{op}(A + i\\*{strideA})\text{op}(B + i\\*{strideB}) + \beta(C + i\\*{strideC}),\text{ for i } \in \lbrack 0,batchCount - 1\rbrack
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$, $B$ and $C$ are arrays of pointers to matrices stored in column-major format with dimensions $\text{op}(A\lbrack i\rbrack)$ $m \times k$, $\text{op}(B\lbrack i\rbrack)$ $k \times n$ and $C\lbrack i\rbrack$ $m \times n$, respectively. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B\lbrack i\rbrack)$ is defined similarly for matrix $B\lbrack i\rbrack$.
    ///
    /// On certain problem sizes, it might be advantageous to make multiple calls to cublas&lt;t>gemm() in different CUDA streams, rather than use this API.
    pub fn cublasZgemmStridedBatched_64(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: i64,
        n: i64,
        k: i64,
        alpha: *const cuDoubleComplex,
        A: *const cuDoubleComplex,
        lda: i64,
        strideA: ::core::ffi::c_longlong,
        B: *const cuDoubleComplex,
        ldb: i64,
        strideB: ::core::ffi::c_longlong,
        beta: *const cuDoubleComplex,
        C: *mut cuDoubleComplex,
        ldc: i64,
        strideC: ::core::ffi::c_longlong,
        batchCount: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function is an extension of cublas&lt;t>gemmBatched() that performs the matrix-matrix multiplication of a batch of matrices and allows the user to individually specify the data types for each of the A, B and C matrix arrays, the precision of computation and the GEMM algorithm to be run. Like cublas&lt;t>gemmBatched(), the batch is considered to be “uniform”, i.e. all instances have the same dimensions (m, n, k), leading dimensions (lda, ldb, ldc) and transpositions (transa, transb) for their respective A, B and C matrices. The address of the input matrices and the output matrix of each instance of the batch are read from arrays of pointers passed to the function by the caller. Supported combinations of arguments are listed further down in this section.
    /// $$
    /// C\lbrack i\rbrack = \alpha\text{op}(A\lbrack i\rbrack)\text{op}(B\lbrack i\rbrack) + \beta C\lbrack i\rbrack,\text{ for i } \in \lbrack 0,batchCount - 1\rbrack
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$, $B$ and $C$ are arrays of pointers to matrices stored in column-major format with dimensions $\text{op}(A\lbrack i\rbrack)$ $m \times k$, $\text{op}(B\lbrack i\rbrack)$ $k \times n$ and $C\lbrack i\rbrack$ $m \times n$, respectively. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B\lbrack i\rbrack)$ is defined similarly for matrix $B\lbrack i\rbrack$.
    ///
    /// On certain problem sizes, it might be advantageous to make multiple calls to cublas&lt;t>gemm() in different CUDA streams, rather than use this API.
    ///
    /// [`cublasGemmBatchedEx`] supports the following Compute Type, Scale Type, Atype/Btype, and Ctype:
    ///
    /// If `Atype` is `cudaDataType_t::CUDA_R_16F` or `cudaDataType_t::CUDA_R_16BF`, or `computeType` is any of the `FAST` options, or when math mode or `algo` enable fast math modes, pointers (not the pointer arrays) placed in the GPU memory must be properly aligned to avoid misaligned memory access errors. Ideally all pointers are aligned to at least 16 Bytes. Otherwise it is recommended that they meet the following rule:
    ///
    /// Also refer to: [sgemm.()](http://www.netlib.org/blas/sgemm.f).
    ///
    /// # Parameters
    ///
    /// - `handle`: Handle to the cuBLAS library context.
    /// - `transa`: Operation op(`Aarray\[i\]`) that is non- or (conj.) transpose.
    /// - `transb`: Operation op(`Barray\[i\]`) that is non- or (conj.) transpose.
    /// - `m`: Number of rows of matrix op(`Aarray\[i\]`) and `Carray\[i\]`.
    /// - `n`: Number of columns of matrix op(`Barray\[i\]`) and `Carray\[i\]`.
    /// - `k`: Number of columns of op(`Aarray\[i\]`) and rows of op(`Barray\[i\]`).
    /// - `alpha`: Scaling factor for matrix products of the type that corresponds to the computeType and Ctype, see the table below for details.
    /// - `Aarray`: Array of pointers to &lt;`Atype`> array, with each array of dim. `lda x k` with `lda >= max(1, m)` if `transa == CUBLAS_OP_N` and `lda x m` with `lda >= max(1, k)` otherwise. All pointers must meet certain alignment criteria. Please see below for details.
    /// - `Atype`: Enumerant specifying the datatype of `Aarray`.
    /// - `lda`: Leading dimension of two-dimensional array used to store the matrix `Aarray\[i\]`.
    /// - `Barray`: Array of pointers to &lt;`Btype`> array, with each array of dim. `ldb x n` with `ldb >= max(1, k)` if `transb == CUBLAS_OP_N` and `ldb x k` with `ldb >= max(1,n)` otherwise. All pointers must meet certain alignment criteria. Please see below for details.
    /// - `Btype`: Enumerant specifying the datatype of `Barray`.
    /// - `ldb`: Leading dimension of two-dimensional array used to store matrix `Barray\[i\]`.
    /// - `beta`: Scaling factor for `Carray` of the type that corresponds to the computeType and Ctype, see the table below for details. If `beta == 0`, `Carray\[i\]` does not have to be a valid input.
    /// - `Carray`: Array of pointers to &lt;`Ctype`> array. It has dimensions `ldc x n` with `ldc >= max(1, m)`. Matrices `Carray\[i\]` should not overlap; otherwise, the behavior is undefined. All pointers must meet certain alignment criteria. Please see below for details.
    /// - `Ctype`: Enumerant specifying the datatype of `Carray`.
    /// - `ldc`: Leading dimension of a two-dimensional array used to store each matrix `Carray\[i\]`.
    /// - `batchCount`: Number of pointers contained in `Aarray`, `Barray` and `Carray`.
    /// - `computeType`: Enumerant specifying the computation type.
    /// - `algo`: Enumerant specifying the algorithm. See [`cublasGemmAlgo_t`].
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_ARCH_MISMATCH`]: [`cublasGemmBatchedEx`] is only supported for GPU with architecture capabilities equal to or greater than 5.0.
    /// - [`cublasStatus_t::CUBLAS_STATUS_EXECUTION_FAILED`]: The function failed to launch on the GPU.
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: If `m &lt; 0` or `n &lt; 0` or `k &lt; 0`, or if `transa` and `transb` are not one of [`cublasOperation_t::CUBLAS_OP_N`], [`cublasOperation_t::CUBLAS_OP_C`], [`cublasOperation_t::CUBLAS_OP_T`], or if `lda &lt; max(1, m)` when `transa == CUBLAS_OP_N` and `lda &lt; max(1, k)` otherwise, or if `ldb &lt; max(1, k)` when `transb == CUBLAS_OP_N` and `ldb &lt; max(1, n)` otherwise, or if `ldc &lt; max(1, m)`, or if `alpha` or `beta` are NULL, or if `Atype` or `Btype` or `Ctype` or `algo` or `computeType` is not supported.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_INITIALIZED`]: The library was not initialized.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_SUPPORTED`]: The combination of the parameters `Atype`, `Btype` and `Ctype` or the algorithm, `algo` is not supported.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The operation completed successfully.
    pub fn cublasGemmBatchedEx(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        alpha: *const ::core::ffi::c_void,
        Aarray: *const *const ::core::ffi::c_void,
        Atype: cudaDataType,
        lda: ::core::ffi::c_int,
        Barray: *const *const ::core::ffi::c_void,
        Btype: cudaDataType,
        ldb: ::core::ffi::c_int,
        beta: *const ::core::ffi::c_void,
        Carray: *const *mut ::core::ffi::c_void,
        Ctype: cudaDataType,
        ldc: ::core::ffi::c_int,
        batchCount: ::core::ffi::c_int,
        computeType: cublasComputeType_t,
        algo: cublasGemmAlgo_t,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function is an extension of cublas&lt;t>gemmBatched() that performs the matrix-matrix multiplication of a batch of matrices and allows the user to individually specify the data types for each of the A, B and C matrix arrays, the precision of computation and the GEMM algorithm to be run. Like cublas&lt;t>gemmBatched(), the batch is considered to be “uniform”, i.e. all instances have the same dimensions (m, n, k), leading dimensions (lda, ldb, ldc) and transpositions (transa, transb) for their respective A, B and C matrices. The address of the input matrices and the output matrix of each instance of the batch are read from arrays of pointers passed to the function by the caller. Supported combinations of arguments are listed further down in this section.
    /// $$
    /// C\lbrack i\rbrack = \alpha\text{op}(A\lbrack i\rbrack)\text{op}(B\lbrack i\rbrack) + \beta C\lbrack i\rbrack,\text{ for i } \in \lbrack 0,batchCount - 1\rbrack
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$, $B$ and $C$ are arrays of pointers to matrices stored in column-major format with dimensions $\text{op}(A\lbrack i\rbrack)$ $m \times k$, $\text{op}(B\lbrack i\rbrack)$ $k \times n$ and $C\lbrack i\rbrack$ $m \times n$, respectively. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B\lbrack i\rbrack)$ is defined similarly for matrix $B\lbrack i\rbrack$.
    ///
    /// On certain problem sizes, it might be advantageous to make multiple calls to cublas&lt;t>gemm() in different CUDA streams, rather than use this API.
    ///
    /// [`cublasGemmBatchedEx`] supports the following Compute Type, Scale Type, Atype/Btype, and Ctype:
    ///
    /// If `Atype` is `cudaDataType_t::CUDA_R_16F` or `cudaDataType_t::CUDA_R_16BF`, or `computeType` is any of the `FAST` options, or when math mode or `algo` enable fast math modes, pointers (not the pointer arrays) placed in the GPU memory must be properly aligned to avoid misaligned memory access errors. Ideally all pointers are aligned to at least 16 Bytes. Otherwise it is recommended that they meet the following rule:
    ///
    /// Also refer to: [sgemm.()](http://www.netlib.org/blas/sgemm.f).
    ///
    /// # Parameters
    ///
    /// - `handle`: Handle to the cuBLAS library context.
    /// - `transa`: Operation op(`Aarray\[i\]`) that is non- or (conj.) transpose.
    /// - `transb`: Operation op(`Barray\[i\]`) that is non- or (conj.) transpose.
    /// - `m`: Number of rows of matrix op(`Aarray\[i\]`) and `Carray\[i\]`.
    /// - `n`: Number of columns of matrix op(`Barray\[i\]`) and `Carray\[i\]`.
    /// - `k`: Number of columns of op(`Aarray\[i\]`) and rows of op(`Barray\[i\]`).
    /// - `alpha`: Scaling factor for matrix products of the type that corresponds to the computeType and Ctype, see the table below for details.
    /// - `Aarray`: Array of pointers to &lt;`Atype`> array, with each array of dim. `lda x k` with `lda >= max(1, m)` if `transa == CUBLAS_OP_N` and `lda x m` with `lda >= max(1, k)` otherwise. All pointers must meet certain alignment criteria. Please see below for details.
    /// - `Atype`: Enumerant specifying the datatype of `Aarray`.
    /// - `lda`: Leading dimension of two-dimensional array used to store the matrix `Aarray\[i\]`.
    /// - `Barray`: Array of pointers to &lt;`Btype`> array, with each array of dim. `ldb x n` with `ldb >= max(1, k)` if `transb == CUBLAS_OP_N` and `ldb x k` with `ldb >= max(1,n)` otherwise. All pointers must meet certain alignment criteria. Please see below for details.
    /// - `Btype`: Enumerant specifying the datatype of `Barray`.
    /// - `ldb`: Leading dimension of two-dimensional array used to store matrix `Barray\[i\]`.
    /// - `beta`: Scaling factor for `Carray` of the type that corresponds to the computeType and Ctype, see the table below for details. If `beta == 0`, `Carray\[i\]` does not have to be a valid input.
    /// - `Carray`: Array of pointers to &lt;`Ctype`> array. It has dimensions `ldc x n` with `ldc >= max(1, m)`. Matrices `Carray\[i\]` should not overlap; otherwise, the behavior is undefined. All pointers must meet certain alignment criteria. Please see below for details.
    /// - `Ctype`: Enumerant specifying the datatype of `Carray`.
    /// - `ldc`: Leading dimension of a two-dimensional array used to store each matrix `Carray\[i\]`.
    /// - `batchCount`: Number of pointers contained in `Aarray`, `Barray` and `Carray`.
    /// - `computeType`: Enumerant specifying the computation type.
    /// - `algo`: Enumerant specifying the algorithm. See [`cublasGemmAlgo_t`].
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_ARCH_MISMATCH`]: [`cublasGemmBatchedEx`] is only supported for GPU with architecture capabilities equal to or greater than 5.0.
    /// - [`cublasStatus_t::CUBLAS_STATUS_EXECUTION_FAILED`]: The function failed to launch on the GPU.
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: If `m &lt; 0` or `n &lt; 0` or `k &lt; 0`, or if `transa` and `transb` are not one of [`cublasOperation_t::CUBLAS_OP_N`], [`cublasOperation_t::CUBLAS_OP_C`], [`cublasOperation_t::CUBLAS_OP_T`], or if `lda &lt; max(1, m)` when `transa == CUBLAS_OP_N` and `lda &lt; max(1, k)` otherwise, or if `ldb &lt; max(1, k)` when `transb == CUBLAS_OP_N` and `ldb &lt; max(1, n)` otherwise, or if `ldc &lt; max(1, m)`, or if `alpha` or `beta` are NULL, or if `Atype` or `Btype` or `Ctype` or `algo` or `computeType` is not supported.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_INITIALIZED`]: The library was not initialized.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_SUPPORTED`]: The combination of the parameters `Atype`, `Btype` and `Ctype` or the algorithm, `algo` is not supported.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The operation completed successfully.
    pub fn cublasGemmBatchedEx_64(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: i64,
        n: i64,
        k: i64,
        alpha: *const ::core::ffi::c_void,
        Aarray: *const *const ::core::ffi::c_void,
        Atype: cudaDataType,
        lda: i64,
        Barray: *const *const ::core::ffi::c_void,
        Btype: cudaDataType,
        ldb: i64,
        beta: *const ::core::ffi::c_void,
        Carray: *const *mut ::core::ffi::c_void,
        Ctype: cudaDataType,
        ldc: i64,
        batchCount: i64,
        computeType: cublasComputeType_t,
        algo: cublasGemmAlgo_t,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function is an extension of cublas&lt;t>gemmStridedBatched() that performs the matrix-matrix multiplication of a batch of matrices and allows the user to individually specify the data types for each of the A, B and C matrices, the precision of computation and the GEMM algorithm to be run. Like cublas&lt;t>gemmStridedBatched(), the batch is considered to be “uniform”, i.e. all instances have the same dimensions (m, n, k), leading dimensions (lda, ldb, ldc) and transpositions (transa, transb) for their respective A, B and C matrices. Input matrices A, B and output matrix C for each instance of the batch are located at fixed offsets in number of elements from their locations in the previous instance. Pointers to A, B and C matrices for the first instance are passed to the function by the user along with the offsets in number of elements - strideA, strideB and strideC that determine the locations of input and output matrices in future instances.
    /// $$
    /// C + i\\*{strideC} = \alpha\text{op}(A + i\\*{strideA})\text{op}(B + i\\*{strideB}) + \beta(C + i\\*{strideC}),\text{ for i } \in \lbrack 0,batchCount - 1\rbrack
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$, $B$ and $C$ are arrays of pointers to matrices stored in column-major format with dimensions $\text{op}(A\lbrack i\rbrack)$ $m \times k$, $\text{op}(B\lbrack i\rbrack)$ $k \times n$ and $C\lbrack i\rbrack$ $m \times n$, respectively. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B\lbrack i\rbrack)$ is defined similarly for matrix $B\lbrack i\rbrack$.
    ///
    /// On certain problem sizes, it might be advantageous to make multiple calls to cublas&lt;t>gemm() in different CUDA streams, rather than use this API.
    ///
    /// [`cublasGemmStridedBatchedEx`] supports the following Compute Type, Scale Type, Atype/Btype, and Ctype:
    ///
    /// Also refer to: [sgemm.()](http://www.netlib.org/blas/sgemm.f).
    ///
    /// # Parameters
    ///
    /// - `handle`: Handle to the cuBLAS library context.
    /// - `transa`: Operation op(`A\[i\]`) that is non- or (conj.) transpose.
    /// - `transb`: Operation op(`B\[i\]`) that is non- or (conj.) transpose.
    /// - `m`: Number of rows of matrix op(`A\[i\]`) and `C\[i\]`.
    /// - `n`: Number of columns of matrix op(`B\[i\]`) and `C\[i\]`.
    /// - `k`: Number of columns of op(`A\[i\]`) and rows of op(`B\[i\]`).
    /// - `alpha`: Scaling factor for A\*B of the &lt;*Scale Type*> that corresponds to the computeType and Ctype, see the table below for details.
    /// - `A`: Pointer to &lt;`Atype`> matrix, A, corresponds to the first instance of the batch, with dimensions `lda x k` with `lda >= max(1, m)` if `transa == CUBLAS_OP_N` and `lda x m` with `lda >= max(1, k)` otherwise.
    /// - `Atype`: Enumerant specifying the datatype of `A`.
    /// - `lda`: Leading dimension of two-dimensional array used to store the matrix `A\[i\]`.
    /// - `strideA`: Value of type long long int that gives the offset in number of elements between `A\[i\]` and `A\[i+1\]`.
    /// - `B`: Pointer to &lt;`Btype`> matrix, B, corresponds to the first instance of the batch, with dimensions `ldb x n` with `ldb >= max(1, k)` if `transb == CUBLAS_OP_N` and `ldb x k` with `ldb >= max(1,n)` otherwise.
    /// - `Btype`: Enumerant specifying the datatype of `B`.
    /// - `ldb`: Leading dimension of two-dimensional array used to store matrix `B\[i\]`.
    /// - `strideB`: Value of type long long int that gives the offset in number of elements between `B\[i\]` and `B\[i+1\]`.
    /// - `beta`: Scaling factor for C of the &lt;*Scale Type*> that corresponds to the computeType and Ctype, see the table below for details. If `beta == 0`, `C\[i\]` does not have to be a valid input.
    /// - `C`: Pointer to &lt;`Ctype`> matrix, C, corresponds to the first instance of the batch, with dimensions `ldc x n` with `ldc >= max(1, m)`. Matrices `C\[i\]` should not overlap; otherwise, undefined behavior is expected.
    /// - `Ctype`: Enumerant specifying the datatype of `C`.
    /// - `ldc`: Leading dimension of a two-dimensional array used to store each matrix `C\[i\]`.
    /// - `strideC`: Value of type long long int that gives the offset in number of elements between `C\[i\]` and `C\[i+1\]`.
    /// - `batchCount`: Number of GEMMs to perform in the batch.
    /// - `computeType`: Enumerant specifying the computation type.
    /// - `algo`: Enumerant specifying the algorithm. See [`cublasGemmAlgo_t`].
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_ARCH_MISMATCH`]: [`cublasGemmBatchedEx`] is only supported for GPU with architecture capabilities equal or greater than 5.0.
    /// - [`cublasStatus_t::CUBLAS_STATUS_EXECUTION_FAILED`]: The function failed to launch on the GPU.
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: If `m &lt; 0` or `n &lt; 0` or `k &lt; 0`, or if `transa` and `transb` are not one of [`cublasOperation_t::CUBLAS_OP_N`], [`cublasOperation_t::CUBLAS_OP_C`], [`cublasOperation_t::CUBLAS_OP_T`], or if `lda &lt; max(1, m)` when `transa == CUBLAS_OP_N` and `lda &lt; max(1, k)` otherwise, or if `ldb &lt; max(1, k)` when `transb == CUBLAS_OP_N` and `ldb &lt; max(1, n)` otherwise, or if `ldc &lt; max(1, m)`, or if `alpha` or `beta` are NULL, or if `Atype` or `Btype` or `Ctype` or `algo` or `computeType` is not supported.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_INITIALIZED`]: The library was not initialized.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_SUPPORTED`]: The combination of the parameters `Atype`, `Btype` and `Ctype` or the algorithm, `algo` is not supported.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The operation completed successfully.
    pub fn cublasGemmStridedBatchedEx(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        alpha: *const ::core::ffi::c_void,
        A: *const ::core::ffi::c_void,
        Atype: cudaDataType,
        lda: ::core::ffi::c_int,
        strideA: ::core::ffi::c_longlong,
        B: *const ::core::ffi::c_void,
        Btype: cudaDataType,
        ldb: ::core::ffi::c_int,
        strideB: ::core::ffi::c_longlong,
        beta: *const ::core::ffi::c_void,
        C: *mut ::core::ffi::c_void,
        Ctype: cudaDataType,
        ldc: ::core::ffi::c_int,
        strideC: ::core::ffi::c_longlong,
        batchCount: ::core::ffi::c_int,
        computeType: cublasComputeType_t,
        algo: cublasGemmAlgo_t,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function is an extension of cublas&lt;t>gemmStridedBatched() that performs the matrix-matrix multiplication of a batch of matrices and allows the user to individually specify the data types for each of the A, B and C matrices, the precision of computation and the GEMM algorithm to be run. Like cublas&lt;t>gemmStridedBatched(), the batch is considered to be “uniform”, i.e. all instances have the same dimensions (m, n, k), leading dimensions (lda, ldb, ldc) and transpositions (transa, transb) for their respective A, B and C matrices. Input matrices A, B and output matrix C for each instance of the batch are located at fixed offsets in number of elements from their locations in the previous instance. Pointers to A, B and C matrices for the first instance are passed to the function by the user along with the offsets in number of elements - strideA, strideB and strideC that determine the locations of input and output matrices in future instances.
    /// $$
    /// C + i\\*{strideC} = \alpha\text{op}(A + i\\*{strideA})\text{op}(B + i\\*{strideB}) + \beta(C + i\\*{strideC}),\text{ for i } \in \lbrack 0,batchCount - 1\rbrack
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$, $B$ and $C$ are arrays of pointers to matrices stored in column-major format with dimensions $\text{op}(A\lbrack i\rbrack)$ $m \times k$, $\text{op}(B\lbrack i\rbrack)$ $k \times n$ and $C\lbrack i\rbrack$ $m \times n$, respectively. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B\lbrack i\rbrack)$ is defined similarly for matrix $B\lbrack i\rbrack$.
    ///
    /// On certain problem sizes, it might be advantageous to make multiple calls to cublas&lt;t>gemm() in different CUDA streams, rather than use this API.
    ///
    /// [`cublasGemmStridedBatchedEx`] supports the following Compute Type, Scale Type, Atype/Btype, and Ctype:
    ///
    /// Also refer to: [sgemm.()](http://www.netlib.org/blas/sgemm.f).
    ///
    /// # Parameters
    ///
    /// - `handle`: Handle to the cuBLAS library context.
    /// - `transa`: Operation op(`A\[i\]`) that is non- or (conj.) transpose.
    /// - `transb`: Operation op(`B\[i\]`) that is non- or (conj.) transpose.
    /// - `m`: Number of rows of matrix op(`A\[i\]`) and `C\[i\]`.
    /// - `n`: Number of columns of matrix op(`B\[i\]`) and `C\[i\]`.
    /// - `k`: Number of columns of op(`A\[i\]`) and rows of op(`B\[i\]`).
    /// - `alpha`: Scaling factor for A\*B of the &lt;*Scale Type*> that corresponds to the computeType and Ctype, see the table below for details.
    /// - `A`: Pointer to &lt;`Atype`> matrix, A, corresponds to the first instance of the batch, with dimensions `lda x k` with `lda >= max(1, m)` if `transa == CUBLAS_OP_N` and `lda x m` with `lda >= max(1, k)` otherwise.
    /// - `Atype`: Enumerant specifying the datatype of `A`.
    /// - `lda`: Leading dimension of two-dimensional array used to store the matrix `A\[i\]`.
    /// - `strideA`: Value of type long long int that gives the offset in number of elements between `A\[i\]` and `A\[i+1\]`.
    /// - `B`: Pointer to &lt;`Btype`> matrix, B, corresponds to the first instance of the batch, with dimensions `ldb x n` with `ldb >= max(1, k)` if `transb == CUBLAS_OP_N` and `ldb x k` with `ldb >= max(1,n)` otherwise.
    /// - `Btype`: Enumerant specifying the datatype of `B`.
    /// - `ldb`: Leading dimension of two-dimensional array used to store matrix `B\[i\]`.
    /// - `strideB`: Value of type long long int that gives the offset in number of elements between `B\[i\]` and `B\[i+1\]`.
    /// - `beta`: Scaling factor for C of the &lt;*Scale Type*> that corresponds to the computeType and Ctype, see the table below for details. If `beta == 0`, `C\[i\]` does not have to be a valid input.
    /// - `C`: Pointer to &lt;`Ctype`> matrix, C, corresponds to the first instance of the batch, with dimensions `ldc x n` with `ldc >= max(1, m)`. Matrices `C\[i\]` should not overlap; otherwise, undefined behavior is expected.
    /// - `Ctype`: Enumerant specifying the datatype of `C`.
    /// - `ldc`: Leading dimension of a two-dimensional array used to store each matrix `C\[i\]`.
    /// - `strideC`: Value of type long long int that gives the offset in number of elements between `C\[i\]` and `C\[i+1\]`.
    /// - `batchCount`: Number of GEMMs to perform in the batch.
    /// - `computeType`: Enumerant specifying the computation type.
    /// - `algo`: Enumerant specifying the algorithm. See [`cublasGemmAlgo_t`].
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_ARCH_MISMATCH`]: [`cublasGemmBatchedEx`] is only supported for GPU with architecture capabilities equal or greater than 5.0.
    /// - [`cublasStatus_t::CUBLAS_STATUS_EXECUTION_FAILED`]: The function failed to launch on the GPU.
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: If `m &lt; 0` or `n &lt; 0` or `k &lt; 0`, or if `transa` and `transb` are not one of [`cublasOperation_t::CUBLAS_OP_N`], [`cublasOperation_t::CUBLAS_OP_C`], [`cublasOperation_t::CUBLAS_OP_T`], or if `lda &lt; max(1, m)` when `transa == CUBLAS_OP_N` and `lda &lt; max(1, k)` otherwise, or if `ldb &lt; max(1, k)` when `transb == CUBLAS_OP_N` and `ldb &lt; max(1, n)` otherwise, or if `ldc &lt; max(1, m)`, or if `alpha` or `beta` are NULL, or if `Atype` or `Btype` or `Ctype` or `algo` or `computeType` is not supported.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_INITIALIZED`]: The library was not initialized.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_SUPPORTED`]: The combination of the parameters `Atype`, `Btype` and `Ctype` or the algorithm, `algo` is not supported.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The operation completed successfully.
    pub fn cublasGemmStridedBatchedEx_64(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: i64,
        n: i64,
        k: i64,
        alpha: *const ::core::ffi::c_void,
        A: *const ::core::ffi::c_void,
        Atype: cudaDataType,
        lda: i64,
        strideA: ::core::ffi::c_longlong,
        B: *const ::core::ffi::c_void,
        Btype: cudaDataType,
        ldb: i64,
        strideB: ::core::ffi::c_longlong,
        beta: *const ::core::ffi::c_void,
        C: *mut ::core::ffi::c_void,
        Ctype: cudaDataType,
        ldc: i64,
        strideC: ::core::ffi::c_longlong,
        batchCount: i64,
        computeType: cublasComputeType_t,
        algo: cublasGemmAlgo_t,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-matrix multiplication on groups of matrices. A given group is considered to be “uniform”, i.e. all instances have the same dimensions (m, n, k), leading dimensions (lda, ldb, ldc) and transpositions (transa, transb) for their respective A, B and C matrices. However, the dimensions, leading dimensions, transpositions, and scaling factors (alpha, beta) may vary between groups. The address of the input matrices and the output matrix of each instance of the batch are read from arrays of pointers passed to the function by the caller. This is functionally equivalent to the following:
    ///
    /// where $\text{$\mathrm{alpha_array}$}$ and $\text{$\mathrm{beta_array}$}$ are arrays of scaling factors, and $\text{Aarray}$, $\text{Barray}$ and $\text{Carray}$ are arrays of pointers to matrices stored in column-major format. For a given index, $\text{idx}$, that is part of group $i$, the dimensions are:
    ///
    /// For matrix $A\[\text{idx}\]$ in group $i$:
    /// $$
    /// \text{op}(A\[\text{idx}\]) = \left\\{ \begin{matrix}
    /// A\[\text{idx}\] & {\text{if }\textsf{$\mathrm{transa_array}\lbrack i\rbrack$ == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A\[\text{idx}\]^{T} & {\text{if }\textsf{$\mathrm{transa_array}\lbrack i\rbrack$ == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A\[\text{idx}\]^{H} & {\text{if }\textsf{$\mathrm{transa_array}\lbrack i\rbrack$ == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B\[\text{idx}\])$ is defined similarly for matrix $B\[\text{idx}\]$ in group $i$.
    ///
    /// On certain problem sizes, it might be advantageous to make multiple calls to cublas&lt;t>gemmBatched() in different CUDA streams, rather than use this API.
    ///
    /// If math mode enables fast math modes when using [`cublasSgemmGroupedBatched`], pointers (not the pointer arrays) placed in the GPU memory must be properly aligned to avoid misaligned memory access errors. Ideally all pointers are aligned to at least 16 Bytes. Otherwise it is required that they meet the following rule.
    pub fn cublasSgemmGroupedBatched(
        handle: cublasHandle_t,
        transa_array: *const cublasOperation_t,
        transb_array: *const cublasOperation_t,
        m_array: *const ::core::ffi::c_int,
        n_array: *const ::core::ffi::c_int,
        k_array: *const ::core::ffi::c_int,
        alpha_array: *const f32,
        Aarray: *const *const f32,
        lda_array: *const ::core::ffi::c_int,
        Barray: *const *const f32,
        ldb_array: *const ::core::ffi::c_int,
        beta_array: *const f32,
        Carray: *const *mut f32,
        ldc_array: *const ::core::ffi::c_int,
        group_count: ::core::ffi::c_int,
        group_size: *const ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-matrix multiplication on groups of matrices. A given group is considered to be “uniform”, i.e. all instances have the same dimensions (m, n, k), leading dimensions (lda, ldb, ldc) and transpositions (transa, transb) for their respective A, B and C matrices. However, the dimensions, leading dimensions, transpositions, and scaling factors (alpha, beta) may vary between groups. The address of the input matrices and the output matrix of each instance of the batch are read from arrays of pointers passed to the function by the caller. This is functionally equivalent to the following:
    ///
    /// where $\text{$\mathrm{alpha_array}$}$ and $\text{$\mathrm{beta_array}$}$ are arrays of scaling factors, and $\text{Aarray}$, $\text{Barray}$ and $\text{Carray}$ are arrays of pointers to matrices stored in column-major format. For a given index, $\text{idx}$, that is part of group $i$, the dimensions are:
    ///
    /// For matrix $A\[\text{idx}\]$ in group $i$:
    /// $$
    /// \text{op}(A\[\text{idx}\]) = \left\\{ \begin{matrix}
    /// A\[\text{idx}\] & {\text{if }\textsf{$\mathrm{transa_array}\lbrack i\rbrack$ == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A\[\text{idx}\]^{T} & {\text{if }\textsf{$\mathrm{transa_array}\lbrack i\rbrack$ == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A\[\text{idx}\]^{H} & {\text{if }\textsf{$\mathrm{transa_array}\lbrack i\rbrack$ == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B\[\text{idx}\])$ is defined similarly for matrix $B\[\text{idx}\]$ in group $i$.
    ///
    /// On certain problem sizes, it might be advantageous to make multiple calls to cublas&lt;t>gemmBatched() in different CUDA streams, rather than use this API.
    ///
    /// If math mode enables fast math modes when using [`cublasSgemmGroupedBatched`], pointers (not the pointer arrays) placed in the GPU memory must be properly aligned to avoid misaligned memory access errors. Ideally all pointers are aligned to at least 16 Bytes. Otherwise it is required that they meet the following rule.
    pub fn cublasSgemmGroupedBatched_64(
        handle: cublasHandle_t,
        transa_array: *const cublasOperation_t,
        transb_array: *const cublasOperation_t,
        m_array: *const i64,
        n_array: *const i64,
        k_array: *const i64,
        alpha_array: *const f32,
        Aarray: *const *const f32,
        lda_array: *const i64,
        Barray: *const *const f32,
        ldb_array: *const i64,
        beta_array: *const f32,
        Carray: *const *mut f32,
        ldc_array: *const i64,
        group_count: i64,
        group_size: *const i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-matrix multiplication on groups of matrices. A given group is considered to be “uniform”, i.e. all instances have the same dimensions (m, n, k), leading dimensions (lda, ldb, ldc) and transpositions (transa, transb) for their respective A, B and C matrices. However, the dimensions, leading dimensions, transpositions, and scaling factors (alpha, beta) may vary between groups. The address of the input matrices and the output matrix of each instance of the batch are read from arrays of pointers passed to the function by the caller. This is functionally equivalent to the following:
    ///
    /// where $\text{$\mathrm{alpha_array}$}$ and $\text{$\mathrm{beta_array}$}$ are arrays of scaling factors, and $\text{Aarray}$, $\text{Barray}$ and $\text{Carray}$ are arrays of pointers to matrices stored in column-major format. For a given index, $\text{idx}$, that is part of group $i$, the dimensions are:
    ///
    /// For matrix $A\[\text{idx}\]$ in group $i$:
    /// $$
    /// \text{op}(A\[\text{idx}\]) = \left\\{ \begin{matrix}
    /// A\[\text{idx}\] & {\text{if }\textsf{$\mathrm{transa_array}\lbrack i\rbrack$ == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A\[\text{idx}\]^{T} & {\text{if }\textsf{$\mathrm{transa_array}\lbrack i\rbrack$ == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A\[\text{idx}\]^{H} & {\text{if }\textsf{$\mathrm{transa_array}\lbrack i\rbrack$ == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B\[\text{idx}\])$ is defined similarly for matrix $B\[\text{idx}\]$ in group $i$.
    ///
    /// On certain problem sizes, it might be advantageous to make multiple calls to cublas&lt;t>gemmBatched() in different CUDA streams, rather than use this API.
    ///
    /// If math mode enables fast math modes when using [`cublasSgemmGroupedBatched`], pointers (not the pointer arrays) placed in the GPU memory must be properly aligned to avoid misaligned memory access errors. Ideally all pointers are aligned to at least 16 Bytes. Otherwise it is required that they meet the following rule.
    pub fn cublasDgemmGroupedBatched(
        handle: cublasHandle_t,
        transa_array: *const cublasOperation_t,
        transb_array: *const cublasOperation_t,
        m_array: *const ::core::ffi::c_int,
        n_array: *const ::core::ffi::c_int,
        k_array: *const ::core::ffi::c_int,
        alpha_array: *const f64,
        Aarray: *const *const f64,
        lda_array: *const ::core::ffi::c_int,
        Barray: *const *const f64,
        ldb_array: *const ::core::ffi::c_int,
        beta_array: *const f64,
        Carray: *const *mut f64,
        ldc_array: *const ::core::ffi::c_int,
        group_count: ::core::ffi::c_int,
        group_size: *const ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-matrix multiplication on groups of matrices. A given group is considered to be “uniform”, i.e. all instances have the same dimensions (m, n, k), leading dimensions (lda, ldb, ldc) and transpositions (transa, transb) for their respective A, B and C matrices. However, the dimensions, leading dimensions, transpositions, and scaling factors (alpha, beta) may vary between groups. The address of the input matrices and the output matrix of each instance of the batch are read from arrays of pointers passed to the function by the caller. This is functionally equivalent to the following:
    ///
    /// where $\text{$\mathrm{alpha_array}$}$ and $\text{$\mathrm{beta_array}$}$ are arrays of scaling factors, and $\text{Aarray}$, $\text{Barray}$ and $\text{Carray}$ are arrays of pointers to matrices stored in column-major format. For a given index, $\text{idx}$, that is part of group $i$, the dimensions are:
    ///
    /// For matrix $A\[\text{idx}\]$ in group $i$:
    /// $$
    /// \text{op}(A\[\text{idx}\]) = \left\\{ \begin{matrix}
    /// A\[\text{idx}\] & {\text{if }\textsf{$\mathrm{transa_array}\lbrack i\rbrack$ == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A\[\text{idx}\]^{T} & {\text{if }\textsf{$\mathrm{transa_array}\lbrack i\rbrack$ == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A\[\text{idx}\]^{H} & {\text{if }\textsf{$\mathrm{transa_array}\lbrack i\rbrack$ == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B\[\text{idx}\])$ is defined similarly for matrix $B\[\text{idx}\]$ in group $i$.
    ///
    /// On certain problem sizes, it might be advantageous to make multiple calls to cublas&lt;t>gemmBatched() in different CUDA streams, rather than use this API.
    ///
    /// If math mode enables fast math modes when using [`cublasSgemmGroupedBatched`], pointers (not the pointer arrays) placed in the GPU memory must be properly aligned to avoid misaligned memory access errors. Ideally all pointers are aligned to at least 16 Bytes. Otherwise it is required that they meet the following rule.
    pub fn cublasDgemmGroupedBatched_64(
        handle: cublasHandle_t,
        transa_array: *const cublasOperation_t,
        transb_array: *const cublasOperation_t,
        m_array: *const i64,
        n_array: *const i64,
        k_array: *const i64,
        alpha_array: *const f64,
        Aarray: *const *const f64,
        lda_array: *const i64,
        Barray: *const *const f64,
        ldb_array: *const i64,
        beta_array: *const f64,
        Carray: *const *mut f64,
        ldc_array: *const i64,
        group_count: i64,
        group_size: *const i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-matrix multiplication on groups of matrices. A given group is considered to be “uniform”, i.e. all instances have the same dimensions (m, n, k), leading dimensions (lda, ldb, ldc) and transpositions (transa, transb) for their respective A, B and C matrices. However, the dimensions, leading dimensions, transpositions, and scaling factors (alpha, beta) may vary between groups. The address of the input matrices and the output matrix of each instance of the batch are read from arrays of pointers passed to the function by the caller. This is functionally equivalent to the following:
    ///
    /// where $\text{$\mathrm{alpha_array}$}$ and $\text{$\mathrm{beta_array}$}$ are arrays of scaling factors, and $\text{Aarray}$, $\text{Barray}$ and $\text{Carray}$ are arrays of pointers to matrices stored in column-major format. For a given index, $\text{idx}$, that is part of group $i$, the dimensions are:
    ///
    /// For matrix $A\[\text{idx}\]$ in group $i$:
    /// $$
    /// \text{op}(A\[\text{idx}\]) = \left\\{ \begin{matrix}
    /// A\[\text{idx}\] & {\text{if }\textsf{$\mathrm{transa_array}\lbrack i\rbrack$ == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A\[\text{idx}\]^{T} & {\text{if }\textsf{$\mathrm{transa_array}\lbrack i\rbrack$ == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A\[\text{idx}\]^{H} & {\text{if }\textsf{$\mathrm{transa_array}\lbrack i\rbrack$ == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B\[\text{idx}\])$ is defined similarly for matrix $B\[\text{idx}\]$ in group $i$.
    ///
    /// On certain problem sizes, it might be advantageous to make multiple calls to [`cublasGemmBatchedEx`] in different CUDA streams, rather than use this API.
    ///
    /// [`cublasGemmGroupedBatchedEx`] supports the following Compute Type, Scale Type, Atype/Btype, and Ctype:
    ///
    /// If `Atype` is `cudaDataType_t::CUDA_R_16F` or `cudaDataType_t::CUDA_R_16BF` or if the `computeType` is any of the `FAST` options, pointers (not the pointer arrays) placed in the GPU memory must be properly aligned to avoid misaligned memory access errors. Ideally all pointers are aligned to at least 16 Bytes. Otherwise it is required that they meet the following rule.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_EXECUTION_FAILED`]: The function failed to launch on the GPU.
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: If `transa_array`, `transb_array`, `m_array`, `n_array`, `k_array`, `alpha_array`, `lda_array`, `ldb_array`, `beta_array`, `ldc_array`, or `group_size` are NULL, or if `group_count &lt; 0`, or if `m_array\[i\] &lt; 0`, `n_array\[i\] &lt; 0`, `k_array\[i\] &lt; 0`, `group_size\[i\] &lt; 0`, or if `transa_array\[i\]` and `transb_array\[i\]` are not one of [`cublasOperation_t::CUBLAS_OP_N`], [`cublasOperation_t::CUBLAS_OP_C`], [`cublasOperation_t::CUBLAS_OP_T`], or if `lda_array\[i\] &lt; max(1, m_array\[i\])` if `transa_array\[i\] == CUBLAS_OP_N` and `lda_array\[i\] &lt; max(1, k_array\[i\])` otherwise, or if `ldb_array\[i\] &lt; max(1, k_array\[i\])` if `transb_array\[i\] == CUBLAS_OP_N` and `ldb_array\[i\] &lt; max(1, n_array\[i\])` otherwise, or if `ldc_array\[i\] &lt; max(1, m_array\[i\])`.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_INITIALIZED`]: The library was not initialized.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_SUPPORTED`]: the pointer mode is set to [`cublasPointerMode_t::CUBLAS_POINTER_MODE_DEVICE`] * `Atype` or `Btype` or `Ctype` or `computeType` are not supported.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The operation completed successfully.
    pub fn cublasGemmGroupedBatchedEx(
        handle: cublasHandle_t,
        transa_array: *const cublasOperation_t,
        transb_array: *const cublasOperation_t,
        m_array: *const ::core::ffi::c_int,
        n_array: *const ::core::ffi::c_int,
        k_array: *const ::core::ffi::c_int,
        alpha_array: *const ::core::ffi::c_void,
        Aarray: *const *const ::core::ffi::c_void,
        Atype: cudaDataType_t,
        lda_array: *const ::core::ffi::c_int,
        Barray: *const *const ::core::ffi::c_void,
        Btype: cudaDataType_t,
        ldb_array: *const ::core::ffi::c_int,
        beta_array: *const ::core::ffi::c_void,
        Carray: *const *mut ::core::ffi::c_void,
        Ctype: cudaDataType_t,
        ldc_array: *const ::core::ffi::c_int,
        group_count: ::core::ffi::c_int,
        group_size: *const ::core::ffi::c_int,
        computeType: cublasComputeType_t,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-matrix multiplication on groups of matrices. A given group is considered to be “uniform”, i.e. all instances have the same dimensions (m, n, k), leading dimensions (lda, ldb, ldc) and transpositions (transa, transb) for their respective A, B and C matrices. However, the dimensions, leading dimensions, transpositions, and scaling factors (alpha, beta) may vary between groups. The address of the input matrices and the output matrix of each instance of the batch are read from arrays of pointers passed to the function by the caller. This is functionally equivalent to the following:
    ///
    /// where $\text{$\mathrm{alpha_array}$}$ and $\text{$\mathrm{beta_array}$}$ are arrays of scaling factors, and $\text{Aarray}$, $\text{Barray}$ and $\text{Carray}$ are arrays of pointers to matrices stored in column-major format. For a given index, $\text{idx}$, that is part of group $i$, the dimensions are:
    ///
    /// For matrix $A\[\text{idx}\]$ in group $i$:
    /// $$
    /// \text{op}(A\[\text{idx}\]) = \left\\{ \begin{matrix}
    /// A\[\text{idx}\] & {\text{if }\textsf{$\mathrm{transa_array}\lbrack i\rbrack$ == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A\[\text{idx}\]^{T} & {\text{if }\textsf{$\mathrm{transa_array}\lbrack i\rbrack$ == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A\[\text{idx}\]^{H} & {\text{if }\textsf{$\mathrm{transa_array}\lbrack i\rbrack$ == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B\[\text{idx}\])$ is defined similarly for matrix $B\[\text{idx}\]$ in group $i$.
    ///
    /// On certain problem sizes, it might be advantageous to make multiple calls to [`cublasGemmBatchedEx`] in different CUDA streams, rather than use this API.
    ///
    /// [`cublasGemmGroupedBatchedEx`] supports the following Compute Type, Scale Type, Atype/Btype, and Ctype:
    ///
    /// If `Atype` is `cudaDataType_t::CUDA_R_16F` or `cudaDataType_t::CUDA_R_16BF` or if the `computeType` is any of the `FAST` options, pointers (not the pointer arrays) placed in the GPU memory must be properly aligned to avoid misaligned memory access errors. Ideally all pointers are aligned to at least 16 Bytes. Otherwise it is required that they meet the following rule.
    ///
    /// # Return value
    ///
    /// - [`cublasStatus_t::CUBLAS_STATUS_EXECUTION_FAILED`]: The function failed to launch on the GPU.
    /// - [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`]: If `transa_array`, `transb_array`, `m_array`, `n_array`, `k_array`, `alpha_array`, `lda_array`, `ldb_array`, `beta_array`, `ldc_array`, or `group_size` are NULL, or if `group_count &lt; 0`, or if `m_array\[i\] &lt; 0`, `n_array\[i\] &lt; 0`, `k_array\[i\] &lt; 0`, `group_size\[i\] &lt; 0`, or if `transa_array\[i\]` and `transb_array\[i\]` are not one of [`cublasOperation_t::CUBLAS_OP_N`], [`cublasOperation_t::CUBLAS_OP_C`], [`cublasOperation_t::CUBLAS_OP_T`], or if `lda_array\[i\] &lt; max(1, m_array\[i\])` if `transa_array\[i\] == CUBLAS_OP_N` and `lda_array\[i\] &lt; max(1, k_array\[i\])` otherwise, or if `ldb_array\[i\] &lt; max(1, k_array\[i\])` if `transb_array\[i\] == CUBLAS_OP_N` and `ldb_array\[i\] &lt; max(1, n_array\[i\])` otherwise, or if `ldc_array\[i\] &lt; max(1, m_array\[i\])`.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_INITIALIZED`]: The library was not initialized.
    /// - [`cublasStatus_t::CUBLAS_STATUS_NOT_SUPPORTED`]: the pointer mode is set to [`cublasPointerMode_t::CUBLAS_POINTER_MODE_DEVICE`] * `Atype` or `Btype` or `Ctype` or `computeType` are not supported.
    /// - [`cublasStatus_t::CUBLAS_STATUS_SUCCESS`]: The operation completed successfully.
    pub fn cublasGemmGroupedBatchedEx_64(
        handle: cublasHandle_t,
        transa_array: *const cublasOperation_t,
        transb_array: *const cublasOperation_t,
        m_array: *const i64,
        n_array: *const i64,
        k_array: *const i64,
        alpha_array: *const ::core::ffi::c_void,
        Aarray: *const *const ::core::ffi::c_void,
        Atype: cudaDataType_t,
        lda_array: *const i64,
        Barray: *const *const ::core::ffi::c_void,
        Btype: cudaDataType_t,
        ldb_array: *const i64,
        beta_array: *const ::core::ffi::c_void,
        Carray: *const *mut ::core::ffi::c_void,
        Ctype: cudaDataType_t,
        ldc_array: *const i64,
        group_count: i64,
        group_size: *const i64,
        computeType: cublasComputeType_t,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-matrix addition/transposition:
    /// $$
    /// C = \alpha\text{op}(A) + \beta\text{op}(B)
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$, $B$ and $C$ are matrices stored in column-major format with dimensions $\text{op}(A)$ $m \times n$, $\text{op}(B)$ $m \times n$ and $C$ $m \times n$, respectively. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B)$ is defined similarly for matrix $B$.
    ///
    /// The operation is out-of-place if C does not overlap A or B.
    ///
    /// The in-place mode supports the following two operations,
    /// $$
    /// C = \alpha\text{\\*}C + \beta\text{op}(B)
    /// $$:
    ///
    /// $$
    /// C = \alpha\text{op}(A) + \beta\text{\\*}C
    /// $$
    ///
    /// For in-place mode, if `C == A`, `ldc == lda` and `transa == CUBLAS_OP_N`. If `C === B`, `ldc == ldb` and `transb == CUBLAS_OP_N`. If the user does not meet above requirements, [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`] is returned.
    ///
    /// The operation includes the following special cases:
    ///
    /// the user can reset matrix C to zero by setting `*alpha = beta = 0`.
    ///
    /// the user can transpose matrix A by setting `*alpha = 1 and *beta = 0`.
    pub fn cublasSgeam(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        alpha: *const f32,
        A: *const f32,
        lda: ::core::ffi::c_int,
        beta: *const f32,
        B: *const f32,
        ldb: ::core::ffi::c_int,
        C: *mut f32,
        ldc: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-matrix addition/transposition:
    /// $$
    /// C = \alpha\text{op}(A) + \beta\text{op}(B)
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$, $B$ and $C$ are matrices stored in column-major format with dimensions $\text{op}(A)$ $m \times n$, $\text{op}(B)$ $m \times n$ and $C$ $m \times n$, respectively. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B)$ is defined similarly for matrix $B$.
    ///
    /// The operation is out-of-place if C does not overlap A or B.
    ///
    /// The in-place mode supports the following two operations,
    /// $$
    /// C = \alpha\text{\\*}C + \beta\text{op}(B)
    /// $$:
    ///
    /// $$
    /// C = \alpha\text{op}(A) + \beta\text{\\*}C
    /// $$
    ///
    /// For in-place mode, if `C == A`, `ldc == lda` and `transa == CUBLAS_OP_N`. If `C === B`, `ldc == ldb` and `transb == CUBLAS_OP_N`. If the user does not meet above requirements, [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`] is returned.
    ///
    /// The operation includes the following special cases:
    ///
    /// the user can reset matrix C to zero by setting `*alpha = beta = 0`.
    ///
    /// the user can transpose matrix A by setting `*alpha = 1 and *beta = 0`.
    pub fn cublasSgeam_64(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: i64,
        n: i64,
        alpha: *const f32,
        A: *const f32,
        lda: i64,
        beta: *const f32,
        B: *const f32,
        ldb: i64,
        C: *mut f32,
        ldc: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-matrix addition/transposition:
    /// $$
    /// C = \alpha\text{op}(A) + \beta\text{op}(B)
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$, $B$ and $C$ are matrices stored in column-major format with dimensions $\text{op}(A)$ $m \times n$, $\text{op}(B)$ $m \times n$ and $C$ $m \times n$, respectively. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B)$ is defined similarly for matrix $B$.
    ///
    /// The operation is out-of-place if C does not overlap A or B.
    ///
    /// The in-place mode supports the following two operations,
    /// $$
    /// C = \alpha\text{\\*}C + \beta\text{op}(B)
    /// $$:
    ///
    /// $$
    /// C = \alpha\text{op}(A) + \beta\text{\\*}C
    /// $$
    ///
    /// For in-place mode, if `C == A`, `ldc == lda` and `transa == CUBLAS_OP_N`. If `C === B`, `ldc == ldb` and `transb == CUBLAS_OP_N`. If the user does not meet above requirements, [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`] is returned.
    ///
    /// The operation includes the following special cases:
    ///
    /// the user can reset matrix C to zero by setting `*alpha = beta = 0`.
    ///
    /// the user can transpose matrix A by setting `*alpha = 1 and *beta = 0`.
    pub fn cublasDgeam(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        alpha: *const f64,
        A: *const f64,
        lda: ::core::ffi::c_int,
        beta: *const f64,
        B: *const f64,
        ldb: ::core::ffi::c_int,
        C: *mut f64,
        ldc: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-matrix addition/transposition:
    /// $$
    /// C = \alpha\text{op}(A) + \beta\text{op}(B)
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$, $B$ and $C$ are matrices stored in column-major format with dimensions $\text{op}(A)$ $m \times n$, $\text{op}(B)$ $m \times n$ and $C$ $m \times n$, respectively. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B)$ is defined similarly for matrix $B$.
    ///
    /// The operation is out-of-place if C does not overlap A or B.
    ///
    /// The in-place mode supports the following two operations,
    /// $$
    /// C = \alpha\text{\\*}C + \beta\text{op}(B)
    /// $$:
    ///
    /// $$
    /// C = \alpha\text{op}(A) + \beta\text{\\*}C
    /// $$
    ///
    /// For in-place mode, if `C == A`, `ldc == lda` and `transa == CUBLAS_OP_N`. If `C === B`, `ldc == ldb` and `transb == CUBLAS_OP_N`. If the user does not meet above requirements, [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`] is returned.
    ///
    /// The operation includes the following special cases:
    ///
    /// the user can reset matrix C to zero by setting `*alpha = beta = 0`.
    ///
    /// the user can transpose matrix A by setting `*alpha = 1 and *beta = 0`.
    pub fn cublasDgeam_64(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: i64,
        n: i64,
        alpha: *const f64,
        A: *const f64,
        lda: i64,
        beta: *const f64,
        B: *const f64,
        ldb: i64,
        C: *mut f64,
        ldc: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-matrix addition/transposition:
    /// $$
    /// C = \alpha\text{op}(A) + \beta\text{op}(B)
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$, $B$ and $C$ are matrices stored in column-major format with dimensions $\text{op}(A)$ $m \times n$, $\text{op}(B)$ $m \times n$ and $C$ $m \times n$, respectively. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B)$ is defined similarly for matrix $B$.
    ///
    /// The operation is out-of-place if C does not overlap A or B.
    ///
    /// The in-place mode supports the following two operations,
    /// $$
    /// C = \alpha\text{\\*}C + \beta\text{op}(B)
    /// $$:
    ///
    /// $$
    /// C = \alpha\text{op}(A) + \beta\text{\\*}C
    /// $$
    ///
    /// For in-place mode, if `C == A`, `ldc == lda` and `transa == CUBLAS_OP_N`. If `C === B`, `ldc == ldb` and `transb == CUBLAS_OP_N`. If the user does not meet above requirements, [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`] is returned.
    ///
    /// The operation includes the following special cases:
    ///
    /// the user can reset matrix C to zero by setting `*alpha = beta = 0`.
    ///
    /// the user can transpose matrix A by setting `*alpha = 1 and *beta = 0`.
    pub fn cublasCgeam(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        alpha: *const cuComplex,
        A: *const cuComplex,
        lda: ::core::ffi::c_int,
        beta: *const cuComplex,
        B: *const cuComplex,
        ldb: ::core::ffi::c_int,
        C: *mut cuComplex,
        ldc: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-matrix addition/transposition:
    /// $$
    /// C = \alpha\text{op}(A) + \beta\text{op}(B)
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$, $B$ and $C$ are matrices stored in column-major format with dimensions $\text{op}(A)$ $m \times n$, $\text{op}(B)$ $m \times n$ and $C$ $m \times n$, respectively. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B)$ is defined similarly for matrix $B$.
    ///
    /// The operation is out-of-place if C does not overlap A or B.
    ///
    /// The in-place mode supports the following two operations,
    /// $$
    /// C = \alpha\text{\\*}C + \beta\text{op}(B)
    /// $$:
    ///
    /// $$
    /// C = \alpha\text{op}(A) + \beta\text{\\*}C
    /// $$
    ///
    /// For in-place mode, if `C == A`, `ldc == lda` and `transa == CUBLAS_OP_N`. If `C === B`, `ldc == ldb` and `transb == CUBLAS_OP_N`. If the user does not meet above requirements, [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`] is returned.
    ///
    /// The operation includes the following special cases:
    ///
    /// the user can reset matrix C to zero by setting `*alpha = beta = 0`.
    ///
    /// the user can transpose matrix A by setting `*alpha = 1 and *beta = 0`.
    pub fn cublasCgeam_64(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: i64,
        n: i64,
        alpha: *const cuComplex,
        A: *const cuComplex,
        lda: i64,
        beta: *const cuComplex,
        B: *const cuComplex,
        ldb: i64,
        C: *mut cuComplex,
        ldc: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-matrix addition/transposition:
    /// $$
    /// C = \alpha\text{op}(A) + \beta\text{op}(B)
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$, $B$ and $C$ are matrices stored in column-major format with dimensions $\text{op}(A)$ $m \times n$, $\text{op}(B)$ $m \times n$ and $C$ $m \times n$, respectively. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B)$ is defined similarly for matrix $B$.
    ///
    /// The operation is out-of-place if C does not overlap A or B.
    ///
    /// The in-place mode supports the following two operations,
    /// $$
    /// C = \alpha\text{\\*}C + \beta\text{op}(B)
    /// $$:
    ///
    /// $$
    /// C = \alpha\text{op}(A) + \beta\text{\\*}C
    /// $$
    ///
    /// For in-place mode, if `C == A`, `ldc == lda` and `transa == CUBLAS_OP_N`. If `C === B`, `ldc == ldb` and `transb == CUBLAS_OP_N`. If the user does not meet above requirements, [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`] is returned.
    ///
    /// The operation includes the following special cases:
    ///
    /// the user can reset matrix C to zero by setting `*alpha = beta = 0`.
    ///
    /// the user can transpose matrix A by setting `*alpha = 1 and *beta = 0`.
    pub fn cublasZgeam(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        alpha: *const cuDoubleComplex,
        A: *const cuDoubleComplex,
        lda: ::core::ffi::c_int,
        beta: *const cuDoubleComplex,
        B: *const cuDoubleComplex,
        ldb: ::core::ffi::c_int,
        C: *mut cuDoubleComplex,
        ldc: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-matrix addition/transposition:
    /// $$
    /// C = \alpha\text{op}(A) + \beta\text{op}(B)
    /// $$
    ///
    /// where $\alpha$ and $\beta$ are scalars, and $A$, $B$ and $C$ are matrices stored in column-major format with dimensions $\text{op}(A)$ $m \times n$, $\text{op}(B)$ $m \times n$ and $C$ $m \times n$, respectively. Also, for matrix $A$:
    /// $$
    /// \text{op}(A) = \left\\{ \begin{matrix}
    /// A & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// A^{T} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// A^{H} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// and $\text{op}(B)$ is defined similarly for matrix $B$.
    ///
    /// The operation is out-of-place if C does not overlap A or B.
    ///
    /// The in-place mode supports the following two operations,
    /// $$
    /// C = \alpha\text{\\*}C + \beta\text{op}(B)
    /// $$:
    ///
    /// $$
    /// C = \alpha\text{op}(A) + \beta\text{\\*}C
    /// $$
    ///
    /// For in-place mode, if `C == A`, `ldc == lda` and `transa == CUBLAS_OP_N`. If `C === B`, `ldc == ldb` and `transb == CUBLAS_OP_N`. If the user does not meet above requirements, [`cublasStatus_t::CUBLAS_STATUS_INVALID_VALUE`] is returned.
    ///
    /// The operation includes the following special cases:
    ///
    /// the user can reset matrix C to zero by setting `*alpha = beta = 0`.
    ///
    /// the user can transpose matrix A by setting `*alpha = 1 and *beta = 0`.
    pub fn cublasZgeam_64(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        m: i64,
        n: i64,
        alpha: *const cuDoubleComplex,
        A: *const cuDoubleComplex,
        lda: i64,
        beta: *const cuDoubleComplex,
        B: *const cuDoubleComplex,
        ldb: i64,
        C: *mut cuDoubleComplex,
        ldc: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function solves an array of triangular linear systems with multiple right-hand-sides:
    /// $$
    /// \left\\{ \begin{matrix}
    /// {\text{op}(A\lbrack i\rbrack)X\lbrack i\rbrack = \alpha B\lbrack i\rbrack} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_LEFT}$}} \\\\
    /// {X\lbrack i\rbrack\text{op}(A\lbrack i\rbrack) = \alpha B\lbrack i\rbrack} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_RIGHT}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $A\lbrack i\rbrack$ is a triangular matrix stored in lower or upper mode with or without the main diagonal, $X\lbrack i\rbrack$ and $B\lbrack i\rbrack$ are $m \times n$ matrices, and $\alpha$ is a scalar. Also, for matrix $A$:
    /// $$
    /// \text{op}(A\lbrack i\rbrack) = \left\\{ \begin{matrix}
    /// {A\lbrack i\rbrack} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A^{T}\lbrack i\rbrack} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// {A^{H}\lbrack i\rbrack} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// The solution $X\lbrack i\rbrack$ overwrites the right-hand-sides $B\lbrack i\rbrack$ on exit.
    ///
    /// No test for singularity or near-singularity is included in this function.
    ///
    /// This function works for any sizes but is intended to be used for matrices of small sizes where the launch overhead is a significant factor. For bigger sizes, it might be advantageous to call `batchCount` times the regular cublas&lt;t>trsm() within a set of CUDA streams.
    ///
    /// The current implementation is limited to devices with compute capability above or equal 2.0.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [strsm()](http://www.netlib.org/blas/strsm.f), [dtrsm()](http://www.netlib.org/blas/dtrsm.f), [ctrsm()](http://www.netlib.org/blas/ctrsm.f), [ztrsm()](http://www.netlib.org/blas/ztrsm.f).
    pub fn cublasStrsmBatched(
        handle: cublasHandle_t,
        side: cublasSideMode_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        alpha: *const f32,
        A: *const *const f32,
        lda: ::core::ffi::c_int,
        B: *const *mut f32,
        ldb: ::core::ffi::c_int,
        batchCount: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function solves an array of triangular linear systems with multiple right-hand-sides:
    /// $$
    /// \left\\{ \begin{matrix}
    /// {\text{op}(A\lbrack i\rbrack)X\lbrack i\rbrack = \alpha B\lbrack i\rbrack} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_LEFT}$}} \\\\
    /// {X\lbrack i\rbrack\text{op}(A\lbrack i\rbrack) = \alpha B\lbrack i\rbrack} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_RIGHT}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $A\lbrack i\rbrack$ is a triangular matrix stored in lower or upper mode with or without the main diagonal, $X\lbrack i\rbrack$ and $B\lbrack i\rbrack$ are $m \times n$ matrices, and $\alpha$ is a scalar. Also, for matrix $A$:
    /// $$
    /// \text{op}(A\lbrack i\rbrack) = \left\\{ \begin{matrix}
    /// {A\lbrack i\rbrack} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A^{T}\lbrack i\rbrack} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// {A^{H}\lbrack i\rbrack} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// The solution $X\lbrack i\rbrack$ overwrites the right-hand-sides $B\lbrack i\rbrack$ on exit.
    ///
    /// No test for singularity or near-singularity is included in this function.
    ///
    /// This function works for any sizes but is intended to be used for matrices of small sizes where the launch overhead is a significant factor. For bigger sizes, it might be advantageous to call `batchCount` times the regular cublas&lt;t>trsm() within a set of CUDA streams.
    ///
    /// The current implementation is limited to devices with compute capability above or equal 2.0.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [strsm()](http://www.netlib.org/blas/strsm.f), [dtrsm()](http://www.netlib.org/blas/dtrsm.f), [ctrsm()](http://www.netlib.org/blas/ctrsm.f), [ztrsm()](http://www.netlib.org/blas/ztrsm.f).
    pub fn cublasStrsmBatched_64(
        handle: cublasHandle_t,
        side: cublasSideMode_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        m: i64,
        n: i64,
        alpha: *const f32,
        A: *const *const f32,
        lda: i64,
        B: *const *mut f32,
        ldb: i64,
        batchCount: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function solves an array of triangular linear systems with multiple right-hand-sides:
    /// $$
    /// \left\\{ \begin{matrix}
    /// {\text{op}(A\lbrack i\rbrack)X\lbrack i\rbrack = \alpha B\lbrack i\rbrack} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_LEFT}$}} \\\\
    /// {X\lbrack i\rbrack\text{op}(A\lbrack i\rbrack) = \alpha B\lbrack i\rbrack} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_RIGHT}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $A\lbrack i\rbrack$ is a triangular matrix stored in lower or upper mode with or without the main diagonal, $X\lbrack i\rbrack$ and $B\lbrack i\rbrack$ are $m \times n$ matrices, and $\alpha$ is a scalar. Also, for matrix $A$:
    /// $$
    /// \text{op}(A\lbrack i\rbrack) = \left\\{ \begin{matrix}
    /// {A\lbrack i\rbrack} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A^{T}\lbrack i\rbrack} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// {A^{H}\lbrack i\rbrack} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// The solution $X\lbrack i\rbrack$ overwrites the right-hand-sides $B\lbrack i\rbrack$ on exit.
    ///
    /// No test for singularity or near-singularity is included in this function.
    ///
    /// This function works for any sizes but is intended to be used for matrices of small sizes where the launch overhead is a significant factor. For bigger sizes, it might be advantageous to call `batchCount` times the regular cublas&lt;t>trsm() within a set of CUDA streams.
    ///
    /// The current implementation is limited to devices with compute capability above or equal 2.0.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [strsm()](http://www.netlib.org/blas/strsm.f), [dtrsm()](http://www.netlib.org/blas/dtrsm.f), [ctrsm()](http://www.netlib.org/blas/ctrsm.f), [ztrsm()](http://www.netlib.org/blas/ztrsm.f).
    pub fn cublasDtrsmBatched(
        handle: cublasHandle_t,
        side: cublasSideMode_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        alpha: *const f64,
        A: *const *const f64,
        lda: ::core::ffi::c_int,
        B: *const *mut f64,
        ldb: ::core::ffi::c_int,
        batchCount: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function solves an array of triangular linear systems with multiple right-hand-sides:
    /// $$
    /// \left\\{ \begin{matrix}
    /// {\text{op}(A\lbrack i\rbrack)X\lbrack i\rbrack = \alpha B\lbrack i\rbrack} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_LEFT}$}} \\\\
    /// {X\lbrack i\rbrack\text{op}(A\lbrack i\rbrack) = \alpha B\lbrack i\rbrack} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_RIGHT}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $A\lbrack i\rbrack$ is a triangular matrix stored in lower or upper mode with or without the main diagonal, $X\lbrack i\rbrack$ and $B\lbrack i\rbrack$ are $m \times n$ matrices, and $\alpha$ is a scalar. Also, for matrix $A$:
    /// $$
    /// \text{op}(A\lbrack i\rbrack) = \left\\{ \begin{matrix}
    /// {A\lbrack i\rbrack} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A^{T}\lbrack i\rbrack} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// {A^{H}\lbrack i\rbrack} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// The solution $X\lbrack i\rbrack$ overwrites the right-hand-sides $B\lbrack i\rbrack$ on exit.
    ///
    /// No test for singularity or near-singularity is included in this function.
    ///
    /// This function works for any sizes but is intended to be used for matrices of small sizes where the launch overhead is a significant factor. For bigger sizes, it might be advantageous to call `batchCount` times the regular cublas&lt;t>trsm() within a set of CUDA streams.
    ///
    /// The current implementation is limited to devices with compute capability above or equal 2.0.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [strsm()](http://www.netlib.org/blas/strsm.f), [dtrsm()](http://www.netlib.org/blas/dtrsm.f), [ctrsm()](http://www.netlib.org/blas/ctrsm.f), [ztrsm()](http://www.netlib.org/blas/ztrsm.f).
    pub fn cublasDtrsmBatched_64(
        handle: cublasHandle_t,
        side: cublasSideMode_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        m: i64,
        n: i64,
        alpha: *const f64,
        A: *const *const f64,
        lda: i64,
        B: *const *mut f64,
        ldb: i64,
        batchCount: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function solves an array of triangular linear systems with multiple right-hand-sides:
    /// $$
    /// \left\\{ \begin{matrix}
    /// {\text{op}(A\lbrack i\rbrack)X\lbrack i\rbrack = \alpha B\lbrack i\rbrack} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_LEFT}$}} \\\\
    /// {X\lbrack i\rbrack\text{op}(A\lbrack i\rbrack) = \alpha B\lbrack i\rbrack} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_RIGHT}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $A\lbrack i\rbrack$ is a triangular matrix stored in lower or upper mode with or without the main diagonal, $X\lbrack i\rbrack$ and $B\lbrack i\rbrack$ are $m \times n$ matrices, and $\alpha$ is a scalar. Also, for matrix $A$:
    /// $$
    /// \text{op}(A\lbrack i\rbrack) = \left\\{ \begin{matrix}
    /// {A\lbrack i\rbrack} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A^{T}\lbrack i\rbrack} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// {A^{H}\lbrack i\rbrack} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// The solution $X\lbrack i\rbrack$ overwrites the right-hand-sides $B\lbrack i\rbrack$ on exit.
    ///
    /// No test for singularity or near-singularity is included in this function.
    ///
    /// This function works for any sizes but is intended to be used for matrices of small sizes where the launch overhead is a significant factor. For bigger sizes, it might be advantageous to call `batchCount` times the regular cublas&lt;t>trsm() within a set of CUDA streams.
    ///
    /// The current implementation is limited to devices with compute capability above or equal 2.0.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [strsm()](http://www.netlib.org/blas/strsm.f), [dtrsm()](http://www.netlib.org/blas/dtrsm.f), [ctrsm()](http://www.netlib.org/blas/ctrsm.f), [ztrsm()](http://www.netlib.org/blas/ztrsm.f).
    pub fn cublasCtrsmBatched(
        handle: cublasHandle_t,
        side: cublasSideMode_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        alpha: *const cuComplex,
        A: *const *const cuComplex,
        lda: ::core::ffi::c_int,
        B: *const *mut cuComplex,
        ldb: ::core::ffi::c_int,
        batchCount: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function solves an array of triangular linear systems with multiple right-hand-sides:
    /// $$
    /// \left\\{ \begin{matrix}
    /// {\text{op}(A\lbrack i\rbrack)X\lbrack i\rbrack = \alpha B\lbrack i\rbrack} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_LEFT}$}} \\\\
    /// {X\lbrack i\rbrack\text{op}(A\lbrack i\rbrack) = \alpha B\lbrack i\rbrack} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_RIGHT}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $A\lbrack i\rbrack$ is a triangular matrix stored in lower or upper mode with or without the main diagonal, $X\lbrack i\rbrack$ and $B\lbrack i\rbrack$ are $m \times n$ matrices, and $\alpha$ is a scalar. Also, for matrix $A$:
    /// $$
    /// \text{op}(A\lbrack i\rbrack) = \left\\{ \begin{matrix}
    /// {A\lbrack i\rbrack} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A^{T}\lbrack i\rbrack} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// {A^{H}\lbrack i\rbrack} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// The solution $X\lbrack i\rbrack$ overwrites the right-hand-sides $B\lbrack i\rbrack$ on exit.
    ///
    /// No test for singularity or near-singularity is included in this function.
    ///
    /// This function works for any sizes but is intended to be used for matrices of small sizes where the launch overhead is a significant factor. For bigger sizes, it might be advantageous to call `batchCount` times the regular cublas&lt;t>trsm() within a set of CUDA streams.
    ///
    /// The current implementation is limited to devices with compute capability above or equal 2.0.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [strsm()](http://www.netlib.org/blas/strsm.f), [dtrsm()](http://www.netlib.org/blas/dtrsm.f), [ctrsm()](http://www.netlib.org/blas/ctrsm.f), [ztrsm()](http://www.netlib.org/blas/ztrsm.f).
    pub fn cublasCtrsmBatched_64(
        handle: cublasHandle_t,
        side: cublasSideMode_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        m: i64,
        n: i64,
        alpha: *const cuComplex,
        A: *const *const cuComplex,
        lda: i64,
        B: *const *mut cuComplex,
        ldb: i64,
        batchCount: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function solves an array of triangular linear systems with multiple right-hand-sides:
    /// $$
    /// \left\\{ \begin{matrix}
    /// {\text{op}(A\lbrack i\rbrack)X\lbrack i\rbrack = \alpha B\lbrack i\rbrack} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_LEFT}$}} \\\\
    /// {X\lbrack i\rbrack\text{op}(A\lbrack i\rbrack) = \alpha B\lbrack i\rbrack} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_RIGHT}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $A\lbrack i\rbrack$ is a triangular matrix stored in lower or upper mode with or without the main diagonal, $X\lbrack i\rbrack$ and $B\lbrack i\rbrack$ are $m \times n$ matrices, and $\alpha$ is a scalar. Also, for matrix $A$:
    /// $$
    /// \text{op}(A\lbrack i\rbrack) = \left\\{ \begin{matrix}
    /// {A\lbrack i\rbrack} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A^{T}\lbrack i\rbrack} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// {A^{H}\lbrack i\rbrack} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// The solution $X\lbrack i\rbrack$ overwrites the right-hand-sides $B\lbrack i\rbrack$ on exit.
    ///
    /// No test for singularity or near-singularity is included in this function.
    ///
    /// This function works for any sizes but is intended to be used for matrices of small sizes where the launch overhead is a significant factor. For bigger sizes, it might be advantageous to call `batchCount` times the regular cublas&lt;t>trsm() within a set of CUDA streams.
    ///
    /// The current implementation is limited to devices with compute capability above or equal 2.0.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [strsm()](http://www.netlib.org/blas/strsm.f), [dtrsm()](http://www.netlib.org/blas/dtrsm.f), [ctrsm()](http://www.netlib.org/blas/ctrsm.f), [ztrsm()](http://www.netlib.org/blas/ztrsm.f).
    pub fn cublasZtrsmBatched(
        handle: cublasHandle_t,
        side: cublasSideMode_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        alpha: *const cuDoubleComplex,
        A: *const *const cuDoubleComplex,
        lda: ::core::ffi::c_int,
        B: *const *mut cuDoubleComplex,
        ldb: ::core::ffi::c_int,
        batchCount: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function solves an array of triangular linear systems with multiple right-hand-sides:
    /// $$
    /// \left\\{ \begin{matrix}
    /// {\text{op}(A\lbrack i\rbrack)X\lbrack i\rbrack = \alpha B\lbrack i\rbrack} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_LEFT}$}} \\\\
    /// {X\lbrack i\rbrack\text{op}(A\lbrack i\rbrack) = \alpha B\lbrack i\rbrack} & {\text{if }\textsf{side == $\mathrm{CUBLAS_SIDE_RIGHT}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $A\lbrack i\rbrack$ is a triangular matrix stored in lower or upper mode with or without the main diagonal, $X\lbrack i\rbrack$ and $B\lbrack i\rbrack$ are $m \times n$ matrices, and $\alpha$ is a scalar. Also, for matrix $A$:
    /// $$
    /// \text{op}(A\lbrack i\rbrack) = \left\\{ \begin{matrix}
    /// {A\lbrack i\rbrack} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A^{T}\lbrack i\rbrack} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// {A^{H}\lbrack i\rbrack} & {\text{if }\textsf{transa == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// The solution $X\lbrack i\rbrack$ overwrites the right-hand-sides $B\lbrack i\rbrack$ on exit.
    ///
    /// No test for singularity or near-singularity is included in this function.
    ///
    /// This function works for any sizes but is intended to be used for matrices of small sizes where the launch overhead is a significant factor. For bigger sizes, it might be advantageous to call `batchCount` times the regular cublas&lt;t>trsm() within a set of CUDA streams.
    ///
    /// The current implementation is limited to devices with compute capability above or equal 2.0.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [strsm()](http://www.netlib.org/blas/strsm.f), [dtrsm()](http://www.netlib.org/blas/dtrsm.f), [ctrsm()](http://www.netlib.org/blas/ctrsm.f), [ztrsm()](http://www.netlib.org/blas/ztrsm.f).
    pub fn cublasZtrsmBatched_64(
        handle: cublasHandle_t,
        side: cublasSideMode_t,
        uplo: cublasFillMode_t,
        trans: cublasOperation_t,
        diag: cublasDiagType_t,
        m: i64,
        n: i64,
        alpha: *const cuDoubleComplex,
        A: *const *const cuDoubleComplex,
        lda: i64,
        B: *const *mut cuDoubleComplex,
        ldb: i64,
        batchCount: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-matrix multiplication:
    /// $$
    /// C = \left\\{ \begin{matrix}
    /// {A \times diag(X)} & {\text{if }\textsf{mode == $\mathrm{CUBLAS_SIDE_RIGHT}$}} \\\\
    /// {diag(X) \times A} & {\text{if }\textsf{mode == $\mathrm{CUBLAS_SIDE_LEFT}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $A$ and $C$ are matrices stored in column-major format with dimensions $m \times n$. $X$ is a vector of size $n$ if `mode == CUBLAS_SIDE_RIGHT` and of size $m$ if `mode == CUBLAS_SIDE_LEFT`. $X$ is gathered from one-dimensional array x with stride `incx`. The absolute value of `incx` is the stride and the sign of `incx` is direction of the stride. If `incx` is positive, then we forward x from the first element. Otherwise, we backward x from the last element. The formula of X is:
    /// $$
    /// X\lbrack j\rbrack = \left\\{ \begin{matrix}
    /// {x\lbrack j \times incx\rbrack} & {\text{if }incx \geq 0} \\\\
    /// {x\lbrack(\chi - 1) \times |incx| - j \times |incx|\rbrack} & {\text{if }incx &lt; 0} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $\chi = m$ if `mode == CUBLAS_SIDE_LEFT` and $\chi = n$ if `mode == CUBLAS_SIDE_RIGHT`.
    ///
    /// Example 1: if the user wants to perform $diag(diag(B)) \times A$, then $incx = ldb + 1$ where $ldb$ is leading dimension of matrix `B`, either row-major or column-major.
    ///
    /// Example 2: if the user wants to perform $\alpha \times A$, then there are two choices, either cublas&lt;t>geam() with `*beta == 0` and `transa == CUBLAS_OP_N` or cublas&lt;t>dgmm() with `incx == 0` and `x\[0\] == alpha`.
    ///
    /// The operation is out-of-place. The in-place only works if `lda == ldc`.
    pub fn cublasSdgmm(
        handle: cublasHandle_t,
        mode: cublasSideMode_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        A: *const f32,
        lda: ::core::ffi::c_int,
        x: *const f32,
        incx: ::core::ffi::c_int,
        C: *mut f32,
        ldc: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-matrix multiplication:
    /// $$
    /// C = \left\\{ \begin{matrix}
    /// {A \times diag(X)} & {\text{if }\textsf{mode == $\mathrm{CUBLAS_SIDE_RIGHT}$}} \\\\
    /// {diag(X) \times A} & {\text{if }\textsf{mode == $\mathrm{CUBLAS_SIDE_LEFT}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $A$ and $C$ are matrices stored in column-major format with dimensions $m \times n$. $X$ is a vector of size $n$ if `mode == CUBLAS_SIDE_RIGHT` and of size $m$ if `mode == CUBLAS_SIDE_LEFT`. $X$ is gathered from one-dimensional array x with stride `incx`. The absolute value of `incx` is the stride and the sign of `incx` is direction of the stride. If `incx` is positive, then we forward x from the first element. Otherwise, we backward x from the last element. The formula of X is:
    /// $$
    /// X\lbrack j\rbrack = \left\\{ \begin{matrix}
    /// {x\lbrack j \times incx\rbrack} & {\text{if }incx \geq 0} \\\\
    /// {x\lbrack(\chi - 1) \times |incx| - j \times |incx|\rbrack} & {\text{if }incx &lt; 0} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $\chi = m$ if `mode == CUBLAS_SIDE_LEFT` and $\chi = n$ if `mode == CUBLAS_SIDE_RIGHT`.
    ///
    /// Example 1: if the user wants to perform $diag(diag(B)) \times A$, then $incx = ldb + 1$ where $ldb$ is leading dimension of matrix `B`, either row-major or column-major.
    ///
    /// Example 2: if the user wants to perform $\alpha \times A$, then there are two choices, either cublas&lt;t>geam() with `*beta == 0` and `transa == CUBLAS_OP_N` or cublas&lt;t>dgmm() with `incx == 0` and `x\[0\] == alpha`.
    ///
    /// The operation is out-of-place. The in-place only works if `lda == ldc`.
    pub fn cublasSdgmm_64(
        handle: cublasHandle_t,
        mode: cublasSideMode_t,
        m: i64,
        n: i64,
        A: *const f32,
        lda: i64,
        x: *const f32,
        incx: i64,
        C: *mut f32,
        ldc: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-matrix multiplication:
    /// $$
    /// C = \left\\{ \begin{matrix}
    /// {A \times diag(X)} & {\text{if }\textsf{mode == $\mathrm{CUBLAS_SIDE_RIGHT}$}} \\\\
    /// {diag(X) \times A} & {\text{if }\textsf{mode == $\mathrm{CUBLAS_SIDE_LEFT}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $A$ and $C$ are matrices stored in column-major format with dimensions $m \times n$. $X$ is a vector of size $n$ if `mode == CUBLAS_SIDE_RIGHT` and of size $m$ if `mode == CUBLAS_SIDE_LEFT`. $X$ is gathered from one-dimensional array x with stride `incx`. The absolute value of `incx` is the stride and the sign of `incx` is direction of the stride. If `incx` is positive, then we forward x from the first element. Otherwise, we backward x from the last element. The formula of X is:
    /// $$
    /// X\lbrack j\rbrack = \left\\{ \begin{matrix}
    /// {x\lbrack j \times incx\rbrack} & {\text{if }incx \geq 0} \\\\
    /// {x\lbrack(\chi - 1) \times |incx| - j \times |incx|\rbrack} & {\text{if }incx &lt; 0} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $\chi = m$ if `mode == CUBLAS_SIDE_LEFT` and $\chi = n$ if `mode == CUBLAS_SIDE_RIGHT`.
    ///
    /// Example 1: if the user wants to perform $diag(diag(B)) \times A$, then $incx = ldb + 1$ where $ldb$ is leading dimension of matrix `B`, either row-major or column-major.
    ///
    /// Example 2: if the user wants to perform $\alpha \times A$, then there are two choices, either cublas&lt;t>geam() with `*beta == 0` and `transa == CUBLAS_OP_N` or cublas&lt;t>dgmm() with `incx == 0` and `x\[0\] == alpha`.
    ///
    /// The operation is out-of-place. The in-place only works if `lda == ldc`.
    pub fn cublasDdgmm(
        handle: cublasHandle_t,
        mode: cublasSideMode_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        A: *const f64,
        lda: ::core::ffi::c_int,
        x: *const f64,
        incx: ::core::ffi::c_int,
        C: *mut f64,
        ldc: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-matrix multiplication:
    /// $$
    /// C = \left\\{ \begin{matrix}
    /// {A \times diag(X)} & {\text{if }\textsf{mode == $\mathrm{CUBLAS_SIDE_RIGHT}$}} \\\\
    /// {diag(X) \times A} & {\text{if }\textsf{mode == $\mathrm{CUBLAS_SIDE_LEFT}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $A$ and $C$ are matrices stored in column-major format with dimensions $m \times n$. $X$ is a vector of size $n$ if `mode == CUBLAS_SIDE_RIGHT` and of size $m$ if `mode == CUBLAS_SIDE_LEFT`. $X$ is gathered from one-dimensional array x with stride `incx`. The absolute value of `incx` is the stride and the sign of `incx` is direction of the stride. If `incx` is positive, then we forward x from the first element. Otherwise, we backward x from the last element. The formula of X is:
    /// $$
    /// X\lbrack j\rbrack = \left\\{ \begin{matrix}
    /// {x\lbrack j \times incx\rbrack} & {\text{if }incx \geq 0} \\\\
    /// {x\lbrack(\chi - 1) \times |incx| - j \times |incx|\rbrack} & {\text{if }incx &lt; 0} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $\chi = m$ if `mode == CUBLAS_SIDE_LEFT` and $\chi = n$ if `mode == CUBLAS_SIDE_RIGHT`.
    ///
    /// Example 1: if the user wants to perform $diag(diag(B)) \times A$, then $incx = ldb + 1$ where $ldb$ is leading dimension of matrix `B`, either row-major or column-major.
    ///
    /// Example 2: if the user wants to perform $\alpha \times A$, then there are two choices, either cublas&lt;t>geam() with `*beta == 0` and `transa == CUBLAS_OP_N` or cublas&lt;t>dgmm() with `incx == 0` and `x\[0\] == alpha`.
    ///
    /// The operation is out-of-place. The in-place only works if `lda == ldc`.
    pub fn cublasDdgmm_64(
        handle: cublasHandle_t,
        mode: cublasSideMode_t,
        m: i64,
        n: i64,
        A: *const f64,
        lda: i64,
        x: *const f64,
        incx: i64,
        C: *mut f64,
        ldc: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-matrix multiplication:
    /// $$
    /// C = \left\\{ \begin{matrix}
    /// {A \times diag(X)} & {\text{if }\textsf{mode == $\mathrm{CUBLAS_SIDE_RIGHT}$}} \\\\
    /// {diag(X) \times A} & {\text{if }\textsf{mode == $\mathrm{CUBLAS_SIDE_LEFT}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $A$ and $C$ are matrices stored in column-major format with dimensions $m \times n$. $X$ is a vector of size $n$ if `mode == CUBLAS_SIDE_RIGHT` and of size $m$ if `mode == CUBLAS_SIDE_LEFT`. $X$ is gathered from one-dimensional array x with stride `incx`. The absolute value of `incx` is the stride and the sign of `incx` is direction of the stride. If `incx` is positive, then we forward x from the first element. Otherwise, we backward x from the last element. The formula of X is:
    /// $$
    /// X\lbrack j\rbrack = \left\\{ \begin{matrix}
    /// {x\lbrack j \times incx\rbrack} & {\text{if }incx \geq 0} \\\\
    /// {x\lbrack(\chi - 1) \times |incx| - j \times |incx|\rbrack} & {\text{if }incx &lt; 0} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $\chi = m$ if `mode == CUBLAS_SIDE_LEFT` and $\chi = n$ if `mode == CUBLAS_SIDE_RIGHT`.
    ///
    /// Example 1: if the user wants to perform $diag(diag(B)) \times A$, then $incx = ldb + 1$ where $ldb$ is leading dimension of matrix `B`, either row-major or column-major.
    ///
    /// Example 2: if the user wants to perform $\alpha \times A$, then there are two choices, either cublas&lt;t>geam() with `*beta == 0` and `transa == CUBLAS_OP_N` or cublas&lt;t>dgmm() with `incx == 0` and `x\[0\] == alpha`.
    ///
    /// The operation is out-of-place. The in-place only works if `lda == ldc`.
    pub fn cublasCdgmm(
        handle: cublasHandle_t,
        mode: cublasSideMode_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        A: *const cuComplex,
        lda: ::core::ffi::c_int,
        x: *const cuComplex,
        incx: ::core::ffi::c_int,
        C: *mut cuComplex,
        ldc: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-matrix multiplication:
    /// $$
    /// C = \left\\{ \begin{matrix}
    /// {A \times diag(X)} & {\text{if }\textsf{mode == $\mathrm{CUBLAS_SIDE_RIGHT}$}} \\\\
    /// {diag(X) \times A} & {\text{if }\textsf{mode == $\mathrm{CUBLAS_SIDE_LEFT}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $A$ and $C$ are matrices stored in column-major format with dimensions $m \times n$. $X$ is a vector of size $n$ if `mode == CUBLAS_SIDE_RIGHT` and of size $m$ if `mode == CUBLAS_SIDE_LEFT`. $X$ is gathered from one-dimensional array x with stride `incx`. The absolute value of `incx` is the stride and the sign of `incx` is direction of the stride. If `incx` is positive, then we forward x from the first element. Otherwise, we backward x from the last element. The formula of X is:
    /// $$
    /// X\lbrack j\rbrack = \left\\{ \begin{matrix}
    /// {x\lbrack j \times incx\rbrack} & {\text{if }incx \geq 0} \\\\
    /// {x\lbrack(\chi - 1) \times |incx| - j \times |incx|\rbrack} & {\text{if }incx &lt; 0} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $\chi = m$ if `mode == CUBLAS_SIDE_LEFT` and $\chi = n$ if `mode == CUBLAS_SIDE_RIGHT`.
    ///
    /// Example 1: if the user wants to perform $diag(diag(B)) \times A$, then $incx = ldb + 1$ where $ldb$ is leading dimension of matrix `B`, either row-major or column-major.
    ///
    /// Example 2: if the user wants to perform $\alpha \times A$, then there are two choices, either cublas&lt;t>geam() with `*beta == 0` and `transa == CUBLAS_OP_N` or cublas&lt;t>dgmm() with `incx == 0` and `x\[0\] == alpha`.
    ///
    /// The operation is out-of-place. The in-place only works if `lda == ldc`.
    pub fn cublasCdgmm_64(
        handle: cublasHandle_t,
        mode: cublasSideMode_t,
        m: i64,
        n: i64,
        A: *const cuComplex,
        lda: i64,
        x: *const cuComplex,
        incx: i64,
        C: *mut cuComplex,
        ldc: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-matrix multiplication:
    /// $$
    /// C = \left\\{ \begin{matrix}
    /// {A \times diag(X)} & {\text{if }\textsf{mode == $\mathrm{CUBLAS_SIDE_RIGHT}$}} \\\\
    /// {diag(X) \times A} & {\text{if }\textsf{mode == $\mathrm{CUBLAS_SIDE_LEFT}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $A$ and $C$ are matrices stored in column-major format with dimensions $m \times n$. $X$ is a vector of size $n$ if `mode == CUBLAS_SIDE_RIGHT` and of size $m$ if `mode == CUBLAS_SIDE_LEFT`. $X$ is gathered from one-dimensional array x with stride `incx`. The absolute value of `incx` is the stride and the sign of `incx` is direction of the stride. If `incx` is positive, then we forward x from the first element. Otherwise, we backward x from the last element. The formula of X is:
    /// $$
    /// X\lbrack j\rbrack = \left\\{ \begin{matrix}
    /// {x\lbrack j \times incx\rbrack} & {\text{if }incx \geq 0} \\\\
    /// {x\lbrack(\chi - 1) \times |incx| - j \times |incx|\rbrack} & {\text{if }incx &lt; 0} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $\chi = m$ if `mode == CUBLAS_SIDE_LEFT` and $\chi = n$ if `mode == CUBLAS_SIDE_RIGHT`.
    ///
    /// Example 1: if the user wants to perform $diag(diag(B)) \times A$, then $incx = ldb + 1$ where $ldb$ is leading dimension of matrix `B`, either row-major or column-major.
    ///
    /// Example 2: if the user wants to perform $\alpha \times A$, then there are two choices, either cublas&lt;t>geam() with `*beta == 0` and `transa == CUBLAS_OP_N` or cublas&lt;t>dgmm() with `incx == 0` and `x\[0\] == alpha`.
    ///
    /// The operation is out-of-place. The in-place only works if `lda == ldc`.
    pub fn cublasZdgmm(
        handle: cublasHandle_t,
        mode: cublasSideMode_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        A: *const cuDoubleComplex,
        lda: ::core::ffi::c_int,
        x: *const cuDoubleComplex,
        incx: ::core::ffi::c_int,
        C: *mut cuDoubleComplex,
        ldc: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function supports the 64-bit Integer Interface.
    ///
    /// This function performs the matrix-matrix multiplication:
    /// $$
    /// C = \left\\{ \begin{matrix}
    /// {A \times diag(X)} & {\text{if }\textsf{mode == $\mathrm{CUBLAS_SIDE_RIGHT}$}} \\\\
    /// {diag(X) \times A} & {\text{if }\textsf{mode == $\mathrm{CUBLAS_SIDE_LEFT}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $A$ and $C$ are matrices stored in column-major format with dimensions $m \times n$. $X$ is a vector of size $n$ if `mode == CUBLAS_SIDE_RIGHT` and of size $m$ if `mode == CUBLAS_SIDE_LEFT`. $X$ is gathered from one-dimensional array x with stride `incx`. The absolute value of `incx` is the stride and the sign of `incx` is direction of the stride. If `incx` is positive, then we forward x from the first element. Otherwise, we backward x from the last element. The formula of X is:
    /// $$
    /// X\lbrack j\rbrack = \left\\{ \begin{matrix}
    /// {x\lbrack j \times incx\rbrack} & {\text{if }incx \geq 0} \\\\
    /// {x\lbrack(\chi - 1) \times |incx| - j \times |incx|\rbrack} & {\text{if }incx &lt; 0} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// where $\chi = m$ if `mode == CUBLAS_SIDE_LEFT` and $\chi = n$ if `mode == CUBLAS_SIDE_RIGHT`.
    ///
    /// Example 1: if the user wants to perform $diag(diag(B)) \times A$, then $incx = ldb + 1$ where $ldb$ is leading dimension of matrix `B`, either row-major or column-major.
    ///
    /// Example 2: if the user wants to perform $\alpha \times A$, then there are two choices, either cublas&lt;t>geam() with `*beta == 0` and `transa == CUBLAS_OP_N` or cublas&lt;t>dgmm() with `incx == 0` and `x\[0\] == alpha`.
    ///
    /// The operation is out-of-place. The in-place only works if `lda == ldc`.
    pub fn cublasZdgmm_64(
        handle: cublasHandle_t,
        mode: cublasSideMode_t,
        m: i64,
        n: i64,
        A: *const cuDoubleComplex,
        lda: i64,
        x: *const cuDoubleComplex,
        incx: i64,
        C: *mut cuDoubleComplex,
        ldc: i64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// `A` and `Ainv` are arrays of pointers to matrices stored in column-major format with dimensions `n*n` and leading dimension `lda` and `lda_inv` respectively.
    ///
    /// This function performs the inversion of matrices `A\[i\]` for i = 0, …, `batchSize-1`.
    ///
    /// This function is a short cut of cublas&lt;t>getrfBatched() plus cublas&lt;t>getriBatched(). However it doesn’t work if `n` is greater than 32. If not, the user has to go through cublas&lt;t>getrfBatched() and cublas&lt;t>getriBatched().
    ///
    /// If the matrix `A\[i\]` is singular, then `info\[i\]` reports singularity, the same as cublas&lt;t>getrfBatched().
    pub fn cublasSmatinvBatched(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        A: *const *const f32,
        lda: ::core::ffi::c_int,
        Ainv: *const *mut f32,
        lda_inv: ::core::ffi::c_int,
        info: *mut ::core::ffi::c_int,
        batchSize: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// `A` and `Ainv` are arrays of pointers to matrices stored in column-major format with dimensions `n*n` and leading dimension `lda` and `lda_inv` respectively.
    ///
    /// This function performs the inversion of matrices `A\[i\]` for i = 0, …, `batchSize-1`.
    ///
    /// This function is a short cut of cublas&lt;t>getrfBatched() plus cublas&lt;t>getriBatched(). However it doesn’t work if `n` is greater than 32. If not, the user has to go through cublas&lt;t>getrfBatched() and cublas&lt;t>getriBatched().
    ///
    /// If the matrix `A\[i\]` is singular, then `info\[i\]` reports singularity, the same as cublas&lt;t>getrfBatched().
    pub fn cublasDmatinvBatched(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        A: *const *const f64,
        lda: ::core::ffi::c_int,
        Ainv: *const *mut f64,
        lda_inv: ::core::ffi::c_int,
        info: *mut ::core::ffi::c_int,
        batchSize: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// `A` and `Ainv` are arrays of pointers to matrices stored in column-major format with dimensions `n*n` and leading dimension `lda` and `lda_inv` respectively.
    ///
    /// This function performs the inversion of matrices `A\[i\]` for i = 0, …, `batchSize-1`.
    ///
    /// This function is a short cut of cublas&lt;t>getrfBatched() plus cublas&lt;t>getriBatched(). However it doesn’t work if `n` is greater than 32. If not, the user has to go through cublas&lt;t>getrfBatched() and cublas&lt;t>getriBatched().
    ///
    /// If the matrix `A\[i\]` is singular, then `info\[i\]` reports singularity, the same as cublas&lt;t>getrfBatched().
    pub fn cublasCmatinvBatched(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        A: *const *const cuComplex,
        lda: ::core::ffi::c_int,
        Ainv: *const *mut cuComplex,
        lda_inv: ::core::ffi::c_int,
        info: *mut ::core::ffi::c_int,
        batchSize: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// `A` and `Ainv` are arrays of pointers to matrices stored in column-major format with dimensions `n*n` and leading dimension `lda` and `lda_inv` respectively.
    ///
    /// This function performs the inversion of matrices `A\[i\]` for i = 0, …, `batchSize-1`.
    ///
    /// This function is a short cut of cublas&lt;t>getrfBatched() plus cublas&lt;t>getriBatched(). However it doesn’t work if `n` is greater than 32. If not, the user has to go through cublas&lt;t>getrfBatched() and cublas&lt;t>getriBatched().
    ///
    /// If the matrix `A\[i\]` is singular, then `info\[i\]` reports singularity, the same as cublas&lt;t>getrfBatched().
    pub fn cublasZmatinvBatched(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        A: *const *const cuDoubleComplex,
        lda: ::core::ffi::c_int,
        Ainv: *const *mut cuDoubleComplex,
        lda_inv: ::core::ffi::c_int,
        info: *mut ::core::ffi::c_int,
        batchSize: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// `Aarray` is an array of pointers to matrices stored in column-major format with dimensions `m x n` and leading dimension `lda`. `TauArray` is an array of pointers to vectors of dimension of at least `max (1, min(m, n)`.
    ///
    /// This function performs the QR factorization of each `Aarray\[i\]` for `i = 0, ...,batchSize-1` using Householder reflections. Each matrix `Q\[i\]` is represented as a product of elementary reflectors and is stored in the lower part of each `Aarray\[i\]` as follows :
    ///
    /// Each H[j](http://www.netlib.org/lapack/single/sgeqrf.f), [dgeqrf()](http://www.netlib.org/lapack/double/dgeqrf.f), [cgeqrf()](http://www.netlib.org/lapack/complex/cgeqrf.f), [zgeqrf()](http://www.netlib.org/lapack/complex16/zgeqrf.f).
    pub fn cublasSgeqrfBatched(
        handle: cublasHandle_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        Aarray: *const *mut f32,
        lda: ::core::ffi::c_int,
        TauArray: *const *mut f32,
        info: *mut ::core::ffi::c_int,
        batchSize: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// `Aarray` is an array of pointers to matrices stored in column-major format with dimensions `m x n` and leading dimension `lda`. `TauArray` is an array of pointers to vectors of dimension of at least `max (1, min(m, n)`.
    ///
    /// This function performs the QR factorization of each `Aarray\[i\]` for `i = 0, ...,batchSize-1` using Householder reflections. Each matrix `Q\[i\]` is represented as a product of elementary reflectors and is stored in the lower part of each `Aarray\[i\]` as follows :
    ///
    /// Each H[j](http://www.netlib.org/lapack/single/sgeqrf.f), [dgeqrf()](http://www.netlib.org/lapack/double/dgeqrf.f), [cgeqrf()](http://www.netlib.org/lapack/complex/cgeqrf.f), [zgeqrf()](http://www.netlib.org/lapack/complex16/zgeqrf.f).
    pub fn cublasDgeqrfBatched(
        handle: cublasHandle_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        Aarray: *const *mut f64,
        lda: ::core::ffi::c_int,
        TauArray: *const *mut f64,
        info: *mut ::core::ffi::c_int,
        batchSize: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// `Aarray` is an array of pointers to matrices stored in column-major format with dimensions `m x n` and leading dimension `lda`. `TauArray` is an array of pointers to vectors of dimension of at least `max (1, min(m, n)`.
    ///
    /// This function performs the QR factorization of each `Aarray\[i\]` for `i = 0, ...,batchSize-1` using Householder reflections. Each matrix `Q\[i\]` is represented as a product of elementary reflectors and is stored in the lower part of each `Aarray\[i\]` as follows :
    ///
    /// Each H[j](http://www.netlib.org/lapack/single/sgeqrf.f), [dgeqrf()](http://www.netlib.org/lapack/double/dgeqrf.f), [cgeqrf()](http://www.netlib.org/lapack/complex/cgeqrf.f), [zgeqrf()](http://www.netlib.org/lapack/complex16/zgeqrf.f).
    pub fn cublasCgeqrfBatched(
        handle: cublasHandle_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        Aarray: *const *mut cuComplex,
        lda: ::core::ffi::c_int,
        TauArray: *const *mut cuComplex,
        info: *mut ::core::ffi::c_int,
        batchSize: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// `Aarray` is an array of pointers to matrices stored in column-major format with dimensions `m x n` and leading dimension `lda`. `TauArray` is an array of pointers to vectors of dimension of at least `max (1, min(m, n)`.
    ///
    /// This function performs the QR factorization of each `Aarray\[i\]` for `i = 0, ...,batchSize-1` using Householder reflections. Each matrix `Q\[i\]` is represented as a product of elementary reflectors and is stored in the lower part of each `Aarray\[i\]` as follows :
    ///
    /// Each H[j](http://www.netlib.org/lapack/single/sgeqrf.f), [dgeqrf()](http://www.netlib.org/lapack/double/dgeqrf.f), [cgeqrf()](http://www.netlib.org/lapack/complex/cgeqrf.f), [zgeqrf()](http://www.netlib.org/lapack/complex16/zgeqrf.f).
    pub fn cublasZgeqrfBatched(
        handle: cublasHandle_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        Aarray: *const *mut cuDoubleComplex,
        lda: ::core::ffi::c_int,
        TauArray: *const *mut cuDoubleComplex,
        info: *mut ::core::ffi::c_int,
        batchSize: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// `Aarray` is an array of pointers to matrices stored in column-major format. `Carray` is an array of pointers to matrices stored in column-major format.
    ///
    /// This function find the least squares solution of a batch of overdetermined systems: it solves the least squares problem described as follows :
    ///
    /// On exit, each `Aarray\[i\]` is overwritten with their QR factorization and each `Carray\[i\]` is overwritten with the least square solution
    ///
    /// cublas&lt;t>gelsBatched supports only the non-transpose operation and only solves over-determined systems (m >= n).
    ///
    /// cublas&lt;t>gelsBatched only supports compute capability 2.0 or above.
    ///
    /// This function is intended to be used for matrices of small sizes where the launch overhead is a significant factor.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sgels()](http://www.netlib.org/lapack/single/sgels.f), [dgels()](http://www.netlib.org/lapack/double/dgels.f), [cgels()](http://www.netlib.org/lapack/complex/cgels.f), [zgels()](http://www.netlib.org/lapack/complex16/zgels.f).
    pub fn cublasSgelsBatched(
        handle: cublasHandle_t,
        trans: cublasOperation_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        nrhs: ::core::ffi::c_int,
        Aarray: *const *mut f32,
        lda: ::core::ffi::c_int,
        Carray: *const *mut f32,
        ldc: ::core::ffi::c_int,
        info: *mut ::core::ffi::c_int,
        devInfoArray: *mut ::core::ffi::c_int,
        batchSize: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// `Aarray` is an array of pointers to matrices stored in column-major format. `Carray` is an array of pointers to matrices stored in column-major format.
    ///
    /// This function find the least squares solution of a batch of overdetermined systems: it solves the least squares problem described as follows :
    ///
    /// On exit, each `Aarray\[i\]` is overwritten with their QR factorization and each `Carray\[i\]` is overwritten with the least square solution
    ///
    /// cublas&lt;t>gelsBatched supports only the non-transpose operation and only solves over-determined systems (m >= n).
    ///
    /// cublas&lt;t>gelsBatched only supports compute capability 2.0 or above.
    ///
    /// This function is intended to be used for matrices of small sizes where the launch overhead is a significant factor.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sgels()](http://www.netlib.org/lapack/single/sgels.f), [dgels()](http://www.netlib.org/lapack/double/dgels.f), [cgels()](http://www.netlib.org/lapack/complex/cgels.f), [zgels()](http://www.netlib.org/lapack/complex16/zgels.f).
    pub fn cublasDgelsBatched(
        handle: cublasHandle_t,
        trans: cublasOperation_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        nrhs: ::core::ffi::c_int,
        Aarray: *const *mut f64,
        lda: ::core::ffi::c_int,
        Carray: *const *mut f64,
        ldc: ::core::ffi::c_int,
        info: *mut ::core::ffi::c_int,
        devInfoArray: *mut ::core::ffi::c_int,
        batchSize: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// `Aarray` is an array of pointers to matrices stored in column-major format. `Carray` is an array of pointers to matrices stored in column-major format.
    ///
    /// This function find the least squares solution of a batch of overdetermined systems: it solves the least squares problem described as follows :
    ///
    /// On exit, each `Aarray\[i\]` is overwritten with their QR factorization and each `Carray\[i\]` is overwritten with the least square solution
    ///
    /// cublas&lt;t>gelsBatched supports only the non-transpose operation and only solves over-determined systems (m >= n).
    ///
    /// cublas&lt;t>gelsBatched only supports compute capability 2.0 or above.
    ///
    /// This function is intended to be used for matrices of small sizes where the launch overhead is a significant factor.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sgels()](http://www.netlib.org/lapack/single/sgels.f), [dgels()](http://www.netlib.org/lapack/double/dgels.f), [cgels()](http://www.netlib.org/lapack/complex/cgels.f), [zgels()](http://www.netlib.org/lapack/complex16/zgels.f).
    pub fn cublasCgelsBatched(
        handle: cublasHandle_t,
        trans: cublasOperation_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        nrhs: ::core::ffi::c_int,
        Aarray: *const *mut cuComplex,
        lda: ::core::ffi::c_int,
        Carray: *const *mut cuComplex,
        ldc: ::core::ffi::c_int,
        info: *mut ::core::ffi::c_int,
        devInfoArray: *mut ::core::ffi::c_int,
        batchSize: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// `Aarray` is an array of pointers to matrices stored in column-major format. `Carray` is an array of pointers to matrices stored in column-major format.
    ///
    /// This function find the least squares solution of a batch of overdetermined systems: it solves the least squares problem described as follows :
    ///
    /// On exit, each `Aarray\[i\]` is overwritten with their QR factorization and each `Carray\[i\]` is overwritten with the least square solution
    ///
    /// cublas&lt;t>gelsBatched supports only the non-transpose operation and only solves over-determined systems (m >= n).
    ///
    /// cublas&lt;t>gelsBatched only supports compute capability 2.0 or above.
    ///
    /// This function is intended to be used for matrices of small sizes where the launch overhead is a significant factor.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sgels()](http://www.netlib.org/lapack/single/sgels.f), [dgels()](http://www.netlib.org/lapack/double/dgels.f), [cgels()](http://www.netlib.org/lapack/complex/cgels.f), [zgels()](http://www.netlib.org/lapack/complex16/zgels.f).
    pub fn cublasZgelsBatched(
        handle: cublasHandle_t,
        trans: cublasOperation_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        nrhs: ::core::ffi::c_int,
        Aarray: *const *mut cuDoubleComplex,
        lda: ::core::ffi::c_int,
        Carray: *const *mut cuDoubleComplex,
        ldc: ::core::ffi::c_int,
        info: *mut ::core::ffi::c_int,
        devInfoArray: *mut ::core::ffi::c_int,
        batchSize: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function performs the conversion from the triangular packed format to the triangular format
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the elements of `AP` are copied into the lower triangular part of the triangular matrix `A` and the upper part of `A` is left untouched. If `uplo == CUBLAS_FILL_MODE_UPPER` then the elements of `AP` are copied into the upper triangular part of the triangular matrix `A` and the lower part of `A` is left untouched.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [stpttr()](http://www.netlib.org/lapack/explore-html/d7/d70/stpttr_8f.html), [dtpttr()](http://www.netlib.org/lapack/explore-html/df/d63/dtpttr_8f.html), [ctpttr()](http://www.netlib.org/lapack/explore-html/de/d13/ctpttr_8f.html), [ztpttr()](http://www.netlib.org/lapack/explore-html/d6/dbc/ztpttr_8f.html).
    pub fn cublasStpttr(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: ::core::ffi::c_int,
        AP: *const f32,
        A: *mut f32,
        lda: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function performs the conversion from the triangular packed format to the triangular format
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the elements of `AP` are copied into the lower triangular part of the triangular matrix `A` and the upper part of `A` is left untouched. If `uplo == CUBLAS_FILL_MODE_UPPER` then the elements of `AP` are copied into the upper triangular part of the triangular matrix `A` and the lower part of `A` is left untouched.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [stpttr()](http://www.netlib.org/lapack/explore-html/d7/d70/stpttr_8f.html), [dtpttr()](http://www.netlib.org/lapack/explore-html/df/d63/dtpttr_8f.html), [ctpttr()](http://www.netlib.org/lapack/explore-html/de/d13/ctpttr_8f.html), [ztpttr()](http://www.netlib.org/lapack/explore-html/d6/dbc/ztpttr_8f.html).
    pub fn cublasDtpttr(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: ::core::ffi::c_int,
        AP: *const f64,
        A: *mut f64,
        lda: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function performs the conversion from the triangular packed format to the triangular format
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the elements of `AP` are copied into the lower triangular part of the triangular matrix `A` and the upper part of `A` is left untouched. If `uplo == CUBLAS_FILL_MODE_UPPER` then the elements of `AP` are copied into the upper triangular part of the triangular matrix `A` and the lower part of `A` is left untouched.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [stpttr()](http://www.netlib.org/lapack/explore-html/d7/d70/stpttr_8f.html), [dtpttr()](http://www.netlib.org/lapack/explore-html/df/d63/dtpttr_8f.html), [ctpttr()](http://www.netlib.org/lapack/explore-html/de/d13/ctpttr_8f.html), [ztpttr()](http://www.netlib.org/lapack/explore-html/d6/dbc/ztpttr_8f.html).
    pub fn cublasCtpttr(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: ::core::ffi::c_int,
        AP: *const cuComplex,
        A: *mut cuComplex,
        lda: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function performs the conversion from the triangular packed format to the triangular format
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the elements of `AP` are copied into the lower triangular part of the triangular matrix `A` and the upper part of `A` is left untouched. If `uplo == CUBLAS_FILL_MODE_UPPER` then the elements of `AP` are copied into the upper triangular part of the triangular matrix `A` and the lower part of `A` is left untouched.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [stpttr()](http://www.netlib.org/lapack/explore-html/d7/d70/stpttr_8f.html), [dtpttr()](http://www.netlib.org/lapack/explore-html/df/d63/dtpttr_8f.html), [ctpttr()](http://www.netlib.org/lapack/explore-html/de/d13/ctpttr_8f.html), [ztpttr()](http://www.netlib.org/lapack/explore-html/d6/dbc/ztpttr_8f.html).
    pub fn cublasZtpttr(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: ::core::ffi::c_int,
        AP: *const cuDoubleComplex,
        A: *mut cuDoubleComplex,
        lda: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function performs the conversion from the triangular format to the triangular packed format
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the lower triangular part of the triangular matrix `A` is copied into the array `AP`. If `uplo == CUBLAS_FILL_MODE_UPPER` then then the upper triangular part of the triangular matrix `A` is copied into the array `AP`.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [strttp()](http://www.netlib.org/lapack/explore-html/d9/def/strttp_8f.html), [dtrttp()](http://www.netlib.org/lapack/explore-html/d0/daf/dtrttp_8f.html), [ctrttp()](http://www.netlib.org/lapack/explore-html/d7/d56/ctrttp_8f.html), [ztrttp()](http://www.netlib.org/lapack/explore-html/da/dc2/ztrttp_8f.html).
    pub fn cublasStrttp(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: ::core::ffi::c_int,
        A: *const f32,
        lda: ::core::ffi::c_int,
        AP: *mut f32,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function performs the conversion from the triangular format to the triangular packed format
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the lower triangular part of the triangular matrix `A` is copied into the array `AP`. If `uplo == CUBLAS_FILL_MODE_UPPER` then then the upper triangular part of the triangular matrix `A` is copied into the array `AP`.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [strttp()](http://www.netlib.org/lapack/explore-html/d9/def/strttp_8f.html), [dtrttp()](http://www.netlib.org/lapack/explore-html/d0/daf/dtrttp_8f.html), [ctrttp()](http://www.netlib.org/lapack/explore-html/d7/d56/ctrttp_8f.html), [ztrttp()](http://www.netlib.org/lapack/explore-html/da/dc2/ztrttp_8f.html).
    pub fn cublasDtrttp(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: ::core::ffi::c_int,
        A: *const f64,
        lda: ::core::ffi::c_int,
        AP: *mut f64,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function performs the conversion from the triangular format to the triangular packed format
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the lower triangular part of the triangular matrix `A` is copied into the array `AP`. If `uplo == CUBLAS_FILL_MODE_UPPER` then then the upper triangular part of the triangular matrix `A` is copied into the array `AP`.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [strttp()](http://www.netlib.org/lapack/explore-html/d9/def/strttp_8f.html), [dtrttp()](http://www.netlib.org/lapack/explore-html/d0/daf/dtrttp_8f.html), [ctrttp()](http://www.netlib.org/lapack/explore-html/d7/d56/ctrttp_8f.html), [ztrttp()](http://www.netlib.org/lapack/explore-html/da/dc2/ztrttp_8f.html).
    pub fn cublasCtrttp(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: ::core::ffi::c_int,
        A: *const cuComplex,
        lda: ::core::ffi::c_int,
        AP: *mut cuComplex,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function performs the conversion from the triangular format to the triangular packed format
    ///
    /// If `uplo == CUBLAS_FILL_MODE_LOWER` then the lower triangular part of the triangular matrix `A` is copied into the array `AP`. If `uplo == CUBLAS_FILL_MODE_UPPER` then then the upper triangular part of the triangular matrix `A` is copied into the array `AP`.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [strttp()](http://www.netlib.org/lapack/explore-html/d9/def/strttp_8f.html), [dtrttp()](http://www.netlib.org/lapack/explore-html/d0/daf/dtrttp_8f.html), [ctrttp()](http://www.netlib.org/lapack/explore-html/d7/d56/ctrttp_8f.html), [ztrttp()](http://www.netlib.org/lapack/explore-html/da/dc2/ztrttp_8f.html).
    pub fn cublasZtrttp(
        handle: cublasHandle_t,
        uplo: cublasFillMode_t,
        n: ::core::ffi::c_int,
        A: *const cuDoubleComplex,
        lda: ::core::ffi::c_int,
        AP: *mut cuDoubleComplex,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// `Aarray` is an array of pointers to matrices stored in column-major format with dimensions `nxn` and leading dimension `lda`.
    ///
    /// This function performs the LU factorization of each `Aarray\[i\]` for i = 0, …, `batchSize-1` by the following equation:
    /// $$
    /// \text{P}\text{\\*}{Aarray}\lbrack i\rbrack = L\text{\\*}U
    /// $$
    ///
    /// where `P` is a permutation matrix which represents partial pivoting with row interchanges. `L` is a lower triangular matrix with unit diagonal and `U` is an upper triangular matrix.
    ///
    /// Formally `P` is written by a product of permutation matrices `Pj`, for `j = 1,2,...,n`, say `P = P1 * P2 * P3 * .... * Pn`. `Pj` is a permutation matrix which interchanges two rows of vector x when performing `Pj*x`. `Pj` can be constructed by `j` element of `PivotArray\[i\]` by the following Matlab code
    ///
    /// `L` and `U` are written back to original matrix `A`, and diagonal elements of `L` are discarded. The `L` and `U` can be constructed by the following Matlab code
    ///
    /// If matrix `A(=Aarray\[i\])` is singular, getrf still works and the value of `info(=infoArray\[i\])` reports first row index that LU factorization cannot proceed. If info is `k`, `U(k,k)` is zero. The equation `P*A == L*U` still holds, however `L` and `U` reconstruction needs a different Matlab code as follows:
    ///
    /// This function is intended to be used for matrices of small sizes where the launch overhead is a significant factor.
    ///
    /// cublas&lt;t>getrfBatched supports non-pivot LU factorization if `PivotArray` is NULL.
    ///
    /// cublas&lt;t>getrfBatched supports arbitrary dimension.
    ///
    /// cublas&lt;t>getrfBatched only supports compute capability 2.0 or above.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sgeqrf()](http://www.netlib.org/lapack/single/sgetrf.f), [dgeqrf()](http://www.netlib.org/lapack/double/dgetrf.f), [cgeqrf()](http://www.netlib.org/lapack/complex/cgetrf.f), [zgeqrf()](http://www.netlib.org/lapack/complex16/zgetrf.f).
    pub fn cublasSgetrfBatched(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        A: *const *mut f32,
        lda: ::core::ffi::c_int,
        P: *mut ::core::ffi::c_int,
        info: *mut ::core::ffi::c_int,
        batchSize: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// `Aarray` is an array of pointers to matrices stored in column-major format with dimensions `nxn` and leading dimension `lda`.
    ///
    /// This function performs the LU factorization of each `Aarray\[i\]` for i = 0, …, `batchSize-1` by the following equation:
    /// $$
    /// \text{P}\text{\\*}{Aarray}\lbrack i\rbrack = L\text{\\*}U
    /// $$
    ///
    /// where `P` is a permutation matrix which represents partial pivoting with row interchanges. `L` is a lower triangular matrix with unit diagonal and `U` is an upper triangular matrix.
    ///
    /// Formally `P` is written by a product of permutation matrices `Pj`, for `j = 1,2,...,n`, say `P = P1 * P2 * P3 * .... * Pn`. `Pj` is a permutation matrix which interchanges two rows of vector x when performing `Pj*x`. `Pj` can be constructed by `j` element of `PivotArray\[i\]` by the following Matlab code
    ///
    /// `L` and `U` are written back to original matrix `A`, and diagonal elements of `L` are discarded. The `L` and `U` can be constructed by the following Matlab code
    ///
    /// If matrix `A(=Aarray\[i\])` is singular, getrf still works and the value of `info(=infoArray\[i\])` reports first row index that LU factorization cannot proceed. If info is `k`, `U(k,k)` is zero. The equation `P*A == L*U` still holds, however `L` and `U` reconstruction needs a different Matlab code as follows:
    ///
    /// This function is intended to be used for matrices of small sizes where the launch overhead is a significant factor.
    ///
    /// cublas&lt;t>getrfBatched supports non-pivot LU factorization if `PivotArray` is NULL.
    ///
    /// cublas&lt;t>getrfBatched supports arbitrary dimension.
    ///
    /// cublas&lt;t>getrfBatched only supports compute capability 2.0 or above.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sgeqrf()](http://www.netlib.org/lapack/single/sgetrf.f), [dgeqrf()](http://www.netlib.org/lapack/double/dgetrf.f), [cgeqrf()](http://www.netlib.org/lapack/complex/cgetrf.f), [zgeqrf()](http://www.netlib.org/lapack/complex16/zgetrf.f).
    pub fn cublasDgetrfBatched(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        A: *const *mut f64,
        lda: ::core::ffi::c_int,
        P: *mut ::core::ffi::c_int,
        info: *mut ::core::ffi::c_int,
        batchSize: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// `Aarray` is an array of pointers to matrices stored in column-major format with dimensions `nxn` and leading dimension `lda`.
    ///
    /// This function performs the LU factorization of each `Aarray\[i\]` for i = 0, …, `batchSize-1` by the following equation:
    /// $$
    /// \text{P}\text{\\*}{Aarray}\lbrack i\rbrack = L\text{\\*}U
    /// $$
    ///
    /// where `P` is a permutation matrix which represents partial pivoting with row interchanges. `L` is a lower triangular matrix with unit diagonal and `U` is an upper triangular matrix.
    ///
    /// Formally `P` is written by a product of permutation matrices `Pj`, for `j = 1,2,...,n`, say `P = P1 * P2 * P3 * .... * Pn`. `Pj` is a permutation matrix which interchanges two rows of vector x when performing `Pj*x`. `Pj` can be constructed by `j` element of `PivotArray\[i\]` by the following Matlab code
    ///
    /// `L` and `U` are written back to original matrix `A`, and diagonal elements of `L` are discarded. The `L` and `U` can be constructed by the following Matlab code
    ///
    /// If matrix `A(=Aarray\[i\])` is singular, getrf still works and the value of `info(=infoArray\[i\])` reports first row index that LU factorization cannot proceed. If info is `k`, `U(k,k)` is zero. The equation `P*A == L*U` still holds, however `L` and `U` reconstruction needs a different Matlab code as follows:
    ///
    /// This function is intended to be used for matrices of small sizes where the launch overhead is a significant factor.
    ///
    /// cublas&lt;t>getrfBatched supports non-pivot LU factorization if `PivotArray` is NULL.
    ///
    /// cublas&lt;t>getrfBatched supports arbitrary dimension.
    ///
    /// cublas&lt;t>getrfBatched only supports compute capability 2.0 or above.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sgeqrf()](http://www.netlib.org/lapack/single/sgetrf.f), [dgeqrf()](http://www.netlib.org/lapack/double/dgetrf.f), [cgeqrf()](http://www.netlib.org/lapack/complex/cgetrf.f), [zgeqrf()](http://www.netlib.org/lapack/complex16/zgetrf.f).
    pub fn cublasCgetrfBatched(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        A: *const *mut cuComplex,
        lda: ::core::ffi::c_int,
        P: *mut ::core::ffi::c_int,
        info: *mut ::core::ffi::c_int,
        batchSize: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// `Aarray` is an array of pointers to matrices stored in column-major format with dimensions `nxn` and leading dimension `lda`.
    ///
    /// This function performs the LU factorization of each `Aarray\[i\]` for i = 0, …, `batchSize-1` by the following equation:
    /// $$
    /// \text{P}\text{\\*}{Aarray}\lbrack i\rbrack = L\text{\\*}U
    /// $$
    ///
    /// where `P` is a permutation matrix which represents partial pivoting with row interchanges. `L` is a lower triangular matrix with unit diagonal and `U` is an upper triangular matrix.
    ///
    /// Formally `P` is written by a product of permutation matrices `Pj`, for `j = 1,2,...,n`, say `P = P1 * P2 * P3 * .... * Pn`. `Pj` is a permutation matrix which interchanges two rows of vector x when performing `Pj*x`. `Pj` can be constructed by `j` element of `PivotArray\[i\]` by the following Matlab code
    ///
    /// `L` and `U` are written back to original matrix `A`, and diagonal elements of `L` are discarded. The `L` and `U` can be constructed by the following Matlab code
    ///
    /// If matrix `A(=Aarray\[i\])` is singular, getrf still works and the value of `info(=infoArray\[i\])` reports first row index that LU factorization cannot proceed. If info is `k`, `U(k,k)` is zero. The equation `P*A == L*U` still holds, however `L` and `U` reconstruction needs a different Matlab code as follows:
    ///
    /// This function is intended to be used for matrices of small sizes where the launch overhead is a significant factor.
    ///
    /// cublas&lt;t>getrfBatched supports non-pivot LU factorization if `PivotArray` is NULL.
    ///
    /// cublas&lt;t>getrfBatched supports arbitrary dimension.
    ///
    /// cublas&lt;t>getrfBatched only supports compute capability 2.0 or above.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sgeqrf()](http://www.netlib.org/lapack/single/sgetrf.f), [dgeqrf()](http://www.netlib.org/lapack/double/dgetrf.f), [cgeqrf()](http://www.netlib.org/lapack/complex/cgetrf.f), [zgeqrf()](http://www.netlib.org/lapack/complex16/zgetrf.f).
    pub fn cublasZgetrfBatched(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        A: *const *mut cuDoubleComplex,
        lda: ::core::ffi::c_int,
        P: *mut ::core::ffi::c_int,
        info: *mut ::core::ffi::c_int,
        batchSize: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// `Aarray` and `Carray` are arrays of pointers to matrices stored in column-major format with dimensions `n*n` and leading dimension `lda` and `ldc` respectively.
    ///
    /// This function performs the inversion of matrices `A\[i\]` for i = 0, …, `batchSize-1`.
    ///
    /// Prior to calling cublas&lt;t>getriBatched, the matrix `A\[i\]` must be factorized first using the routine cublas&lt;t>getrfBatched. After the call of cublas&lt;t>getrfBatched, the matrix pointing by `Aarray\[i\]` will contain the LU factors of the matrix `A\[i\]` and the vector pointing by `(PivotArray+i)` will contain the pivoting sequence.
    ///
    /// Following the LU factorization, cublas&lt;t>getriBatched uses forward and backward triangular solvers to complete inversion of matrices `A\[i\]` for i = 0, …, `batchSize-1`. The inversion is out-of-place, so memory space of Carray\[i\] cannot overlap memory space of Array\[i\].
    ///
    /// Typically all parameters in cublas&lt;t>getrfBatched would be passed into cublas&lt;t>getriBatched. For example,
    ///
    /// The user can check singularity from either cublas&lt;t>getrfBatched or cublas&lt;t>getriBatched.
    ///
    /// This function is intended to be used for matrices of small sizes where the launch overhead is a significant factor.
    ///
    /// If cublas&lt;t>getrfBatched is performed by non-pivoting, `PivotArray` of cublas&lt;t>getriBatched should be NULL.
    ///
    /// cublas&lt;t>getriBatched supports arbitrary dimension.
    ///
    /// cublas&lt;t>getriBatched only supports compute capability 2.0 or above.
    pub fn cublasSgetriBatched(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        A: *const *const f32,
        lda: ::core::ffi::c_int,
        P: *const ::core::ffi::c_int,
        C: *const *mut f32,
        ldc: ::core::ffi::c_int,
        info: *mut ::core::ffi::c_int,
        batchSize: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// `Aarray` and `Carray` are arrays of pointers to matrices stored in column-major format with dimensions `n*n` and leading dimension `lda` and `ldc` respectively.
    ///
    /// This function performs the inversion of matrices `A\[i\]` for i = 0, …, `batchSize-1`.
    ///
    /// Prior to calling cublas&lt;t>getriBatched, the matrix `A\[i\]` must be factorized first using the routine cublas&lt;t>getrfBatched. After the call of cublas&lt;t>getrfBatched, the matrix pointing by `Aarray\[i\]` will contain the LU factors of the matrix `A\[i\]` and the vector pointing by `(PivotArray+i)` will contain the pivoting sequence.
    ///
    /// Following the LU factorization, cublas&lt;t>getriBatched uses forward and backward triangular solvers to complete inversion of matrices `A\[i\]` for i = 0, …, `batchSize-1`. The inversion is out-of-place, so memory space of Carray\[i\] cannot overlap memory space of Array\[i\].
    ///
    /// Typically all parameters in cublas&lt;t>getrfBatched would be passed into cublas&lt;t>getriBatched. For example,
    ///
    /// The user can check singularity from either cublas&lt;t>getrfBatched or cublas&lt;t>getriBatched.
    ///
    /// This function is intended to be used for matrices of small sizes where the launch overhead is a significant factor.
    ///
    /// If cublas&lt;t>getrfBatched is performed by non-pivoting, `PivotArray` of cublas&lt;t>getriBatched should be NULL.
    ///
    /// cublas&lt;t>getriBatched supports arbitrary dimension.
    ///
    /// cublas&lt;t>getriBatched only supports compute capability 2.0 or above.
    pub fn cublasDgetriBatched(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        A: *const *const f64,
        lda: ::core::ffi::c_int,
        P: *const ::core::ffi::c_int,
        C: *const *mut f64,
        ldc: ::core::ffi::c_int,
        info: *mut ::core::ffi::c_int,
        batchSize: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// `Aarray` and `Carray` are arrays of pointers to matrices stored in column-major format with dimensions `n*n` and leading dimension `lda` and `ldc` respectively.
    ///
    /// This function performs the inversion of matrices `A\[i\]` for i = 0, …, `batchSize-1`.
    ///
    /// Prior to calling cublas&lt;t>getriBatched, the matrix `A\[i\]` must be factorized first using the routine cublas&lt;t>getrfBatched. After the call of cublas&lt;t>getrfBatched, the matrix pointing by `Aarray\[i\]` will contain the LU factors of the matrix `A\[i\]` and the vector pointing by `(PivotArray+i)` will contain the pivoting sequence.
    ///
    /// Following the LU factorization, cublas&lt;t>getriBatched uses forward and backward triangular solvers to complete inversion of matrices `A\[i\]` for i = 0, …, `batchSize-1`. The inversion is out-of-place, so memory space of Carray\[i\] cannot overlap memory space of Array\[i\].
    ///
    /// Typically all parameters in cublas&lt;t>getrfBatched would be passed into cublas&lt;t>getriBatched. For example,
    ///
    /// The user can check singularity from either cublas&lt;t>getrfBatched or cublas&lt;t>getriBatched.
    ///
    /// This function is intended to be used for matrices of small sizes where the launch overhead is a significant factor.
    ///
    /// If cublas&lt;t>getrfBatched is performed by non-pivoting, `PivotArray` of cublas&lt;t>getriBatched should be NULL.
    ///
    /// cublas&lt;t>getriBatched supports arbitrary dimension.
    ///
    /// cublas&lt;t>getriBatched only supports compute capability 2.0 or above.
    pub fn cublasCgetriBatched(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        A: *const *const cuComplex,
        lda: ::core::ffi::c_int,
        P: *const ::core::ffi::c_int,
        C: *const *mut cuComplex,
        ldc: ::core::ffi::c_int,
        info: *mut ::core::ffi::c_int,
        batchSize: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// `Aarray` and `Carray` are arrays of pointers to matrices stored in column-major format with dimensions `n*n` and leading dimension `lda` and `ldc` respectively.
    ///
    /// This function performs the inversion of matrices `A\[i\]` for i = 0, …, `batchSize-1`.
    ///
    /// Prior to calling cublas&lt;t>getriBatched, the matrix `A\[i\]` must be factorized first using the routine cublas&lt;t>getrfBatched. After the call of cublas&lt;t>getrfBatched, the matrix pointing by `Aarray\[i\]` will contain the LU factors of the matrix `A\[i\]` and the vector pointing by `(PivotArray+i)` will contain the pivoting sequence.
    ///
    /// Following the LU factorization, cublas&lt;t>getriBatched uses forward and backward triangular solvers to complete inversion of matrices `A\[i\]` for i = 0, …, `batchSize-1`. The inversion is out-of-place, so memory space of Carray\[i\] cannot overlap memory space of Array\[i\].
    ///
    /// Typically all parameters in cublas&lt;t>getrfBatched would be passed into cublas&lt;t>getriBatched. For example,
    ///
    /// The user can check singularity from either cublas&lt;t>getrfBatched or cublas&lt;t>getriBatched.
    ///
    /// This function is intended to be used for matrices of small sizes where the launch overhead is a significant factor.
    ///
    /// If cublas&lt;t>getrfBatched is performed by non-pivoting, `PivotArray` of cublas&lt;t>getriBatched should be NULL.
    ///
    /// cublas&lt;t>getriBatched supports arbitrary dimension.
    ///
    /// cublas&lt;t>getriBatched only supports compute capability 2.0 or above.
    pub fn cublasZgetriBatched(
        handle: cublasHandle_t,
        n: ::core::ffi::c_int,
        A: *const *const cuDoubleComplex,
        lda: ::core::ffi::c_int,
        P: *const ::core::ffi::c_int,
        C: *const *mut cuDoubleComplex,
        ldc: ::core::ffi::c_int,
        info: *mut ::core::ffi::c_int,
        batchSize: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function solves an array of systems of linear equations of the form:
    /// $$
    /// \text{op}(A\lbrack i \rbrack) X\lbrack i\rbrack = B\lbrack i\rbrack
    /// $$
    ///
    /// where $A\lbrack i\rbrack$ is a matrix which has been LU factorized with pivoting, $X\lbrack i\rbrack$ and $B\lbrack i\rbrack$ are $n \times {nrhs}$ matrices. Also, for matrix $A$:
    /// $$
    /// \text{op}(A\lbrack i\rbrack) = \left\\{ \begin{matrix}
    /// {A\lbrack i\rbrack} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A^{T}\lbrack i\rbrack} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// {A^{H}\lbrack i\rbrack} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// This function is intended to be used for matrices of small sizes where the launch overhead is a significant factor.
    ///
    /// cublas&lt;t>getrsBatched() supports non-pivot LU factorization if `devIpiv` is NULL.
    ///
    /// cublas&lt;t>getrsBatched() supports arbitrary dimension.
    ///
    /// cublas&lt;t>getrsBatched() only supports compute capability 2.0 or above.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sgeqrs()](http://www.netlib.org/lapack/single/sgetrs.f), [dgeqrs()](http://www.netlib.org/lapack/double/dgetrs.f), [cgeqrs()](http://www.netlib.org/lapack/complex/cgetrs.f), [zgeqrs()](http://www.netlib.org/lapack/complex16/zgetrs.f).
    pub fn cublasSgetrsBatched(
        handle: cublasHandle_t,
        trans: cublasOperation_t,
        n: ::core::ffi::c_int,
        nrhs: ::core::ffi::c_int,
        Aarray: *const *const f32,
        lda: ::core::ffi::c_int,
        devIpiv: *const ::core::ffi::c_int,
        Barray: *const *mut f32,
        ldb: ::core::ffi::c_int,
        info: *mut ::core::ffi::c_int,
        batchSize: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function solves an array of systems of linear equations of the form:
    /// $$
    /// \text{op}(A\lbrack i \rbrack) X\lbrack i\rbrack = B\lbrack i\rbrack
    /// $$
    ///
    /// where $A\lbrack i\rbrack$ is a matrix which has been LU factorized with pivoting, $X\lbrack i\rbrack$ and $B\lbrack i\rbrack$ are $n \times {nrhs}$ matrices. Also, for matrix $A$:
    /// $$
    /// \text{op}(A\lbrack i\rbrack) = \left\\{ \begin{matrix}
    /// {A\lbrack i\rbrack} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A^{T}\lbrack i\rbrack} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// {A^{H}\lbrack i\rbrack} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// This function is intended to be used for matrices of small sizes where the launch overhead is a significant factor.
    ///
    /// cublas&lt;t>getrsBatched() supports non-pivot LU factorization if `devIpiv` is NULL.
    ///
    /// cublas&lt;t>getrsBatched() supports arbitrary dimension.
    ///
    /// cublas&lt;t>getrsBatched() only supports compute capability 2.0 or above.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sgeqrs()](http://www.netlib.org/lapack/single/sgetrs.f), [dgeqrs()](http://www.netlib.org/lapack/double/dgetrs.f), [cgeqrs()](http://www.netlib.org/lapack/complex/cgetrs.f), [zgeqrs()](http://www.netlib.org/lapack/complex16/zgetrs.f).
    pub fn cublasDgetrsBatched(
        handle: cublasHandle_t,
        trans: cublasOperation_t,
        n: ::core::ffi::c_int,
        nrhs: ::core::ffi::c_int,
        Aarray: *const *const f64,
        lda: ::core::ffi::c_int,
        devIpiv: *const ::core::ffi::c_int,
        Barray: *const *mut f64,
        ldb: ::core::ffi::c_int,
        info: *mut ::core::ffi::c_int,
        batchSize: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function solves an array of systems of linear equations of the form:
    /// $$
    /// \text{op}(A\lbrack i \rbrack) X\lbrack i\rbrack = B\lbrack i\rbrack
    /// $$
    ///
    /// where $A\lbrack i\rbrack$ is a matrix which has been LU factorized with pivoting, $X\lbrack i\rbrack$ and $B\lbrack i\rbrack$ are $n \times {nrhs}$ matrices. Also, for matrix $A$:
    /// $$
    /// \text{op}(A\lbrack i\rbrack) = \left\\{ \begin{matrix}
    /// {A\lbrack i\rbrack} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A^{T}\lbrack i\rbrack} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// {A^{H}\lbrack i\rbrack} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// This function is intended to be used for matrices of small sizes where the launch overhead is a significant factor.
    ///
    /// cublas&lt;t>getrsBatched() supports non-pivot LU factorization if `devIpiv` is NULL.
    ///
    /// cublas&lt;t>getrsBatched() supports arbitrary dimension.
    ///
    /// cublas&lt;t>getrsBatched() only supports compute capability 2.0 or above.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sgeqrs()](http://www.netlib.org/lapack/single/sgetrs.f), [dgeqrs()](http://www.netlib.org/lapack/double/dgetrs.f), [cgeqrs()](http://www.netlib.org/lapack/complex/cgetrs.f), [zgeqrs()](http://www.netlib.org/lapack/complex16/zgetrs.f).
    pub fn cublasCgetrsBatched(
        handle: cublasHandle_t,
        trans: cublasOperation_t,
        n: ::core::ffi::c_int,
        nrhs: ::core::ffi::c_int,
        Aarray: *const *const cuComplex,
        lda: ::core::ffi::c_int,
        devIpiv: *const ::core::ffi::c_int,
        Barray: *const *mut cuComplex,
        ldb: ::core::ffi::c_int,
        info: *mut ::core::ffi::c_int,
        batchSize: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    /// This function solves an array of systems of linear equations of the form:
    /// $$
    /// \text{op}(A\lbrack i \rbrack) X\lbrack i\rbrack = B\lbrack i\rbrack
    /// $$
    ///
    /// where $A\lbrack i\rbrack$ is a matrix which has been LU factorized with pivoting, $X\lbrack i\rbrack$ and $B\lbrack i\rbrack$ are $n \times {nrhs}$ matrices. Also, for matrix $A$:
    /// $$
    /// \text{op}(A\lbrack i\rbrack) = \left\\{ \begin{matrix}
    /// {A\lbrack i\rbrack} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_N}$}} \\\\
    /// {A^{T}\lbrack i\rbrack} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_T}$}} \\\\
    /// {A^{H}\lbrack i\rbrack} & {\text{if }\textsf{trans == $\mathrm{CUBLAS_OP_C}$}} \\\\
    /// \end{matrix} \right.
    /// $$
    ///
    /// This function is intended to be used for matrices of small sizes where the launch overhead is a significant factor.
    ///
    /// cublas&lt;t>getrsBatched() supports non-pivot LU factorization if `devIpiv` is NULL.
    ///
    /// cublas&lt;t>getrsBatched() supports arbitrary dimension.
    ///
    /// cublas&lt;t>getrsBatched() only supports compute capability 2.0 or above.
    ///
    /// For references please refer to NETLIB documentation:
    ///
    /// [sgeqrs()](http://www.netlib.org/lapack/single/sgetrs.f), [dgeqrs()](http://www.netlib.org/lapack/double/dgetrs.f), [cgeqrs()](http://www.netlib.org/lapack/complex/cgetrs.f), [zgeqrs()](http://www.netlib.org/lapack/complex16/zgetrs.f).
    pub fn cublasZgetrsBatched(
        handle: cublasHandle_t,
        trans: cublasOperation_t,
        n: ::core::ffi::c_int,
        nrhs: ::core::ffi::c_int,
        Aarray: *const *const cuDoubleComplex,
        lda: ::core::ffi::c_int,
        devIpiv: *const ::core::ffi::c_int,
        Barray: *const *mut cuDoubleComplex,
        ldb: ::core::ffi::c_int,
        info: *mut ::core::ffi::c_int,
        batchSize: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
unsafe extern "C" {
    pub fn cublasUint8gemmBias(
        handle: cublasHandle_t,
        transa: cublasOperation_t,
        transb: cublasOperation_t,
        transc: cublasOperation_t,
        m: ::core::ffi::c_int,
        n: ::core::ffi::c_int,
        k: ::core::ffi::c_int,
        A: *const ::core::ffi::c_uchar,
        A_bias: ::core::ffi::c_int,
        lda: ::core::ffi::c_int,
        B: *const ::core::ffi::c_uchar,
        B_bias: ::core::ffi::c_int,
        ldb: ::core::ffi::c_int,
        C: *mut ::core::ffi::c_uchar,
        C_bias: ::core::ffi::c_int,
        ldc: ::core::ffi::c_int,
        C_mult: ::core::ffi::c_int,
        C_shift: ::core::ffi::c_int,
    ) -> cublasStatus_t;
}
