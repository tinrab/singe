/* automatically generated by rust-bindgen 0.72.1 */

pub const CUDNN_MAJOR: u32 = 9;
pub const CUDNN_MINOR: u32 = 22;
pub const CUDNN_PATCHLEVEL: u32 = 0;
pub const CUDNN_VERSION: u32 = 92200;
pub const CUDNN_MAX_SM_MAJOR_NUMBER: u32 = 12;
pub const CUDNN_MAX_SM_MINOR_NUMBER: u32 = 1;
pub const CUDNN_MAX_DEVICE_VERSION: u32 = 1210;
pub const CUDNN_GRAPH_MAJOR: u32 = 9;
pub const CUDNN_GRAPH_MINOR: u32 = 22;
pub const CUDNN_GRAPH_PATCH: u32 = 0;
pub const CUDNN_DIM_MAX: u32 = 8;
pub const CUDNN_OPS_MAJOR: u32 = 9;
pub const CUDNN_OPS_MINOR: u32 = 22;
pub const CUDNN_OPS_PATCH: u32 = 0;
pub const CUDNN_LRN_MIN_N: u32 = 1;
pub const CUDNN_LRN_MAX_N: u32 = 16;
pub const CUDNN_LRN_MIN_K: f64 = 0.00001;
pub const CUDNN_LRN_MIN_BETA: f64 = 0.01;
pub const CUDNN_BN_MIN_EPSILON: f64 = 0.0;
pub const CUDNN_ADV_MAJOR: u32 = 9;
pub const CUDNN_ADV_MINOR: u32 = 22;
pub const CUDNN_ADV_PATCH: u32 = 0;
pub const CUDNN_RNN_PADDED_IO_DISABLED: u32 = 0;
pub const CUDNN_RNN_PADDED_IO_ENABLED: u32 = 1;
pub const CUDNN_SEQDATA_DIM_COUNT: u32 = 4;
pub const CUDNN_ATTN_QUERYMAP_ALL_TO_ONE: u32 = 0;
pub const CUDNN_ATTN_QUERYMAP_ONE_TO_ONE: u32 = 1;
pub const CUDNN_ATTN_DISABLE_PROJ_BIASES: u32 = 0;
pub const CUDNN_ATTN_ENABLE_PROJ_BIASES: u32 = 2;
pub const CUDNN_ATTN_WKIND_COUNT: u32 = 8;
pub const CUDNN_CNN_MAJOR: u32 = 9;
pub const CUDNN_CNN_MINOR: u32 = 22;
pub const CUDNN_CNN_PATCH: u32 = 0;
pub type size_t = ::core::ffi::c_ulong;
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct CUstream_st {
    _unused: [u8; 0],
}
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct CUgraph_st {
    _unused: [u8; 0],
}
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct cudnnContext {
    _unused: [u8; 0],
}
/// [`cudnnHandle_t`] is a pointer to an opaque structure holding the cuDNN library context. The cuDNN library context must be created using [`cudnnCreate`] and the returned handle must be passed to all subsequent library function calls. The context should be destroyed at the end using [`cudnnDestroy`]. The context is associated with only one GPU device, the current device at the time of the call to [`cudnnCreate`]. However, multiple contexts can be created on the same GPU device.
pub type cudnnHandle_t = *mut cudnnContext;
unsafe extern "C" {
    /// This function returns the version number of the cuDNN library. It returns the [`CUDNN_VERSION`] defined present in the `cudnn.h` header file. Starting with release R2, the routine can be used to identify dynamically the current cuDNN library used by the application. The defined [`CUDNN_VERSION`] can be used to have the same application linked against different cuDNN versions using conditional compilation statements.
    pub fn cudnnGetVersion() -> size_t;
}
unsafe extern "C" {
    /// This function returns the maximum SM version that the cuDNN library is aware of and supports natively. Any SM version higher than this would be supported in forward compatibility mode. For more information about forward compatibility, refer to [Hardware Forward Compatibility](https://docs.nvidia.com/deeplearning/cudnn//latest/developer/forward-compatibility.html#forward-compat-native-mode "(in NVIDIA cuDNN)") in the *Frontend Developer Guide*.
    ///
    /// A `size_t` type value indicating the latest known SM number for the current version of the library. For example, if NVIDIA Hopper (GH100) is the latest known SM that the library is aware of, the value returned would be `900`.
    pub fn cudnnGetMaxDeviceVersion() -> size_t;
}
unsafe extern "C" {
    /// The same version of a given cuDNN library can be compiled against different CUDA toolkit versions. This routine returns the CUDA toolkit version that the currently used cuDNN library has been compiled against.
    pub fn cudnnGetCudartVersion() -> size_t;
}
impl cudnnStatus_t {
    pub const CUDNN_STATUS_ALLOC_FAILED: cudnnStatus_t = cudnnStatus_t::CUDNN_STATUS_INTERNAL_ERROR_HOST_ALLOCATION_FAILED;
}
impl cudnnStatus_t {
    pub const CUDNN_STATUS_ARCH_MISMATCH: cudnnStatus_t = cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED_ARCH_MISMATCH;
}
impl cudnnStatus_t {
    pub const CUDNN_STATUS_MAPPING_ERROR: cudnnStatus_t = cudnnStatus_t::CUDNN_STATUS_INTERNAL_ERROR_TEXTURE_CREATION_FAILED;
}
impl cudnnStatus_t {
    pub const CUDNN_STATUS_RUNTIME_PREREQUISITE_MISSING: cudnnStatus_t = cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED_RUNTIME_PREREQUISITE_MISSING;
}
impl cudnnStatus_t {
    pub const CUDNN_STATUS_VERSION_MISMATCH: cudnnStatus_t = cudnnStatus_t::CUDNN_STATUS_SUBLIBRARY_VERSION_MISMATCH;
}
/// [`cudnnStatus_t`] is an enumerated type used for function status returns. All cuDNN library functions return their status, which can be one of the following values:
///
/// Additionally, the following macros can be used on [`cudnnStatus_t`] error codes.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnStatus_t {
    /// The operation was completed successfully.
    CUDNN_STATUS_SUCCESS = 0,
    /// The cuDNN library was not initialized properly. This error is usually returned when a call to [`cudnnCreate`] fails or when [`cudnnCreate`] has not been called prior to calling another cuDNN routine. In the former case, it is usually due to an error in the CUDA Runtime API called by [`cudnnCreate`] or by an error in the hardware setup.
    CUDNN_STATUS_NOT_INITIALIZED = 1001,
    /// Some cuDNN sub libraries have different versions, indicative of an installation issue.
    CUDNN_STATUS_SUBLIBRARY_VERSION_MISMATCH = 1002,
    /// The schema used for serialization is not what the current cuDNN library expects, thus the serialized artifact is no longer valid and needs to be re-serialized.
    CUDNN_STATUS_SERIALIZATION_VERSION_MISMATCH = 1003,
    /// This error code may be reported in the warning logging level as a reminder that some functionality is under deprecation and will be removed in the next major version update.
    CUDNN_STATUS_DEPRECATED = 1004,
    /// The functionality requested requires some license and an error was detected when trying to check the current licensing. This error can happen if the license is not present or is expired or if the environment variable `NVIDIA_LICENSE_FILE` is not set properly.
    CUDNN_STATUS_LICENSE_ERROR = 1005,
    /// Some tasks in the user stream are not completed.
    CUDNN_STATUS_RUNTIME_IN_PROGRESS = 1006,
    /// Numerical overflow occurred during the GPU kernel execution.
    CUDNN_STATUS_RUNTIME_FP_OVERFLOW = 1007,
    CUDNN_STATUS_SUBLIBRARY_LOADING_FAILED = 1008,
    /// This is an error category code. An incorrect value or parameter was passed to the function.
    CUDNN_STATUS_BAD_PARAM = 2000,
    /// The cuDNN API has unexpectedly received a null pointer from the user.
    CUDNN_STATUS_BAD_PARAM_NULL_POINTER = 2002,
    /// The cuDNN API has received a misaligned pointer from the user.
    CUDNN_STATUS_BAD_PARAM_MISALIGNED_POINTER = 2003,
    /// The backend descriptor has not been finalized.
    CUDNN_STATUS_BAD_PARAM_NOT_FINALIZED = 2004,
    /// The cuDNN API has received an out-of-bound value.
    CUDNN_STATUS_BAD_PARAM_OUT_OF_BOUND = 2005,
    /// The cuDNN API has received a memory buffer with insufficient space.
    CUDNN_STATUS_BAD_PARAM_SIZE_INSUFFICIENT = 2006,
    /// The cuDNN API has received an unexpected stream.
    CUDNN_STATUS_BAD_PARAM_STREAM_MISMATCH = 2007,
    /// The cuDNN API has received inconsistent tensor shapes.
    CUDNN_STATUS_BAD_PARAM_SHAPE_MISMATCH = 2008,
    /// The cuDNN API has received duplicated entries.
    CUDNN_STATUS_BAD_PARAM_DUPLICATED_ENTRIES = 2009,
    /// The cuDNN API has received an invalid or unsupported attribute type.
    CUDNN_STATUS_BAD_PARAM_ATTRIBUTE_TYPE = 2010,
    /// The cuDNN API has received an unexpected CUDA graph.
    CUDNN_STATUS_BAD_PARAM_CUDA_GRAPH_MISMATCH = 2011,
    CUDNN_STATUS_BAD_PARAM_DESCRIPTOR_TYPE = 2012,
    /// This is an error category code. The functionality requested is not currently supported by cuDNN.
    CUDNN_STATUS_NOT_SUPPORTED = 3000,
    /// cuDNN does not currently support such an operation graph pattern.
    CUDNN_STATUS_NOT_SUPPORTED_GRAPH_PATTERN = 3001,
    /// cuDNN does not currently support the tensor shapes used in some specific operation or graph pattern.
    CUDNN_STATUS_NOT_SUPPORTED_SHAPE = 3002,
    /// cuDNN does not currently support the tensor data type.
    CUDNN_STATUS_NOT_SUPPORTED_DATA_TYPE = 3003,
    /// cuDNN does not currently support the tensor layout.
    CUDNN_STATUS_NOT_SUPPORTED_LAYOUT = 3004,
    /// The requested functionality is not compatible with the current CUDA driver.
    CUDNN_STATUS_NOT_SUPPORTED_INCOMPATIBLE_CUDA_DRIVER = 3005,
    /// The requested functionality is not compatible with the current CUDA runtime.
    CUDNN_STATUS_NOT_SUPPORTED_INCOMPATIBLE_CUDART = 3006,
    /// The function requires a feature absent from the current GPU device.
    CUDNN_STATUS_NOT_SUPPORTED_ARCH_MISMATCH = 3007,
    /// A runtime library required by cuDNN cannot be found in the predefined search paths. These libraries are `libcuda.so` (`nvcuda.dll`) and `libnvrtc.so` (`nvrtc64_&lt;Major Release Version>&lt;Minor Release Version>_0.dll` and `nvrtc-builtins64_&lt;Major Release Version>&lt;Minor Release Version>.dll`).
    CUDNN_STATUS_NOT_SUPPORTED_RUNTIME_PREREQUISITE_MISSING = 3008,
    /// The requested functionality is not available due to missing a sublibrary.
    CUDNN_STATUS_NOT_SUPPORTED_SUBLIBRARY_UNAVAILABLE = 3009,
    /// The requested functionality is not available due to the insufficient shared memory size on the GPU.
    CUDNN_STATUS_NOT_SUPPORTED_SHARED_MEMORY_INSUFFICIENT = 3010,
    /// The requested functionality is not available due to padding requirements.
    CUDNN_STATUS_NOT_SUPPORTED_PADDING = 3011,
    /// The requested functionality is not available because they lead to invalid kernel launch parameters.
    CUDNN_STATUS_NOT_SUPPORTED_BAD_LAUNCH_PARAM = 3012,
    /// The requested functionality is not available because this particular engine does not support the native CUDA graph API. (The engines that do support that API have the behavior note [`cudnnBackendBehaviorNote_t::CUDNN_BEHAVIOR_NOTE_SUPPORTS_CUDA_GRAPH_NATIVE_API`].)
    CUDNN_STATUS_NOT_SUPPORTED_CUDA_GRAPH_NATIVE_API = 3013,
    CUDNN_STATUS_NOT_SUPPORTED_INVALID_DYNAMIC_SHAPE = 3014,
    /// This is an error category code. An internal cuDNN operation failed.
    CUDNN_STATUS_INTERNAL_ERROR = 4000,
    /// A runtime kernel has failed to be compiled.
    CUDNN_STATUS_INTERNAL_ERROR_COMPILATION_FAILED = 4001,
    /// An unexpected internal inconsistency has been detected.
    CUDNN_STATUS_INTERNAL_ERROR_UNEXPECTED_VALUE = 4002,
    /// An internal host memory allocation failed inside the cuDNN library.
    CUDNN_STATUS_INTERNAL_ERROR_HOST_ALLOCATION_FAILED = 4003,
    /// Resource allocation failed inside the cuDNN library.
    CUDNN_STATUS_INTERNAL_ERROR_DEVICE_ALLOCATION_FAILED = 4004,
    /// Invalid kernel launch parameters are unexpectedly detected.
    CUDNN_STATUS_INTERNAL_ERROR_BAD_LAUNCH_PARAM = 4005,
    /// Access to GPU memory space failed, which is usually caused by a failure to bind a texture. To correct, prior to the function call, unbind any previously bound textures. Otherwise, this may indicate an internal error/bug in the library.
    CUDNN_STATUS_INTERNAL_ERROR_TEXTURE_CREATION_FAILED = 4006,
    /// This is an error category code. The GPU program failed to execute. This is usually caused by a failure to launch a kernel on the GPU, which can be caused by another library that cuDNN depends on.
    CUDNN_STATUS_EXECUTION_FAILED = 5000,
    /// The GPU program failed to execute due to an error reported by the CUDA driver.
    CUDNN_STATUS_EXECUTION_FAILED_CUDA_DRIVER = 5001,
    /// The GPU program failed to execute due to an error reported by cuBLAS.
    CUDNN_STATUS_EXECUTION_FAILED_CUBLAS = 5002,
    /// The GPU program failed to execute due to an error reported by the CUDA runtime.
    CUDNN_STATUS_EXECUTION_FAILED_CUDART = 5003,
    /// The GPU program failed to execute due to an error reported by cuRAND.
    CUDNN_STATUS_EXECUTION_FAILED_CURAND = 5004,
    CUDNN_STATUS_INVALID_VALUE = 2001,
}
unsafe extern "C" {
    /// This function converts the cuDNN status code to a `NULL` terminated (ASCIIZ) static string. For example, when the input argument is [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`], the returned string is [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]. When an invalid status value is passed to the function, the returned string is `CUDNN_UNKNOWN_STATUS`.
    ///
    /// Pointer to a static, NULL terminated string with the status name.
    ///
    /// # Parameters
    ///
    /// - `status`: Input. cuDNN enumerant status code.
    pub fn cudnnGetErrorString(status: cudnnStatus_t) -> *const ::core::ffi::c_char;
}
unsafe extern "C" {
    /// This function retrieves the last encountered cuDNN error message in the current thread to a `NULL` terminated (ASCIIZ) string. Inside the cuDNN library, the messages are stored in thread local buffers. The error is cleared after the user calls this API to retrieve it.
    ///
    /// # Parameters
    ///
    /// - `message`: Output. Pointer to a character buffer that can store the error message. As we do not manage the thread-safety of the pre-allocated output buffer “message” to avoid unnecessary overhead, we ask that the user ensure it is thread-safe themselves on their own need-basis.
    /// - `max_size`: Input. Maximum size that can be stored in the location pointed to by message. The output is strictly limited by the size limit `max_size`, also counting the terminator character `\0`, which will be automatically appended to the message if there is space.
    pub fn cudnnGetLastErrorString(message: *mut ::core::ffi::c_char, max_size: size_t);
}
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct cudnnRuntimeTag_t {
    _unused: [u8; 0],
}
/// [`cudnnErrQueryMode_t`] is an enumerated type passed to [`cudnnQueryRuntimeError`] to select the remote kernel error query mode.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnErrQueryMode_t {
    /// Read the error storage location regardless of the kernel completion status.
    CUDNN_ERRQUERY_RAWCODE = 0,
    /// Report if all tasks in the user stream of the cuDNN handle were completed. If that is the case, report the remote kernel error code.
    CUDNN_ERRQUERY_NONBLOCKING = 1,
    /// Wait for all tasks to complete in the user stream before reporting the remote kernel error code.
    CUDNN_ERRQUERY_BLOCKING = 2,
}
unsafe extern "C" {
    /// cuDNN library functions perform extensive input argument checking before launching GPU kernels. The last step is to verify that the GPU kernel actually started. When a kernel fails to start, [`cudnnStatus_t::CUDNN_STATUS_EXECUTION_FAILED`] is returned by the corresponding API call. Typically, after a GPU kernel starts, no runtime checks are performed by the kernel itself - numerical results are simply written to output buffers.
    ///
    /// When the [`cudnnBatchNormMode_t::CUDNN_BATCHNORM_SPATIAL_PERSISTENT`] mode is selected in [`cudnnBatchNormalizationForwardTraining`] or [`cudnnBatchNormalizationBackward`], the algorithm may encounter numerical overflows where [`cudnnBatchNormMode_t::CUDNN_BATCHNORM_SPATIAL`] performs just fine albeit at a slower speed. The user can invoke [`cudnnQueryRuntimeError`] to make sure numerical overflows did not occur during the kernel execution. Those issues are reported by the kernel that performs computations.
    ///
    /// [`cudnnQueryRuntimeError`] can be used in polling and blocking software control flows. There are two polling modes ([`cudnnErrQueryMode_t::CUDNN_ERRQUERY_RAWCODE`] and [`cudnnErrQueryMode_t::CUDNN_ERRQUERY_NONBLOCKING`]) and one blocking mode [`cudnnErrQueryMode_t::CUDNN_ERRQUERY_BLOCKING`].
    ///
    /// [`cudnnErrQueryMode_t::CUDNN_ERRQUERY_RAWCODE`] reads the error storage location regardless of the kernel completion status. The kernel might not even start and the error storage (allocated per cuDNN handle) might be used by an earlier call.
    ///
    /// [`cudnnErrQueryMode_t::CUDNN_ERRQUERY_NONBLOCKING`] checks if all tasks in the user stream are completed. The [`cudnnQueryRuntimeError`] function will return immediately and report [`cudnnStatus_t::CUDNN_STATUS_RUNTIME_IN_PROGRESS`] in `rstatus` if some tasks in the user stream are pending. Otherwise, the function will copy the remote kernel error code to `rstatus`.
    ///
    /// In the blocking mode ([`cudnnErrQueryMode_t::CUDNN_ERRQUERY_BLOCKING`]), the function waits for all tasks to drain in the user stream before reporting the remote kernel error code. The blocking flavor can be further adjusted by calling `cudaSetDeviceFlags` with the `cudaDeviceScheduleSpin`, `cudaDeviceScheduleYield`, or `cudaDeviceScheduleBlockingSync` flag.
    ///
    /// [`cudnnErrQueryMode_t::CUDNN_ERRQUERY_NONBLOCKING`] and [`cudnnErrQueryMode_t::CUDNN_ERRQUERY_BLOCKING`] modes should not be used when the user stream is changed in the cuDNN handle, meaning, [`cudnnSetStream`] is invoked between functions that report runtime kernel errors and the [`cudnnQueryRuntimeError`] function.
    ///
    /// The remote error status reported in `rstatus` can be set to: [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`], [`cudnnStatus_t::CUDNN_STATUS_RUNTIME_IN_PROGRESS`], or [`cudnnStatus_t::CUDNN_STATUS_RUNTIME_FP_OVERFLOW`]. The remote kernel error is automatically cleared by [`cudnnQueryRuntimeError`].
    ///
    /// The [`cudnnQueryRuntimeError`] function should be used in conjunction with [`cudnnBatchNormalizationForwardTraining`] and [`cudnnBatchNormalizationBackward`] when the [`cudnnBatchNormMode_t`] argument is [`cudnnBatchNormMode_t::CUDNN_BATCHNORM_SPATIAL_PERSISTENT`].
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN context.
    /// - `rstatus`: Output. Pointer to the user’s error code storage.
    /// - `mode`: Input. Remote error query mode.
    /// - `tag`: *Input/Output*. Currently, this argument should be `NULL`.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: Invalid input argument.
    /// - [`cudnnStatus_t::CUDNN_STATUS_INTERNAL_ERROR`]: A stream blocking synchronization or a non-blocking stream query failed.
    /// - `CUDNN_STATUS_MAPPING_ERROR`: The device cannot access zero-copy memory to report kernel errors.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: No errors detected (`rstatus` holds a valid value).
    pub fn cudnnQueryRuntimeError(
        handle: cudnnHandle_t,
        rstatus: *mut cudnnStatus_t,
        mode: cudnnErrQueryMode_t,
        tag: *mut cudnnRuntimeTag_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function writes a specific part of the cuDNN library version number into the provided host storage.
    ///
    /// # Parameters
    ///
    /// - `value`: Output. Host pointer where the version information should be written.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_INVALID_VALUE`]: Invalid value of the `type` argument.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: Version information was stored successfully at the provided address.
    pub fn cudnnGetProperty(
        type_: libraryPropertyType,
        value: *mut ::core::ffi::c_int,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function initializes the cuDNN library and creates a handle to an opaque structure holding the cuDNN library context. It allocates hardware resources on the host and device and must be called prior to making any other cuDNN library calls.
    ///
    /// The cuDNN library handle is tied to the current CUDA device (context). To use the library on multiple devices, one cuDNN handle needs to be created for each device.
    ///
    /// For a given device, multiple cuDNN handles with different configurations (for example, different current CUDA streams) may be created. Because [`cudnnCreate`] allocates some internal resources, the release of those resources by calling [`cudnnDestroy`] will implicitly call [cudaDeviceSynchronize](https://docs.nvidia.com/cuda/cuda-runtime-api/group__CUDART__DEVICE.html#group__CUDART__DEVICE_1g10e20b05a95f638a4071a655503df25d); therefore, the recommended best practice is to call [`cudnnCreate`] or [`cudnnDestroy`] outside of performance-critical code paths.
    ///
    /// For multithreaded applications that use the same device from different threads, the recommended programming model is to create one (or a few, as is convenient) cuDNN handles per thread and use that cuDNN handle for the entire life of the thread.
    ///
    /// # Parameters
    ///
    /// - `handle`: Output. Pointer to pointer where to store the address to the allocated cuDNN handle. For more information, refer to [`cudnnHandle_t`].
    ///
    /// # Return value
    ///
    /// - `CUDNN_STATUS_ALLOC_FAILED`: Host memory allocation failed.
    /// - `CUDNN_STATUS_ARCH_MISMATCH`: NVIDIA GPU architecture is too old.
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: Invalid (`NULL`) input pointer supplied.
    /// - [`cudnnStatus_t::CUDNN_STATUS_INTERNAL_ERROR`]: CUDA resource allocation failed.
    /// - [`cudnnStatus_t::CUDNN_STATUS_LICENSE_ERROR`]: cuDNN license validation failed (only when the feature is enabled).
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_INITIALIZED`]: No compatible GPU found, CUDA driver not installed or disabled, CUDA runtime API initialization failed.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: cuDNN handle was created successfully.
    pub fn cudnnCreate(handle: *mut cudnnHandle_t) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function releases the resources used by the cuDNN handle. This function is usually the last call made to cuDNN with a particular handle. Because [`cudnnCreate`] allocates internal resources, the release of those resources by calling [`cudnnDestroy`] will implicitly call [cudaDeviceSynchronize](https://docs.nvidia.com/cuda/cuda-runtime-api/group__CUDART__DEVICE.html#group__CUDART__DEVICE_1g10e20b05a95f638a4071a655503df25d); therefore, the recommended best practice is to call [`cudnnCreate`] or [`cudnnDestroy`] outside of performance-critical code paths.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. The cuDNN handle to be destroyed.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The cuDNN context destruction was successful.
    pub fn cudnnDestroy(handle: cudnnHandle_t) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function sets the user’s CUDA stream in the cuDNN handle. The new stream will be used to launch cuDNN GPU kernels or to synchronize to this stream when cuDNN kernels are launched in the internal streams. If the cuDNN library stream is not set, all kernels use the default (`NULL`) stream. Setting the user stream in the cuDNN handle guarantees the issue-order execution of cuDNN calls and other GPU kernels launched in the same stream.
    ///
    /// With CUDA 11.x or later, internal streams have the same priority as the stream set by the last call to this function. In CUDA graph capture mode, CUDA 11.8 or later is required in order for the stream priorities to match.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Pointer to the cuDNN handle.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: Invalid (`NULL`) handle.
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM_STREAM_MISMATCH`]: Mismatch between the user stream and the cuDNN handle context.
    /// - [`cudnnStatus_t::CUDNN_STATUS_INTERNAL_ERROR`]: CUDA stream APIs reported further errors inside cuDNN.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: The stream priority is out of range.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The new stream was set successfully.
    pub fn cudnnSetStream(
        handle: cudnnHandle_t,
        streamId: cudaStream_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function retrieves the user CUDA stream programmed in the cuDNN handle. When the user’s CUDA stream is not set in the cuDNN handle, this function reports the null-stream.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Pointer to the cuDNN handle.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: Invalid (`NULL`) handle.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The stream identifier was retrieved successfully.
    pub fn cudnnGetStream(
        handle: cudnnHandle_t,
        streamId: *mut cudaStream_t,
    ) -> cudnnStatus_t;
}
/// [`cudnnDataType_t`] is an enumerated type indicating the data type to which a tensor descriptor or filter descriptor refers.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnDataType_t {
    /// The data is a 32-bit single-precision floating-point (`float`).
    CUDNN_DATA_FLOAT = 0,
    /// The data is a 64-bit double-precision floating-point (`double`).
    CUDNN_DATA_DOUBLE = 1,
    /// The data is a 16-bit floating-point.
    CUDNN_DATA_HALF = 2,
    /// The data is an 8-bit signed integer.
    CUDNN_DATA_INT8 = 3,
    /// The data is a 32-bit signed integer.
    CUDNN_DATA_INT32 = 4,
    /// The data is 32-bit elements each composed of 4 8-bit signed integers. This data type is only supported with the tensor format [`cudnnTensorFormat_t::CUDNN_TENSOR_NCHW_VECT_C`].
    CUDNN_DATA_INT8x4 = 5,
    /// The data is an 8-bit unsigned integer.
    CUDNN_DATA_UINT8 = 6,
    /// The data is 32-bit elements each composed of 4 8-bit unsigned integers. This data type is only supported with the tensor format [`cudnnTensorFormat_t::CUDNN_TENSOR_NCHW_VECT_C`].
    CUDNN_DATA_UINT8x4 = 7,
    /// The data is 32-element vectors, each element being an 8-bit signed integer. This data type is only supported with the tensor format [`cudnnTensorFormat_t::CUDNN_TENSOR_NCHW_VECT_C`]. Moreover, this data type can only be used with `algo 1`, meaning, [`cudnnConvolutionFwdAlgo_t::CUDNN_CONVOLUTION_FWD_ALGO_IMPLICIT_PRECOMP_GEMM`]. For more information, refer to [`cudnnConvolutionFwdAlgo_t`].
    CUDNN_DATA_INT8x32 = 8,
    /// The data is a 16-bit quantity, with 7 mantissa bits, 8 exponent bits, and 1 sign bit.
    CUDNN_DATA_BFLOAT16 = 9,
    /// The data is a 64-bit signed integer.
    CUDNN_DATA_INT64 = 10,
    /// The data is a boolean (`bool`).
    ///
    /// Note that for type [`cudnnBackendAttributeType_t::CUDNN_TYPE_BOOLEAN`], elements are expected to be “packed”: that is, one byte contains 8 elements of type [`cudnnBackendAttributeType_t::CUDNN_TYPE_BOOLEAN`]. Further, within each byte, elements are indexed from the least significant bit to the most significant bit. For example, a 1 dimensional tensor of 8 elements containing 01001111 has value 1 for elements 0 through 3, 0 for elements 4 and 5, 1 for element 6 and 0 for element 7.
    ///
    /// Tensors with more than 8 elements simply use more bytes, where the order is also from least significant to most significant byte. Note, CUDA is little-endian, meaning that the least significant byte has the lower memory address address. For example, in the case of 16 elements, 01001111 11111100 has value 1 for elements 0 through 3, 0 for elements 4 and 5, 1 for element 6 and 0 for element 7, value 0 for elements 8 and 9, 1 for elements 10 through 15.
    CUDNN_DATA_BOOLEAN = 11,
    /// The data is an 8-bit quantity, with 3 mantissa bits, 4 exponent bits, and 1 sign bit.
    CUDNN_DATA_FP8_E4M3 = 12,
    /// The data is an 8-bit quantity, with 2 mantissa bits, 5 exponent bits, and 1 sign bit.
    CUDNN_DATA_FP8_E5M2 = 13,
    /// The data type is a higher throughput but lower precision compute type (compared to [`cudnnDataType_t::CUDNN_DATA_FLOAT`]) used for FP8 tensor core operations.
    CUDNN_DATA_FAST_FLOAT_FOR_FP8 = 14,
    CUDNN_DATA_FP8_E8M0 = 15,
    CUDNN_DATA_FP4_E2M1 = 16,
    CUDNN_DATA_INT4 = 17,
    CUDNN_DATA_UINT4 = 18,
    CUDNN_DATA_UINT32 = 19,
    CUDNN_DATA_COMPLEX_FP32 = 20,
    CUDNN_DATA_COMPLEX_FP64 = 21,
}
/// [`cudnnMathType_t`] is an enumerated type used to indicate if the use of Tensor Core operations is permitted in a given library routine.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnMathType_t {
    /// Tensor Core operations are not used on pre-NVIDIA A100 GPU devices. On A100 GPU architecture devices, Tensor Core TF32 operation is permitted.
    CUDNN_DEFAULT_MATH = 0,
    /// The use of Tensor Core operations is permitted but will not actively perform datatype down conversion on tensors in order to utilize Tensor Cores.
    CUDNN_TENSOR_OP_MATH = 1,
    /// The use of Tensor Core operations is permitted and will actively perform datatype down conversion on tensors in order to utilize Tensor Cores.
    CUDNN_TENSOR_OP_MATH_ALLOW_CONVERSION = 2,
    /// Restricted to only kernels that use FMA instructions.
    ///
    /// On pre-NVIDIA A100 GPU devices, [`cudnnMathType_t::CUDNN_DEFAULT_MATH`] and [`cudnnMathType_t::CUDNN_FMA_MATH`] have the same behavior: Tensor Core kernels will not be selected. With NVIDIA Ampere architecture and CUDA toolkit 11, [`cudnnMathType_t::CUDNN_DEFAULT_MATH`] permits TF32 Tensor Core operation and [`cudnnMathType_t::CUDNN_FMA_MATH`] does not. The TF32 behavior for [`cudnnMathType_t::CUDNN_DEFAULT_MATH`] and the other Tensor Core math types can be explicitly disabled by the environment variable `NVIDIA_TF32_OVERRIDE=0`.
    CUDNN_FMA_MATH = 3,
}
/// [`cudnnNanPropagation_t`] is an enumerated type used to indicate if a given routine should propagate Nan numbers. This enumerated type is used as a field for the [`cudnnActivationDescriptor_t`] descriptor and [`cudnnPoolingDescriptor_t`] descriptor.
#[deprecated]
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnNanPropagation_t {
    /// `NAN` numbers are not propagated.
    #[deprecated]
    CUDNN_NOT_PROPAGATE_NAN = 0,
    /// `NAN` numbers are propagated.
    #[deprecated]
    CUDNN_PROPAGATE_NAN = 1,
}
/// Enumerated type used by [`cudnnSetCTCLossDescriptor_v9`] and [`cudnnGetCTCLossDescriptor_v9`] to indicate the behavior for out of boundary (OOB) samples. OOB samples are samples where L+R > T is encountered during the gradient calculation.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnCTCGradMode_t {
    CUDNN_CTC_ZERO_OOB_GRADIENTS = 0,
    CUDNN_CTC_SKIP_OOB_GRADIENTS = 1,
}
/// [`cudnnTensorFormat_t`] is an enumerated type used by [`cudnnSetTensor4dDescriptor`] to create a tensor with a pre-defined layout. For a detailed explanation of how these tensors are arranged in memory, refer to [Data Layout Formats](https://docs.nvidia.com/deeplearning/cudnn//latest/developer/core-concepts.html#data-layout-formats "(in NVIDIA cuDNN)").
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnTensorFormat_t {
    /// This tensor format specifies that the data is laid out in the following order: batch size, feature maps, rows, columns. The strides are implicitly defined in such a way that the data are contiguous in memory with no padding between images, feature maps, rows, and columns; the columns are the inner dimension and the images are the outermost dimension.
    CUDNN_TENSOR_NCHW = 0,
    /// This tensor format specifies that the data is laid out in the following order: batch size, rows, columns, feature maps. The strides are implicitly defined in such a way that the data are contiguous in memory with no padding between images, rows, columns, and feature maps; the feature maps are the inner dimension and the images are the outermost dimension.
    CUDNN_TENSOR_NHWC = 1,
    /// This tensor format specifies that the data is laid out in the following order: batch size, feature maps, rows, columns. However, each element of the tensor is a vector of multiple feature maps. The length of the vector is carried by the data type of the tensor. The strides are implicitly defined in such a way that the data are contiguous in memory with no padding between images, feature maps, rows, and columns; the columns are the inner dimension and the images are the outermost dimension. This format is only supported with tensor data types [`cudnnDataType_t::CUDNN_DATA_INT8x4`], [`cudnnDataType_t::CUDNN_DATA_INT8x32`], and [`cudnnDataType_t::CUDNN_DATA_UINT8x4`].
    ///
    /// The [`cudnnTensorFormat_t::CUDNN_TENSOR_NCHW_VECT_C`] can also be interpreted in the following way: The NCHW INT8x32 format is really N x (C/32) x H x W x 32 (32 Cs for every W), just as the NCHW INT8x4 format is N x (C/4) x H x W x 4 (4 Cs for every W). Hence, the VECT_C name - each W is a vector (4 or 32) of Cs.
    CUDNN_TENSOR_NCHW_VECT_C = 2,
}
/// [`cudnnReduceTensorOp_t`] is an enumerated type used to indicate the Tensor Core operation to be used by the [`cudnnReduceTensor`] routine. This enumerated type is used as a field for the [`cudnnReduceTensorDescriptor_t`] descriptor.
#[deprecated]
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnReduceTensorOp_t {
    /// The operation to be performed is addition.
    #[deprecated]
    CUDNN_REDUCE_TENSOR_ADD = 0,
    /// The operation to be performed is multiplication.
    #[deprecated]
    CUDNN_REDUCE_TENSOR_MUL = 1,
    /// The operation to be performed is a minimum comparison.
    #[deprecated]
    CUDNN_REDUCE_TENSOR_MIN = 2,
    /// The operation to be performed is a maximum comparison.
    #[deprecated]
    CUDNN_REDUCE_TENSOR_MAX = 3,
    /// The operation to be performed is a maximum comparison of absolute values.
    #[deprecated]
    CUDNN_REDUCE_TENSOR_AMAX = 4,
    /// The operation to be performed is averaging.
    #[deprecated]
    CUDNN_REDUCE_TENSOR_AVG = 5,
    /// The operation to be performed is addition of absolute values.
    #[deprecated]
    CUDNN_REDUCE_TENSOR_NORM1 = 6,
    /// The operation to be performed is a square root of the sum of squares.
    #[deprecated]
    CUDNN_REDUCE_TENSOR_NORM2 = 7,
    /// The operation to be performed is multiplication, not including elements of value zero.
    #[deprecated]
    CUDNN_REDUCE_TENSOR_MUL_NO_ZEROS = 8,
}
/// [`cudnnActivationMode_t`] is an enumerated type used to select the neuron activation function used in [`cudnnActivationForward`], [`cudnnActivationBackward`], and [`cudnnConvolutionBiasActivationForward`].
#[deprecated]
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnActivationMode_t {
    /// Selects the sigmoid function.
    #[deprecated]
    CUDNN_ACTIVATION_SIGMOID = 0,
    /// Selects the rectified linear function.
    #[deprecated]
    CUDNN_ACTIVATION_RELU = 1,
    /// Selects the hyperbolic tangent function.
    #[deprecated]
    CUDNN_ACTIVATION_TANH = 2,
    /// Selects the clipped rectified linear function.
    #[deprecated]
    CUDNN_ACTIVATION_CLIPPED_RELU = 3,
    /// Selects the exponential linear function.
    #[deprecated]
    CUDNN_ACTIVATION_ELU = 4,
    /// Selects the identity function, intended for bypassing the activation step in [`cudnnConvolutionBiasActivationForward`]. (The [`cudnnConvolutionBiasActivationForward`] function must use [`cudnnConvolutionFwdAlgo_t::CUDNN_CONVOLUTION_FWD_ALGO_IMPLICIT_PRECOMP_GEMM`].) Does not work with [`cudnnActivationForward`] or [`cudnnActivationBackward`].
    #[deprecated]
    CUDNN_ACTIVATION_IDENTITY = 5,
    /// Selects the swish function.
    #[deprecated]
    CUDNN_ACTIVATION_SWISH = 6,
}
/// [`cudnnSeverity_t`] is an enumerated type passed to the customized callback function for logging that users may set. This enumerate describes the severity level of the item, so the customized logging call back may react differently. The numerical values are the same that are to be used for setting the `CUDNN_LOGLEVEL_DBG` environment variable.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnSeverity_t {
    CUDNN_SEV_FATAL = 0,
    CUDNN_SEV_ERROR = 1,
    CUDNN_SEV_WARNING = 2,
    CUDNN_SEV_INFO = 3,
}
#[repr(C)]
pub struct cudnnDebugStruct {
    pub cudnn_version: ::core::ffi::c_uint,
    pub cudnnStatus: cudnnStatus_t,
    pub time_sec: ::core::ffi::c_uint,
    pub time_usec: ::core::ffi::c_uint,
    pub time_delta: ::core::ffi::c_uint,
    pub handle: cudnnHandle_t,
    pub stream: cudaStream_t,
    pub pid: ::core::ffi::c_ulonglong,
    pub tid: ::core::ffi::c_ulonglong,
    pub cudaDeviceId: ::core::ffi::c_int,
    pub reserved: [::core::ffi::c_int; 15usize],
}
impl Default for cudnnDebugStruct {
    fn default() -> Self {
        let mut s = ::core::mem::MaybeUninit::<Self>::uninit();
        unsafe {
            ::core::ptr::write_bytes(s.as_mut_ptr(), 0, 1);
            s.assume_init()
        }
    }
}
/// [`cudnnDebug_t`] is a structure used by [`cudnnSetCallback`] and [`cudnnGetCallback`] containing the metadata, such as time, time since start, stream ID, process and thread ID, that the user may choose to print or store in customized callbacks.
pub type cudnnDebug_t = cudnnDebugStruct;
pub type cudnnCallback_t = ::core::option::Option<
    unsafe extern "C" fn(
        sev: cudnnSeverity_t,
        udata: *mut ::core::ffi::c_void,
        dbg: *const cudnnDebug_t,
        msg: *const ::core::ffi::c_char,
    ),
>;
unsafe extern "C" {
    /// This function sets the internal states of cuDNN error reporting functionality.
    ///
    /// # Parameters
    ///
    /// - `mask`: Input. An unsigned integer. The four least significant bits (LSBs) of this unsigned integer are used for switching on and off the different levels of error reporting messages. This applies for both the default callbacks, and for the customized callbacks. The bit position is in correspondence with the enum of [`cudnnSeverity_t`]. The user may utilize the predefined macros `CUDNN_SEV_ERROR_EN`, `CUDNN_SEV_WARNING_EN`, and `CUDNN_SEV_INFO_EN` to form the bit mask. When a bit is set to 1, the corresponding message channel is enabled.
    ///
    /// ```text
    /// For example, when bit 3 is set to 1, the API logging is enabled. Currently, only the log output of level `CUDNN_SEV_INFO` is functional; the others are not yet implemented. When used for turning on and off the logging with the default callback, the user may pass `NULL` to `udata` and `fptr`. In addition, the environment variable `CUDNN_LOGDEST_DBG` must be set. For more information, refer to the [Deprecation Policy](https://docs.nvidia.com/deeplearning/cudnn/backend/v9.22.0/developer/misc.html#backward-compatibility).
    ///
    /// `CUDNN_SEV_INFO_EN = 0b1000` (functional).
    ///
    /// `CUDNN_SEV_ERROR_EN = 0b0010` (functional).
    ///
    /// `CUDNN_SEV_WARNING_EN = 0b0100` (functional).
    ///
    /// The output of `CUDNN_SEV_FATAL` is always enabled and cannot be disabled.
    /// ```
    /// - `udata`: Input. A pointer provided by the user. This pointer will be passed to the user’s custom logging callback function. The data it points to will not be read, nor be changed by cuDNN. This pointer may be used in many ways, such as in a mutex or in a communication socket for the user’s callback function for logging. If the user is utilizing the default callback function, or doesn’t want to use this input in the customized callback function, they may pass in `NULL`.
    /// - `fptr`: Input. A pointer to a user-supplied callback function. When `NULL` is passed to this pointer, then cuDNN switches back to the built-in default callback function. The user-supplied callback function prototype must be similar to the following (also defined in the header file):
    ///
    /// ```text
    /// void customizedLoggingCallback (cudnnSeverity_t sev, void *udata, const cudnnDebug_t *dbg, const char *msg);
    /// ```
    ///
    /// The structure [`cudnnDebug_t`] is defined in the header file. It provides the metadata, such as time, time since start, stream ID, process and thread ID, that the user may choose to print or store in their customized callback.
    ///
    /// The variable `msg` is the logging message generated by cuDNN. Each line of this message is terminated by `\0`, and the end of the message is terminated by `\0\0`. Users may select what is necessary to show in the log, and may reformat the string.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The function launched successfully.
    pub fn cudnnSetCallback(
        mask: ::core::ffi::c_uint,
        udata: *mut ::core::ffi::c_void,
        fptr: cudnnCallback_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function queries the internal states of cuDNN error reporting functionality.
    ///
    /// # Parameters
    ///
    /// - `mask`: Output. Pointer to the address where the current internal error reporting message bit mask will be outputted.
    /// - `udata`: Output. Pointer to the address where the current internally stored `udata` address will be stored.
    /// - `fptr`: Output. Pointer to the address where the current internally stored `callback` function pointer will be stored. When the built-in default callback function is used, `NULL` will be outputted.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: If any of the input parameters are `NULL`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The function launched successfully.
    pub fn cudnnGetCallback(
        mask: *mut ::core::ffi::c_uint,
        udata: *mut *mut ::core::ffi::c_void,
        fptr: *mut cudnnCallback_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// Cross-library version checker. Each sub-library has a version checker that checks whether its own version matches that of its dependencies.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUBLIBRARY_VERSION_MISMATCH`]: The versions are inconsistent.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The version check passed.
    pub fn cudnnGraphVersionCheck() -> cudnnStatus_t;
}
/// [`cudnnConvolutionMode_t`] is an enumerated type used by [`cudnnSetConvolution2dDescriptor`] to configure a convolution `descriptor`. The filter used for the convolution can be applied in two different ways, corresponding mathematically to a convolution or to a cross-correlation. (A cross-correlation is equivalent to a convolution with its filter rotated by 180 degrees.)
#[deprecated]
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnConvolutionMode_t {
    /// In this mode, a convolution operation will be done when applying the filter to the images.
    #[deprecated]
    CUDNN_CONVOLUTION = 0,
    /// In this mode, a cross-correlation operation will be done when applying the filter to the images.
    #[deprecated]
    CUDNN_CROSS_CORRELATION = 1,
}
/// [`cudnnReorderType_t`] is an enumerated type to set the convolution reordering type. The reordering type can be set by [`cudnnSetConvolutionReorderType`] and its status can be read by [`cudnnGetConvolutionReorderType`].
#[deprecated]
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnReorderType_t {
    #[deprecated]
    CUDNN_DEFAULT_REORDER = 0,
    #[deprecated]
    CUDNN_NO_REORDER = 1,
}
/// [`cudnnBackendDescriptor_t`] is a typedef void pointer to one of many opaque `descriptor` structures. The type of structure that it points to is determined by the argument when allocating the memory for the opaque structure using [`cudnnBackendCreateDescriptor`].
///
/// Attributes of a `descriptor` can be set using [`cudnnBackendSetAttribute`]. After all required attributes of a `descriptor` are set, the `descriptor` can be finalized by [`cudnnBackendFinalize`]. From a finalized `descriptor`, one can query its queryable attributes using [`cudnnBackendGetAttribute`]. Finally, the memory allocated for a `descriptor` can be freed using [`cudnnBackendDestroyDescriptor`].
pub type cudnnBackendDescriptor_t = *mut ::core::ffi::c_void;
#[repr(C)]
#[derive(Debug, Default, Copy, Clone, Hash, PartialOrd, Ord, PartialEq, Eq)]
pub struct cudnnFractionStruct {
    pub numerator: i64,
    pub denominator: i64,
}
/// [`cudnnFraction_t`] is a structure that allows a user to define `int64_t` fractions.
pub type cudnnFraction_t = cudnnFractionStruct;
/// [`cudnnPointwiseMode_t`] is an enumerated type to indicate the intended pointwise math operation in the backend pointwise operation descriptor.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnPointwiseMode_t {
    /// A pointwise addition between two tensors is computed.
    CUDNN_POINTWISE_ADD = 0,
    /// A pointwise addition between the first tensor and the square of the second tensor is computed.
    CUDNN_POINTWISE_ADD_SQUARE = 5,
    /// A pointwise true division of the first tensor by second tensor is computed.
    CUDNN_POINTWISE_DIV = 6,
    /// A pointwise maximum is taken between two tensors.
    CUDNN_POINTWISE_MAX = 3,
    /// A pointwise minimum is taken between two tensors.
    CUDNN_POINTWISE_MIN = 2,
    /// A pointwise floating-point remainder of the first tensor’s division by the second tensor is computed.
    CUDNN_POINTWISE_MOD = 7,
    /// A pointwise multiplication between two tensors is computed.
    CUDNN_POINTWISE_MUL = 1,
    /// A pointwise value from the first tensor to the power of the second tensor is computed.
    CUDNN_POINTWISE_POW = 8,
    /// A pointwise subtraction between two tensors is computed.
    CUDNN_POINTWISE_SUB = 9,
    /// A pointwise absolute value of the input tensor is computed.
    CUDNN_POINTWISE_ABS = 10,
    /// A pointwise ceiling of the input tensor is computed.
    CUDNN_POINTWISE_CEIL = 11,
    /// A pointwise trigonometric cosine of the input tensor is computed.
    CUDNN_POINTWISE_COS = 12,
    /// A pointwise exponential of the input tensor is computed.
    CUDNN_POINTWISE_EXP = 13,
    /// A pointwise floor of the input tensor is computed.
    CUDNN_POINTWISE_FLOOR = 14,
    /// A pointwise natural logarithm of the input tensor is computed.
    CUDNN_POINTWISE_LOG = 15,
    /// A pointwise numerical negative of the input tensor is computed.
    CUDNN_POINTWISE_NEG = 16,
    /// A pointwise reciprocal of the square root of the input tensor is computed.
    CUDNN_POINTWISE_RSQRT = 17,
    /// A pointwise trigonometric sine of the input tensor is computed.
    CUDNN_POINTWISE_SIN = 18,
    /// A pointwise square root of the input tensor is computed.
    CUDNN_POINTWISE_SQRT = 4,
    /// A pointwise trigonometric tangent of the input tensor is computed.
    CUDNN_POINTWISE_TAN = 19,
    /// A pointwise Error Function is computed.
    CUDNN_POINTWISE_ERF = 20,
    /// No computation is performed. As with other pointwise modes, this mode provides implicit conversions by specifying the data type of the input tensor as one type, and the data type of the output tensor as another.
    CUDNN_POINTWISE_IDENTITY = 21,
    /// A pointwise reciprocal of the input tensor is computed. In other words, for every element x in the input tensor, 1/x is computed.
    CUDNN_POINTWISE_RECIPROCAL = 22,
    CUDNN_POINTWISE_ATAN2 = 23,
    /// A pointwise rectified linear activation function of the input tensor is computed.
    CUDNN_POINTWISE_RELU_FWD = 100,
    /// A pointwise tanh activation function of the input tensor is computed.
    CUDNN_POINTWISE_TANH_FWD = 101,
    /// A pointwise sigmoid activation function of the input tensor is computed.
    CUDNN_POINTWISE_SIGMOID_FWD = 102,
    /// A pointwise Exponential Linear Unit activation function of the input tensor is computed.
    CUDNN_POINTWISE_ELU_FWD = 103,
    /// A pointwise Gaussian Error Linear Unit activation function of the input tensor is computed.
    CUDNN_POINTWISE_GELU_FWD = 104,
    /// A pointwise softplus activation function of the input tensor is computed.
    CUDNN_POINTWISE_SOFTPLUS_FWD = 105,
    /// A pointwise swish activation function of the input tensor is computed.
    CUDNN_POINTWISE_SWISH_FWD = 106,
    /// A pointwise tanh approximation of the Gaussian Error Linear Unit activation function of the input tensor is computed. The tanh GELU approximation is computed as $0.5x\left( 1+\tanh\left\[ \sqrt{2/\pi}\left( x+0.044715x^{3} \right) \right\] \right)$. For more information, refer to the [GAUSSIAN ERROR LINEAR UNIT (GELUS)](https://arxiv.org/pdf/1606.08415.pdf) paper.
    CUDNN_POINTWISE_GELU_APPROX_TANH_FWD = 107,
    /// A pointwise first derivative of rectified linear activation of the input tensor is computed.
    CUDNN_POINTWISE_RELU_BWD = 200,
    /// A pointwise first derivative of tanh activation of the input tensor is computed.
    CUDNN_POINTWISE_TANH_BWD = 201,
    /// A pointwise first derivative of sigmoid activation of the input tensor is computed.
    CUDNN_POINTWISE_SIGMOID_BWD = 202,
    /// A pointwise first derivative of Exponential Linear Unit activation of the input tensor is computed.
    CUDNN_POINTWISE_ELU_BWD = 203,
    /// A pointwise first derivative of Gaussian Error Linear Unit activation of the input tensor is computed.
    CUDNN_POINTWISE_GELU_BWD = 204,
    /// A pointwise first derivative of softplus activation of the input tensor is computed.
    CUDNN_POINTWISE_SOFTPLUS_BWD = 205,
    /// A pointwise first derivative of swish activation of the input tensor is computed.
    CUDNN_POINTWISE_SWISH_BWD = 206,
    /// A pointwise first derivative of the tanh approximation of the Gaussian Error Linear Unit activation of the input tensor is computed. This is computed as $0.5\left( 1+\tanh\left( b\left( x+cx^{3} \right) \right)+bxsech^{2}\left( b\left( cx^{3}+x \right) \right)\left( 3cx^{2}+1 \right)dy \right)$ where $b$ is $\sqrt{2/\pi}$ and $c$ is $0.044715$.
    CUDNN_POINTWISE_GELU_APPROX_TANH_BWD = 207,
    /// A pointwise truth value of the first tensor equal to the second tensor is computed.
    CUDNN_POINTWISE_CMP_EQ = 300,
    /// A pointwise truth value of the first tensor not equal to the second tensor is computed.
    CUDNN_POINTWISE_CMP_NEQ = 301,
    /// A pointwise truth value of the first tensor greater than the second tensor is computed.
    CUDNN_POINTWISE_CMP_GT = 302,
    /// A pointwise truth value of the first tensor greater than equal to the second tensor is computed.
    CUDNN_POINTWISE_CMP_GE = 303,
    /// A pointwise truth value of the first tensor less than the second tensor is computed.
    CUDNN_POINTWISE_CMP_LT = 304,
    /// A pointwise truth value of the first tensor less than equal to the second tensor is computed.
    CUDNN_POINTWISE_CMP_LE = 305,
    /// A pointwise truth value of the first tensor logical `AND` second tensor is computed.
    CUDNN_POINTWISE_LOGICAL_AND = 400,
    /// A pointwise truth value of the first tensor logical `OR` second tensor is computed.
    CUDNN_POINTWISE_LOGICAL_OR = 401,
    /// A pointwise truth value of input tensors logical `NOT` is computed.
    CUDNN_POINTWISE_LOGICAL_NOT = 402,
    /// A pointwise index value of the input tensor is generated along a given axis.
    CUDNN_POINTWISE_GEN_INDEX = 501,
    /// A pointwise value is selected amongst two input tensors based on a given predicate tensor.
    CUDNN_POINTWISE_BINARY_SELECT = 601,
}
impl cudnnResampleMode_t {
    pub const CUDNN_RESAMPLE_AVGPOOL_INCLUDE_PADDING: cudnnResampleMode_t = cudnnResampleMode_t::CUDNN_RESAMPLE_AVGPOOL;
}
/// [`cudnnResampleMode_t`] is an enumerated type to indicate the resample mode in the backend resample operations.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnResampleMode_t {
    CUDNN_RESAMPLE_NEAREST = 0,
    CUDNN_RESAMPLE_BILINEAR = 1,
    CUDNN_RESAMPLE_AVGPOOL = 2,
    CUDNN_RESAMPLE_AVGPOOL_EXCLUDE_PADDING = 4,
    CUDNN_RESAMPLE_MAXPOOL = 3,
}
/// [`cudnnSignalMode_t`] is an enumerated type to indicate the signaling mode in the backend signal operation.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnSignalMode_t {
    /// The flag variable is updated with the provided signal value atomically.
    CUDNN_SIGNAL_SET = 0,
    /// The operation blocks until the flag variable keeps comparing equal to the provided signal value.
    CUDNN_SIGNAL_WAIT = 1,
}
/// [`cudnnGenStatsMode_t`] is an enumerated type to indicate the statistics mode in the backend statistics generation operation.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnGenStatsMode_t {
    /// In this mode, the sum and sum of squares of the input tensor along the specified dimensions are computed and written out. The reduction dimensions currently supported are limited per channel, however additional support may be added upon request.
    CUDNN_GENSTATS_SUM_SQSUM = 0,
}
/// [`cudnnBnFinalizeStatsMode_t`] is an enumerated type that that exposes the different mathematical operation modes that converts batch norm statistics and the trained scale and bias to the equivalent scale and bias to be applied in the next normalization stage for inference and training use cases.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnBnFinalizeStatsMode_t {
    CUDNN_BN_FINALIZE_STATISTICS_TRAINING = 0,
    CUDNN_BN_FINALIZE_STATISTICS_INFERENCE = 1,
}
/// [`cudnnRngDistribution_t`] is an enumerated type to indicate the distribution to be used in the backend `Rng` (random number generator) operation.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnRngDistribution_t {
    /// The bernoulli distribution is used for the random number generation. The attribute [`cudnnBackendAttributeName_t::CUDNN_ATTR_RNG_BERNOULLI_DIST_PROBABILITY`] can be used to specify the probability of generating 1’s.
    CUDNN_RNG_DISTRIBUTION_BERNOULLI = 0,
    /// The uniform distribution is used for the random number generation. The attribute [`cudnnBackendAttributeName_t::CUDNN_ATTR_RNG_UNIFORM_DIST_MINIMUM`] and [`cudnnBackendAttributeName_t::CUDNN_ATTR_RNG_UNIFORM_DIST_MAXIMUM`] can be used to specify the minimum and maximum value between which the random numbers should be uniformly generated.
    CUDNN_RNG_DISTRIBUTION_UNIFORM = 1,
    /// The normal distribution is used for the random number generation. The attribute [`cudnnBackendAttributeName_t::CUDNN_ATTR_RNG_NORMAL_DIST_MEAN`] and [`cudnnBackendAttributeName_t::CUDNN_ATTR_RNG_NORMAL_DIST_STANDARD_DEVIATION`] can be used to specify the mean and standard deviation of the random number generator.
    CUDNN_RNG_DISTRIBUTION_NORMAL = 2,
}
#[repr(u32)]
#[derive(Debug, Copy, Clone, Hash, PartialOrd, Ord, PartialEq, Eq)]
pub enum cudnnMoeGroupedMatmulMode_t {
    CUDNN_MOE_GROUPED_MATMUL_MODE_NONE = 0,
    CUDNN_MOE_GROUPED_MATMUL_MODE_GATHER = 1,
    CUDNN_MOE_GROUPED_MATMUL_MODE_SCATTER = 2,
}
/// [`cudnnBackendAttributeName_t`] is an enumerated type that indicates the backend descriptor attributes that can be set or get using [`cudnnBackendSetAttribute`] and [`cudnnBackendGetAttribute`] functions. The backend descriptor to which an attribute belongs is identified by the prefix of the attribute name.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnBackendAttributeName_t {
    CUDNN_ATTR_POINTWISE_MODE = 0,
    CUDNN_ATTR_POINTWISE_MATH_PREC = 1,
    CUDNN_ATTR_POINTWISE_NAN_PROPAGATION = 2,
    CUDNN_ATTR_POINTWISE_RELU_LOWER_CLIP = 3,
    CUDNN_ATTR_POINTWISE_RELU_UPPER_CLIP = 4,
    CUDNN_ATTR_POINTWISE_RELU_LOWER_CLIP_SLOPE = 5,
    CUDNN_ATTR_POINTWISE_ELU_ALPHA = 6,
    CUDNN_ATTR_POINTWISE_SOFTPLUS_BETA = 7,
    CUDNN_ATTR_POINTWISE_SWISH_BETA = 8,
    CUDNN_ATTR_POINTWISE_AXIS = 9,
    CUDNN_ATTR_CONVOLUTION_COMP_TYPE = 100,
    CUDNN_ATTR_CONVOLUTION_CONV_MODE = 101,
    CUDNN_ATTR_CONVOLUTION_DILATIONS = 102,
    CUDNN_ATTR_CONVOLUTION_FILTER_STRIDES = 103,
    CUDNN_ATTR_CONVOLUTION_POST_PADDINGS = 104,
    CUDNN_ATTR_CONVOLUTION_PRE_PADDINGS = 105,
    CUDNN_ATTR_CONVOLUTION_SPATIAL_DIMS = 106,
    CUDNN_ATTR_ENGINEHEUR_MODE = 200,
    CUDNN_ATTR_ENGINEHEUR_OPERATION_GRAPH = 201,
    CUDNN_ATTR_ENGINEHEUR_RESULTS = 202,
    CUDNN_ATTR_ENGINEHEUR_SM_COUNT_TARGET = 203,
    CUDNN_ATTR_ENGINEHEUR_DEVICEPROP = 204,
    CUDNN_ATTR_ENGINECFG_ENGINE = 300,
    CUDNN_ATTR_ENGINECFG_INTERMEDIATE_INFO = 301,
    CUDNN_ATTR_ENGINECFG_KNOB_CHOICES = 302,
    CUDNN_ATTR_ENGINECFG_WORKSPACE_SIZE = 303,
    CUDNN_ATTR_ENGINECFG_SHARED_MEMORY_USED = 304,
    CUDNN_ATTR_EXECUTION_PLAN_HANDLE = 400,
    CUDNN_ATTR_EXECUTION_PLAN_ENGINE_CONFIG = 401,
    CUDNN_ATTR_EXECUTION_PLAN_WORKSPACE_SIZE = 402,
    CUDNN_ATTR_EXECUTION_PLAN_COMPUTED_INTERMEDIATE_UIDS = 403,
    CUDNN_ATTR_EXECUTION_PLAN_RUN_ONLY_INTERMEDIATE_UIDS = 404,
    CUDNN_ATTR_EXECUTION_PLAN_JSON_REPRESENTATION = 405,
    CUDNN_ATTR_EXECUTION_PLAN_KERNEL_CACHE = 406,
    CUDNN_ATTR_EXECUTION_PLAN_DEVICEPROP = 407,
    CUDNN_ATTR_INTERMEDIATE_INFO_UNIQUE_ID = 500,
    CUDNN_ATTR_INTERMEDIATE_INFO_SIZE = 501,
    CUDNN_ATTR_INTERMEDIATE_INFO_DEPENDENT_DATA_UIDS = 502,
    CUDNN_ATTR_INTERMEDIATE_INFO_DEPENDENT_ATTRIBUTES = 503,
    CUDNN_ATTR_KNOB_CHOICE_KNOB_TYPE = 600,
    CUDNN_ATTR_KNOB_CHOICE_KNOB_VALUE = 601,
    CUDNN_ATTR_OPERATION_CONVOLUTION_FORWARD_ALPHA = 700,
    CUDNN_ATTR_OPERATION_CONVOLUTION_FORWARD_BETA = 701,
    CUDNN_ATTR_OPERATION_CONVOLUTION_FORWARD_CONV_DESC = 702,
    CUDNN_ATTR_OPERATION_CONVOLUTION_FORWARD_W = 703,
    CUDNN_ATTR_OPERATION_CONVOLUTION_FORWARD_X = 704,
    CUDNN_ATTR_OPERATION_CONVOLUTION_FORWARD_Y = 705,
    CUDNN_ATTR_OPERATION_CONVOLUTION_BWD_DATA_ALPHA = 706,
    CUDNN_ATTR_OPERATION_CONVOLUTION_BWD_DATA_BETA = 707,
    CUDNN_ATTR_OPERATION_CONVOLUTION_BWD_DATA_CONV_DESC = 708,
    CUDNN_ATTR_OPERATION_CONVOLUTION_BWD_DATA_W = 709,
    CUDNN_ATTR_OPERATION_CONVOLUTION_BWD_DATA_DX = 710,
    CUDNN_ATTR_OPERATION_CONVOLUTION_BWD_DATA_DY = 711,
    CUDNN_ATTR_OPERATION_CONVOLUTION_BWD_FILTER_ALPHA = 712,
    CUDNN_ATTR_OPERATION_CONVOLUTION_BWD_FILTER_BETA = 713,
    CUDNN_ATTR_OPERATION_CONVOLUTION_BWD_FILTER_CONV_DESC = 714,
    CUDNN_ATTR_OPERATION_CONVOLUTION_BWD_FILTER_DW = 715,
    CUDNN_ATTR_OPERATION_CONVOLUTION_BWD_FILTER_X = 716,
    CUDNN_ATTR_OPERATION_CONVOLUTION_BWD_FILTER_DY = 717,
    CUDNN_ATTR_OPERATION_POINTWISE_PW_DESCRIPTOR = 750,
    CUDNN_ATTR_OPERATION_POINTWISE_XDESC = 751,
    CUDNN_ATTR_OPERATION_POINTWISE_BDESC = 752,
    CUDNN_ATTR_OPERATION_POINTWISE_YDESC = 753,
    CUDNN_ATTR_OPERATION_POINTWISE_ALPHA1 = 754,
    CUDNN_ATTR_OPERATION_POINTWISE_ALPHA2 = 755,
    CUDNN_ATTR_OPERATION_POINTWISE_DXDESC = 756,
    CUDNN_ATTR_OPERATION_POINTWISE_DYDESC = 757,
    CUDNN_ATTR_OPERATION_POINTWISE_TDESC = 758,
    CUDNN_ATTR_OPERATION_GENSTATS_MODE = 770,
    CUDNN_ATTR_OPERATION_GENSTATS_MATH_PREC = 771,
    CUDNN_ATTR_OPERATION_GENSTATS_XDESC = 772,
    CUDNN_ATTR_OPERATION_GENSTATS_SUMDESC = 773,
    CUDNN_ATTR_OPERATION_GENSTATS_SQSUMDESC = 774,
    CUDNN_ATTR_OPERATION_BN_FINALIZE_STATS_MODE = 780,
    CUDNN_ATTR_OPERATION_BN_FINALIZE_MATH_PREC = 781,
    CUDNN_ATTR_OPERATION_BN_FINALIZE_Y_SUM_DESC = 782,
    CUDNN_ATTR_OPERATION_BN_FINALIZE_Y_SQ_SUM_DESC = 783,
    CUDNN_ATTR_OPERATION_BN_FINALIZE_SCALE_DESC = 784,
    CUDNN_ATTR_OPERATION_BN_FINALIZE_BIAS_DESC = 785,
    CUDNN_ATTR_OPERATION_BN_FINALIZE_PREV_RUNNING_MEAN_DESC = 786,
    CUDNN_ATTR_OPERATION_BN_FINALIZE_PREV_RUNNING_VAR_DESC = 787,
    CUDNN_ATTR_OPERATION_BN_FINALIZE_UPDATED_RUNNING_MEAN_DESC = 788,
    CUDNN_ATTR_OPERATION_BN_FINALIZE_UPDATED_RUNNING_VAR_DESC = 789,
    CUDNN_ATTR_OPERATION_BN_FINALIZE_SAVED_MEAN_DESC = 790,
    CUDNN_ATTR_OPERATION_BN_FINALIZE_SAVED_INV_STD_DESC = 791,
    CUDNN_ATTR_OPERATION_BN_FINALIZE_EQ_SCALE_DESC = 792,
    CUDNN_ATTR_OPERATION_BN_FINALIZE_EQ_BIAS_DESC = 793,
    CUDNN_ATTR_OPERATION_BN_FINALIZE_ACCUM_COUNT_DESC = 794,
    CUDNN_ATTR_OPERATION_BN_FINALIZE_EPSILON_DESC = 795,
    CUDNN_ATTR_OPERATION_BN_FINALIZE_EXP_AVERATE_FACTOR_DESC = 796,
    CUDNN_ATTR_OPERATIONGRAPH_HANDLE = 800,
    CUDNN_ATTR_OPERATIONGRAPH_OPS = 801,
    CUDNN_ATTR_OPERATIONGRAPH_ENGINE_GLOBAL_COUNT = 802,
    CUDNN_ATTR_OPERATIONGRAPH_IS_DYNAMIC_SHAPE_ENABLED = 803,
    CUDNN_ATTR_OPERATIONGRAPH_IS_SAME_TOPOLOGY = 804,
    CUDNN_ATTR_OPERATIONGRAPH_IS_OVERRIDE_SHAPE_ENABLED = 805,
    CUDNN_ATTR_TENSOR_BYTE_ALIGNMENT = 900,
    CUDNN_ATTR_TENSOR_DATA_TYPE = 901,
    CUDNN_ATTR_TENSOR_DIMENSIONS = 902,
    CUDNN_ATTR_TENSOR_STRIDES = 903,
    CUDNN_ATTR_TENSOR_VECTOR_COUNT = 904,
    CUDNN_ATTR_TENSOR_VECTORIZED_DIMENSION = 905,
    CUDNN_ATTR_TENSOR_UNIQUE_ID = 906,
    CUDNN_ATTR_TENSOR_IS_VIRTUAL = 907,
    CUDNN_ATTR_TENSOR_IS_BY_VALUE = 908,
    CUDNN_ATTR_TENSOR_REORDERING_MODE = 909,
    CUDNN_ATTR_TENSOR_CONSTANT_VALUE = 910,
    CUDNN_ATTR_TENSOR_RAGGED_OFFSET_DESC = 913,
    CUDNN_ATTR_VARIANT_PACK_UNIQUE_IDS = 1000,
    CUDNN_ATTR_VARIANT_PACK_DATA_POINTERS = 1001,
    CUDNN_ATTR_VARIANT_PACK_INTERMEDIATES = 1002,
    CUDNN_ATTR_VARIANT_PACK_WORKSPACE = 1003,
    CUDNN_ATTR_VARIANT_PACK_OVERRIDE_UNIQUE_IDS = 1010,
    CUDNN_ATTR_VARIANT_PACK_OVERRIDE_SHAPES = 1011,
    CUDNN_ATTR_VARIANT_PACK_OVERRIDE_STRIDES = 1012,
    CUDNN_ATTR_LAYOUT_INFO_TENSOR_UID = 1100,
    CUDNN_ATTR_LAYOUT_INFO_TYPES = 1101,
    CUDNN_ATTR_KNOB_INFO_TYPE = 1200,
    CUDNN_ATTR_KNOB_INFO_MAXIMUM_VALUE = 1201,
    CUDNN_ATTR_KNOB_INFO_MINIMUM_VALUE = 1202,
    CUDNN_ATTR_KNOB_INFO_STRIDE = 1203,
    CUDNN_ATTR_ENGINE_OPERATION_GRAPH = 1300,
    CUDNN_ATTR_ENGINE_GLOBAL_INDEX = 1301,
    CUDNN_ATTR_ENGINE_KNOB_INFO = 1302,
    CUDNN_ATTR_ENGINE_NUMERICAL_NOTE = 1303,
    CUDNN_ATTR_ENGINE_LAYOUT_INFO = 1304,
    CUDNN_ATTR_ENGINE_BEHAVIOR_NOTE = 1305,
    CUDNN_ATTR_ENGINE_SM_COUNT_TARGET = 1306,
    CUDNN_ATTR_ENGINE_DEVICEPROP = 1307,
    CUDNN_ATTR_ENGINE_DISABLE_CLUSTER_COOPERATIVE = 1308,
    CUDNN_ATTR_MATMUL_COMP_TYPE = 1500,
    CUDNN_ATTR_MATMUL_PADDING_VALUE = 1503,
    CUDNN_ATTR_OPERATION_MATMUL_ADESC = 1520,
    CUDNN_ATTR_OPERATION_MATMUL_BDESC = 1521,
    CUDNN_ATTR_OPERATION_MATMUL_CDESC = 1522,
    CUDNN_ATTR_OPERATION_MATMUL_DESC = 1523,
    CUDNN_ATTR_OPERATION_MATMUL_IRREGULARLY_STRIDED_BATCH_COUNT = 1524,
    CUDNN_ATTR_OPERATION_MATMUL_GEMM_M_OVERRIDE_DESC = 1525,
    CUDNN_ATTR_OPERATION_MATMUL_GEMM_N_OVERRIDE_DESC = 1526,
    CUDNN_ATTR_OPERATION_MATMUL_GEMM_K_OVERRIDE_DESC = 1527,
    CUDNN_ATTR_REDUCTION_OPERATOR = 1600,
    CUDNN_ATTR_REDUCTION_COMP_TYPE = 1601,
    CUDNN_ATTR_REDUCTION_IS_DETERMINISTIC = 1602,
    CUDNN_ATTR_OPERATION_REDUCTION_XDESC = 1610,
    CUDNN_ATTR_OPERATION_REDUCTION_YDESC = 1611,
    CUDNN_ATTR_OPERATION_REDUCTION_DESC = 1612,
    CUDNN_ATTR_OPERATION_BN_BWD_WEIGHTS_MATH_PREC = 1620,
    CUDNN_ATTR_OPERATION_BN_BWD_WEIGHTS_MEAN_DESC = 1621,
    CUDNN_ATTR_OPERATION_BN_BWD_WEIGHTS_INVSTD_DESC = 1622,
    CUDNN_ATTR_OPERATION_BN_BWD_WEIGHTS_BN_SCALE_DESC = 1623,
    CUDNN_ATTR_OPERATION_BN_BWD_WEIGHTS_X_DESC = 1624,
    CUDNN_ATTR_OPERATION_BN_BWD_WEIGHTS_DY_DESC = 1625,
    CUDNN_ATTR_OPERATION_BN_BWD_WEIGHTS_DBN_SCALE_DESC = 1626,
    CUDNN_ATTR_OPERATION_BN_BWD_WEIGHTS_DBN_BIAS_DESC = 1627,
    CUDNN_ATTR_OPERATION_BN_BWD_WEIGHTS_EQ_DY_SCALE_DESC = 1628,
    CUDNN_ATTR_OPERATION_BN_BWD_WEIGHTS_EQ_X_SCALE_DESC = 1629,
    CUDNN_ATTR_OPERATION_BN_BWD_WEIGHTS_EQ_BIAS = 1630,
    CUDNN_ATTR_RESAMPLE_MODE = 1700,
    CUDNN_ATTR_RESAMPLE_COMP_TYPE = 1701,
    CUDNN_ATTR_RESAMPLE_SPATIAL_DIMS = 1702,
    CUDNN_ATTR_RESAMPLE_POST_PADDINGS = 1703,
    CUDNN_ATTR_RESAMPLE_PRE_PADDINGS = 1704,
    CUDNN_ATTR_RESAMPLE_STRIDES = 1705,
    CUDNN_ATTR_RESAMPLE_WINDOW_DIMS = 1706,
    CUDNN_ATTR_RESAMPLE_NAN_PROPAGATION = 1707,
    CUDNN_ATTR_RESAMPLE_PADDING_MODE = 1708,
    CUDNN_ATTR_OPERATION_RESAMPLE_FWD_XDESC = 1710,
    CUDNN_ATTR_OPERATION_RESAMPLE_FWD_YDESC = 1711,
    CUDNN_ATTR_OPERATION_RESAMPLE_FWD_IDXDESC = 1712,
    CUDNN_ATTR_OPERATION_RESAMPLE_FWD_ALPHA = 1713,
    CUDNN_ATTR_OPERATION_RESAMPLE_FWD_BETA = 1714,
    CUDNN_ATTR_OPERATION_RESAMPLE_FWD_DESC = 1716,
    CUDNN_ATTR_OPERATION_RESAMPLE_BWD_DXDESC = 1720,
    CUDNN_ATTR_OPERATION_RESAMPLE_BWD_DYDESC = 1721,
    CUDNN_ATTR_OPERATION_RESAMPLE_BWD_IDXDESC = 1722,
    CUDNN_ATTR_OPERATION_RESAMPLE_BWD_ALPHA = 1723,
    CUDNN_ATTR_OPERATION_RESAMPLE_BWD_BETA = 1724,
    CUDNN_ATTR_OPERATION_RESAMPLE_BWD_DESC = 1725,
    CUDNN_ATTR_OPERATION_RESAMPLE_BWD_XDESC = 1726,
    CUDNN_ATTR_OPERATION_RESAMPLE_BWD_YDESC = 1727,
    CUDNN_ATTR_OPERATION_CONCAT_AXIS = 1800,
    CUDNN_ATTR_OPERATION_CONCAT_INPUT_DESCS = 1801,
    CUDNN_ATTR_OPERATION_CONCAT_INPLACE_INDEX = 1802,
    CUDNN_ATTR_OPERATION_CONCAT_OUTPUT_DESC = 1803,
    CUDNN_ATTR_OPERATION_SIGNAL_MODE = 1900,
    CUDNN_ATTR_OPERATION_SIGNAL_FLAGDESC = 1901,
    CUDNN_ATTR_OPERATION_SIGNAL_VALUE = 1902,
    CUDNN_ATTR_OPERATION_SIGNAL_XDESC = 1903,
    CUDNN_ATTR_OPERATION_SIGNAL_YDESC = 1904,
    CUDNN_ATTR_OPERATION_PAGED_CACHE_LOAD_CONTAINER_DESC = 1950,
    CUDNN_ATTR_OPERATION_PAGED_CACHE_LOAD_YDESC = 1951,
    CUDNN_ATTR_OPERATION_PAGED_CACHE_LOAD_SEQUENCE_DESC = 1952,
    CUDNN_ATTR_OPERATION_PAGED_CACHE_LOAD_PAGE_TABLE_DESC = 1953,
    CUDNN_ATTR_OPERATION_NORM_FWD_MODE = 2000,
    CUDNN_ATTR_OPERATION_NORM_FWD_PHASE = 2001,
    CUDNN_ATTR_OPERATION_NORM_FWD_XDESC = 2002,
    CUDNN_ATTR_OPERATION_NORM_FWD_MEAN_DESC = 2003,
    CUDNN_ATTR_OPERATION_NORM_FWD_INV_VARIANCE_DESC = 2004,
    CUDNN_ATTR_OPERATION_NORM_FWD_SCALE_DESC = 2005,
    CUDNN_ATTR_OPERATION_NORM_FWD_BIAS_DESC = 2006,
    CUDNN_ATTR_OPERATION_NORM_FWD_EPSILON_DESC = 2007,
    CUDNN_ATTR_OPERATION_NORM_FWD_EXP_AVG_FACTOR_DESC = 2008,
    CUDNN_ATTR_OPERATION_NORM_FWD_INPUT_RUNNING_MEAN_DESC = 2009,
    CUDNN_ATTR_OPERATION_NORM_FWD_INPUT_RUNNING_VAR_DESC = 2010,
    CUDNN_ATTR_OPERATION_NORM_FWD_OUTPUT_RUNNING_MEAN_DESC = 2011,
    CUDNN_ATTR_OPERATION_NORM_FWD_OUTPUT_RUNNING_VAR_DESC = 2012,
    CUDNN_ATTR_OPERATION_NORM_FWD_YDESC = 2013,
    CUDNN_ATTR_OPERATION_NORM_FWD_PEER_STAT_DESCS = 2014,
    CUDNN_ATTR_OPERATION_NORM_BWD_MODE = 2100,
    CUDNN_ATTR_OPERATION_NORM_BWD_XDESC = 2101,
    CUDNN_ATTR_OPERATION_NORM_BWD_MEAN_DESC = 2102,
    CUDNN_ATTR_OPERATION_NORM_BWD_INV_VARIANCE_DESC = 2103,
    CUDNN_ATTR_OPERATION_NORM_BWD_DYDESC = 2104,
    CUDNN_ATTR_OPERATION_NORM_BWD_SCALE_DESC = 2105,
    CUDNN_ATTR_OPERATION_NORM_BWD_EPSILON_DESC = 2106,
    CUDNN_ATTR_OPERATION_NORM_BWD_DSCALE_DESC = 2107,
    CUDNN_ATTR_OPERATION_NORM_BWD_DBIAS_DESC = 2108,
    CUDNN_ATTR_OPERATION_NORM_BWD_DXDESC = 2109,
    CUDNN_ATTR_OPERATION_NORM_BWD_PEER_STAT_DESCS = 2110,
    CUDNN_ATTR_OPERATION_RESHAPE_XDESC = 2200,
    CUDNN_ATTR_OPERATION_RESHAPE_YDESC = 2201,
    CUDNN_ATTR_OPERATION_RESHAPE_MODE = 2202,
    CUDNN_ATTR_OPERATION_TRANSPOSE_XDESC = 3200,
    CUDNN_ATTR_OPERATION_TRANSPOSE_YDESC = 3201,
    CUDNN_ATTR_OPERATION_TRANSPOSE_PERMUTATION = 3202,
    CUDNN_ATTR_OPERATION_SLICE_XDESC = 3300,
    CUDNN_ATTR_OPERATION_SLICE_YDESC = 3301,
    CUDNN_ATTR_OPERATION_SLICE_START_INDICES = 3302,
    CUDNN_ATTR_OPERATION_SLICE_LIMIT_INDICES = 3303,
    CUDNN_ATTR_OPERATION_SLICE_STRIDES = 3304,
    CUDNN_ATTR_OPERATION_EXPAND_BAND_MATRIX_XDESC = 2250,
    CUDNN_ATTR_OPERATION_EXPAND_BAND_MATRIX_YDESC = 2251,
    CUDNN_ATTR_OPERATION_EXPAND_BAND_MATRIX_LOWER_BANDWIDTH = 2252,
    CUDNN_ATTR_OPERATION_EXPAND_BAND_MATRIX_UPPER_BANDWIDTH = 2253,
    CUDNN_ATTR_OPERATION_EXPAND_BAND_MATRIX_AXIS = 2254,
    CUDNN_ATTR_OPERATION_EXPAND_BAND_MATRIX_PAD_VALUE = 2255,
    CUDNN_ATTR_OPERATION_EXPAND_BAND_MATRIX_KV_TOKEN_OFFSET_DESC = 2256,
    CUDNN_ATTR_OPERATION_EXPAND_BAND_MATRIX_SPECULATIVE_MASK_DESC = 2257,
    CUDNN_ATTR_OPERATION_CONTRACT_BAND_MATRIX_XDESC = 2270,
    CUDNN_ATTR_OPERATION_CONTRACT_BAND_MATRIX_YDESC = 2271,
    CUDNN_ATTR_OPERATION_CONTRACT_BAND_MATRIX_LOWER_BANDWIDTH = 2272,
    CUDNN_ATTR_OPERATION_CONTRACT_BAND_MATRIX_UPPER_BANDWIDTH = 2273,
    CUDNN_ATTR_OPERATION_CONTRACT_BAND_MATRIX_AXIS = 2274,
    CUDNN_ATTR_OPERATION_CONTRACT_BAND_MATRIX_PAD_VALUE = 2275,
    CUDNN_ATTR_OPERATION_CONTRACT_BAND_MAX_TOKEN_VALUE = 2276,
    CUDNN_ATTR_RNG_DISTRIBUTION = 2300,
    CUDNN_ATTR_RNG_NORMAL_DIST_MEAN = 2301,
    CUDNN_ATTR_RNG_NORMAL_DIST_STANDARD_DEVIATION = 2302,
    CUDNN_ATTR_RNG_UNIFORM_DIST_MAXIMUM = 2303,
    CUDNN_ATTR_RNG_UNIFORM_DIST_MINIMUM = 2304,
    CUDNN_ATTR_RNG_BERNOULLI_DIST_PROBABILITY = 2305,
    CUDNN_ATTR_OPERATION_RNG_YDESC = 2310,
    CUDNN_ATTR_OPERATION_RNG_SEED = 2311,
    CUDNN_ATTR_OPERATION_RNG_DESC = 2312,
    CUDNN_ATTR_OPERATION_RNG_OFFSET_DESC = 2313,
    CUDNN_ATTR_KERNEL_CACHE_OPERATION_GRAPH = 2400,
    CUDNN_ATTR_KERNEL_CACHE_IS_ENGINECFG_KERNEL_CACHED = 2401,
    CUDNN_ATTR_KERNEL_CACHE_JSON_REPRESENTATION = 2402,
    CUDNN_ATTR_OPERATION_BLOCK_SCALE_QUANTIZE_XDESC = 2500,
    CUDNN_ATTR_OPERATION_BLOCK_SCALE_QUANTIZE_YDESC = 2501,
    CUDNN_ATTR_OPERATION_BLOCK_SCALE_QUANTIZE_SCALE_DESC = 2502,
    CUDNN_ATTR_OPERATION_BLOCK_SCALE_QUANTIZE_MATH_PREC = 2503,
    CUDNN_ATTR_OPERATION_BLOCK_SCALE_QUANTIZE_BLOCK_SIZE = 2504,
    CUDNN_ATTR_OPERATION_BLOCK_SCALE_DEQUANTIZE_XDESC = 2600,
    CUDNN_ATTR_OPERATION_BLOCK_SCALE_DEQUANTIZE_SCALE_DESC = 2601,
    CUDNN_ATTR_OPERATION_BLOCK_SCALE_DEQUANTIZE_YDESC = 2602,
    CUDNN_ATTR_OPERATION_BLOCK_SCALE_DEQUANTIZE_MATH_PREC = 2603,
    CUDNN_ATTR_OPERATION_BLOCK_SCALE_DEQUANTIZE_BLOCK_SIZE = 2604,
    CUDNN_ATTR_OPERATION_BLOCK_SCALE_DEQUANTIZE_NEG_SCALE = 2605,
    CUDNN_ATTR_DEVICEPROP_DEVICE_ID = 2700,
    CUDNN_ATTR_DEVICEPROP_HANDLE = 2701,
    CUDNN_ATTR_DEVICEPROP_JSON_REPRESENTATION = 2702,
    CUDNN_ATTR_OPERATION_SDPA_FWD_QDESC = 2800,
    CUDNN_ATTR_OPERATION_SDPA_FWD_KDESC = 2801,
    CUDNN_ATTR_OPERATION_SDPA_FWD_VDESC = 2802,
    CUDNN_ATTR_OPERATION_SDPA_FWD_ODESC = 2803,
    CUDNN_ATTR_OPERATION_SDPA_FWD_STATSDESC = 2804,
    CUDNN_ATTR_OPERATION_SDPA_FWD_SCALEDESC = 2805,
    CUDNN_ATTR_OPERATION_SDPA_FWD_BLOCK_MASK_DESC = 2806,
    CUDNN_ATTR_OPERATION_SDPA_FWD_PAGE_TABLE_KDESC = 2807,
    CUDNN_ATTR_OPERATION_SDPA_FWD_PAGE_TABLE_VDESC = 2808,
    CUDNN_ATTR_OPERATION_SDPA_FWD_SEQ_LEN_QDESC = 2809,
    CUDNN_ATTR_OPERATION_SDPA_FWD_SEQ_LEN_KVDESC = 2810,
    CUDNN_ATTR_OPERATION_SDPA_FWD_SUBGRAPH = 2811,
    CUDNN_ATTR_OPERATION_SDPA_FWD_SUBGRAPH_INPUT_UID = 2812,
    CUDNN_ATTR_OPERATION_SDPA_FWD_SUBGRAPH_OUTPUT_UID = 2813,
    CUDNN_ATTR_OPERATION_SDPA_FWD_SOFTMAX_DESC = 2814,
    CUDNN_ATTR_OPERATION_SDPA_FWD_DROPOUT_SEED_DESC = 2815,
    CUDNN_ATTR_OPERATION_SDPA_FWD_DROPOUT_OFFSET_DESC = 2816,
    CUDNN_ATTR_OPERATION_SDPA_FWD_DROPOUT_RNG_DUMP_DESC = 2817,
    CUDNN_ATTR_OPERATION_SDPA_FWD_DROPOUT_PROBABILITY = 2818,
    CUDNN_ATTR_OPERATION_SDPA_FWD_UNFUSE_FMA = 2819,
    CUDNN_ATTR_OPERATION_SDPA_BWD_QDESC = 2851,
    CUDNN_ATTR_OPERATION_SDPA_BWD_KDESC = 2852,
    CUDNN_ATTR_OPERATION_SDPA_BWD_VDESC = 2853,
    CUDNN_ATTR_OPERATION_SDPA_BWD_ODESC = 2854,
    CUDNN_ATTR_OPERATION_SDPA_BWD_STATSDESC = 2855,
    CUDNN_ATTR_OPERATION_SDPA_BWD_SCALEDESC = 2856,
    CUDNN_ATTR_OPERATION_SDPA_BWD_SEQ_LEN_QDESC = 2857,
    CUDNN_ATTR_OPERATION_SDPA_BWD_SEQ_LEN_KVDESC = 2858,
    CUDNN_ATTR_OPERATION_SDPA_BWD_DQDESC = 2859,
    CUDNN_ATTR_OPERATION_SDPA_BWD_DKDESC = 2860,
    CUDNN_ATTR_OPERATION_SDPA_BWD_DVDESC = 2861,
    CUDNN_ATTR_OPERATION_SDPA_BWD_DODDESC = 2862,
    CUDNN_ATTR_OPERATION_SDPA_BWD_SINK_DESC = 2863,
    CUDNN_ATTR_OPERATION_SDPA_BWD_DSINK_DESC = 2864,
    CUDNN_ATTR_OPERATION_SDPA_BWD_MAX_TOTAL_SEQ_LEN_Q = 2865,
    CUDNN_ATTR_OPERATION_SDPA_BWD_MAX_TOTAL_SEQ_LEN_KV = 2866,
    CUDNN_ATTR_OPERATION_SDPA_BWD_SUBGRAPH = 2867,
    CUDNN_ATTR_OPERATION_SDPA_BWD_SUBGRAPH_INPUT_UID = 2868,
    CUDNN_ATTR_OPERATION_SDPA_BWD_SUBGRAPH_OUTPUT_UID = 2869,
    CUDNN_ATTR_OPERATION_MOE_GROUPED_MATMUL_MODE = 2900,
    CUDNN_ATTR_OPERATION_MOE_GROUPED_MATMUL_MATH_PREC = 2901,
    CUDNN_ATTR_OPERATION_MOE_GROUPED_MATMUL_TOKEN_DESC = 2902,
    CUDNN_ATTR_OPERATION_MOE_GROUPED_MATMUL_WEIGHT_DESC = 2903,
    CUDNN_ATTR_OPERATION_MOE_GROUPED_MATMUL_FIRST_TOKEN_OFFSET_DESC = 2904,
    CUDNN_ATTR_OPERATION_MOE_GROUPED_MATMUL_OUTPUT_DESC = 2905,
    CUDNN_ATTR_OPERATION_MOE_GROUPED_MATMUL_TOKEN_INDEX_DESC = 2906,
    CUDNN_ATTR_OPERATION_MOE_GROUPED_MATMUL_TOKEN_KS_DESC = 2907,
    CUDNN_ATTR_OPERATION_MOE_GROUPED_MATMUL_TOP_K = 2908,
    CUDNN_ATTR_OPERATION_MOE_GROUPED_MATMUL_BWD_MATH_PREC = 2951,
    CUDNN_ATTR_OPERATION_MOE_GROUPED_MATMUL_BWD_TOKEN_DESC = 2952,
    CUDNN_ATTR_OPERATION_MOE_GROUPED_MATMUL_BWD_DWEIGHT_DESC = 2953,
    CUDNN_ATTR_OPERATION_MOE_GROUPED_MATMUL_BWD_FIRST_TOKEN_OFFSET_DESC = 2954,
    CUDNN_ATTR_OPERATION_MOE_GROUPED_MATMUL_BWD_DOUTPUT_DESC = 2955,
    CUDNN_ATTR_OPERATION_DIAGONAL_BAND_MASK_XDESC = 3000,
    CUDNN_ATTR_OPERATION_DIAGONAL_BAND_MASK_SEQ_LEN_KVDESC = 3001,
    CUDNN_ATTR_OPERATION_DIAGONAL_BAND_MASK_SEQ_LEN_QDESC = 3002,
    CUDNN_ATTR_OPERATION_DIAGONAL_BAND_MASK_LEFT_BOUND_DESC = 3003,
    CUDNN_ATTR_OPERATION_DIAGONAL_BAND_MASK_SHIFT_RIGHT_BOUND_DESC = 3004,
    CUDNN_ATTR_OPERATION_DIAGONAL_BAND_MASK_BDESC = 3005,
    CUDNN_ATTR_OPERATION_DIAGONAL_BAND_MASK_YDESC = 3006,
    CUDNN_ATTR_OPERATION_DIAGONAL_BAND_MASK_COMPARISON_MODE = 3007,
    CUDNN_ATTR_OPERATION_SOFTMAX_XDESC = 3100,
    CUDNN_ATTR_OPERATION_SOFTMAX_YDESC = 3101,
    CUDNN_ATTR_OPERATION_SOFTMAX_STATS_DESC = 3102,
    CUDNN_ATTR_OPERATION_SOFTMAX_MAX_DESC = 3103,
    CUDNN_ATTR_OPERATION_SOFTMAX_SUM_EXP_DESC = 3104,
    CUDNN_ATTR_OPERATION_SOFTMAX_SINK_DESC = 3105,
}
/// The enumeration type [`cudnnBackendAttributeType_t`] specifies the data type of an attribute of a cuDNN backend descriptor. It is used to specify the type of data pointed to by the void `*arrayOfElements` argument of [`cudnnBackendSetAttribute`] and [`cudnnBackendGetAttribute`].
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnBackendAttributeType_t {
    CUDNN_TYPE_HANDLE = 0,
    CUDNN_TYPE_DATA_TYPE = 1,
    CUDNN_TYPE_BOOLEAN = 2,
    CUDNN_TYPE_INT64 = 3,
    CUDNN_TYPE_FLOAT = 4,
    CUDNN_TYPE_DOUBLE = 5,
    CUDNN_TYPE_VOID_PTR = 6,
    CUDNN_TYPE_CONVOLUTION_MODE = 7,
    CUDNN_TYPE_HEUR_MODE = 8,
    CUDNN_TYPE_KNOB_TYPE = 9,
    CUDNN_TYPE_NAN_PROPOGATION = 10,
    CUDNN_TYPE_NUMERICAL_NOTE = 11,
    CUDNN_TYPE_LAYOUT_TYPE = 12,
    CUDNN_TYPE_ATTRIB_NAME = 13,
    CUDNN_TYPE_POINTWISE_MODE = 14,
    CUDNN_TYPE_BACKEND_DESCRIPTOR = 15,
    CUDNN_TYPE_GENSTATS_MODE = 16,
    CUDNN_TYPE_BN_FINALIZE_STATS_MODE = 17,
    CUDNN_TYPE_REDUCTION_OPERATOR_TYPE = 18,
    CUDNN_TYPE_BEHAVIOR_NOTE = 19,
    CUDNN_TYPE_TENSOR_REORDERING_MODE = 20,
    CUDNN_TYPE_RESAMPLE_MODE = 21,
    CUDNN_TYPE_PADDING_MODE = 22,
    CUDNN_TYPE_INT32 = 23,
    CUDNN_TYPE_CHAR = 24,
    CUDNN_TYPE_SIGNAL_MODE = 25,
    CUDNN_TYPE_FRACTION = 26,
    CUDNN_TYPE_NORM_MODE = 27,
    CUDNN_TYPE_NORM_FWD_PHASE = 28,
    CUDNN_TYPE_RNG_DISTRIBUTION = 29,
    CUDNN_TYPE_MOE_GROUPED_MATMUL_MODE = 30,
    CUDNN_TYPE_RESHAPE_MODE = 31,
}
/// [`cudnnBackendDescriptor_t`] is an enumerated type that indicates the type of backend descriptors. Users create a backend descriptor of a particular type by passing a value from this enumerate to the [`cudnnBackendCreateDescriptor`] function.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnBackendDescriptorType_t {
    CUDNN_BACKEND_POINTWISE_DESCRIPTOR = 0,
    CUDNN_BACKEND_CONVOLUTION_DESCRIPTOR = 1,
    CUDNN_BACKEND_ENGINE_DESCRIPTOR = 2,
    CUDNN_BACKEND_ENGINECFG_DESCRIPTOR = 3,
    CUDNN_BACKEND_ENGINEHEUR_DESCRIPTOR = 4,
    CUDNN_BACKEND_EXECUTION_PLAN_DESCRIPTOR = 5,
    CUDNN_BACKEND_INTERMEDIATE_INFO_DESCRIPTOR = 6,
    CUDNN_BACKEND_KNOB_CHOICE_DESCRIPTOR = 7,
    CUDNN_BACKEND_KNOB_INFO_DESCRIPTOR = 8,
    CUDNN_BACKEND_LAYOUT_INFO_DESCRIPTOR = 9,
    CUDNN_BACKEND_OPERATION_CONVOLUTION_FORWARD_DESCRIPTOR = 10,
    CUDNN_BACKEND_OPERATION_CONVOLUTION_BACKWARD_FILTER_DESCRIPTOR = 11,
    CUDNN_BACKEND_OPERATION_CONVOLUTION_BACKWARD_DATA_DESCRIPTOR = 12,
    CUDNN_BACKEND_OPERATION_POINTWISE_DESCRIPTOR = 13,
    CUDNN_BACKEND_OPERATION_GEN_STATS_DESCRIPTOR = 14,
    CUDNN_BACKEND_OPERATIONGRAPH_DESCRIPTOR = 15,
    CUDNN_BACKEND_VARIANT_PACK_DESCRIPTOR = 16,
    CUDNN_BACKEND_TENSOR_DESCRIPTOR = 17,
    CUDNN_BACKEND_MATMUL_DESCRIPTOR = 18,
    CUDNN_BACKEND_OPERATION_MATMUL_DESCRIPTOR = 19,
    CUDNN_BACKEND_OPERATION_BN_FINALIZE_STATISTICS_DESCRIPTOR = 20,
    CUDNN_BACKEND_REDUCTION_DESCRIPTOR = 21,
    CUDNN_BACKEND_OPERATION_REDUCTION_DESCRIPTOR = 22,
    CUDNN_BACKEND_OPERATION_BN_BWD_WEIGHTS_DESCRIPTOR = 23,
    CUDNN_BACKEND_RESAMPLE_DESCRIPTOR = 24,
    CUDNN_BACKEND_OPERATION_RESAMPLE_FWD_DESCRIPTOR = 25,
    CUDNN_BACKEND_OPERATION_RESAMPLE_BWD_DESCRIPTOR = 26,
    CUDNN_BACKEND_OPERATION_CONCAT_DESCRIPTOR = 27,
    CUDNN_BACKEND_OPERATION_SIGNAL_DESCRIPTOR = 28,
    CUDNN_BACKEND_OPERATION_NORM_FORWARD_DESCRIPTOR = 29,
    CUDNN_BACKEND_OPERATION_NORM_BACKWARD_DESCRIPTOR = 30,
    CUDNN_BACKEND_OPERATION_RESHAPE_DESCRIPTOR = 31,
    CUDNN_BACKEND_RNG_DESCRIPTOR = 32,
    CUDNN_BACKEND_OPERATION_RNG_DESCRIPTOR = 33,
    CUDNN_BACKEND_KERNEL_CACHE_DESCRIPTOR = 34,
    CUDNN_BACKEND_OPERATION_PAGED_CACHE_LOAD_DESCRIPTOR = 35,
    CUDNN_BACKEND_OPERATION_BLOCK_SCALE_QUANTIZE_DESCRIPTOR = 36,
    CUDNN_BACKEND_OPERATION_BLOCK_SCALE_DEQUANTIZE_DESCRIPTOR = 37,
    CUDNN_BACKEND_DEVICEPROP_DESCRIPTOR = 38,
    CUDNN_BACKEND_OPERATION_EXPAND_BAND_MATRIX_DESCRIPTOR = 39,
    CUDNN_BACKEND_OPERATION_CONTRACT_BAND_MATRIX_DESCRIPTOR = 40,
    CUDNN_BACKEND_OPERATION_SDPA_FWD_DESCRIPTOR = 41,
    CUDNN_BACKEND_OPERATION_MOE_GROUPED_MATMUL_DESCRIPTOR = 42,
    CUDNN_BACKEND_OPERATION_SDPA_BWD_DESCRIPTOR = 43,
    CUDNN_BACKEND_OPERATION_DIAGONAL_BAND_MASK_DESCRIPTOR = 44,
    CUDNN_BACKEND_OPERATION_SOFTMAX_DESCRIPTOR = 45,
    CUDNN_BACKEND_OPERATION_TRANSPOSE_DESCRIPTOR = 46,
    CUDNN_BACKEND_OPERATION_SLICE_DESCRIPTOR = 47,
    CUDNN_BACKEND_OPERATION_MOE_GROUPED_MATMUL_BWD_DESCRIPTOR = 48,
}
/// `cudnnBackendNumericalNot_t` is an enumerated type that indicates queryable numerical properties of an engine. Users can query for an array of numerical notes from an [`cudnnBackendDescriptorType_t::CUDNN_BACKEND_ENGINE_DESCRIPTOR`] using the [`cudnnBackendGetAttribute`] function.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnBackendNumericalNote_t {
    CUDNN_NUMERICAL_NOTE_TENSOR_CORE = 0,
    CUDNN_NUMERICAL_NOTE_DOWN_CONVERT_INPUTS = 1,
    CUDNN_NUMERICAL_NOTE_REDUCED_PRECISION_REDUCTION = 2,
    CUDNN_NUMERICAL_NOTE_FFT = 3,
    CUDNN_NUMERICAL_NOTE_NONDETERMINISTIC = 4,
    CUDNN_NUMERICAL_NOTE_WINOGRAD = 5,
    CUDNN_NUMERICAL_NOTE_WINOGRAD_TILE_4x4 = 6,
    CUDNN_NUMERICAL_NOTE_WINOGRAD_TILE_6x6 = 7,
    CUDNN_NUMERICAL_NOTE_WINOGRAD_TILE_13x13 = 8,
    CUDNN_NUMERICAL_NOTE_STRICT_NAN_PROP = 9,
    CUDNN_NUMERICAL_NOTE_TYPE_COUNT = 10,
}
/// [`cudnnBackendBehaviorNote_t`] is an enumerated type that indicates queryable behavior notes of an engine. Users can query for an array of behavior notes from an [`cudnnBackendDescriptorType_t::CUDNN_BACKEND_ENGINE_DESCRIPTOR`] using the [`cudnnBackendGetAttribute`] function.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnBackendBehaviorNote_t {
    CUDNN_BEHAVIOR_NOTE_RUNTIME_COMPILATION = 0,
    CUDNN_BEHAVIOR_NOTE_REQUIRES_FILTER_INT8x32_REORDER = 1,
    CUDNN_BEHAVIOR_NOTE_REQUIRES_BIAS_INT8x32_REORDER = 2,
    CUDNN_BEHAVIOR_NOTE_SUPPORTS_CUDA_GRAPH_NATIVE_API = 3,
    CUDNN_BEHAVIOR_NOTE_CUBLASLT_DEPENDENCY = 4,
    CUDNN_BEHAVIOR_NOTE_TYPE_COUNT = 5,
}
/// [`cudnnBackendKnobType_t`] is an enumerated type that indicates the type of performance knobs. Performance knobs are runtime settings to an engine that will affect its performance. Users can query for an array of performance knobs and their valid value range from a [`cudnnBackendDescriptorType_t::CUDNN_BACKEND_ENGINE_DESCRIPTOR`] using the [`cudnnBackendGetAttribute`] function. Users can set the choice for each knob using the [`cudnnBackendSetAttribute`] function with a [`cudnnBackendDescriptorType_t::CUDNN_BACKEND_KNOB_CHOICE_DESCRIPTOR`].
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnBackendKnobType_t {
    CUDNN_KNOB_TYPE_SPLIT_K = 0,
    CUDNN_KNOB_TYPE_SWIZZLE = 1,
    CUDNN_KNOB_TYPE_TILE_SIZE = 2,
    CUDNN_KNOB_TYPE_USE_TEX = 3,
    CUDNN_KNOB_TYPE_EDGE = 4,
    CUDNN_KNOB_TYPE_KBLOCK = 5,
    CUDNN_KNOB_TYPE_LDGA = 6,
    CUDNN_KNOB_TYPE_LDGB = 7,
    CUDNN_KNOB_TYPE_CHUNK_K = 8,
    CUDNN_KNOB_TYPE_SPLIT_H = 9,
    CUDNN_KNOB_TYPE_WINO_TILE = 10,
    CUDNN_KNOB_TYPE_MULTIPLY = 11,
    CUDNN_KNOB_TYPE_SPLIT_K_BUF = 12,
    CUDNN_KNOB_TYPE_TILEK = 13,
    CUDNN_KNOB_TYPE_STAGES = 14,
    CUDNN_KNOB_TYPE_REDUCTION_MODE = 15,
    CUDNN_KNOB_TYPE_CTA_SPLIT_K_MODE = 16,
    CUDNN_KNOB_TYPE_SPLIT_K_SLC = 17,
    CUDNN_KNOB_TYPE_IDX_MODE = 18,
    CUDNN_KNOB_TYPE_SLICED = 19,
    CUDNN_KNOB_TYPE_SPLIT_RS = 20,
    CUDNN_KNOB_TYPE_SINGLEBUFFER = 21,
    CUDNN_KNOB_TYPE_LDGC = 22,
    CUDNN_KNOB_TYPE_SPECFILT = 23,
    CUDNN_KNOB_TYPE_KERNEL_CFG = 24,
    CUDNN_KNOB_TYPE_WORKSPACE = 25,
    #[deprecated]
    CUDNN_KNOB_TYPE_TILE_CGA = 26,
    CUDNN_KNOB_TYPE_TILE_CGA_M = 27,
    CUDNN_KNOB_TYPE_TILE_CGA_N = 28,
    CUDNN_KNOB_TYPE_BLOCK_SIZE = 29,
    CUDNN_KNOB_TYPE_OCCUPANCY = 30,
    CUDNN_KNOB_TYPE_ARRAY_SIZE_PER_THREAD = 31,
    #[deprecated]
    CUDNN_KNOB_TYPE_NUM_C_PER_BLOCK = 32,
    CUDNN_KNOB_TYPE_SPLIT_COLS = 33,
    CUDNN_KNOB_TYPE_TILE_ROWS = 34,
    CUDNN_KNOB_TYPE_TILE_COLS = 35,
    CUDNN_KNOB_TYPE_LOAD_SIZE = 36,
    CUDNN_KNOB_TYPE_CTA_COUNT = 37,
    CUDNN_KNOB_TYPE_STREAM_K = 38,
    CUDNN_KNOB_TYPE_SPLIT_P_SLC = 39,
    CUDNN_KNOB_TYPE_TILE_M = 40,
    CUDNN_KNOB_TYPE_TILE_N = 41,
    CUDNN_KNOB_TYPE_WARP_SPEC_CFG = 42,
    CUDNN_KNOB_TYPE_SWAP_AB = 43,
    CUDNN_KNOB_TYPE_INPUT_TMA_ENABLE = 44,
    CUDNN_KNOB_TYPE_OUTPUT_TMA_ENABLE = 45,
    CUDNN_KNOB_TYPE_COUNTS = 46,
}
/// [`cudnnBackendLayoutType_t`] is an enumerated type that indicates queryable layout requirements of an engine. Users can query for layout requirements from a [`cudnnBackendDescriptorType_t::CUDNN_BACKEND_ENGINE_DESCRIPTOR`] using the [`cudnnBackendGetAttribute`] function.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnBackendLayoutType_t {
    CUDNN_LAYOUT_TYPE_PREFERRED_NCHW = 0,
    CUDNN_LAYOUT_TYPE_PREFERRED_NHWC = 1,
    CUDNN_LAYOUT_TYPE_PREFERRED_PAD4CK = 2,
    CUDNN_LAYOUT_TYPE_PREFERRED_PAD8CK = 3,
    CUDNN_LAYOUT_TYPE_COUNT = 4,
}
/// [`cudnnBackendHeurMode_t`] is an enumerated type that indicates the operation mode of a [`cudnnBackendDescriptorType_t::CUDNN_BACKEND_ENGINEHEUR_DESCRIPTOR`].
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnBackendHeurMode_t {
    CUDNN_HEUR_MODE_INSTANT = 0,
    /// Can utilize the neural net based heuristics to improve generalization performance compared to [`cudnnBackendHeurMode_t::CUDNN_HEUR_MODE_INSTANT`].
    ///
    /// In cases where the neural net is utilized, inference time on the CPU will be increased by 10-100x compared to [`cudnnBackendHeurMode_t::CUDNN_HEUR_MODE_INSTANT`]. These neural net heuristics are not supported for any of the following cases:
    ///
    /// * 3-D convolutions
    /// * Grouped convolutions (`groupCount` larger than `1`)
    /// * Dilated convolutions (any dilation for any spatial dimension larger than `1`)
    ///
    /// Further, the neural net is only enabled on x86 platforms when cuDNN is run on an A100 GPU. In cases where the neural net is not supported, [`cudnnBackendHeurMode_t::CUDNN_HEUR_MODE_B`] will fall back to [`cudnnBackendHeurMode_t::CUDNN_HEUR_MODE_INSTANT`]. [`cudnnBackendHeurMode_t::CUDNN_HEUR_MODE_B`] will also fall back to [`cudnnBackendHeurMode_t::CUDNN_HEUR_MODE_INSTANT`] in cases where the overhead of [`cudnnBackendHeurMode_t::CUDNN_HEUR_MODE_B`] is projected to reduce overall network performance.
    CUDNN_HEUR_MODE_B = 1,
    /// This heuristic mode is intended to be used for finding fallback options which provide functional support (without any expectation of providing optimal GPU performance).
    CUDNN_HEUR_MODE_FALLBACK = 2,
    CUDNN_HEUR_MODE_A = 3,
    CUDNN_HEUR_MODES_COUNT = 4,
}
/// [`cudnnBackendTensorReordering_t`] is an enumerated type that indicates tensor reordering as a property of the tensor descriptor. Users can get and set this property in a [`cudnnBackendDescriptorType_t::CUDNN_BACKEND_TENSOR_DESCRIPTOR`] using the [`cudnnBackendSetAttribute`] and [`cudnnBackendGetAttribute`] functions.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnBackendTensorReordering_t {
    CUDNN_TENSOR_REORDERING_NONE = 0,
    CUDNN_TENSOR_REORDERING_INT8x32 = 1,
    CUDNN_TENSOR_REORDERING_F16x16 = 2,
    CUDNN_TENSOR_REORDERING_F8_128x4 = 3,
}
/// [`cudnnPaddingMode_t`] is an enumerated type to indicate the padding mode in the backend resample operations.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnPaddingMode_t {
    CUDNN_ZERO_PAD = 0,
    CUDNN_NEG_INF_PAD = 1,
    CUDNN_EDGE_VAL_PAD = 2,
}
/// [`cudnnBackendNormMode_t`] is an enumerated type to indicate the normalization mode in the backend normalization forward and normalization backward operations.
///
/// For reference:
///
/// [`cudnnBackendNormMode_t::CUDNN_GROUP_NORM`] is not yet supported. If you try to use it, cuDNN returns a [`cudnnStatus_t::CUDNN_STATUS_INTERNAL_ERROR`] error.
#[repr(u32)]
#[derive(Debug, Copy, Clone, Hash, PartialOrd, Ord, PartialEq, Eq)]
pub enum cudnnBackendNormMode_t {
    CUDNN_LAYER_NORM = 0,
    CUDNN_INSTANCE_NORM = 1,
    CUDNN_BATCH_NORM = 2,
    CUDNN_GROUP_NORM = 3,
    CUDNN_RMS_NORM = 4,
    CUDNN_ADA_LAYER_NORM = 5,
}
/// [`cudnnBackendNormFwdPhase_t`] is an enumerated type used to distinguish the inference and training phase of the normalization forward operation.
#[repr(u32)]
#[derive(Debug, Copy, Clone, Hash, PartialOrd, Ord, PartialEq, Eq)]
pub enum cudnnBackendNormFwdPhase_t {
    CUDNN_NORM_FWD_INFERENCE = 0,
    CUDNN_NORM_FWD_TRAINING = 1,
}
#[repr(u32)]
#[derive(Debug, Copy, Clone, Hash, PartialOrd, Ord, PartialEq, Eq)]
pub enum cudnnBackendReshapeMode_t {
    CUDNN_RESHAPE_VIEW_ONLY = 0,
    CUDNN_RESHAPE_LOGICAL = 1,
}
unsafe extern "C" {
    /// This function allocates memory in the `descriptor` for a given `descriptor` type and at the location pointed by the `descriptor`.
    ///
    /// # Parameters
    ///
    /// - `descriptorType`: Input. One among the enumerated [`cudnnBackendDescriptorType_t`].
    /// - `descriptor`: Input. Pointer to an instance of [`cudnnBackendDescriptor_t`] to be created.
    ///
    /// # Return value
    ///
    /// - `CUDNN_STATUS_ALLOC_FAILED`: The memory allocation failed.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: Creating a descriptor of a given type is not supported.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The creation was successful.
    pub fn cudnnBackendCreateDescriptor(
        descriptorType: cudnnBackendDescriptorType_t,
        descriptor: *mut cudnnBackendDescriptor_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function destroys instances of [`cudnnBackendDescriptor_t`] that were previously created using [`cudnnBackendCreateDescriptor`].
    ///
    /// # Parameters
    ///
    /// - `descriptor`: Input. Instance of [`cudnnBackendDescriptor_t`] previously created by [`cudnnBackendCreateDescriptor`].
    ///
    /// # Return value
    ///
    /// - `CUDNN_STATUS_ALLOC_FAILED`: The destruction of memory failed.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The memory was destroyed successfully.
    /// - `Undefined`: The value pointed by the `descriptor` will be `Undefined` after the memory is free and done.
    /// - `Undefined Behavior`: The `descriptor` was altered between the `Create` and `Destroy Descriptor`.
    pub fn cudnnBackendDestroyDescriptor(
        descriptor: cudnnBackendDescriptor_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function repurposes a pre-allocated memory pointed to by a `descriptor` of size `sizeInByte` to a backend `descriptor` of type `descriptorType`. The finalized state of the `descriptor` is set to `false`.
    ///
    /// # Parameters
    ///
    /// - `descriptor`: Input. Instance of [`cudnnBackendDescriptor_t`] to be initialized.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: An invalid or inconsistent argument value is encountered. Some examples include:
    ///
    /// * `descriptor` is a `nullptr`
    /// * `sizeInBytes` is less than the size required by the `descriptor` type.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The memory was initialized successfully.
    #[deprecated]
    pub fn cudnnBackendInitialize(descriptor: cudnnBackendDescriptor_t) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function finalizes the memory pointed to by the `descriptor`. The type of finalization is done depending on the `descriptorType` argument with which the `descriptor` was created using [`cudnnBackendCreateDescriptor`] or initialized using [`cudnnBackendInitialize`].
    ///
    /// [`cudnnBackendFinalize`] also checks all the attributes set between the create/initialization and finalize phase. If successful, [`cudnnBackendFinalize`] returns [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`] and the finalized state of the `descriptor` is set to `true`. In this state, setting attributes using [`cudnnBackendSetAttribute`] is not allowed. Getting attributes using [`cudnnBackendGetAttribute`] is only allowed when the finalized state of the `descriptor` is `true`.
    ///
    /// # Parameters
    ///
    /// - `descriptor`: Input. Instance of [`cudnnBackendDescriptor_t`] to finalize.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: Invalid `descriptor` attribute values or combination thereof is encountered.
    /// - [`cudnnStatus_t::CUDNN_STATUS_INTERNAL_ERROR`]: Some internal errors are encountered.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: Descriptor attribute values or combinations therefore not supported by the current version of cuDNN are encountered.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The `descriptor` was finalized successfully.
    pub fn cudnnBackendFinalize(descriptor: cudnnBackendDescriptor_t) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function sets an attribute of a `descriptor` to values provided as a pointer. `descriptor` is the `descriptor` to be set. `attributeName` is the name of the attribute to be set. `attributeType` is the type of attribute. The value to which the attribute is set, is pointed by the `arrayOfElements`. The number of elements is given by `elementCount`. This function will return `CUDNN_STATUS_NOT_INTIALIZED` if the `descriptor` is already successfully finalized using [`cudnnBackendFinalize`].
    ///
    /// # Parameters
    ///
    /// - `descriptor`: Input. Instance of [`cudnnBackendDescriptor_t`] whose attribute is being set.
    /// - `attributeName`: Input. The name of the attribute being set on the `descriptor`.
    /// - `attributeType`: Input. The type of attribute.
    /// - `elementCount`: Input. Number of elements being set.
    /// - `arrayOfElements`: Input. The starting location for an array from where to read the values from. The elements of the array are expected to be of the datatype of the `attributeType`. The datatype of the `attributeType` is listed in the mapping table of [`cudnnBackendAttributeType_t`].
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: The function is called with arguments that correspond to invalid values. Some examples include:
    ///
    /// * `attributeName` is not a settable attribute of `descriptor`.
    /// * `attributeType` is incorrect for this `attributeName`.
    /// * `elemCount` value is unexpected.
    /// * `arrayOfElements` contains values invalid for the `attributeType`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_INITIALIZED`]: The backend `descriptor` pointed to by the `descriptor` is already in the finalized state.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: The values to which the attributes are being set are not supported by the current version of cuDNN.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The `attributeName` was set to the `descriptor`.
    pub fn cudnnBackendSetAttribute(
        descriptor: cudnnBackendDescriptor_t,
        attributeName: cudnnBackendAttributeName_t,
        attributeType: cudnnBackendAttributeType_t,
        elementCount: i64,
        arrayOfElements: *const ::core::ffi::c_void,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function retrieves the values of an attribute of a `descriptor`. `attributeName` is the name of the attribute whose value is requested. `attributeType` is the type of attribute. `requestsedElementCount` is the number of elements to be potentially retrieved. The number of elements for the requested attribute is stored in `elementCount`. The retrieved values are stored in `arrayOfElements`. When the attribute is expected to have a single value, `arrayOfElements` can be pointer to the output value. This function will return `CUDNN_STATUS_NOT_INTIALIZED` if the `descriptor` has not been successfully finalized using [`cudnnBackendFinalize`].
    ///
    /// # Parameters
    ///
    /// - `descriptor`: Input. Instance of [`cudnnBackendDescriptor_t`] whose attribute the user wants to retrieve.
    /// - `attributeName`: Input. The name of the attribute being get from the on the `descriptor`.
    /// - `attributeType`: Input. The type of attribute.
    /// - `requestedElementCount`: Input. Number of elements to output to `arrayOfElements`.
    /// - `elementCount`: Input. Output pointer for the number of elements the `descriptor` attribute has. Note that [`cudnnBackendGetAttribute`] will only write the least of this and `requestedElementCount` elements to `arrayOfElements`.
    /// - `arrayOfElements`: Input. Array of elements of the datatype of the `attributeType`. The data type of the `attributeType` is listed in the mapping table of [`cudnnBackendAttributeType_t`].
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: One or more invalid or inconsistent argument values were encountered. Some examples include:
    ///
    /// * `attributeName` is not a valid attribute for the descriptor.
    /// * `attributeType` is not one of the valid types for the attribute.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_INITIALIZED`]: The `descriptor` has not been successfully finalized using [`cudnnBackendFinalize`].
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The `attributeName` was given to the `descriptor` successfully.
    pub fn cudnnBackendGetAttribute(
        descriptor: cudnnBackendDescriptor_t,
        attributeName: cudnnBackendAttributeName_t,
        attributeType: cudnnBackendAttributeType_t,
        requestedElementCount: i64,
        elementCount: *mut i64,
        arrayOfElements: *mut ::core::ffi::c_void,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function executes the given `Engine Configuration Plan` on the `VariantPack` and the finalized `ExecutionPlan` on the data. The data and the working space are encapsulated in the `VariantPack`.
    ///
    /// # Parameters
    ///
    /// - `executionPlan`: Input. Pointer to the finalized `ExecutionPlan`.
    /// - `variantPack`: Input. Pointer to the finalized `VariantPack` consisting of:
    ///
    /// * Data pointer for each non-virtual pointer of the operation set in the execution plan.
    /// * Pointer to user-allocated workspace in global memory at least as large as the size queried from `CUDNN_BACKEND_`.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: An incorrect or inconsistent value is encountered. For example, a required data pointer is invalid.
    /// - [`cudnnStatus_t::CUDNN_STATUS_EXECUTION_FAILED`]: An error was encountered executing the plan with the variant pack.
    /// - [`cudnnStatus_t::CUDNN_STATUS_INTERNAL_ERROR`]: Some internal errors were encountered.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The `ExecutionPlan` was executed successfully.
    pub fn cudnnBackendExecute(
        handle: cudnnHandle_t,
        executionPlan: cudnnBackendDescriptor_t,
        variantPack: cudnnBackendDescriptor_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This method, part of the new [Native CUDA Graph API](https://docs.nvidia.com/deeplearning/cudnn//latest/developer/misc.html#native-cuda-graph-api "(in NVIDIA cuDNN)"), directly builds a CUDA graph (not to be confused with a cuDNN graph) representing the given engine. When the caller instantiates and executes this CUDA graph, the graph will execute the engine configuration plan on the `VariantPack` and the finalized `ExecutionPlan` on the data.
    ///
    /// The resulting CUDA graph captures the pointers (data and working space) in the `VariantPack` at the time this API is called, but it can be run arbitrarily many times with different data at the same pointers. The graph can also later be modified in place with different `VariantPack` pointers by using [`cudnnBackendUpdateCudaGraph`].
    ///
    /// The initial CUDA graph passed in to this API must be empty (having no nodes), and the caller should not append additional nodes to the resulting graph. However, the graph can be embedded as a child node of a larger CUDA graph, for example, by `cudaGraphAddChildGraphNode`. (This is typical usage.)
    ///
    /// Only a limited number of engines currently support this API (with more to be added in future releases of cuDNN). Those supporting it have the behavior note [`cudnnBackendBehaviorNote_t::CUDNN_BEHAVIOR_NOTE_SUPPORTS_CUDA_GRAPH_NATIVE_API`].
    ///
    /// # Parameters
    ///
    /// - `executionPlan`: Input. Pointer to the finalized `ExecutionPlan`.
    /// - `variantPack`: Input. Pointer to the finalized VariantPack consisting of:
    ///
    /// * Data pointer for each non-virtual pointer of the operation set in the execution plan.
    /// * Pointer to user-allocated workspace in global memory at least as large as the size queried from `CUDNN_BACKEND_`.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: An incorrect or inconsistent value is encountered. For example, a required data pointer is invalid, or the given `cudaGraph` isn’t initially empty.
    /// - [`cudnnStatus_t::CUDNN_STATUS_EXECUTION_FAILED`]: An error was encountered creating a CUDA graph for the plan with the variant pack.
    /// - [`cudnnStatus_t::CUDNN_STATUS_INTERNAL_ERROR`]: Some internal errors were encountered.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED_CUDA_GRAPH_NATIVE_API`]: This particular engine does not support the native CUDA graph API.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED_INCOMPATIBLE_CUDART`]: This cuDNN was built to a CUDA runtime less than 12.0, and doesn’t support the native CUDA graph API.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The CUDA graph was generated successfully.
    pub fn cudnnBackendPopulateCudaGraph(
        handle: cudnnHandle_t,
        executionPlan: cudnnBackendDescriptor_t,
        variantPack: cudnnBackendDescriptor_t,
        graph: cudaGraph_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This method, part of the new [Native CUDA Graph API](https://docs.nvidia.com/deeplearning/cudnn//latest/developer/misc.html#native-cuda-graph-api "(in NVIDIA cuDNN)"), updates an existing CUDA graph previously populated by [`cudnnBackendPopulateCudaGraph`] (or a clone thereof) with a new `variantPack`.
    ///
    /// Only a limited number of engines currently support this API (with more to be added in future releases of cuDNN). Those supporting it have the behavior note [`cudnnBackendBehaviorNote_t::CUDNN_BEHAVIOR_NOTE_SUPPORTS_CUDA_GRAPH_NATIVE_API`].
    ///
    /// # Parameters
    ///
    /// - `executionPlan`: Input. Pointer to the finalized `ExecutionPlan`. This must match the `ExecutionPlan` originally passed to [`cudnnBackendPopulateCudaGraph`].
    /// - `variantPack`: Input. Pointer to a finalized `VariantPack` consisting of the following pointers, which replace the `VariantPack` pointers captured in the CUDA graph:
    ///
    /// * Data pointer for each non-virtual pointer of the operation set in the execution plan.
    /// * Pointer to a user-allocated workspace in global memory at least as large as the size queried from `CUDNN_BACKEND_`.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: An incorrect or inconsistent value is encountered. For example, a required data pointer is invalid.
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM_CUDA_GRAPH_MISMATCH`]: The CUDA graph doesn’t appear to have been populated by [`cudnnBackendPopulateCudaGraph`], (or a clone thereof) for this execution plan, or contains unexpected additional nodes.
    /// - [`cudnnStatus_t::CUDNN_STATUS_EXECUTION_FAILED`]: An error was encountered updating a CUDA graph for the plan with the variant pack.
    /// - [`cudnnStatus_t::CUDNN_STATUS_INTERNAL_ERROR`]: Some internal errors were encountered.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED_CUDA_GRAPH_NATIVE_API`]: This particular engine does not support the native CUDA graph API.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED_INCOMPATIBLE_CUDART`]: This cuDNN was built to a CUDA runtime less than 12.0, and doesn’t support the native CUDA graph API.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The CUDA graph was updated successfully.
    pub fn cudnnBackendUpdateCudaGraph(
        handle: cudnnHandle_t,
        executionPlan: cudnnBackendDescriptor_t,
        variantPack: cudnnBackendDescriptor_t,
        graph: cudaGraph_t,
    ) -> cudnnStatus_t;
}
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct cudnnTensorStruct {
    _unused: [u8; 0],
}
/// [`cudnnTensorDescriptor_t`] is a pointer to an opaque structure holding the description of a generic n-D dataset. [`cudnnCreateTensorDescriptor`] is used to create one instance, and one of the routines [`cudnnSetTensorNdDescriptor`], [`cudnnSetTensor4dDescriptor`], or [`cudnnSetTensor4dDescriptorEx`] must be used to initialize this instance.
pub type cudnnTensorDescriptor_t = *mut cudnnTensorStruct;
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct cudnnPoolingStruct {
    _unused: [u8; 0],
}
/// [`cudnnPoolingDescriptor_t`] is a pointer to an opaque structure holding the description of a pooling operation. [`cudnnCreatePoolingDescriptor`] is used to create one instance, and [`cudnnSetPoolingNdDescriptor`] or [`cudnnSetPooling2dDescriptor`] must be used to initialize this instance.
#[deprecated]
pub type cudnnPoolingDescriptor_t = *mut cudnnPoolingStruct;
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct cudnnFilterStruct {
    _unused: [u8; 0],
}
/// [`cudnnFilterDescriptor_t`] is a pointer to an opaque structure holding the description of a filter dataset. [`cudnnCreateFilterDescriptor`] is used to create one instance, and [`cudnnSetFilter4dDescriptor`] or [`cudnnSetFilterNdDescriptor`] must be used to initialize this instance.
#[deprecated]
pub type cudnnFilterDescriptor_t = *mut cudnnFilterStruct;
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct cudnnLRNStruct {
    _unused: [u8; 0],
}
/// [`cudnnLRNDescriptor_t`] is a pointer to an opaque structure holding the parameters of a local response normalization. [`cudnnCreateLRNDescriptor`] is used to create one instance, and the routine [`cudnnSetLRNDescriptor`] must be used to initialize this instance.
pub type cudnnLRNDescriptor_t = *mut cudnnLRNStruct;
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct cudnnActivationStruct {
    _unused: [u8; 0],
}
/// [`cudnnActivationDescriptor_t`] is a pointer to an opaque structure holding the description of an activation operation. [`cudnnCreateActivationDescriptor`] is used to create one instance, and [`cudnnSetActivationDescriptor`] must be used to initialize this instance.
#[deprecated]
pub type cudnnActivationDescriptor_t = *mut cudnnActivationStruct;
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct cudnnSpatialTransformerStruct {
    _unused: [u8; 0],
}
/// [`cudnnSpatialTransformerDescriptor_t`] is a pointer to an opaque structure holding the description of a spatial transformation operation. [`cudnnCreateSpatialTransformerDescriptor`] is used to create one instance, [`cudnnSetSpatialTransformerNdDescriptor`] is used to initialize this instance, and [`cudnnDestroySpatialTransformerDescriptor`] is used to destroy this instance.
pub type cudnnSpatialTransformerDescriptor_t = *mut cudnnSpatialTransformerStruct;
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct cudnnOpTensorStruct {
    _unused: [u8; 0],
}
/// [`cudnnOpTensorDescriptor_t`] is a pointer to an opaque structure holding the description of a Tensor Core operation, used as a parameter to [`cudnnOpTensor`]. [`cudnnCreateOpTensorDescriptor`] is used to create one instance, and [`cudnnSetOpTensorDescriptor`] must be used to initialize this instance.
#[deprecated]
pub type cudnnOpTensorDescriptor_t = *mut cudnnOpTensorStruct;
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct cudnnReduceTensorStruct {
    _unused: [u8; 0],
}
/// [`cudnnReduceTensorDescriptor_t`] is a pointer to an opaque structure holding the description of a tensor reduction operation, used as a parameter to [`cudnnReduceTensor`]. [`cudnnCreateReduceTensorDescriptor`] is used to create one instance, and [`cudnnSetReduceTensorDescriptor`] must be used to initialize this instance.
#[deprecated]
pub type cudnnReduceTensorDescriptor_t = *mut cudnnReduceTensorStruct;
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct cudnnCTCLossStruct {
    _unused: [u8; 0],
}
/// [`cudnnCTCLossDescriptor_t`] is a pointer to an opaque structure holding the description of a CTC loss operation. [`cudnnCreateCTCLossDescriptor`] is used to create one instance, [`cudnnSetCTCLossDescriptor`] is used to initialize this instance, and [`cudnnDestroyCTCLossDescriptor`] is used to destroy this instance.
pub type cudnnCTCLossDescriptor_t = *mut cudnnCTCLossStruct;
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct cudnnTensorTransformStruct {
    _unused: [u8; 0],
}
/// [`cudnnTensorTransformDescriptor_t`] is an opaque structure containing the description of the tensor transform. Use the [`cudnnCreateTensorTransformDescriptor`] function to create an instance of this descriptor, and [`cudnnDestroyTensorTransformDescriptor`] function to destroy a previously created instance.
#[deprecated]
pub type cudnnTensorTransformDescriptor_t = *mut cudnnTensorTransformStruct;
/// [`cudnnDeterminism_t`] is an enumerated type used to indicate if the computed results are deterministic (reproducible). For more information, refer to [Reproducibility (Determinism)](https://docs.nvidia.com/deeplearning/cudnn/backend/v9.22.0/developer/misc.html#reproducibility).
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnDeterminism_t {
    /// Results are not guaranteed to be reproducible.
    CUDNN_NON_DETERMINISTIC = 0,
    /// Results are guaranteed to be reproducible.
    CUDNN_DETERMINISTIC = 1,
}
unsafe extern "C" {
    /// This function creates a generic tensor descriptor object by allocating the memory needed to hold its opaque structure. The data is initialized to all zeros.
    ///
    /// # Parameters
    ///
    /// - `tensorDesc`: Input. Pointer to pointer where the address to the allocated tensor descriptor object should be stored.
    ///
    /// # Return value
    ///
    /// - `CUDNN_STATUS_ALLOC_FAILED`: The resources could not be allocated.
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: Invalid input argument.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The object was created successfully.
    pub fn cudnnCreateTensorDescriptor(
        tensorDesc: *mut cudnnTensorDescriptor_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function initializes a previously created generic tensor descriptor object into a 4D tensor. The strides of the four dimensions are inferred from the format parameter and set in such a way that the data is contiguous in memory with no padding between dimensions.
    ///
    /// The total size of a tensor including the potential padding between dimensions is limited to 2 Giga-elements of type `datatype`.
    ///
    /// # Parameters
    ///
    /// - `tensorDesc`: Input/output. Handle to a previously created tensor descriptor.
    /// - `format`: Input. Type of format.
    /// - `n`: Input. Number of images.
    /// - `c`: Input. Number of feature maps per image.
    /// - `h`: Input. Height of each feature map.
    /// - `w`: Input. Width of each feature map.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the parameters `n`, `c`, `h`, `w` was negative or `format` has an invalid enumerant value or `dataType` has an invalid enumerant value.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: The total size of the tensor descriptor exceeds the maximum limit of 2 Giga-elements.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The object was set successfully.
    pub fn cudnnSetTensor4dDescriptor(
        tensorDesc: cudnnTensorDescriptor_t,
        format: cudnnTensorFormat_t,
        dataType: cudnnDataType_t,
        n: ::core::ffi::c_int,
        c: ::core::ffi::c_int,
        h: ::core::ffi::c_int,
        w: ::core::ffi::c_int,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function initializes a previously created generic tensor descriptor object into a 4D tensor, similarly to [`cudnnSetTensor4dDescriptor`] but with the strides explicitly passed as parameters. This can be used to lay out the 4D tensor in any order or simply to define gaps between dimensions.
    ///
    /// At present, some cuDNN routines have limited support for strides. Those routines will return [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`] if a 4D tensor object with an unsupported stride is used. [`cudnnTransformTensor`] can be used to convert the data to a supported layout.
    ///
    /// The total size of a tensor including the potential padding between dimensions is limited to 2 Giga-elements of type `datatype`.
    ///
    /// # Parameters
    ///
    /// - `tensorDesc`: Input/output. Handle to a previously created tensor descriptor.
    /// - `n`: Input. Number of images.
    /// - `c`: Input. Number of feature maps per image.
    /// - `h`: Input. Height of each feature map.
    /// - `w`: Input. Width of each feature map.
    /// - `nStride`: Input. Stride between two consecutive images.
    /// - `cStride`: Input. Stride between two consecutive feature maps.
    /// - `hStride`: Input. Stride between two consecutive rows.
    /// - `wStride`: Input. Stride between two consecutive columns.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the parameters `n`, `c`, `h`, `w` or `nStride`, `cStride`, `hStride`, `wStride` is negative or `dataType` has an invalid enumerant value.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: The total size of the tensor descriptor exceeds the maximum limit of 2 Giga-elements.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The object was set successfully.
    pub fn cudnnSetTensor4dDescriptorEx(
        tensorDesc: cudnnTensorDescriptor_t,
        dataType: cudnnDataType_t,
        n: ::core::ffi::c_int,
        c: ::core::ffi::c_int,
        h: ::core::ffi::c_int,
        w: ::core::ffi::c_int,
        nStride: ::core::ffi::c_int,
        cStride: ::core::ffi::c_int,
        hStride: ::core::ffi::c_int,
        wStride: ::core::ffi::c_int,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function queries the parameters of the previously initialized `Tensor4d` descriptor object.
    ///
    /// # Parameters
    ///
    /// - `tensorDesc`: Input. Handle to a previously initialized tensor descriptor.
    /// - `n`: Output. Number of images.
    /// - `c`: Output. Number of feature maps per image.
    /// - `h`: Output. Height of each feature map.
    /// - `w`: Output. Width of each feature map.
    /// - `nStride`: Output. Stride between two consecutive images.
    /// - `cStride`: Output. Stride between two consecutive feature maps.
    /// - `hStride`: Output. Stride between two consecutive rows.
    /// - `wStride`: Output. Stride between two consecutive columns.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The operation succeeded.
    pub fn cudnnGetTensor4dDescriptor(
        tensorDesc: cudnnTensorDescriptor_t,
        dataType: *mut cudnnDataType_t,
        n: *mut ::core::ffi::c_int,
        c: *mut ::core::ffi::c_int,
        h: *mut ::core::ffi::c_int,
        w: *mut ::core::ffi::c_int,
        nStride: *mut ::core::ffi::c_int,
        cStride: *mut ::core::ffi::c_int,
        hStride: *mut ::core::ffi::c_int,
        wStride: *mut ::core::ffi::c_int,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function initializes a previously created generic tensor descriptor object.
    ///
    /// The total size of a tensor including the potential padding between dimensions is limited to 2 Giga-elements of type `datatype`. Tensors are restricted to having at least 4 dimensions, and at most [`CUDNN_DIM_MAX`] dimensions (defined in `cudnn.h`). When working with lower dimensional data, it is recommended that the user create a 4D tensor, and set the size along unused dimensions to 1.
    ///
    /// # Parameters
    ///
    /// - `tensorDesc`: Input/output. Handle to a previously created tensor descriptor.
    /// - `nbDims`: Input. Dimension of the tensor.
    ///
    /// Do not use 2 dimensions. Due to historical reasons, the minimum number of dimensions in the filter descriptor is three.
    /// - `dimA`: Input. Array of dimension `nbDims` that contain the size of the tensor for every dimension. The size along unused dimensions should be set to `1`. By convention, the ordering of dimensions in the array follows the format - `\[N, C, D, H, W\]`, with `W` occupying the smallest index in the array.
    /// - `strideA`: Input. Array of dimension `nbDims` that contain the stride of the tensor for every dimension. By convention, the ordering of the strides in the array follows the format - `\[Nstride, Cstride, Dstride, Hstride, Wstride\]`, with `Wstride` occupying the smallest index in the array.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the elements of the array `dimA` was negative or zero, or `dataType` has an invalid enumerant value.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: The parameter `nbDims` is outside the range `\[4, CUDNN_DIM_MAX\]`, or the total size of the tensor descriptor exceeds the maximum limit of 2 Giga-elements.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The object was set successfully.
    pub fn cudnnSetTensorNdDescriptor(
        tensorDesc: cudnnTensorDescriptor_t,
        dataType: cudnnDataType_t,
        nbDims: ::core::ffi::c_int,
        dimA: *const ::core::ffi::c_int,
        strideA: *const ::core::ffi::c_int,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function initializes an `Nd` tensor descriptor.
    ///
    /// # Parameters
    ///
    /// - `tensorDesc`: Output. Pointer to the tensor descriptor struct to be initialized.
    /// - `format`: Input. Tensor format.
    /// - `dataType`: Input. Tensor data type.
    /// - `nbDims`: Input. Dimension of the tensor.
    ///
    /// Do not use 2 dimensions. Due to historical reasons, the minimum number of dimensions in the filter descriptor is three.
    /// - `dimA`: Input. Array containing the size of each dimension.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: Tensor descriptor was not allocated properly; or input parameters are not set correctly.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: Dimension size requested is larger than maximum dimension size supported.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The function was successful.
    pub fn cudnnSetTensorNdDescriptorEx(
        tensorDesc: cudnnTensorDescriptor_t,
        format: cudnnTensorFormat_t,
        dataType: cudnnDataType_t,
        nbDims: ::core::ffi::c_int,
        dimA: *const ::core::ffi::c_int,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function retrieves values stored in a previously initialized `TensorNd` descriptor object.
    ///
    /// # Parameters
    ///
    /// - `tensorDesc`: Input. Handle to a previously initialized tensor descriptor.
    /// - `nbDimsRequested`: Input. Number of dimensions to extract from a given tensor descriptor. It is also the minimum size of the arrays `dimA` and `strideA`. If this number is greater than the resulting `nbDims\[0\]`, only `nbDims\[0\]` dimensions will be returned.
    /// - `nbDims`: Output. Actual number of dimensions of the tensor will be returned in `nbDims\[0\]`.
    /// - `dimA`: Output. Array of dimensions of at least `nbDimsRequested` that will be filled with the dimensions from the provided tensor descriptor.
    /// - `strideA`: Output. Array of dimensions of at least `nbDimsRequested` that will be filled with the strides from the provided tensor descriptor.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: Either `tensorDesc` or `nbDims` pointer is `NULL`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The results were returned successfully.
    pub fn cudnnGetTensorNdDescriptor(
        tensorDesc: cudnnTensorDescriptor_t,
        nbDimsRequested: ::core::ffi::c_int,
        dataType: *mut cudnnDataType_t,
        nbDims: *mut ::core::ffi::c_int,
        dimA: *mut ::core::ffi::c_int,
        strideA: *mut ::core::ffi::c_int,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function returns the size of the tensor in memory in respect to the given descriptor. This function can be used to know the amount of GPU memory to be allocated to hold that tensor.
    ///
    /// # Parameters
    ///
    /// - `tensorDesc`: Input. Handle to a previously initialized tensor descriptor.
    /// - `size`: Output. Size in bytes needed to hold the tensor in GPU memory.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The results were returned successfully.
    pub fn cudnnGetTensorSizeInBytes(
        tensorDesc: cudnnTensorDescriptor_t,
        size: *mut size_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function destroys a previously created tensor descriptor object. When the input pointer is `NULL`, this function performs no destroy operation.
    ///
    /// # Parameters
    ///
    /// - `tensorDesc`: Input. Pointer to the tensor descriptor object to be destroyed.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The object was destroyed successfully.
    pub fn cudnnDestroyTensorDescriptor(
        tensorDesc: cudnnTensorDescriptor_t,
    ) -> cudnnStatus_t;
}
/// [`cudnnFoldingDirection_t`] is an enumerated type used to select the folding direction. For more information, refer to [`cudnnTensorTransformDescriptor_t`].
///
/// **Data Member**.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnFoldingDirection_t {
    CUDNN_TRANSFORM_FOLD = 0,
    CUDNN_TRANSFORM_UNFOLD = 1,
}
unsafe extern "C" {
    /// This function initializes and returns a destination tensor descriptor `destDesc` for tensor transform operations. The initialization is done with the desired parameters described in the transform descriptor [`cudnnTensorDescriptor_t`].
    ///
    /// The returned Tensor descriptor will be packed.
    ///
    /// # Parameters
    ///
    /// - `transformDesc`: Input. Handle to a previously initialized tensor transform descriptor.
    /// - `srcDesc`: Input. Handle to a previously initialized tensor descriptor.
    /// - `destDesc`: Output. Handle of the tensor descriptor that will be initialized and returned.
    /// - `destSizeInBytes`: Output. A pointer to hold the size, in bytes, of the new tensor.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: If either `srcDesc` or `destDesc` is `NULL`, or if the tensor descriptors `nbDims` is incorrect. For more information, refer to [Tensor Descriptor](https://docs.nvidia.com/deeplearning/cudnn//latest/developer/core-concepts.html#tensor-descriptor "(in NVIDIA cuDNN)").
    /// - [`cudnnStatus_t::CUDNN_STATUS_EXECUTION_FAILED`]: Function failed to launch on the GPU.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: If the provided configuration is not 4D.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The tensor descriptor was initialized successfully.
    #[deprecated]
    pub fn cudnnInitTransformDest(
        transformDesc: cudnnTensorTransformDescriptor_t,
        srcDesc: cudnnTensorDescriptor_t,
        destDesc: cudnnTensorDescriptor_t,
        destSizeInBytes: *mut size_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function creates a tensor transform descriptor object by allocating the memory needed to hold its opaque structure. The tensor data is initialized to all zeros. Use the [`cudnnSetTensorTransformDescriptor`] function to initialize the descriptor created by this function.
    ///
    /// # Parameters
    ///
    /// - `transformDesc`: Output. A pointer to an uninitialized tensor transform descriptor.
    ///
    /// # Return value
    ///
    /// - `CUDNN_STATUS_ALLOC_FAILED`: The memory allocation failed.
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: The `transformDesc` is `NULL`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The descriptor object was created successfully.
    #[deprecated]
    pub fn cudnnCreateTensorTransformDescriptor(
        transformDesc: *mut cudnnTensorTransformDescriptor_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function initializes a tensor transform descriptor that was previously created using the [`cudnnCreateTensorTransformDescriptor`] function.
    ///
    /// # Parameters
    ///
    /// - `transformDesc`: Output. The tensor transform descriptor to be initialized.
    /// - `nbDims`: Input. The dimensionality of the transform operands. Must be greater than 2. For more information, refer to [Tensor Descriptor](https://docs.nvidia.com/deeplearning/cudnn//latest/developer/core-concepts.html#tensor-descriptor "(in NVIDIA cuDNN)").
    /// - `destFormat`: Input. The desired destination format.
    /// - `direction`: Input. Selects folding or unfolding. This input has no effect when folding parameters are all &lt;= 1. For more information, refer to [`cudnnFoldingDirection_t`].
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: The parameter `transformDesc` is `NULL`, or if `direction` is invalid, or `nbDims` is &lt;= 2.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: If the dimension size requested is larger than maximum dimension size supported (meaning, one of the `nbDims` is larger than [`CUDNN_DIM_MAX`]), or if `destFromat` is something other than NCHW or NHWC.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The function was launched successfully.
    #[deprecated]
    pub fn cudnnSetTensorTransformDescriptor(
        transformDesc: cudnnTensorTransformDescriptor_t,
        nbDims: u32,
        destFormat: cudnnTensorFormat_t,
        padBeforeA: *const i32,
        padAfterA: *const i32,
        foldA: *const u32,
        direction: cudnnFoldingDirection_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function returns the values stored in a previously initialized tensor transform descriptor.
    ///
    /// # Parameters
    ///
    /// - `transformDesc`: Input. A previously initialized tensor transform descriptor.
    /// - `nbDimsRequested`: Input. The number of dimensions to consider. For more information, refer to [Tensor Descriptor](https://docs.nvidia.com/deeplearning/cudnn//latest/developer/core-concepts.html#tensor-descriptor "(in NVIDIA cuDNN)").
    /// - `destFormat`: Output. The transform format that will be returned.
    /// - `direction`: Output. The setting that selects folding or unfolding. For more information, refer to [`cudnnFoldingDirection_t`].
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: If `transformDesc` is `NULL` or if `nbDimsRequested` is less than `3` or greater than [`CUDNN_DIM_MAX`].
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The results were obtained successfully.
    #[deprecated]
    pub fn cudnnGetTensorTransformDescriptor(
        transformDesc: cudnnTensorTransformDescriptor_t,
        nbDimsRequested: u32,
        destFormat: *mut cudnnTensorFormat_t,
        padBeforeA: *mut i32,
        padAfterA: *mut i32,
        foldA: *mut u32,
        direction: *mut cudnnFoldingDirection_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// Destroys a previously created tensor transform descriptor.
    ///
    /// # Parameters
    ///
    /// - `transformDesc`: Input. The tensor transform descriptor to be destroyed.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The object was destroyed successfully.
    #[deprecated]
    pub fn cudnnDestroyTensorTransformDescriptor(
        transformDesc: cudnnTensorTransformDescriptor_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function copies the scaled data from one tensor to another tensor with a different layout. Those descriptors need to have the same dimensions but not necessarily the same strides. The input and output tensors must not overlap in any way (meaning, tensors cannot be transformed in place). This function can be used to convert a tensor with an unsupported format to a supported one.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN context.
    /// - `xDesc`: Input. Handle to a previously initialized tensor descriptor. For more information, refer to [`cudnnTensorDescriptor_t`].
    /// - `x`: Input. Pointer to data of the tensor described by the `xDesc` descriptor.
    /// - `yDesc`: Input. Handle to a previously initialized tensor descriptor. For more information, refer to [`cudnnTensorDescriptor_t`].
    /// - `y`: Output. Pointer to data of the tensor described by the `yDesc` descriptor.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: The dimensions `n`, `c`, `h`, `w` or the `dataType` of the two tensor descriptors are different.
    /// - [`cudnnStatus_t::CUDNN_STATUS_EXECUTION_FAILED`]: The function failed to launch on the GPU.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: The function does not support the provided configuration.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The function launched successfully.
    #[deprecated]
    pub fn cudnnTransformTensor(
        handle: cudnnHandle_t,
        alpha: *const ::core::ffi::c_void,
        xDesc: cudnnTensorDescriptor_t,
        x: *const ::core::ffi::c_void,
        beta: *const ::core::ffi::c_void,
        yDesc: cudnnTensorDescriptor_t,
        y: *mut ::core::ffi::c_void,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function converts the tensor layouts between different formats. It can be used to convert a tensor with an unsupported layout format to a tensor with a supported layout format.
    ///
    /// This function copies the scaled data from the input tensor `srcDesc` to the output tensor `destDesc` with a different layout. The tensor descriptors of `srcDesc` and `destDesc` should have the same dimensions but need not have the same strides.
    ///
    /// The `srcDesc` and `destDesc` tensors must not overlap in any way (meaning, tensors cannot be transformed in place).
    ///
    /// When performing a folding transform or a zero-padding transform, the scaling factors (`alpha`, `beta`) should be set to (1, 0). However, unfolding transforms support any (`alpha`, `beta`) values. This function is thread safe.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN context. For more information, refer to [`cudnnHandle_t`].
    /// - `transDesc`: Input. A descriptor containing the details of the requested tensor transformation. For more information, refer to [`cudnnTensorTransformDescriptor_t`].
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: A parameter is uninitialized or initialized incorrectly, or the number of dimensions is different between `srcDesc` and `destDesc`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_EXECUTION_FAILED`]: Function failed to launch on the GPU.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: Function does not support the provided configuration. Also, in the folding and padding paths, any value other than `A=1` and `B=0` will result in a [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`].
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The function was launched successfully.
    #[deprecated]
    pub fn cudnnTransformTensorEx(
        handle: cudnnHandle_t,
        transDesc: cudnnTensorTransformDescriptor_t,
        alpha: *const ::core::ffi::c_void,
        srcDesc: cudnnTensorDescriptor_t,
        srcData: *const ::core::ffi::c_void,
        beta: *const ::core::ffi::c_void,
        destDesc: cudnnTensorDescriptor_t,
        destData: *mut ::core::ffi::c_void,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function adds the scaled values of a bias tensor to another tensor. Each dimension of the bias tensor `A` must match the corresponding dimension of the destination tensor `C` or must be equal to `1`. In the latter case, the same value from the bias tensor for those dimensions will be used to blend into the `C` tensor.
    ///
    /// Only 4D and 5D tensors are supported. Beyond these dimensions, this routine is not supported.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN context. For more information, refer to [`cudnnHandle_t`].
    /// - `aDesc`: Input. Handle to a previously initialized tensor descriptor. For more information, refer to [`cudnnTensorDescriptor_t`].
    /// - `A`: Input. Pointer to data of the tensor described by the `aDesc` descriptor.
    /// - `cDesc`: Input. Handle to a previously initialized tensor descriptor.
    /// - `C`: Input/output. Pointer to data of the tensor described by the `cDesc` descriptor.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: The dimensions of the bias tensor refer to an amount of data that is incompatible with the output tensor dimensions or the `dataType` of the two tensor descriptors are different.
    /// - [`cudnnStatus_t::CUDNN_STATUS_EXECUTION_FAILED`]: The function failed to launch on the GPU.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: The function does not support the provided configuration.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The function executed successfully.
    #[deprecated]
    pub fn cudnnAddTensor(
        handle: cudnnHandle_t,
        alpha: *const ::core::ffi::c_void,
        aDesc: cudnnTensorDescriptor_t,
        A: *const ::core::ffi::c_void,
        beta: *const ::core::ffi::c_void,
        cDesc: cudnnTensorDescriptor_t,
        C: *mut ::core::ffi::c_void,
    ) -> cudnnStatus_t;
}
/// [`cudnnOpTensorOp_t`] is an enumerated type used to indicate the Tensor Core operation to be used by the [`cudnnOpTensor`] routine. This enumerated type is used as a field for the [`cudnnOpTensorDescriptor_t`] descriptor.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnOpTensorOp_t {
    /// The operation to be performed is addition.
    CUDNN_OP_TENSOR_ADD = 0,
    /// The operation to be performed is multiplication.
    CUDNN_OP_TENSOR_MUL = 1,
    /// The operation to be performed is a minimum comparison.
    CUDNN_OP_TENSOR_MIN = 2,
    /// The operation to be performed is a maximum comparison.
    CUDNN_OP_TENSOR_MAX = 3,
    /// The operation to be performed is square root, performed on only the `A` tensor.
    CUDNN_OP_TENSOR_SQRT = 4,
    /// The operation to be performed is negation, performed on only the `A` tensor.
    CUDNN_OP_TENSOR_NOT = 5,
}
unsafe extern "C" {
    /// This function creates a tensor pointwise math descriptor. For more information, refer to [`cudnnOpTensorDescriptor_t`].
    ///
    /// # Parameters
    ///
    /// - `opTensorDesc`: Output. Pointer to the structure holding the description of the tensor pointwise math such as add, multiply, and more.
    ///
    /// # Return value
    ///
    /// - `CUDNN_STATUS_ALLOC_FAILED`: Memory allocation for this tensor pointwise math descriptor failed.
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: Tensor pointwise math descriptor passed to the function is invalid.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The function returned successfully.
    pub fn cudnnCreateOpTensorDescriptor(
        opTensorDesc: *mut cudnnOpTensorDescriptor_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function initializes a tensor pointwise math descriptor.
    ///
    /// # Parameters
    ///
    /// - `opTensorDesc`: Output. Pointer to the structure holding the description of the tensor pointwise math descriptor.
    /// - `opTensorOp`: Input. Tensor pointwise math operation for this tensor pointwise math descriptor.
    /// - `opTensorCompType`: Input. Computation datatype for this tensor pointwise math descriptor.
    /// - `opTensorNanOpt`: Input. NAN propagation policy.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the input parameters passed is invalid.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The function returned successfully.
    pub fn cudnnSetOpTensorDescriptor(
        opTensorDesc: cudnnOpTensorDescriptor_t,
        opTensorOp: cudnnOpTensorOp_t,
        opTensorCompType: cudnnDataType_t,
        opTensorNanOpt: cudnnNanPropagation_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function returns the configuration of the passed tensor pointwise math descriptor.
    ///
    /// # Parameters
    ///
    /// - `opTensorDesc`: Input. Tensor pointwise math descriptor passed to get the configuration from.
    /// - `opTensorOp`: Output. Pointer to the tensor pointwise math operation type, associated with this tensor pointwise math descriptor.
    /// - `opTensorCompType`: Output. Pointer to the cuDNN data-type associated with this tensor pointwise math descriptor.
    /// - `opTensorNanOpt`: Output. Pointer to the NAN propagation option associated with this tensor pointwise math descriptor.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: Input tensor pointwise math descriptor passed is invalid.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The function returned successfully.
    pub fn cudnnGetOpTensorDescriptor(
        opTensorDesc: cudnnOpTensorDescriptor_t,
        opTensorOp: *mut cudnnOpTensorOp_t,
        opTensorCompType: *mut cudnnDataType_t,
        opTensorNanOpt: *mut cudnnNanPropagation_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function deletes a tensor pointwise math descriptor object.
    ///
    /// # Parameters
    ///
    /// - `opTensorDesc`: Input. Pointer to the structure holding the description of the tensor pointwise math to be deleted.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The function returned successfully.
    pub fn cudnnDestroyOpTensorDescriptor(
        opTensorDesc: cudnnOpTensorDescriptor_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function implements the equation `C = op(alpha1\[0\] * A`, `alpha2\[0\] * B) + beta\[0\] * C`, given the tensors A, B, and C and the scaling factors `alpha1`, `alpha2`, and `beta`. The op to use is indicated by the descriptor [`cudnnOpTensorDescriptor_t`], meaning, the type of `opTensorDesc`. Currently-supported ops are listed by the [`cudnnOpTensorOp_t`] enum.
    ///
    /// The following restrictions on the input and destination tensors apply:
    ///
    /// [`cudnnTensorFormat_t::CUDNN_TENSOR_NCHW_VECT_C`] is not supported as input tensor format. All tensors up to dimension five (5) are supported. This routine does not support tensor formats beyond these dimensions.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN context.
    /// - `opTensorDesc`: Input. Handle to a previously initialized op tensor descriptor.
    /// - `C`: Input/output. Pointer to data of the tensor described by the `cDesc` descriptor.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: The data type of the destination tensor C is unrecognized, or the restrictions on the input and destination tensors, stated above, are not met.
    /// - [`cudnnStatus_t::CUDNN_STATUS_EXECUTION_FAILED`]: The function failed to launch on the GPU.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: The function does not support the provided configuration. Some examples include:
    ///
    /// * The dimensions of the bias tensor and the output tensor dimensions are above 5.
    /// * `opTensorCompType` is not set as stated above.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The function executed successfully.
    #[deprecated]
    pub fn cudnnOpTensor(
        handle: cudnnHandle_t,
        opTensorDesc: cudnnOpTensorDescriptor_t,
        alpha1: *const ::core::ffi::c_void,
        aDesc: cudnnTensorDescriptor_t,
        A: *const ::core::ffi::c_void,
        alpha2: *const ::core::ffi::c_void,
        bDesc: cudnnTensorDescriptor_t,
        B: *const ::core::ffi::c_void,
        beta: *const ::core::ffi::c_void,
        cDesc: cudnnTensorDescriptor_t,
        C: *mut ::core::ffi::c_void,
    ) -> cudnnStatus_t;
}
/// [`cudnnReduceTensorIndices_t`] is an enumerated type used to indicate whether indices are to be computed by the [`cudnnReduceTensor`] routine. This enumerated type is used as a field for the [`cudnnReduceTensorDescriptor_t`] descriptor.
#[deprecated]
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnReduceTensorIndices_t {
    /// Do not compute indices.
    #[deprecated]
    CUDNN_REDUCE_TENSOR_NO_INDICES = 0,
    /// Compute indices. The resulting indices are relative, and flattened.
    #[deprecated]
    CUDNN_REDUCE_TENSOR_FLATTENED_INDICES = 1,
}
/// [`cudnnIndicesType_t`] is an enumerated type used to indicate the data type for the indices to be computed by the [`cudnnReduceTensor`] routine. This enumerated type is used as a field for the [`cudnnReduceTensorDescriptor_t`] descriptor.
#[deprecated]
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnIndicesType_t {
    /// Compute unsigned int indices.
    #[deprecated]
    CUDNN_32BIT_INDICES = 0,
    /// Compute unsigned long indices.
    #[deprecated]
    CUDNN_64BIT_INDICES = 1,
    /// Compute unsigned short indices.
    #[deprecated]
    CUDNN_16BIT_INDICES = 2,
    /// Compute unsigned char indices.
    #[deprecated]
    CUDNN_8BIT_INDICES = 3,
}
unsafe extern "C" {
    /// This function creates a reduced tensor descriptor object by allocating the memory needed to hold its opaque structure.
    ///
    /// # Return value
    ///
    /// - `CUDNN_STATUS_ALLOC_FAILED`: The resources could not be allocated.
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: `reduceTensorDesc` is a `NULL` pointer.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The object was created successfully.
    pub fn cudnnCreateReduceTensorDescriptor(
        reduceTensorDesc: *mut cudnnReduceTensorDescriptor_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function initializes a previously created reduce tensor descriptor object.
    ///
    /// # Parameters
    ///
    /// - `reduceTensorDesc`: Input/output. Handle to a previously created reduce tensor descriptor.
    /// - `reduceTensorOp`: Input. Enumerant to specify the reduced tensor operation.
    /// - `reduceTensorCompType`: Input. Enumerant to specify the computation datatype of the reduction.
    /// - `reduceTensorNanOpt`: Input. Enumerant to specify the Nan propagation mode.
    /// - `reduceTensorIndices`: Input. Enumerant to specify the reduced tensor indices.
    /// - `reduceTensorIndicesType`: Input. Enumerant to specify the reduced tensor indices type.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: `reduceTensorDesc` is `NULL` (`reduceTensorOp`, `reduceTensorCompType`, `reduceTensorNanOpt`, `reduceTensorIndices` or `reduceTensorIndicesType` has an invalid enumerant value).
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The object was set successfully.
    pub fn cudnnSetReduceTensorDescriptor(
        reduceTensorDesc: cudnnReduceTensorDescriptor_t,
        reduceTensorOp: cudnnReduceTensorOp_t,
        reduceTensorCompType: cudnnDataType_t,
        reduceTensorNanOpt: cudnnNanPropagation_t,
        reduceTensorIndices: cudnnReduceTensorIndices_t,
        reduceTensorIndicesType: cudnnIndicesType_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function queries a previously initialized reduce tensor descriptor object.
    ///
    /// # Parameters
    ///
    /// - `reduceTensorDesc`: Input. Pointer to a previously initialized reduce tensor descriptor object.
    /// - `reduceTensorOp`: Output. Enumerant to specify the reduced tensor operation.
    /// - `reduceTensorCompType`: Output. Enumerant to specify the computation datatype of the reduction.
    /// - `reduceTensorNanOpt`: Input. Enumerant to specify the Nan propagation mode.
    /// - `reduceTensorIndices`: Output. Enumerant to specify the reduced tensor indices.
    /// - `reduceTensorIndicesType`: Output. Enumerant to specify the reduced tensor indices type.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: `reduceTensorDesc` is `NULL`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The object was queried successfully.
    pub fn cudnnGetReduceTensorDescriptor(
        reduceTensorDesc: cudnnReduceTensorDescriptor_t,
        reduceTensorOp: *mut cudnnReduceTensorOp_t,
        reduceTensorCompType: *mut cudnnDataType_t,
        reduceTensorNanOpt: *mut cudnnNanPropagation_t,
        reduceTensorIndices: *mut cudnnReduceTensorIndices_t,
        reduceTensorIndicesType: *mut cudnnIndicesType_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function destroys a previously created reduce tensor descriptor object. When the input pointer is `NULL`, this function performs no destroy operation.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The object was destroyed successfully.
    pub fn cudnnDestroyReduceTensorDescriptor(
        reduceTensorDesc: cudnnReduceTensorDescriptor_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This is a helper function to return the minimum size of the index space to be passed to the reduction given the input and output tensors.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN library descriptor.
    /// - `aDesc`: Input. Pointer to the input tensor descriptor.
    /// - `cDesc`: Input. Pointer to the output tensor descriptor.
    /// - `sizeInBytes`: Output. Minimum size of the index space to be passed to the reduction.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The index space size is returned successfully.
    pub fn cudnnGetReductionIndicesSize(
        handle: cudnnHandle_t,
        reduceTensorDesc: cudnnReduceTensorDescriptor_t,
        aDesc: cudnnTensorDescriptor_t,
        cDesc: cudnnTensorDescriptor_t,
        sizeInBytes: *mut size_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This is a helper function to return the minimum size of the workspace to be passed to the reduction given the input and output tensors.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN library descriptor.
    /// - `aDesc`: Input. Pointer to the input tensor descriptor.
    /// - `cDesc`: Input. Pointer to the output tensor descriptor.
    /// - `sizeInBytes`: Output. Minimum size of the index space to be passed to the reduction.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The workspace size is returned successfully.
    pub fn cudnnGetReductionWorkspaceSize(
        handle: cudnnHandle_t,
        reduceTensorDesc: cudnnReduceTensorDescriptor_t,
        aDesc: cudnnTensorDescriptor_t,
        cDesc: cudnnTensorDescriptor_t,
        sizeInBytes: *mut size_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function reduces tensor A by implementing the equation `C = alpha * reduce op ( A ) + beta * C`, given tensors A and C and scaling factors `alpha` and `beta`. The reduction op to use is indicated by the descriptor `reduceTensorDesc`. Currently-supported ops are listed by the [`cudnnReduceTensorOp_t`] enum.
    ///
    /// Each dimension of the output tensor C must match the corresponding dimension of the input tensor A or must be equal to `1`. The dimensions equal to `1` indicate the dimensions of A to be reduced.
    ///
    /// The implementation will generate indices for the `min` and `max` ops only, as indicated by the [`cudnnReduceTensorIndices_t`] enum of the `reduceTensorDesc`. Requesting indices for the other reduction ops results in an error. The data type of the indices is indicated by the [`cudnnIndicesType_t`] enum; currently only the 32-bit (unsigned int) type is supported.
    ///
    /// The indices returned by the implementation are not absolute indices but relative to the dimensions being reduced. The indices are also flattened, meaning, not coordinate tuples.
    ///
    /// The data types of the tensors A and C must match if of type `double`. In this case, `alpha` and `beta` and the computation enum of `reduceTensorDesc` are all assumed to be of type `double`.
    ///
    /// The `HALF` and `INT8` data types may be mixed with the `FLOAT` data types. In these cases, the computation enum of `reduceTensorDesc` is required to be of type `FLOAT`.
    ///
    /// Up to dimension 8, all tensor formats are supported. Beyond those dimensions, this routine is not supported.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN context.
    /// - `reduceTensorDesc`: Input. Handle to a previously initialized reduce tensor descriptor.
    /// - `indices`: Output. Handle to a previously allocated space for writing indices.
    /// - `indicesSizeInBytes`: Input. Size of the above previously allocated space.
    /// - `workspace`: Input. Handle to a previously allocated space for the reduction implementation.
    /// - `workspaceSizeInBytes`: Input. Size of the above previously allocated space.
    /// - `A`: Input. Pointer to data of the tensor described by the `aDesc` descriptor.
    /// - `C`: Input/output. Pointer to data of the tensor described by the `cDesc` descriptor.
    ///
    /// # Return value
    ///
    /// - `CUDNN_INVALID_VALUE`: The allocations for the indices or workspace are insufficient.
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: The corresponding dimensions of the input and output tensors all match, or the conditions in the above paragraphs are unmet.
    /// - [`cudnnStatus_t::CUDNN_STATUS_EXECUTION_FAILED`]: The function failed to launch on the GPU.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: The function does not support the provided configuration. Some examples include:
    ///
    /// * The dimensions of the input tensor and the output tensor are above 8.
    /// * `reduceTensorCompType` is not set as stated above.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The function executed successfully.
    #[deprecated]
    pub fn cudnnReduceTensor(
        handle: cudnnHandle_t,
        reduceTensorDesc: cudnnReduceTensorDescriptor_t,
        indices: *mut ::core::ffi::c_void,
        indicesSizeInBytes: size_t,
        workspace: *mut ::core::ffi::c_void,
        workspaceSizeInBytes: size_t,
        alpha: *const ::core::ffi::c_void,
        aDesc: cudnnTensorDescriptor_t,
        A: *const ::core::ffi::c_void,
        beta: *const ::core::ffi::c_void,
        cDesc: cudnnTensorDescriptor_t,
        C: *mut ::core::ffi::c_void,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function sets all the elements of a tensor to a given value.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN context.
    /// - `yDesc`: Input. Handle to a previously initialized tensor descriptor.
    /// - `y`: Input/output. Pointer to data of the tensor described by the `yDesc` descriptor.
    /// - `valuePtr`: Input. Pointer in host memory to a single value. All elements of the `y` tensor will be set to `value\[0\]`. The data type of the element in `value\[0\]` has to match the data type of tensor `y`.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: One of the provided pointers is `NIL`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_EXECUTION_FAILED`]: The function failed to launch on the GPU.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: The function does not support the provided configuration.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The function launched successfully.
    pub fn cudnnSetTensor(
        handle: cudnnHandle_t,
        yDesc: cudnnTensorDescriptor_t,
        y: *mut ::core::ffi::c_void,
        valuePtr: *const ::core::ffi::c_void,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function scales all the elements of a tensor by a given factor.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN context.
    /// - `yDesc`: Input. Handle to a previously initialized tensor descriptor.
    /// - `y`: Input/output. Pointer to data of the tensor described by the `yDesc` descriptor.
    /// - `alpha`: Input. Pointer in Host memory to a single value that all elements of the tensor will be scaled with. For more information, refer to [Scaling Parameters](https://docs.nvidia.com/deeplearning/cudnn/backend/v9.22.0/developer/misc.html#scaling-parameters).
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: One of the provided pointers is `NIL`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_EXECUTION_FAILED`]: The function failed to launch on the GPU.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: The function does not support the provided configuration.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The function launched successfully.
    #[deprecated]
    pub fn cudnnScaleTensor(
        handle: cudnnHandle_t,
        yDesc: cudnnTensorDescriptor_t,
        y: *mut ::core::ffi::c_void,
        alpha: *const ::core::ffi::c_void,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function creates a filter descriptor object by allocating the memory needed to hold its opaque structure. For more information, refer to [`cudnnFilterDescriptor_t`].
    ///
    /// # Return value
    ///
    /// - `CUDNN_STATUS_ALLOC_FAILED`: The resources could not be allocated.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The object was created successfully.
    #[deprecated]
    pub fn cudnnCreateFilterDescriptor(
        filterDesc: *mut cudnnFilterDescriptor_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function initializes a previously created filter descriptor object into a 4D filter. The layout of the filters must be contiguous in memory.
    ///
    /// Tensor format [`cudnnTensorFormat_t::CUDNN_TENSOR_NHWC`] has limited support in [`cudnnConvolutionForward`], [`cudnnConvolutionBackwardData`], and [`cudnnConvolutionBackwardFilter`].
    ///
    /// # Parameters
    ///
    /// - `filterDesc`: Input/output. Handle to a previously created filter descriptor.
    /// - `format`: Input. Type of the filter layout format. If this input is set to [`cudnnTensorFormat_t::CUDNN_TENSOR_NCHW`], which is one of the enumerant values allowed by [`cudnnTensorFormat_t`] descriptor, then the layout of the filter is in the form of `KCRS`, where:
    ///
    /// * `K` represents the number of output feature maps
    /// * `C` is the number of input feature maps
    /// * `R` is the number of rows per filter
    /// * `S` is the number of columns per filter
    ///
    /// If this input is set to [`cudnnTensorFormat_t::CUDNN_TENSOR_NHWC`], then the layout of the filter is in the form of `KRSC`.
    /// - `k`: Input. Number of output feature maps.
    /// - `c`: Input. Number of input feature maps.
    /// - `h`: Input. Height of each filter.
    /// - `w`: Input. Width of each filter.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the parameters `k`, `c`, `h`, `w` is negative or `dataType` or `format` has an invalid enumerant value.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The object was set successfully.
    #[deprecated]
    pub fn cudnnSetFilter4dDescriptor(
        filterDesc: cudnnFilterDescriptor_t,
        dataType: cudnnDataType_t,
        format: cudnnTensorFormat_t,
        k: ::core::ffi::c_int,
        c: ::core::ffi::c_int,
        h: ::core::ffi::c_int,
        w: ::core::ffi::c_int,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function queries the parameters of the previously initialized `Filter4d` descriptor object.
    ///
    /// # Parameters
    ///
    /// - `filterDesc`: Input. Handle to a previously created filter descriptor.
    /// - `format`: Output. Type of format.
    /// - `k`: Output. Number of output feature maps.
    /// - `c`: Output. Number of input feature maps.
    /// - `h`: Output. Height of each filter.
    /// - `w`: Output. Width of each filter.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The object was set successfully.
    #[deprecated]
    pub fn cudnnGetFilter4dDescriptor(
        filterDesc: cudnnFilterDescriptor_t,
        dataType: *mut cudnnDataType_t,
        format: *mut cudnnTensorFormat_t,
        k: *mut ::core::ffi::c_int,
        c: *mut ::core::ffi::c_int,
        h: *mut ::core::ffi::c_int,
        w: *mut ::core::ffi::c_int,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function initializes a previously created filter descriptor object. The layout of the filters must be contiguous in memory.
    ///
    /// The tensor format [`cudnnTensorFormat_t::CUDNN_TENSOR_NHWC`] has limited support in [`cudnnConvolutionForward`], [`cudnnConvolutionBackwardData`], and [`cudnnConvolutionBackwardFilter`].
    ///
    /// # Parameters
    ///
    /// - `filterDesc`: Input/output. Handle to a previously created filter descriptor.
    /// - `format`: Input.Type of the filter layout format. If this input is set to [`cudnnTensorFormat_t::CUDNN_TENSOR_NCHW`], which is one of the enumerant values allowed by [`cudnnTensorFormat_t`] descriptor, then the layout of the filter is as follows:
    ///
    /// ```text
    /// * For `N=4`, a 4D filter descriptor, the filter layout is in the form of `KCRS`:
    /// :   + `K` represents the number of output feature maps
    ///       + `C` is the number of input feature maps
    ///       + `R` is the number of rows per filter
    ///       + `S` is the number of columns per filter
    /// * For `N=3`, a 3D filter descriptor, the number `S` (number of columns per filter) is omitted.
    /// * For `N=5` and greater, the layout of the higher dimensions immediately follows `RS`.
    ///
    /// On the other hand, if this input is set to [`cudnnTensorFormat_t::CUDNN_TENSOR_NHWC`], then the layout of the filter is as follows:
    ///
    /// * For `N=4`, a 4D filter descriptor, the filter layout is in the form of `KRSC`.
    /// * For `N=3`, a 3D filter descriptor, the number `S` (number of columns per filter) is omitted, and the layout of `C` immediately follows `R`.
    /// * For `N=5` and greater, the layout of the higher dimensions are inserted between `S` and `C`.
    /// ```
    /// - `nbDims`: Input. Dimension of the filter.
    /// - `filterDimA`: Input. Array of dimension `nbDims` containing the size of the filter for each dimension.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the elements of the array `filterDimA` is negative or `dataType` or `format` has an invalid enumerant value.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: The parameter `nbDims` exceeds [`CUDNN_DIM_MAX`].
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The object was set successfully.
    #[deprecated]
    pub fn cudnnSetFilterNdDescriptor(
        filterDesc: cudnnFilterDescriptor_t,
        dataType: cudnnDataType_t,
        format: cudnnTensorFormat_t,
        nbDims: ::core::ffi::c_int,
        filterDimA: *const ::core::ffi::c_int,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function queries a previously initialized `FilterNd` descriptor object.
    ///
    /// # Parameters
    ///
    /// - `nbDimsRequested`: Input. Dimension of the expected filter descriptor. It is also the minimum size of the arrays filterDimA in order to be able to hold the results.
    /// - `format`: Output. Type of format.
    /// - `nbDims`: Output. Actual dimension of the filter.
    /// - `filterDimA`: Output. Array of dimensions of at least `nbDimsRequested` that will be filled with the filter parameters from the provided filter descriptor.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: The parameter `nbDimsRequested` is negative.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The object was set successfully.
    #[deprecated]
    pub fn cudnnGetFilterNdDescriptor(
        filterDesc: cudnnFilterDescriptor_t,
        nbDimsRequested: ::core::ffi::c_int,
        dataType: *mut cudnnDataType_t,
        format: *mut cudnnTensorFormat_t,
        nbDims: *mut ::core::ffi::c_int,
        filterDimA: *mut ::core::ffi::c_int,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function returns the size of the filter tensor in memory with respect to the given descriptor. It can be used to know the amount of GPU memory to be allocated to hold that filter tensor.
    ///
    /// # Parameters
    ///
    /// - `filterDesc`: Input. handle to a previously initialized filter descriptor.
    /// - `size`: Output. size in bytes needed to hold the tensor in GPU memory.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: `filerDesc` is invalid.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: `filterDesc` is valid.
    #[deprecated]
    pub fn cudnnGetFilterSizeInBytes(
        filterDesc: cudnnFilterDescriptor_t,
        size: *mut size_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function converts the filter between different formats, data types, or dimensions based on the described transformation. It can be used to convert a filter with an unsupported layout format to a filter with a supported layout format.
    ///
    /// This function copies the scaled data from the input filter `srcDesc` to the output tensor `destDesc` with a different layout. If the filter descriptors of `srcDesc` and `destDesc` have different dimensions, they must be consistent with folding and padding amount and order specified in `transDesc`.
    ///
    /// The `srcDesc` and `destDesc` tensors must not overlap in any way (meaning, tensors cannot be transformed in place).
    ///
    /// When performing a folding transform or a zero-padding transform, the scaling factors (`alpha`, `beta`) should be set to (1, 0). However, unfolding transforms support any (`alpha`, `beta`) values. This function is thread safe.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN context. For more information, refer to [`cudnnHandle_t`].
    /// - `transDesc`: Input. A descriptor containing the details of the requested filter transformation. For more information, refer to [`cudnnTensorTransformDescriptor_t`].
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: A parameter is uninitialized or initialized incorrectly, or the number of dimensions is different between `srcDesc` and `destDesc`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_EXECUTION_FAILED`]: The function failed to launch on the GPU.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: The function does not support the provided configuration. Also, in the folding and padding paths, any value other than `A=1` and `B=0` will result in a [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`].
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The function launched successfully.
    #[deprecated]
    pub fn cudnnTransformFilter(
        handle: cudnnHandle_t,
        transDesc: cudnnTensorTransformDescriptor_t,
        alpha: *const ::core::ffi::c_void,
        srcDesc: cudnnFilterDescriptor_t,
        srcData: *const ::core::ffi::c_void,
        beta: *const ::core::ffi::c_void,
        destDesc: cudnnFilterDescriptor_t,
        destData: *mut ::core::ffi::c_void,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function destroys a filter object.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The object was destroyed successfully.
    #[deprecated]
    pub fn cudnnDestroyFilterDescriptor(
        filterDesc: cudnnFilterDescriptor_t,
    ) -> cudnnStatus_t;
}
/// [`cudnnSoftmaxAlgorithm_t`] is used to select an implementation of the softmax function used in [`cudnnSoftmaxForward`] and [`cudnnSoftmaxBackward`].
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnSoftmaxAlgorithm_t {
    /// This implementation applies the straightforward softmax operation.
    CUDNN_SOFTMAX_FAST = 0,
    /// This implementation scales each point of the softmax input domain by its maximum value to avoid potential floating point overflows in the softmax evaluation.
    CUDNN_SOFTMAX_ACCURATE = 1,
    /// This entry performs the log softmax operation, avoiding overflows by scaling each point in the input domain as in [`cudnnSoftmaxAlgorithm_t::CUDNN_SOFTMAX_ACCURATE`].
    CUDNN_SOFTMAX_LOG = 2,
}
/// [`cudnnSoftmaxMode_t`] is used to select over which data the [`cudnnSoftmaxForward`] and [`cudnnSoftmaxBackward`] are computing their results.
///
/// [`cudnnSoftmaxMode_t::CUDNN_SOFTMAX_MODE_INSTANCE`]
/// The softmax operation is computed per image (`N`) across the dimensions `C,H,W`.
///
/// [`cudnnSoftmaxMode_t::CUDNN_SOFTMAX_MODE_CHANNEL`]
/// The softmax operation is computed per spatial location (`H,W`) per image (`N`) across dimension `C`.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnSoftmaxMode_t {
    CUDNN_SOFTMAX_MODE_INSTANCE = 0,
    CUDNN_SOFTMAX_MODE_CHANNEL = 1,
}
unsafe extern "C" {
    /// This routine computes the softmax function.
    ///
    /// In-place operation is allowed for this routine; meaning, `x` and `y` pointers may be equal. However, this requires `xDesc` and `yDesc` descriptors to be identical (particularly, the strides of the input and output must match for in-place operation to be allowed).
    ///
    /// All tensor formats are supported for all modes and algorithms with 4 and 5D tensors. Performance is expected to be highest with NCHW fully-packed tensors. For more than 5 dimensions tensors must be packed in their spatial dimensions.
    ///
    /// [`cudnnStatus_t::CUDNN_STATUS_EXECUTION_FAILED`]
    /// The function failed to launch on the GPU.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN context.
    /// - `mode`: Input. Enumerant to specify the softmax mode.
    /// - `xDesc`: Input. Handle to the previously initialized input tensor descriptor.
    /// - `x`: Input. Data pointer to GPU memory associated with the tensor descriptor `xDesc`.
    /// - `yDesc`: Input. Handle to the previously initialized output tensor descriptor.
    /// - `y`: Output. Data pointer to GPU memory associated with the output tensor descriptor `yDesc`.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * The dimensions `n`, `c`, `h`, `w` of the input tensor and output tensors differ.
    /// * The `datatype` of the input tensor and output tensors differ.
    /// * The parameters `algorithm` or `mode` have an invalid enumerant value.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: The function does not support the provided configuration.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The function launched successfully.
    pub fn cudnnSoftmaxForward(
        handle: cudnnHandle_t,
        algo: cudnnSoftmaxAlgorithm_t,
        mode: cudnnSoftmaxMode_t,
        alpha: *const ::core::ffi::c_void,
        xDesc: cudnnTensorDescriptor_t,
        x: *const ::core::ffi::c_void,
        beta: *const ::core::ffi::c_void,
        yDesc: cudnnTensorDescriptor_t,
        y: *mut ::core::ffi::c_void,
    ) -> cudnnStatus_t;
}
/// [`cudnnPoolingMode_t`] is an enumerated type passed to [`cudnnSetPooling2dDescriptor`] to select the pooling method to be used by [`cudnnPoolingForward`] and [`cudnnPoolingBackward`].
#[deprecated]
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnPoolingMode_t {
    /// The maximum value inside the pooling window is used.
    #[deprecated]
    CUDNN_POOLING_MAX = 0,
    /// Values inside the pooling window are averaged. The number of elements used to calculate the average includes spatial locations falling in the padding region.
    #[deprecated]
    CUDNN_POOLING_AVERAGE_COUNT_INCLUDE_PADDING = 1,
    /// Values inside the pooling window are averaged. The number of elements used to calculate the average excludes spatial locations falling in the padding region.
    #[deprecated]
    CUDNN_POOLING_AVERAGE_COUNT_EXCLUDE_PADDING = 2,
    /// The maximum value inside the pooling window is used. The algorithm used is deterministic.
    #[deprecated]
    CUDNN_POOLING_MAX_DETERMINISTIC = 3,
}
unsafe extern "C" {
    /// This function creates a pooling descriptor object by allocating the memory needed to hold its opaque structure.
    ///
    /// # Return value
    ///
    /// - `CUDNN_STATUS_ALLOC_FAILED`: The resources could not be allocated.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The object was created successfully.
    pub fn cudnnCreatePoolingDescriptor(
        poolingDesc: *mut cudnnPoolingDescriptor_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function initializes a previously created generic pooling descriptor object into a 2D description.
    ///
    /// # Parameters
    ///
    /// - `poolingDesc`: Input/output. Handle to a previously created pooling descriptor.
    /// - `mode`: Input. Enumerant to specify the pooling mode.
    /// - `maxpoolingNanOpt`: Input. Enumerant to specify the Nan propagation mode.
    /// - `windowHeight`: Input. Height of the pooling window.
    /// - `windowWidth`: Input. Width of the pooling window.
    /// - `verticalPadding`: Input. Size of vertical padding.
    /// - `horizontalPadding`: Input. Size of horizontal padding.
    /// - `verticalStride`: Input. Pooling vertical stride.
    /// - `horizontalStride`: Input. Pooling horizontal stride.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the parameters `windowHeight`, `windowWidth`, `verticalStride`, `horizontalStride` is negative or mode or `maxpoolingNanOpt` has an invalid enumerate value.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The object was set successfully.
    pub fn cudnnSetPooling2dDescriptor(
        poolingDesc: cudnnPoolingDescriptor_t,
        mode: cudnnPoolingMode_t,
        maxpoolingNanOpt: cudnnNanPropagation_t,
        windowHeight: ::core::ffi::c_int,
        windowWidth: ::core::ffi::c_int,
        verticalPadding: ::core::ffi::c_int,
        horizontalPadding: ::core::ffi::c_int,
        verticalStride: ::core::ffi::c_int,
        horizontalStride: ::core::ffi::c_int,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function queries a previously created `Pooling2d` descriptor object.
    ///
    /// # Parameters
    ///
    /// - `poolingDesc`: Input. Handle to a previously created pooling descriptor.
    /// - `mode`: Output. Enumerant to specify the pooling mode.
    /// - `maxpoolingNanOpt`: Output. Enumerant to specify the Nan propagation mode.
    /// - `windowHeight`: Output. Height of the pooling window.
    /// - `windowWidth`: Output. Width of the pooling window.
    /// - `verticalPadding`: Output. Size of vertical padding.
    /// - `horizontalPadding`: Output. Size of horizontal padding.
    /// - `verticalStride`: Output. Pooling vertical stride.
    /// - `horizontalStride`: Output. Pooling horizontal stride.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The object was set successfully.
    pub fn cudnnGetPooling2dDescriptor(
        poolingDesc: cudnnPoolingDescriptor_t,
        mode: *mut cudnnPoolingMode_t,
        maxpoolingNanOpt: *mut cudnnNanPropagation_t,
        windowHeight: *mut ::core::ffi::c_int,
        windowWidth: *mut ::core::ffi::c_int,
        verticalPadding: *mut ::core::ffi::c_int,
        horizontalPadding: *mut ::core::ffi::c_int,
        verticalStride: *mut ::core::ffi::c_int,
        horizontalStride: *mut ::core::ffi::c_int,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function initializes a previously created generic pooling descriptor object.
    ///
    /// # Parameters
    ///
    /// - `poolingDesc`: Input/output. Handle to a previously created pooling descriptor.
    /// - `mode`: Input. Enumerant to specify the pooling mode.
    /// - `maxpoolingNanOpt`: Input. Enumerant to specify the Nan propagation mode.
    /// - `nbDims`: Input. Dimension of the pooling operation. Must be greater than zero.
    /// - `windowDimA`: Input. Array of dimension `nbDims` containing the window size for each dimension. The value of array elements must be greater than zero.
    /// - `paddingA`: Input. Array of dimension `nbDims` containing the padding size for each dimension. Negative padding is allowed.
    /// - `strideA`: Input. Array of dimension `nbDims` containing the striding size for each dimension. The value of array elements must be greater than zero (meaning, negative striding size is not allowed).
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: Either `nbDims`, or at least one of the elements of the arrays `windowDimA` or `strideA` is negative, or mode or `maxpoolingNanOpt` has an invalid enumerate value.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: If (`nbDims > CUDNN_DIM_MAX-2`).
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The object was initialized successfully.
    pub fn cudnnSetPoolingNdDescriptor(
        poolingDesc: cudnnPoolingDescriptor_t,
        mode: cudnnPoolingMode_t,
        maxpoolingNanOpt: cudnnNanPropagation_t,
        nbDims: ::core::ffi::c_int,
        windowDimA: *const ::core::ffi::c_int,
        paddingA: *const ::core::ffi::c_int,
        strideA: *const ::core::ffi::c_int,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function queries a previously initialized generic `PoolingNd` descriptor object.
    ///
    /// # Parameters
    ///
    /// - `poolingDesc`: Input. Handle to a previously created pooling descriptor.
    /// - `nbDimsRequested`: Input. Dimension of the expected pooling descriptor. It is also the minimum size of the arrays `windowDimA`, `paddingA`, and `strideA` in order to be able to hold the results.
    /// - `mode`: Output. Enumerant to specify the pooling mode.
    /// - `maxpoolingNanOpt`: Input. Enumerant to specify the Nan propagation mode.
    /// - `nbDims`: Output. Actual dimension of the pooling descriptor.
    /// - `windowDimA`: Output. Array of dimension of at least `nbDimsRequested` that will be filled with the window parameters from the provided pooling descriptor.
    /// - `paddingA`: Output. Array of dimension of at least `nbDimsRequested` that will be filled with the padding parameters from the provided pooling descriptor.
    /// - `strideA`: Output. Array of dimension at least `nbDimsRequested` that will be filled with the stride parameters from the provided pooling descriptor.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: The parameter `nbDimsRequested` is greater than [`CUDNN_DIM_MAX`].
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The object was queried successfully.
    pub fn cudnnGetPoolingNdDescriptor(
        poolingDesc: cudnnPoolingDescriptor_t,
        nbDimsRequested: ::core::ffi::c_int,
        mode: *mut cudnnPoolingMode_t,
        maxpoolingNanOpt: *mut cudnnNanPropagation_t,
        nbDims: *mut ::core::ffi::c_int,
        windowDimA: *mut ::core::ffi::c_int,
        paddingA: *mut ::core::ffi::c_int,
        strideA: *mut ::core::ffi::c_int,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function provides the output dimensions of a tensor after `PoolingNd` has been applied.
    ///
    /// Each dimension of the `(nbDims-2)-D` images of the output tensor is computed as follows.
    ///
    /// # Parameters
    ///
    /// - `poolingDesc`: Input. Handle to a previously initialized pooling descriptor.
    /// - `nbDims`: Input. Number of dimensions in which pooling is to be applied.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * `poolingDesc` has not been initialized.
    /// * The value of `nbDims` is inconsistent with the dimensionality of `poolingDesc` and `inputDesc`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The function launched successfully.
    pub fn cudnnGetPoolingNdForwardOutputDim(
        poolingDesc: cudnnPoolingDescriptor_t,
        inputTensorDesc: cudnnTensorDescriptor_t,
        nbDims: ::core::ffi::c_int,
        outputTensorDimA: *mut ::core::ffi::c_int,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function provides the output dimensions of a tensor after `Pooling2d` has been applied.
    ///
    /// Each dimension `h` and `w` of the output images is computed as follows.
    ///
    /// # Parameters
    ///
    /// - `poolingDesc`: Input. Handle to a previously initialized pooling descriptor.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * `poolingDesc` has not been initialized.
    /// * `poolingDesc` or `inputDesc` has an invalid number of dimensions (2 and 4 respectively are required).
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The function launched successfully.
    pub fn cudnnGetPooling2dForwardOutputDim(
        poolingDesc: cudnnPoolingDescriptor_t,
        inputTensorDesc: cudnnTensorDescriptor_t,
        n: *mut ::core::ffi::c_int,
        c: *mut ::core::ffi::c_int,
        h: *mut ::core::ffi::c_int,
        w: *mut ::core::ffi::c_int,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function destroys a previously created pooling descriptor object.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The object was destroyed successfully.
    pub fn cudnnDestroyPoolingDescriptor(
        poolingDesc: cudnnPoolingDescriptor_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function computes pooling of input values (meaning, the maximum or average of several adjacent values) to produce an output with smaller height and/or width.
    ///
    /// All tensor formats are supported, best performance is expected when using HW-packed tensors. Only 2 and 3 spatial dimensions are allowed. Vectorized tensors are only supported if they have 2 spatial dimensions.
    ///
    /// The dimensions of the output tensor `yDesc` can be smaller or bigger than the dimensions advised by the routine [`cudnnGetPooling2dForwardOutputDim`] or [`cudnnGetPoolingNdForwardOutputDim`].
    ///
    /// For average pooling, the compute type is `float` even for integer input and output data type. Output round is nearest-even and clamp to the most negative or most positive value of type if out of range.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN context.
    /// - `poolingDesc`: Input. Handle to a previously initialized pooling descriptor.
    /// - `xDesc`: Input. Handle to the previously initialized input tensor descriptor. Must be of type `FLOAT`, `DOUBLE`, `HALF`, `INT8`, `INT8x4`, `INT8x32`, or `BFLOAT16`. For more information, refer to [`cudnnDataType_t`].
    /// - `x`: Input. Data pointer to GPU memory associated with the tensor descriptor `xDesc`.
    /// - `yDesc`: Input. Handle to the previously initialized output tensor descriptor. Must be of type `FLOAT`, `DOUBLE`, `HALF`, `INT8`, `INT8x4`, `INT8x32`, or `BFLOAT16`. For more information, refer to [`cudnnDataType_t`].
    /// - `y`: Output. Data pointer to GPU memory associated with the output tensor descriptor `yDesc`.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * The dimensions `n`, `c` of the input tensor and output tensors differ.
    /// * The datatype of the input tensor and output tensors differs.
    /// - [`cudnnStatus_t::CUDNN_STATUS_EXECUTION_FAILED`]: The function failed to launch on the GPU.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: The function does not support the provided configuration.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The function launched successfully.
    #[deprecated]
    pub fn cudnnPoolingForward(
        handle: cudnnHandle_t,
        poolingDesc: cudnnPoolingDescriptor_t,
        alpha: *const ::core::ffi::c_void,
        xDesc: cudnnTensorDescriptor_t,
        x: *const ::core::ffi::c_void,
        beta: *const ::core::ffi::c_void,
        yDesc: cudnnTensorDescriptor_t,
        y: *mut ::core::ffi::c_void,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function creates an activation descriptor object by allocating the memory needed to hold its opaque structure. For more information, refer to [`cudnnActivationDescriptor_t`].
    ///
    /// # Return value
    ///
    /// - `CUDNN_STATUS_ALLOC_FAILED`: The resources could not be allocated.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The object was created successfully.
    pub fn cudnnCreateActivationDescriptor(
        activationDesc: *mut cudnnActivationDescriptor_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function initializes a previously created generic activation descriptor object.
    ///
    /// # Parameters
    ///
    /// - `activationDesc`: Input/output. Handle to a previously created activation descriptor.
    /// - `mode`: Input. Enumerant to specify the activation mode.
    /// - `reluNanOpt`: Input. Enumerant to specify the `Nan` propagation mode.
    /// - `coef`: Input. Floating point number. When the activation mode (refer to [`cudnnActivationMode_t`]) is set to [`cudnnActivationMode_t::CUDNN_ACTIVATION_CLIPPED_RELU`], this input specifies the clipping threshold; and when the activation mode is set to [`cudnnActivationMode_t::CUDNN_ACTIVATION_RELU`], this input specifies the upper bound.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: `mode` or `reluNanOpt` has an invalid enumerant value.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The object was set successfully.
    pub fn cudnnSetActivationDescriptor(
        activationDesc: cudnnActivationDescriptor_t,
        mode: cudnnActivationMode_t,
        reluNanOpt: cudnnNanPropagation_t,
        coef: f64,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function queries a previously initialized generic activation descriptor object.
    ///
    /// # Parameters
    ///
    /// - `activationDesc`: Input. Handle to a previously created activation descriptor.
    /// - `mode`: Output. Enumerant to specify the activation mode.
    /// - `reluNanOpt`: Output. Enumerant to specify the Nan propagation mode.
    /// - `coef`: Output. Floating point number to specify the clipping threshold when the activation mode is set to [`cudnnActivationMode_t::CUDNN_ACTIVATION_CLIPPED_RELU`] or to specify the alpha coefficient when the activation mode is set to [`cudnnActivationMode_t::CUDNN_ACTIVATION_ELU`].
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The object was queried successfully.
    pub fn cudnnGetActivationDescriptor(
        activationDesc: cudnnActivationDescriptor_t,
        mode: *mut cudnnActivationMode_t,
        reluNanOpt: *mut cudnnNanPropagation_t,
        coef: *mut f64,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function sets the beta parameter of the SWISH activation function to `swish_beta`.
    ///
    /// # Parameters
    ///
    /// - `activationDesc`: Input/output. Handle to a previously created activation descriptor.
    /// - `swish_beta`: Input. The value to set the SWISH activations’ beta parameter to.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: The activation descriptor is a `NULL` pointer.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The value was set successfully.
    pub fn cudnnSetActivationDescriptorSwishBeta(
        activationDesc: cudnnActivationDescriptor_t,
        swish_beta: f64,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function queries the current beta parameter set for `SWISH` activation.
    ///
    /// # Parameters
    ///
    /// - `activationDesc`: Input. Handle to a previously created activation descriptor.
    /// - `swish_beta`: Output. Pointer to a double value that will receive the currently configured SWISH beta parameter.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of `activationDesc` or `swish_beta` were `NULL`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The beta parameter was queried successfully.
    pub fn cudnnGetActivationDescriptorSwishBeta(
        activationDesc: cudnnActivationDescriptor_t,
        swish_beta: *mut f64,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function destroys a previously created activation descriptor object.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The object was destroyed successfully.
    pub fn cudnnDestroyActivationDescriptor(
        activationDesc: cudnnActivationDescriptor_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This routine applies a specified neuron activation function element-wise over each input value.
    ///
    /// In-place operation is allowed for this routine; meaning, `xData` and `yData` pointers may be equal. However, this requires `xDesc` and `yDesc` descriptors to be identical (particularly, the strides of the input and output must match for an in-place operation to be allowed).
    ///
    /// All tensor formats are supported for 4 and 5 dimensions, however, the best performance is obtained when the strides of `xDesc` and `yDesc` are equal and HW-packed. For more than 5 dimensions the tensors must have their spatial dimensions packed.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN context. For more information, refer to [`cudnnHandle_t`].
    /// - `activationDesc`: Input. Activation descriptor. For more information, refer to [`cudnnActivationDescriptor_t`].
    /// - `xDesc`: Input. Handle to the previously initialized input tensor descriptor. For more information, refer to [`cudnnTensorDescriptor_t`].
    /// - `x`: Input. Data pointer to GPU memory associated with the tensor descriptor `xDesc`.
    /// - `yDesc`: Input. Handle to the previously initialized input tensor descriptor.
    /// - `y`: Input. Data pointer to GPU memory associated with the tensor descriptor `yDesc`.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * The parameter `mode` has an invalid enumerant value.
    /// * The dimensions `n`, `c`, `h`, `w` of the input tensor and output tensor differ.
    /// * The `datatype` of the input tensor and output tensor differs.
    /// * The strides `nStride`, `cStride`, `hStride`, `wStride` of the input tensor and output tensor differ and in-place operation is used (meaning, `x` and `y` pointers are equal).
    /// - [`cudnnStatus_t::CUDNN_STATUS_EXECUTION_FAILED`]: The function failed to launch on the GPU.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: The function does not support the provided configuration.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The function launched successfully.
    #[deprecated]
    pub fn cudnnActivationForward(
        handle: cudnnHandle_t,
        activationDesc: cudnnActivationDescriptor_t,
        alpha: *const ::core::ffi::c_void,
        xDesc: cudnnTensorDescriptor_t,
        x: *const ::core::ffi::c_void,
        beta: *const ::core::ffi::c_void,
        yDesc: cudnnTensorDescriptor_t,
        y: *mut ::core::ffi::c_void,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function allocates the memory needed to hold the data needed for LRN and `DivisiveNormalization` layers operation and returns a descriptor used with subsequent layer forward and backward calls.
    ///
    /// # Return value
    ///
    /// - `CUDNN_STATUS_ALLOC_FAILED`: The resources could not be allocated.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The object was created successfully.
    pub fn cudnnCreateLRNDescriptor(
        normDesc: *mut cudnnLRNDescriptor_t,
    ) -> cudnnStatus_t;
}
/// [`cudnnLRNMode_t`] is an enumerated type used to specify the mode of operation in [`cudnnLRNCrossChannelForward`] and [`cudnnLRNCrossChannelBackward`].
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnLRNMode_t {
    /// LRN computation is performed across the tensor’s dimension `dimA\[1\]`.
    CUDNN_LRN_CROSS_CHANNEL_DIM1 = 0,
}
unsafe extern "C" {
    /// This function initializes a previously created LRN descriptor object.
    ///
    /// # Parameters
    ///
    /// - `normDesc`: Output. Handle to a previously created LRN descriptor.
    /// - `lrnN`: Input. Normalization window width in elements. The LRN layer uses a window `\[center-lookBehind, center+lookAhead\]`, where `lookBehind = floor( (lrnN-1)/2 )`, `lookAhead = lrnN-lookBehind-1`. So for `n=10`, the window is `\[k-4...k...k+5\]` with a total of 10 samples. For the `DivisiveNormalization` layer, the window has the same extent as above in all spatial dimensions (`dimA\[2\]`, `dimA\[3\]`, `dimA\[4\]`). By default, `lrnN` is set to 5 in [`cudnnCreateLRNDescriptor`].
    /// - `lrnAlpha`: Input. Value of the alpha variance scaling parameter in the normalization formula. Inside the library code, this value is divided by the window width for LRN and by `(window width)^#spatialDimensions` for `DivisiveNormalization`. By default, this value is set to 1e-4 in [`cudnnCreateLRNDescriptor`].
    /// - `lrnBeta`: Input. Value of the beta power parameter in the normalization formula. By default, this value is set to `0.75` in [`cudnnCreateLRNDescriptor`].
    /// - `lrnK`: Input. Value of the `k` parameter in the normalization formula. By default, this value is set to `2.0`.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: One of the input parameters was out of valid range as described above.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The object was set successfully.
    pub fn cudnnSetLRNDescriptor(
        normDesc: cudnnLRNDescriptor_t,
        lrnN: ::core::ffi::c_uint,
        lrnAlpha: f64,
        lrnBeta: f64,
        lrnK: f64,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function retrieves values stored in the previously initialized `LRN` descriptor object.
    ///
    /// `lrnN`, `lrnAlpha`, `lrnBeta`, `lrnK`
    /// `Outputs`. Pointers to receive values of parameters stored in the descriptor object. For more information, refer to [`cudnnSetLRNDescriptor`]. Any of these pointers can be `NULL` (no value is returned for the corresponding parameter).
    ///
    /// # Parameters
    ///
    /// - `normDesc`: Output. Handle to a previously created LRN descriptor.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: Function completed successfully.
    pub fn cudnnGetLRNDescriptor(
        normDesc: cudnnLRNDescriptor_t,
        lrnN: *mut ::core::ffi::c_uint,
        lrnAlpha: *mut f64,
        lrnBeta: *mut f64,
        lrnK: *mut f64,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function destroys a previously created LRN descriptor object.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The object was destroyed successfully.
    pub fn cudnnDestroyLRNDescriptor(lrnDesc: cudnnLRNDescriptor_t) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function performs the forward LRN layer computation.
    ///
    /// Supported formats are: `positive-strided`, NCHW and NHWC for 4D `x` and `y`, and only NCDHW DHW-packed for 5D (for both `x` and `y`). Only non-overlapping 4D and 5D tensors are supported. NCHW layout is preferred for performance.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN library descriptor.
    /// - `normDesc`: Input. Handle to a previously initialized LRN parameter descriptor.
    /// - `lrnMode`: Input. LRN layer mode of operation. Currently only [`cudnnLRNMode_t::CUDNN_LRN_CROSS_CHANNEL_DIM1`] is implemented. Normalization is performed along the tensor’s `dimA\[1\]`.
    /// - `x`: Input. Input tensor data pointer in device memory.
    /// - `y`: Output. Output tensor data pointer in device memory.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * One of the tensor pointers `x`, `y` is `NULL`.
    /// * Number of input tensor dimensions is 2 or less.
    /// * LRN descriptor parameters are outside of their valid ranges.
    /// * One of the tensor parameters is 5D but is not in NCDHW DHW-packed format.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: The function does not support the provided configuration. Some examples include:
    ///
    /// * Any of the input tensor datatypes is not the same as any of the output tensor datatype.
    /// * `x` and `y` tensor dimensions mismatch.
    /// * Any tensor parameters strides are negative.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The computation was performed successfully.
    pub fn cudnnLRNCrossChannelForward(
        handle: cudnnHandle_t,
        normDesc: cudnnLRNDescriptor_t,
        lrnMode: cudnnLRNMode_t,
        alpha: *const ::core::ffi::c_void,
        xDesc: cudnnTensorDescriptor_t,
        x: *const ::core::ffi::c_void,
        beta: *const ::core::ffi::c_void,
        yDesc: cudnnTensorDescriptor_t,
        y: *mut ::core::ffi::c_void,
    ) -> cudnnStatus_t;
}
/// [`cudnnDivNormMode_t`] is an enumerated type used to specify the mode of operation in [`cudnnDivisiveNormalizationForward`] and [`cudnnDivisiveNormalizationBackward`].
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnDivNormMode_t {
    /// The means tensor data pointer is expected to contain means or other kernel convolution values precomputed by the user. The means pointer can also be `NULL`, in that case, it’s considered to be filled with zeroes. This is equivalent to spatial LRN.
    ///
    /// In the backward pass, the means are treated as independent inputs and the gradient over means is computed independently. In this mode, to yield a net gradient over the entire LCN computational graph, the `destDiffMeans` result should be backpropagated through the user’s means layer (which can be implemented using average pooling) and added to the `destDiffData` tensor produced by [`cudnnDivisiveNormalizationBackward`].
    CUDNN_DIVNORM_PRECOMPUTED_MEANS = 0,
}
unsafe extern "C" {
    /// This function performs the forward spatial `DivisiveNormalization` layer computation. It divides every value in a layer by the standard deviation of its spatial neighbors. Note that `DivisiveNormalization` only implements the `x/max(c, sigma_x)` portion of the computation, where `sigma_x` is the variance over the spatial neighborhood of `x`.
    ///
    /// The full LCN (Local Contrastive Normalization) computation can be implemented as a two-step process:
    ///
    /// The `x-mean(x)` which is often referred to as “subtractive normalization” portion of the computation can be implemented using cuDNN average pooling layer followed by a call to `addTensor`.
    ///
    /// Supported tensor formats are NCHW for 4D and NCDHW for 5D with any non-overlapping non-negative strides. Only 4D and 5D tensors are supported.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN library descriptor.
    /// - `normDesc`: Input. Handle to a previously initialized LRN parameter descriptor. This descriptor is used for both LRN and `DivisiveNormalization` layers.
    /// - `x`: Input. Input tensor data pointer in device memory.
    /// - `means`: Input. Input means tensor data pointer in device memory. Note that this tensor can be `NULL` (in that case its values are assumed to be zero during the computation). This tensor also doesn’t have to contain `means`, these can be any values, a frequently used variation is a result of convolution with a normalized positive kernel (such as Gaussian).
    /// - `y`: Output. Pointer in device memory to a tensor for the result of the forward `DivisiveNormalization` computation.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * One of the tensor pointers `x`, `y`, `temp`, and `temp2` is `NULL`.
    /// * Number of input tensor or output tensor dimensions is outside of \[4,5\] range.
    /// * A mismatch in dimensions between any two of the input or output tensors.
    /// * For in-place computation when pointers `x == y`, a mismatch in strides between the input data and output data tensors.
    /// * `alpha` or `beta` pointer is `NULL`.
    /// * LRN descriptor parameters are outside of their valid ranges.
    /// * Any of the tensor strides are negative.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The computation was performed successfully.
    /// - `CUDNN_STATUS_UNSUPPORTED`: The function does not support the provided configuration, for example, any of the input and output tensor strides mismatch (for the same dimension) is a non-supported configuration.
    pub fn cudnnDivisiveNormalizationForward(
        handle: cudnnHandle_t,
        normDesc: cudnnLRNDescriptor_t,
        mode: cudnnDivNormMode_t,
        alpha: *const ::core::ffi::c_void,
        xDesc: cudnnTensorDescriptor_t,
        x: *const ::core::ffi::c_void,
        means: *const ::core::ffi::c_void,
        temp: *mut ::core::ffi::c_void,
        temp2: *mut ::core::ffi::c_void,
        beta: *const ::core::ffi::c_void,
        yDesc: cudnnTensorDescriptor_t,
        y: *mut ::core::ffi::c_void,
    ) -> cudnnStatus_t;
}
/// [`cudnnBatchNormMode_t`] is an enumerated type used to specify the mode of operation in [`cudnnBatchNormalizationForwardInference`], [`cudnnBatchNormalizationForwardTraining`], [`cudnnBatchNormalizationBackward`], and [`cudnnDeriveBNTensorDescriptor`] routines.
///
/// **NCHW Mode Only**
/// This mode may use a scaled atomic integer reduction that is deterministic but imposes more restrictions on the input data range. When a numerical overflow occurs, the algorithm may produce NaN-s or Inf-s (infinity) in output buffers.
///
/// When Inf-s/NaN-s are present in the input data, the output in this mode is the same as from a pure floating-point implementation.
///
/// For finite but very large input values, the algorithm may encounter overflows more frequently due to a lower dynamic range and emit Inf-s/NaN-s while [`cudnnBatchNormMode_t::CUDNN_BATCHNORM_SPATIAL`] will produce finite results. The user can invoke [`cudnnQueryRuntimeError`] to check if a numerical overflow occurred in this mode.
#[deprecated]
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnBatchNormMode_t {
    /// Normalization is performed per-activation. This mode is intended to be used after the non-convolutional network layers. In this mode, the tensor dimensions of `bnBias` and `bnScale` and the parameters used in the `cudnnBatchNormalization*` functions are 1xCxHxW.
    #[deprecated]
    CUDNN_BATCHNORM_PER_ACTIVATION = 0,
    /// Normalization is performed over N+spatial dimensions. This mode is intended for use after convolutional layers (where spatial invariance is desired). In this mode, the `bnBias` and `bnScale` tensor dimensions are 1xCx1x1.
    #[deprecated]
    CUDNN_BATCHNORM_SPATIAL = 1,
    /// This mode is similar to [`cudnnBatchNormMode_t::CUDNN_BATCHNORM_SPATIAL`] but it can be faster for some tasks.
    ///
    /// An optimized path may be selected for [`cudnnDataType_t::CUDNN_DATA_FLOAT`] and [`cudnnDataType_t::CUDNN_DATA_HALF`] types, compute capability 6.0 or higher for the following two batch normalization API calls: [`cudnnBatchNormalizationForwardTraining`], and [`cudnnBatchNormalizationBackward`]. In the case of [`cudnnBatchNormalizationBackward`], the `savedMean` and `savedInvVariance` arguments should not be `NULL`.
    #[deprecated]
    CUDNN_BATCHNORM_SPATIAL_PERSISTENT = 2,
}
unsafe extern "C" {
    /// This function derives a secondary tensor descriptor for the batch normalization `scale`, `invVariance`, `bnBias`, and `bnScale` subtensors from the layer’s `x` data descriptor.
    ///
    /// Use the tensor descriptor produced by this function as the `bnScaleBiasMeanVarDesc` parameter for the [`cudnnBatchNormalizationForwardInference`] and [`cudnnBatchNormalizationForwardTraining`] functions, and as the `bnScaleBiasDiffDesc` parameter in the [`cudnnBatchNormalizationBackward`] function.
    ///
    /// The resulting dimensions will be:
    ///
    /// For `HALF` input data type the resulting tensor descriptor will have a `FLOAT` type. For other data types, it will have the same type as the input data.
    ///
    /// # Parameters
    ///
    /// - `derivedBnDesc`: Output. Handle to a previously created tensor descriptor.
    /// - `xDesc`: Input. Handle to a previously created and initialized layer’s `x` data descriptor.
    /// - `mode`: Input. Batch normalization layer mode of operation.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: Invalid batch normalization mode.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The computation was performed successfully.
    pub fn cudnnDeriveBNTensorDescriptor(
        derivedBnDesc: cudnnTensorDescriptor_t,
        xDesc: cudnnTensorDescriptor_t,
        mode: cudnnBatchNormMode_t,
    ) -> cudnnStatus_t;
}
/// [`cudnnBatchNormOps_t`] is an enumerated type used to specify the mode of operation in [`cudnnGetBatchNormalizationForwardTrainingExWorkspaceSize`], [`cudnnBatchNormalizationForwardTrainingEx`], [`cudnnGetBatchNormalizationBackwardExWorkspaceSize`], [`cudnnBatchNormalizationBackwardEx`], and [`cudnnGetBatchNormalizationTrainingExReserveSpaceSize`] functions.
#[deprecated]
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnBatchNormOps_t {
    /// Only batch normalization is performed, per-activation.
    #[deprecated]
    CUDNN_BATCHNORM_OPS_BN = 0,
    /// First, the batch normalization is performed, and then the activation is performed.
    #[deprecated]
    CUDNN_BATCHNORM_OPS_BN_ACTIVATION = 1,
    /// Performs the batch normalization, then element-wise addition, followed by the activation operation.
    #[deprecated]
    CUDNN_BATCHNORM_OPS_BN_ADD_ACTIVATION = 2,
}
unsafe extern "C" {
    /// This function performs the forward batch normalization layer computation for the inference phase. This layer is based on the [Batch Normalization: Accelerating Deep Network Training by Reducing Internal Covariate Shift paper](https://arxiv.org/abs/1502.03167).
    ///
    /// Only 4D and 5D tensors are supported.
    ///
    /// The input transformation performed by this function is defined as:
    ///
    /// The `epsilon` value has to be the same during training, backpropagation and inference.
    ///
    /// For the training phase, refer to [`cudnnBatchNormalizationForwardTraining`].
    ///
    /// Higher performance can be obtained when HW-packed tensors are used for all of `x` and `dx`.
    ///
    /// For more information, refer to [`cudnnDeriveBNTensorDescriptor`] for the secondary tensor descriptor generation for the parameters used in this function.
    ///
    /// **Supported Configurations**
    ///
    /// This function supports the following combinations of data types for various descriptors.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN library descriptor. For more information, refer to [`cudnnHandle_t`].
    /// - `mode`: Input. Mode of operation (spatial or per-activation). For more information, refer to [`cudnnBatchNormMode_t`].
    /// - `epsilon`: Input. Epsilon value used in batch normalization formula. Its value should be equal to or greater than the value defined for [`CUDNN_BN_MIN_EPSILON`] in `cudnn.h`. The same epsilon value should be used in forward and backward functions.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * One of the pointers `alpha`, `beta`, `x`, `y`, `bnScale`, `bnBias`, `estimatedMean`, and `estimatedInvVariance` is `NULL`.
    /// * The number of `xDesc` or `yDesc` tensor descriptor dimensions is not within the range of \[4,5\] (only 4D and 5D tensors are supported.)
    /// * `bnScaleBiasMeanVarDesc` dimensions are not 1xCx1x1 for 4D and 1xCx1x1x1 for 5D for spatial, and are not 1xCxHxW for 4D and 1xCxDxHxW for 5D for per-activation mode.
    /// * `epsilon` value is less than [`CUDNN_BN_MIN_EPSILON`].
    /// * Dimensions or data types mismatch for `xDesc`, `yDesc`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: The function does not support the provided configuration.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The computation was performed successfully.
    #[deprecated]
    pub fn cudnnBatchNormalizationForwardInference(
        handle: cudnnHandle_t,
        mode: cudnnBatchNormMode_t,
        alpha: *const ::core::ffi::c_void,
        beta: *const ::core::ffi::c_void,
        xDesc: cudnnTensorDescriptor_t,
        x: *const ::core::ffi::c_void,
        yDesc: cudnnTensorDescriptor_t,
        y: *mut ::core::ffi::c_void,
        bnScaleBiasMeanVarDesc: cudnnTensorDescriptor_t,
        bnScale: *const ::core::ffi::c_void,
        bnBias: *const ::core::ffi::c_void,
        estimatedMean: *const ::core::ffi::c_void,
        estimatedVariance: *const ::core::ffi::c_void,
        epsilon: f64,
    ) -> cudnnStatus_t;
}
/// [`cudnnNormMode_t`] is an enumerated type used to specify the mode of operation in [`cudnnNormalizationForwardInference`], [`cudnnNormalizationForwardTraining`], [`cudnnBatchNormalizationBackward`], [`cudnnGetNormalizationForwardTrainingWorkspaceSize`], [`cudnnGetNormalizationBackwardWorkspaceSize`], and [`cudnnGetNormalizationTrainingReserveSpaceSize`] routines.
#[deprecated]
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnNormMode_t {
    /// Normalization is performed per-activation. This mode is intended to be used after the non-convolutional network layers. In this mode, the tensor dimensions of `normBias` and `normScale` and the parameters used in the `cudnnNormalization*` functions are 1xCxHxW.
    #[deprecated]
    CUDNN_NORM_PER_ACTIVATION = 0,
    /// Normalization is performed per-channel over N+spatial dimensions. This mode is intended for use after convolutional layers (where spatial invariance is desired). In this mode, the `normBias` and `normScale` tensor dimensions are 1xCx1x1.
    #[deprecated]
    CUDNN_NORM_PER_CHANNEL = 1,
}
/// [`cudnnNormAlgo_t`] is an enumerated type used to specify the algorithm to execute the normalization operation.
///
/// **NCHW Mode Only**
/// This mode may use a scaled atomic integer reduction that is deterministic but imposes more restrictions on the input data range. When a numerical overflow occurs, the algorithm may produce NaN-s or Inf-s (infinity) in output buffers.
///
/// When Inf-s/NaN-s are present in the input data, the output in this mode is the same as from a pure floating-point implementation.
///
/// For finite but very large input values, the algorithm may encounter overflows more frequently due to a lower dynamic range and emit Inf-s/NaN-s while [`cudnnNormAlgo_t::CUDNN_NORM_ALGO_STANDARD`] will produce finite results. The user can invoke [`cudnnQueryRuntimeError`] to check if a numerical overflow occurred in this mode.
#[deprecated]
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnNormAlgo_t {
    /// Standard normalization is performed.
    #[deprecated]
    CUDNN_NORM_ALGO_STANDARD = 0,
    /// This mode is similar to [`cudnnNormAlgo_t::CUDNN_NORM_ALGO_STANDARD`], however it only supports [`cudnnNormMode_t::CUDNN_NORM_PER_CHANNEL`] and can be faster for some tasks.
    ///
    /// An optimized path may be selected for [`cudnnDataType_t::CUDNN_DATA_FLOAT`] and [`cudnnDataType_t::CUDNN_DATA_HALF`] types, compute capability 6.0 or higher for the following two normalization API calls: [`cudnnNormalizationForwardTraining`] and [`cudnnNormalizationBackward`]. In the case of [`cudnnNormalizationBackward`], the `savedMean` and `savedInvVariance` arguments should not be `NULL`.
    #[deprecated]
    CUDNN_NORM_ALGO_PERSIST = 1,
}
unsafe extern "C" {
    /// This function derives tensor descriptors for the normalization `mean`, `invariance`, `normBias`, and `normScale` subtensors from the layer’s `x` data descriptor and norm mode. Normalization `mean` and `invariance` share the same descriptor while `bias` and `scale` share the same descriptor.
    ///
    /// Use the tensor descriptor produced by this function as the `normScaleBiasDesc` or `normMeanVarDesc` parameter for the [`cudnnNormalizationForwardInference`] and [`cudnnNormalizationForwardTraining`] functions, and as the `dNormScaleBiasDesc` and `normMeanVarDesc` parameters in the [`cudnnNormalizationBackward`] function.
    ///
    /// The resulting dimensions will be:
    ///
    /// For `HALF` input data type the resulting tensor descriptor will have a `FLOAT` type. For other data types, it will have the same type as the input data.
    ///
    /// # Parameters
    ///
    /// - `derivedNormScaleBiasDesc`: Output. Handle to a previously created tensor descriptor.
    /// - `derivedNormMeanVarDesc`: Output. Handle to a previously created tensor descriptor.
    /// - `xDesc`: Input. Handle to a previously created and initialized layer’s `x` data descriptor.
    /// - `mode`: Input. Batch normalization layer mode of operation.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: Invalid batch normalization mode.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The computation was performed successfully.
    pub fn cudnnDeriveNormTensorDescriptor(
        derivedNormScaleBiasDesc: cudnnTensorDescriptor_t,
        derivedNormMeanVarDesc: cudnnTensorDescriptor_t,
        xDesc: cudnnTensorDescriptor_t,
        mode: cudnnNormMode_t,
        groupCnt: ::core::ffi::c_int,
    ) -> cudnnStatus_t;
}
/// [`cudnnNormOps_t`] is an enumerated type used to specify the mode of operation in [`cudnnGetNormalizationForwardTrainingWorkspaceSize`], [`cudnnNormalizationForwardTraining`], [`cudnnGetNormalizationBackwardWorkspaceSize`], [`cudnnNormalizationBackward`], and [`cudnnGetNormalizationTrainingReserveSpaceSize`] functions.
#[deprecated]
#[repr(u32)]
#[derive(Debug, Copy, Clone, Hash, PartialOrd, Ord, PartialEq, Eq)]
pub enum cudnnNormOps_t {
    /// Only normalization is performed.
    #[deprecated]
    CUDNN_NORM_OPS_NORM = 0,
    /// First, the normalization is performed, then the activation is performed.
    #[deprecated]
    CUDNN_NORM_OPS_NORM_ACTIVATION = 1,
    /// Performs the normalization, then element-wise addition, followed by the activation operation.
    #[deprecated]
    CUDNN_NORM_OPS_NORM_ADD_ACTIVATION = 2,
}
unsafe extern "C" {
    /// This function performs the forward normalization layer computation for the inference phase. Per-channel normalization layer is based on the [Batch Normalization: Accelerating Deep Network Training by Reducing Internal Covariate Shift paper](https://arxiv.org/abs/1502.03167).
    ///
    /// Only 4D and 5D tensors are supported.
    ///
    /// The input transformation performed by this function is defined as:
    ///
    /// The `epsilon` value has to be the same during training, backpropagation, and inference.
    ///
    /// For the training phase, refer to [`cudnnNormalizationForwardTraining`].
    ///
    /// Higher performance can be obtained when HW-packed tensors are used for all of `x` and `y`.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN library descriptor. For more information, refer to [`cudnnHandle_t`].
    /// - `mode`: Input. Mode of operation (per-channel or per-activation). For more information, refer to [`cudnnNormMode_t`].
    /// - `normOps`: Input. Mode of post-operative. Currently [`cudnnNormOps_t::CUDNN_NORM_OPS_NORM_ACTIVATION`] and [`cudnnNormOps_t::CUDNN_NORM_OPS_NORM_ADD_ACTIVATION`] are only supported in the NHWC layout. For more information, refer to [`cudnnNormOps_t`]. This input can be used to set this function to perform either only the normalization, or normalization followed by activation, or normalization followed by element-wise addition and then activation.
    /// - `algo`: Input. Algorithm to be performed. For more information, refer to [`cudnnNormAlgo_t`].
    /// - `activationDesc`: Input. Descriptor for the activation operation. When the `normOps` input is set to either [`cudnnNormOps_t::CUDNN_NORM_OPS_NORM_ACTIVATION`] or [`cudnnNormOps_t::CUDNN_NORM_OPS_NORM_ADD_ACTIVATION`] then this activation is used, otherwise the user may pass `NULL`. Since `normOps` is only supported for [`cudnnNormOps_t::CUDNN_NORM_OPS_NORM`], we can set these to `NULL` for now.
    /// - `epsilon`: Input. Epsilon value used in the normalization formula. Its value should be equal to or greater than zero.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * One of the pointers `alpha`, `beta`, `x`, `y`, `normScale`, `normBias`, `estimatedMean`, and `estimatedInvVariance` is `NULL`.
    /// * The number of `xDesc` or `yDesc` tensor descriptor dimensions is not within the range of \[4,5\] (only 4D and 5D tensors are supported).
    /// * `normScaleBiasDesc` and `normMeanVarDesc` dimensions are not 1xCx1x1 for 4D and 1xCx1x1x1 for 5D for per-channel, and are not 1xCxHxW for 4D and 1xCxDxHxW for 5D for per-activation mode.
    /// * `epsilon` value is less than zero.
    /// * Dimensions or data types mismatch for `xDesc` and `yDesc`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: A compute or data type other than what is supported was chosen, or an unknown algorithm type was chosen.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The computation was performed successfully.
    #[deprecated]
    pub fn cudnnNormalizationForwardInference(
        handle: cudnnHandle_t,
        mode: cudnnNormMode_t,
        normOps: cudnnNormOps_t,
        algo: cudnnNormAlgo_t,
        alpha: *const ::core::ffi::c_void,
        beta: *const ::core::ffi::c_void,
        xDesc: cudnnTensorDescriptor_t,
        x: *const ::core::ffi::c_void,
        normScaleBiasDesc: cudnnTensorDescriptor_t,
        normScale: *const ::core::ffi::c_void,
        normBias: *const ::core::ffi::c_void,
        normMeanVarDesc: cudnnTensorDescriptor_t,
        estimatedMean: *const ::core::ffi::c_void,
        estimatedVariance: *const ::core::ffi::c_void,
        zDesc: cudnnTensorDescriptor_t,
        z: *const ::core::ffi::c_void,
        activationDesc: cudnnActivationDescriptor_t,
        yDesc: cudnnTensorDescriptor_t,
        y: *mut ::core::ffi::c_void,
        epsilon: f64,
        groupCnt: ::core::ffi::c_int,
    ) -> cudnnStatus_t;
}
/// [`cudnnSamplerType_t`] is an enumerated type passed to [`cudnnSetSpatialTransformerNdDescriptor`] to select the sampler type to be used by [`cudnnSpatialTfSamplerForward`] and [`cudnnSpatialTfSamplerBackward`].
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnSamplerType_t {
    /// Selects the bilinear sampler.
    CUDNN_SAMPLER_BILINEAR = 0,
}
unsafe extern "C" {
    /// This function creates a generic spatial transformer descriptor object by allocating the memory needed to hold its opaque structure.
    ///
    /// # Return value
    ///
    /// - `CUDNN_STATUS_ALLOC_FAILED`: The resources could not be allocated.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The object was created successfully.
    pub fn cudnnCreateSpatialTransformerDescriptor(
        stDesc: *mut cudnnSpatialTransformerDescriptor_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function initializes a previously created generic spatial transformer descriptor object.
    ///
    /// # Parameters
    ///
    /// - `stDesc`: Input/output. Previously created spatial transformer descriptor object.
    /// - `samplerType`: Input. Enumerant to specify the sampler type.
    /// - `dataType`: Input. Data type.
    /// - `nbDims`: Input. Dimension of the transformed tensor.
    /// - `dimA`: Input. Array of dimension `nbDims` containing the size of the transformed tensor for every dimension.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * Either `stDesc` or `dimA` is `NULL`.
    /// * Either `dataType` or `samplerType` has an invalid enumerant value.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The call was successful.
    pub fn cudnnSetSpatialTransformerNdDescriptor(
        stDesc: cudnnSpatialTransformerDescriptor_t,
        samplerType: cudnnSamplerType_t,
        dataType: cudnnDataType_t,
        nbDims: ::core::ffi::c_int,
        dimA: *const ::core::ffi::c_int,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function destroys a previously created spatial transformer descriptor object.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The object was destroyed successfully.
    pub fn cudnnDestroySpatialTransformerDescriptor(
        stDesc: cudnnSpatialTransformerDescriptor_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function generates a grid of coordinates in the input tensor corresponding to each pixel from the output tensor.
    ///
    /// Only 2D transformation is supported.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN context.
    /// - `stDesc`: Input. Previously created spatial transformer descriptor object.
    /// - `theta`: Input. Affine transformation matrix. It should be of size `n*2*3` for a 2D transformation, where `n` is the number of images specified in `stDesc`.
    /// - `grid`: Output. A grid of coordinates. It is of size `n*h*w*2` for a 2D transformation, where `n`, `h`, `w` is specified in `stDesc`. In the 4th dimension, the first coordinate is `x`, and the second coordinate is `y`.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * `handle` is `NULL`.
    /// * One of the parameters `grid` or `theta` is `NULL`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_EXECUTION_FAILED`]: The function failed to launch on the GPU.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: The function does not support the provided configuration. Some examples include:
    ///
    /// * The dimension of the transformed tensor specified in `stDesc` > 4.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The call was successful.
    pub fn cudnnSpatialTfGridGeneratorForward(
        handle: cudnnHandle_t,
        stDesc: cudnnSpatialTransformerDescriptor_t,
        theta: *const ::core::ffi::c_void,
        grid: *mut ::core::ffi::c_void,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function performs a sampler operation and generates the output tensor using the grid given by the grid generator.
    ///
    /// Only 2D transformation is supported.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN context.
    /// - `stDesc`: Input. Previously created spatial transformer descriptor object.
    /// - `xDesc`: Input. Handle to the previously initialized input tensor descriptor.
    /// - `x`: Input. Data pointer to GPU memory associated with the tensor descriptor `xDesc`.
    /// - `grid`: Input. A grid of coordinates generated by [`cudnnSpatialTfGridGeneratorForward`].
    /// - `yDesc`: Input. Handle to the previously initialized output tensor descriptor.
    /// - `y`: Output. Data pointer to GPU memory associated with the output tensor descriptor `yDesc`.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * `handle` is `NULL`.
    /// * One of the parameters `x`, `y`, or `grid` is `NULL`.
    /// * The dimension of `dy` differs from those specified in `stDesc`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_EXECUTION_FAILED`]: The function failed to launch on the GPU.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: The function does not support the provided configuration. Some examples include:
    ///
    /// * The dimension of transformed tensor > 4.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The call was successful.
    pub fn cudnnSpatialTfSamplerForward(
        handle: cudnnHandle_t,
        stDesc: cudnnSpatialTransformerDescriptor_t,
        alpha: *const ::core::ffi::c_void,
        xDesc: cudnnTensorDescriptor_t,
        x: *const ::core::ffi::c_void,
        grid: *const ::core::ffi::c_void,
        beta: *const ::core::ffi::c_void,
        yDesc: cudnnTensorDescriptor_t,
        y: *mut ::core::ffi::c_void,
    ) -> cudnnStatus_t;
}
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct cudnnDropoutStruct {
    _unused: [u8; 0],
}
/// [`cudnnDropoutDescriptor_t`] is a pointer to an opaque structure holding the description of a dropout operation. [`cudnnCreateDropoutDescriptor`] is used to create one instance, [`cudnnSetDropoutDescriptor`] is used to initialize this instance, [`cudnnDestroyDropoutDescriptor`] is used to destroy this instance, [`cudnnGetDropoutDescriptor`] is used to query fields of a previously initialized instance, [`cudnnRestoreDropoutDescriptor`] is used to restore an instance to a previously saved off state.
pub type cudnnDropoutDescriptor_t = *mut cudnnDropoutStruct;
unsafe extern "C" {
    /// This function creates a generic dropout descriptor object by allocating the memory needed to hold its opaque structure. For more information, refer to [`cudnnDropoutDescriptor_t`].
    ///
    /// # Return value
    ///
    /// - `CUDNN_STATUS_ALLOC_FAILED`: The resources could not be allocated.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The object was created successfully.
    pub fn cudnnCreateDropoutDescriptor(
        dropoutDesc: *mut cudnnDropoutDescriptor_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function destroys a previously created dropout descriptor object.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The object was destroyed successfully.
    pub fn cudnnDestroyDropoutDescriptor(
        dropoutDesc: cudnnDropoutDescriptor_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function is used to query the amount of space required to store the states of the random number generators used by the [`cudnnDropoutForward`] function.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN context.
    /// - `sizeInBytes`: Output. Amount of GPU memory needed to store random generator states.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The query was successful.
    pub fn cudnnDropoutGetStatesSize(
        handle: cudnnHandle_t,
        sizeInBytes: *mut size_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function is used to query the amount of reserve needed to run dropout with the input dimensions given by `xDesc`. The same reserve space is expected to be passed to [`cudnnDropoutForward`] and [`cudnnDropoutBackward`], and its contents is expected to remain unchanged between [`cudnnDropoutForward`] and [`cudnnDropoutBackward`] calls.
    ///
    /// # Parameters
    ///
    /// - `sizeInBytes`: Output. Amount of GPU memory needed as reserve space to be able to run dropout with an input tensor descriptor specified by `xDesc`.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The query was successful.
    pub fn cudnnDropoutGetReserveSpaceSize(
        xdesc: cudnnTensorDescriptor_t,
        sizeInBytes: *mut size_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function initializes a previously created dropout descriptor object. If the `states` argument is equal to `NULL`, then the random number generator states won’t be initialized, and only the `dropout` value will be set. The user is expected not to change the memory pointed at by `states` for the duration of the computation.
    ///
    /// When the `states` argument is not `NULL`, a cuRAND initialization kernel is invoked by [`cudnnSetDropoutDescriptor`]. This kernel requires a substantial amount of GPU memory for the stack. Memory is released when the kernel finishes. The `CUDNN_STATUS_ALLOC_FAILED` status is returned when no sufficient free memory is available for the GPU stack.
    ///
    /// # Parameters
    ///
    /// - `dropoutDesc`: Input/output. Previously created dropout descriptor object.
    /// - `handle`: Input. Handle to a previously created cuDNN context.
    /// - `dropout`: Input. The probability with which the value from input is set to zero during the dropout layer.
    /// - `states`: Output. Pointer to user-allocated GPU memory that will hold random number generator states.
    /// - `stateSizeInBytes`: Input. Specifies the size in bytes of the provided memory for the states.
    /// - `seed`: Input. Seed used to initialize random number generator states.
    ///
    /// # Return value
    ///
    /// - `CUDNN_STATUS_ALLOC_FAILED`: The function failed to temporarily extend the GPU stack.
    /// - [`cudnnStatus_t::CUDNN_STATUS_EXECUTION_FAILED`]: The function failed to launch on the GPU.
    /// - [`cudnnStatus_t::CUDNN_STATUS_INTERNAL_ERROR`]: Internally used CUDA functions returned an error status.
    /// - [`cudnnStatus_t::CUDNN_STATUS_INVALID_VALUE`]: The `sizeInBytes` argument is less than the value returned by [`cudnnDropoutGetStatesSize`].
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The call was successful.
    pub fn cudnnSetDropoutDescriptor(
        dropoutDesc: cudnnDropoutDescriptor_t,
        handle: cudnnHandle_t,
        dropout: f32,
        states: *mut ::core::ffi::c_void,
        stateSizeInBytes: size_t,
        seed: ::core::ffi::c_ulonglong,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function restores a dropout descriptor to a previously saved-off state.
    ///
    /// # Parameters
    ///
    /// - `dropoutDesc`: Input/output. Previously created dropout descriptor.
    /// - `handle`: Input. Handle to a previously created cuDNN context.
    /// - `dropout`: Input. Probability with which the value from an input tensor is set to 0 when performing dropout.
    /// - `states`: Input. Pointer to GPU memory that holds random number generator states initialized by a prior call to [`cudnnSetDropoutDescriptor`].
    /// - `stateSizeInBytes`: Input. Size in bytes of buffer holding random number generator `states`.
    /// - `seed`: Input. Seed used in prior calls to [`cudnnSetDropoutDescriptor`] that initialized `states` buffer. Using a different seed from this has no effect. A change of seed, and subsequent update to random number generator states can be achieved by calling [`cudnnSetDropoutDescriptor`].
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_INVALID_VALUE`]: The `states` buffer size (as indicated in `stateSizeInBytes`) is too small.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The call was successful.
    pub fn cudnnRestoreDropoutDescriptor(
        dropoutDesc: cudnnDropoutDescriptor_t,
        handle: cudnnHandle_t,
        dropout: f32,
        states: *mut ::core::ffi::c_void,
        stateSizeInBytes: size_t,
        seed: ::core::ffi::c_ulonglong,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function queries the fields of a previously initialized dropout descriptor.
    ///
    /// # Parameters
    ///
    /// - `dropoutDesc`: Input. Previously initialized dropout descriptor.
    /// - `handle`: Input. Handle to a previously created cuDNN context.
    /// - `dropout`: Output. The probability with which the value from input is set to 0 during the dropout layer.
    /// - `states`: Output. Pointer to user-allocated GPU memory that holds random number generator states.
    /// - `seed`: Output. Seed used to initialize random number generator states.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: One or more of the arguments was an invalid pointer.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The call was successful.
    pub fn cudnnGetDropoutDescriptor(
        dropoutDesc: cudnnDropoutDescriptor_t,
        handle: cudnnHandle_t,
        dropout: *mut f32,
        states: *mut *mut ::core::ffi::c_void,
        seed: *mut ::core::ffi::c_ulonglong,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function performs forward dropout operation over `x` returning results in `y`. If dropout was used as a parameter to [`cudnnSetDropoutDescriptor`], the approximate `dropout` fraction of `x` values will be replaced by a `0`, and the rest will be scaled by `1/(1-dropout)`. This function should not be running concurrently with another [`cudnnDropoutForward`] function using the same states.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN context.
    /// - `dropoutDesc`: Input. Previously created dropout descriptor object.
    /// - `x`: Input. Pointer to data of the tensor described by the `xDesc` descriptor.
    /// - `y`: Output. Pointer to data of the tensor described by the `yDesc` descriptor.
    /// - `reserveSpace`: Output. Pointer to user-allocated GPU memory used by this function. It is expected that the contents of `reserveSpace` does not change between [`cudnnDropoutForward`] and [`cudnnDropoutBackward`] calls.
    /// - `reserveSpaceSizeInBytes`: Input. Specifies the size in bytes of the provided memory for the reserve space.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * The number of elements of input tensor and output tensors differ.
    /// * The `datatype` of the input tensor and output tensors differs.
    /// * The strides of the input tensor and output tensors differ and in-place operation is used (meaning, `x` and `y` pointers are equal).
    /// * The provided `reserveSpaceSizeInBytes` is less than the value returned by [`cudnnDropoutGetReserveSpaceSize`].
    /// * [`cudnnSetDropoutDescriptor`] has not been called on `dropoutDesc` with non-`NULL` states argument.
    /// - [`cudnnStatus_t::CUDNN_STATUS_EXECUTION_FAILED`]: The function failed to launch on the GPU.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: The function does not support the provided configuration.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The call was successful.
    pub fn cudnnDropoutForward(
        handle: cudnnHandle_t,
        dropoutDesc: cudnnDropoutDescriptor_t,
        xdesc: cudnnTensorDescriptor_t,
        x: *const ::core::ffi::c_void,
        ydesc: cudnnTensorDescriptor_t,
        y: *mut ::core::ffi::c_void,
        reserveSpace: *mut ::core::ffi::c_void,
        reserveSpaceSizeInBytes: size_t,
    ) -> cudnnStatus_t;
}
/// [`cudnnConvolutionFwdAlgo_t`] is an enumerated type that exposes the different algorithms available to execute the forward convolution operation.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnConvolutionFwdAlgo_t {
    /// This algorithm expresses the convolution as a matrix product without actually explicitly forming the matrix that holds the input tensor data.
    CUDNN_CONVOLUTION_FWD_ALGO_IMPLICIT_GEMM = 0,
    CUDNN_CONVOLUTION_FWD_ALGO_IMPLICIT_PRECOMP_GEMM = 1,
    /// This algorithm expresses convolution as an explicit matrix product. A significant memory workspace is needed to store the matrix that holds the input tensor data.
    CUDNN_CONVOLUTION_FWD_ALGO_GEMM = 2,
    /// This algorithm expresses the convolution as a direct convolution (for example, without implicitly or explicitly doing a matrix multiplication).
    CUDNN_CONVOLUTION_FWD_ALGO_DIRECT = 3,
    /// This algorithm uses the Fast-Fourier Transform approach to compute the convolution. A significant memory workspace is needed to store intermediate results.
    CUDNN_CONVOLUTION_FWD_ALGO_FFT = 4,
    /// This algorithm uses the Fast-Fourier Transform approach but splits the inputs into tiles. A significant memory workspace is needed to store intermediate results but less than [`cudnnConvolutionFwdAlgo_t::CUDNN_CONVOLUTION_FWD_ALGO_FFT`] for large size images.
    CUDNN_CONVOLUTION_FWD_ALGO_FFT_TILING = 5,
    /// This algorithm uses the Winograd Transform approach to compute the convolution. A reasonably sized workspace is needed to store intermediate results.
    ///
    /// # Note
    ///
    /// The [`cudnnConvolutionFwdAlgo_t::CUDNN_CONVOLUTION_FWD_ALGO_WINOGRAD`] algorithm is **not** supported on GPUs based on the NVIDIA Hopper GPU architecture and later GPU architectures.
    CUDNN_CONVOLUTION_FWD_ALGO_WINOGRAD = 6,
    /// This algorithm uses the Winograd Transform approach to compute the convolution. A significant workspace may be needed to store intermediate results.
    CUDNN_CONVOLUTION_FWD_ALGO_WINOGRAD_NONFUSED = 7,
    CUDNN_CONVOLUTION_FWD_ALGO_COUNT = 8,
}
/// [`cudnnConvolutionBwdFilterAlgo_t`] is an enumerated type that exposes the different algorithms available to execute the backward filter convolution operation.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnConvolutionBwdFilterAlgo_t {
    /// This algorithm expresses the convolution as a sum of matrix products without actually explicitly forming the matrix that holds the input tensor data. The sum is done using the atomic add operation, thus the results are non-deterministic.
    CUDNN_CONVOLUTION_BWD_FILTER_ALGO_0 = 0,
    /// This algorithm expresses the convolution as a matrix product without actually explicitly forming the matrix that holds the input tensor data. The results are deterministic.
    CUDNN_CONVOLUTION_BWD_FILTER_ALGO_1 = 1,
    /// This algorithm uses the Fast-Fourier Transform approach to compute the convolution. A significant workspace is needed to store intermediate results. The results are deterministic.
    CUDNN_CONVOLUTION_BWD_FILTER_ALGO_FFT = 2,
    /// This algorithm is similar to [`cudnnConvolutionBwdFilterAlgo_t::CUDNN_CONVOLUTION_BWD_FILTER_ALGO_0`] but uses some small workspace to precompute some indices. The results are also non-deterministic.
    CUDNN_CONVOLUTION_BWD_FILTER_ALGO_3 = 3,
    CUDNN_CONVOLUTION_BWD_FILTER_ALGO_WINOGRAD = 4,
    CUDNN_CONVOLUTION_BWD_FILTER_ALGO_WINOGRAD_NONFUSED = 5,
    /// This algorithm uses the Fast-Fourier Transform approach to compute the convolution but splits the input tensor into tiles. A significant workspace may be needed to store intermediate results. The results are deterministic.
    CUDNN_CONVOLUTION_BWD_FILTER_ALGO_FFT_TILING = 6,
    CUDNN_CONVOLUTION_BWD_FILTER_ALGO_COUNT = 7,
}
/// [`cudnnConvolutionBwdDataAlgo_t`] is an enumerated type that exposes the different algorithms available to execute the backward data convolution operation.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnConvolutionBwdDataAlgo_t {
    /// This algorithm expresses the convolution as a sum of matrix products without actually explicitly forming the matrix that holds the input tensor data. The sum is done using the atomic add operation, thus the results are non-deterministic.
    CUDNN_CONVOLUTION_BWD_DATA_ALGO_0 = 0,
    /// This algorithm expresses the convolution as a matrix product without actually explicitly forming the matrix that holds the input tensor data. The results are deterministic.
    CUDNN_CONVOLUTION_BWD_DATA_ALGO_1 = 1,
    /// This algorithm uses a Fast-Fourier Transform approach to compute the convolution. A significant memory workspace is needed to store intermediate results. The results are deterministic.
    CUDNN_CONVOLUTION_BWD_DATA_ALGO_FFT = 2,
    /// This algorithm uses the Fast-Fourier Transform approach but splits the inputs into tiles. A significant memory workspace is needed to store intermediate results but less than [`cudnnConvolutionBwdDataAlgo_t::CUDNN_CONVOLUTION_BWD_DATA_ALGO_FFT`] for large size images. The results are deterministic.
    CUDNN_CONVOLUTION_BWD_DATA_ALGO_FFT_TILING = 3,
    /// This algorithm uses the Winograd Transform approach to compute the convolution. A reasonably sized workspace is needed to store intermediate results. The results are deterministic.
    ///
    /// # Note
    ///
    /// The [`cudnnConvolutionBwdDataAlgo_t::CUDNN_CONVOLUTION_BWD_DATA_ALGO_WINOGRAD`] algorithm is **not** supported on GPUs based on the NVIDIA Hopper GPU architecture and later GPU architectures.
    CUDNN_CONVOLUTION_BWD_DATA_ALGO_WINOGRAD = 4,
    /// This algorithm uses the Winograd Transform approach to compute the convolution. A significant workspace may be needed to store intermediate results. The results are deterministic.
    CUDNN_CONVOLUTION_BWD_DATA_ALGO_WINOGRAD_NONFUSED = 5,
    CUDNN_CONVOLUTION_BWD_DATA_ALGO_COUNT = 6,
}
/// [`cudnnCTCLossAlgo_t`] is an enumerated type that exposes the different algorithms available to execute the CTC loss operation.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnCTCLossAlgo_t {
    /// Results are guaranteed to be reproducible.
    CUDNN_CTC_LOSS_ALGO_DETERMINISTIC = 0,
    /// Results are not guaranteed to be reproducible.
    CUDNN_CTC_LOSS_ALGO_NON_DETERMINISTIC = 1,
}
unsafe extern "C" {
    /// Cross-library version checker. Each sublibrary has a version checker that checks whether its own version matches that of its dependencies.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUBLIBRARY_VERSION_MISMATCH`]: The versions are inconsistent.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The version check passed.
    pub fn cudnnOpsVersionCheck() -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This routine computes the gradient of the softmax function.
    ///
    /// In-place operation is allowed for this routine; meaning, `dy` and `dx` pointers may be equal. However, this requires `dyDesc` and `dxDesc` descriptors to be identical (particularly, the strides of the input and output must match for in-place operation to be allowed).
    ///
    /// All tensor formats are supported for all modes and algorithms with 4 and 5D tensors. Performance is expected to be highest with NCHW fully-packed tensors. For more than 5 dimensions tensors must be packed in their spatial dimensions.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN context.
    /// - `mode`: Input. Enumerant to specify the softmax mode.
    /// - `yDesc`: Input. Handle to the previously initialized input tensor descriptor.
    /// - `y`: Input. Data pointer to GPU memory associated with the tensor descriptor `yDesc`.
    /// - `dyDesc`: Input. Handle to the previously initialized input differential tensor descriptor.
    /// - `dy`: Input. Data pointer to GPU memory associated with the tensor descriptor `dyData`.
    /// - `dxDesc`: Input. Handle to the previously initialized output differential tensor descriptor.
    /// - `dx`: Output. Data pointer to GPU memory associated with the output tensor descriptor `dxDesc`.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * The dimensions `n`, `c`, `h`, `w` of the `yDesc`, `dyDesc` and `dxDesc` tensors differ.
    /// * The strides `nStride`, `cStride`, `hStride`, `wStride` of the `yDesc` and `dyDesc` tensors differ.
    /// * The `datatype` of the three tensors differs.
    /// - [`cudnnStatus_t::CUDNN_STATUS_EXECUTION_FAILED`]: The function failed to launch on the GPU.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: The function does not support the provided configuration.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The function launched successfully.
    pub fn cudnnSoftmaxBackward(
        handle: cudnnHandle_t,
        algo: cudnnSoftmaxAlgorithm_t,
        mode: cudnnSoftmaxMode_t,
        alpha: *const ::core::ffi::c_void,
        yDesc: cudnnTensorDescriptor_t,
        y: *const ::core::ffi::c_void,
        dyDesc: cudnnTensorDescriptor_t,
        dy: *const ::core::ffi::c_void,
        beta: *const ::core::ffi::c_void,
        dxDesc: cudnnTensorDescriptor_t,
        dx: *mut ::core::ffi::c_void,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function computes the gradient of a pooling operation.
    ///
    /// As of cuDNN version 6.0, a deterministic algorithm is implemented for max backwards pooling. This algorithm can be chosen via the pooling mode enum of `poolingDesc`. The deterministic algorithm has been measured to be up to 50% slower than the legacy max backwards pooling algorithm, or up to 20% faster, depending upon the use case.
    ///
    /// Tensor vectorization is not supported for any tensor descriptor arguments in this function. Best performance is expected when using HW-packed tensors. Only 2 and 3 spatial dimensions are supported.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN context.
    /// - `poolingDesc`: Input. Handle to the previously initialized pooling descriptor.
    /// - `yDesc`: Input. Handle to the previously initialized input tensor descriptor. Can be `NULL` for avg pooling.
    /// - `y`: Input. Data pointer to GPU memory associated with the tensor descriptor `yDesc`. Can be `NULL` for avg pooling.
    /// - `dyDesc`: Input. Handle to the previously initialized input differential tensor descriptor. Must be of type `FLOAT`, `DOUBLE`, `HALF`, or `BFLOAT16`. For more information, refer to [`cudnnDataType_t`].
    /// - `dy`: Input. Data pointer to GPU memory associated with the tensor descriptor `dyData`.
    /// - `xDesc`: Input. Handle to the previously initialized output tensor descriptor. Can be `NULL` for avg pooling.
    /// - `x`: Input. Data pointer to GPU memory associated with the output tensor descriptor `xDesc`. Can be `NULL` for avg pooling.
    /// - `dxDesc`: Input. Handle to the previously initialized output differential tensor descriptor. Must be of type `FLOAT`, `DOUBLE`, `HALF`, or `BFLOAT16`. For more information, refer to [`cudnnDataType_t`].
    /// - `dx`: Output. Data pointer to GPU memory associated with the output tensor descriptor `dxDesc`.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * The dimensions `n`, `c`, `h`, `w` of the `yDesc` and `dyDesc` tensors differ.
    /// * The strides `nStride`, `cStride`, `hStride`, `wStride` of the `yDesc` and `dyDesc` tensors differ.
    /// * The dimensions `n`, `c`, `h`, `w` of the `dxDesc` and `dxDesc` tensors differ.
    /// * The strides `nStride`, `cStride`, `hStride`, `wStride` of the `xDesc` and `dxDesc` tensors differ.
    /// * The datatype of the four tensors differ.
    /// - [`cudnnStatus_t::CUDNN_STATUS_EXECUTION_FAILED`]: The function failed to launch on the GPU.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: The function does not support the provided configuration. Some examples includes:
    ///
    /// * The `wStride` of input tensor or output tensor is not `1`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The function launched successfully.
    #[deprecated]
    pub fn cudnnPoolingBackward(
        handle: cudnnHandle_t,
        poolingDesc: cudnnPoolingDescriptor_t,
        alpha: *const ::core::ffi::c_void,
        yDesc: cudnnTensorDescriptor_t,
        y: *const ::core::ffi::c_void,
        dyDesc: cudnnTensorDescriptor_t,
        dy: *const ::core::ffi::c_void,
        xDesc: cudnnTensorDescriptor_t,
        x: *const ::core::ffi::c_void,
        beta: *const ::core::ffi::c_void,
        dxDesc: cudnnTensorDescriptor_t,
        dx: *mut ::core::ffi::c_void,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This routine computes the gradient of a neuron activation function.
    ///
    /// In-place operation is allowed for this routine; meaning `dy` and `dx` pointers may be equal. However, this requires the corresponding tensor descriptors to be identical (particularly, the strides of the input and output must match for an in-place operation to be allowed).
    ///
    /// All tensor formats are supported for 4 and 5 dimensions, however, the best performance is obtained when the strides of `yDesc` and `xDesc` are equal and HW-packed. For more than 5 dimensions the tensors must have their spatial dimensions packed.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN context. For more information, refer to [`cudnnHandle_t`].
    /// - `activationDesc`: Input. Activation descriptor. For more information, refer to [`cudnnActivationDescriptor_t`].
    /// - `yDesc`: Input. Handle to the previously initialized input tensor descriptor. For more information, refer to [`cudnnTensorDescriptor_t`].
    /// - `y`: Input. Data pointer to GPU memory associated with the tensor descriptor `yDesc`.
    /// - `dyDesc`: Input. Handle to the previously initialized input differential tensor descriptor.
    /// - `dy`: Input. Data pointer to GPU memory associated with the tensor descriptor `dyDesc`.
    /// - `xDesc`: Input. Handle to the previously initialized output tensor descriptor.
    /// - `x`: Input. Data pointer to GPU memory associated with the output tensor descriptor `xDesc`.
    /// - `dxDesc`: Input. Handle to the previously initialized output differential tensor descriptor.
    /// - `dx`: Output. Data pointer to GPU memory associated with the output tensor descriptor `dxDesc`.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * The strides `nStride`, `cStride`, `hStride`, `wStride` of the input differential tensor and output differential tensor differ and in-place operation is used.
    /// - [`cudnnStatus_t::CUDNN_STATUS_EXECUTION_FAILED`]: The function failed to launch on the GPU.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: The function does not support the provided configuration. Refer to the following for some examples of non-supported configurations:
    ///
    /// * The dimensions `n`, `c`, `h`, and `w` of the input tensor and output tensor differ.
    /// * The `datatype` of the input tensor and output tensor differs.
    /// * The strides `nStride`, `cStride`, `hStride`, and `wStride` of the input tensor and the input differential tensor differ.
    /// * The strides `nStride`, `cStride`, `hStride`, and `wStride` of the output tensor and the output differential tensor differ.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The function launched successfully.
    #[deprecated]
    pub fn cudnnActivationBackward(
        handle: cudnnHandle_t,
        activationDesc: cudnnActivationDescriptor_t,
        alpha: *const ::core::ffi::c_void,
        yDesc: cudnnTensorDescriptor_t,
        y: *const ::core::ffi::c_void,
        dyDesc: cudnnTensorDescriptor_t,
        dy: *const ::core::ffi::c_void,
        xDesc: cudnnTensorDescriptor_t,
        x: *const ::core::ffi::c_void,
        beta: *const ::core::ffi::c_void,
        dxDesc: cudnnTensorDescriptor_t,
        dx: *mut ::core::ffi::c_void,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function performs the backward LRN layer computation.
    ///
    /// Supported formats are: `positive-strided`, NCHW and NHWC for 4D `x` and `y`, and only NCDHW DHW-packed for 5D (for both `x` and `y`). Only non-overlapping 4D and 5D tensors are supported. NCHW layout is preferred for performance.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN library descriptor.
    /// - `normDesc`: Input. Handle to a previously initialized LRN parameter descriptor.
    /// - `lrnMode`: Input. LRN layer mode of operation. Currently only [`cudnnLRNMode_t::CUDNN_LRN_CROSS_CHANNEL_DIM1`] is implemented. Normalization is performed along the tensor’s `dimA\[1\]`.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * One of the tensor pointers `x`, `y` is `NULL`.
    /// * Number of input tensor dimensions is 2 or less.
    /// * LRN descriptor parameters are outside of their valid ranges.
    /// * One of the tensor parameters is 5D but is not in NCDHW DHW-packed format.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: The function does not support the provided configuration. Some examples include:
    ///
    /// * Any of the input tensor datatypes is not the same as any of the output tensor datatype.
    /// * Any pairwise tensor dimensions mismatch for `x`, `y`, `dx`, or `dy`.
    /// * Any tensor parameters strides are negative.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The computation was performed successfully.
    pub fn cudnnLRNCrossChannelBackward(
        handle: cudnnHandle_t,
        normDesc: cudnnLRNDescriptor_t,
        lrnMode: cudnnLRNMode_t,
        alpha: *const ::core::ffi::c_void,
        yDesc: cudnnTensorDescriptor_t,
        y: *const ::core::ffi::c_void,
        dyDesc: cudnnTensorDescriptor_t,
        dy: *const ::core::ffi::c_void,
        xDesc: cudnnTensorDescriptor_t,
        x: *const ::core::ffi::c_void,
        beta: *const ::core::ffi::c_void,
        dxDesc: cudnnTensorDescriptor_t,
        dx: *mut ::core::ffi::c_void,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function performs the backward `DivisiveNormalization` layer computation.
    ///
    /// Supported tensor formats are NCHW for 4D and NCDHW for 5D with any non-overlapping non-negative strides. Only 4D and 5D tensors are supported.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN library descriptor.
    /// - `normDesc`: Input. Handle to a previously initialized LRN parameter descriptor (this descriptor is used for both LRN and `DivisiveNormalization` layers).
    /// - `mode`: Input. `DivisiveNormalization` layer mode of operation. Currently only [`cudnnDivNormMode_t::CUDNN_DIVNORM_PRECOMPUTED_MEANS`] is implemented. Normalization is performed using the means input tensor that is expected to be precomputed by the user.
    /// - `dy`: Input. Tensor pointer in device memory for the layer’s `dy` cumulative loss differential data (error backpropagation).
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * One of the tensor pointers `x`, `dx`, `temp`, `tmep2`, and `dy` is `NULL`.
    /// * Number of any of the input or output tensor dimensions is not within the \[4,5\] range.
    /// * Either `alpha` or `beta` pointer is `NULL`.
    /// * A mismatch in dimensions between `xDesc` and `dxDesc`.
    /// * LRN descriptor parameters are outside of their valid ranges.
    /// * Any of the tensor strides is negative.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The computation was performed successfully.
    /// - `CUDNN_STATUS_UNSUPPORTED`: The function does not support the provided configuration, for example, any of the input and output tensor strides mismatch (for the same dimension) is a non-supported configuration.
    pub fn cudnnDivisiveNormalizationBackward(
        handle: cudnnHandle_t,
        normDesc: cudnnLRNDescriptor_t,
        mode: cudnnDivNormMode_t,
        alpha: *const ::core::ffi::c_void,
        xDesc: cudnnTensorDescriptor_t,
        x: *const ::core::ffi::c_void,
        means: *const ::core::ffi::c_void,
        dy: *const ::core::ffi::c_void,
        temp: *mut ::core::ffi::c_void,
        temp2: *mut ::core::ffi::c_void,
        beta: *const ::core::ffi::c_void,
        dXdMeansDesc: cudnnTensorDescriptor_t,
        dx: *mut ::core::ffi::c_void,
        dMeans: *mut ::core::ffi::c_void,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function returns the amount of GPU memory workspace the user should allocate to be able to call [`cudnnGetBatchNormalizationForwardTrainingExWorkspaceSize`] function for the specified `bnOps` input setting. The workspace allocated should then be passed by the user to the function [`cudnnGetBatchNormalizationForwardTrainingExWorkspaceSize`].
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN library descriptor. For more information, refer to [`cudnnHandle_t`].
    /// - `mode`: Input. Mode of operation (spatial or per-activation). For more information, refer to [`cudnnBatchNormMode_t`].
    /// - `bnOps`: Input. Mode of operation for the fast NHWC kernel. For more information, refer to [`cudnnBatchNormOps_t`]. This input can be used to set this function to perform either only the batch normalization, or batch normalization followed by activation, or batch normalization followed by element-wise addition and then activation.
    /// - `bnScaleBiasMeanVarDesc`: Input. Shared tensor descriptor for the following six tensors: `bnScaleData`, `bnBiasData`, `dBnScaleData`, `dBnBiasData`, `savedMean`, and `savedInvVariance`. This is the shared tensor descriptor desc for the secondary tensor that was derived by [`cudnnDeriveBNTensorDescriptor`]. The dimensions for this tensor descriptor are dependent on normalization mode. Note that the data type of this tensor descriptor must be `float` for FP16 and FP32 input tensors, and `double` for FP64 input tensors.
    /// - `activationDesc`: Input. Descriptor for the activation operation. When the `bnOps` input is set to either [`cudnnBatchNormOps_t::CUDNN_BATCHNORM_OPS_BN_ACTIVATION`] or [`cudnnBatchNormOps_t::CUDNN_BATCHNORM_OPS_BN_ADD_ACTIVATION`], then this activation is used, otherwise user may pass `NULL`.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * Number of `xDesc`, `yDesc`, or `dxDesc` tensor descriptor dimensions is not within the range of \[4,5\] (only 4D and 5D tensors are supported).
    /// * `dBnScaleBiasDesc` dimensions not 1xCx1x1 for 4D and 1xCx1x1x1 for 5D for spatial, and are not 1xCxHxW for 4D and 1xCxDxHxW for 5D for per-activation mode.
    /// * Dimensions or data types mismatch for any pair of `xDesc` or `dyDesc`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: The function does not support the provided configuration.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The computation was performed successfully.
    #[deprecated]
    pub fn cudnnGetBatchNormalizationForwardTrainingExWorkspaceSize(
        handle: cudnnHandle_t,
        mode: cudnnBatchNormMode_t,
        bnOps: cudnnBatchNormOps_t,
        xDesc: cudnnTensorDescriptor_t,
        zDesc: cudnnTensorDescriptor_t,
        yDesc: cudnnTensorDescriptor_t,
        bnScaleBiasMeanVarDesc: cudnnTensorDescriptor_t,
        activationDesc: cudnnActivationDescriptor_t,
        sizeInBytes: *mut size_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function returns the amount of GPU memory workspace the user should allocate to be able to call [`cudnnGetBatchNormalizationBackwardExWorkspaceSize`] function for the specified `bnOps` input setting. The workspace allocated will then be passed to the function [`cudnnGetBatchNormalizationBackwardExWorkspaceSize`].
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN library descriptor. For more information, refer to [`cudnnHandle_t`].
    /// - `mode`: Input. Mode of operation (spatial or per-activation). For more information, refer to [`cudnnBatchNormMode_t`].
    /// - `bnOps`: Input. Mode of operation for the fast NHWC kernel. For more information, refer to [`cudnnBatchNormOps_t`]. This input can be used to set this function to perform either only the batch normalization, or batch normalization followed by activation, or batch normalization followed by element-wise addition and then activation.
    /// - `dBnScaleBiasDesc`: Input. Shared tensor descriptor for the following six tensors: `bnScaleData`, `bnBiasData`, `dBnScaleData`, `dBnBiasData`, `savedMean`, and `savedInvVariance`. This is the shared tensor descriptor desc for the secondary tensor that was derived by [`cudnnDeriveBNTensorDescriptor`]. The dimensions for this tensor descriptor are dependent on normalization mode. Note that the data type of this tensor descriptor must be `float` for FP16 and FP32 input tensors, and `double` for FP64 input tensors.
    /// - `activationDesc`: Input. Descriptor for the activation operation. When the `bnOps` input is set to either [`cudnnBatchNormOps_t::CUDNN_BATCHNORM_OPS_BN_ACTIVATION`] or [`cudnnBatchNormOps_t::CUDNN_BATCHNORM_OPS_BN_ADD_ACTIVATION`], then this activation is used, otherwise user may pass `NULL`.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * Number of `xDesc`, `yDesc`, or `dxDesc` tensor descriptor dimensions is not within the range of \[4,5\] (only 4D and 5D tensors are supported).
    /// * `dBnScaleBiasDesc` dimensions not 1xCx1x1 for 4D and 1xCx1x1x1 for 5D for spatial, and are not 1xCxHxW for 4D and 1xCxDxHxW for 5D for per-activation mode.
    /// * Dimensions or data types mismatch for any pair of `xDesc`, `dyDesc`, or `dxDesc`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: The function does not support the provided configuration.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The computation was performed successfully.
    pub fn cudnnGetBatchNormalizationBackwardExWorkspaceSize(
        handle: cudnnHandle_t,
        mode: cudnnBatchNormMode_t,
        bnOps: cudnnBatchNormOps_t,
        xDesc: cudnnTensorDescriptor_t,
        yDesc: cudnnTensorDescriptor_t,
        dyDesc: cudnnTensorDescriptor_t,
        dzDesc: cudnnTensorDescriptor_t,
        dxDesc: cudnnTensorDescriptor_t,
        dBnScaleBiasDesc: cudnnTensorDescriptor_t,
        activationDesc: cudnnActivationDescriptor_t,
        sizeInBytes: *mut size_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function returns the amount of reserve GPU memory workspace the user should allocate for the batch normalization operation, for the specified `bnOps` input setting. In contrast to the `workspace`, the reserved space should be preserved between the forward and backward calls, and the data should not be altered.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN library descriptor. For more information, refer to [`cudnnHandle_t`].
    /// - `mode`: Input. Mode of operation (spatial or per-activation). For more information, refer to [`cudnnBatchNormMode_t`].
    /// - `bnOps`: Input. Mode of operation for the fast NHWC kernel. For more information, refer to [`cudnnBatchNormOps_t`]. This input can be used to set this function to perform either only the batch normalization, or batch normalization followed by activation, or batch normalization followed by element-wise addition and then activation.
    /// - `activationDesc`: Input. Descriptor for the activation operation. When the `bnOps` input is set to either [`cudnnBatchNormOps_t::CUDNN_BATCHNORM_OPS_BN_ACTIVATION`] or [`cudnnBatchNormOps_t::CUDNN_BATCHNORM_OPS_BN_ADD_ACTIVATION`], then this activation is used, otherwise user may pass `NULL`.
    /// - `xDesc`: Input. Tensor descriptors and pointers in the device memory for the layer’s `x` data. For more information, refer to [`cudnnTensorDescriptor_t`].
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * The `xDesc` tensor descriptor dimension is not within the \[4,5\] range (only 4D and 5D tensors are supported).
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: The function does not support the provided configuration.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The computation was performed successfully.
    #[deprecated]
    pub fn cudnnGetBatchNormalizationTrainingExReserveSpaceSize(
        handle: cudnnHandle_t,
        mode: cudnnBatchNormMode_t,
        bnOps: cudnnBatchNormOps_t,
        activationDesc: cudnnActivationDescriptor_t,
        xDesc: cudnnTensorDescriptor_t,
        sizeInBytes: *mut size_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function performs the forward batch normalization layer computation for the training phase. This layer is based on the [Batch Normalization: Accelerating Deep Network Training by Reducing Internal Covariate Shift paper](https://arxiv.org/abs/1502.03167).
    ///
    /// Only 4D and 5D tensors are supported.
    ///
    /// The `epsilon` value has to be the same during training, backpropagation, and inference.
    ///
    /// For the inference phase, use [`cudnnBatchNormalizationForwardInference`].
    ///
    /// Higher performance can be obtained when HW-packed tensors are used for both `x` and `y`.
    ///
    /// Refer to [`cudnnDeriveBNTensorDescriptor`] for the secondary tensor descriptor generation for the parameters used in this function.
    ///
    /// **Supported Configurations**
    ///
    /// This function supports the following combinations of data types for various descriptors.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN library descriptor. For more information, refer to [`cudnnHandle_t`].
    /// - `mode`: Input. Mode of operation (spatial or per-activation). For more information, refer to [`cudnnBatchNormMode_t`].
    /// - `bnScaleBiasMeanVarDesc`: Input. Shared tensor descriptor desc for the secondary tensor that was derived by [`cudnnDeriveBNTensorDescriptor`]. The dimensions for this tensor descriptor are dependent on the normalization mode.
    /// - `exponentialAverageFactor`: Input. Factor used in the moving average computation as follows:
    ///
    /// ```text
    /// runningMean = runningMean*(1-factor) + newMean*factor
    /// ```
    ///
    /// Use a `factor=1/(1+n)` at `N-th` call to the function to get the Cumulative Moving Average (CMA) behavior, for example:
    ///
    /// ```text
    /// CMA\[n\] = (x\[1\]+...+x\[n\])/n
    /// ```
    ///
    /// For example:
    ///
    /// ```text
    /// CMA\[n+1\] = (n*CMA\[n\]+x\[n+1\])/(n+1)
    /// = ((n+1)*CMA\[n\]-CMA\[n\])/(n+1) + x\[n+1\]/(n+1)
    /// = CMA\[n\]*(1-1/(n+1))+x\[n+1\]*1/(n+1)
    /// = CMA\[n\]*(1-factor) + x(n+1)*factor
    /// ```
    /// - `epsilon`: Input. Epsilon value used in the batch normalization formula. Its value should be equal to or greater than the value defined for [`CUDNN_BN_MIN_EPSILON`] in `cudnn.h`. The same epsilon value should be used in forward and backward functions.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * One of the pointers `alpha`, `beta`, `x`, `y`, `bnScale`, and `bnBias` is `NULL`.
    /// * The number of `xDesc` or `yDesc` tensor descriptor dimensions is not within the range of \[4,5\] (only 4D and 5D tensors are supported.)
    /// * `bnScaleBiasMeanVarDesc` dimensions are not 1xCx1x1 for 4D and 1xCx1x1x1 for 5D for spatial, and are not 1xCxHxW for 4D and 1xCxDxHxW for 5D for per-activation mode.
    /// * Exactly one of `resultSaveMean`, `resultSaveInvVariance` pointers are `NULL`.
    /// * Exactly one of `resultRunningMean`, `resultRunningInvVariance` pointers are `NULL`.
    /// * `epsilon` value is less than [`CUDNN_BN_MIN_EPSILON`].
    /// * Dimensions or data types mismatch for `xDesc`, `yDesc`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: The function does not support the provided configuration.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The computation was performed successfully.
    #[deprecated]
    pub fn cudnnBatchNormalizationForwardTraining(
        handle: cudnnHandle_t,
        mode: cudnnBatchNormMode_t,
        alpha: *const ::core::ffi::c_void,
        beta: *const ::core::ffi::c_void,
        xDesc: cudnnTensorDescriptor_t,
        x: *const ::core::ffi::c_void,
        yDesc: cudnnTensorDescriptor_t,
        y: *mut ::core::ffi::c_void,
        bnScaleBiasMeanVarDesc: cudnnTensorDescriptor_t,
        bnScale: *const ::core::ffi::c_void,
        bnBias: *const ::core::ffi::c_void,
        exponentialAverageFactor: f64,
        resultRunningMean: *mut ::core::ffi::c_void,
        resultRunningVariance: *mut ::core::ffi::c_void,
        epsilon: f64,
        resultSaveMean: *mut ::core::ffi::c_void,
        resultSaveInvVariance: *mut ::core::ffi::c_void,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function is an extension of the [`cudnnBatchNormalizationForwardTraining`] for performing the forward batch normalization layer computation.
    ///
    /// This API will trigger the new semi-persistent NHWC kernel when the following conditions are true:
    ///
    /// If `workspace` is `NULL` and `workSpaceSizeInBytes` of zero is passed in, this API will function exactly like the non-extended function [`cudnnBatchNormalizationForwardTraining`].
    ///
    /// This workspace is not required to be clean. Moreover, the workspace does not have to remain unchanged between the forward and backward pass, as it is not used for passing any information.
    ///
    /// This extended function can accept a `*workspace` pointer to the GPU workspace, and `workSpaceSizeInBytes`, the size of the workspace, from the user.
    ///
    /// The `bnOps` input can be used to set this function to perform either only the batch normalization, or batch normalization followed by activation, or batch normalization followed by element-wise addition and then activation.
    ///
    /// Only 4D and 5D tensors are supported. The `epsilon` value has to be the same during the training, the backpropagation, and the inference.
    ///
    /// When the tensor layout is NCHW, higher performance can be obtained when HW-packed tensors are used for `x`, `dy`, and `dx`.
    ///
    /// **Supported Configurations**
    ///
    /// This function supports the following combinations of data types for various descriptors.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN library descriptor. For more information, refer to [`cudnnHandle_t`].
    /// - `mode`: Input. Mode of operation (spatial or per-activation). For more information, refer to [`cudnnBatchNormMode_t`].
    /// - `bnOps`: Input. Mode of operation for the fast NHWC kernel. For more information, refer to [`cudnnBatchNormOps_t`]. This input can be used to set this function to perform either only the batch normalization, or batch normalization followed by activation, or batch normalization followed by element-wise addition and then activation.
    /// - `bnScaleBiasMeanVarDesc`: Input. Shared tensor descriptor `desc` for the secondary tensor that was derived by [`cudnnDeriveBNTensorDescriptor`]. The dimensions for this tensor descriptor are dependent on the normalization mode.
    /// - `exponentialAverageFactor`: Input. Factor used in the moving average computation as follows:
    ///
    /// ```text
    /// runningMean = runningMean*(1-factor) + newMean*factor
    /// ```
    ///
    /// Use a `factor=1/(1+n)` at `N-th` call to the function to get the Cumulative Moving Average (CMA) behavior, for example:
    ///
    /// ```text
    /// CMA\[n\] = (x\[1\]+...+x\[n\])/n
    /// ```
    ///
    /// For example:
    ///
    /// ```text
    /// CMA\[n+1\] = (n*CMA\[n\]+x\[n+1\])/(n+1)
    /// = ((n+1)*CMA\[n\]-CMA\[n\])/(n+1) + x\[n+1\]/(n+1)
    /// = CMA\[n\]*(1-1/(n+1))+x\[n+1\]*1/(n+1)
    /// = CMA\[n\]*(1-factor) + x(n+1)*factor
    /// ```
    /// - `epsilon`: Input. Epsilon value used in the batch normalization formula. Its value should be equal to or greater than the value defined for [`CUDNN_BN_MIN_EPSILON`] in `cudnn.h`. The same epsilon value should be used in forward and backward functions.
    /// - `activationDesc`: Input. The tensor descriptor for the activation operation. When the `bnOps` input is set to either [`cudnnBatchNormOps_t::CUDNN_BATCHNORM_OPS_BN_ACTIVATION`] or [`cudnnBatchNormOps_t::CUDNN_BATCHNORM_OPS_BN_ADD_ACTIVATION`] then this activation is used, otherwise user may pass `NULL`.
    /// - `reserveSpaceSizeInBytes`: Input. The size of the `reserveSpace`. Must be equal or larger than the amount required by [`cudnnGetBatchNormalizationTrainingExReserveSpaceSize`].
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * One of the pointers `alpha`, `beta`, `x`, `y`, `bnScaleData`, and `bnBiasData` is `NULL`.
    /// * The number of `xDesc` or `yDesc` tensor descriptor dimensions is not within the \[4,5\] range (only 4D and 5D tensors are supported.)
    /// * `bnScaleBiasMeanVarDesc` dimensions are not 1xCx1x1 for 4D and 1xCx1x1x1 for 5D for spatial, and are not 1xCxHxW for 4D and 1xCxDxHxW for 5D for per-activation mode.
    /// * Exactly one of `saveMean`, `saveInvVariance` pointers are `NULL`.
    /// * Exactly one of `resultRunningMeanData`, `resultRunningInvVarianceData` pointers are `NULL`.
    /// * `epsilon` value is less than [`CUDNN_BN_MIN_EPSILON`].
    /// * Dimensions or data types mismatch for `xDesc` and `yDesc`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: The function does not support the provided configuration.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The computation was performed successfully.
    #[deprecated]
    pub fn cudnnBatchNormalizationForwardTrainingEx(
        handle: cudnnHandle_t,
        mode: cudnnBatchNormMode_t,
        bnOps: cudnnBatchNormOps_t,
        alpha: *const ::core::ffi::c_void,
        beta: *const ::core::ffi::c_void,
        xDesc: cudnnTensorDescriptor_t,
        xData: *const ::core::ffi::c_void,
        zDesc: cudnnTensorDescriptor_t,
        zData: *const ::core::ffi::c_void,
        yDesc: cudnnTensorDescriptor_t,
        yData: *mut ::core::ffi::c_void,
        bnScaleBiasMeanVarDesc: cudnnTensorDescriptor_t,
        bnScale: *const ::core::ffi::c_void,
        bnBias: *const ::core::ffi::c_void,
        exponentialAverageFactor: f64,
        resultRunningMean: *mut ::core::ffi::c_void,
        resultRunningVariance: *mut ::core::ffi::c_void,
        epsilon: f64,
        resultSaveMean: *mut ::core::ffi::c_void,
        resultSaveInvVariance: *mut ::core::ffi::c_void,
        activationDesc: cudnnActivationDescriptor_t,
        workspace: *mut ::core::ffi::c_void,
        workSpaceSizeInBytes: size_t,
        reserveSpace: *mut ::core::ffi::c_void,
        reserveSpaceSizeInBytes: size_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function performs the backward batch normalization layer computation. This layer is based on the [Batch Normalization: Accelerating Deep Network Training by Reducing Internal Covariate Shift paper](https://arxiv.org/abs/1502.03167).
    ///
    /// Only 4D and 5D tensors are supported.
    ///
    /// The `epsilon` value has to be the same during training, backpropagation, and inference.
    ///
    /// Higher performance can be obtained when HW-packed tensors are used for all of `x`, `dy`, and `dx`.
    ///
    /// For more information, refer to [`cudnnDeriveBNTensorDescriptor`] for the secondary tensor descriptor generation for the parameters used in this function.
    ///
    /// **Supported Configurations**
    ///
    /// This function supports the following combinations of data types for various descriptors.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN library descriptor. For more information, refer to [`cudnnHandle_t`].
    /// - `mode`: Input. Mode of operation (spatial or per-activation). For more information, refer to [`cudnnBatchNormMode_t`].
    /// - `epsilon`: Input. Epsilon value used in batch normalization formula. Its value should be equal to or greater than the value defined for [`CUDNN_BN_MIN_EPSILON`] in `cudnn.h`. The same `epsilon` value should be used in forward and backward functions.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * Any of the pointers `alpha`, `beta`, `x`, `dy`, `dx`, `bnScale`, `resultBnScaleDiff`, and `resultBnBiasDiff` is `NULL`.
    /// * The number of `xDesc`, `yDesc`, or `dxDesc` tensor descriptor dimensions is not within the range of \[4,5\] (only 4D and 5D tensors are supported).
    /// * `bnScaleBiasDiffDesc` dimensions are not 1xCx1x1 for 4D and 1xCx1x1x1 for 5D for spatial, and are not 1xCxHxW for 4D and 1xCxDxHxW for 5D for per-activation mode.
    /// * Exactly one of `savedMean`, `savedInvVariance` pointers is `NULL`.
    /// * `epsilon` value is less than [`CUDNN_BN_MIN_EPSILON`].
    /// * Dimensions or data types mismatch for any pair of `xDesc`, `dyDesc`, or `dxDesc`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: The function does not support the provided configuration.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The computation was performed successfully.
    #[deprecated]
    pub fn cudnnBatchNormalizationBackward(
        handle: cudnnHandle_t,
        mode: cudnnBatchNormMode_t,
        alphaDataDiff: *const ::core::ffi::c_void,
        betaDataDiff: *const ::core::ffi::c_void,
        alphaParamDiff: *const ::core::ffi::c_void,
        betaParamDiff: *const ::core::ffi::c_void,
        xDesc: cudnnTensorDescriptor_t,
        x: *const ::core::ffi::c_void,
        dyDesc: cudnnTensorDescriptor_t,
        dy: *const ::core::ffi::c_void,
        dxDesc: cudnnTensorDescriptor_t,
        dx: *mut ::core::ffi::c_void,
        dBnScaleBiasDesc: cudnnTensorDescriptor_t,
        bnScale: *const ::core::ffi::c_void,
        dBnScaleResult: *mut ::core::ffi::c_void,
        dBnBiasResult: *mut ::core::ffi::c_void,
        epsilon: f64,
        savedMean: *const ::core::ffi::c_void,
        savedInvVariance: *const ::core::ffi::c_void,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function is an extension of the [`cudnnBatchNormalizationBackward`] for performing the backward batch normalization layer computation with a fast NHWC semi-persistent kernel.
    ///
    /// This API will trigger the new semi-persistent NHWC kernel when the following conditions are true:
    ///
    /// If `workspace` is `NULL` and `workSpaceSizeInBytes` of zero is passed in, this API will function exactly like the non-extended function [`cudnnBatchNormalizationBackward`].
    ///
    /// This `workspace` is not required to be clean. Moreover, the `workspace` does not have to remain unchanged between the forward and backward pass, as it is not used for passing any information.
    ///
    /// This extended function can accept a `*workspace` pointer to the GPU workspace, and `workSpaceSizeInBytes`, the size of the workspace, from the user.
    ///
    /// The `bnOps` input can be used to set this function to perform either only the batch normalization, or batch normalization followed by activation, or batch normalization followed by element-wise addition and then activation.
    ///
    /// Only 4D and 5D tensors are supported. The `epsilon` value has to be the same during the training, the backpropagation, and the inference.
    ///
    /// When the tensor layout is NCHW, higher performance can be obtained when HW-packed tensors are used for `x`, `dy`, and `dx`.
    ///
    /// **Supported Configurations**
    ///
    /// This function supports the following combinations of data types for various descriptors.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN library descriptor. For more information, refer to [`cudnnHandle_t`].
    /// - `mode`: Input. Mode of operation (spatial or per-activation). For more information, refer to [`cudnnBatchNormMode_t`].
    /// - `bnOps`: Input. Mode of operation. Currently, [`cudnnBatchNormOps_t::CUDNN_BATCHNORM_OPS_BN_ACTIVATION`] and [`cudnnBatchNormOps_t::CUDNN_BATCHNORM_OPS_BN_ADD_ACTIVATION`] are only supported in the NHWC layout. For more information, refer to [`cudnnBatchNormOps_t`]. This input can be used to set this function to perform either only the batch normalization, or batch normalization followed by activation, or batch normalization followed by element-wise addition and then activation.
    /// - `dBnScaleBiasDesc`: Input. Shared tensor descriptor for the following six tensors: `bnScaleData`, `bnBiasData`, `dBnScaleData`, `dBnBiasData`, `savedMean`, and `savedInvVariance`. For more information, refer to [`cudnnDeriveBNTensorDescriptor`].
    ///
    /// The dimensions for this tensor descriptor are dependent on normalization mode.
    ///
    /// Note
    ///
    /// The data type of this tensor descriptor must be `float` for FP16 and FP32 input tensors and `double` for FP64 input tensors. For more information, refer to [`cudnnTensorDescriptor_t`].
    /// - `epsilon`: Input. Epsilon value used in batch normalization formula. Its value should be equal to or greater than the value defined for [`CUDNN_BN_MIN_EPSILON`] in `cudnn.h`. The same epsilon value should be used in forward and backward functions.
    /// - `activationDesc`: Input. Descriptor for the activation operation. When the `bnOps` input is set to either [`cudnnBatchNormOps_t::CUDNN_BATCHNORM_OPS_BN_ACTIVATION`] or [`cudnnBatchNormOps_t::CUDNN_BATCHNORM_OPS_BN_ADD_ACTIVATION`] then this activation is used, otherwise the user may pass `NULL`.
    /// - `workSpaceSizeInBytes`: Input. The size of the workspace. It must be large enough to trigger the fast NHWC semi-persistent kernel by this function.
    /// - `reserveSpaceSizeInBytes`: Input. The size of the `reserveSpace`. It must be equal or larger than the amount required by [`cudnnGetBatchNormalizationTrainingExReserveSpaceSize`].
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * Any of the pointers `alphaDataDiff`, `betaDataDiff`, `alphaParamDiff`, `betaParamDiff`, `x`, `dy`, `dx`, `bnScale`, `resultBnScaleDiff`, and `resultBnBiasDiff` is `NULL`.
    /// * The number of `xDesc`, `yDesc`, or `dxDesc` tensor descriptor dimensions is not within the range of \[4,5\] (only 4D and 5D tensors are supported).
    /// * `dBnScaleBiasDesc` dimensions not 1xCx1x1 for 4D and 1xCx1x1x1 for 5D for spatial, and are not 1xCxHxW for 4D and 1xCxDxHxW for 5D for per-activation mode.
    /// * Exactly one of `savedMean`, `savedInvVariance` pointers is `NULL`.
    /// * `epsilon` value is less than [`CUDNN_BN_MIN_EPSILON`].
    /// * Dimensions or data types mismatch for any pair of `xDesc`, `dyDesc`, and `dxDesc`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: The function does not support the provided configuration.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The computation was performed successfully.
    #[deprecated]
    pub fn cudnnBatchNormalizationBackwardEx(
        handle: cudnnHandle_t,
        mode: cudnnBatchNormMode_t,
        bnOps: cudnnBatchNormOps_t,
        alphaDataDiff: *const ::core::ffi::c_void,
        betaDataDiff: *const ::core::ffi::c_void,
        alphaParamDiff: *const ::core::ffi::c_void,
        betaParamDiff: *const ::core::ffi::c_void,
        xDesc: cudnnTensorDescriptor_t,
        xData: *const ::core::ffi::c_void,
        yDesc: cudnnTensorDescriptor_t,
        yData: *const ::core::ffi::c_void,
        dyDesc: cudnnTensorDescriptor_t,
        dyData: *const ::core::ffi::c_void,
        dzDesc: cudnnTensorDescriptor_t,
        dzData: *mut ::core::ffi::c_void,
        dxDesc: cudnnTensorDescriptor_t,
        dxData: *mut ::core::ffi::c_void,
        dBnScaleBiasDesc: cudnnTensorDescriptor_t,
        bnScaleData: *const ::core::ffi::c_void,
        bnBiasData: *const ::core::ffi::c_void,
        dBnScaleData: *mut ::core::ffi::c_void,
        dBnBiasData: *mut ::core::ffi::c_void,
        epsilon: f64,
        savedMean: *const ::core::ffi::c_void,
        savedInvVariance: *const ::core::ffi::c_void,
        activationDesc: cudnnActivationDescriptor_t,
        workSpace: *mut ::core::ffi::c_void,
        workSpaceSizeInBytes: size_t,
        reserveSpace: *mut ::core::ffi::c_void,
        reserveSpaceSizeInBytes: size_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function returns the amount of GPU memory workspace the user should allocate to be able to call [`cudnnNormalizationForwardTraining`] function for the specified `normOps` and `algo` input setting. The workspace allocated should then be passed by the user to the function [`cudnnNormalizationForwardTraining`].
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN library descriptor. For more information, refer to [`cudnnHandle_t`].
    /// - `mode`: Input. Mode of operation (per-channel or per-activation). For more information, refer to [`cudnnNormMode_t`].
    /// - `normOps`: Input. Mode of post-operative. Currently [`cudnnNormOps_t::CUDNN_NORM_OPS_NORM_ACTIVATION`] and [`cudnnNormOps_t::CUDNN_NORM_OPS_NORM_ADD_ACTIVATION`] are only supported in the NHWC layout. For more information, refer to [`cudnnNormOps_t`]. This input can be used to set this function to perform either only the normalization, or normalization followed by activation, or normalization followed by element-wise addition and then activation.
    /// - `algo`: Input. Algorithm to be performed. For more information, refer to [`cudnnNormAlgo_t`].
    /// - `normScaleBiasDesc`: Input. Shared tensor descriptor for the following four tensors: `normScaleData` and `normBiasData`. The dimensions for this tensor descriptor are dependent on normalization mode. Note that the data type of this tensor descriptor must be `float` for FP16 and FP32 input tensors, and `double` for FP64 input tensors.
    /// - `activationDesc`: Input. Descriptor for the activation operation. When the `normOps` input is set to either [`cudnnNormOps_t::CUDNN_NORM_OPS_NORM_ACTIVATION`] or [`cudnnNormOps_t::CUDNN_NORM_OPS_NORM_ADD_ACTIVATION`], then this activation is used, otherwise the user may pass `NULL`.
    /// - `normMeanVarDesc`: Input. Shared tensor descriptor for the following tensors: `savedMean` and `savedInvVariance`. The dimensions for this tensor descriptor are dependent on normalization mode. Note that the data type of this tensor descriptor must be `float` for FP16 and FP32 input tensors, and `double` for FP64 input tensors.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * Number of `xDesc`, `yDesc`, or `zDesc` tensor descriptor dimensions is not within the range of \[4,5\] (only 4D and 5D tensors are supported).
    /// * `normScaleBiasDesc` dimensions not 1xCx1x1 for 4D and 1xCx1x1x1 for 5D for per-channel, and are not 1xCxHxW for 4D and 1xCxDxHxW for 5D for per-activation mode.
    /// * Dimensions or data types mismatch for any pair of `xDesc` or `yDesc`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: The function does not support the provided configuration.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The computation was performed successfully.
    pub fn cudnnGetNormalizationForwardTrainingWorkspaceSize(
        handle: cudnnHandle_t,
        mode: cudnnNormMode_t,
        normOps: cudnnNormOps_t,
        algo: cudnnNormAlgo_t,
        xDesc: cudnnTensorDescriptor_t,
        zDesc: cudnnTensorDescriptor_t,
        yDesc: cudnnTensorDescriptor_t,
        normScaleBiasDesc: cudnnTensorDescriptor_t,
        activationDesc: cudnnActivationDescriptor_t,
        normMeanVarDesc: cudnnTensorDescriptor_t,
        sizeInBytes: *mut size_t,
        groupCnt: ::core::ffi::c_int,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function returns the amount of GPU memory workspace the user should allocate to be able to call [`cudnnNormalizationBackward`] function for the specified `normOps` and `algo` input setting. The workspace allocated will then be passed to the function [`cudnnNormalizationBackward`].
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN library descriptor. For more information, refer to [`cudnnHandle_t`].
    /// - `mode`: Input. Mode of operation (per-channel or per-activation). For more information, refer to [`cudnnNormMode_t`].
    /// - `normOps`: Input. Mode of post-operative. Currently [`cudnnNormOps_t::CUDNN_NORM_OPS_NORM_ACTIVATION`] and [`cudnnNormOps_t::CUDNN_NORM_OPS_NORM_ADD_ACTIVATION`] are only supported in the NHWC layout. For more information, refer to [`cudnnNormOps_t`]. This input can be used to set this function to perform either only the normalization, or normalization followed by activation, or normalization followed by element-wise addition and then activation.
    /// - `algo`: Input. Algorithm to be performed. For more information, refer to [`cudnnNormAlgo_t`].
    /// - `dNormScaleBiasDesc`: Input. Shared tensor descriptor for the following four tensors: `normScaleData`, `normBiasData`, `dNormScaleData`, `dNormBiasData`. The dimensions for this tensor descriptor are dependent on normalization mode. Note that the data type of this tensor descriptor must be `float` for FP16 and FP32 input tensors, and `double` for FP64 input tensors.
    /// - `activationDesc`: Input. Descriptor for the activation operation. When the `normOps` input is set to either [`cudnnNormOps_t::CUDNN_NORM_OPS_NORM_ACTIVATION`] or [`cudnnNormOps_t::CUDNN_NORM_OPS_NORM_ADD_ACTIVATION`], then this activation is used, otherwise the user may pass `NULL`.
    /// - `normMeanVarDesc`: Input. Shared tensor descriptor for the following tensors: `savedMean` and `savedInvVariance`. The dimensions for this tensor descriptor are dependent on normalization mode. Note that the data type of this tensor descriptor must be `float` for FP16 and FP32 input tensors, and `double` for FP64 input tensors.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * Number of `xDesc`, `yDesc`, or `dxDesc` tensor descriptor dimensions is not within the range of \[4,5\] (only 4D and 5D tensors are supported).
    /// * `dNormScaleBiasDesc` dimensions not 1xCx1x1 for 4D and 1xCx1x1x1 for 5D for per-channel, and are not 1xCxHxW for 4D and 1xCxDxHxW for 5D for per-activation mode.
    /// * Dimensions or data types mismatch for any pair of `xDesc`, `dyDesc`, or `dxDesc`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: The function does not support the provided configuration.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The computation was performed successfully.
    pub fn cudnnGetNormalizationBackwardWorkspaceSize(
        handle: cudnnHandle_t,
        mode: cudnnNormMode_t,
        normOps: cudnnNormOps_t,
        algo: cudnnNormAlgo_t,
        xDesc: cudnnTensorDescriptor_t,
        yDesc: cudnnTensorDescriptor_t,
        dyDesc: cudnnTensorDescriptor_t,
        dzDesc: cudnnTensorDescriptor_t,
        dxDesc: cudnnTensorDescriptor_t,
        dNormScaleBiasDesc: cudnnTensorDescriptor_t,
        activationDesc: cudnnActivationDescriptor_t,
        normMeanVarDesc: cudnnTensorDescriptor_t,
        sizeInBytes: *mut size_t,
        groupCnt: ::core::ffi::c_int,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function returns the amount of reserve GPU memory workspace the user should allocate for the normalization operation, for the specified `normOps` input setting. In contrast to the workspace, the reserved space should be preserved between the forward and backward calls, and the data should not be altered.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN library descriptor. For more information, refer to [`cudnnHandle_t`].
    /// - `mode`: Input. Mode of operation (per-channel or per-activation). For more information, refer to [`cudnnNormMode_t`].
    /// - `normOps`: Input. Mode of post-operative. Currently [`cudnnNormOps_t::CUDNN_NORM_OPS_NORM_ACTIVATION`] and [`cudnnNormOps_t::CUDNN_NORM_OPS_NORM_ADD_ACTIVATION`] are only supported in the NHWC layout. For more information, refer to [`cudnnNormOps_t`]. This input can be used to set this function to perform either only the normalization, or normalization followed by activation, or normalization followed by element-wise addition and then activation.
    /// - `algo`: Input. Algorithm to be performed. For more information, refer to [`cudnnNormAlgo_t`].
    /// - `activationDesc`: Input. Descriptor for the activation operation. When the `normOps` input is set to either [`cudnnNormOps_t::CUDNN_NORM_OPS_NORM_ACTIVATION`] or [`cudnnNormOps_t::CUDNN_NORM_OPS_NORM_ADD_ACTIVATION`], then this activation is used, otherwise the user may pass `NULL`.
    /// - `xDesc`: Input. Tensor descriptors for the layer’s `x` data. For more information, refer to [`cudnnTensorDescriptor_t`].
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * The `xDesc` tensor descriptor dimension is not within the \[4,5\] range (only 4D and 5D tensors are supported).
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: The function does not support the provided configuration.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The computation was performed successfully.
    pub fn cudnnGetNormalizationTrainingReserveSpaceSize(
        handle: cudnnHandle_t,
        mode: cudnnNormMode_t,
        normOps: cudnnNormOps_t,
        algo: cudnnNormAlgo_t,
        activationDesc: cudnnActivationDescriptor_t,
        xDesc: cudnnTensorDescriptor_t,
        sizeInBytes: *mut size_t,
        groupCnt: ::core::ffi::c_int,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function performs the forward normalization layer computation for the training phase. Depending on mode, different normalization operations will be performed. Per-channel layer is based on the [Batch Normalization: Accelerating Deep Network Training by Reducing Internal Covariate Shift paper](https://arxiv.org/abs/1502.03167).
    ///
    /// Only 4D and 5D tensors are supported.
    ///
    /// The `epsilon` value has to be the same during training, back propagation, and inference.
    ///
    /// For the inference phase, refer to [`cudnnNormalizationForwardInference`].
    ///
    /// Higher performance can be obtained when HW-packed tensors are used for both `x` and `y`.
    ///
    /// This API will trigger the new semi-persistent NHWC kernel when the following conditions are true:
    ///
    /// This `workspace` is not required to be clean. Moreover, the `workspace` does not have to remain unchanged between the forward and backward pass, as it is not used for passing any information. This extended function can accept a `*workspace` pointer to the GPU workspace, and `workSpaceSizeInBytes`, the size of the workspace, from the user.
    ///
    /// The `normOps` input can be used to set this function to perform either only the normalization, or normalization followed by activation, or normalization followed by element-wise addition and then activation.
    ///
    /// Only 4D and 5D tensors are supported. The `epsilon` value has to be the same during the training, the backpropagation, and the inference.
    ///
    /// When the tensor layout is NCHW, higher performance can be obtained when HW-packed tensors are used for `xData`, `yData`.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN library descriptor. For more information, refer to [`cudnnHandle_t`].
    /// - `mode`: Input. Mode of operation (per-channel or per-activation). For more information, refer to [`cudnnNormMode_t`].
    /// - `normOps`: Input. Mode of post-operative. Currently [`cudnnNormOps_t::CUDNN_NORM_OPS_NORM_ACTIVATION`] and [`cudnnNormOps_t::CUDNN_NORM_OPS_NORM_ADD_ACTIVATION`] are only supported in the NHWC layout. For more information, refer to [`cudnnNormOps_t`]. This input can be used to set this function to perform either only the normalization, or normalization followed by activation, or normalization followed by element-wise addition and then activation.
    /// - `algo`: Input. Algorithm to be performed. For more information, refer to [`cudnnNormAlgo_t`].
    /// - `exponentialAverageFactor`: Input. Factor used in the moving average computation as follows:
    ///
    /// ```text
    /// runningMean = runningMean*(1-factor) + newMean*factor
    /// ```
    ///
    /// Use a `factor=1/(1+n)` at `N-th` call to the function to get Cumulative Moving Average (CMA) behavior such that:
    ///
    /// ```text
    /// CMA\[n\] = (x\[1\]+...+x\[n\])/n
    /// ```
    ///
    /// This is proved below:
    ///
    /// ```text
    /// Writing
    /// CMA\[n+1\] = (n*CMA\[n\]+x\[n+1\])/(n+1)
    /// = ((n+1)*CMA\[n\]-CMA\[n\])/(n+1) + x\[n+1\]/(n+1)
    /// = CMA\[n\]*(1-1/(n+1))+x\[n+1\]*1/(n+1)
    /// = CMA\[n\]*(1-factor) + x(n+1)*factor
    /// ```
    /// - `normMeanVarDesc`: `Inputs`. Tensor descriptor used for following tensors: `resultRunningMean`, `resultRunningVariance`, `resultSaveMean`, `resultSaveInvVariance`.
    /// - `epsilon`: Input. Epsilon value used in the normalization formula. Its value should be equal to or greater than zero.
    /// - `activationDesc`: Input. The tensor descriptor for the activation operation. When the `normOps` input is set to either [`cudnnNormOps_t::CUDNN_NORM_OPS_NORM_ACTIVATION`] or [`cudnnNormOps_t::CUDNN_NORM_OPS_NORM_ADD_ACTIVATION`] then this activation is used, otherwise the user may pass `NULL`.
    /// - `reserveSpaceSizeInBytes`: Input. The size of the `reserveSpace`. Must be equal or larger than the amount required by [`cudnnGetNormalizationTrainingReserveSpaceSize`].
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * One of the pointers `alpha`, `beta`, `xData`, `yData`, `normScale`, and `normBias` is `NULL`.
    /// * The number of `xDesc` or `yDesc` tensor descriptor dimensions is not within the \[4,5\] range (only 4D and 5D tensors are supported).
    /// * `normScaleBiasDesc` dimensions are not 1xCx1x1 for 4D and 1xCx1x1x1 for 5D for per-channel mode, and are not 1xCxHxW for 4D and 1xCxDxHxW for 5D for per-activation mode.
    /// * Exactly one of `resultSaveMean`, `resultSaveInvVariance` pointers are `NULL`.
    /// * Exactly one of `resultRunningMean`, `resultRunningInvVariance` pointers are `NULL`.
    /// * `epsilon` value is less than zero.
    /// * Dimensions or data types mismatch for `xDesc` or `yDesc`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: The function does not support the provided configuration.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The computation was performed successfully.
    #[deprecated]
    pub fn cudnnNormalizationForwardTraining(
        handle: cudnnHandle_t,
        mode: cudnnNormMode_t,
        normOps: cudnnNormOps_t,
        algo: cudnnNormAlgo_t,
        alpha: *const ::core::ffi::c_void,
        beta: *const ::core::ffi::c_void,
        xDesc: cudnnTensorDescriptor_t,
        xData: *const ::core::ffi::c_void,
        normScaleBiasDesc: cudnnTensorDescriptor_t,
        normScale: *const ::core::ffi::c_void,
        normBias: *const ::core::ffi::c_void,
        exponentialAverageFactor: f64,
        normMeanVarDesc: cudnnTensorDescriptor_t,
        resultRunningMean: *mut ::core::ffi::c_void,
        resultRunningVariance: *mut ::core::ffi::c_void,
        epsilon: f64,
        resultSaveMean: *mut ::core::ffi::c_void,
        resultSaveInvVariance: *mut ::core::ffi::c_void,
        activationDesc: cudnnActivationDescriptor_t,
        zDesc: cudnnTensorDescriptor_t,
        zData: *const ::core::ffi::c_void,
        yDesc: cudnnTensorDescriptor_t,
        yData: *mut ::core::ffi::c_void,
        workspace: *mut ::core::ffi::c_void,
        workSpaceSizeInBytes: size_t,
        reserveSpace: *mut ::core::ffi::c_void,
        reserveSpaceSizeInBytes: size_t,
        groupCnt: ::core::ffi::c_int,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function performs backward normalization layer computation that is specified by mode. Per-channel normalization layer is based on the [Batch Normalization: Accelerating Deep Network Training by Reducing Internal Covariate Shift paper](https://arxiv.org/abs/1502.03167).
    ///
    /// Only 4D and 5D tensors are supported.
    ///
    /// The `epsilon` value has to be the same during training, backpropagation, and inference. This workspace is not required to be clean. Moreover, the workspace does not have to remain unchanged between the forward and backward pass, as it is not used for passing any information.
    ///
    /// This function can accept a `*workspace` pointer to the GPU workspace, and `workSpaceSizeInBytes`, the size of the workspace, from the user.
    ///
    /// The `normOps` input can be used to set this function to perform either only the normalization, or normalization followed by activation, or normalization followed by element-wise addition and then activation.
    ///
    /// When the tensor layout is NCHW, higher performance can be obtained when HW-packed tensors are used for `x`, `dy`, `dx`.
    ///
    /// Higher performance for [`cudnnNormMode_t::CUDNN_NORM_PER_CHANNEL`] mode can be obtained when the following conditions are true.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN library descriptor. For more information, refer to [`cudnnHandle_t`].
    /// - `mode`: Input. Mode of operation (per-channel or per-activation). For more information, refer to [`cudnnNormMode_t`].
    /// - `normOps`: Input. Mode of post-operative. Currently [`cudnnNormOps_t::CUDNN_NORM_OPS_NORM_ACTIVATION`] and [`cudnnNormOps_t::CUDNN_NORM_OPS_NORM_ADD_ACTIVATION`] are only supported in the NHWC layout. For more information, refer to [`cudnnNormOps_t`]. This input can be used to set this function to perform either only the normalization, or normalization followed by activation, or normalization followed by element-wise addition and then activation.
    /// - `algo`: Input. Algorithm to be performed. For more information, refer to [`cudnnNormAlgo_t`].
    /// - `dNormScaleBiasDesc`: Input. Shared tensor descriptor for the following six tensors: `normScaleData`, `normBiasData`, `dNormScaleData`, and `dNormBiasData`. The dimensions for this tensor descriptor are dependent on normalization mode.
    ///
    /// The data type of this tensor descriptor must be `float` for FP16 and FP32 input tensors and `double` for FP64 input tensors. For more information, refer to [`cudnnTensorDescriptor_t`].
    /// - `epsilon`: Input. Epsilon value used in normalization formula. Its value should be equal to or greater than zero. The same epsilon value should be used in forward and backward functions.
    /// - `normMeanVarDesc`: Input. Shared tensor descriptor for the following tensors: `savedMean` and `savedInvVariance`. The dimensions for this tensor descriptor are dependent on normalization mode.
    ///
    /// The data type of this tensor descriptor must be `float` for FP16 and FP32 input tensors and `double` for FP64 input tensors. For more information, refer to [`cudnnTensorDescriptor_t`].
    /// - `activationDesc`: Input. Descriptor for the activation operation. When the `normOps` input is set to either [`cudnnNormOps_t::CUDNN_NORM_OPS_NORM_ACTIVATION`] or [`cudnnNormOps_t::CUDNN_NORM_OPS_NORM_ADD_ACTIVATION`] then this activation is used, otherwise the user may pass `NULL`.
    /// - `workSpaceSizeInBytes`: Input. The size of the workspace. It must be large enough to trigger the fast NHWC semi-persistent kernel by this function.
    /// - `reserveSpaceSizeInBytes`: Input. The size of the `reserveSpace`. It must be equal or larger than the amount required by [`cudnnGetNormalizationTrainingReserveSpaceSize`].
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * Any of the pointers `alphaDataDiff`, `betaDataDiff`, `alphaParamDiff`, `betaParamDiff`, `xData`, `dyData`, `dxData`, `normScaleData`, `dNormScaleData`, and `dNormBiasData` is `NULL`.
    /// * The number of `xDesc`, `yDesc`, or `dxDesc` tensor descriptor dimensions is not within the range of \[4,5\] (only 4D and 5D tensors are supported).
    /// * `dNormScaleBiasDesc` dimensions not 1xCx1x1 for 4D and 1xCx1x1x1 for 5D for per-channel, and are not 1xCxHxW for 4D and 1xCxDxHxW for 5D for per-activation mode.
    /// * Exactly one of `savedMean`, `savedInvVariance` pointers is `NULL`.
    /// * `epsilon` value is less than zero.
    /// * Dimensions or data types mismatch for any pair of `xDesc`, `dyDesc`, `dxDesc`, `dNormScaleBiasDesc`, or `normMeanVarDesc`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: The function does not support the provided configuration.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The computation was performed successfully.
    #[deprecated]
    pub fn cudnnNormalizationBackward(
        handle: cudnnHandle_t,
        mode: cudnnNormMode_t,
        normOps: cudnnNormOps_t,
        algo: cudnnNormAlgo_t,
        alphaDataDiff: *const ::core::ffi::c_void,
        betaDataDiff: *const ::core::ffi::c_void,
        alphaParamDiff: *const ::core::ffi::c_void,
        betaParamDiff: *const ::core::ffi::c_void,
        xDesc: cudnnTensorDescriptor_t,
        xData: *const ::core::ffi::c_void,
        yDesc: cudnnTensorDescriptor_t,
        yData: *const ::core::ffi::c_void,
        dyDesc: cudnnTensorDescriptor_t,
        dyData: *const ::core::ffi::c_void,
        dzDesc: cudnnTensorDescriptor_t,
        dzData: *mut ::core::ffi::c_void,
        dxDesc: cudnnTensorDescriptor_t,
        dxData: *mut ::core::ffi::c_void,
        dNormScaleBiasDesc: cudnnTensorDescriptor_t,
        normScaleData: *const ::core::ffi::c_void,
        normBiasData: *const ::core::ffi::c_void,
        dNormScaleData: *mut ::core::ffi::c_void,
        dNormBiasData: *mut ::core::ffi::c_void,
        epsilon: f64,
        normMeanVarDesc: cudnnTensorDescriptor_t,
        savedMean: *const ::core::ffi::c_void,
        savedInvVariance: *const ::core::ffi::c_void,
        activationDesc: cudnnActivationDescriptor_t,
        workSpace: *mut ::core::ffi::c_void,
        workSpaceSizeInBytes: size_t,
        reserveSpace: *mut ::core::ffi::c_void,
        reserveSpaceSizeInBytes: size_t,
        groupCnt: ::core::ffi::c_int,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function computes the gradient of a grid generation operation.
    ///
    /// Only 2D transformation is supported.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN context.
    /// - `stDesc`: Input. Previously created spatial transformer descriptor object.
    /// - `dgrid`: Input. Data pointer to GPU memory contains the input differential data.
    /// - `dtheta`: Output. Data pointer to GPU memory contains the output differential data.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * `handle` is `NULL`.
    /// * One of the parameters `dgrid` or `dtheta` is `NULL`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_EXECUTION_FAILED`]: The function failed to launch on the GPU.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: The function does not support the provided configuration. Some examples incldue:
    ///
    /// * The dimension of the transformed tensor specified in `stDesc` > 4.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The call was successful.
    pub fn cudnnSpatialTfGridGeneratorBackward(
        handle: cudnnHandle_t,
        stDesc: cudnnSpatialTransformerDescriptor_t,
        dgrid: *const ::core::ffi::c_void,
        dtheta: *mut ::core::ffi::c_void,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function computes the gradient of a sampling operation.
    ///
    /// Only 2D transformation is supported.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN context.
    /// - `stDesc`: Input. Previously created spatial transformer descriptor object.
    /// - `xDesc`: Input. Handle to the previously initialized input tensor descriptor.
    /// - `x`: Input. Data pointer to GPU memory associated with the tensor descriptor `xDesc`.
    /// - `dxDesc`: Input. Handle to the previously initialized output differential tensor descriptor.
    /// - `dx`: Output. Data pointer to GPU memory associated with the output tensor descriptor `dxDesc`.
    /// - `dyDesc`: Input. Handle to the previously initialized input differential tensor descriptor.
    /// - `dy`: Input. Data pointer to GPU memory associated with the tensor descriptor `dyDesc`.
    /// - `grid`: Input. A grid of coordinates generated by [`cudnnSpatialTfGridGeneratorForward`].
    /// - `dgrid`: Output. Data pointer to GPU memory contains the output differential data.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * `handle` is `NULL`.
    /// * One of the parameters `x`, `dx`, `y`, `dy`, `grid`, and `dgrid` is `NULL`.
    /// * The dimension of `dy` differs from those specified in `stDesc`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_EXECUTION_FAILED`]: The function failed to launch on the GPU.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: The function does not support the provided configuration. Some examples include:
    ///
    /// * The dimension of transformed tensor > 4.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The call was successful.
    pub fn cudnnSpatialTfSamplerBackward(
        handle: cudnnHandle_t,
        stDesc: cudnnSpatialTransformerDescriptor_t,
        alpha: *const ::core::ffi::c_void,
        xDesc: cudnnTensorDescriptor_t,
        x: *const ::core::ffi::c_void,
        beta: *const ::core::ffi::c_void,
        dxDesc: cudnnTensorDescriptor_t,
        dx: *mut ::core::ffi::c_void,
        alphaDgrid: *const ::core::ffi::c_void,
        dyDesc: cudnnTensorDescriptor_t,
        dy: *const ::core::ffi::c_void,
        grid: *const ::core::ffi::c_void,
        betaDgrid: *const ::core::ffi::c_void,
        dgrid: *mut ::core::ffi::c_void,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function performs backward dropout operation over `dy` returning results in `dx`. If during forward dropout operation value from `x` was propagated to `y` then during backward operation value from `dy` will be propagated to `dx`, otherwise, `dx` value will be set to `0`.
    ///
    /// Better performance is obtained for fully packed tensors.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN context.
    /// - `dropoutDesc`: Input. Previously created dropout descriptor object.
    /// - `dy`: Input. Pointer to data of the tensor described by the `dyDesc` descriptor.
    /// - `dx`: Output. Pointer to data of the tensor described by the `dxDesc` descriptor.
    /// - `reserveSpace`: Input. Pointer to user-allocated GPU memory used by this function. It is expected that `reserveSpace` was populated during a call to [`cudnnDropoutForward`] and has not been changed.
    /// - `reserveSpaceSizeInBytes`: Input. Specifies the size in bytes of the provided memory for the reserve space.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * The number of elements of input tensor and output tensors differ.
    /// * The `datatype` of the input tensor and output tensors differs.
    /// * The strides of the input tensor and output tensors differ and in-place operation is used (meaning, `x` and `y` pointers are equal).
    /// * The provided `reserveSpaceSizeInBytes` is less than the value returned by [`cudnnDropoutGetReserveSpaceSize`].
    /// * [`cudnnSetDropoutDescriptor`] has not been called on `dropoutDesc` with non-`NULL` states argument.
    /// - [`cudnnStatus_t::CUDNN_STATUS_EXECUTION_FAILED`]: The function failed to launch on the GPU.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: The function does not support the provided configuration.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The call was successful.
    pub fn cudnnDropoutBackward(
        handle: cudnnHandle_t,
        dropoutDesc: cudnnDropoutDescriptor_t,
        dydesc: cudnnTensorDescriptor_t,
        dy: *const ::core::ffi::c_void,
        dxdesc: cudnnTensorDescriptor_t,
        dx: *mut ::core::ffi::c_void,
        reserveSpace: *mut ::core::ffi::c_void,
        reserveSpaceSizeInBytes: size_t,
    ) -> cudnnStatus_t;
}
/// [`cudnnRNNAlgo_t`] is an enumerated type used to specify the algorithm.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnRNNAlgo_t {
    /// This algorithm uses cuBLASLt to perform all matrix multiplications and dedicated kernels for cell-specific operations such as applying nonlinearities or adding biases. This is the most versatile RNN algorithm. It supports pseudo-random dropout masks between RNN layers, variable length sequences in unpacked data layouts, recurrent projection in LSTM models, and multiple choices for RNN biases: no bias, one bias, or two biases. The algorithm traverses RNN cells layer-by-layer or in a diagonal pattern through multiple layers with a certain number of time steps grouped into one “comptational chunk”. Whenever possible GEMMs are executed in parallel CUDA streams. This algorithm is expected to deliver robust performance across a wide range of RNN configurations. It is also supported on a broad range of architectures, including the oldest GPUs.
    CUDNN_RNN_ALGO_STANDARD = 0,
    /// Input GEMMs in this algorithm are performed by cuBLASLt. Recurrent GEMMs, typically with fused element-wise cell operations, are handled by persistent kernels that require all thread blocks of a grid to run concurrently on GPU and communicate. All recurrent weights are stored collaboratively in stream multi-processor (SM) registers and optionally in shared memory. RNN cells are traversed layer-by-layer. GPUs with a larger number of SMs can handle longer hidden state vectors using this algorithm. This method is expected to be fast when the first dimension of the input tensor is small (meaning, a small minibatch). [`cudnnRNNAlgo_t::CUDNN_RNN_ALGO_PERSIST_STATIC`] is supported on devices with compute capability >= 6.0.
    CUDNN_RNN_ALGO_PERSIST_STATIC = 1,
    /// The recurrent parts of the network are executed using a *persistent kernel* approach. This method is expected to perform reasonably well for small RNN models. [`cudnnRNNAlgo_t::CUDNN_RNN_ALGO_PERSIST_DYNAMIC`] kernels are compiled at runtime and are optimized for specific parameters of the RNN model and active GPU. The limits on the maximum size of a hidden vector when using [`cudnnRNNAlgo_t::CUDNN_RNN_ALGO_PERSIST_DYNAMIC`] may be higher than the corresponding limits of [`cudnnRNNAlgo_t::CUDNN_RNN_ALGO_PERSIST_STATIC`]. This algorithm does not utilize NVIDIA Tensor Cores. [`cudnnRNNAlgo_t::CUDNN_RNN_ALGO_PERSIST_DYNAMIC`] is supported on devices with compute capability >= 6.0.
    CUDNN_RNN_ALGO_PERSIST_DYNAMIC = 2,
    /// Despite its name, this algorithm does not rely on persistent GPU kernels (all thread blocks being active at the same time) but in other aspects it operates similarly to [`cudnnRNNAlgo_t::CUDNN_RNN_ALGO_PERSIST_STATIC`]. Input GEMMs for all time-steps are performed by cuBLASLt and recurrent GEMMs with fused element-wise operations are handled by “regular” CUDA thread blocks. One thread block collaboratively loads all recurrent weights of one layer (square matrix) and a small number of input data vectors to compute the same number of output elements without any synchronization with other thread blocks. The algorithm is limited by available register resources so the hidden vector size cannot be very large, for example, up to 192 elements for LSTM/GRU cells and up to 384 elements for RELU/TANH cells in the forward pass. This algorithm could be surprisingly fast and it scales well with the number of available SMs for large batch sizes.
    CUDNN_RNN_ALGO_PERSIST_STATIC_SMALL_H = 3,
    CUDNN_RNN_ALGO_COUNT = 4,
}
/// [`cudnnForwardMode_t`] is an enumerated type to specify inference or training mode in RNN API. This parameter allows the cuDNN library to tune more precisely the size of the workspace buffer that could be different in inference and training regimens.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnForwardMode_t {
    /// Selects the inference mode.
    CUDNN_FWD_MODE_INFERENCE = 0,
    /// Selects the training mode.
    CUDNN_FWD_MODE_TRAINING = 1,
}
/// [`cudnnRNNMode_t`] is an enumerated type used to specify the type of network.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnRNNMode_t {
    /// A single-gate recurrent neural network with a ReLU activation function.
    ///
    /// In the forward pass, the output h t for a given iteration can be computed from the recurrent input h t-1 and the previous layer input x t, given the matrices `W`, `R`, the bias vectors, and where `ReLU(x) = max(x, 0)`.
    ///
    /// If `cudnnRNNBiasMode_t biasMode` in `rnnDesc` is [`cudnnRNNBiasMode_t::CUDNN_RNN_DOUBLE_BIAS`] (default mode), then the following equation with biases b W and b R applies:
    ///
    /// h t = ReLU(W i x t + R i h t-1 + b Wi + b Ri)
    ///
    /// If `cudnnRNNBiasMode_t biasMode` in `rnnDesc` is [`cudnnRNNBiasMode_t::CUDNN_RNN_SINGLE_INP_BIAS`] or [`cudnnRNNBiasMode_t::CUDNN_RNN_SINGLE_REC_BIAS`], then the following equation with bias `b` applies:
    ///
    /// h t = ReLU(W i x t + R i h t-1 + b i)
    ///
    /// If `cudnnRNNBiasMode_t biasMode` in `rnnDesc` is [`cudnnRNNBiasMode_t::CUDNN_RNN_NO_BIAS`], then the following equation applies:
    ///
    /// h t = ReLU(W i x t + R i h t-1).
    CUDNN_RNN_RELU = 0,
    /// A single-gate recurrent neural network with a `tanh` activation function.
    ///
    /// In the forward pass, the output h t for a given iteration can be computed from the recurrent input h t-1 and the previous layer input x t, given the matrices `W`, `R` the bias vectors, and where `tanh` is the hyperbolic tangent function.
    ///
    /// If `cudnnRNNBiasMode_t biasMode` in `rnnDesc` is [`cudnnRNNBiasMode_t::CUDNN_RNN_DOUBLE_BIAS`] (default mode), then the following equation with biases b W and b R applies:
    ///
    /// h t = tanh(W i x t + R i h t-1 + b Wi + b Ri)
    ///
    /// If `cudnnRNNBiasMode_t biasMode` in `rnnDesc` is [`cudnnRNNBiasMode_t::CUDNN_RNN_SINGLE_INP_BIAS`] or [`cudnnRNNBiasMode_t::CUDNN_RNN_SINGLE_REC_BIAS`], then the following equation with bias `b` applies:
    ///
    /// h t = tanh(W i x t + R i h t-1 + b i)
    ///
    /// If `cudnnRNNBiasMode_t biasMode` in `rnnDesc` is [`cudnnRNNBiasMode_t::CUDNN_RNN_NO_BIAS`], then the following equation applies:
    ///
    /// h t = tanh(W i x t + R i h t-1).
    CUDNN_RNN_TANH = 1,
    /// A four-gate LSTM (Long Short-Term Memory) network with no peephole connections.
    ///
    /// In the forward pass, the output h t and cell output c t for a given iteration can be computed from the recurrent input h t-1, the cell input c t-1 and the previous layer input x t, given the matrices `W`, `R`, and the bias vectors. In addition, the following applies:
    ///
    /// * σ is the sigmoid operator such that: σ(x) = 1 / (1 + e -x),
    /// * ◦ represents a point-wise multiplication,
    /// * `tanh` is the hyperbolic tangent function, and
    /// * i t, f t, o t, c’ t represent the input, forget, output and new gates respectively.
    ///
    /// If `cudnnRNNBiasMode_t biasMode` in `rnnDesc` is [`cudnnRNNBiasMode_t::CUDNN_RNN_DOUBLE_BIAS`] (default mode), then the following equations with biases b W and b R apply:
    ///
    /// i t = σ(W i x t + R i h t-1 + b Wi + b Ri)
    ///
    /// f t = σ(W f x t + R f h t-1 + b Wf + b Rf)
    ///
    /// o t = σ(W o x t + R o h t-1 + b Wo + b Ro)
    ///
    /// c’ t = tanh(W c x t + R c h t-1 + b Wc + b Rc)
    ///
    /// c t = f t ◦ c t-1 + i t ◦ c’ t
    ///
    /// h t = o t ◦ tanh(c t)
    ///
    /// If `cudnnRNNBiasMode_t biasMode` in `rnnDesc` is [`cudnnRNNBiasMode_t::CUDNN_RNN_SINGLE_INP_BIAS`] or [`cudnnRNNBiasMode_t::CUDNN_RNN_SINGLE_REC_BIAS`], then the following equations with bias `b` apply:
    ///
    /// i t = σ(W i x t + R i h t-1 + b i)
    ///
    /// f t = σ(W f x t + R f h t-1 + b f)
    ///
    /// o t = σ(W o x t + R o h t-1 + b o)
    ///
    /// c’ t = tanh(W c x t + R c h t-1 + b c)
    ///
    /// c t = f t ◦ c t-1 + i t ◦ c’ t
    ///
    /// h t = o t ◦ tanh(c t)
    ///
    /// If `cudnnRNNBiasMode_t biasMode` in `rnnDesc` is [`cudnnRNNBiasMode_t::CUDNN_RNN_NO_BIAS`], then the following equations apply:
    ///
    /// i t = σ(W i x t + R i h t-1)
    ///
    /// f t = σ(W f x t + R f h t-1)
    ///
    /// o t = σ(W o x t + R o h t-1)
    ///
    /// c’ t = tanh(W c x t + R c h t-1)
    ///
    /// c t = f t ◦ c t-1 + i t ◦ c’ t
    ///
    /// h t = o t ◦ tanh(c t).
    CUDNN_LSTM = 2,
    /// A three-gate network consisting of Gated Recurrent Units (GRU).
    ///
    /// In the forward pass, the output h t for a given iteration can be computed from the recurrent input h t-1 and the previous layer input x t given matrices `W`, `R`, and the bias vectors. In addition, the following applies:
    ///
    /// * σ is the sigmoid operator such that: σ(x) = 1 / (1 + e -x),
    /// * ◦ represents a point-wise multiplication,
    /// * `tanh` is the hyperbolic tangent function, and
    /// * i t, r t, h’ t represent the input, reset, and new gates respectively.
    ///
    /// If `cudnnRNNBiasMode_t biasMode` in `rnnDesc` is [`cudnnRNNBiasMode_t::CUDNN_RNN_DOUBLE_BIAS`] (default mode), then the following equations with biases b W and b R apply:
    ///
    /// i t = σ(W i x t + R i h t-1 + b Wi + b Ru)
    ///
    /// r t = σ(W r x t + R r h t-1 + b Wr + b Rr)
    ///
    /// h’ t = tanh(W h x t + r t ◦ (R h h t-1 + b Rh) + b Wh)
    ///
    /// h t = (1 - i t) ◦ h’ t + i t ◦ h t-1
    ///
    /// If `cudnnRNNBiasMode_t biasMode` in `rnnDesc` is [`cudnnRNNBiasMode_t::CUDNN_RNN_SINGLE_INP_BIAS`], then the following equations with bias `b` apply:
    ///
    /// i t = σ(W i x t + R i h t-1 + b i)
    ///
    /// r t = σ(W r x t + R r h t-1 + b r)
    ///
    /// h’ t = tanh(W h x t + r t ◦ (R h h t-1) + b Wh)
    ///
    /// h t = (1 - i t) ◦ h’ t + i t ◦ h t-1
    ///
    /// If `cudnnRNNBiasMode_t biasMode` in `rnnDesc` is [`cudnnRNNBiasMode_t::CUDNN_RNN_SINGLE_REC_BIAS`], then the following equations with bias `b` apply:
    ///
    /// i t = σ(W i x t + R i h t-1 + b i)
    ///
    /// r t = σ(W r x t + R r h t-1 + b r)
    ///
    /// h’ t = tanh(W h x t + r t ◦ (R h h t-1 + b Rh))
    ///
    /// h t = (1 - i t) ◦ h’ t + i t ◦ h t-1
    ///
    /// If `cudnnRNNBiasMode_t biasMode` in `rnnDesc` is [`cudnnRNNBiasMode_t::CUDNN_RNN_NO_BIAS`], then the following equations apply:
    ///
    /// i t = σ(W i x t + R i h t-1)
    ///
    /// r t = σ(W r x t + R r h t-1)
    ///
    /// h’ t = tanh(W h x t + rt ◦ (R h h t-1))
    ///
    /// h t = (1 - i t) ◦ h’ t + i t ◦ h t-1.
    CUDNN_GRU = 3,
}
/// [`cudnnRNNBiasMode_t`] is an enumerated type used to specify the number of bias vectors for RNN functions. Refer to the description of the [`cudnnRNNMode_t`] enumerated type for the equations for each cell type based on the bias mode.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnRNNBiasMode_t {
    /// Applies RNN cell formulas that do not use biases.
    CUDNN_RNN_NO_BIAS = 0,
    /// Applies RNN cell formulas that use one input bias vector in the input GEMM.
    CUDNN_RNN_SINGLE_INP_BIAS = 1,
    /// Applies RNN cell formulas that use two bias vectors.
    CUDNN_RNN_DOUBLE_BIAS = 2,
    /// Applies RNN cell formulas that use one recurrent bias vector in the recurrent GEMM.
    CUDNN_RNN_SINGLE_REC_BIAS = 3,
}
/// [`cudnnDirectionMode_t`] is an enumerated type used to specify the recurrence pattern.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnDirectionMode_t {
    /// The network iterates recurrently from the first input to the last.
    CUDNN_UNIDIRECTIONAL = 0,
    /// Each layer of the network iterates recurrently from the first input to the last and separately from the last input to the first. The outputs of the two are concatenated at each iteration giving the output of the layer.
    CUDNN_BIDIRECTIONAL = 1,
}
/// [`cudnnRNNInputMode_t`] is an enumerated type used to specify the behavior of the first layer.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnRNNInputMode_t {
    /// A biased matrix multiplication is performed at the input of the first recurrent layer.
    CUDNN_LINEAR_INPUT = 0,
    /// No operation is performed at the input of the first recurrent layer. If [`cudnnRNNInputMode_t::CUDNN_SKIP_INPUT`] is used the leading dimension of the input tensor must be equal to the hidden state size of the network.
    CUDNN_SKIP_INPUT = 1,
}
/// [`cudnnRNNClipMode_t`] is an enumerated type used to select the LSTM cell clipping mode.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnRNNClipMode_t {
    /// Disables LSTM cell clipping.
    CUDNN_RNN_CLIP_NONE = 0,
    /// Enables LSTM cell clipping.
    CUDNN_RNN_CLIP_MINMAX = 1,
}
/// [`cudnnRNNDataLayout_t`] is an enumerated type used to select the RNN data layout. It is used in the API calls [`cudnnGetRNNDataDescriptor`] and [`cudnnSetRNNDataDescriptor`].
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnRNNDataLayout_t {
    /// Data layout is padded, with outer stride from one time-step to the next.
    CUDNN_RNN_DATA_LAYOUT_SEQ_MAJOR_UNPACKED = 0,
    /// The sequence length is sorted and packed as in the basic RNN API.
    CUDNN_RNN_DATA_LAYOUT_SEQ_MAJOR_PACKED = 1,
    /// Data layout is padded, with outer stride from one batch to the next.
    CUDNN_RNN_DATA_LAYOUT_BATCH_MAJOR_UNPACKED = 2,
}
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct cudnnRNNStruct {
    _unused: [u8; 0],
}
/// [`cudnnRNNDescriptor_t`] is a pointer to an opaque structure holding the description of an RNN operation. [`cudnnCreateRNNDescriptor`] is used to create one instance.
pub type cudnnRNNDescriptor_t = *mut cudnnRNNStruct;
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct cudnnRNNDataStruct {
    _unused: [u8; 0],
}
/// [`cudnnRNNDataDescriptor_t`] is a pointer to an opaque structure holding the description of an RNN data set. The function [`cudnnCreateRNNDataDescriptor`] is used to create one instance, and [`cudnnSetRNNDataDescriptor`] must be used to initialize this instance.
pub type cudnnRNNDataDescriptor_t = *mut cudnnRNNDataStruct;
unsafe extern "C" {
    /// This function creates a generic RNN descriptor object by allocating the memory needed to hold its opaque structure.
    ///
    /// # Parameters
    ///
    /// - `rnnDesc`: Output. Pointer to where the address to the newly created RNN descriptor should be written.
    ///
    /// # Return value
    ///
    /// - `CUDNN_STATUS_ALLOC_FAILED`: The resources could not be allocated.
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: The `rnnDesc` argument is `NULL`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The object was created successfully.
    pub fn cudnnCreateRNNDescriptor(rnnDesc: *mut cudnnRNNDescriptor_t) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function destroys a previously created RNN descriptor object. Invoking [`cudnnDestroyRNNDescriptor`] with the `NULL` argument is a no operation (NOP).
    ///
    /// The [`cudnnDestroyRNNDescriptor`] function is not able to detect if the `rnnDesc` argument holds a valid address. Undefined behavior will occur in cases of passing an invalid pointer, not returned by the [`cudnnCreateRNNDescriptor`] function, or in the double deletion scenario of a valid address.
    ///
    /// # Parameters
    ///
    /// - `rnnDesc`: Input. Pointer to the RNN descriptor object to be destroyed.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The object was destroyed successfully.
    pub fn cudnnDestroyRNNDescriptor(rnnDesc: cudnnRNNDescriptor_t) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function initializes a previously created RNN descriptor object. The RNN descriptor configured by [`cudnnSetRNNDescriptor_v8`] was enhanced to store all information needed to compute the total number of adjustable weights/biases in the RNN model.
    ///
    /// # Parameters
    ///
    /// - `rnnDesc`: Input. A previously initialized RNN descriptor.
    /// - `algo`: Input. RNN algo ([`cudnnRNNAlgo_t::CUDNN_RNN_ALGO_STANDARD`], [`cudnnRNNAlgo_t::CUDNN_RNN_ALGO_PERSIST_STATIC`], [`cudnnRNNAlgo_t::CUDNN_RNN_ALGO_PERSIST_DYNAMIC`], or [`cudnnRNNAlgo_t::CUDNN_RNN_ALGO_PERSIST_STATIC_SMALL_H`]).
    /// - `cellMode`: Input. Specifies the RNN cell type in the entire model ([`cudnnRNNMode_t::CUDNN_RNN_RELU`], [`cudnnRNNMode_t::CUDNN_RNN_TANH`], `CUDNN_RNN_LSTM`, `CUDNN_RNN_GRU`).
    /// - `biasMode`: Input. Sets the number of bias vectors ([`cudnnRNNBiasMode_t::CUDNN_RNN_NO_BIAS`], [`cudnnRNNBiasMode_t::CUDNN_RNN_SINGLE_INP_BIAS`], [`cudnnRNNBiasMode_t::CUDNN_RNN_SINGLE_REC_BIAS`], [`cudnnRNNBiasMode_t::CUDNN_RNN_DOUBLE_BIAS`]). The two single bias settings are functionally the same for `RELU`, `TANH`, and `LSTM` cell types. For differences in GRU cells, refer to the description of [`cudnnRNNMode_t::CUDNN_GRU`] in the [`cudnnRNNMode_t`] enumerated type. [`cudnnRNNAlgo_t::CUDNN_RNN_ALGO_STANDARD`] accepts all bias modes. The remaining RNN algorithms work with [`cudnnRNNBiasMode_t::CUDNN_RNN_DOUBLE_BIAS`] only.
    /// - `dirMode`: Input. Specifies the recurrence pattern: [`cudnnDirectionMode_t::CUDNN_UNIDIRECTIONAL`] or [`cudnnDirectionMode_t::CUDNN_BIDIRECTIONAL`]. In bidirectional RNNs, the hidden states passed between physical layers are concatenations of forward and backward hidden states.
    /// - `inputMode`: Input. Specifies how the input to the RNN model is processed by the first layer. When `inputMode` is [`cudnnRNNInputMode_t::CUDNN_LINEAR_INPUT`], original input vectors of size `inputSize` are multiplied by the weight matrix to obtain vectors of `hiddenSize`. When `inputMode` is [`cudnnRNNInputMode_t::CUDNN_SKIP_INPUT`], the original input vectors to the first layer are used as is without multiplying them by the weight matrix.
    /// - `dataType`: Input. Specifies data type for RNN weights/biases and input and output data.
    /// - `mathPrec`: Input. This parameter is used to control the compute math precision in the RNN model. The following applies:
    ///
    /// * For the input/output in FP16, the parameter `mathPrec` can be [`cudnnDataType_t::CUDNN_DATA_HALF`] or [`cudnnDataType_t::CUDNN_DATA_FLOAT`].
    /// * For the input/output in FP32, the parameter `mathPrec` can only be [`cudnnDataType_t::CUDNN_DATA_FLOAT`].
    /// * For the input/output in FP64, double type, the parameter `mathPrec` can only be [`cudnnDataType_t::CUDNN_DATA_DOUBLE`].
    /// - `mathType`: Input. Sets the preferred option to use NVIDIA Tensor Cores accelerators on Volta (SM 7.0) or higher GPUs.
    ///
    /// * When `dataType` is [`cudnnDataType_t::CUDNN_DATA_HALF`], the `mathType` parameter can be [`cudnnMathType_t::CUDNN_DEFAULT_MATH`] or [`cudnnMathType_t::CUDNN_TENSOR_OP_MATH`]. The `ALLOW_CONVERSION` setting is treated the same as [`cudnnMathType_t::CUDNN_TENSOR_OP_MATH`] for this data type.
    /// * When `dataType` is [`cudnnDataType_t::CUDNN_DATA_FLOAT`], the `mathType` parameter can be [`cudnnMathType_t::CUDNN_DEFAULT_MATH`] or [`cudnnMathType_t::CUDNN_TENSOR_OP_MATH_ALLOW_CONVERSION`]. When the latter settings are used, original weights and intermediate results will be down-converted to [`cudnnDataType_t::CUDNN_DATA_HALF`] before they are used in another recursive iteration.
    /// * When `dataType` is [`cudnnDataType_t::CUDNN_DATA_DOUBLE`], the `mathType` parameter can be [`cudnnMathType_t::CUDNN_DEFAULT_MATH`].
    ///
    /// This option has an advisory status meaning Tensor Cores may not be always utilized, for example, due to specific GEMM dimensions restrictions.
    /// - `inputSize`: Input. Size of the input vector in the RNN model. When the `inputMode=CUDNN_SKIP_INPUT`, the `inputSize` should match the `hiddenSize` value.
    /// - `hiddenSize`: Input. Size of the hidden state vector in the RNN model. The same hidden size is used in all RNN layers.
    /// - `projSize`: Input. The size of the LSTM cell output after the recurrent projection. When the LSTM projection is enabled, this value should be smaller than `hiddenSize`. When the LSTM projection is disabled, and for all other RNN cell types ([`cudnnRNNMode_t::CUDNN_RNN_RELU`], [`cudnnRNNMode_t::CUDNN_RNN_TANH`], and `CUDNN_RNN_GRU`), `projSize` must be equal to `hiddenSize`. The recurrent projection is an additional matrix multiplication in the LSTM cell to project (compress) hidden state vectors h t into smaller vectors r t = W r h t, where W r is a rectangular matrix with `projSize` rows and `hiddenSize` columns. When the recurrent projection is enabled, the output of the LSTM cell (both to the next layer and unrolled in-time) is r t instead of h t. The recurrent projection can be enabled for LSTM cells and [`cudnnRNNAlgo_t::CUDNN_RNN_ALGO_STANDARD`] only.
    /// - `numLayers`: Input. Number of stacked, physical layers in the deep RNN model. When `dirMode= CUDNN_BIDIRECTIONAL`, the physical layer consists of two pseudo-layers corresponding to forward and backward directions.
    /// - `dropoutDesc`: Input. Handle to a previously created and initialized dropout descriptor. Dropout operation will be applied between physical layers. A single layer network will have no dropout applied. Dropout is used in the training mode only.
    /// - `auxFlags`: Input. This argument is used to pass miscellaneous switches that do not require additional numerical values to configure the corresponding feature. In future cuDNN releases, this parameter will be used to extend the RNN functionality without adding new API functions (applicable options should be bitwise `OR-ed`). Currently, this parameter is used to enable or disable padded input/output ([`CUDNN_RNN_PADDED_IO_DISABLED`], [`CUDNN_RNN_PADDED_IO_ENABLED`]). When the padded I/O is enabled, layouts [`cudnnRNNDataLayout_t::CUDNN_RNN_DATA_LAYOUT_SEQ_MAJOR_UNPACKED`] and [`cudnnRNNDataLayout_t::CUDNN_RNN_DATA_LAYOUT_BATCH_MAJOR_UNPACKED`] are permitted in RNN data descriptors.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: An invalid input argument was detected.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: An incompatible or unsupported combination of input arguments was detected.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The RNN descriptor was configured successfully.
    pub fn cudnnSetRNNDescriptor_v8(
        rnnDesc: cudnnRNNDescriptor_t,
        algo: cudnnRNNAlgo_t,
        cellMode: cudnnRNNMode_t,
        biasMode: cudnnRNNBiasMode_t,
        dirMode: cudnnDirectionMode_t,
        inputMode: cudnnRNNInputMode_t,
        dataType: cudnnDataType_t,
        mathPrec: cudnnDataType_t,
        mathType: cudnnMathType_t,
        inputSize: i32,
        hiddenSize: i32,
        projSize: i32,
        numLayers: i32,
        dropoutDesc: cudnnDropoutDescriptor_t,
        auxFlags: u32,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function retrieves RNN network parameters that were configured by [`cudnnSetRNNDescriptor_v8`]. The user can assign `NULL` to any pointer except `rnnDesc` when the retrieved value is not needed. The function does not check the validity of retrieved parameters.
    ///
    /// # Parameters
    ///
    /// - `rnnDesc`: Input. A previously created and initialized RNN descriptor.
    /// - `algo`: Output. Pointer to where RNN algorithm type should be stored.
    /// - `cellMode`: Output. Pointer to where RNN cell type should be saved.
    /// - `biasMode`: Output. Pointer to where RNN bias mode [`cudnnRNNBiasMode_t`] should be saved.
    /// - `dirMode`: Output. Pointer to where RNN unidirectional/bidirectional mode should be saved.
    /// - `inputMode`: Output. Pointer to where the mode of the first RNN layer should be saved.
    /// - `dataType`: Output. Pointer to where the data type of RNN weights/biases should be stored.
    /// - `mathPrec`: Output. Pointer to where the math precision type should be stored.
    /// - `mathType`: Output. Pointer to where the preferred option for Tensor Cores are saved.
    /// - `inputSize`: Output. Pointer to where the RNN input vector size is stored.
    /// - `hiddenSize`: Output. Pointer to where the size of the hidden state should be stored (the same value is used in every RNN layer).
    /// - `projSize`: Output. Pointer to where the LSTM cell output size after the recurrent projection is stored.
    /// - `numLayers`: Output. Pointer to where the number of RNN layers should be stored.
    /// - `dropoutDesc`: Output. Pointer to where the handle to a previously configured dropout descriptor should be stored.
    /// - `auxFlags`: Output. Pointer to miscellaneous RNN options (flags) that do not require passing additional numerical values to configure.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: An invalid input argument was found (`rnnDesc` was `NULL`).
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_INITIALIZED`]: The cuDNN library was not initialized properly.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: RNN parameters were successfully retrieved from the RNN descriptor.
    pub fn cudnnGetRNNDescriptor_v8(
        rnnDesc: cudnnRNNDescriptor_t,
        algo: *mut cudnnRNNAlgo_t,
        cellMode: *mut cudnnRNNMode_t,
        biasMode: *mut cudnnRNNBiasMode_t,
        dirMode: *mut cudnnDirectionMode_t,
        inputMode: *mut cudnnRNNInputMode_t,
        dataType: *mut cudnnDataType_t,
        mathPrec: *mut cudnnDataType_t,
        mathType: *mut cudnnMathType_t,
        inputSize: *mut i32,
        hiddenSize: *mut i32,
        projSize: *mut i32,
        numLayers: *mut i32,
        dropoutDesc: *mut cudnnDropoutDescriptor_t,
        auxFlags: *mut u32,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// Sets the LSTM cell clipping mode. The LSTM clipping is disabled by default. When enabled, clipping is applied to all layers. This [`cudnnRNNSetClip_v8`] function does not affect the work, reserve, and weight-space buffer sizes and may be called multiple times.
    ///
    /// # Parameters
    ///
    /// - `rnnDesc`: Input. A previously initialized RNN descriptor.
    /// - `clipMode`: Input. Enables or disables the LSTM cell clipping. When `clipMode` is set to [`cudnnRNNClipMode_t::CUDNN_RNN_CLIP_NONE`] no LSTM cell state clipping is performed. When `clipMode` is [`cudnnRNNClipMode_t::CUDNN_RNN_CLIP_MINMAX`] the cell state activation to other units is clipped.
    /// - `clipNanOpt`: Input. When set to [`cudnnNanPropagation_t::CUDNN_PROPAGATE_NAN`] (see the description for [`cudnnNanPropagation_t`]), `NaN` is propagated from the LSTM cell, or it can be set to one of the clipping range boundary values, instead of propagating.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: An invalid input argument was found, for example:
    ///
    /// * `rnnDesc` was `NULL`
    /// * `lclip` > `rclip`
    /// * either `lclip` or `rclip` is `NaN`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The function completed successfully.
    #[deprecated]
    pub fn cudnnRNNSetClip_v8(
        rnnDesc: cudnnRNNDescriptor_t,
        clipMode: cudnnRNNClipMode_t,
        clipNanOpt: cudnnNanPropagation_t,
        lclip: f64,
        rclip: f64,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// Sets the LSTM cell clipping mode. The LSTM clipping is disabled by default. When enabled, clipping is applied to all layers. This [`cudnnRNNSetClip_v8`] function does not affect the work, reserve, and weight-space buffer sizes and may be called multiple times.
    ///
    /// # Parameters
    ///
    /// - `rnnDesc`: Input. A previously initialized RNN descriptor.
    /// - `clipMode`: Input. Enables or disables the LSTM cell clipping. When `clipMode` is set to [`cudnnRNNClipMode_t::CUDNN_RNN_CLIP_NONE`] no LSTM cell state clipping is performed. When `clipMode` is [`cudnnRNNClipMode_t::CUDNN_RNN_CLIP_MINMAX`] the cell state activation to other units is clipped.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: An invalid input argument was found, for example:
    ///
    /// * `rnnDesc` was `NULL`
    /// * `lclip` > `rclip`
    /// * either `lclip` or `rclip` is `NaN`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The function completed successfully.
    pub fn cudnnRNNSetClip_v9(
        rnnDesc: cudnnRNNDescriptor_t,
        clipMode: cudnnRNNClipMode_t,
        lclip: f64,
        rclip: f64,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// Retrieves the current LSTM cell clipping parameters, and stores them in the arguments provided. The user can assign `NULL` to any pointer except `rnnDesc` when the retrieved value is not needed. The function does not check the validity of retrieved parameters.
    ///
    /// # Parameters
    ///
    /// - `rnnDesc`: Input. A previously initialized RNN descriptor.
    /// - `clipMode`: Output. Pointer to the location where the retrieved [`cudnnRNNClipMode_t`] value is stored. The `clipMode` can be [`cudnnRNNClipMode_t::CUDNN_RNN_CLIP_NONE`] in which case no LSTM cell state clipping is being performed; or [`cudnnRNNClipMode_t::CUDNN_RNN_CLIP_MINMAX`], in which case the cell state activation to other units are being clipped.
    /// - `clipNanOpt`: Output. Pointer to the location where the retrieved [`cudnnNanPropagation_t`] value is stored.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: An invalid input argument was found (`rnnDesc` was `NULL`).
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: LSTM clipping parameters were successfully retrieved from the RNN descriptor.
    #[deprecated]
    pub fn cudnnRNNGetClip_v8(
        rnnDesc: cudnnRNNDescriptor_t,
        clipMode: *mut cudnnRNNClipMode_t,
        clipNanOpt: *mut cudnnNanPropagation_t,
        lclip: *mut f64,
        rclip: *mut f64,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// Retrieves the current LSTM cell clipping parameters, and stores them in the arguments provided. The user can assign NULL to any pointer except rnnDesc when the retrieved value is not needed. The function does not check the validity of retrieved parameters.
    ///
    /// # Parameters
    ///
    /// - `rnnDesc`: Input. A previously initialized RNN descriptor.
    /// - `clipMode`: Output. Pointer to the location where the retrieved [`cudnnRNNClipMode_t`] value is stored. The `clipMode` can be [`cudnnRNNClipMode_t::CUDNN_RNN_CLIP_NONE`] in which case no LSTM cell state clipping is being performed; or [`cudnnRNNClipMode_t::CUDNN_RNN_CLIP_MINMAX`], in which case the cell state activation to other units are being clipped.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: An invalid input argument was found (`rnnDesc` was `NULL`).
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: LSTM clipping parameters were successfully retrieved from the RNN descriptor.
    pub fn cudnnRNNGetClip_v9(
        rnnDesc: cudnnRNNDescriptor_t,
        clipMode: *mut cudnnRNNClipMode_t,
        lclip: *mut f64,
        rclip: *mut f64,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function compiles the RNN persistent code using CUDA runtime compilation library (NVRTC) when the [`cudnnRNNAlgo_t::CUDNN_RNN_ALGO_PERSIST_DYNAMIC`] algo is selected. The code is tailored to the current GPU and specific hyperparameters (`miniBatch`). This call is expected to be expensive in terms of runtime and should be invoked infrequently. Note that the [`cudnnRNNAlgo_t::CUDNN_RNN_ALGO_PERSIST_DYNAMIC`] algo does not support variable length sequences within the batch.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN context.
    /// - `rnnDesc`: Input. A previously initialized RNN descriptor.
    /// - `miniBatch`: Input. The exact number of sequences in a batch.
    ///
    /// # Return value
    ///
    /// - `CUDNN_STATUS_ALLOC_FAILED`: The resources could not be allocated.
    /// - `CUDNN_STATUS_MAPPING_ERROR`: A GPU/CUDA resource, such as a texture object, shared memory, or zero-copy memory is not available in the required size or there is a mismatch between the user resource and cuDNN internal resources. A resource mismatch may occur, for example, when calling [`cudnnSetStream`]. There could be a mismatch between the user provided CUDA stream and the internal CUDA events instantiated in the cuDNN handle when [`cudnnCreate`] was invoked.
    ///
    /// This error status may not be correctable when it is related to texture dimensions, shared memory size, or zero-copy memory availability. If `CUDNN_STATUS_MAPPING_ERROR` is returned by [`cudnnSetStream`], then it is typically correctable, however, it means that the cuDNN handle was created on one GPU and the user stream passed to this function is associated with another GPU.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: The current hyper-parameters are invalid.
    /// - `CUDNN_STATUS_RUNTIME_PREREQUISITE_MISSING`: The prerequisite runtime library could not be found.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The code was built and linked successfully.
    pub fn cudnnBuildRNNDynamic(
        handle: cudnnHandle_t,
        rnnDesc: cudnnRNNDescriptor_t,
        miniBatch: ::core::ffi::c_int,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function computes the work and reserve space buffer sizes based on the RNN network geometry stored in `rnnDesc`, designated usage (inference or training) defined by the `fMode` argument, and the current RNN data dimensions (`maxSeqLength`, `batchSize`) retrieved from `xDesc`. When RNN data dimensions change, the [`cudnnGetRNNTempSpaceSizes`] must be called again because RNN temporary buffer sizes are not monotonic.
    ///
    /// The user can assign `NULL` to `workSpaceSize` or `reserveSpaceSize` pointers when the corresponding value is not needed.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. The current cuDNN context handle.
    /// - `rnnDesc`: Input. A previously initialized RNN descriptor.
    /// - `xDesc`: Input. A single RNN data descriptor that specifies current RNN data dimensions: `maxSeqLength` and `batchSize`.
    /// - `workSpaceSize`: Output. Minimum amount of GPU memory in bytes needed as a workspace buffer. The workspace buffer is not used to pass intermediate results between APIs but as a temporary read/write buffer.
    /// - `reserveSpaceSize`: Output. Minimum amount of GPU memory in bytes needed as the reserve-space buffer. The reserve space buffer is used to pass intermediate results from [`cudnnRNNForward`] to RNN `BackwardData` and `BackwardWeights` routines that compute first order derivatives with respect to RNN inputs or trainable weight and biases.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: An invalid input argument was detected.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: An incompatible or unsupported combination of input arguments was detected.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: RNN temporary buffer sizes were computed successfully.
    pub fn cudnnGetRNNTempSpaceSizes(
        handle: cudnnHandle_t,
        rnnDesc: cudnnRNNDescriptor_t,
        fwdMode: cudnnForwardMode_t,
        xDesc: cudnnRNNDataDescriptor_t,
        workSpaceSize: *mut size_t,
        reserveSpaceSize: *mut size_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function reports the required size of the weight space buffer in bytes. The weight space buffer holds all RNN weight matrices and bias vectors.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. The current cuDNN context handle.
    /// - `rnnDesc`: Input. A previously initialized RNN descriptor.
    /// - `weightSpaceSize`: Output. Minimum size in bytes of GPU memory needed for all RNN trainable parameters.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: An invalid input argument was encountered. For example, any input argument was `NULL`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_INITIALIZED`]: The cuDNN library was not initialized properly.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The query was successful.
    pub fn cudnnGetRNNWeightSpaceSize(
        handle: cudnnHandle_t,
        rnnDesc: cudnnRNNDescriptor_t,
        weightSpaceSize: *mut size_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function is used to obtain the start address and shape of every RNN weight matrix and bias vector in each pseudo-layer within the recurrent neural network model.
    ///
    /// For more information on modes and bias modes, refer to [`cudnnRNNMode_t`].
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN library descriptor.
    /// - `rnnDesc`: Input. A previously initialized RNN descriptor.
    /// - `pseudoLayer`: Input. The pseudo-layer to query. In unidirectional RNNs, a pseudo-layer is the same as a physical layer (`pseudoLayer=0` is the RNN input layer, `pseudoLayer=1` is the first hidden layer). In bidirectional RNNs, there are twice as many pseudo-layers in comparison to physical layers:
    ///
    /// * `pseudoLayer=0` refers to the forward direction sub-layer of the physical input layer
    /// * `pseudoLayer=1` refers to the backward direction sub-layer of the physical input layer
    /// * `pseudoLayer=2` is the forward direction sub-layer of the first hidden layer, and so on.
    /// - `weightSpaceSize`: Input. Address of the weight space buffer. Starting from cuDNN version 9.1, this parameter can be `NULL`. This allows you to retrieve weight/bias offsets instead of the actual pointers within the buffer. For best performance, the recommended alignment of the weight space buffer should be 256 B or the same as returned by `cudaMalloc()`.
    /// - `weightSpace`: Input. Pointer to the weight space buffer.
    /// - `linLayerID`: Input. Weight matrix or bias vector linear ID index.
    ///
    /// If `cellMode` in `rnnDesc` was set to [`cudnnRNNMode_t::CUDNN_RNN_RELU`] or [`cudnnRNNMode_t::CUDNN_RNN_TANH`]:
    ///
    /// * Value `0` references the weight matrix or bias vector used in conjunction with the input from the previous layer or input to the RNN model.
    /// * Value `1` references the weight matrix or bias vector used in conjunction with the hidden state from the previous time step or the initial hidden state.
    ///
    /// If `cellMode` in `rnnDesc` was set to [`cudnnRNNMode_t::CUDNN_LSTM`]:
    ///
    /// * Values `0`, `1`, `2`, and `3` reference weight matrices or bias vectors used in conjunction with the input from the previous layer or input to the RNN model.
    /// * Values `4`, `5`, `6`, and `7` reference weight matrices or bias vectors used in conjunction with the hidden state from the previous time step or the initial hidden state.
    /// * Value 8 corresponds to the projection matrix, if enabled (there is no bias in this operation).
    ///
    /// Values and their LSTM gates:
    ///
    /// * `linLayerID 0` and `4` correspond to the input gate.
    /// * `linLayerID 1` and `5` correspond to the forget gate.
    /// * `linLayerID 2` and `6` correspond to the new cell state calculations with hyperbolic tangent.
    /// * `linLayerID 3` and `7` correspond to the output gate.
    ///
    /// If `cellMode` in `rnnDesc` was set to [`cudnnRNNMode_t::CUDNN_GRU`]:
    ///
    /// * `Values 0`, `1`, and `2` reference weight matrices or bias vectors used in conjunction with the input from the previous layer or input to the RNN model.
    /// * `Values 3`, `4`, and `5` reference weight matrices or bias vectors used in conjunction with the hidden state from the previous time step or the initial hidden state.
    ///
    /// Values and their GRU gates:
    ///
    /// * `linLayerID 0` and `3` correspond to the reset gate.
    /// * `linLayerID 1` and `4` reference to the update gate.
    /// * `linLayerID 2` and `5` correspond to the new hidden state calculations with hyperbolic tangent.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: An invalid input argument was encountered. For example, the value of `pseudoLayer` is out of range or `linLayerID` is negative or larger than `8`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_INVALID_VALUE`]: Some weight/bias elements are outside the weight space buffer boundaries.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_INITIALIZED`]: The cuDNN library was not initialized properly.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The query was completed successfully.
    pub fn cudnnGetRNNWeightParams(
        handle: cudnnHandle_t,
        rnnDesc: cudnnRNNDescriptor_t,
        pseudoLayer: i32,
        weightSpaceSize: size_t,
        weightSpace: *const ::core::ffi::c_void,
        linLayerID: i32,
        mDesc: cudnnTensorDescriptor_t,
        mAddr: *mut *mut ::core::ffi::c_void,
        bDesc: cudnnTensorDescriptor_t,
        bAddr: *mut *mut ::core::ffi::c_void,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function creates a RNN data descriptor object by allocating the memory needed to hold its opaque structure.
    ///
    /// # Return value
    ///
    /// - `CUDNN_STATUS_ALLOC_FAILED`: The resources could not be allocated.
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: The `RNNDataDesc` argument is `NULL`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The RNN data descriptor object was created successfully.
    pub fn cudnnCreateRNNDataDescriptor(
        rnnDataDesc: *mut cudnnRNNDataDescriptor_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function destroys a previously created RNN data descriptor object. Invoking [`cudnnDestroyRNNDataDescriptor`] with the `NULL` argument is a no operation (NOP).
    ///
    /// The [`cudnnDestroyRNNDataDescriptor`] function is not able to detect if the `RNNDataDesc` argument holds a valid address. Undefined behavior will occur in cases of passing an invalid pointer, not returned by the [`cudnnCreateRNNDataDescriptor`] function, or in the double deletion scenario of a valid address.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The RNN data descriptor object was destroyed successfully.
    pub fn cudnnDestroyRNNDataDescriptor(
        rnnDataDesc: cudnnRNNDataDescriptor_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function initializes a previously created RNN data descriptor object. This data structure is intended to support the unpacked (padded) layout for input and output of extended RNN inference and training functions. A packed (unpadded) layout is also supported for backward compatibility.
    ///
    /// # Parameters
    ///
    /// - `dataType`: Input. The datatype of the RNN data tensor. For more information, refer to [`cudnnDataType_t`].
    /// - `layout`: Input. The memory layout of the RNN data tensor.
    /// - `maxSeqLength`: Input. The maximum sequence length within this RNN data tensor. In the unpacked (padded) layout, this should include the padding vectors in each sequence. In the packed (unpadded) layout, this should be equal to the greatest element in `seqLengthArray`.
    /// - `batchSize`: Input. The number of sequences within the mini-batch.
    /// - `vectorSize`: Input. The vector length (embedding size) of the input or output tensor at each time-step.
    /// - `seqLengthArray`: Input. An integer array with `batchSize` number of elements. Describes the length (number of time-steps) of each sequence. Each element in `seqLengthArray` must be greater than or equal to 0 but less than or equal to `maxSeqLength`. In the packed layout, the elements should be sorted in descending order, similar to the layout required by the non-extended RNN compute functions.
    /// - `paddingFill`: Input. A user-defined symbol for filling the padding position in RNN output. This is only effective when the descriptor is describing the RNN output, and the unpacked layout is specified. The symbol should be in the host memory, and is interpreted as the same data type as that of the RNN data tensor. If a `NULL` pointer is passed in, then the padding position in the output will be undefined.
    ///
    /// # Return value
    ///
    /// - `CUDNN_STATUS_ALLOC_FAILED`: The allocation of internal array storage has failed.
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: Any one of these have occurred:
    ///
    /// * `RNNDataDesc` is `NULL`.
    /// * Any one of `maxSeqLength`, `batchSize`, or `vectorSize` is less than or equal to zero.
    /// * An element of `seqLengthArray` is less than zero or greater than `maxSeqLength`.
    /// * Layout is not one of [`cudnnRNNDataLayout_t::CUDNN_RNN_DATA_LAYOUT_SEQ_MAJOR_UNPACKED`], [`cudnnRNNDataLayout_t::CUDNN_RNN_DATA_LAYOUT_SEQ_MAJOR_PACKED`], or [`cudnnRNNDataLayout_t::CUDNN_RNN_DATA_LAYOUT_BATCH_MAJOR_UNPACKED`].
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: Any one of these have occurred:
    ///
    /// * `dataType` is not one of [`cudnnDataType_t::CUDNN_DATA_HALF`], [`cudnnDataType_t::CUDNN_DATA_FLOAT`], [`cudnnDataType_t::CUDNN_DATA_DOUBLE`].
    /// * `maxSeqLength` is larger than 65535 (0xffff).
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The object was set successfully.
    pub fn cudnnSetRNNDataDescriptor(
        rnnDataDesc: cudnnRNNDataDescriptor_t,
        dataType: cudnnDataType_t,
        layout: cudnnRNNDataLayout_t,
        maxSeqLength: ::core::ffi::c_int,
        batchSize: ::core::ffi::c_int,
        vectorSize: ::core::ffi::c_int,
        seqLengthArray: *const ::core::ffi::c_int,
        paddingFill: *mut ::core::ffi::c_void,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function retrieves a previously created RNN data descriptor object.
    ///
    /// # Parameters
    ///
    /// - `dataType`: Output. Pointer to the host memory location to store the datatype of the RNN data tensor.
    /// - `layout`: Output. Pointer to the host memory location to store the memory layout of the RNN data tensor.
    /// - `maxSeqLength`: Output. The maximum sequence length within this RNN data tensor, including the padding vectors.
    /// - `batchSize`: Output. The number of sequences within the mini-batch.
    /// - `vectorSize`: Output. The vector length (meaning, embedding size) of the input or output tensor at each time-step.
    /// - `arrayLengthRequested`: Input. The number of elements that the user requested for `seqLengthArray`.
    /// - `seqLengthArray`: Output. Pointer to the host memory location to store the integer array describing the length (meaning, number of timesteps) of each sequence. This is allowed to be a `NULL` pointer if `arrayLengthRequested` is `0`.
    /// - `paddingFill`: Output. Pointer to the host memory location to store the user defined symbol. The symbol should be interpreted as the same data type as the RNN data tensor.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: Any one of these have occurred:
    ///
    /// * Any of `RNNDataDesc`, `dataType`, `layout`, `maxSeqLength`, `batchSize`, `vectorSize`, or `paddingFill` is `NULL`.
    /// * `seqLengthArray` is `NULL` while `arrayLengthRequested` is greater than zero.
    /// * `arrayLengthRequested` is less than zero.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The parameters are fetched successfully.
    pub fn cudnnGetRNNDataDescriptor(
        rnnDataDesc: cudnnRNNDataDescriptor_t,
        dataType: *mut cudnnDataType_t,
        layout: *mut cudnnRNNDataLayout_t,
        maxSeqLength: *mut ::core::ffi::c_int,
        batchSize: *mut ::core::ffi::c_int,
        vectorSize: *mut ::core::ffi::c_int,
        arrayLengthRequested: ::core::ffi::c_int,
        seqLengthArray: *mut ::core::ffi::c_int,
        paddingFill: *mut ::core::ffi::c_void,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This routine computes the forward response of the recurrent neural network described by `rnnDesc` with inputs in `x`, `hx`, `cx`, and weights/biases in the `weightSpace` buffer. RNN outputs are written to `y`, `hy`, and `cy` buffers. Locations of `x`, `y`, `hx`, `cx`, `hy`, and `cy` signals in the multi-layer RNN model are shown in the following figure. Note that internal RNN signals between time-steps and between layers are not exposed to the user.
    ///
    /// The next figure depicts data flow when the RNN model is bidirectional. In this mode, each RNN physical layer consists of two consecutive pseudo-layers, each with its own weights, biases, the initial hidden state `hx`, and for LSTM, also the initial cell state `cx`. Even pseudo-layers 0, 2, 4 process input vectors from left to right or in the forward (`F`) direction. Odd pseudo-layers 1, 3, 5 process input vectors from right to left or in the reverse (`R`) direction. Two successive pseudo-layers operate on the same input vectors, just in a different order. Pseudo-layers 0 and 1 access the original sequences stored in the x buffer. Outputs of `F` and `R` cells are concatenated so vectors fed to the next two pseudo-layers have lengths of 2x `hiddenSize` or 2x `projSize`. Input GEMMs in subsequent pseudo-layers adjust vector lengths to 1x `hiddenSize`.
    ///
    /// When the `fwdMode` parameter is set to [`cudnnForwardMode_t::CUDNN_FWD_MODE_TRAINING`], the [`cudnnRNNForward`] function stores intermediate data required to compute first order derivatives in the reserve space buffer. Work and reserve space buffer sizes should be computed by the [`cudnnGetRNNTempSpaceSizes`] function with the same `fwdMode` setting as used in the [`cudnnRNNForward`] call.
    ///
    /// The same layout type must be specified in `xDesc` and `yDesc` descriptors. The same sequence lengths must be configured in `xDesc`, `yDesc` and in the device array `devSeqLengths`. Starting in cuDNN 8.9.1, the `devSeqLengths` parameter is no longer required and can be set to `NULL`. The variable sequence length array is transferred automatically to GPU memory by the [`cudnnRNNForward`] function.
    ///
    /// The [`cudnnRNNForward`] function does not verify that sequence lengths stored in `devSeqLengths` in GPU memory are the same as in `xDesc` and `yDesc` descriptors in CPU memory. Sequence length arrays from `xDesc` and `yDesc` descriptors are checked for consistency, however.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. The current cuDNN context handle.
    /// - `rnnDesc`: Input. A previously initialized RNN descriptor.
    /// - `fwdMode`: Input. Specifies inference or training mode ([`cudnnForwardMode_t::CUDNN_FWD_MODE_INFERENCE`] and [`cudnnForwardMode_t::CUDNN_FWD_MODE_TRAINING`]). In the training mode, additional data is stored in the reserve space buffer. This information is used in the backward pass to compute derivatives.
    /// - `devSeqLengths`: Input. A copy of `seqLengthArray` from `xDesc` or `yDesc` RNN data descriptor. The `devSeqLengths` array must be stored in GPU memory as it is accessed asynchronously by GPU kernels, possibly after the [`cudnnRNNForward`] function exists. In cuDNN 8.9.1 and later versions, `devSeqLengths` should be `NULL`.
    /// - `xDesc`: Input. A previously initialized descriptor corresponding to the RNN model primary input. The `dataType`, `layout`, `maxSeqLength`, `batchSize`, and `seqLengthArray` must match that of `yDesc`. The parameter `vectorSize` must match the `inputSize` argument passed to the [`cudnnSetRNNDescriptor_v8`] function.
    /// - `x`: Input. Data pointer to the GPU memory associated with the RNN data descriptor `xDesc`. The vectors are expected to be arranged in memory according to the layout specified by `xDesc`. The elements in the tensor (including padding vectors) must be densely packed.
    /// - `yDesc`: Input. A previously initialized RNN data descriptor. The `dataType`, `layout`, `maxSeqLength`, `batchSize`, and `seqLengthArray` must match that of `xDesc`. The parameter `vectorSize` depends on whether LSTM projection is enabled and whether the network is bidirectional. Specifically:
    ///
    /// * For unidirectional models, the parameter `vectorSize` must match the `hiddenSize` argument passed to [`cudnnSetRNNDescriptor_v8`]. If the LSTM projection is enabled, the `vectorSize` must be the same as the `projSize` argument passed to [`cudnnSetRNNDescriptor_v8`].
    /// * For bidirectional models, if the RNN `cellMode` is [`cudnnRNNMode_t::CUDNN_LSTM`] and the projection feature is enabled, the parameter `vectorSize` must be 2x the `projSize` argument passed to [`cudnnSetRNNDescriptor_v8`]. Otherwise, it should be 2x the `hiddenSize` value.
    /// - `y`: Output. Data pointer to the GPU memory associated with the RNN data descriptor `yDesc`. The vectors are expected to be laid out in memory according to the layout specified by `yDesc`. The elements in the tensor (including elements in the padding vector) must be densely packed, and no strides are supported.
    /// - `hDesc`: Input. A tensor descriptor specifying layouts of the initial or final hidden state buffers (hx, hy). Hidden state data must be fully packed. The first dimension of the tensor depends on the `dirMode` argument passed to the [`cudnnSetRNNDescriptor_v8`] function.
    ///
    /// * If `dirMode` is [`cudnnDirectionMode_t::CUDNN_UNIDIRECTIONAL`], then the first dimension should match the `numLayers` argument passed to [`cudnnSetRNNDescriptor_v8`].
    /// * If `dirMode` is [`cudnnDirectionMode_t::CUDNN_BIDIRECTIONAL`], then the first dimension should be double the `numLayers` argument passed to [`cudnnSetRNNDescriptor_v8`].
    ///
    /// The second dimension must match the `batchSize` parameter described in `xDesc`. The third dimension depends on whether RNN mode is [`cudnnRNNMode_t::CUDNN_LSTM`] and whether the LSTM projection is enabled. Specifically:
    ///
    /// * If RNN mode is [`cudnnRNNMode_t::CUDNN_LSTM`] and LSTM projection is enabled, the third dimension must match the `projSize` argument.
    /// * Otherwise, the third dimension must match the `hiddenSize` argument passed to the [`cudnnSetRNNDescriptor_v8`] call used to initialize `rnnDesc`.
    /// - `hx`: Input. Pointer to the GPU buffer with the RNN initial hidden state. Data dimensions are described by the `hDesc` tensor descriptor. If a `NULL` pointer is passed, the initial hidden state of the network will be initialized to zero.
    /// - `hy`: Output. Pointer to the GPU buffer where the final RNN hidden state should be stored. Data dimensions are described by the `hDesc` tensor descriptor. If a `NULL` pointer is passed, the final hidden state of the network will not be saved.
    /// - `cDesc`: Input. For LSTM networks only. This argument should be `NULL` for `RELU`, `TANH`, or `GRU` cell types. `cDesc` is a tensor descriptor specifying layouts of the initial or final cell state buffers (`cx, cy`) used byLSTM networks. Cell state data must be fully packed. The first dimension of the tensor depends on the `dirMode` argument passed to the [`cudnnSetRNNDescriptor_v8`] call.
    ///
    /// * If `dirMode` is [`cudnnDirectionMode_t::CUDNN_UNIDIRECTIONAL`] the first dimension should match the `numLayers` argument passed to [`cudnnSetRNNDescriptor_v8`].
    /// * If `dirMode` is [`cudnnDirectionMode_t::CUDNN_BIDIRECTIONAL`] the first dimension should match double the `numLayers` argument passed to [`cudnnSetRNNDescriptor_v8`].
    ///
    /// The second tensor dimension must match the `batchSize` parameter in `xDesc`. The third dimension must match the `hiddenSize` argument passed to the [`cudnnSetRNNDescriptor_v8`] call.
    /// - `cx`: Input. For LSTM networks only. Pointer to the GPU buffer with the initial LSTM state data. Data dimensions are described by the `cDesc` tensor descriptor. If a `NULL` pointer is passed, the initial cell state of the network will be initialized to zero.
    /// - `cy`: Output. For LSTM networks only. Pointer to the GPU buffer where final LSTM state data should be stored. Data dimensions are described by the `cDesc` tensor descriptor. If a `NULL` pointer is passed, the final LSTM cell state will not be saved.
    /// - `weightSpaceSize`: Input. Specifies the size in bytes of the provided weight space buffer.
    /// - `weightSpace`: Input. Address of the weight space buffer in GPU memory.
    /// - `workSpaceSize`: Input. Specifies the size in bytes of the provided workspace buffer.
    /// - `workSpace`: Input/output. Address of the workspace buffer in GPU memory to store temporary data.
    /// - `reserveSpaceSize`: Input. Specifies the size in bytes of the reserve-space buffer.
    /// - `reserveSpace`: Input/output. Address of the reserve-space buffer in GPU memory.
    ///
    /// # Return value
    ///
    /// - `CUDNN_STATUS_ALLOC_FAILED`: The function was unable to allocate CPU memory.
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: An invalid or incompatible input argument was encountered. Some examples include:
    ///
    /// * some input descriptors are `NULL`
    /// * at least one of the settings in `rnnDesc`, `xDesc`, `yDesc`, `hDesc`, or `cDesc` descriptors is invalid
    /// * `weightSpaceSize`, `workSpaceSize`, or `reserveSpaceSize` is too small.
    /// - [`cudnnStatus_t::CUDNN_STATUS_EXECUTION_FAILED`]: The process of launching a GPU kernel returned an error, or an earlier kernel did not complete successfully.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: At least one of the following conditions are met:
    ///
    /// * variable sequence length input is passed while [`cudnnRNNAlgo_t::CUDNN_RNN_ALGO_PERSIST_STATIC`], [`cudnnRNNAlgo_t::CUDNN_RNN_ALGO_PERSIST_DYNAMIC`], or [`cudnnRNNAlgo_t::CUDNN_RNN_ALGO_PERSIST_STATIC_SMALL_H`] is specified
    /// * [`cudnnRNNAlgo_t::CUDNN_RNN_ALGO_PERSIST_STATIC`] or [`cudnnRNNAlgo_t::CUDNN_RNN_ALGO_PERSIST_DYNAMIC`] is requested on pre-Pascal devices
    /// * the ‘double’ floating point type is used for input/output and the [`cudnnRNNAlgo_t::CUDNN_RNN_ALGO_PERSIST_STATIC`] algo.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: No errors were detected while processing API input arguments and launching GPU kernels.
    pub fn cudnnRNNForward(
        handle: cudnnHandle_t,
        rnnDesc: cudnnRNNDescriptor_t,
        fwdMode: cudnnForwardMode_t,
        devSeqLengths: *const i32,
        xDesc: cudnnRNNDataDescriptor_t,
        x: *const ::core::ffi::c_void,
        yDesc: cudnnRNNDataDescriptor_t,
        y: *mut ::core::ffi::c_void,
        hDesc: cudnnTensorDescriptor_t,
        hx: *const ::core::ffi::c_void,
        hy: *mut ::core::ffi::c_void,
        cDesc: cudnnTensorDescriptor_t,
        cx: *const ::core::ffi::c_void,
        cy: *mut ::core::ffi::c_void,
        weightSpaceSize: size_t,
        weightSpace: *const ::core::ffi::c_void,
        workSpaceSize: size_t,
        workSpace: *mut ::core::ffi::c_void,
        reserveSpaceSize: size_t,
        reserveSpace: *mut ::core::ffi::c_void,
    ) -> cudnnStatus_t;
}
/// [`cudnnSeqDataAxis_t`] is an enumerated type that indexes active dimensions in the `dimA[]` argument that is passed to the [`cudnnSetSeqDataDescriptor`] function to configure the sequence data descriptor of type [`cudnnSeqDataDescriptor_t`].
///
/// [`cudnnSeqDataAxis_t`] constants are also used in the `axis[]` argument of the [`cudnnSetSeqDataDescriptor`] call to define the layout of the sequence data buffer in memory. Refer to [`cudnnSetSeqDataDescriptor`] for a detailed description on how to use the [`cudnnSeqDataAxis_t`] enumerated type.
///
/// The [`CUDNN_SEQDATA_DIM_COUNT`] macro defines the number of constants in the [`cudnnSeqDataAxis_t`] enumerated type. This value is currently set to `4`.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnSeqDataAxis_t {
    /// Identifies the `TIME` (sequence length) dimension or specifies the `TIME` in the data layout.
    CUDNN_SEQDATA_TIME_DIM = 0,
    /// Identifies the `BATCH` dimension or specifies the `BATCH` in the data layout.
    CUDNN_SEQDATA_BATCH_DIM = 1,
    /// Identifies the `BEAM` dimension or specifies the `BEAM` in the data layout.
    CUDNN_SEQDATA_BEAM_DIM = 2,
    /// Identifies the `VECT` (vector) dimension or specifies the `VECT` in the data layout.
    CUDNN_SEQDATA_VECT_DIM = 3,
}
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct cudnnSeqDataStruct {
    _unused: [u8; 0],
}
/// [`cudnnSeqDataDescriptor_t`] is a pointer to an opaque structure holding parameters of the sequence data container or buffer. The sequence data container is used to store fixed size vectors defined by the `VECT` dimension. Vectors are arranged in additional three dimensions: `TIME`, `BATCH`, and `BEAM`.
///
/// The `TIME` dimension is used to bundle vectors into sequences of vectors. The actual sequences can be shorter than the `TIME` dimension, therefore, additional information is needed about each sequence length and how unused (padding) vectors should be saved.
///
/// It is assumed that the sequence data container is fully packed. The `TIME`, `BATCH`, and `BEAM` dimensions can be in any order when vectors are traversed in the ascending order of addresses. Six data layouts (permutation of `TIME`, `BATCH`, and `BEAM`) are possible.
///
/// The [`cudnnSeqDataDescriptor_t`] object holds the following parameters:
///
/// Use the [`cudnnCreateSeqDataDescriptor`] function to create one instance of the sequence data descriptor object and [`cudnnDestroySeqDataDescriptor`] to delete a previously created descriptor. Use the [`cudnnSetSeqDataDescriptor`] function to configure the descriptor.
///
/// This descriptor is used by multihead attention API functions.
#[deprecated]
pub type cudnnSeqDataDescriptor_t = *mut cudnnSeqDataStruct;
unsafe extern "C" {
    /// This function creates one instance of an opaque sequence data descriptor object by allocating the host memory for it and initializing all descriptor fields. The function writes `NULL` to `seqDataDesc` when the sequence data descriptor object cannot be allocated.
    ///
    /// Use the [`cudnnSetSeqDataDescriptor`] function to configure the sequence data descriptor and [`cudnnDestroySeqDataDescriptor`] to destroy it and release the allocated memory.
    ///
    /// # Parameters
    ///
    /// - `seqDataDesc`: Output. Pointer where the address to the newly created sequence data descriptor should be written.
    ///
    /// # Return value
    ///
    /// - `CUDNN_STATUS_ALLOC_FAILED`: The memory allocation failed.
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: An invalid input argument was encountered (`seqDataDesc=NULL`).
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The descriptor object was created successfully.
    #[deprecated]
    pub fn cudnnCreateSeqDataDescriptor(
        seqDataDesc: *mut cudnnSeqDataDescriptor_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function destroys the sequence data descriptor object and releases its memory. The `seqDataDesc` argument can be `NULL`. Invoking [`cudnnDestroySeqDataDescriptor`] with a `NULL` argument is a no operation (NOP).
    ///
    /// The [`cudnnDestroySeqDataDescriptor`] function is not able to detect if the `seqDataDesc` argument holds a valid address. Undefined behavior will occur in case of passing an invalid pointer, not returned by the [`cudnnCreateSeqDataDescriptor`] function, or in the double deletion scenario of a valid address.
    ///
    /// # Parameters
    ///
    /// - `seqDataDesc`: Input. Pointer to the sequence data descriptor object to be destroyed.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The descriptor was destroyed successfully.
    #[deprecated]
    pub fn cudnnDestroySeqDataDescriptor(
        seqDataDesc: cudnnSeqDataDescriptor_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function initializes a previously created sequence data descriptor object. In the most simplified view, this descriptor defines dimensions (`dimA`) and the data layout (`axes`) of a four-dimensional tensor.
    ///
    /// All four dimensions of the sequence data descriptor have unique identifiers that can be used to index the `dimA[]` array:
    ///
    /// For example, to express information that vectors in our sequence data buffer are five elements long, we need to assign `dimA\[CUDNN_SEQDATA_VECT_DIM\]=5` in the `dimA[]` array.
    ///
    /// The number of active dimensions in the `dimA[]` and `axes[]` arrays is defined by the `nbDims` argument. Currently, the value of this argument should be four. The actual size of the `dimA[]` and `axes[]` arrays should be declared using the [`CUDNN_SEQDATA_DIM_COUNT`] macro.
    ///
    /// The [`cudnnSeqDataDescriptor_t`] container is treated as a collection of fixed length vectors that form sequences, similarly to words (vectors of characters) constructing sentences. The `TIME` dimension spans the sequence length. Different sequences are bundled together in a batch. A `BATCH` may be a group of individual sequences or beams. A `BEAM` is a cluster of alternative sequences or candidates. When thinking about the beam, consider a translation task from one language to another. You may want to keep around and experiment with several translated versions of the original sentence before selecting the best one. The number of candidates kept around is the `BEAM` size.
    ///
    /// Every sequence can have a different length, even within the same beam, so vectors toward the end of the sequence can be just padding. The `paddingFill` argument specifies how the padding vectors should be written in output sequence data buffers. The `paddingFill` argument points to one value of type `dataType` that should be copied to all elements in padding vectors. Currently, the only supported value for `paddingFill` is `NULL` which means this option should be ignored. In this case, elements of the padding vectors in output buffers will have undefined values.
    ///
    /// It is assumed that a non-empty sequence always starts from the time index zero. The `seqLengthArray[]` must specify all sequence lengths in the container so the total size of this array should be `dimA\[CUDNN_SEQDATA_BATCH_DIM\] * dimA\[CUDNN_SEQDATA_BEAM_DIM\]`. Each element of the `seqLengthArray[]` array should have a non-negative value, less than or equal to `dimA\[CUDNN_SEQDATA_TIME_DIM\]`; the maximum sequence length. Elements in `seqLengthArray[]` are always arranged in the same batch-major order, meaning, when considering `BEAM` and `BATCH` dimensions, `BATCH` is the outer or the slower changing index when we traverse the array in ascending order of the addresses. Using a simple example, the `seqLengthArray[]` array should hold sequence lengths in the following order:
    ///
    /// When `dimA\[CUDNN_SEQDATA_BATCH_DIM\]=3` and `dimA\[CUDNN_SEQDATA_BEAM_DIM\]=2`.
    ///
    /// Data stored in the [`cudnnSeqDataDescriptor_t`] container must comply with the following constraints:
    ///
    /// The axes argument in the [`cudnnSeqDataDescriptor_t`] function is a bit more complicated. This array should have the same capacity as `dimA[]`. The `axes[]` array specifies the actual data layout in the GPU memory. In this function, the layout is described in the following way: as we move from one element of a vector to another in memory by incrementing the element pointer, what is the order of `VECT`, `TIME`, `BATCH`, and `BEAM` dimensions that we encounter. Let us assume that we want to define the following data layout:
    ///
    /// That corresponds to tensor dimensions:
    ///
    /// Now, let’s initialize the `axes[]` array. Note that the most inner dimension is described by the last active element of `axes[]`. There is only one valid configuration here as we always traverse a full vector first. Thus, we need to write [`cudnnSeqDataAxis_t::CUDNN_SEQDATA_VECT_DIM`] in the last active element of `axes[]`.
    ///
    /// Now, let’s work on the remaining three elements of `axes[]`. When we reach the end of the first vector, we jump to the next `BEAM`, therefore:
    ///
    /// When we approach the end of the second vector, we move to the next batch, therefore:
    ///
    /// The last (outermost) dimension is `TIME`:
    ///
    /// The four values of the `axes[]` array fully describe the data layout depicted in the figure.
    ///
    /// The sequence data descriptor allows the user to select `3! = 6` different data layouts or permutations of `BEAM`, `BATCH`, and `TIME` dimensions. The multihead attention API supports all six layouts.
    ///
    /// # Parameters
    ///
    /// - `seqDataDesc`: Output. Pointer to a previously created sequence data descriptor.
    /// - `dataType`: Input. Data type of the sequence data buffer ([`cudnnDataType_t::CUDNN_DATA_HALF`], [`cudnnDataType_t::CUDNN_DATA_FLOAT`], or [`cudnnDataType_t::CUDNN_DATA_DOUBLE`]).
    /// - `nbDims`: Input. Must be 4. The number of active dimensions in `dimA[]` and `axes[]` arrays. Both arrays should be declared to contain at least [`CUDNN_SEQDATA_DIM_COUNT`] elements.
    /// - `seqLengthArraySize`: Input. Number of elements in the sequence length array, `seqLengthArray[]`.
    /// - `paddingFill`: Input. Must be `NULL`. Pointer to a value of `dataType` that is used to fill up output vectors beyond the valid length of each sequence or `NULL` to ignore this setting.
    ///
    /// # Return value
    ///
    /// - `CUDNN_STATUS_ALLOC_FAILED`: Failed to allocate storage for the sequence data descriptor object.
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: An invalid input argument was found. Some examples include:
    ///
    /// * `seqDataDesc=NULL`
    /// * `dateType` was not a valid type of [`cudnnDataType_t`]
    /// * `nbDims` was negative or zero
    /// * `seqLengthArraySize` did not match the expected length
    /// * some elements of `seqLengthArray[]` were invalid.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: An unsupported input argument was encountered. Some examples include:
    ///
    /// * `nbDims` is not equal to 4
    /// * `paddingFill` is not `NULL`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: All input arguments were validated and the sequence data descriptor was successfully updated.
    #[deprecated]
    pub fn cudnnSetSeqDataDescriptor(
        seqDataDesc: cudnnSeqDataDescriptor_t,
        dataType: cudnnDataType_t,
        nbDims: ::core::ffi::c_int,
        dimA: *const ::core::ffi::c_int,
        axes: *const cudnnSeqDataAxis_t,
        seqLengthArraySize: size_t,
        seqLengthArray: *const ::core::ffi::c_int,
        paddingFill: *mut ::core::ffi::c_void,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function retrieves settings from a previously created sequence data descriptor. The user can assign `NULL` to any pointer except `seqDataDesc` when the retrieved value is not needed. The `nbDimsRequested` argument applies to both `dimA[]` and `axes[]` arrays. A positive value of `nbDimsRequested` or `seqLengthSizeRequested` is ignored when the corresponding array, `dimA[]`, `axes[]`, or `seqLengthArray[]` is `NULL`.
    ///
    /// The [`cudnnGetSeqDataDescriptor`] function does not report the actual strides in the sequence data buffer. Those strides can be handy in computing the offset to any sequence data element. The user must precompute strides based on the `axes[]` and `dimA[]` arrays reported by the [`cudnnGetSeqDataDescriptor`] function. Below is sample code that performs this task:
    ///
    /// Now, the `strA[]` array can be used to compute the index to any sequence data element, for example:
    ///
    /// The above code assumes that all four indices (`batch`, `beam`, `time`, `vect`) are less than the corresponding value in the `dimA[]` array. The sample code also omits the `strA\[CUDNN_SEQDATA_VECT_DIM\]` stride because its value is always `1`, meaning, elements of one vector occupy a contiguous block of memory.
    ///
    /// # Parameters
    ///
    /// - `seqDataDesc`: Input. Sequence data descriptor.
    /// - `dataType`: Output. Data type used in the sequence data buffer.
    /// - `nbDims`: Output. The number of active dimensions in the `dimA[]` and `axes[]` arrays.
    /// - `nbDimsRequested`: Input. The maximum number of consecutive elements that can be written to `dimA[]` and `axes[]` arrays starting from index zero. The recommended value for this argument is [`CUDNN_SEQDATA_DIM_COUNT`].
    /// - `seqLengthArraySize`: Output. The number of required elements in `seqLengthArray[]` to save all sequence lengths.
    /// - `seqLengthSizeRequested`: Input. The maximum number of consecutive elements that can be written to the `seqLengthArray[]` array starting from index zero.
    /// - `paddingFill`: Output. Pointer to a storage location of `dataType` with the fill value that should be written to all padding vectors. Use `NULL` when an explicit initialization of output padding vectors was not requested.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: An invalid input argument was found.
    /// - [`cudnnStatus_t::CUDNN_STATUS_INTERNAL_ERROR`]: An inconsistent internal state was encountered.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: Requested sequence data descriptor fields were retrieved successfully.
    #[deprecated]
    pub fn cudnnGetSeqDataDescriptor(
        seqDataDesc: cudnnSeqDataDescriptor_t,
        dataType: *mut cudnnDataType_t,
        nbDims: *mut ::core::ffi::c_int,
        nbDimsRequested: ::core::ffi::c_int,
        dimA: *mut ::core::ffi::c_int,
        axes: *mut cudnnSeqDataAxis_t,
        seqLengthArraySize: *mut size_t,
        seqLengthSizeRequested: size_t,
        seqLengthArray: *mut ::core::ffi::c_int,
        paddingFill: *mut ::core::ffi::c_void,
    ) -> cudnnStatus_t;
}
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct cudnnAttnStruct {
    _unused: [u8; 0],
}
/// [`cudnnAttnDescriptor_t`] is a pointer to an opaque structure holding parameters of the multihead attention layer, such as:
///
/// Use the [`cudnnCreateAttnDescriptor`] function to create an instance of the attention descriptor object and [`cudnnDestroyAttnDescriptor`] to delete the previously created descriptor. Use the [`cudnnSetAttnDescriptor`] function to configure the descriptor.
#[deprecated]
pub type cudnnAttnDescriptor_t = *mut cudnnAttnStruct;
unsafe extern "C" {
    /// This function creates one instance of an opaque attention descriptor object by allocating the host memory for it and initializing all descriptor fields. The function writes `NULL` to `attnDesc` when the attention descriptor object cannot be allocated.
    ///
    /// Use the [`cudnnSetAttnDescriptor`] function to configure the attention descriptor and [`cudnnDestroyAttnDescriptor`] to destroy it and release the allocated memory.
    ///
    /// # Parameters
    ///
    /// - `attnDesc`: Output. Pointer where the address to the newly created attention descriptor should be written.
    ///
    /// # Return value
    ///
    /// - `CUDNN_STATUS_ALLOC_FAILED`: The memory allocation failed.
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: An invalid input argument was encountered (`attnDesc=NULL`).
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The descriptor object was created successfully.
    #[deprecated]
    pub fn cudnnCreateAttnDescriptor(
        attnDesc: *mut cudnnAttnDescriptor_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function destroys the attention descriptor object and releases its memory. The `attnDesc` argument can be `NULL`. Invoking [`cudnnDestroyAttnDescriptor`] with a `NULL` argument is a no operation (NOP).
    ///
    /// The [`cudnnDestroyAttnDescriptor`] function is not able to detect if the `attnDesc` argument holds a valid address. Undefined behavior will occur in case of passing an invalid pointer, not returned by the [`cudnnCreateAttnDescriptor`] function, or in the double deletion scenario of a valid address.
    ///
    /// # Parameters
    ///
    /// - `attnDesc`: Input. Pointer to the attention descriptor object to be destroyed.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The descriptor was destroyed successfully.
    #[deprecated]
    pub fn cudnnDestroyAttnDescriptor(attnDesc: cudnnAttnDescriptor_t) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function configures a multihead attention descriptor that was previously created using the [`cudnnCreateAttnDescriptor`] function. The function sets attention parameters that are necessary to compute internal buffer sizes, dimensions of weight and bias tensors, or to select optimized code paths.
    ///
    /// Input sequence data descriptors in [`cudnnMultiHeadAttnForward`], [`cudnnMultiHeadAttnBackwardData`], and [`cudnnMultiHeadAttnBackwardWeights`] functions are checked against the configuration parameters stored in the attention descriptor. Some parameters must match exactly while `max` arguments such as `maxBatchSize` or `qoMaxSeqLength` establish upper limits for the corresponding dimensions.
    ///
    /// The multihead attention model can be described by the following equations:
    /// $$
    /// \textbf{h}_{i}=\left( \textbf{W}_{V,i} \textbf{V}\right)softmax\left( smScaler\left( \textbf{K}^{\textbf{T}}\mathrm{\textbf{W}}_{K,i}^{T} \right)\left( \textbf{W}_{Q,i} \textbf{q}\right) \right), for i=0 \cdots nHeads -1
    /// $$:
    ///
    /// $$
    /// MultiHeadAttn\left( \textbf{q},\textbf{K},\textbf{V},\textbf{W}_{Q},\textbf{W}_{K},\textbf{W}_{V},\textbf{W}_{O} \right)=\sum_{i=0}^{nHeads-1}\textbf{W}_{O,i}\textbf{h}_{i}
    /// $$
    ///
    /// Where:
    ///
    /// The lengths of `query`, `key`, and `value` vectors are defined by the `qSize`, `kSize`, and `vSize` arguments respectively.
    ///
    /// For simplicity, the above equations are presented using a single embedding vector **q** but the cuDNN API can handle multiple **q** candidates in the beam search scheme, process **q** vectors from multiple sequences bundled into a batch, or automatically iterate through all embedding vectors (time-steps) of a sequence. Thus, in general, **q**, **K**, **V** inputs are tensors with additional pieces of information such as the active length of each sequence or how unused padding vectors should be saved.
    ///
    /// In some publications, **W** O,i matrices are combined into one output projection matrix and **h** i vectors are merged explicitly into a single vector. This is an equivalent notation. In the cuDNN library, **W** O,i matrices are conceptually treated the same way as **W** Q,i, **W** K,i, or **W** V,i input projection weights. Refer to the description of the `cudnnGetMultiHeadAttnWeights` function for more details.
    ///
    /// Weight matrices **W** Q,i, **W** K,i, **W** V,i, and **W** O,i play similar roles, adjusting vector lengths in **q**, **K**, **V** inputs and in the multihead attention final output. The user can disable any or all projections by setting `qProjSize`, `kProjSize`, `vProjSize`, or `oProjSize` arguments to zero.
    ///
    /// Embedding vector sizes in **q**, **K**, **V** and the vector lengths after projections need to be selected in such a way that matrix multiplications described above are feasible. Otherwise, [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`] is returned by the `cudnnSetAttnDescriptor()` function. All four weight matrices are used when it is desirable to maintain rank deficiency $\textbf{W}_{KQ,i}=\mathrm{\textbf{W}}_{K,i}^{T}\textbf{W}_{Q,i}$ or $\textbf{W}_{OV,i}=\textbf{W}_{O,i}\textbf{W}_{V,i}$ of matrices to eliminate one or more dimensions during linear transformations in each head. This is a form of feature extraction. In such cases, the projected sizes are smaller than the original vector lengths.
    ///
    /// For each attention head, weight matrix sizes are defined as follows:
    ///
    /// When the output projection is disabled (`oProjSize=0`), the output vector length is `nHeads * (vProjSize > 0 ? vProjSize : vSize)`, meaning, the output is a concatenation of all **h** i vectors. In the alternative interpretation, a concatenated matrix **W** O = \[**W** O,0, **W** O,1, **W** O,2, …\] forms the identity matrix.
    ///
    /// Softmax is a normalized, exponential vector function that takes and outputs vectors of the same size. The multihead attention API utilizes softmax of the [`cudnnSoftmaxAlgorithm_t::CUDNN_SOFTMAX_ACCURATE`] type to reduce the likelihood of the floating-point overflow.
    ///
    /// The `smScaler` parameter is the softmax sharpening/smoothing coefficient. When `smScaler=1.0`, softmax uses the natural exponential function `exp(x)` or `2.7183*`. When `smScaler&lt;1.0`, for example `smScaler=0.2`, the function used by the softmax block will not grow as fast because exp(0.2\*x) ≈ 1.2214 x.
    ///
    /// The `smScaler` parameter can be adjusted to process larger ranges of values fed to softmax. When the range is too large (or `smScaler` is not sufficiently small for the given range), the output vector of the softmax block becomes categorical, meaning, one vector element is close to 1.0 and other outputs are zero or very close to zero. When this occurs, the Jacobian matrix of the softmax block is also close to zero so deltas are not back-propagated during training from output to input except through residual connections, if these connections are enabled. The user can set `smScaler` to any positive floating-point value or even zero. The `smScaler` parameter is not trainable.
    ///
    /// The `qoMaxSeqLength`, `kvMaxSeqLength`, `maxBatchSize`, and `maxBeamSize` arguments declare the maximum sequence lengths, maximum batch size, and maximum beam size respectively, in the `cudnnSeqDataDescriptor_t` containers. The actual dimensions supplied to forward and backward (gradient) API functions should not exceed the `max` limits. The `max` arguments should be set carefully because too large values will result in excessive memory usage due to oversized work and reserve space buffers.
    ///
    /// The `attnMode` argument is treated as a binary mask where various on/off options are set. These options can affect the internal buffer sizes, enforce certain argument checks, select optimized code execution paths, or enable attention variants that do not require additional numerical arguments. An example of such options is the inclusion of biases in input and output projections.
    ///
    /// The `attnDropoutDesc` and `postDropoutDesc` arguments are descriptors that define two dropout layers active in the training mode. The first dropout operation defined by `attnDropoutDesc`, is applied directly to the softmax output. The second dropout operation, specified by `postDropoutDesc`, alters the multihead attention output, just before the point where residual connections are added.
    ///
    /// **Supported`, `attnMode`` Flags**
    ///
    /// **Unsupported Features**.
    ///
    /// # Parameters
    ///
    /// - `attnDesc`: Output. Attention descriptor to be configured.
    /// - `attnMode`: Input. Enables various attention options that do not require additional numerical values. Refer to the table below for the list of supported flags. The user should assign a preferred set of bitwise `OR-ed` flags to this argument.
    /// - `nHeads`: Input. Number of attention heads.
    /// - `smScaler`: Input. Softmax smoothing (`1.0 >= smScaler >= 0.0`) or sharpening (`smScaler > 1.0`) coefficient. Negative values are not accepted.
    /// - `dataType`: Input. Data type used to represent attention inputs, attention weights and attention outputs.
    /// - `computePrec`: Input.Compute precision.
    /// - `mathType`: Input. NVIDIA Tensor Core settings.
    /// - `attnDropoutDesc`: Input. Descriptor of the dropout operation applied to the softmax output. Refer to the table below for a list of unsupported features.
    /// - `postDropoutDesc`: Input. Descriptor of the dropout operation applied to the multihead attention output, just before the point where residual connections are added. Refer to the table below for a list of unsupported features.
    /// - `oProjSize`: Input. The **h** i vector length after the output projection. Use zero to disable this projection.
    /// - `qoMaxSeqLength`: Input. Largest sequence length expected in sequence data descriptors related to **Q**, **O**, **dQ**, and **dO** inputs and outputs.
    /// - `kvMaxSeqLength`: Input. Largest sequence length expected in sequence data descriptors related to **K**, **V**, **dK**, and **dV** inputs and outputs.
    /// - `maxBatchSize`: Input. Largest batch size expected in any `cudnnSeqDataDescriptor_t` container.
    /// - `maxBeamSize`: Input. Largest beam size expected in any `cudnnSeqDataDescriptor_t` container.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: An invalid input argument was encountered. Some examples include:
    ///
    /// * post projection **Q** and **K** sizes were not equal
    /// * `dataType`, `computePrec`, or `mathType` were invalid
    /// * one or more of the following arguments were either negative or zero: `nHeads`, `qSize`, `kSize`, `vSize`, `qoMaxSeqLength`, `kvMaxSeqLength`, `maxBatchSize`, `maxBeamSize`
    /// * one or more of the following arguments were negative: `qProjSize`, `kProjSize`, `vProjSize`, `smScaler`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: A requested option or a combination of input arguments is not supported.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The attention descriptor was configured successfully.
    #[deprecated]
    pub fn cudnnSetAttnDescriptor(
        attnDesc: cudnnAttnDescriptor_t,
        attnMode: ::core::ffi::c_uint,
        nHeads: ::core::ffi::c_int,
        smScaler: f64,
        dataType: cudnnDataType_t,
        computePrec: cudnnDataType_t,
        mathType: cudnnMathType_t,
        attnDropoutDesc: cudnnDropoutDescriptor_t,
        postDropoutDesc: cudnnDropoutDescriptor_t,
        qSize: ::core::ffi::c_int,
        kSize: ::core::ffi::c_int,
        vSize: ::core::ffi::c_int,
        qProjSize: ::core::ffi::c_int,
        kProjSize: ::core::ffi::c_int,
        vProjSize: ::core::ffi::c_int,
        oProjSize: ::core::ffi::c_int,
        qoMaxSeqLength: ::core::ffi::c_int,
        kvMaxSeqLength: ::core::ffi::c_int,
        maxBatchSize: ::core::ffi::c_int,
        maxBeamSize: ::core::ffi::c_int,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function retrieves settings from the previously created attention descriptor. The user can assign `NULL` to any pointer except `attnDesc` when the retrieved value is not needed.
    ///
    /// # Parameters
    ///
    /// - `attnDesc`: Input. Attention descriptor.
    /// - `attnMode`: Output. Pointer to the storage for binary attention flags.
    /// - `nHeads`: Output. Pointer to the storage for the number of attention heads.
    /// - `smScaler`: Output. Pointer to the storage for the softmax smoothing/sharpening coefficient.
    /// - `dataType`: Output. Data type for attention weights, sequence data inputs, and outputs.
    /// - `computePrec`: Output. Pointer to the storage for the compute precision.
    /// - `mathType`: Output. NVIDIA Tensor Core settings.
    /// - `attnDropoutDesc`: Output. Descriptor of the dropout operation applied to the softmax output.
    /// - `postDropoutDesc`: Output. Descriptor of the dropout operation applied to the multihead attention output.
    /// - `oProjSize`: Output. Pointer to store the output vector length after projection.
    /// - `qoMaxSeqLength`: Output. Largest sequence length expected in sequence data descriptors related to **Q**, **O**, **dQ**, **dO** inputs and outputs.
    /// - `kvMaxSeqLength`: Output. Largest sequence length expected in sequence data descriptors related to **K**, **V**, **dK**, **dV** inputs and outputs.
    /// - `maxBatchSize`: Output. Largest batch size expected in the [`cudnnSeqDataDescriptor_t`] container.
    /// - `maxBeamSize`: Output. Largest beam size expected in the [`cudnnSeqDataDescriptor_t`] container.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: An invalid input argument was found.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: Requested attention descriptor fields were retrieved successfully.
    #[deprecated]
    pub fn cudnnGetAttnDescriptor(
        attnDesc: cudnnAttnDescriptor_t,
        attnMode: *mut ::core::ffi::c_uint,
        nHeads: *mut ::core::ffi::c_int,
        smScaler: *mut f64,
        dataType: *mut cudnnDataType_t,
        computePrec: *mut cudnnDataType_t,
        mathType: *mut cudnnMathType_t,
        attnDropoutDesc: *mut cudnnDropoutDescriptor_t,
        postDropoutDesc: *mut cudnnDropoutDescriptor_t,
        qSize: *mut ::core::ffi::c_int,
        kSize: *mut ::core::ffi::c_int,
        vSize: *mut ::core::ffi::c_int,
        qProjSize: *mut ::core::ffi::c_int,
        kProjSize: *mut ::core::ffi::c_int,
        vProjSize: *mut ::core::ffi::c_int,
        oProjSize: *mut ::core::ffi::c_int,
        qoMaxSeqLength: *mut ::core::ffi::c_int,
        kvMaxSeqLength: *mut ::core::ffi::c_int,
        maxBatchSize: *mut ::core::ffi::c_int,
        maxBeamSize: *mut ::core::ffi::c_int,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function computes weight, work, and reserve space buffer sizes used by the following functions:
    ///
    /// Assigning `NULL` to the `reserveSpaceSizeInBytes` argument indicates that the user does not plan to invoke multihead attention gradient functions: [`cudnnMultiHeadAttnBackwardData`] and [`cudnnMultiHeadAttnBackwardWeights`]. This situation occurs in the inference mode.
    ///
    /// The user must allocate weight, work, and reserve space buffer sizes in the GPU memory using `cudaMalloc()` with the reported buffer sizes. The buffers can be also carved out from a larger chunk of allocated memory but the buffer addresses must be at least 16B aligned.
    ///
    /// The workspace buffer is used for temporary storage. Its content can be discarded or modified after all GPU kernels launched by the corresponding API complete. The reserve-space buffer is used to transfer intermediate results from [`cudnnMultiHeadAttnForward`] to [`cudnnMultiHeadAttnBackwardData`], and from [`cudnnMultiHeadAttnBackwardData`] to [`cudnnMultiHeadAttnBackwardWeights`]. The content of the reserve-space buffer cannot be modified until all GPU kernels launched by the above three multihead attention API functions finish.
    ///
    /// All multihead attention weight and bias tensors are stored in a single weight buffer. For speed optimizations, the cuDNN API may change tensor layouts and their relative locations in the weight buffer based on the provided attention parameters. Use the [`cudnnGetMultiHeadAttnWeights`] function to obtain the start address and the shape of each weight or bias tensor.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. The current cuDNN context handle.
    /// - `attnDesc`: Input. Pointer to a previously initialized attention descriptor.
    /// - `weightSizeInBytes`: Output. Minimum buffer size required to store all multihead attention trainable parameters.
    /// - `workSpaceSizeInBytes`: Output. Minimum buffer size required to hold all temporary surfaces used by the forward and gradient multihead attention API calls.
    /// - `reserveSpaceSizeInBytes`: Output. Minimum buffer size required to store all intermediate data exchanged between forward and backward (gradient) multihead attention functions. Set this parameter to `NULL` in the inference mode indicating that gradient API calls will not be invoked.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: An invalid input argument was found.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The requested buffer sizes were computed successfully.
    #[deprecated]
    pub fn cudnnGetMultiHeadAttnBuffers(
        handle: cudnnHandle_t,
        attnDesc: cudnnAttnDescriptor_t,
        weightSizeInBytes: *mut size_t,
        workSpaceSizeInBytes: *mut size_t,
        reserveSpaceSizeInBytes: *mut size_t,
    ) -> cudnnStatus_t;
}
/// [`cudnnMultiHeadAttnWeightKind_t`] is an enumerated type that specifies a group of weights or biases in the [`cudnnGetMultiHeadAttnWeights`] function.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnMultiHeadAttnWeightKind_t {
    /// Selects the input projection weights for `queries`.
    CUDNN_MH_ATTN_Q_WEIGHTS = 0,
    /// Selects the input projection weights for `keys`.
    CUDNN_MH_ATTN_K_WEIGHTS = 1,
    /// Selects the input projection weights for `values`.
    CUDNN_MH_ATTN_V_WEIGHTS = 2,
    /// Selects the output projection weights.
    CUDNN_MH_ATTN_O_WEIGHTS = 3,
    /// Selects the input projection biases for `queries`.
    CUDNN_MH_ATTN_Q_BIASES = 4,
    /// Selects the input projection biases for `keys`.
    CUDNN_MH_ATTN_K_BIASES = 5,
    /// Selects the input projection biases for `values`.
    CUDNN_MH_ATTN_V_BIASES = 6,
    /// Selects the output projection biases.
    CUDNN_MH_ATTN_O_BIASES = 7,
}
unsafe extern "C" {
    /// This function obtains the shape of the weight or bias tensor. It also retrieves the start address of tensor data located in the `weight` buffer. Use the `wKind` argument to select a particular tensor. For more information, refer to [`cudnnMultiHeadAttnWeightKind_t`] for the description of the enumerant type.
    ///
    /// Biases are used in the input and output projections when the [`CUDNN_ATTN_ENABLE_PROJ_BIASES`] flag is set in the attention descriptor. Refer to [`cudnnSetAttnDescriptor`] for the description of flags to control projection biases.
    ///
    /// When the corresponding weight or bias tensor does not exist, the function writes `NULL` to the storage location pointed by `wAddr` and returns zeros in the `wDesc` tensor descriptor. The return status of the [`cudnnGetMultiHeadAttnWeights`] function is [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`] in this case.
    ///
    /// The cuDNN `multiHeadAttention` sample code demonstrates how to access multihead attention weights. Although the buffer with weights and biases should be allocated in the GPU memory, the user can copy it to the host memory and invoke the [`cudnnGetMultiHeadAttnWeights`] function with the host weights address to obtain tensor pointers in the host memory. This scheme allows the user to inspect trainable parameters directly in the CPU memory.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. The current cuDNN context handle.
    /// - `attnDesc`: Input. A previously configured attention descriptor.
    /// - `wKind`: Input. Enumerant type to specify which weight or bias tensor should be retrieved.
    /// - `weightSizeInBytes`: Input. Buffer size that stores all multihead attention weights and biases.
    /// - `weights`: Input. Pointer to the `weight` buffer in the host or device memory.
    /// - `wDesc`: Output. The descriptor specifying weight or bias tensor shape. For weights, the `wDesc.dimA[]` array has three elements: `\[nHeads, projected size, original size\]`. For biases, the `wDesc.dimA[]` array also has three elements: `\[nHeads, projected size, 1\]`. The `wDesc.strideA[]` array describes how tensor elements are arranged in memory.
    /// - `wAddr`: Output. Pointer to a location where the start address of the requested tensor should be written. When the corresponding projection is disabled, the address written to `wAddr` is `NULL`.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: An invalid or incompatible input argument was encountered. For example, `wKind` did not have a valid value or `weightSizeInBytes` was too small.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The weight tensor descriptor and the address of data in the device memory were successfully retrieved.
    #[deprecated]
    pub fn cudnnGetMultiHeadAttnWeights(
        handle: cudnnHandle_t,
        attnDesc: cudnnAttnDescriptor_t,
        wKind: cudnnMultiHeadAttnWeightKind_t,
        weightSizeInBytes: size_t,
        weights: *const ::core::ffi::c_void,
        wDesc: cudnnTensorDescriptor_t,
        wAddr: *mut *mut ::core::ffi::c_void,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// The [`cudnnMultiHeadAttnForward`] function computes the forward responses of the multihead attention layer. When `reserveSpaceSizeInBytes=0` and `reserveSpace=NULL`, the function operates in the inference mode in which backward (gradient) functions are not invoked, otherwise, the training mode is assumed. In the training mode, the reserve space is used to pass intermediate results from [`cudnnMultiHeadAttnForward`] to [`cudnnMultiHeadAttnBackwardData`] and from [`cudnnMultiHeadAttnBackwardData`] to [`cudnnMultiHeadAttnBackwardWeights`].
    ///
    /// In the inference mode, the `currIdx` specifies the time-step or sequence index of the embedding vectors to be processed. In this mode, the user can perform one iteration for time-step zero (`currIdx=0`), then update **Q**, **K**, **V** vectors and the attention window, and execute the next step (`currIdx=1`). The iterative process can be repeated for all time-steps.
    ///
    /// When all **Q** time-steps are available (for example, in the training mode or in the inference mode on the encoder side in self-attention), the user can assign a negative value to `currIdx` and the [`cudnnMultiHeadAttnForward`] API will automatically sweep through all **Q** time-steps.
    ///
    /// The `loWinIdx[]` and `hiWinIdx[]` host arrays specify the attention window size for each **Q** time-step. In a typical self-attention case, the user must include all previously visited embedding vectors but not the current or future vectors. In this situation, the user should set:
    ///
    /// When `currIdx` is negative in [`cudnnMultiHeadAttnForward`], the `loWinIdx[]` and `hiWinIdx[]` arrays must be fully initialized for all time-steps. When [`cudnnMultiHeadAttnForward`] is invoked with `currIdx=0`, `currIdx=1`, `currIdx=2`, and so on, then the user can update `loWinIdx\[currIdx\]` and `hiWinIdx\[currIdx\]` elements only before invoking the forward response function. All other elements in the `loWinIdx[]` and `hiWinIdx[]` arrays will not be accessed. Any adaptive attention window scheme can be implemented that way.
    ///
    /// Use the following settings when the attention window should be the maximum size, for example, in cross-attention:
    ///
    /// The `maxSeqLenK` value above should be equal to or larger than `dimA\[CUDNN_SEQDATA_TIME_DIM\]` in the `kDesc` descriptor. A good choice is to use `maxSeqLenK=INT_MAX` from `limits.h`.
    ///
    /// `devSeqLengthsQO[]` and `devSeqLengthsKV[]` are pointers to device (not host) arrays with **Q**, **O**, and **K**, **V** sequence lengths. Note that the same information is also passed in the corresponding descriptors of type [`cudnnSeqDataDescriptor_t`] on the host side. The need for extra device arrays comes from the asynchronous nature of cuDNN calls and limited size of the constant memory dedicated to GPU kernel arguments. When the [`cudnnMultiHeadAttnForward`] API returns, the sequence length arrays stored in the descriptors can be immediately modified for the next iteration. However, the GPU kernels launched by the forward call may not have started at this point. For this reason, copies of sequence arrays are needed on the device side to be accessed directly by GPU kernels. Those copies cannot be created inside the [`cudnnMultiHeadAttnForward`] function for very large **K**, **V** inputs without the device memory allocation and CUDA stream synchronization.
    ///
    /// To reduce the [`cudnnMultiHeadAttnForward`] API overhead, `devSeqLengthsQO[]` and `devSeqLengthsKV[]` device arrays are not validated to contain the same settings as `seqLengthArray[]` in the sequence data descriptors.
    ///
    /// Sequence lengths in the `kDesc` and `vDesc` descriptors should be the same. Similarly, sequence lengths in the `qDesc` and `oDesc` descriptors should match. The user can define six different data layouts in the `qDesc`, `kDesc`, `vDesc`, and `oDesc` descriptors. Refer to the [`cudnnSetSeqDataDescriptor`] function for the discussion of those layouts. All multihead attention API calls require that the same layout is used in all sequence data descriptors.
    ///
    /// In the transformer model, the multihead attention block is tightly coupled with the layer normalization and residual connections. [`cudnnMultiHeadAttnForward`] does not encompass the layer normalization but it can be used to handle residual connections as depicted in the following figure.
    ///
    /// Queries and residuals share the same `qDesc` descriptor in [`cudnnMultiHeadAttnForward`]. When residual connections are disabled, the residuals pointer should be `NULL`. When residual connections are enabled, the vector length in `qDesc` should match the vector length specified in the `oDesc` descriptor, so that a vector addition is feasible.
    ///
    /// The `queries`, `keys`, and `values` pointers are not allowed to be `NULL`, even when **K** and **V** are the same inputs or **Q**, **K**, **V** are the same inputs.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. The current cuDNN context handle.
    /// - `attnDesc`: Input. A previously initialized attention descriptor.
    /// - `currIdx`: Input. Time-step in queries to process. When the `currIdx` argument is negative, all **Q** time-steps are processed. When `currIdx` is zero or positive, the forward response is computed for the selected time-step only. The latter input can be used in inference mode only, to process one time-step while updating the next attention window and **Q**, **R**, **K**, **V** inputs in-between calls.
    /// - `qDesc`: Input. Descriptor for the query and residual sequence data.
    /// - `queries`: Input. Pointer to queries data in the device memory.
    /// - `residuals`: Input. Pointer to residual data in device memory. Set this argument to `NULL` if no residual connections are required.
    /// - `kDesc`: Input. Descriptor for the keys sequence data.
    /// - `keys`: Input. Pointer to `keys` data in the device memory.
    /// - `vDesc`: Input. Descriptor for the `values` sequence data.
    /// - `values`: Input. Pointer to `values` data in the device memory.
    /// - `oDesc`: Input. Descriptor for the multihead attention output sequence data.
    /// - `out`: Output. Pointer to device memory where the output response should be written.
    /// - `weightSizeInBytes`: Input. Size of the `weight` buffer in bytes where all multihead attention trainable parameters are stored.
    /// - `weights`: Input. Pointer to the `weight` buffer in device memory.
    /// - `workSpaceSizeInBytes`: Input. Size of the workspace buffer in bytes used for temporary API storage.
    /// - `workSpace`: Input/output. Pointer to the workspace buffer in device memory.
    /// - `reserveSpaceSizeInBytes`: Input. Size of the reserve-space buffer in bytes used for data exchange between forward and backward (gradient) API calls. This parameter should be zero in the inference mode and non-zero in the training mode.
    /// - `reserveSpace`: Input/output. Pointer to the reserve-space buffer in device memory. This argument should be `NULL` in inference mode and `non-NULL` in the training mode.
    ///
    /// # Return value
    ///
    /// - `CUDNN_STATUS_ALLOC_FAILED`: Insufficient amount of shared memory to launch a GPU kernel.
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: An invalid or incompatible input argument was encountered. Some examples include:
    ///
    /// * a required input pointer was `NULL`
    /// * `currIdx` was out of bound
    /// * the descriptor value for `attention`, `query`, `key`, `value`, and `output` were incompatible with one another.
    /// - [`cudnnStatus_t::CUDNN_STATUS_EXECUTION_FAILED`]: The process of launching a GPU kernel returned an error, or an earlier kernel did not complete successfully.
    /// - [`cudnnStatus_t::CUDNN_STATUS_INTERNAL_ERROR`]: An inconsistent internal state was encountered.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: A requested option or a combination of input arguments is not supported.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: No errors were detected while processing API input arguments and launching GPU kernels.
    #[deprecated]
    pub fn cudnnMultiHeadAttnForward(
        handle: cudnnHandle_t,
        attnDesc: cudnnAttnDescriptor_t,
        currIdx: ::core::ffi::c_int,
        loWinIdx: *const ::core::ffi::c_int,
        hiWinIdx: *const ::core::ffi::c_int,
        devSeqLengthsQO: *const ::core::ffi::c_int,
        devSeqLengthsKV: *const ::core::ffi::c_int,
        qDesc: cudnnSeqDataDescriptor_t,
        queries: *const ::core::ffi::c_void,
        residuals: *const ::core::ffi::c_void,
        kDesc: cudnnSeqDataDescriptor_t,
        keys: *const ::core::ffi::c_void,
        vDesc: cudnnSeqDataDescriptor_t,
        values: *const ::core::ffi::c_void,
        oDesc: cudnnSeqDataDescriptor_t,
        out: *mut ::core::ffi::c_void,
        weightSizeInBytes: size_t,
        weights: *const ::core::ffi::c_void,
        workSpaceSizeInBytes: size_t,
        workSpace: *mut ::core::ffi::c_void,
        reserveSpaceSizeInBytes: size_t,
        reserveSpace: *mut ::core::ffi::c_void,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// Cross-library version checker. Each sublibrary has a version checker that checks whether its own version matches that of its dependencies.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUBLIBRARY_VERSION_MISMATCH`]: The versions are inconsistent.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The version check passed.
    pub fn cudnnAdvVersionCheck() -> cudnnStatus_t;
}
/// [`cudnnWgradMode_t`] is an enumerated type that selects how buffers holding gradients of the loss function, computed with respect to trainable parameters, are updated. Currently, this type is used by the [`cudnnMultiHeadAttnBackwardWeights`] and [`cudnnRNNBackwardWeights_v8`] functions only.
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnWgradMode_t {
    /// A weight gradient component corresponding to a new batch of inputs is added to previously evaluated weight gradients. Before using this mode, the buffer holding weight gradients should be initialized to zero. Alternatively, the first API call outputting to an uninitialized buffer should use the [`cudnnWgradMode_t::CUDNN_WGRAD_MODE_SET`] option.
    CUDNN_WGRAD_MODE_ADD = 0,
    /// A weight gradient component, corresponding to a new batch of inputs, overwrites previously stored weight gradients in the output buffer.
    CUDNN_WGRAD_MODE_SET = 1,
}
unsafe extern "C" {
    /// This function computes exact, first-order derivatives of the RNN model with respect to its inputs: `x`, `hx` and for the LSTM cell type also `cx`. If `o = \[y, hy, cy\] = F(x, hx, cx) = F(z)` is a vector-valued function that represents the entire RNN model and it takes vectors `x` (for all time-steps) and vectors `hx`, `cx` (for all layers) as inputs, concatenated into $\textbf{z}\epsilon\mathbb{R}^{n}$ (network weights and biases are assumed constant), and outputs vectors `y`, `hy`, `cy` concatenated into a vector $\textbf{o}\epsilon\mathbb{R}^{m}$, then [`cudnnRNNBackwardData_v8`] computes the result of $\left(\partial o_{i}/\partial z_{j}\right)^{T} \delta_{out}$ where $\delta_{out}$ is the `mx1` gradient of the loss function with respect to all RNN outputs. The $\delta_{out}$ gradient is back propagated through prior layers of the deep learning model, starting from the model output. $\partial o_{i}/\partial z_{j}$ is the `mxn` Jacobian matrix of `F(z)`. The $\delta_{out}$ input is supplied via the `dy`, `dhy`, and `dcy` arguments and gradient results $\left(\partial o_{i}/\partial z_{j}\right)^{T} \delta_{out}$ are written to the `dx`, `dhx`, and `dcx` buffers.
    ///
    /// Locations of `x`, `y`, `hx`, `cx`, `hy`, `cy`, `dx`, `dy`, `dhx`, `dcx`, `dhy`, and `dcy` signals a multi-layer RNN model are shown in the following figure. Note that internal RNN signals (between time-steps and between layers) are not exposed by the [`cudnnRNNBackwardData_v8`] function.
    ///
    /// Memory addresses to the primary RNN output `y`, the initial hidden state `hx`, and the initial cell state `cx` (for LSTM only) should point to the same data as in the preceding [`cudnnRNNForward`] call. The `dy` and `dx` pointers cannot be `NULL`.
    ///
    /// The [`cudnnRNNBackwardData_v8`] function accepts any combination of `dhy`, `dhx`, `dcy`, `dcx` buffer addresses being `NULL`. When `dhy` or `dcy` are `NULL`, it is assumed that those inputs are zero. When `dhx` or `dcx` pointers are `NULL`, then the corresponding results are not written by [`cudnnRNNBackwardData_v8`]. When all `hx`, `dhy`, `dhx` pointers are `NULL`, then the corresponding tensor descriptor `hDesc` can be `NULL` too. The same rule applies to the `cx`, `dcy`, `dcx` pointers and the `cDesc` tensor descriptor.
    ///
    /// The [`cudnnRNNBackwardData_v8`] function allows the user to use padded layouts for inputs `y`, `dy`, and output `dx`. In padded or unpacked layouts ([`cudnnRNNDataLayout_t::CUDNN_RNN_DATA_LAYOUT_SEQ_MAJOR_UNPACKED`], [`cudnnRNNDataLayout_t::CUDNN_RNN_DATA_LAYOUT_BATCH_MAJOR_UNPACKED`]) each sequence of vectors in a mini-batch has a fixed length defined by the `maxSeqLength` argument in the [`cudnnSetRNNDataDescriptor`] function. The term “unpacked” refers here to the presence of padding vectors, and not unused address ranges between contiguous vectors.
    ///
    /// Each padded, fixed-length sequence starts from a segment of valid vectors. The valid vector count is stored in `seqLengthArray` passed to [`cudnnSetRNNDataDescriptor`], such that `0 &lt; seqLengthArray\[i\] &lt;= maxSeqLength` for all sequences in a mini-batch, that is, for `i=0..batchSize-1`. The remaining, padding vectors make the combined sequence length equal to `maxSeqLength`. Both sequence-major and batch-major padded layouts are supported. In addition, a packed sequence-major layout is supported: [`cudnnRNNDataLayout_t::CUDNN_RNN_DATA_LAYOUT_SEQ_MAJOR_PACKED`].
    ///
    /// In the latter layout, sequences of vectors in a mini-batch are sorted in the descending order according to the sequence lengths. First all vectors for time step zero are stored. They are followed by all vectors for time step one, and so on. This layout uses no padding vectors.
    ///
    /// The same layout type must be specified in `xDesc` and `yDesc` descriptors.
    ///
    /// Two host arrays named `seqLengthArray` in `xDesc` and `yDesc` RNN data descriptors must be the same. In addition, a copy of `seqLengthArray` in the device memory must be passed via the `devSeqLengths` argument. This array is supplied directly to GPU kernels. Starting in cuDNN 8.9.1, the `devSeqLengths` parameter is no longer required and can be set to `NULL`. The variable sequence length array is transferred automatically to GPU memory by the [`cudnnRNNBackwardData_v8`] function.
    ///
    /// The [`cudnnRNNBackwardData_v8`] function does not verify that sequence lengths stored in `devSeqLengths` in GPU memory are the same as in `xDesc` and `yDesc` descriptors in CPU memory. Sequence length arrays from `xDesc` and `yDesc` descriptors are checked for consistency, however.
    ///
    /// The [`cudnnRNNBackwardData_v8`] function must be called after [`cudnnRNNForward`]. The [`cudnnRNNForward`] function should be invoked with the fwdMode argument of type [`cudnnForwardMode_t`] set to [`cudnnForwardMode_t::CUDNN_FWD_MODE_TRAINING`].
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. The current cuDNN context handle.
    /// - `rnnDesc`: Input. A previously initialized RNN descriptor.
    /// - `devSeqLengths`: Input. A copy of `seqLengthArray` from `xDesc` or `yDesc` RNN data descriptors. The `devSeqLengths` array must be stored in GPU memory as it is accessed asynchronously by GPU kernels, possibly after the [`cudnnRNNBackwardData_v8`] function exists. In cuDNN 8.9.1 and later versions, `devSeqLengths` should be `NULL`.
    /// - `yDesc`: Input. A previously initialized descriptor corresponding to the RNN model primary output. The `dataType`, `layout`, `maxSeqLength`, `batchSize`, and `seqLengthArray` need to match that of `xDesc`.
    /// - `xDesc`: Input. A previously initialized RNN data descriptor corresponding to the gradient of the loss function with respect to the RNN primary model input. The `dataType`, `layout`, `maxSeqLength`, `batchSize`, and `seqLengthArray` must match that of `yDesc`. The parameter `vectorSize` must match the `inputSize` argument passed to the [`cudnnSetRNNDescriptor_v8`] function.
    /// - `dx`: Output. Data pointer to GPU memory where back-propagated gradients of the loss function with respect to the RNN primary input x should be stored. The vectors are expected to be arranged in memory according to the layout specified by `xDesc`. The elements in the tensor (including padding vectors) must be densely packed. This argument cannot be `NULL`.
    /// - `hDesc`: Input. A tensor descriptor describing the initial RNN hidden state `hx` and gradient deltas `dhy, dhx` of the loss function. Hidden state data and gradients must be fully packed. The first dimension of the tensor depends on the `dirMode` argument passed to the [`cudnnSetRNNDescriptor_v8`] function.
    ///
    /// * If `dirMode` is [`cudnnDirectionMode_t::CUDNN_UNIDIRECTIONAL`], then the first dimension should match the `numLayers` argument passed to [`cudnnSetRNNDescriptor_v8`].
    /// * If `dirMode` is [`cudnnDirectionMode_t::CUDNN_BIDIRECTIONAL`], then the first dimension should be double the `numLayers` argument passed to [`cudnnSetRNNDescriptor_v8`].
    ///
    /// The second dimension must match the `batchSize` parameter described in `xDesc`. The third dimension depends on whether RNN mode is [`cudnnRNNMode_t::CUDNN_LSTM`] and whether the LSTM projection is enabled. Specifically:
    ///
    /// * If RNN mode is [`cudnnRNNMode_t::CUDNN_LSTM`] and LSTM projection is enabled, the third dimension must match the `projSize` argument.
    /// * Otherwise, the third dimension must match the `hiddenSize` argument.
    /// - `dhx`: Output. Pointer to the GPU buffer where first-order derivatives corresponding to initial hidden state variables should be stored. Data dimensions are described by the `hDesc` tensor descriptor. If a `NULL` pointer is assigned to `dhx`, the back-propagated derivatives are not saved.
    /// - `cDesc`: Input. For LSTM networks only. This argument should be `NULL` for `RELU`, `TANH`, or `GRU` cell types. `cDesc` is a tensor descriptor specifying buffer layouts of the initial cell state `cx` and gradient deltas `dcy, dcx` of the loss function. Cell state data must be fully packed. The first dimension of the tensor depends on the `dirMode` argument passed to the [`cudnnSetRNNDescriptor_v8`] call.
    ///
    /// * If `dirMode` is [`cudnnDirectionMode_t::CUDNN_UNIDIRECTIONAL`], then the first dimension should match the `numLayers` argument passed to [`cudnnSetRNNDescriptor_v8`].
    /// * If `dirMode` is [`cudnnDirectionMode_t::CUDNN_BIDIRECTIONAL`], then the first dimension should be double the `numLayers` argument passed to [`cudnnSetRNNDescriptor_v8`].
    ///
    /// The second tensor dimension must match the `batchSize` parameter in `xDesc`. The third dimension must match the `hiddenSize` argument passed to the [`cudnnSetRNNDescriptor_v8`] call.
    /// - `dcx`: Output. For LSTM networks only. Pointer to the GPU buffer where first-order derivatives corresponding to initial LSTM state variables should be stored. Data dimensions are described by the `cDesc` tensor descriptor. If a `NULL` pointer is assigned to `dcx`, the back-propagated derivatives are not saved.
    /// - `weightSpaceSize`: Input. Specifies the size in bytes of the provided weight space buffer.
    /// - `weightSpace`: Input. Address of the weight space buffer in GPU memory.
    /// - `workSpaceSize`: Input. Specifies the size in bytes of the provided workspace buffer.
    /// - `workSpace`: Input/output. Address of the workspace buffer in GPU memory to store temporary data.
    /// - `reserveSpaceSize`: Input. Specifies the size in bytes of the reserve-space buffer.
    /// - `reserveSpace`: Input/output. Address of the reserve-space buffer in GPU memory.
    ///
    /// # Return value
    ///
    /// - `CUDNN_STATUS_ALLOC_FAILED`: The function was unable to allocate CPU memory.
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: An invalid or incompatible input argument was encountered. Some examples include:
    ///
    /// * some descriptors or data buffer addresses are `NULL`
    /// * settings in `rnnDesc`, `xDesc`, `yDesc`, `hDesc`, or `cDesc` descriptors are invalid
    /// * `weightSpaceSize`, `workSpaceSize`, or `reserveSpaceSize` is too small.
    /// - [`cudnnStatus_t::CUDNN_STATUS_EXECUTION_FAILED`]: The process of launching a GPU kernel returned an error, or an earlier kernel did not complete successfully.
    /// - `CUDNN_STATUS_MAPPING_ERROR`: A GPU/CUDA resource, such as a texture object, shared memory, or zero-copy memory is not available in the required size or there is a mismatch between the user resource and cuDNN internal resources. A resource mismatch may occur, for example, when calling [`cudnnSetStream`]. There could be a mismatch between the user provided CUDA stream and the internal CUDA events instantiated in the cuDNN handle when [`cudnnCreate`] was invoked.
    ///
    /// This error status may not be correctable when it is related to texture dimensions, shared memory size, or zero-copy memory availability. If `CUDNN_STATUS_MAPPING_ERROR` is returned by [`cudnnSetStream`], then it is typically correctable, however, it means that the cuDNN handle was created on one GPU and the user stream passed to this function is associated with another GPU.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: At least one of the following conditions are met:
    ///
    /// * variable sequence length input is passed while [`cudnnRNNAlgo_t::CUDNN_RNN_ALGO_PERSIST_STATIC`], [`cudnnRNNAlgo_t::CUDNN_RNN_ALGO_PERSIST_DYNAMIC`], or [`cudnnRNNAlgo_t::CUDNN_RNN_ALGO_PERSIST_STATIC_SMALL_H`] is specified
    /// * [`cudnnRNNAlgo_t::CUDNN_RNN_ALGO_PERSIST_STATIC`] or [`cudnnRNNAlgo_t::CUDNN_RNN_ALGO_PERSIST_DYNAMIC`] is requested on pre-Pascal devices
    /// * the ‘double’ floating point type is used for input/output and the [`cudnnRNNAlgo_t::CUDNN_RNN_ALGO_PERSIST_STATIC`] algo.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: No errors were detected while processing API input arguments and launching GPU kernels.
    pub fn cudnnRNNBackwardData_v8(
        handle: cudnnHandle_t,
        rnnDesc: cudnnRNNDescriptor_t,
        devSeqLengths: *const i32,
        yDesc: cudnnRNNDataDescriptor_t,
        y: *const ::core::ffi::c_void,
        dy: *const ::core::ffi::c_void,
        xDesc: cudnnRNNDataDescriptor_t,
        dx: *mut ::core::ffi::c_void,
        hDesc: cudnnTensorDescriptor_t,
        hx: *const ::core::ffi::c_void,
        dhy: *const ::core::ffi::c_void,
        dhx: *mut ::core::ffi::c_void,
        cDesc: cudnnTensorDescriptor_t,
        cx: *const ::core::ffi::c_void,
        dcy: *const ::core::ffi::c_void,
        dcx: *mut ::core::ffi::c_void,
        weightSpaceSize: size_t,
        weightSpace: *const ::core::ffi::c_void,
        workSpaceSize: size_t,
        workSpace: *mut ::core::ffi::c_void,
        reserveSpaceSize: size_t,
        reserveSpace: *mut ::core::ffi::c_void,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function computes exact, first-order derivatives of the RNN model with respect to all trainable parameters: weights and biases. If `o = \[y, hy, cy\] = F(w)` is a vector-valued function that represents the multi-layer RNN model and it takes some vector $\textbf{w}\epsilon\mathbb{R}^{n}$ of “flatten” weights or biases as input (with all other data inputs constant), and outputs vector $\textbf{o}\epsilon\mathbb{R}^{m}$, then [`cudnnRNNBackwardWeights_v8`] computes the result of $\left(\partial o_{i}/\partial w_{j}\right)^{T} \delta_{out}$ where $\delta_{out}$ is the `mx1` gradient of the loss function with respect to all RNN outputs. The $\delta_{out}$ gradient is back propagated through prior layers of the deep learning model, starting from the model output. $\partial o_{i}/\partial w_{j}$ is the `mxn` Jacobian matrix of `F(w)`. The $\delta_{out}$ input is supplied via the `dy`, `dhy`, and `dcy` arguments in the [`cudnnRNNBackwardData_v8`] function.
    ///
    /// All gradient results $\left(\partial o_{i}/\partial w_{j}\right)^{T} \delta_{out}$ with respect to weights and biases are written to the `dweightSpace` buffer. The size and the organization of the `dweightSpace` buffer is the same as the `weightSpace` buffer that holds RNN weights and biases.
    ///
    /// Gradient of the loss function with respect to weights and biases is typically computed over multiple mini-batches. In such a case, partial results computed for each mini-batch should be aggregated. The `addGrad` argument specifies if gradients from the current mini-batch should be added to previously computed results ([`cudnnWgradMode_t::CUDNN_WGRAD_MODE_ADD`]) or the `dweightSpace` buffer should be overwritten with the new results ([`cudnnWgradMode_t::CUDNN_WGRAD_MODE_SET`]). Currently, the [`cudnnRNNBackwardWeights_v8`] function supports the [`cudnnWgradMode_t::CUDNN_WGRAD_MODE_ADD`] mode only so the `dweightSpace` buffer should be zeroed by the user before invoking the routine for the first time.
    ///
    /// The same sequence lengths must be specified in the `xDesc` descriptor and in the device array `devSeqLengths`. Starting in cuDNN 8.9.1, the `devSeqLengths` parameter is no longer required and can be set to `NULL`. The variable sequence length array is transferred automatically to GPU memory by the [`cudnnRNNBackwardWeights_v8`] function.
    ///
    /// The [`cudnnRNNBackwardWeights_v8`] function should be invoked after [`cudnnRNNBackwardData_v8`].
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. The current cuDNN context handle.
    /// - `rnnDesc`: Input. A previously initialized RNN descriptor.
    /// - `addGrad`: Input. Weight gradient output mode. For more details, refer to the description of the [`cudnnWgradMode_t`] enumerated type. Currently, only the [`cudnnWgradMode_t::CUDNN_WGRAD_MODE_ADD`] mode is supported by the [`cudnnRNNBackwardWeights_v8`] function.
    /// - `devSeqLengths`: Input. A copy of `seqLengthArray` from the `xDesc` RNN data descriptor. The `devSeqLengths` array must be stored in GPU memory as it is accessed asynchronously by GPU kernels, possibly after the [`cudnnRNNBackwardWeights_v8`] function exists. In cuDNN 8.9.1 and later versions, `devSeqLengths` should be `NULL`.
    /// - `xDesc`: Input. A previously initialized descriptor corresponding to the RNN model input data. This is the same RNN data descriptor as used in the preceding [`cudnnRNNForward`] and [`cudnnRNNBackwardData_v8`] calls.
    /// - `x`: Input. Pointer to the GPU buffer with the primary RNN input. The same buffer address `x` should be provided in prior [`cudnnRNNForward`] and [`cudnnRNNBackwardData_v8`] calls.
    /// - `hDesc`: Input. A tensor descriptor describing the initial RNN hidden state. Hidden state data are fully packed. This is the same tensor descriptor as used in prior [`cudnnRNNForward`] and [`cudnnRNNBackwardData_v8`] calls.
    /// - `hx`: Input. Pointer to the GPU buffer with the RNN initial hidden state. The same buffer address `hx` should be provided in prior [`cudnnRNNForward`] and [`cudnnRNNBackwardData_v8`] calls.
    /// - `yDesc`: Input. A previously initialized descriptor corresponding to the RNN model output data. This is the same RNN data descriptor as used in prior [`cudnnRNNForward`] and [`cudnnRNNBackwardData_v8`] calls.
    /// - `y`: Output. Pointer to the GPU buffer with the primary RNN output as generated by the prior [`cudnnRNNForward`] call. Data in the `y` buffer are described by the `yDesc` descriptor. Elements in the `y` tensor (including elements in padding vectors) must be densely packed.
    /// - `weightSpaceSize`: Input. Specifies the size in bytes of the provided weight space buffer.
    /// - `dweightSpace`: Output. Address of the weight space buffer in GPU memory.
    /// - `workSpaceSize`: Input. Specifies the size in bytes of the provided workspace buffer.
    /// - `workSpace`: Input/output. Address of the workspace buffer in GPU memory to store temporary data.
    /// - `reserveSpaceSize`: Input. Specifies the size in bytes of the reserve-space buffer.
    /// - `reserveSpace`: Input/output. Address of the reserve-space buffer in GPU memory.
    ///
    /// # Return value
    ///
    /// - `CUDNN_STATUS_ALLOC_FAILED`: The function was unable to allocate CPU memory.
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: An invalid or incompatible input argument was encountered. Some examples include:
    ///
    /// * some descriptors or data buffer addresses are `NULL`
    /// * settings in `rnnDesc`, `xDesc`, `yDesc`, or `hDesc` descriptors are invalid
    /// * `weightSpaceSize`, `workSpaceSize`, or `reserveSpaceSize` values are too small
    /// * the `addGrad` argument is not equal to [`cudnnWgradMode_t::CUDNN_WGRAD_MODE_ADD`].
    /// - [`cudnnStatus_t::CUDNN_STATUS_EXECUTION_FAILED`]: The process of launching a GPU kernel returned an error, or an earlier kernel did not complete successfully.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: The function does not support the provided configuration.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: No errors were detected while processing API input arguments and launching GPU kernels.
    pub fn cudnnRNNBackwardWeights_v8(
        handle: cudnnHandle_t,
        rnnDesc: cudnnRNNDescriptor_t,
        addGrad: cudnnWgradMode_t,
        devSeqLengths: *const i32,
        xDesc: cudnnRNNDataDescriptor_t,
        x: *const ::core::ffi::c_void,
        hDesc: cudnnTensorDescriptor_t,
        hx: *const ::core::ffi::c_void,
        yDesc: cudnnRNNDataDescriptor_t,
        y: *const ::core::ffi::c_void,
        weightSpaceSize: size_t,
        dweightSpace: *mut ::core::ffi::c_void,
        workSpaceSize: size_t,
        workSpace: *mut ::core::ffi::c_void,
        reserveSpaceSize: size_t,
        reserveSpace: *mut ::core::ffi::c_void,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function computes exact, first-order derivatives of the multihead attention block with respect to its inputs: **Q**, **K**, **V**. If `y=F(w)` is a vector-valued function that represents the multihead attention layer and it takes some vector $\chi\epsilon\mathbb{R}^{n}$ as an input (with all other parameters and inputs constant), and outputs vector $\chi\epsilon\mathbb{R}^{m}$, then [`cudnnMultiHeadAttnBackwardData`] computes the result of $\left(\partial y_{i}/\partial x_{j}\right)^{T} \delta_{out}$ where $\delta_{out}$ is the `mx1` gradient of the loss function with respect to multihead attention outputs. The $\delta_{out}$ gradient is back propagated through prior layers of the deep learning model. $\partial y_{i}/\partial x_{j}$ is the `mxn` Jacobian matrix of `F(x)`. The input is supplied via the `dout` argument and gradient results for **Q**, **K**, **V** are written to the `dqueries`, `dkeys`, and `dvalues` buffers.
    ///
    /// The [`cudnnMultiHeadAttnBackwardData`] function does not output partial derivatives for residual connections because this result is equal to $\delta_{out}$. If the multihead attention model enables residual connections sourced directly from **Q**, then the `dout` tensor needs to be added to `dqueries` to obtain the correct result of the latter. This operation is demonstrated in the cuDNN `multiHeadAttention` sample code.
    ///
    /// The [`cudnnMultiHeadAttnBackwardData`] function must be invoked after [`cudnnMultiHeadAttnForward`]. The `loWinIdx[]`, `hiWinIdx[]`, `queries`, `keys`, `values`, `weights`, and `reserveSpace` arguments should be the same as in the [`cudnnMultiHeadAttnForward`] call. `devSeqLengthsDQDO[]` and `devSeqLengthsDKDV[]` device arrays should contain the same start and end attention window indices as `devSeqLengthsQO[]` and `devSeqLengthsKV[]` arrays in the forward function invocation.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. The current cuDNN context handle.
    /// - `attnDesc`: Input. A previously initialized attention descriptor.
    /// - `doDesc`: Input. Descriptor for the $\delta_{out}$ gradients (vectors of partial derivatives of the loss function with respect to the multihead attention outputs).
    /// - `dout`: Input. Pointer to the $\delta_{out}$ gradient data in the device memory.
    /// - `dqDesc`: Input. Descriptor for `queries` and `dqueries` sequence data.
    /// - `dqueries`: Output. Device pointer to gradients of the loss function computed with respect to `queries` vectors.
    /// - `queries`: Input. Pointer to `queries` data in the device memory. This is the same input as in [`cudnnMultiHeadAttnForward`].
    /// - `dkDesc`: Input. Descriptor for keys and `dkeys` sequence data.
    /// - `dkeys`: Output. Device pointer to gradients of the loss function computed with respect to `keys` vectors.
    /// - `keys`: Input. Pointer to `keys` data in the device memory. This is the same input as in [`cudnnMultiHeadAttnForward`].
    /// - `dvDesc`: Input. Descriptor for `values` and `dvalues` sequence data.
    /// - `dvalues`: Output. Device pointer to gradients of the loss function computed with respect to `values` vectors.
    /// - `values`: Input. Pointer to `values` data in the device memory. This is the same input as in [`cudnnMultiHeadAttnForward`].
    /// - `weightSizeInBytes`: Input. Size of the `weight` buffer in bytes where all multihead attention trainable parameters are stored.
    /// - `weights`: Input. Address of the `weight` buffer in the device memory.
    /// - `workSpaceSizeInBytes`: Input. Size of the workspace buffer in bytes used for temporary API storage.
    /// - `workSpace`: Input/output. Address of the workspace buffer in the device memory.
    /// - `reserveSpaceSizeInBytes`: Input. Size of the reserve-space buffer in bytes used for data exchange between forward and backward (gradient) API calls.
    /// - `reserveSpace`: Input/output. Address to the reserve-space buffer in the device memory.
    ///
    /// # Return value
    ///
    /// - `CUDNN_STATUS_ALLOC_FAILED`: Insufficient amount of shared memory to launch a GPU kernel.
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: An invalid or incompatible input argument was encountered.
    /// - [`cudnnStatus_t::CUDNN_STATUS_EXECUTION_FAILED`]: The process of launching a GPU kernel returned an error, or an earlier kernel did not complete successfully.
    /// - [`cudnnStatus_t::CUDNN_STATUS_INTERNAL_ERROR`]: An inconsistent internal state was encountered.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: A requested option or a combination of input arguments is not supported.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: No errors were detected while processing API input arguments and launching GPU kernels.
    #[deprecated]
    pub fn cudnnMultiHeadAttnBackwardData(
        handle: cudnnHandle_t,
        attnDesc: cudnnAttnDescriptor_t,
        loWinIdx: *const ::core::ffi::c_int,
        hiWinIdx: *const ::core::ffi::c_int,
        devSeqLengthsDQDO: *const ::core::ffi::c_int,
        devSeqLengthsDKDV: *const ::core::ffi::c_int,
        doDesc: cudnnSeqDataDescriptor_t,
        dout: *const ::core::ffi::c_void,
        dqDesc: cudnnSeqDataDescriptor_t,
        dqueries: *mut ::core::ffi::c_void,
        queries: *const ::core::ffi::c_void,
        dkDesc: cudnnSeqDataDescriptor_t,
        dkeys: *mut ::core::ffi::c_void,
        keys: *const ::core::ffi::c_void,
        dvDesc: cudnnSeqDataDescriptor_t,
        dvalues: *mut ::core::ffi::c_void,
        values: *const ::core::ffi::c_void,
        weightSizeInBytes: size_t,
        weights: *const ::core::ffi::c_void,
        workSpaceSizeInBytes: size_t,
        workSpace: *mut ::core::ffi::c_void,
        reserveSpaceSizeInBytes: size_t,
        reserveSpace: *mut ::core::ffi::c_void,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function computes exact, first-order derivatives of the multihead attention block with respect to its trainable parameters: projection weights and projection biases. If `y=F(w)` is a vector-valued function that represents the multihead attention layer and it takes some vector $\chi\epsilon\mathbb{R}^{n}$ of “flatten” weights or biases as an input (with all other parameters and inputs fixed), and outputs vector $\chi\epsilon\mathbb{R}^{m}$, then [`cudnnMultiHeadAttnBackwardWeights`] computes the result of $\left(\partial y_{i}/\partial w_{j}\right)^{T} \delta_{out}$ where $\delta_{out}$ is the `mx1` gradient of the loss function with respect to multihead attention outputs. The $\delta_{out}$ gradient is back propagated through prior layers of the deep learning model. $\partial y_{i}/\partial w_{j}$ is the `mxn` Jacobian matrix of `F(w)`. The $\delta_{out}$ input is supplied via the `dout` argument.
    ///
    /// All gradient results with respect to weights and biases are written to the `dweights` buffer. The size and the organization of the `dweights` buffer is the same as the `weights` buffer that holds multihead attention weights and biases. The cuDNN `multiHeadAttention` sample code demonstrates how to access those weights.
    ///
    /// Gradient of the loss function with respect to weights or biases is typically computed over multiple batches. In such a case, partial results computed for each batch should be summed together. The `addGrad` argument specifies if the gradients from the current batch should be added to previously computed results or the `dweights` buffer should be overwritten with the new results.
    ///
    /// The [`cudnnMultiHeadAttnBackwardWeights`] function should be invoked after [`cudnnMultiHeadAttnBackwardData`]. The `queries`, `keys`, `values`, `weights`, and `reserveSpace` arguments should be the same as in [`cudnnMultiHeadAttnForward`] and [`cudnnMultiHeadAttnBackwardData`] calls. The `dout` argument should be the same as in [`cudnnMultiHeadAttnBackwardData`].
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. The current cuDNN context handle.
    /// - `attnDesc`: Input. A previously initialized attention descriptor.
    /// - `addGrad`: Input. Weight gradient output mode.
    /// - `qDesc`: Input. Descriptor for the query sequence data.
    /// - `queries`: Input. Pointer to `queries` sequence data in the device memory.
    /// - `kDesc`: Input. Descriptor for the `keys` sequence data.
    /// - `keys`: Input. Pointer to `keys` sequence data in the device memory.
    /// - `vDesc`: Input. Descriptor for the `values` sequence data.
    /// - `values`: Input. Pointer to `values` sequence data in the device memory.
    /// - `doDesc`: Input. Descriptor for the $\delta_{out}$ gradients (vectors of partial derivatives of the loss function with respect to the multihead attention outputs).
    /// - `dout`: Input. Pointer to the $\delta_{out}$ gradient vectors in the device memory.
    /// - `weightSizeInBytes`: Input. Size of the `weights` and `dweights` buffers in bytes.
    /// - `weights`: Input. Address of the `weight` buffer in the device memory.
    /// - `dweights`: Output. Address of the weight gradient buffer in the device memory.
    /// - `workSpaceSizeInBytes`: Input. Size of the workspace buffer in bytes used for temporary API storage.
    /// - `workSpace`: Input/output. Address of the workspace buffer in the device memory.
    /// - `reserveSpaceSizeInBytes`: Input. Size of the reserve-space buffer in bytes used for data exchange between forward and backward (gradient) API calls.
    /// - `reserveSpace`: Input/output. Address to the reserve-space buffer in the device memory.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: An invalid or incompatible input argument was encountered.
    /// - [`cudnnStatus_t::CUDNN_STATUS_EXECUTION_FAILED`]: The process of launching a GPU kernel returned an error, or an earlier kernel did not complete successfully.
    /// - [`cudnnStatus_t::CUDNN_STATUS_INTERNAL_ERROR`]: An inconsistent internal state was encountered.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: A requested option or a combination of input arguments is not supported.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: No errors were detected while processing API input arguments and launching GPU kernels.
    #[deprecated]
    pub fn cudnnMultiHeadAttnBackwardWeights(
        handle: cudnnHandle_t,
        attnDesc: cudnnAttnDescriptor_t,
        addGrad: cudnnWgradMode_t,
        qDesc: cudnnSeqDataDescriptor_t,
        queries: *const ::core::ffi::c_void,
        kDesc: cudnnSeqDataDescriptor_t,
        keys: *const ::core::ffi::c_void,
        vDesc: cudnnSeqDataDescriptor_t,
        values: *const ::core::ffi::c_void,
        doDesc: cudnnSeqDataDescriptor_t,
        dout: *const ::core::ffi::c_void,
        weightSizeInBytes: size_t,
        weights: *const ::core::ffi::c_void,
        dweights: *mut ::core::ffi::c_void,
        workSpaceSizeInBytes: size_t,
        workSpace: *mut ::core::ffi::c_void,
        reserveSpaceSizeInBytes: size_t,
        reserveSpace: *mut ::core::ffi::c_void,
    ) -> cudnnStatus_t;
}
/// [`cudnnLossNormalizationMode_t`] is an enumerated type that controls the input normalization mode for a loss function. This type can be used with [`cudnnSetCTCLossDescriptorEx`].
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnLossNormalizationMode_t {
    /// The input probs of the [`cudnnCTCLoss`] function is expected to be the normalized probability, and the output `gradients` is the gradient of loss with respect to the unnormalized probability.
    CUDNN_LOSS_NORMALIZATION_NONE = 0,
    /// The input probs of the [`cudnnCTCLoss`] function is expected to be the unnormalized activation from the previous layer, and the output `gradients` is the gradient with respect to the activation. Internally the probability is computed by softmax normalization.
    CUDNN_LOSS_NORMALIZATION_SOFTMAX = 1,
}
unsafe extern "C" {
    /// This function creates a CTC loss function descriptor.
    ///
    /// # Parameters
    ///
    /// - `ctcLossDesc`: Output. CTC loss descriptor to be set. For more information, refer to [`cudnnCTCLossDescriptor_t`].
    ///
    /// # Return value
    ///
    /// - `CUDNN_STATUS_ALLOC_FAILED`: Memory allocation for this CTC loss descriptor failed.
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: The CTC loss descriptor passed to the function is invalid.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The function returned successfully.
    pub fn cudnnCreateCTCLossDescriptor(
        ctcLossDesc: *mut cudnnCTCLossDescriptor_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function sets a CTC loss function descriptor. Refer to the extended version [`cudnnSetCTCLossDescriptorEx`] to set the input normalization mode.
    ///
    /// When the extended version [`cudnnSetCTCLossDescriptorEx`] is used with `normMode` set to [`cudnnLossNormalizationMode_t::CUDNN_LOSS_NORMALIZATION_NONE`] and the `gradMode` set to [`cudnnNanPropagation_t::CUDNN_NOT_PROPAGATE_NAN`], then it is the same as the current function [`cudnnSetCTCLossDescriptor`], meaning.
    ///
    /// # Parameters
    ///
    /// - `ctcLossDesc`: Output. CTC loss descriptor to be set.
    /// - `compType`: Input. Compute type for this CTC loss function.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the input parameters passed is invalid.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The function returned successfully.
    #[deprecated]
    pub fn cudnnSetCTCLossDescriptor(
        ctcLossDesc: cudnnCTCLossDescriptor_t,
        compType: cudnnDataType_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function is an extension of [`cudnnSetCTCLossDescriptor`]. This function provides an additional interface `normMode` to set the input normalization mode for the CTC loss function, and `gradMode` to control the `NaN` propagation type.
    ///
    /// When this function [`cudnnSetCTCLossDescriptorEx`] is used with `normMode` set to [`cudnnLossNormalizationMode_t::CUDNN_LOSS_NORMALIZATION_NONE`] and the `gradMode` set to [`cudnnNanPropagation_t::CUDNN_NOT_PROPAGATE_NAN`], then it is the same as [`cudnnSetCTCLossDescriptor`], meaning.
    ///
    /// # Parameters
    ///
    /// - `ctcLossDesc`: Output. CTC loss descriptor to be set.
    /// - `compType`: Input. Compute type for this CTC loss function.
    /// - `normMode`: Input. Input normalization type for this CTC loss function. For more information, refer to [`cudnnLossNormalizationMode_t`].
    /// - `gradMode`: Input. `NaN` propagation type for this CTC loss function. For `L` the sequence length, `R` the number of repeated letters in the sequence, and `T` the length of sequential data, the following applies: when a sample with `L+R > T` is encountered during the gradient calculation, if `gradMode` is set to [`cudnnNanPropagation_t::CUDNN_PROPAGATE_NAN`] (refer to [`cudnnNanPropagation_t`]), then the CTC loss function does not write to the gradient buffer for that sample. Instead, the current values, even not finite, are retained. If `gradMode` is set to [`cudnnNanPropagation_t::CUDNN_NOT_PROPAGATE_NAN`], then the gradient for that sample is set to zero. This guarantees a finite gradient.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the input parameters passed is invalid.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The function returned successfully.
    #[deprecated]
    pub fn cudnnSetCTCLossDescriptorEx(
        ctcLossDesc: cudnnCTCLossDescriptor_t,
        compType: cudnnDataType_t,
        normMode: cudnnLossNormalizationMode_t,
        gradMode: cudnnNanPropagation_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// Many CTC API functions are updated in v8 to support CUDA graphs. In order to do so, a new parameter is needed, `maxLabelLength`. Now that label and input data are assumed to be in GPU memory, this information is not otherwise readily available.
    ///
    /// # Parameters
    ///
    /// - `ctcLossDesc`: Output. CTC loss descriptor to be set.
    /// - `compType`: Input. Compute type for this CTC loss function.
    /// - `normMode`: Input. Input normalization type for this CTC loss function. For more information, refer to [`cudnnLossNormalizationMode_t`].
    /// - `gradMode`: Input. `NaN` propagation type for this CTC loss function. For `L` the sequence length, `R` the number of repeated letters in the sequence, and `T` the length of sequential data, the following applies: when a sample with `L+R > T` is encountered during the gradient calculation, if `gradMode` is set to [`cudnnNanPropagation_t::CUDNN_PROPAGATE_NAN`] (refer to [`cudnnNanPropagation_t`]), then the CTC loss function does not write to the gradient buffer for that sample. Instead, the current values, even not finite, are retained. If `gradMode` is set to [`cudnnNanPropagation_t::CUDNN_NOT_PROPAGATE_NAN`], then the gradient for that sample is set to zero. This guarantees a finite gradient.
    /// - `maxLabelLength`: Input. The maximum label length from the labels data.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the input parameters passed is invalid.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The function returned successfully.
    #[deprecated]
    pub fn cudnnSetCTCLossDescriptor_v8(
        ctcLossDesc: cudnnCTCLossDescriptor_t,
        compType: cudnnDataType_t,
        normMode: cudnnLossNormalizationMode_t,
        gradMode: cudnnNanPropagation_t,
        maxLabelLength: ::core::ffi::c_int,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function sets a CTC loss function descriptor.
    ///
    /// # Parameters
    ///
    /// - `ctcLossDesc`: Output. CTC loss descriptor to be set.
    /// - `compType`: Input. Compute type for this CTC loss function.
    /// - `normMode`: Input. Input normalization type for this CTC loss function. For more information, refer to [`cudnnLossNormalizationMode_t`].
    /// - `ctcGradMode`: Behavior for out of boundary (OOB) samples. OOB samples are samples where L+R > T is encountered during the gradient calculation.
    ///
    /// * If `ctcGradMode` is set to [`cudnnCTCGradMode_t::CUDNN_CTC_SKIP_OOB_GRADIENTS`], then the CTC loss function does not write to the gradient buffer for that sample. Instead, the current values, even not finite, are retained.
    /// * If `ctcGradMode` is set to [`cudnnCTCGradMode_t::CUDNN_CTC_ZERO_OOB_GRADIENTS`], then the gradient for that sample is set to zero. This guarantees a finite gradient.
    /// - `maxLabelLength`: Input. The maximum label length from the labels data.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the input parameters passed is invalid.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The function returned successfully.
    pub fn cudnnSetCTCLossDescriptor_v9(
        ctcLossDesc: cudnnCTCLossDescriptor_t,
        compType: cudnnDataType_t,
        normMode: cudnnLossNormalizationMode_t,
        ctcGradMode: cudnnCTCGradMode_t,
        maxLabelLength: ::core::ffi::c_int,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function returns the configuration of the passed CTC loss function descriptor.
    ///
    /// # Parameters
    ///
    /// - `ctcLossDesc`: Input. CTC loss function descriptor passed, from which to retrieve the configuration.
    /// - `compType`: Output. Compute type associated with this CTC loss function descriptor.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: Input `ctcLossDesc` descriptor passed is invalid.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The function returned successfully.
    #[deprecated]
    pub fn cudnnGetCTCLossDescriptor(
        ctcLossDesc: cudnnCTCLossDescriptor_t,
        compType: *mut cudnnDataType_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function returns the configuration of the passed CTC loss function descriptor.
    ///
    /// # Parameters
    ///
    /// - `ctcLossDesc`: Input. CTC loss function descriptor passed, from which to retrieve the configuration.
    /// - `compType`: Output. Compute type associated with this CTC loss function descriptor.
    /// - `normMode`: Output. Input normalization type for this CTC loss function descriptor. For more information, refer to [`cudnnLossNormalizationMode_t`].
    /// - `gradMode`: Output. NaN propagation type for this CTC loss function descriptor.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: Input `ctcLossDesc` descriptor passed is invalid.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The function returned successfully.
    #[deprecated]
    pub fn cudnnGetCTCLossDescriptorEx(
        ctcLossDesc: cudnnCTCLossDescriptor_t,
        compType: *mut cudnnDataType_t,
        normMode: *mut cudnnLossNormalizationMode_t,
        gradMode: *mut cudnnNanPropagation_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function returns the configuration of the passed CTC loss function descriptor.
    ///
    /// # Parameters
    ///
    /// - `ctcLossDesc`: Input. CTC loss function descriptor passed, from which to retrieve the configuration.
    /// - `compType`: Output. Compute type associated with this CTC loss function descriptor.
    /// - `normMode`: Output. Input normalization type for this CTC loss function descriptor. For more information, refer to [`cudnnLossNormalizationMode_t`].
    /// - `gradMode`: Output. NaN propagation type for this CTC loss function descriptor.
    /// - `maxLabelLength`: Output. The max label length for this CTC loss function descriptor.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: Input `ctcLossDesc` descriptor passed is invalid.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The function returned successfully.
    #[deprecated]
    pub fn cudnnGetCTCLossDescriptor_v8(
        ctcLossDesc: cudnnCTCLossDescriptor_t,
        compType: *mut cudnnDataType_t,
        normMode: *mut cudnnLossNormalizationMode_t,
        gradMode: *mut cudnnNanPropagation_t,
        maxLabelLength: *mut ::core::ffi::c_int,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function returns the configuration of the passed CTC loss function descriptor.
    ///
    /// # Parameters
    ///
    /// - `ctcLossDesc`: Input. CTC loss function descriptor passed, from which to retrieve the configuration.
    /// - `compType`: Output. Compute type associated with this CTC loss function descriptor.
    /// - `normMode`: Output. Input normalization type for this CTC loss function descriptor. For more information, refer to [`cudnnLossNormalizationMode_t`].
    /// - `ctcGradMode`: Output. The gradient mode for handling OOB samples for this CTC loss function descriptor. Refer to [`cudnnSetCTCLossDescriptor_v9`] for more information.
    /// - `maxLabelLength`: Output. The max label length for this CTC loss function descriptor.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: Input `ctcLossDesc` descriptor passed is invalid.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The function returned successfully.
    pub fn cudnnGetCTCLossDescriptor_v9(
        ctcLossDesc: cudnnCTCLossDescriptor_t,
        compType: *mut cudnnDataType_t,
        normMode: *mut cudnnLossNormalizationMode_t,
        ctcGradMode: *mut cudnnCTCGradMode_t,
        maxLabelLength: *mut ::core::ffi::c_int,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function destroys a CTC loss function descriptor object.
    ///
    /// # Parameters
    ///
    /// - `ctcLossDesc`: Input. CTC loss function descriptor to be destroyed.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The function returned successfully.
    pub fn cudnnDestroyCTCLossDescriptor(
        ctcLossDesc: cudnnCTCLossDescriptor_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function returns the CTC costs and gradients, given the probabilities and labels.
    ///
    /// This function can have an inconsistent interface depending on the [`cudnnLossNormalizationMode_t`] chosen (bound to the [`cudnnCTCLossDescriptor_t`] with [`cudnnSetCTCLossDescriptorEx`]). For the [`cudnnLossNormalizationMode_t::CUDNN_LOSS_NORMALIZATION_NONE`], this function has an inconsistent interface, for example, the probs input is probability normalized by softmax, but the gradients output is with respect to the unnormalized activation. However, for [`cudnnLossNormalizationMode_t::CUDNN_LOSS_NORMALIZATION_SOFTMAX`], the function has a consistent interface; all values are normalized by softmax.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN context. For more information, refer to [`cudnnHandle_t`].
    /// - `probsDesc`: Input. Handle to the previously initialized probabilities tensor descriptor. For more information, refer to [`cudnnTensorDescriptor_t`].
    /// - `probs`: Input. Pointer to a previously initialized probabilities tensor. These input probabilities are normalized by softmax.
    /// - `hostLabels`: Input. Pointer to a previously initialized labels list, in CPU memory.
    /// - `hostLabelLengths`: Input. Pointer to a previously initialized lengths list in CPU memory, to walk the above labels list.
    /// - `hostInputLengths`: Input. Pointer to a previously initialized list of the lengths of the timing steps in each batch, in CPU memory.
    /// - `costs`: Output. Pointer to the computed costs of CTC.
    /// - `gradientsDesc`: Input. Handle to a previously initialized gradient tensor descriptor.
    /// - `gradients`: Output. Pointer to the computed gradients of CTC. These computed gradient outputs are with respect to the unnormalized activation.
    /// - `algo`: Input. Enumerant that specifies the chosen CTC loss algorithm. For more information, refer to [`cudnnCTCLossAlgo_t`].
    /// - `ctcLossDesc`: Input. Handle to the previously initialized CTC loss descriptor. For more information, refer to [`cudnnCTCLossDescriptor_t`].
    /// - `workspace`: Input. Pointer to GPU memory of a workspace needed to be able to execute the specified algorithm.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * The dimensions of `probsDesc` do not match the dimensions of `gradientsDesc`.
    /// * The `inputLengths` do not agree with the first dimension of `probsDesc`.
    /// * The `workSpaceSizeInBytes` is not sufficient.
    /// * The `labelLengths` is greater than `255`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_EXECUTION_FAILED`]: The function failed to launch on the GPU.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: A compute or data type other than `FLOAT` was chosen, or an unknown algorithm type was chosen.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The query was successful.
    pub fn cudnnCTCLoss(
        handle: cudnnHandle_t,
        probsDesc: cudnnTensorDescriptor_t,
        probs: *const ::core::ffi::c_void,
        hostLabels: *const ::core::ffi::c_int,
        hostLabelLengths: *const ::core::ffi::c_int,
        hostInputLengths: *const ::core::ffi::c_int,
        costs: *mut ::core::ffi::c_void,
        gradientsDesc: cudnnTensorDescriptor_t,
        gradients: *mut ::core::ffi::c_void,
        algo: cudnnCTCLossAlgo_t,
        ctcLossDesc: cudnnCTCLossDescriptor_t,
        workspace: *mut ::core::ffi::c_void,
        workSpaceSizeInBytes: size_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function returns the CTC costs and gradients, given the probabilities and labels. Many CTC API functions were updated in version 8 with the `_v8` suffix to support CUDA graphs. Label and input data is now passed in GPU memory.
    ///
    /// This function can have an inconsistent interface depending on the [`cudnnLossNormalizationMode_t`] chosen (bound to the [`cudnnCTCLossDescriptor_t`] with [`cudnnSetCTCLossDescriptorEx`]). For the [`cudnnLossNormalizationMode_t::CUDNN_LOSS_NORMALIZATION_NONE`], this function has an inconsistent interface, for example, the probs input is probability normalized by softmax, but the gradients output is with respect to the unnormalized activation. However, for [`cudnnLossNormalizationMode_t::CUDNN_LOSS_NORMALIZATION_SOFTMAX`], the function has a consistent interface; all values are normalized by softmax.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN context. For more information, refer to [`cudnnHandle_t`].
    /// - `algo`: Input. Enumerant that specifies the chosen CTC loss algorithm. For more information, refer to [`cudnnCTCLossAlgo_t`].
    /// - `ctcLossDesc`: Input. Handle to the previously initialized CTC loss descriptor. For more information, refer to [`cudnnCTCLossDescriptor_t`].
    /// - `probsDesc`: Input. Handle to the previously initialized probabilities tensor descriptor. For more information, refer to [`cudnnTensorDescriptor_t`].
    /// - `probs`: Input. Pointer to a previously initialized probabilities tensor. These input probabilities are normalized by softmax.
    /// - `labels`: Input. Pointer to a previously initialized labels list, in GPU memory.
    /// - `labelLengths`: Input. Pointer to a previously initialized lengths list in GPU memory, to walk the above labels list.
    /// - `inputLengths`: Input. Pointer to a previously initialized list of the lengths of the timing steps in each batch, in GPU memory.
    /// - `costs`: Output. Pointer to the computed costs of CTC.
    /// - `gradientsDesc`: Input. Handle to a previously initialized gradient tensor descriptor.
    /// - `gradients`: Output. Pointer to the computed gradients of CTC. These computed gradient outputs are with respect to the unnormalized activation.
    /// - `workspace`: Input. Pointer to GPU memory of a workspace needed to be able to execute the specified algorithm.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * The dimensions of `probsDesc` do not match the dimensions of `gradientsDesc`.
    /// * The `workSpaceSizeInBytes` is not sufficient.
    /// - [`cudnnStatus_t::CUDNN_STATUS_EXECUTION_FAILED`]: The function failed to launch on the GPU.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: A compute or data type other than `FLOAT` was chosen, or an unknown algorithm type was chosen.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The query was successful.
    pub fn cudnnCTCLoss_v8(
        handle: cudnnHandle_t,
        algo: cudnnCTCLossAlgo_t,
        ctcLossDesc: cudnnCTCLossDescriptor_t,
        probsDesc: cudnnTensorDescriptor_t,
        probs: *const ::core::ffi::c_void,
        labels: *const ::core::ffi::c_int,
        labelLengths: *const ::core::ffi::c_int,
        inputLengths: *const ::core::ffi::c_int,
        costs: *mut ::core::ffi::c_void,
        gradientsDesc: cudnnTensorDescriptor_t,
        gradients: *mut ::core::ffi::c_void,
        workSpaceSizeInBytes: size_t,
        workspace: *mut ::core::ffi::c_void,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function returns the amount of GPU memory workspace the user needs to allocate to be able to call [`cudnnCTCLoss`] with the specified algorithm. The workspace allocated will then be passed to the routine [`cudnnCTCLoss`].
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN context.
    /// - `probsDesc`: Input. Handle to the previously initialized probabilities tensor descriptor.
    /// - `gradientsDesc`: Input. Handle to a previously initialized gradient tensor descriptor.
    /// - `labels`: Input. Pointer to a previously initialized labels list.
    /// - `labelLengths`: Input. Pointer to a previously initialized lengths list, to walk the above labels list.
    /// - `inputLengths`: Input. Pointer to a previously initialized list of the lengths of the timing steps in each batch.
    /// - `algo`: Input. Enumerant that specifies the chosen CTC loss algorithm.
    /// - `ctcLossDesc`: Input. Handle to the previously initialized CTC loss descriptor.
    /// - `sizeInBytes`: Output. Amount of GPU memory needed as workspace to be able to execute the CTC loss computation with the specified `algo`.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * The dimensions of `probsDesc` do not match the dimensions of `gradientsDesc`
    /// * The `inputLengths` do not agree with the first dimension of `probsDesc`
    /// * The `workSpaceSizeInBytes` is not sufficient
    /// * The `labelLengths` is greater than `256`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: A compute or data type other than `FLOAT` was chosen, or an unknown algorithm type was chosen.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The query was successful.
    pub fn cudnnGetCTCLossWorkspaceSize(
        handle: cudnnHandle_t,
        probsDesc: cudnnTensorDescriptor_t,
        gradientsDesc: cudnnTensorDescriptor_t,
        labels: *const ::core::ffi::c_int,
        labelLengths: *const ::core::ffi::c_int,
        inputLengths: *const ::core::ffi::c_int,
        algo: cudnnCTCLossAlgo_t,
        ctcLossDesc: cudnnCTCLossDescriptor_t,
        sizeInBytes: *mut size_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function returns the amount of GPU memory workspace the user needs to allocate to be able to call [`cudnnCTCLoss_v8`] with the specified algorithm. The workspace allocated will then be passed to the routine [`cudnnCTCLoss_v8`].
    ///
    /// [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]
    /// - A compute or data type other than `FLOAT` was chosen, or an unknown algorithm type was chosen.
    /// - For the deterministic CTC loss algorithm, the `maxLabelLength` in `ctcLossDesc` is greater than or equal to 256.
    /// - For the nondeterministic CTC loss algorithm, the `maxLabelLength` in `ctcLossDesc` is greater than or equal to 2048.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN context.
    /// - `algo`: Input. Enumerant that specifies the chosen CTC loss algorithm.
    /// - `ctcLossDesc`: Input. Handle to the previously initialized CTC loss descriptor.
    /// - `probsDesc`: Input. Handle to the previously initialized probabilities tensor descriptor.
    /// - `gradientsDesc`: Input. Handle to a previously initialized gradient tensor descriptor.
    /// - `sizeInBytes`: Output. Amount of GPU memory needed as workspace to be able to execute the CTC loss computation with the specified `algo`.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * The dimensions of `probsDesc` do not match the dimensions of `gradientsDesc`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The query was successful.
    pub fn cudnnGetCTCLossWorkspaceSize_v8(
        handle: cudnnHandle_t,
        algo: cudnnCTCLossAlgo_t,
        ctcLossDesc: cudnnCTCLossDescriptor_t,
        probsDesc: cudnnTensorDescriptor_t,
        gradientsDesc: cudnnTensorDescriptor_t,
        sizeInBytes: *mut size_t,
    ) -> cudnnStatus_t;
}
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct cudnnConvolutionStruct {
    _unused: [u8; 0],
}
/// [`cudnnConvolutionDescriptor_t`] is a pointer to an opaque structure holding the description of a convolution operation. [`cudnnCreateConvolutionDescriptor`] is used to create one instance, and [`cudnnSetConvolutionNdDescriptor`] or [`cudnnSetConvolution2dDescriptor`] must be used to initialize this instance.
#[deprecated]
pub type cudnnConvolutionDescriptor_t = *mut cudnnConvolutionStruct;
#[repr(C)]
#[derive(Debug, Copy, Clone, PartialOrd, PartialEq)]
pub struct cudnnConvolutionFwdAlgoPerfStruct {
    pub algo: cudnnConvolutionFwdAlgo_t,
    pub status: cudnnStatus_t,
    pub time: f32,
    pub memory: size_t,
    pub determinism: cudnnDeterminism_t,
    pub mathType: cudnnMathType_t,
    pub reserved: [::core::ffi::c_int; 3usize],
}
impl Default for cudnnConvolutionFwdAlgoPerfStruct {
    fn default() -> Self {
        let mut s = ::core::mem::MaybeUninit::<Self>::uninit();
        unsafe {
            ::core::ptr::write_bytes(s.as_mut_ptr(), 0, 1);
            s.assume_init()
        }
    }
}
/// [`cudnnConvolutionFwdAlgoPerf_t`] is a structure containing performance results returned by [`cudnnFindConvolutionForwardAlgorithm`] or heuristic results returned by [`cudnnGetConvolutionForwardAlgorithm_v7`].
///
/// **Data Members**.
#[deprecated]
pub type cudnnConvolutionFwdAlgoPerf_t = cudnnConvolutionFwdAlgoPerfStruct;
unsafe extern "C" {
    /// This function creates a convolution descriptor object by allocating the memory needed to hold its opaque structure. For more information, refer to [`cudnnConvolutionDescriptor_t`].
    ///
    /// # Return value
    ///
    /// - `CUDNN_STATUS_ALLOC_FAILED`: The resources could not be allocated.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The object was created successfully.
    #[deprecated]
    pub fn cudnnCreateConvolutionDescriptor(
        convDesc: *mut cudnnConvolutionDescriptor_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function destroys a previously created convolution descriptor object.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The descriptor was destroyed successfully.
    #[deprecated]
    pub fn cudnnDestroyConvolutionDescriptor(
        convDesc: cudnnConvolutionDescriptor_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function allows the user to specify whether or not the use of tensor op is permitted in the library routines associated with a given convolution descriptor.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: Either an invalid convolution descriptor was provided or an invalid math type was specified.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The math type was set successfully.
    #[deprecated]
    pub fn cudnnSetConvolutionMathType(
        convDesc: cudnnConvolutionDescriptor_t,
        mathType: cudnnMathType_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function returns the math type specified in a given convolution descriptor.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: An invalid convolution descriptor was provided.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The math type was returned successfully.
    #[deprecated]
    pub fn cudnnGetConvolutionMathType(
        convDesc: cudnnConvolutionDescriptor_t,
        mathType: *mut cudnnMathType_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function allows the user to specify the number of groups to be used in the associated convolution.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: An invalid convolution descriptor was provided.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The group count was set successfully.
    #[deprecated]
    pub fn cudnnSetConvolutionGroupCount(
        convDesc: cudnnConvolutionDescriptor_t,
        groupCount: ::core::ffi::c_int,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function returns the group count specified in the given convolution descriptor.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: An invalid convolution descriptor was provided.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The group count was returned successfully.
    #[deprecated]
    pub fn cudnnGetConvolutionGroupCount(
        convDesc: cudnnConvolutionDescriptor_t,
        groupCount: *mut ::core::ffi::c_int,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function sets the convolution reorder type for the given convolution descriptor.
    ///
    /// # Parameters
    ///
    /// - `convDesc`: Input. The convolution descriptor for which the reorder type should be set.
    /// - `reorderType`: Input. Set the reorder type to this value. For more information, refer to [`cudnnReorderType_t`].
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: The reorder type supplied is not supported.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: Reorder type is set successfully.
    #[deprecated]
    pub fn cudnnSetConvolutionReorderType(
        convDesc: cudnnConvolutionDescriptor_t,
        reorderType: cudnnReorderType_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function retrieves the convolution reorder type from the given convolution descriptor.
    ///
    /// # Parameters
    ///
    /// - `convDesc`: Input. The convolution descriptor from which the reorder type should be retrieved.
    /// - `reorderType`: Output. The retrieved reorder type. For more information, refer to [`cudnnReorderType_t`].
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: One of the inputs to this function is not valid.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The reorder type is retrieved successfully.
    #[deprecated]
    pub fn cudnnGetConvolutionReorderType(
        convDesc: cudnnConvolutionDescriptor_t,
        reorderType: *mut cudnnReorderType_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function initializes a previously created convolution descriptor object into a 2D correlation. This function assumes that the tensor and filter descriptors correspond to the forward convolution path and checks if their settings are valid. That same convolution descriptor can be reused in the backward path provided it corresponds to the same layer.
    ///
    /// # Parameters
    ///
    /// - `convDesc`: Input/output. Handle to a previously created convolution descriptor.
    /// - `pad_h`: Input. Zero-padding height: number of rows of zeros implicitly concatenated onto the top and onto the bottom of input images.
    /// - `pad_w`: Input. Zero-padding width: number of columns of zeros implicitly concatenated onto the left and onto the right of input images.
    /// - `u`: Input. Vertical filter stride.
    /// - `v`: Input. Horizontal filter stride.
    /// - `dilation_h`: Input. Filter height dilation.
    /// - `dilation_w`: Input. Filter width dilation.
    /// - `mode`: Input. Selects between [`cudnnConvolutionMode_t::CUDNN_CONVOLUTION`] and [`cudnnConvolutionMode_t::CUDNN_CROSS_CORRELATION`].
    /// - `computeType`: Input. compute precision.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * The descriptor `convDesc` is `NIL`.
    /// * One of the parameters `pad_h`, `pad_w` is strictly negative.
    /// * One of the parameters `u`, `v` is negative or zero.
    /// * One of the parameters `dilation_h`, `dilation_w` is negative or zero.
    /// * The parameter mode has an invalid enumerant value.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The object was set successfully.
    #[deprecated]
    pub fn cudnnSetConvolution2dDescriptor(
        convDesc: cudnnConvolutionDescriptor_t,
        pad_h: ::core::ffi::c_int,
        pad_w: ::core::ffi::c_int,
        u: ::core::ffi::c_int,
        v: ::core::ffi::c_int,
        dilation_h: ::core::ffi::c_int,
        dilation_w: ::core::ffi::c_int,
        mode: cudnnConvolutionMode_t,
        computeType: cudnnDataType_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function queries a previously initialized 2D convolution descriptor object.
    ///
    /// # Parameters
    ///
    /// - `convDesc`: Input/output. Handle to a previously created convolution descriptor.
    /// - `pad_h`: Output. Zero-padding height: number of rows of zeros implicitly concatenated onto the top and onto the bottom of input images.
    /// - `pad_w`: Output. Zero-padding width: number of columns of zeros implicitly concatenated onto the left and onto the right of input images.
    /// - `u`: Output. Vertical filter stride.
    /// - `v`: Output. Horizontal filter stride.
    /// - `dilation_h`: Output. Filter height dilation.
    /// - `dilation_w`: Output. Filter width dilation.
    /// - `mode`: Output. Convolution mode.
    /// - `computeType`: Output. Compute precision.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: The parameter `convDesc` is `NIL`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The operation was successful.
    #[deprecated]
    pub fn cudnnGetConvolution2dDescriptor(
        convDesc: cudnnConvolutionDescriptor_t,
        pad_h: *mut ::core::ffi::c_int,
        pad_w: *mut ::core::ffi::c_int,
        u: *mut ::core::ffi::c_int,
        v: *mut ::core::ffi::c_int,
        dilation_h: *mut ::core::ffi::c_int,
        dilation_w: *mut ::core::ffi::c_int,
        mode: *mut cudnnConvolutionMode_t,
        computeType: *mut cudnnDataType_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function initializes a previously created generic convolution descriptor object into a `Nd` correlation. That same convolution descriptor can be reused in the backward path provided it corresponds to the same layer. The convolution computation will be done in the specified `dataType`, which can be potentially different from the input/output tensors.
    ///
    /// # Parameters
    ///
    /// - `convDesc`: Input/output. Handle to a previously created convolution descriptor.
    /// - `arrayLength`: Input. Dimension of the convolution.
    /// - `padA`: Input. Array of dimension `arrayLength` containing the zero-padding size for each dimension. For every dimension, the padding represents the number of extra zeros implicitly concatenated at the start and at the end of every element of that dimension.
    /// - `filterStrideA`: Input. Array of dimension `arrayLength` containing the filter stride for each dimension. For every dimension, the filter stride represents the number of elements to slide to reach the next start of the filtering window of the next point.
    /// - `dilationA`: Input. Array of dimension `arrayLength` containing the dilation factor for each dimension.
    /// - `mode`: Input. Selects between [`cudnnConvolutionMode_t::CUDNN_CONVOLUTION`] and [`cudnnConvolutionMode_t::CUDNN_CROSS_CORRELATION`].
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * The descriptor `convDesc` is `NIL`.
    /// * The `arrayLengthRequest` is negative.
    /// * The enumerant mode has an invalid value.
    /// * The enumerant `datatype` has an invalid value.
    /// * One of the elements of `padA` is strictly negative.
    /// * One of the elements of `strideA` is negative or zero.
    /// * One of the elements of `dilationA` is negative or zero.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: At least one of the following conditions are met:
    ///
    /// * The `arrayLengthRequest` is greater than [`CUDNN_DIM_MAX`].
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The object was set successfully.
    #[deprecated]
    pub fn cudnnSetConvolutionNdDescriptor(
        convDesc: cudnnConvolutionDescriptor_t,
        arrayLength: ::core::ffi::c_int,
        padA: *const ::core::ffi::c_int,
        filterStrideA: *const ::core::ffi::c_int,
        dilationA: *const ::core::ffi::c_int,
        mode: cudnnConvolutionMode_t,
        computeType: cudnnDataType_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function queries a previously initialized convolution descriptor object.
    ///
    /// # Parameters
    ///
    /// - `convDesc`: Input/output. Handle to a previously created convolution descriptor.
    /// - `arrayLengthRequested`: Input. Dimension of the expected convolution descriptor. It is also the minimum size of the arrays `padA`, `filterStrideA`, and `dilationA` in order to be able to hold the results.
    /// - `arrayLength`: Output. Actual dimension of the convolution descriptor.
    /// - `padA`: Output. Array of dimension of at least `arrayLengthRequested` that will be filled with the padding parameters from the provided convolution descriptor.
    /// - `dilationA`: Output. Array of dimension of at least `arrayLengthRequested` that will be filled with the dilation parameters from the provided convolution descriptor.
    /// - `mode`: Output. Convolution mode of the provided descriptor.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * The descriptor `convDesc` is `NIL`.
    /// * The `arrayLengthRequest` is negative.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: The `arrayLengthRequested` is greater than `CUDNN_DIM_MAX-2`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The query was successful.
    #[deprecated]
    pub fn cudnnGetConvolutionNdDescriptor(
        convDesc: cudnnConvolutionDescriptor_t,
        arrayLengthRequested: ::core::ffi::c_int,
        arrayLength: *mut ::core::ffi::c_int,
        padA: *mut ::core::ffi::c_int,
        strideA: *mut ::core::ffi::c_int,
        dilationA: *mut ::core::ffi::c_int,
        mode: *mut cudnnConvolutionMode_t,
        computeType: *mut cudnnDataType_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function returns the dimensions of the resulting 4D tensor of a 2D convolution, given the convolution descriptor, the input tensor descriptor and the filter descriptor. This function can help to set up the output tensor and allocate the proper amount of memory prior to launching the actual convolution.
    ///
    /// Each dimension `h` and `w` of the output images is computed as follows.
    ///
    /// # Parameters
    ///
    /// - `convDesc`: Input. Handle to a previously created convolution descriptor.
    /// - `inputTensorDesc`: Input. Handle to a previously initialized tensor descriptor.
    /// - `filterDesc`: Input. Handle to a previously initialized filter descriptor.
    /// - `n`: Output. Number of output images.
    /// - `c`: Output. Number of output feature maps per image.
    /// - `h`: Output. Height of each output feature map.
    /// - `w`: Output. Width of each output feature map.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: One or more of the descriptors has not been created correctly or there is a mismatch between the feature maps of `inputTensorDesc` and `filterDesc`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The object was set successfully.
    #[deprecated]
    pub fn cudnnGetConvolution2dForwardOutputDim(
        convDesc: cudnnConvolutionDescriptor_t,
        inputTensorDesc: cudnnTensorDescriptor_t,
        filterDesc: cudnnFilterDescriptor_t,
        n: *mut ::core::ffi::c_int,
        c: *mut ::core::ffi::c_int,
        h: *mut ::core::ffi::c_int,
        w: *mut ::core::ffi::c_int,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function returns the dimensions of the resulting `Nd` tensor of a `nbDims-2-D` convolution, given the convolution descriptor, the input tensor descriptor and the filter descriptor This function can help to setup the output tensor and allocate the proper amount of memory prior to launch the actual convolution.
    ///
    /// Each dimension of the `(nbDims-2)-D` images of the output tensor is computed as follows:
    ///
    /// The dimensions provided by this routine must be strictly respected when calling [`cudnnConvolutionForward`] or [`cudnnConvolutionBackwardBias`]. Providing a smaller or larger output tensor is not supported by the convolution routines.
    ///
    /// # Parameters
    ///
    /// - `convDesc`: Input. Handle to a previously created convolution descriptor.
    /// - `inputTensorDesc`: Input. Handle to a previously initialized tensor descriptor.
    /// - `filterDesc`: Input. Handle to a previously initialized filter descriptor.
    /// - `nbDims`: Input. Dimension of the output tensor.
    /// - `tensorOuputDimA`: Output. Array of dimensions `nbDims` that contains on exit of this routine the sizes of the output tensor.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * One of the parameters `convDesc`, `inputTensorDesc`, and `filterDesc` is `NIL`.
    /// * The dimension of the filter descriptor `filterDesc` is different from the dimension of input tensor descriptor `inputTensorDesc`.
    /// * The dimension of the convolution descriptor is different from the dimension of input tensor descriptor `inputTensorDesc-2`.
    /// * The features map of the filter descriptor `filterDesc` is different from the one of input tensor descriptor `inputTensorDesc`.
    /// * The size of the dilated filter `filterDesc` is larger than the padded sizes of the input tensor.
    /// * The dimension `nbDims` of the output array is negative or greater than the dimension of input tensor descriptor `inputTensorDesc`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The routine exited successfully.
    #[deprecated]
    pub fn cudnnGetConvolutionNdForwardOutputDim(
        convDesc: cudnnConvolutionDescriptor_t,
        inputTensorDesc: cudnnTensorDescriptor_t,
        filterDesc: cudnnFilterDescriptor_t,
        nbDims: ::core::ffi::c_int,
        tensorOuputDimA: *mut ::core::ffi::c_int,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function returns the maximum number of algorithms which can be returned from [`cudnnFindConvolutionForwardAlgorithm`] and [`cudnnGetConvolutionForwardAlgorithm_v7`]. This is the sum of all algorithms plus the sum of all algorithms with Tensor Core operations supported for the current device.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN context.
    /// - `count`: Output. The resulting maximum number of algorithms.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: The provided handle is not allocated properly.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The function was successful.
    #[deprecated]
    pub fn cudnnGetConvolutionForwardAlgorithmMaxCount(
        handle: cudnnHandle_t,
        count: *mut ::core::ffi::c_int,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function serves as a heuristic for obtaining the best suited algorithm for [`cudnnConvolutionForward`] for the given layer specifications. This function will return all algorithms (including [`cudnnMathType_t::CUDNN_TENSOR_OP_MATH`] and [`cudnnMathType_t::CUDNN_DEFAULT_MATH`] versions of algorithms where [`cudnnMathType_t::CUDNN_TENSOR_OP_MATH`] may be available) sorted by expected (based on internal heuristic) relative performance with the fastest being index `0` of `perfResults`. For an exhaustive search for the fastest algorithm, use [`cudnnFindConvolutionForwardAlgorithm`]. The total number of resulting algorithms can be queried through the `returnedAlgoCount` variable.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN context.
    /// - `convDesc`: Input. Previously initialized convolution descriptor.
    /// - `requestedAlgoCount`: Input. The maximum number of elements to be stored in `perfResults`.
    /// - `returnedAlgoCount`: Output. The number of output elements stored in `perfResults`.
    /// - `perfResults`: Output. A user-allocated array to store performance metrics sorted ascending by compute time.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * One of the parameters `handle`, `xDesc`, `wDesc`, `convDesc`, `yDesc`, `perfResults`, or `returnedAlgoCount` is `NULL`.
    /// * Either `yDesc` or `wDesc` have different dimensions from `xDesc`.
    /// * The data types of tensors `xDesc`, `yDesc`, or `wDesc` are not all the same.
    /// * The number of feature maps in `xDesc` and `wDesc` differs.
    /// * The tensor `xDesc` has a dimension smaller than `3`.
    /// * `requestedAlgoCount` is less than or equal to `0`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The query was successful.
    #[deprecated]
    pub fn cudnnGetConvolutionForwardAlgorithm_v7(
        handle: cudnnHandle_t,
        srcDesc: cudnnTensorDescriptor_t,
        filterDesc: cudnnFilterDescriptor_t,
        convDesc: cudnnConvolutionDescriptor_t,
        destDesc: cudnnTensorDescriptor_t,
        requestedAlgoCount: ::core::ffi::c_int,
        returnedAlgoCount: *mut ::core::ffi::c_int,
        perfResults: *mut cudnnConvolutionFwdAlgoPerf_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function attempts all algorithms available for [`cudnnConvolutionForward`]. It will attempt both the provided `convDesc mathType` and [`cudnnMathType_t::CUDNN_DEFAULT_MATH`] (assuming the two differ).
    ///
    /// Algorithms without the [`cudnnMathType_t::CUDNN_TENSOR_OP_MATH`] availability will only be tried with [`cudnnMathType_t::CUDNN_DEFAULT_MATH`], and returned as such.
    ///
    /// Memory is allocated via `cudaMalloc()`. The performance metrics are returned in the user-allocated array of [`cudnnConvolutionFwdAlgoPerf_t`]. These metrics are written in a sorted fashion where the first element has the lowest compute time. The total number of resulting algorithms can be queried through the API [`cudnnGetConvolutionForwardAlgorithmMaxCount`].
    ///
    /// [`cudnnStatus_t::CUDNN_STATUS_INTERNAL_ERROR`]
    /// At least one of the following conditions are met.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN context.
    /// - `xDesc`: Input. Handle to the previously initialized input tensor descriptor.
    /// - `wDesc`: Input. Handle to a previously initialized filter descriptor.
    /// - `convDesc`: Input. Previously initialized convolution descriptor.
    /// - `yDesc`: Input. Handle to the previously initialized output tensor descriptor.
    /// - `requestedAlgoCount`: Input. The maximum number of elements to be stored in `perfResults`.
    /// - `returnedAlgoCount`: Output. The number of output elements stored in `perfResults`.
    /// - `perfResults`: Output. A user-allocated array to store performance metrics sorted ascending by compute time.
    ///
    /// # Return value
    ///
    /// - `CUDNN_STATUS_ALLOC_FAILED`: This function was unable to allocate memory to store sample input, filters, and output.
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * `handle` is not allocated properly
    /// * `xDesc`, `dyDesc`, or `dwDesc` are not allocated properly
    /// * `xDesc`, `dyDesc`, or `dwDesc` has fewer than `1` dimension
    /// * Either `returnedCount` or `perfResults` is `NIL`
    /// * `requestedCount` is less than `1`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The query was successful.
    #[deprecated]
    pub fn cudnnFindConvolutionForwardAlgorithm(
        handle: cudnnHandle_t,
        xDesc: cudnnTensorDescriptor_t,
        wDesc: cudnnFilterDescriptor_t,
        convDesc: cudnnConvolutionDescriptor_t,
        yDesc: cudnnTensorDescriptor_t,
        requestedAlgoCount: ::core::ffi::c_int,
        returnedAlgoCount: *mut ::core::ffi::c_int,
        perfResults: *mut cudnnConvolutionFwdAlgoPerf_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function attempts all algorithms available for [`cudnnConvolutionForward`]. It will attempt both the provided `convDesc mathType` and [`cudnnMathType_t::CUDNN_DEFAULT_MATH`] (assuming the two differ).
    ///
    /// Algorithms without the [`cudnnMathType_t::CUDNN_TENSOR_OP_MATH`] availability will only be tried with [`cudnnMathType_t::CUDNN_DEFAULT_MATH`], and returned as such.
    ///
    /// Memory is allocated via `cudaMalloc()`. The performance metrics are returned in the user-allocated array of [`cudnnConvolutionFwdAlgoPerf_t`]. These metrics are written in a sorted fashion where the first element has the lowest compute time. The total number of resulting algorithms can be queried through the API [`cudnnGetConvolutionForwardAlgorithmMaxCount`].
    ///
    /// [`cudnnStatus_t::CUDNN_STATUS_INTERNAL_ERROR`]
    /// At least one of the following conditions are met.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN context.
    /// - `xDesc`: Input. Handle to the previously initialized input tensor descriptor.
    /// - `x`: Input. Data pointer to GPU memory associated with the tensor descriptor `xDesc`.
    /// - `wDesc`: Input. Handle to a previously initialized filter descriptor.
    /// - `w`: Input. Data pointer to GPU memory associated with the filter descriptor `wDesc`.
    /// - `convDesc`: Input. Previously initialized convolution descriptor.
    /// - `yDesc`: Input. Handle to the previously initialized output tensor descriptor.
    /// - `y`: Input/output. Data pointer to GPU memory associated with the tensor descriptor `yDesc`. The content of this tensor will be overwritten with arbitrary values.
    /// - `requestedAlgoCount`: Input. The maximum number of elements to be stored in `perfResults`.
    /// - `returnedAlgoCount`: Output. The number of output elements stored in `perfResults`.
    /// - `perfResults`: Output. A user-allocated array to store performance metrics sorted ascending by compute time.
    /// - `workSpace`: Input. Data pointer to GPU memory is a necessary workspace for some algorithms. The size of this workspace will determine the availability of algorithms. A `NIL` pointer is considered a `workSpace` of `0` bytes.
    /// - `workSpaceSizeInBytes`: Input. Specifies the size in bytes of the provided `workSpace`.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * `handle` is not allocated properly
    /// * `xDesc`, `dyDesc`, or `dwDesc` are not allocated properly
    /// * `xDesc`, `dyDesc`, or `dwDesc` has fewer than `1` dimension
    /// * `x`, `w`, or `y` is `NIL`
    /// * Either `returnedCount` or `perfResults` is `NIL`
    /// * `requestedCount` is less than `1`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The query was successful.
    #[deprecated]
    pub fn cudnnFindConvolutionForwardAlgorithmEx(
        handle: cudnnHandle_t,
        xDesc: cudnnTensorDescriptor_t,
        x: *const ::core::ffi::c_void,
        wDesc: cudnnFilterDescriptor_t,
        w: *const ::core::ffi::c_void,
        convDesc: cudnnConvolutionDescriptor_t,
        yDesc: cudnnTensorDescriptor_t,
        y: *mut ::core::ffi::c_void,
        requestedAlgoCount: ::core::ffi::c_int,
        returnedAlgoCount: *mut ::core::ffi::c_int,
        perfResults: *mut cudnnConvolutionFwdAlgoPerf_t,
        workSpace: *mut ::core::ffi::c_void,
        workSpaceSizeInBytes: size_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function constructs the A matrix necessary to perform a forward pass of GEMM convolution.
    ///
    /// This A matrix has a height of `batch_size`y_height`y_width` and width of `input_channels`filter_height`filter_width`, where:
    ///
    /// The A matrix is stored in format HW fully-packed in GPU memory.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN context.
    /// - `convDesc`: Input. Handle to a previously initialized convolution descriptor.
    /// - `colBuffer`: Output. Data pointer to GPU memory storing the output matrix.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: `srcData` or `colBuffer` is `NULL`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_EXECUTION_FAILED`]: The CUDA kernel execution was unsuccessful.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: Any of `srcDesc`, `filterDesc`, `convDesc` has `dataType` of [`cudnnDataType_t::CUDNN_DATA_INT8`], [`cudnnDataType_t::CUDNN_DATA_INT8x4`], [`cudnnDataType_t::CUDNN_DATA_INT8`], or [`cudnnDataType_t::CUDNN_DATA_INT8x4`] `convDesc` has `groupCount` larger than `1`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The output data array is successfully generated.
    #[deprecated]
    pub fn cudnnIm2Col(
        handle: cudnnHandle_t,
        xDesc: cudnnTensorDescriptor_t,
        x: *const ::core::ffi::c_void,
        wDesc: cudnnFilterDescriptor_t,
        convDesc: cudnnConvolutionDescriptor_t,
        colBuffer: *mut ::core::ffi::c_void,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function reorders the filter and bias values for tensors with data type [`cudnnDataType_t::CUDNN_DATA_INT8x32`] and tensor format [`cudnnTensorFormat_t::CUDNN_TENSOR_NCHW_VECT_C`]. It can be used to enhance the inference time by separating the reordering operation from convolution.
    ///
    /// Filter and bias tensors with data type [`cudnnDataType_t::CUDNN_DATA_INT8x32`] (also implying tensor format [`cudnnTensorFormat_t::CUDNN_TENSOR_NCHW_VECT_C`]) requires permutation of output channel axes in order to take advantage of the Tensor Core IMMA instruction. This is done in every [`cudnnConvolutionForward`] and [`cudnnConvolutionBiasActivationForward`] call when the reorder type attribute of the convolution descriptor is set to [`cudnnReorderType_t::CUDNN_DEFAULT_REORDER`]. Users can avoid the repeated reordering kernel call by first using this call to reorder the filter and bias tensor and call the convolution forward APIs with reorder type set to [`cudnnReorderType_t::CUDNN_NO_REORDER`].
    ///
    /// For example, convolutions in a neural network of multiple layers can require reordering of kernels at every layer, which can take up a significant fraction of the total inference time. Using this function, the reordering can be done one time on the filter and bias data. This is followed by the convolution operations at the multiple layers, which enhance the inference time.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN context.
    /// - `filterDesc`: Input. Descriptor for the kernel dataset.
    /// - `reorderType`: Input. Setting to either perform reordering or not. For more information, refer to [`cudnnReorderType_t`].
    /// - `filterData`: Input. Pointer to the filter (kernel) data location in the device memory.
    /// - `reorderedFilterData`: Output. Pointer to the location in the device memory where the reordered filter data will be written to, by this function. This tensor has the same dimensions as `filterData`.
    /// - `reorderBias`: Input. If > 0, then reorders the `biasData` also. If &lt;= 0 then does not perform reordering operations on the `biasData`.
    /// - `biasData`: Input. Pointer to the bias data location in the device memory.
    /// - `reorderedBiasData`: Output. Pointer to the location in the device memory where the reordered `biasData` will be written to, by this function. This tensor has the same dimensions as `biasData`.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: The handle, filter descriptor, filter data, or reordered data is `NULL`. Or, if the bias reordering is requested (`reorderBias > 0`), the `biasData` or reordered `biasData` is `NULL`. This status can also be returned if the filter dimension size is not `4`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_EXECUTION_FAILED`]: Either the reordering of the filter data or of the `biasData` failed.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: Filter descriptor data type is not [`cudnnDataType_t::CUDNN_DATA_INT8x32`]; the filter descriptor tensor is not in a vectorized layout ([`cudnnTensorFormat_t::CUDNN_TENSOR_NCHW_VECT_C`]).
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: Reordering was successful.
    #[deprecated]
    pub fn cudnnReorderFilterAndBias(
        handle: cudnnHandle_t,
        filterDesc: cudnnFilterDescriptor_t,
        reorderType: cudnnReorderType_t,
        filterData: *const ::core::ffi::c_void,
        reorderedFilterData: *mut ::core::ffi::c_void,
        reorderBias: ::core::ffi::c_int,
        biasData: *const ::core::ffi::c_void,
        reorderedBiasData: *mut ::core::ffi::c_void,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function returns the amount of GPU memory workspace the user needs to allocate to be able to call [`cudnnConvolutionForward`] with the specified algorithm. The workspace allocated will then be passed to the routine [`cudnnConvolutionForward`]. The specified algorithm can be the result of the call to [`cudnnGetConvolutionForwardAlgorithm_v7`] or can be chosen arbitrarily by the user. Note that not every algorithm is available for every configuration of the input tensor and/or every configuration of the convolution descriptor.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN context.
    /// - `xDesc`: Input. Handle to the previously initialized `x` tensor descriptor.
    /// - `wDesc`: Input. Handle to a previously initialized filter descriptor.
    /// - `convDesc`: Input. Previously initialized convolution descriptor.
    /// - `yDesc`: Input. Handle to the previously initialized `y` tensor descriptor.
    /// - `algo`: Input. Enumerant that specifies the chosen convolution algorithm.
    /// - `sizeInBytes`: Output. Amount of GPU memory needed as workspace to be able to execute a forward convolution with the specified `algo`.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * One of the parameters `handle`, `xDesc`, `wDesc`, `convDesc`, or `yDesc` is `NULL`.
    /// * The tensor `yDesc` or `wDesc` are not of the same dimension as `xDesc`.
    /// * The tensor `xDesc`, `yDesc`, or `wDesc` are not of the same data type.
    /// * The numbers of feature maps of the tensor `xDesc` and `wDesc` differ.
    /// * The tensor `xDesc` has a dimension smaller than `3`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: The combination of the tensor descriptors, filter descriptor, and convolution descriptor is not supported for the specified algorithm.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The query was successful.
    #[deprecated]
    pub fn cudnnGetConvolutionForwardWorkspaceSize(
        handle: cudnnHandle_t,
        xDesc: cudnnTensorDescriptor_t,
        wDesc: cudnnFilterDescriptor_t,
        convDesc: cudnnConvolutionDescriptor_t,
        yDesc: cudnnTensorDescriptor_t,
        algo: cudnnConvolutionFwdAlgo_t,
        sizeInBytes: *mut size_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function executes convolutions or cross-correlations over `x` using filters specified with `w`, returning results in `y`. Scaling factors `alpha` and `beta` can be used to scale the input tensor and the output tensor respectively.
    ///
    /// The routine [`cudnnGetConvolution2dForwardOutputDim`] or [`cudnnGetConvolutionNdForwardOutputDim`] can be used to determine the proper dimensions of the output tensor descriptor `yDesc` with respect to `xDesc`, `convDesc`, and `wDesc`.
    ///
    /// **Supported Configurations**
    ///
    /// This function supports the following combinations of data types for `xDesc`, `wDesc`, `convDesc`, and `yDesc`.
    ///
    /// **Supported Algorithms**
    ///
    /// For this function, all algorithms perform deterministic computations. Specifying a separate algorithm can cause changes in performance and support.
    ///
    /// The following table shows the list of the supported 2D and 3D convolutions. The 2D convolutions are described first, followed by the 3D convolutions.
    ///
    /// For brevity, the short-form versions followed by > are used in the table below:
    ///
    /// Tensors can be converted to and from [`cudnnTensorFormat_t::CUDNN_TENSOR_NCHW_VECT_C`] with [`cudnnTransformTensor`].
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN context. For more information, refer to [`cudnnHandle_t`].
    /// - `xDesc`: Input. Handle to a previously initialized tensor descriptor. For more information, refer to [`cudnnTensorDescriptor_t`].
    /// - `x`: Input. Data pointer to GPU memory associated with the tensor descriptor `xDesc`.
    /// - `wDesc`: Input. Handle to a previously initialized filter descriptor. For more information, refer to [`cudnnFilterDescriptor_t`].
    /// - `w`: Input. Data pointer to GPU memory associated with the filter descriptor `wDesc`.
    /// - `convDesc`: Input. Previously initialized convolution descriptor. For more information, refer to [`cudnnConvolutionDescriptor_t`].
    /// - `algo`: Input. Enumerant that specifies which convolution algorithm should be used to compute the results. For more information, refer to [`cudnnConvolutionFwdAlgo_t`].
    /// - `workSpace`: Input. Data pointer to GPU memory to a workspace needed to be able to execute the specified algorithm. If no workspace is needed for a particular algorithm, that pointer can be `NIL`.
    /// - `workSpaceSizeInBytes`: Input. Specifies the size in bytes of the provided `workSpace`.
    /// - `yDesc`: Input. Handle to a previously initialized tensor descriptor.
    /// - `y`: Input/output. Data pointer to GPU memory associated with the tensor descriptor `yDesc` that carries the result of the convolution.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * At least one of the following is `NULL`: `handle`, `xDesc`, `wDesc`, `convDesc`, `yDesc`, `xData`, `w`, `yData`, `alpha`, and `beta`
    /// * `xDesc` and `yDesc` have a non-matching number of dimensions
    /// * `xDesc` and `wDesc` have a non-matching number of dimensions
    /// * `xDesc` has fewer than three number of dimensions
    /// * `xDesc` number of dimensions is not equal to `convDesc` array length + 2
    /// * `xDesc` and `wDesc` have a non-matching number of input feature maps per image (or group in case of grouped convolutions)
    /// * `yDesc` or `wDesc` indicate an output channel count that isn’t a multiple of group count (if group count has been set in `convDesc`)
    /// * `xDesc`, `wDesc`, and `yDesc` have a non-matching data type
    /// * For some spatial dimension, `wDesc` has a spatial size that is larger than the input spatial size (including zero-padding size).
    /// - [`cudnnStatus_t::CUDNN_STATUS_EXECUTION_FAILED`]: The function failed to launch on the GPU.
    /// - `CUDNN_STATUS_MAPPING_ERROR`: An error occurs during the texture object creation associated with the filter data.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: At least one of the following conditions are met:
    ///
    /// ```text
    /// * `xDesc` or `yDesc` have negative tensor striding
    /// * `xDesc`, `wDesc`, or `yDesc` has a number of dimensions that is not `4` or `5`
    /// * `yDesc` spatial sizes do not match with the expected size as determined by `cudnnGetConvolutionNdForwardOutputDim`
    /// * The chosen algo does not support the parameters provided; see above for an exhaustive list of parameters supported for each algo
    /// ```
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The operation was launched successfully.
    #[deprecated]
    pub fn cudnnConvolutionForward(
        handle: cudnnHandle_t,
        alpha: *const ::core::ffi::c_void,
        xDesc: cudnnTensorDescriptor_t,
        x: *const ::core::ffi::c_void,
        wDesc: cudnnFilterDescriptor_t,
        w: *const ::core::ffi::c_void,
        convDesc: cudnnConvolutionDescriptor_t,
        algo: cudnnConvolutionFwdAlgo_t,
        workSpace: *mut ::core::ffi::c_void,
        workSpaceSizeInBytes: size_t,
        beta: *const ::core::ffi::c_void,
        yDesc: cudnnTensorDescriptor_t,
        y: *mut ::core::ffi::c_void,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function applies a bias and then an activation to the convolutions or cross-correlations of [`cudnnConvolutionForward`], returning results in `y`. The full computation follows the equation `y = act (alpha1 * conv(x) + alpha2 * z + bias)`.
    ///
    /// The routine [`cudnnGetConvolution2dForwardOutputDim`] or [`cudnnGetConvolutionNdForwardOutputDim`] can be used to determine the proper dimensions of the output tensor descriptor `yDesc` with respect to `xDesc`, `convDesc`, and `wDesc`.
    ///
    /// Only the [`cudnnConvolutionFwdAlgo_t::CUDNN_CONVOLUTION_FWD_ALGO_IMPLICIT_PRECOMP_GEMM`] algo is enabled with [`cudnnActivationMode_t::CUDNN_ACTIVATION_IDENTITY`]. In other words, in the [`cudnnActivationDescriptor_t`] structure of the input `activationDesc`, if the mode of the [`cudnnActivationMode_t`] field is set to the enum value [`cudnnActivationMode_t::CUDNN_ACTIVATION_IDENTITY`], then the input [`cudnnConvolutionFwdAlgo_t`] of this function [`cudnnConvolutionBiasActivationForward`] must be set to the enum value [`cudnnConvolutionFwdAlgo_t::CUDNN_CONVOLUTION_FWD_ALGO_IMPLICIT_PRECOMP_GEMM`]. For more information, refer to [`cudnnSetActivationDescriptor`].
    ///
    /// Device pointer `z` and `y` may be pointing to the same buffer, however, `x` cannot point to the same buffer as `z` or `y`.
    ///
    /// For the convolution step, this function supports the specific combinations of data types for `xDesc`, `wDesc`, `convDesc`, and `yDesc` as listed in the documentation of [`cudnnConvolutionForward`]. The following table specifies the supported combinations of data types for `x`, `y`, `z`, `bias`, `alpha1`, and `alpha2`.
    ///
    /// In addition to the error values listed by the documentation of [`cudnnConvolutionForward`],.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN context. For more information, refer to [`cudnnHandle_t`].
    /// - `xDesc`: Input. Handle to a previously initialized tensor descriptor. For more information, refer to [`cudnnTensorDescriptor_t`].
    /// - `x`: Input. Data pointer to GPU memory associated with the tensor descriptor `xDesc`.
    /// - `wDesc`: Input. Handle to a previously initialized filter descriptor. For more information, refer to [`cudnnFilterDescriptor_t`].
    /// - `w`: Input. Data pointer to GPU memory associated with the filter descriptor `wDesc`.
    /// - `convDesc`: Input. Previously initialized convolution descriptor. For more information, refer to [`cudnnConvolutionDescriptor_t`].
    /// - `algo`: Input. Enumerant that specifies which convolution algorithm should be used to compute the results. For more information, refer to [`cudnnConvolutionFwdAlgo_t`].
    /// - `workSpace`: Input. Data pointer to GPU memory to a workspace needed to be able to execute the specified algorithm. If no workspace is needed for a particular algorithm, that pointer can be `NIL`.
    /// - `workSpaceSizeInBytes`: Input. Specifies the size in bytes of the provided `workSpace`.
    /// - `zDesc`: Input. Handle to a previously initialized tensor descriptor.
    /// - `z`: Input. Data pointer to GPU memory associated with the tensor descriptor zDesc.
    /// - `biasDesc`: Input. Handle to a previously initialized tensor descriptor.
    /// - `bias`: Input. Data pointer to GPU memory associated with the tensor descriptor `biasDesc`.
    /// - `activationDesc`: Input. Handle to a previously initialized activation descriptor. For more information, refer to [`cudnnActivationDescriptor_t`].
    /// - `yDesc`: Input. Handle to a previously initialized tensor descriptor.
    /// - `y`: *Input/Output*. Data pointer to GPU memory associated with the tensor descriptor `yDesc` that carries the result of the convolution.
    #[deprecated]
    pub fn cudnnConvolutionBiasActivationForward(
        handle: cudnnHandle_t,
        alpha1: *const ::core::ffi::c_void,
        xDesc: cudnnTensorDescriptor_t,
        x: *const ::core::ffi::c_void,
        wDesc: cudnnFilterDescriptor_t,
        w: *const ::core::ffi::c_void,
        convDesc: cudnnConvolutionDescriptor_t,
        algo: cudnnConvolutionFwdAlgo_t,
        workSpace: *mut ::core::ffi::c_void,
        workSpaceSizeInBytes: size_t,
        alpha2: *const ::core::ffi::c_void,
        zDesc: cudnnTensorDescriptor_t,
        z: *const ::core::ffi::c_void,
        biasDesc: cudnnTensorDescriptor_t,
        bias: *const ::core::ffi::c_void,
        activationDesc: cudnnActivationDescriptor_t,
        yDesc: cudnnTensorDescriptor_t,
        y: *mut ::core::ffi::c_void,
    ) -> cudnnStatus_t;
}
#[repr(C)]
#[derive(Debug, Copy, Clone, PartialOrd, PartialEq)]
pub struct cudnnConvolutionBwdDataAlgoPerfStruct {
    pub algo: cudnnConvolutionBwdDataAlgo_t,
    pub status: cudnnStatus_t,
    pub time: f32,
    pub memory: size_t,
    pub determinism: cudnnDeterminism_t,
    pub mathType: cudnnMathType_t,
    pub reserved: [::core::ffi::c_int; 3usize],
}
impl Default for cudnnConvolutionBwdDataAlgoPerfStruct {
    fn default() -> Self {
        let mut s = ::core::mem::MaybeUninit::<Self>::uninit();
        unsafe {
            ::core::ptr::write_bytes(s.as_mut_ptr(), 0, 1);
            s.assume_init()
        }
    }
}
/// [`cudnnConvolutionBwdDataAlgoPerf_t`] is a structure containing performance results returned by [`cudnnFindConvolutionBackwardDataAlgorithm`] or heuristic results returned by [`cudnnGetConvolutionBackwardDataAlgorithm_v7`].
///
/// **Data Members**.
#[deprecated]
pub type cudnnConvolutionBwdDataAlgoPerf_t = cudnnConvolutionBwdDataAlgoPerfStruct;
unsafe extern "C" {
    /// This function returns the maximum number of algorithms which can be returned from [`cudnnFindConvolutionBackwardDataAlgorithm`] and [`cudnnGetConvolutionForwardAlgorithm_v7`]. This is the sum of all algorithms plus the sum of all algorithms with Tensor Core operations supported for the current device.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN context.
    /// - `count`: Output. The resulting maximum number of algorithms.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: The provided handle is not allocated properly.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The function was successful.
    #[deprecated]
    pub fn cudnnGetConvolutionBackwardDataAlgorithmMaxCount(
        handle: cudnnHandle_t,
        count: *mut ::core::ffi::c_int,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function attempts all algorithms available for [`cudnnConvolutionBackwardData`]. It will attempt both the provided `convDesc` `mathType` and [`cudnnMathType_t::CUDNN_DEFAULT_MATH`] (assuming the two differ).
    ///
    /// Algorithms without the [`cudnnMathType_t::CUDNN_TENSOR_OP_MATH`] availability will only be tried with [`cudnnMathType_t::CUDNN_DEFAULT_MATH`], and returned as such.
    ///
    /// Memory is allocated via `cudaMalloc()`. The performance metrics are returned in the user-allocated array of [`cudnnConvolutionBwdDataAlgoPerf_t`]. These metrics are written in a sorted fashion where the first element has the lowest compute time. The total number of resulting algorithms can be queried through the API [`cudnnGetConvolutionBackwardDataAlgorithmMaxCount`].
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN context.
    /// - `wDesc`: Input. Handle to a previously initialized filter descriptor.
    /// - `dyDesc`: Input. Handle to the previously initialized input differential tensor descriptor.
    /// - `convDesc`: Input. Previously initialized convolution descriptor.
    /// - `dxDesc`: Input. Handle to the previously initialized output tensor descriptor.
    /// - `requestedAlgoCount`: Input. The maximum number of elements to be stored in `perfResults`.
    /// - `returnedAlgoCount`: Output. The number of output elements stored in `perfResults`.
    /// - `perfResults`: Output. A user-allocated array to store performance metrics sorted ascending by compute time.
    ///
    /// # Return value
    ///
    /// - `CUDNN_STATUS_ALLOC_FAILED`: This function was unable to allocate memory to store sample input, filters, and output.
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * `handle` is not allocated properly
    /// * `wDesc`, `dyDesc`, or `dxDesc` is not allocated properly
    /// * `wDesc`, `dyDesc`, or `dxDesc` has fewer than `1` dimension
    /// * Either `returnedCount` or `perfResults` is `NIL`
    /// * `requestedCount` is less than `1`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_INTERNAL_ERROR`]: At least one of the following conditions are met:
    ///
    /// * The function was unable to allocate necessary timing objects
    /// * The function was unable to deallocate necessary timing objects
    /// * The function was unable to deallocate sample input, filters, and output.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The query was successful.
    #[deprecated]
    pub fn cudnnFindConvolutionBackwardDataAlgorithm(
        handle: cudnnHandle_t,
        wDesc: cudnnFilterDescriptor_t,
        dyDesc: cudnnTensorDescriptor_t,
        convDesc: cudnnConvolutionDescriptor_t,
        dxDesc: cudnnTensorDescriptor_t,
        requestedAlgoCount: ::core::ffi::c_int,
        returnedAlgoCount: *mut ::core::ffi::c_int,
        perfResults: *mut cudnnConvolutionBwdDataAlgoPerf_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function attempts all algorithms available for [`cudnnConvolutionBackwardData`]. It will attempt both the provided `convDesc` `mathType` and [`cudnnMathType_t::CUDNN_DEFAULT_MATH`] (assuming the two differ).
    ///
    /// Algorithms without the [`cudnnMathType_t::CUDNN_TENSOR_OP_MATH`] availability will only be tried with [`cudnnMathType_t::CUDNN_DEFAULT_MATH`], and returned as such.
    ///
    /// Memory is allocated via `cudaMalloc()`. The performance metrics are returned in the user-allocated array of [`cudnnConvolutionBwdDataAlgoPerf_t`]. These metrics are written in a sorted fashion where the first element has the lowest compute time. The total number of resulting algorithms can be queried through the API [`cudnnGetConvolutionBackwardDataAlgorithmMaxCount`].
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN context.
    /// - `wDesc`: Input. Handle to a previously initialized filter descriptor.
    /// - `w`: Input. Data pointer to GPU memory associated with the filter descriptor `wDesc`.
    /// - `dyDesc`: Input. Handle to the previously initialized input differential tensor descriptor.
    /// - `dy`: Input. Data pointer to GPU memory associated with the filter descriptor `dyDesc`.
    /// - `convDesc`: Input. Previously initialized convolution descriptor.
    /// - `dxDesc`: Input. Handle to the previously initialized output tensor descriptor.
    /// - `dx`: Input/output. Data pointer to GPU memory associated with the tensor descriptor `dxDesc`. The content of this tensor will be overwritten with arbitrary values.
    /// - `requestedAlgoCount`: Input. The maximum number of elements to be stored in `perfResults`.
    /// - `returnedAlgoCount`: Output. The number of output elements stored in `perfResults`.
    /// - `perfResults`: Output. A user-allocated array to store performance metrics sorted ascending by compute time.
    /// - `workSpace`: Input. Data pointer to GPU memory is a necessary workspace for some algorithms. The size of this workspace will determine the availability of algorithms. A `NIL` pointer is considered a `workSpace` of `0` bytes.
    /// - `workSpaceSizeInBytes`: Input. Specifies the size in bytes of the provided `workSpace`.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * `handle` is not allocated properly
    /// * `wDesc`, `dyDesc`, or `dxDesc` is not allocated properly
    /// * `wDesc`, `dyDesc`, or `dxDesc` has fewer than `1` dimension
    /// * Either `returnedCount` or `perfResults` is `NIL`
    /// * `requestedCount` is less than `1`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_INTERNAL_ERROR`]: At least one of the following conditions are met:
    ///
    /// * The function was unable to allocate necessary timing objects
    /// * The function was unable to deallocate necessary timing objects
    /// * The function was unable to deallocate sample input, filters, and output.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The query was successful.
    #[deprecated]
    pub fn cudnnFindConvolutionBackwardDataAlgorithmEx(
        handle: cudnnHandle_t,
        wDesc: cudnnFilterDescriptor_t,
        w: *const ::core::ffi::c_void,
        dyDesc: cudnnTensorDescriptor_t,
        dy: *const ::core::ffi::c_void,
        convDesc: cudnnConvolutionDescriptor_t,
        dxDesc: cudnnTensorDescriptor_t,
        dx: *mut ::core::ffi::c_void,
        requestedAlgoCount: ::core::ffi::c_int,
        returnedAlgoCount: *mut ::core::ffi::c_int,
        perfResults: *mut cudnnConvolutionBwdDataAlgoPerf_t,
        workSpace: *mut ::core::ffi::c_void,
        workSpaceSizeInBytes: size_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function serves as a heuristic for obtaining the best suited algorithm for [`cudnnConvolutionBackwardData`] for the given layer specifications. This function will return all algorithms (including [`cudnnMathType_t::CUDNN_TENSOR_OP_MATH`] and [`cudnnMathType_t::CUDNN_DEFAULT_MATH`] versions of algorithms where [`cudnnMathType_t::CUDNN_TENSOR_OP_MATH`] may be available) sorted by expected (based on internal heuristic) relative performance with the fastest being index `0` of `perfResults`. For an exhaustive search for the fastest algorithm, use [`cudnnFindConvolutionBackwardDataAlgorithm`]. The total number of resulting algorithms can be queried through the `returnedAlgoCount` variable.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN context.
    /// - `convDesc`: Input. Previously initialized convolution descriptor.
    /// - `requestedAlgoCount`: Input. The maximum number of elements to be stored in `perfResults`.
    /// - `returnedAlgoCount`: Output. The number of output elements stored in `perfResults`.
    /// - `perfResults`: Output. A user-allocated array to store performance metrics sorted ascending by compute time.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * One of the parameters `handle`, `wDesc`, `dyDesc`, `convDesc`, `dxDesc`, `perfResults`, or `returnedAlgoCount` is `NULL`.
    /// * The numbers of feature maps of the input tensor and output tensor differ.
    /// * The `dataType` of the two tensor descriptors or the filters are different.
    /// * `requestedAlgoCount` is less than or equal to `0`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The query was successful.
    #[deprecated]
    pub fn cudnnGetConvolutionBackwardDataAlgorithm_v7(
        handle: cudnnHandle_t,
        filterDesc: cudnnFilterDescriptor_t,
        diffDesc: cudnnTensorDescriptor_t,
        convDesc: cudnnConvolutionDescriptor_t,
        gradDesc: cudnnTensorDescriptor_t,
        requestedAlgoCount: ::core::ffi::c_int,
        returnedAlgoCount: *mut ::core::ffi::c_int,
        perfResults: *mut cudnnConvolutionBwdDataAlgoPerf_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function returns the amount of GPU memory workspace the user needs to allocate to be able to call [`cudnnConvolutionBackwardData`] with the specified algorithm. The workspace allocated will then be passed to the routine [`cudnnConvolutionBackwardData`]. The specified algorithm can be the result of the call to [`cudnnGetConvolutionBackwardDataAlgorithm_v7`] or can be chosen arbitrarily by the user. Note that not every algorithm is available for every configuration of the input tensor and/or every configuration of the convolution descriptor.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN context.
    /// - `wDesc`: Input. Handle to a previously initialized filter descriptor.
    /// - `dyDesc`: Input. Handle to the previously initialized input differential tensor descriptor.
    /// - `convDesc`: Input. Previously initialized convolution descriptor.
    /// - `dxDesc`: Input. Handle to the previously initialized output tensor descriptor.
    /// - `algo`: Input. Enumerant that specifies the chosen convolution algorithm.
    /// - `sizeInBytes`: Output. Amount of GPU memory needed as workspace to be able to execute a forward convolution with the specified `algo`.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * The numbers of feature maps of the input tensor and output tensor differ.
    /// * The `dataType` of the two tensor descriptors or the filter are different.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: The combination of the tensor descriptors, filter descriptor, and convolution descriptor is not supported for the specified algorithm.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The query was successful.
    #[deprecated]
    pub fn cudnnGetConvolutionBackwardDataWorkspaceSize(
        handle: cudnnHandle_t,
        wDesc: cudnnFilterDescriptor_t,
        dyDesc: cudnnTensorDescriptor_t,
        convDesc: cudnnConvolutionDescriptor_t,
        dxDesc: cudnnTensorDescriptor_t,
        algo: cudnnConvolutionBwdDataAlgo_t,
        sizeInBytes: *mut size_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function computes the convolution data gradient of the tensor `dy`, where `y` is the output of the forward convolution in [`cudnnConvolutionForward`]. It uses the specified `algo`, and returns the results in the output tensor `dx`. Scaling factors `alpha` and `beta` can be used to scale the computed result or accumulate with the current `dx`.
    ///
    /// **Supported Configurations**
    ///
    /// This function supports the following combinations of data types for `wDesc`, `dyDesc`, `convDesc`, and `dxDesc`.
    ///
    /// **Supported Algorithms**
    ///
    /// Specifying a separate algorithm can cause changes in performance, support and computation determinism. Refer to the following list of algorithm options, and their respective supported parameters and deterministic behavior.
    ///
    /// The table below shows the list of the supported 2D and 3D convolutions. The 2D convolutions are described first, followed by the 3D convolutions.
    ///
    /// For brevity, the short-form versions followed by > are used in the table below.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN context. For more information, refer to [`cudnnHandle_t`].
    /// - `wDesc`: Input. Handle to a previously initialized filter descriptor. For more information, refer to [`cudnnFilterDescriptor_t`].
    /// - `w`: Input. Data pointer to GPU memory associated with the filter descriptor `wDesc`.
    /// - `dyDesc`: Input. Handle to the previously initialized input differential tensor descriptor. For more information, refer to [`cudnnTensorDescriptor_t`].
    /// - `dy`: Input. Data pointer to GPU memory associated with the input differential tensor descriptor `dyDesc`.
    /// - `convDesc`: Input. Previously initialized convolution descriptor. For more information, refer to [`cudnnConvolutionDescriptor_t`].
    /// - `algo`: Input. Enumerant that specifies which backward data convolution algorithm should be used to compute the results. For more information, refer to [`cudnnConvolutionBwdDataAlgo_t`].
    /// - `workSpace`: Input. Data pointer to GPU memory to a workspace needed to be able to execute the specified algorithm. If no workspace is needed for a particular algorithm, that pointer can be `NIL`.
    /// - `workSpaceSizeInBytes`: Input. Specifies the size in bytes of the provided `workSpace`.
    /// - `dxDesc`: Input. Handle to the previously initialized output tensor descriptor.
    /// - `dx`: *Input/Output*. Data pointer to GPU memory associated with the output tensor descriptor `dxDesc` that carries the result.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * At least one of the following is `NULL: handle`, `dyDesc`, `wDesc`, `convDesc`, `dxDesc`, `dy`, `w`, `dx`, `alpha`, and `beta`
    /// * `wDesc` and `dyDesc` have a non-matching number of dimensions
    /// * `wDesc` and `dxDesc` have a non-matching number of dimensions
    /// * `wDesc` has fewer than three number of dimensions
    /// * `wDesc`, `dxDesc`, and `dyDesc` have a non-matching data type
    /// * `wDesc` and `dxDesc` have a non-matching number of input feature maps per image (or group in case of grouped convolutions)
    /// * `dyDesc` spatial sizes do not match with the expected size as determined by [`cudnnGetConvolutionNdForwardOutputDim`].
    /// - [`cudnnStatus_t::CUDNN_STATUS_EXECUTION_FAILED`]: The function failed to launch on the GPU.
    /// - `CUDNN_STATUS_MAPPING_ERROR`: An error occurs during the texture binding of texture object creation associated with the filter data or the input differential tensor data.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: At least one of the following conditions are met:
    ///
    /// ```text
    /// * `dyDesc` or `dxDesc` have a negative tensor striding
    /// * `dyDesc`, `wDesc`, or `dxDesc` has a number of dimensions that is not `4` or `5`
    /// * The chosen algo does not support the parameters provided; refer to the above tables for an exhaustive list of parameters that support each algo.
    /// * `dyDesc` or `wDesc` indicate an output channel count that isn’t a multiple of group count (if group count has been set in `convDesc`)
    /// ```
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The operation was launched successfully.
    #[deprecated]
    pub fn cudnnConvolutionBackwardData(
        handle: cudnnHandle_t,
        alpha: *const ::core::ffi::c_void,
        wDesc: cudnnFilterDescriptor_t,
        w: *const ::core::ffi::c_void,
        dyDesc: cudnnTensorDescriptor_t,
        dy: *const ::core::ffi::c_void,
        convDesc: cudnnConvolutionDescriptor_t,
        algo: cudnnConvolutionBwdDataAlgo_t,
        workSpace: *mut ::core::ffi::c_void,
        workSpaceSizeInBytes: size_t,
        beta: *const ::core::ffi::c_void,
        dxDesc: cudnnTensorDescriptor_t,
        dx: *mut ::core::ffi::c_void,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function calculates folding descriptors for backward data gradients. It takes as input the data descriptors along with the convolution descriptor and computes the folded data descriptors and the folding transform descriptors. These can then be used to do the actual folding transform.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN context.
    /// - `filterDesc`: Input. Filter descriptor before folding.
    /// - `diffDesc`: Input. `Diff` descriptor before folding.
    /// - `convDesc`: Input. Convolution descriptor before folding.
    /// - `gradDesc`: Input. Gradient descriptor before folding.
    /// - `transformFormat`: Input. Transform format for folding.
    /// - `foldedFilterDesc`: Output. Folded filter descriptor.
    /// - `paddedDiffDesc`: Output. Padded `Diff` descriptor.
    /// - `foldedConvDesc`: Output. Folded convolution descriptor.
    /// - `foldedGradDesc`: Output. Folded gradient descriptor.
    /// - `filterFoldTransDesc`: Output. Folding transform descriptor for filter.
    /// - `diffPadTransDesc`: Output. Folding transform descriptor for `Desc`.
    /// - `gradFoldTransDesc`: Output. Folding transform descriptor for gradient.
    /// - `gradUnfoldTransDesc`: Output. Unfolding transform descriptor for folded gradient.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: If any of the input parameters is `NULL` or if the input tensor has more than 4 dimensions.
    /// - [`cudnnStatus_t::CUDNN_STATUS_EXECUTION_FAILED`]: Computing the folded descriptors failed.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: Folded descriptors were computed successfully.
    pub fn cudnnGetFoldedConvBackwardDataDescriptors(
        handle: cudnnHandle_t,
        filterDesc: cudnnFilterDescriptor_t,
        diffDesc: cudnnTensorDescriptor_t,
        convDesc: cudnnConvolutionDescriptor_t,
        gradDesc: cudnnTensorDescriptor_t,
        transformFormat: cudnnTensorFormat_t,
        foldedFilterDesc: cudnnFilterDescriptor_t,
        paddedDiffDesc: cudnnTensorDescriptor_t,
        foldedConvDesc: cudnnConvolutionDescriptor_t,
        foldedGradDesc: cudnnTensorDescriptor_t,
        filterFoldTransDesc: cudnnTensorTransformDescriptor_t,
        diffPadTransDesc: cudnnTensorTransformDescriptor_t,
        gradFoldTransDesc: cudnnTensorTransformDescriptor_t,
        gradUnfoldTransDesc: cudnnTensorTransformDescriptor_t,
    ) -> cudnnStatus_t;
}
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct cudnnFusedOpsConstParamStruct {
    _unused: [u8; 0],
}
/// [`cudnnFusedOpsConstParamPack_t`] is a pointer to an opaque structure holding the description of the `cudnnFusedOps` constant parameters. Use the function [`cudnnCreateFusedOpsConstParamPack`] to create one instance of this structure, and the function [`cudnnDestroyFusedOpsConstParamPack`] to destroy a previously-created descriptor.
#[deprecated]
pub type cudnnFusedOpsConstParamPack_t = *mut cudnnFusedOpsConstParamStruct;
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct cudnnFusedOpsVariantParamStruct {
    _unused: [u8; 0],
}
/// [`cudnnFusedOpsVariantParamPack_t`] is a pointer to an opaque structure holding the description of the `cudnnFusedOps` variant parameters. Use the function [`cudnnCreateFusedOpsVariantParamPack`] to create one instance of this structure, and the function [`cudnnDestroyFusedOpsVariantParamPack`] to destroy a previously-created descriptor.
#[deprecated]
pub type cudnnFusedOpsVariantParamPack_t = *mut cudnnFusedOpsVariantParamStruct;
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct cudnnFusedOpsPlanStruct {
    _unused: [u8; 0],
}
/// [`cudnnFusedOpsPlan_t`] is a pointer to an opaque structure holding the description of the `cudnnFusedOpsPlan`. This descriptor contains the plan information, including the problem type and size, which kernels should be run, and the internal workspace partition. Use the function [`cudnnCreateFusedOpsPlan`] to create one instance of this structure, and the function [`cudnnDestroyFusedOpsPlan`] to destroy a previously-created descriptor.
#[deprecated]
pub type cudnnFusedOpsPlan_t = *mut cudnnFusedOpsPlanStruct;
/// The [`cudnnFusedOps_t`] type is an enumerated type to select a specific sequence of computations to perform in the fused operations.
///
/// **Members and Descriptions**.
#[deprecated]
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnFusedOps_t {
    CUDNN_FUSED_SCALE_BIAS_ACTIVATION_CONV_BNSTATS = 0,
    CUDNN_FUSED_SCALE_BIAS_ACTIVATION_WGRAD = 1,
    CUDNN_FUSED_BN_FINALIZE_STATISTICS_TRAINING = 2,
    CUDNN_FUSED_BN_FINALIZE_STATISTICS_INFERENCE = 3,
    CUDNN_FUSED_CONV_SCALE_BIAS_ADD_ACTIVATION = 4,
    CUDNN_FUSED_SCALE_BIAS_ADD_ACTIVATION_GEN_BITMASK = 5,
    CUDNN_FUSED_DACTIVATION_FORK_DBATCHNORM = 6,
}
/// The [`cudnnFusedOpsConstParamLabel_t`] is an enumerated type for the selection of the type of the `cudnnFusedOps` descriptor. For more information, refer to [`cudnnSetFusedOpsConstParamPackAttribute`].
///
/// As of cuDNN 7.6.0, if the following conditions in the table are met, then the fully fused fast path will be triggered. Otherwise, a slower partially fused path will be triggered.
///
/// As of cuDNN 7.6.0, if the following conditions in the table are met, then the fully fused fast path will be triggered. Otherwise, a slower partially fused path will be triggered.
///
/// The following operation performs the computation, where $\\*$ denotes convolution operator: $y=\alpha_{1}\left( w\\*x \right)+\alpha_{2}z+b$.
#[deprecated]
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnFusedOpsConstParamLabel_t {
    #[deprecated]
    CUDNN_PARAM_XDESC = 0,
    #[deprecated]
    CUDNN_PARAM_XDATA_PLACEHOLDER = 1,
    #[deprecated]
    CUDNN_PARAM_BN_MODE = 2,
    #[deprecated]
    CUDNN_PARAM_BN_EQSCALEBIAS_DESC = 3,
    #[deprecated]
    CUDNN_PARAM_BN_EQSCALE_PLACEHOLDER = 4,
    #[deprecated]
    CUDNN_PARAM_BN_EQBIAS_PLACEHOLDER = 5,
    #[deprecated]
    CUDNN_PARAM_ACTIVATION_DESC = 6,
    #[deprecated]
    CUDNN_PARAM_CONV_DESC = 7,
    #[deprecated]
    CUDNN_PARAM_WDESC = 8,
    #[deprecated]
    CUDNN_PARAM_WDATA_PLACEHOLDER = 9,
    #[deprecated]
    CUDNN_PARAM_DWDESC = 10,
    #[deprecated]
    CUDNN_PARAM_DWDATA_PLACEHOLDER = 11,
    #[deprecated]
    CUDNN_PARAM_YDESC = 12,
    #[deprecated]
    CUDNN_PARAM_YDATA_PLACEHOLDER = 13,
    #[deprecated]
    CUDNN_PARAM_DYDESC = 14,
    #[deprecated]
    CUDNN_PARAM_DYDATA_PLACEHOLDER = 15,
    #[deprecated]
    CUDNN_PARAM_YSTATS_DESC = 16,
    #[deprecated]
    CUDNN_PARAM_YSUM_PLACEHOLDER = 17,
    #[deprecated]
    CUDNN_PARAM_YSQSUM_PLACEHOLDER = 18,
    #[deprecated]
    CUDNN_PARAM_BN_SCALEBIAS_MEANVAR_DESC = 19,
    #[deprecated]
    CUDNN_PARAM_BN_SCALE_PLACEHOLDER = 20,
    #[deprecated]
    CUDNN_PARAM_BN_BIAS_PLACEHOLDER = 21,
    #[deprecated]
    CUDNN_PARAM_BN_SAVED_MEAN_PLACEHOLDER = 22,
    #[deprecated]
    CUDNN_PARAM_BN_SAVED_INVSTD_PLACEHOLDER = 23,
    #[deprecated]
    CUDNN_PARAM_BN_RUNNING_MEAN_PLACEHOLDER = 24,
    #[deprecated]
    CUDNN_PARAM_BN_RUNNING_VAR_PLACEHOLDER = 25,
    #[deprecated]
    CUDNN_PARAM_ZDESC = 26,
    #[deprecated]
    CUDNN_PARAM_ZDATA_PLACEHOLDER = 27,
    #[deprecated]
    CUDNN_PARAM_BN_Z_EQSCALEBIAS_DESC = 28,
    #[deprecated]
    CUDNN_PARAM_BN_Z_EQSCALE_PLACEHOLDER = 29,
    #[deprecated]
    CUDNN_PARAM_BN_Z_EQBIAS_PLACEHOLDER = 30,
    #[deprecated]
    CUDNN_PARAM_ACTIVATION_BITMASK_DESC = 31,
    #[deprecated]
    CUDNN_PARAM_ACTIVATION_BITMASK_PLACEHOLDER = 32,
    #[deprecated]
    CUDNN_PARAM_DXDESC = 33,
    #[deprecated]
    CUDNN_PARAM_DXDATA_PLACEHOLDER = 34,
    #[deprecated]
    CUDNN_PARAM_DZDESC = 35,
    #[deprecated]
    CUDNN_PARAM_DZDATA_PLACEHOLDER = 36,
    #[deprecated]
    CUDNN_PARAM_BN_DSCALE_PLACEHOLDER = 37,
    #[deprecated]
    CUDNN_PARAM_BN_DBIAS_PLACEHOLDER = 38,
}
/// [`cudnnFusedOpsPointerPlaceHolder_t`] is an enumerated type used to select the alignment type of the `cudnnFusedOps` descriptor pointer.
///
/// **Members and Descriptions**.
#[deprecated]
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnFusedOpsPointerPlaceHolder_t {
    CUDNN_PTR_NULL = 0,
    CUDNN_PTR_ELEM_ALIGNED = 1,
    CUDNN_PTR_16B_ALIGNED = 2,
}
/// The [`cudnnFusedOpsVariantParamLabel_t`] is an enumerated type that is used to set the buffer pointers. These buffer pointers can be changed in each iteration.
#[deprecated]
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cudnnFusedOpsVariantParamLabel_t {
    #[deprecated]
    CUDNN_PTR_XDATA = 0,
    #[deprecated]
    CUDNN_PTR_BN_EQSCALE = 1,
    #[deprecated]
    CUDNN_PTR_BN_EQBIAS = 2,
    #[deprecated]
    CUDNN_PTR_WDATA = 3,
    #[deprecated]
    CUDNN_PTR_DWDATA = 4,
    #[deprecated]
    CUDNN_PTR_YDATA = 5,
    #[deprecated]
    CUDNN_PTR_DYDATA = 6,
    #[deprecated]
    CUDNN_PTR_YSUM = 7,
    #[deprecated]
    CUDNN_PTR_YSQSUM = 8,
    #[deprecated]
    CUDNN_PTR_WORKSPACE = 9,
    #[deprecated]
    CUDNN_PTR_BN_SCALE = 10,
    #[deprecated]
    CUDNN_PTR_BN_BIAS = 11,
    #[deprecated]
    CUDNN_PTR_BN_SAVED_MEAN = 12,
    #[deprecated]
    CUDNN_PTR_BN_SAVED_INVSTD = 13,
    #[deprecated]
    CUDNN_PTR_BN_RUNNING_MEAN = 14,
    #[deprecated]
    CUDNN_PTR_BN_RUNNING_VAR = 15,
    #[deprecated]
    CUDNN_PTR_ZDATA = 16,
    #[deprecated]
    CUDNN_PTR_BN_Z_EQSCALE = 17,
    #[deprecated]
    CUDNN_PTR_BN_Z_EQBIAS = 18,
    #[deprecated]
    CUDNN_PTR_ACTIVATION_BITMASK = 19,
    #[deprecated]
    CUDNN_PTR_DXDATA = 20,
    #[deprecated]
    CUDNN_PTR_DZDATA = 21,
    #[deprecated]
    CUDNN_PTR_BN_DSCALE = 22,
    #[deprecated]
    CUDNN_PTR_BN_DBIAS = 23,
    #[deprecated]
    CUDNN_SCALAR_SIZE_T_WORKSPACE_SIZE_IN_BYTES = 100,
    #[deprecated]
    CUDNN_SCALAR_INT64_T_BN_ACCUMULATION_COUNT = 101,
    #[deprecated]
    CUDNN_SCALAR_DOUBLE_BN_EXP_AVG_FACTOR = 102,
    #[deprecated]
    CUDNN_SCALAR_DOUBLE_BN_EPSILON = 103,
}
unsafe extern "C" {
    /// Cross-library version checker. Each sublibrary has a version checker that checks whether its own version matches that of its dependencies.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUBLIBRARY_VERSION_MISMATCH`]: The versions are inconsistent.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The version check passed.
    pub fn cudnnCnnVersionCheck() -> cudnnStatus_t;
}
#[repr(C)]
#[derive(Debug, Copy, Clone, PartialOrd, PartialEq)]
pub struct cudnnConvolutionBwdFilterAlgoPerfStruct {
    pub algo: cudnnConvolutionBwdFilterAlgo_t,
    pub status: cudnnStatus_t,
    pub time: f32,
    pub memory: size_t,
    pub determinism: cudnnDeterminism_t,
    pub mathType: cudnnMathType_t,
    pub reserved: [::core::ffi::c_int; 3usize],
}
impl Default for cudnnConvolutionBwdFilterAlgoPerfStruct {
    fn default() -> Self {
        let mut s = ::core::mem::MaybeUninit::<Self>::uninit();
        unsafe {
            ::core::ptr::write_bytes(s.as_mut_ptr(), 0, 1);
            s.assume_init()
        }
    }
}
/// [`cudnnConvolutionBwdFilterAlgoPerf_t`] is a structure containing performance results returned by [`cudnnFindConvolutionBackwardFilterAlgorithm`] or heuristic results returned by [`cudnnGetConvolutionBackwardFilterAlgorithm_v7`].
///
/// **Data Members**.
#[deprecated]
pub type cudnnConvolutionBwdFilterAlgoPerf_t = cudnnConvolutionBwdFilterAlgoPerfStruct;
unsafe extern "C" {
    /// This function returns the maximum number of algorithms which can be returned from [`cudnnFindConvolutionBackwardFilterAlgorithm`] and [`cudnnGetConvolutionForwardAlgorithm_v7`]. This is the sum of all algorithms plus the sum of all algorithms with Tensor Core operations supported for the current device.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN context.
    /// - `count`: Output. The resulting maximum count of algorithms.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: The provided handle is not allocated properly.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The function was successful.
    #[deprecated]
    pub fn cudnnGetConvolutionBackwardFilterAlgorithmMaxCount(
        handle: cudnnHandle_t,
        count: *mut ::core::ffi::c_int,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function attempts all algorithms available for [`cudnnConvolutionBackwardFilter`]. It will attempt both the provided `convDesc mathType` and [`cudnnMathType_t::CUDNN_DEFAULT_MATH`] (assuming the two differ).
    ///
    /// Algorithms without the [`cudnnMathType_t::CUDNN_TENSOR_OP_MATH`] availability will only be tried with [`cudnnMathType_t::CUDNN_DEFAULT_MATH`], and returned as such.
    ///
    /// Memory is allocated via `cudaMalloc()`. The performance metrics are returned in the user-allocated array of [`cudnnConvolutionBwdFilterAlgoPerf_t`]. These metrics are written in a sorted fashion where the first element has the lowest compute time. The total number of resulting algorithms can be queried through the API [`cudnnGetConvolutionBackwardFilterAlgorithmMaxCount`].
    ///
    /// `CUDNN_STATUS_ALLOC_FAILED`
    /// This function was unable to allocate memory to store sample input, filters and output.
    ///
    /// [`cudnnStatus_t::CUDNN_STATUS_INTERNAL_ERROR`]
    /// At least one of the following conditions are met.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN context.
    /// - `xDesc`: Input. Handle to the previously initialized input tensor descriptor.
    /// - `dyDesc`: Input. Handle to the previously initialized input differential tensor descriptor.
    /// - `convDesc`: Input. Previously initialized convolution descriptor.
    /// - `dwDesc`: Input. Handle to a previously initialized filter descriptor.
    /// - `requestedAlgoCount`: Input. The maximum number of elements to be stored in `perfResults`.
    /// - `returnedAlgoCount`: Output. The number of output elements stored in `perfResults`.
    /// - `perfResults`: Output. A user-allocated array to store performance metrics sorted ascending by compute time.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * `handle` is not allocated properly
    /// * `xDesc`, `dyDesc`, or `dwDesc` are not allocated properly
    /// * `xDesc`, `dyDesc`, or `dwDesc` has fewer than `1` dimension
    /// * Either `returnedCount` or `perfResults` is `NIL`
    /// * `requestedCount` is less than `1`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The query was successful.
    #[deprecated]
    pub fn cudnnFindConvolutionBackwardFilterAlgorithm(
        handle: cudnnHandle_t,
        xDesc: cudnnTensorDescriptor_t,
        dyDesc: cudnnTensorDescriptor_t,
        convDesc: cudnnConvolutionDescriptor_t,
        dwDesc: cudnnFilterDescriptor_t,
        requestedAlgoCount: ::core::ffi::c_int,
        returnedAlgoCount: *mut ::core::ffi::c_int,
        perfResults: *mut cudnnConvolutionBwdFilterAlgoPerf_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function attempts all algorithms available for [`cudnnConvolutionBackwardFilter`]. It will attempt both the provided `convDesc mathType` and [`cudnnMathType_t::CUDNN_DEFAULT_MATH`] (assuming the two differ).
    ///
    /// Algorithms without the [`cudnnMathType_t::CUDNN_TENSOR_OP_MATH`] availability will only be tried with [`cudnnMathType_t::CUDNN_DEFAULT_MATH`], and returned as such.
    ///
    /// Memory is allocated via `cudaMalloc()`. The performance metrics are returned in the user-allocated array of [`cudnnConvolutionBwdFilterAlgoPerf_t`]. These metrics are written in a sorted fashion where the first element has the lowest compute time. The total number of resulting algorithms can be queried through the API [`cudnnGetConvolutionBackwardFilterAlgorithmMaxCount`].
    ///
    /// [`cudnnStatus_t::CUDNN_STATUS_INTERNAL_ERROR`]
    /// At least one of the following conditions are met.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN context.
    /// - `xDesc`: Input. Handle to the previously initialized input tensor descriptor.
    /// - `x`: Input. Data pointer to GPU memory associated with the filter descriptor `xDesc`.
    /// - `dyDesc`: Input. Handle to the previously initialized input differential tensor descriptor.
    /// - `convDesc`: Input. Previously initialized convolution descriptor.
    /// - `dwDesc`: Input. Handle to a previously initialized filter descriptor.
    /// - `dw`: Input/output. Data pointer to GPU memory associated with the filter descriptor `dwDesc`. The content of this tensor will be overwritten with arbitrary values.
    /// - `requestedAlgoCount`: Input. The maximum number of elements to be stored in `perfResults`.
    /// - `returnedAlgoCount`: Output. The number of output elements stored in `perfResults`.
    /// - `perfResults`: Output. A user-allocated array to store performance metrics sorted ascending by compute time.
    /// - `workSpace`: Input. Data pointer to GPU memory is a necessary workspace for some algorithms. The size of this workspace will determine the availability of algorithms. A `NIL` pointer is considered a `workSpace` of `0` bytes.
    /// - `workSpaceSizeInBytes`: Input. Specifies the size in bytes of the provided `workSpace`.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * `handle` is not allocated properly
    /// * `xDesc`, `dyDesc`, or `dwDesc` are not allocated properly
    /// * `xDesc`, `dyDesc`, or `dwDesc` has fewer than `1` dimension
    /// * `x`, `dy`, or `dw` is `NIL`
    /// * Either `returnedCount` or `perfResults` is `NIL`
    /// * `requestedCount` is less than `1`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The query was successful.
    #[deprecated]
    pub fn cudnnFindConvolutionBackwardFilterAlgorithmEx(
        handle: cudnnHandle_t,
        xDesc: cudnnTensorDescriptor_t,
        x: *const ::core::ffi::c_void,
        dyDesc: cudnnTensorDescriptor_t,
        y: *const ::core::ffi::c_void,
        convDesc: cudnnConvolutionDescriptor_t,
        dwDesc: cudnnFilterDescriptor_t,
        dw: *mut ::core::ffi::c_void,
        requestedAlgoCount: ::core::ffi::c_int,
        returnedAlgoCount: *mut ::core::ffi::c_int,
        perfResults: *mut cudnnConvolutionBwdFilterAlgoPerf_t,
        workSpace: *mut ::core::ffi::c_void,
        workSpaceSizeInBytes: size_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function serves as a heuristic for obtaining the best suited algorithm for [`cudnnConvolutionBackwardFilter`] for the given layer specifications. This function will return all algorithms (including [`cudnnMathType_t::CUDNN_TENSOR_OP_MATH`] and [`cudnnMathType_t::CUDNN_DEFAULT_MATH`] versions of algorithms where [`cudnnMathType_t::CUDNN_TENSOR_OP_MATH`] may be available) sorted by expected (based on internal heuristic) relative performance with the fastest being index `0` of `perfResults`. For an exhaustive search for the fastest algorithm, use [`cudnnFindConvolutionBackwardFilterAlgorithm`]. The total number of resulting algorithms can be queried through the `returnedAlgoCount` variable.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN context.
    /// - `convDesc`: Input. Previously initialized convolution descriptor.
    /// - `requestedAlgoCount`: Input. The maximum number of elements to be stored in `perfResults`.
    /// - `returnedAlgoCount`: Output. The number of output elements stored in `perfResults`.
    /// - `perfResults`: Output. A user-allocated array to store performance metrics sorted ascending by compute time.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * One of the parameters `handle`, `xDesc`, `dyDesc`, `convDesc`, `dwDesc`, `perfResults`, or `returnedAlgoCount` is `NULL`.
    /// * The numbers of feature maps of the input tensor and output tensor differ.
    /// * The `dataType` of the two tensor descriptors or the filter are different.
    /// * `requestedAlgoCount` is less than or equal to `0`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The query was successful.
    #[deprecated]
    pub fn cudnnGetConvolutionBackwardFilterAlgorithm_v7(
        handle: cudnnHandle_t,
        srcDesc: cudnnTensorDescriptor_t,
        diffDesc: cudnnTensorDescriptor_t,
        convDesc: cudnnConvolutionDescriptor_t,
        gradDesc: cudnnFilterDescriptor_t,
        requestedAlgoCount: ::core::ffi::c_int,
        returnedAlgoCount: *mut ::core::ffi::c_int,
        perfResults: *mut cudnnConvolutionBwdFilterAlgoPerf_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function returns the amount of GPU memory workspace the user needs to allocate to be able to call [`cudnnConvolutionBackwardFilter`] with the specified algorithm. The workspace allocated will then be passed to the routine [`cudnnConvolutionBackwardFilter`]. The specified algorithm can be the result of the call to [`cudnnGetConvolutionBackwardFilterAlgorithm_v7`] or can be chosen arbitrarily by the user. Note that not every algorithm is available for every configuration of the input tensor and/or every configuration of the convolution descriptor.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN context.
    /// - `xDesc`: Input. Handle to the previously initialized input tensor descriptor.
    /// - `dyDesc`: Input. Handle to the previously initialized input differential tensor descriptor.
    /// - `convDesc`: Input. Previously initialized convolution descriptor.
    /// - `algo`: Input. Enumerant that specifies the chosen convolution algorithm.
    /// - `sizeInBytes`: Output. Amount of GPU memory needed as workspace to be able to execute a forward convolution with the specified `algo`.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * The numbers of feature maps of the input tensor and output tensor differ.
    /// * The `dataType` of the two tensor descriptors or the filter are different.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: The combination of the tensor descriptors, filter descriptor and convolution descriptor is not supported for the specified algorithm.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The query was successful.
    #[deprecated]
    pub fn cudnnGetConvolutionBackwardFilterWorkspaceSize(
        handle: cudnnHandle_t,
        xDesc: cudnnTensorDescriptor_t,
        dyDesc: cudnnTensorDescriptor_t,
        convDesc: cudnnConvolutionDescriptor_t,
        gradDesc: cudnnFilterDescriptor_t,
        algo: cudnnConvolutionBwdFilterAlgo_t,
        sizeInBytes: *mut size_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function computes the convolution weight (filter) gradient of the tensor `dy`, where `y` is the output of the forward convolution in [`cudnnConvolutionForward`]. It uses the specified `algo`, and returns the results in the output tensor `dw`. Scaling factors `alpha` and `beta` can be used to scale the computed result or accumulate with the current `dw`.
    ///
    /// **Supported Configurations**
    ///
    /// This function supports the following combinations of data types for `xDesc`, `dyDesc`, `convDesc`, and `dwDesc`.
    ///
    /// **Supported Algorithms**
    ///
    /// Specifying a separate algorithm can cause changes in performance, support, and computation determinism. Refer to the following table for an exhaustive list of algorithm options and their respective supported parameters and deterministic behavior.
    ///
    /// The table below shows the list of the supported 2D and 3D convolutions. The 2D convolutions are described first, followed by the 3D convolutions.
    ///
    /// For the following terms, the short-form versions shown in parentheses are used in the table below, for brevity:
    ///
    /// For brevity, the short-form versions followed by > are used in the table below.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN context. For more information, refer to [`cudnnHandle_t`].
    /// - `xDesc`: Input. Handle to a previously initialized tensor descriptor. For more information, refer to [`cudnnTensorDescriptor_t`].
    /// - `x`: Input. Data pointer to GPU memory associated with the tensor descriptor `xDesc`.
    /// - `dyDesc`: Input. Handle to the previously initialized input differential tensor descriptor.
    /// - `dy`: Input. Data pointer to GPU memory associated with the backpropagation gradient tensor descriptor `dyDesc`.
    /// - `convDesc`: Input. Previously initialized convolution descriptor. For more information, refer to [`cudnnConvolutionDescriptor_t`].
    /// - `algo`: Input. Enumerant that specifies which convolution algorithm should be used to compute the results. For more information, refer to [`cudnnConvolutionBwdFilterAlgo_t`].
    /// - `workSpace`: Input. Data pointer to GPU memory to a workspace needed to be able to execute the specified algorithm. If no workspace is needed for a particular algorithm, that pointer can be `NIL`.
    /// - `workSpaceSizeInBytes`: Input. Specifies the size in bytes of the provided `workSpace`.
    /// - `dwDesc`: Input. Handle to a previously initialized filter gradient descriptor. For more information, refer to [`cudnnFilterDescriptor_t`].
    /// - `dw`: Input/output. Data pointer to GPU memory associated with the filter gradient descriptor `dwDesc` that carries the result.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * At least one of the following is `NULL`: `handle`, `xDesc`, `dyDesc`, `convDesc`, `dwDesc`, `xData`, `dyData`, `dwData`, `alpha`, or `beta`
    /// * `xDesc` and `dyDesc` have a non-matching number of dimensions
    /// * `xDesc` and `dwDesc` have a non-matching number of dimensions
    /// * `xDesc` has fewer than three number of dimensions
    /// * `xDesc`, `dyDesc`, and `dwDesc` have a non-matching data type
    /// * `xDesc` and `dwDesc` have a non-matching number of input feature maps per image (or group in case of grouped convolutions)
    /// * `yDesc` or `dwDesc` indicate an output channel count that isn’t a multiple of group count (if group count has been set in convDesc).
    /// - [`cudnnStatus_t::CUDNN_STATUS_EXECUTION_FAILED`]: The function failed to launch on the GPU.
    /// - `CUDNN_STATUS_MAPPING_ERROR`: An error occurs during the texture object creation associated with the filter data.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: At least one of the following conditions are met:
    ///
    /// ```text
    /// * `xDesc` or `dyDesc` have negative tensor striding
    /// * `xDesc`, `dyDesc`, or `dwDesc` has a number of dimensions that is not `4` or `5`
    /// * The chosen `algo` does not support the parameters provided; see above for an exhaustive list of parameter support for each `algo`
    /// ```
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The operation was launched successfully.
    #[deprecated]
    pub fn cudnnConvolutionBackwardFilter(
        handle: cudnnHandle_t,
        alpha: *const ::core::ffi::c_void,
        xDesc: cudnnTensorDescriptor_t,
        x: *const ::core::ffi::c_void,
        dyDesc: cudnnTensorDescriptor_t,
        dy: *const ::core::ffi::c_void,
        convDesc: cudnnConvolutionDescriptor_t,
        algo: cudnnConvolutionBwdFilterAlgo_t,
        workSpace: *mut ::core::ffi::c_void,
        workSpaceSizeInBytes: size_t,
        beta: *const ::core::ffi::c_void,
        dwDesc: cudnnFilterDescriptor_t,
        dw: *mut ::core::ffi::c_void,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function computes the convolution function gradient with respect to the bias, which is the sum of every element belonging to the same feature map across all of the images of the input tensor. Therefore, the number of elements produced is equal to the number of features maps of the input tensor.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Handle to a previously created cuDNN context. For more information, refer to [`cudnnHandle_t`].
    /// - `dyDesc`: Input. Handle to the previously initialized input tensor descriptor. For more information, refer to [`cudnnTensorDescriptor_t`].
    /// - `dy`: Input. Data pointer to GPU memory associated with the tensor descriptor `dyDesc`.
    /// - `dbDesc`: Input. Handle to the previously initialized output tensor descriptor.
    /// - `db`: Output. Data pointer to GPU memory associated with the output tensor descriptor `dbDesc`.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: At least one of the following conditions are met:
    ///
    /// * One of the parameters `n`, `height`, or `width` of the output tensor is not `1`.
    /// * The numbers of feature maps of the input tensor and output tensor differ.
    /// * The `dataType` of the two tensor descriptors is different.
    /// - [`cudnnStatus_t::CUDNN_STATUS_NOT_SUPPORTED`]: The function does not support the provided configuration.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The operation was launched successfully.
    #[deprecated]
    pub fn cudnnConvolutionBackwardBias(
        handle: cudnnHandle_t,
        alpha: *const ::core::ffi::c_void,
        dyDesc: cudnnTensorDescriptor_t,
        dy: *const ::core::ffi::c_void,
        beta: *const ::core::ffi::c_void,
        dbDesc: cudnnTensorDescriptor_t,
        db: *mut ::core::ffi::c_void,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function creates an opaque structure to store the various problem size information, such as the shape, layout and the type of tensors, and the descriptors for convolution and activation, for the selected sequence of `cudnnFusedOps` computations.
    ///
    /// # Parameters
    ///
    /// - `constPack`: Input. The opaque structure that is created by this function. For more information, refer to [`cudnnFusedOpsConstParamPack_t`].
    /// - `ops`: Input. The specific sequence of computations to perform in the `cudnnFusedOps` computations, as defined in the enumerant type [`cudnnFusedOps_t`].
    ///
    /// # Return value
    ///
    /// - `CUDNN_STATUS_ALLOC_FAILED`: The resources could not be allocated.
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: If either `constPack` or `ops` is `NULL`.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: If the descriptor is created successfully.
    #[deprecated]
    pub fn cudnnCreateFusedOpsConstParamPack(
        constPack: *mut cudnnFusedOpsConstParamPack_t,
        ops: cudnnFusedOps_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function destroys a previously-created [`cudnnFusedOpsConstParamPack_t`] structure.
    ///
    /// # Parameters
    ///
    /// - `constPack`: Input. The [`cudnnFusedOpsConstParamPack_t`] structure that should be destroyed.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_INTERNAL_ERROR`]: The `ops` enum value is either not supported or is invalid.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The descriptor was destroyed successfully.
    #[deprecated]
    pub fn cudnnDestroyFusedOpsConstParamPack(
        constPack: cudnnFusedOpsConstParamPack_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function sets the descriptor pointed to by the `param` pointer input. The type of the descriptor to be set is indicated by the enum value of the `paramLabel` input.
    ///
    /// # Parameters
    ///
    /// - `constPack`: Input. The opaque [`cudnnFusedOpsConstParamPack_t`] structure that contains the various problem size information, such as the shape, layout and the type of tensors, the descriptors for convolution and activation, and settings for operations such as convolution and activation.
    /// - `paramLabel`: Input. Several types of descriptors can be set by this setter function. The `param` input points to the descriptor itself, and this input indicates the type of the descriptor pointed to by the `param` input. The [`cudnnFusedOpsConstParamPack_t`] enumerant type enables the selection of the type of the descriptor.
    /// - `param`: Input. Data pointer to the host memory, associated with the specific descriptor. The type of the descriptor depends on the value of `paramLabel`. For more information, refer to the table in [`cudnnFusedOpsConstParamPack_t`].
    ///
    /// If this pointer is set to `NULL`, then the cuDNN library will record as such. If not, then the values pointed to by this pointer (meaning, the value or the opaque structure underneath) will be copied into the `constPack` during [`cudnnSetFusedOpsConstParamPackAttribute`] operation.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: If `constPack` is `NULL`, or if `paramLabel` or the ops setting for `constPack` is invalid.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The descriptor is set successfully.
    #[deprecated]
    pub fn cudnnSetFusedOpsConstParamPackAttribute(
        constPack: cudnnFusedOpsConstParamPack_t,
        paramLabel: cudnnFusedOpsConstParamLabel_t,
        param: *const ::core::ffi::c_void,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function retrieves the values of the descriptor pointed to by the `param` pointer input. The type of the descriptor is indicated by the enum value of `paramLabel` input.
    ///
    /// # Parameters
    ///
    /// - `constPack`: Input. The opaque [`cudnnFusedOpsConstParamPack_t`] structure that contains the various problem size information, such as the shape, layout, and the type of tensors, and the descriptors for convolution and activation, for the selected sequence of [`cudnnFusedOps_t`] computations.
    /// - `paramLabel`: Input. Several types of descriptors can be retrieved by this getter function. The `param` input points to the descriptor itself, and this input indicates the type of the descriptor pointed to by the `param` input. The [`cudnnFusedOpsConstParamLabel_t`] enumerant type enables the selection of the type of the descriptor. Refer to the `param` description below.
    /// - `param`: Input. Data pointer to the host memory associated with the descriptor that should be retrieved. The type of this descriptor depends on the value of `paramLabel`. For the given `paramLabel`, if the associated value inside the `constPack` is set to `NULL` or by default `NULL`, then cuDNN will copy the value or the opaque structure in the `constPack` to the host memory buffer pointed to by `param`. For more information, refer to the table in [`cudnnFusedOpsConstParamLabel_t`].
    /// - `isNULL`: Input/output. Users must pass a pointer to an integer in the host memory in this field. If the value in the `constPack` associated with the given `paramLabel` is by default `NULL` or previously set by the user to `NULL`, then cuDNN will write a non-zero value to the location pointed by `isNULL`.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: If either `constPack`, `param`, or `isNULL` is `NULL`; or if `paramLabel` is invalid.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The descriptor values are retrieved successfully.
    pub fn cudnnGetFusedOpsConstParamPackAttribute(
        constPack: cudnnFusedOpsConstParamPack_t,
        paramLabel: cudnnFusedOpsConstParamLabel_t,
        param: *mut ::core::ffi::c_void,
        isNULL: *mut ::core::ffi::c_int,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function creates the variant pack descriptor for the `cudnnFusedOps` computation.
    ///
    /// # Parameters
    ///
    /// - `varPack`: Input. Pointer to the descriptor created by this function. For more information, refer to [`cudnnFusedOpsVariantParamPack_t`].
    /// - `ops`: Input. The specific sequence of fused operations computations for which this descriptor should be created.
    ///
    /// # Return value
    ///
    /// - `CUDNN_STATUS_ALLOC_FAILED`: The resources could not be allocated.
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: If any input is invalid.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The descriptor was destroyed successfully.
    #[deprecated]
    pub fn cudnnCreateFusedOpsVariantParamPack(
        varPack: *mut cudnnFusedOpsVariantParamPack_t,
        ops: cudnnFusedOps_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function destroys a previously-created descriptor for `cudnnFusedOps` constant parameters.
    ///
    /// # Parameters
    ///
    /// - `varPack`: Input. The descriptor that should be destroyed.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The descriptor was successfully destroyed.
    #[deprecated]
    pub fn cudnnDestroyFusedOpsVariantParamPack(
        varPack: cudnnFusedOpsVariantParamPack_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function sets the variable parameter pack descriptor.
    ///
    /// Footnotes.
    ///
    /// # Parameters
    ///
    /// - `varPack`: Input. Pointer to the `cudnnFusedOps` variant parameter pack (`varPack`) descriptor.
    /// - `paramLabel`: Input. Type to which the buffer pointer parameter (in the `varPack` descriptor) is set by this function. For more information, refer to [`cudnnFusedOpsConstParamLabel_t`].
    /// - `ptr`: Input. Pointer to the host or device memory, to the value to which the descriptor parameter is set. The data type of the pointer, and the host/device memory location, depend on the `paramLabel` input selection. For more information, refer to [`cudnnFusedOpsVariantParamLabel_t`].
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: If `varPack` is `NULL` or if `paramLabel` is set to an unsupported value.
    ///
    /// [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]
    /// The descriptor is set successfully.
    #[deprecated]
    pub fn cudnnSetFusedOpsVariantParamPackAttribute(
        varPack: cudnnFusedOpsVariantParamPack_t,
        paramLabel: cudnnFusedOpsVariantParamLabel_t,
        ptr: *mut ::core::ffi::c_void,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function retrieves the settings of the variable parameter pack descriptor.
    ///
    /// # Parameters
    ///
    /// - `varPack`: Input. Pointer to the `cudnnFusedOps` variant parameter pack (`varPack`) descriptor.
    /// - `paramLabel`: Input. Type of the buffer pointer parameter (in the `varPack` descriptor). For more information, refer to [`cudnnFusedOpsConstParamLabel_t`]. The retrieved descriptor values vary according to this type.
    /// - `ptr`: Output. Pointer to the host or device memory where the retrieved value is written by this function. The data type of the pointer, and the host/device memory location, depend on the `paramLabel` input selection. For more information, refer to [`cudnnFusedOpsVariantParamLabel_t`].
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: If either `varPack` or `ptr` is `NULL`, or if `paramLabel` is set to invalid value.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The descriptor values are retrieved successfully.
    pub fn cudnnGetFusedOpsVariantParamPackAttribute(
        varPack: cudnnFusedOpsVariantParamPack_t,
        paramLabel: cudnnFusedOpsVariantParamLabel_t,
        ptr: *mut ::core::ffi::c_void,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function creates the plan descriptor for the `cudnnFusedOps` computation. This descriptor contains the plan information, including the problem type and size, which kernels should be run, and the internal workspace partition.
    ///
    /// # Parameters
    ///
    /// - `plan`: Input. A pointer to the instance of the descriptor created by this function.
    /// - `ops`: Input. The specific sequence of fused operations computations for which this plan descriptor should be created. For more information, refer to [`cudnnFusedOps_t`].
    ///
    /// # Return value
    ///
    /// - `CUDNN_STATUS_ALLOC_FAILED`: The resources could not be allocated.
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: If either the input `*plan` is `NULL` or the `ops` input is not a valid `cudnnFusedOp` enum.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The plan descriptor is created successfully.
    #[deprecated]
    pub fn cudnnCreateFusedOpsPlan(
        plan: *mut cudnnFusedOpsPlan_t,
        ops: cudnnFusedOps_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function destroys the plan descriptor provided.
    ///
    /// # Parameters
    ///
    /// - `plan`: Input. The descriptor that should be destroyed by this function.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: Either the plan descriptor is `NULL` or the descriptor was successfully destroyed.
    #[deprecated]
    pub fn cudnnDestroyFusedOpsPlan(plan: cudnnFusedOpsPlan_t) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function determines the optimum kernel to execute, and the workspace size the user should allocate, prior to the actual execution of the fused operations by [`cudnnFusedOpsExecute`].
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Pointer to the cuDNN library context.
    /// - `plan`: Input. Pointer to a previously-created and initialized plan descriptor.
    /// - `constPack`: Input. Pointer to the descriptor to the const parameters pack.
    /// - `workspaceSizeInBytes`: Output. The amount of workspace size the user should allocate for the execution of this plan.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: If any of the inputs is `NULL`, or if the type of [`cudnnFusedOps_t`] in the `constPack` descriptor is unsupported.
    /// - [`cudnnStatus_t::CUDNN_STATUS_SUCCESS`]: The function executed successfully.
    #[deprecated]
    pub fn cudnnMakeFusedOpsPlan(
        handle: cudnnHandle_t,
        plan: cudnnFusedOpsPlan_t,
        constPack: cudnnFusedOpsConstParamPack_t,
        workspaceSizeInBytes: *mut size_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    /// This function executes the sequence of `cudnnFusedOps` operations.
    ///
    /// # Parameters
    ///
    /// - `handle`: Input. Pointer to the cuDNN library context.
    /// - `plan`: Input. Pointer to a previously-created and initialized plan descriptor.
    /// - `varPack`: Input. Pointer to the descriptor to the variant parameters pack.
    ///
    /// # Return value
    ///
    /// - [`cudnnStatus_t::CUDNN_STATUS_BAD_PARAM`]: If the type of [`cudnnFusedOps_t`] in the plan descriptor is unsupported.
    pub fn cudnnFusedOpsExecute(
        handle: cudnnHandle_t,
        plan: cudnnFusedOpsPlan_t,
        varPack: cudnnFusedOpsVariantParamPack_t,
    ) -> cudnnStatus_t;
}
#[repr(u32)]
#[derive(Debug, Copy, Clone, Hash, PartialOrd, Ord, PartialEq, Eq)]
pub enum cudnnCausalConv1dActivation_t {
    CUDNN_CAUSAL_CONV1D_ACTIVATION_IDENTITY = 0,
    CUDNN_CAUSAL_CONV1D_ACTIVATION_SILU = 1,
}
unsafe extern "C" {
    pub fn cudnnSubquadraticOpsVersionCheck() -> cudnnStatus_t;
}
unsafe extern "C" {
    pub fn cudnnCausalConv1dForward(
        stream: cudaStream_t,
        x: *const ::core::ffi::c_void,
        weight: *const ::core::ffi::c_void,
        bias: *const ::core::ffi::c_void,
        y: *mut ::core::ffi::c_void,
        batch: ::core::ffi::c_int,
        dim: ::core::ffi::c_int,
        seqLen: ::core::ffi::c_int,
        kernelSize: ::core::ffi::c_int,
        dataType: cudnnDataType_t,
        activation: cudnnCausalConv1dActivation_t,
    ) -> cudnnStatus_t;
}
unsafe extern "C" {
    pub fn cudnnCausalConv1dBackward(
        stream: cudaStream_t,
        x: *const ::core::ffi::c_void,
        weight: *const ::core::ffi::c_void,
        bias: *const ::core::ffi::c_void,
        dy: *const ::core::ffi::c_void,
        dx: *mut ::core::ffi::c_void,
        dweight: *mut ::core::ffi::c_void,
        dbias: *mut ::core::ffi::c_void,
        batch: ::core::ffi::c_int,
        dim: ::core::ffi::c_int,
        seqLen: ::core::ffi::c_int,
        kernelSize: ::core::ffi::c_int,
        dataType: cudnnDataType_t,
        dwDataType: cudnnDataType_t,
        activation: cudnnCausalConv1dActivation_t,
    ) -> cudnnStatus_t;
}
