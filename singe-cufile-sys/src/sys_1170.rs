/* automatically generated by rust-bindgen 0.72.1 */

pub const CUFILEOP_BASE_ERR: u32 = 5000;
pub const CU_FILE_RDMA_REGISTER: u32 = 1;
pub const CU_FILE_RDMA_RELAXED_ORDERING: u32 = 2;
pub const CU_FILE_STREAM_FIXED_BUF_OFFSET: u32 = 1;
pub const CU_FILE_STREAM_FIXED_FILE_OFFSET: u32 = 2;
pub const CU_FILE_STREAM_FIXED_FILE_SIZE: u32 = 4;
pub const CU_FILE_STREAM_PAGE_ALIGNED_INPUTS: u32 = 8;
pub const CUFILE_GPU_UUID_LEN: u32 = 16;
pub type size_t = ::core::ffi::c_ulong;
pub type __off_t = ::core::ffi::c_long;
pub type __off64_t = ::core::ffi::c_long;
pub type __time_t = ::core::ffi::c_long;
pub type __ssize_t = ::core::ffi::c_long;
pub type __syscall_slong_t = ::core::ffi::c_long;
pub type __loff_t = __off64_t;
pub type loff_t = __loff_t;
pub type off_t = __off_t;
pub type ssize_t = __ssize_t;
#[repr(C)]
#[derive(Debug, Default, Copy, Clone, Hash, PartialOrd, Ord, PartialEq, Eq)]
pub struct timespec {
    pub tv_sec: __time_t,
    pub tv_nsec: __syscall_slong_t,
}
pub type sa_family_t = ::core::ffi::c_ushort;
#[repr(C)]
#[derive(Debug, Default, Copy, Clone, Hash, PartialOrd, Ord, PartialEq, Eq)]
pub struct sockaddr {
    pub sa_family: sa_family_t,
    pub sa_data: [::core::ffi::c_char; 14usize],
}
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum CUfileOpError {
    CU_FILE_SUCCESS = 0,
    CU_FILE_DRIVER_NOT_INITIALIZED = 5001,
    CU_FILE_DRIVER_INVALID_PROPS = 5002,
    CU_FILE_DRIVER_UNSUPPORTED_LIMIT = 5003,
    CU_FILE_DRIVER_VERSION_MISMATCH = 5004,
    CU_FILE_DRIVER_VERSION_READ_ERROR = 5005,
    CU_FILE_DRIVER_CLOSING = 5006,
    CU_FILE_PLATFORM_NOT_SUPPORTED = 5007,
    CU_FILE_IO_NOT_SUPPORTED = 5008,
    CU_FILE_DEVICE_NOT_SUPPORTED = 5009,
    CU_FILE_NVFS_DRIVER_ERROR = 5010,
    CU_FILE_CUDA_DRIVER_ERROR = 5011,
    CU_FILE_CUDA_POINTER_INVALID = 5012,
    CU_FILE_CUDA_MEMORY_TYPE_INVALID = 5013,
    CU_FILE_CUDA_POINTER_RANGE_ERROR = 5014,
    CU_FILE_CUDA_CONTEXT_MISMATCH = 5015,
    CU_FILE_INVALID_MAPPING_SIZE = 5016,
    CU_FILE_INVALID_MAPPING_RANGE = 5017,
    CU_FILE_INVALID_FILE_TYPE = 5018,
    CU_FILE_INVALID_FILE_OPEN_FLAG = 5019,
    CU_FILE_DIO_NOT_SET = 5020,
    CU_FILE_INVALID_VALUE = 5022,
    CU_FILE_MEMORY_ALREADY_REGISTERED = 5023,
    CU_FILE_MEMORY_NOT_REGISTERED = 5024,
    CU_FILE_PERMISSION_DENIED = 5025,
    CU_FILE_DRIVER_ALREADY_OPEN = 5026,
    CU_FILE_HANDLE_NOT_REGISTERED = 5027,
    CU_FILE_HANDLE_ALREADY_REGISTERED = 5028,
    CU_FILE_DEVICE_NOT_FOUND = 5029,
    CU_FILE_INTERNAL_ERROR = 5030,
    CU_FILE_GETNEWFD_FAILED = 5031,
    CU_FILE_NVFS_SETUP_ERROR = 5033,
    CU_FILE_IO_DISABLED = 5034,
    CU_FILE_BATCH_SUBMIT_FAILED = 5035,
    CU_FILE_GPU_MEMORY_PINNING_FAILED = 5036,
    CU_FILE_BATCH_FULL = 5037,
    CU_FILE_ASYNC_NOT_SUPPORTED = 5038,
    CU_FILE_INTERNAL_BATCH_SETUP_ERROR = 5039,
    CU_FILE_INTERNAL_BATCH_SUBMIT_ERROR = 5040,
    CU_FILE_INTERNAL_BATCH_GETSTATUS_ERROR = 5041,
    CU_FILE_INTERNAL_BATCH_CANCEL_ERROR = 5042,
    CU_FILE_NOMEM_ERROR = 5043,
    CU_FILE_IO_ERROR = 5044,
    CU_FILE_INTERNAL_BUF_REGISTER_ERROR = 5045,
    CU_FILE_HASH_OPR_ERROR = 5046,
    CU_FILE_INVALID_CONTEXT_ERROR = 5047,
    CU_FILE_NVFS_INTERNAL_DRIVER_ERROR = 5048,
    CU_FILE_BATCH_NOCOMPAT_ERROR = 5049,
    CU_FILE_IO_MAX_ERROR = 5050,
}
#[repr(C)]
pub struct CUfileError {
    pub err: CUfileOpError,
    pub cu_err: CUresult,
}
impl Default for CUfileError {
    fn default() -> Self {
        let mut s = ::core::mem::MaybeUninit::<Self>::uninit();
        unsafe {
            ::core::ptr::write_bytes(s.as_mut_ptr(), 0, 1);
            s.assume_init()
        }
    }
}
pub type CUfileError_t = CUfileError;
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum CUfileDriverStatusFlags {
    CU_FILE_LUSTRE_SUPPORTED = 0,
    CU_FILE_WEKAFS_SUPPORTED = 1,
    CU_FILE_NFS_SUPPORTED = 2,
    CU_FILE_GPFS_SUPPORTED = 3,
    CU_FILE_NVME_SUPPORTED = 4,
    CU_FILE_NVMEOF_SUPPORTED = 5,
    CU_FILE_SCSI_SUPPORTED = 6,
    CU_FILE_SCALEFLUX_CSD_SUPPORTED = 7,
    CU_FILE_NVMESH_SUPPORTED = 8,
    CU_FILE_BEEGFS_SUPPORTED = 9,
    CU_FILE_NVME_P2P_SUPPORTED = 11,
    CU_FILE_SCATEFS_SUPPORTED = 12,
    CU_FILE_VIRTIOFS_SUPPORTED = 13,
    CU_FILE_MAX_TARGET_TYPES = 14,
}
pub use self::CUfileDriverStatusFlags as CUfileDriverStatusFlags_t;
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum CUfileDriverControlFlags {
    CU_FILE_USE_POLL_MODE = 0,
    CU_FILE_ALLOW_COMPAT_MODE = 1,
    CU_FILE_POSIX_IO_MODE = 2,
    CU_FILE_FALLBACK_IO_MODE = 4,
}
pub use self::CUfileDriverControlFlags as CUfileDriverControlFlags_t;
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum CUfileFeatureFlags {
    CU_FILE_DYN_ROUTING_SUPPORTED = 0,
    CU_FILE_BATCH_IO_SUPPORTED = 1,
    CU_FILE_STREAMS_SUPPORTED = 2,
    CU_FILE_PARALLEL_IO_SUPPORTED = 3,
    CU_FILE_P2P_SUPPORTED = 4,
}
pub use self::CUfileFeatureFlags as CUfileFeatureFlags_t;
impl CUfileP2PFlags {
    pub const CUFILE_P2PDMA: CUfileP2PFlags = CUfileP2PFlags(0);
    pub const CUFILE_NVFS: CUfileP2PFlags = CUfileP2PFlags(1);
    pub const CUFILE_DMABUF: CUfileP2PFlags = CUfileP2PFlags(2);
    pub const CUFILE_C2C: CUfileP2PFlags = CUfileP2PFlags(3);
    pub const CUFILE_NVIDIA_PEERMEM: CUfileP2PFlags = CUfileP2PFlags(4);
}
#[repr(transparent)]
#[derive(Debug, Copy, Clone, Hash, PartialOrd, Ord, PartialEq, Eq)]
pub struct CUfileP2PFlags(pub ::core::ffi::c_uint);
pub use self::CUfileP2PFlags as CUfileP2PFlags_t;
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum CUfileOpcode {
    CUFILE_READ = 0,
    CUFILE_WRITE = 1,
}
pub use self::CUfileOpcode as CUfileOpcode_t;
#[repr(C)]
#[derive(Debug, Default, Copy, Clone, Hash, PartialOrd, Ord, PartialEq, Eq)]
pub struct CUfileDrvProps {
    pub nvfs: CUfileDrvProps__bindgen_ty_1,
    pub fflags: ::core::ffi::c_uint,
    pub max_device_cache_size: ::core::ffi::c_uint,
    pub per_buffer_cache_size: ::core::ffi::c_uint,
    pub max_device_pinned_mem_size: ::core::ffi::c_uint,
    pub max_batch_io_size: ::core::ffi::c_uint,
    pub max_batch_io_timeout_msecs: ::core::ffi::c_uint,
}
#[repr(C)]
#[derive(Debug, Default, Copy, Clone, Hash, PartialOrd, Ord, PartialEq, Eq)]
pub struct CUfileDrvProps__bindgen_ty_1 {
    pub major_version: ::core::ffi::c_uint,
    pub minor_version: ::core::ffi::c_uint,
    pub poll_thresh_size: size_t,
    pub max_direct_io_size: size_t,
    pub dstatusflags: ::core::ffi::c_uint,
    pub dcontrolflags: ::core::ffi::c_uint,
}
pub type CUfileDrvProps_t = CUfileDrvProps;
pub type sockaddr_t = sockaddr;
#[repr(C)]
#[derive(Debug, Copy, Clone, Hash, PartialOrd, Ord, PartialEq, Eq)]
pub struct cufileRDMAInfo {
    pub version: ::core::ffi::c_int,
    pub desc_len: ::core::ffi::c_int,
    pub desc_str: *const ::core::ffi::c_char,
}
impl Default for cufileRDMAInfo {
    fn default() -> Self {
        let mut s = ::core::mem::MaybeUninit::<Self>::uninit();
        unsafe {
            ::core::ptr::write_bytes(s.as_mut_ptr(), 0, 1);
            s.assume_init()
        }
    }
}
pub type cufileRDMAInfo_t = cufileRDMAInfo;
#[repr(C)]
#[derive(Debug, Default, Copy, Clone, Hash, PartialOrd, Ord, PartialEq, Eq)]
pub struct CUfileFSOps {
    pub fs_type: ::core::option::Option<
        unsafe extern "C" fn(
            handle: *const ::core::ffi::c_void,
        ) -> *const ::core::ffi::c_char,
    >,
    pub getRDMADeviceList: ::core::option::Option<
        unsafe extern "C" fn(
            handle: *const ::core::ffi::c_void,
            hostaddrs: *mut *mut sockaddr_t,
        ) -> ::core::ffi::c_int,
    >,
    pub getRDMADevicePriority: ::core::option::Option<
        unsafe extern "C" fn(
            handle: *const ::core::ffi::c_void,
            arg1: *mut ::core::ffi::c_char,
            arg2: size_t,
            arg3: loff_t,
            hostaddr: *const sockaddr_t,
        ) -> ::core::ffi::c_int,
    >,
    pub read: ::core::option::Option<
        unsafe extern "C" fn(
            handle: *const ::core::ffi::c_void,
            arg1: *mut ::core::ffi::c_char,
            arg2: size_t,
            arg3: loff_t,
            arg4: *const cufileRDMAInfo_t,
        ) -> ssize_t,
    >,
    pub write: ::core::option::Option<
        unsafe extern "C" fn(
            handle: *const ::core::ffi::c_void,
            arg1: *const ::core::ffi::c_char,
            arg2: size_t,
            arg3: loff_t,
            arg4: *const cufileRDMAInfo_t,
        ) -> ssize_t,
    >,
}
pub type CUfileFSOps_t = CUfileFSOps;
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum CUfileFileHandleType {
    CU_FILE_HANDLE_TYPE_OPAQUE_FD = 1,
    CU_FILE_HANDLE_TYPE_OPAQUE_WIN32 = 2,
    CU_FILE_HANDLE_TYPE_USERSPACE_FS = 3,
}
#[repr(C)]
#[derive(Copy, Clone)]
pub struct CUfileDescr_t {
    pub type_: CUfileFileHandleType,
    pub handle: CUfileDescr_t__bindgen_ty_1,
    pub fs_ops: *const CUfileFSOps_t,
}
#[repr(C)]
#[derive(Copy, Clone)]
pub union CUfileDescr_t__bindgen_ty_1 {
    pub fd: ::core::ffi::c_int,
    pub handle: *mut ::core::ffi::c_void,
}
impl Default for CUfileDescr_t__bindgen_ty_1 {
    fn default() -> Self {
        let mut s = ::core::mem::MaybeUninit::<Self>::uninit();
        unsafe {
            ::core::ptr::write_bytes(s.as_mut_ptr(), 0, 1);
            s.assume_init()
        }
    }
}
impl Default for CUfileDescr_t {
    fn default() -> Self {
        let mut s = ::core::mem::MaybeUninit::<Self>::uninit();
        unsafe {
            ::core::ptr::write_bytes(s.as_mut_ptr(), 0, 1);
            s.assume_init()
        }
    }
}
pub type CUfileHandle_t = *mut ::core::ffi::c_void;
unsafe extern "C" {
    /// Register an open file.
    /// * [`cuFileHandleRegister`] is required and performs extra checking that is memoized to provide increased
    ///   performance on later cuFile operations.
    /// * This API is OS agnostic.
    ///
    /// > Note
    /// >
    /// > CUDA toolkit 12.2 (GDS version 1.7.x) supports non O_DIRECT open flags as well as O_DIRECT. Application
    /// > is allowed to open a file in non O_DIRECT mode in compat mode and also with nvidia-fs.ko installed. In the latter case, an O_DIRECT path between GPU and Storage will be used if such a path exists.
    ///
    /// * Given a file-descriptor will populate and return the [`CUfileHandle_t`] needed for issuing IO with cuFile APIs.
    /// * A return value of anything other than CU_FILE_SUCCESS leaves fh in an undefined state but has no other side effects.
    /// * By default this API accepts whether the file descriptor is opened with O_DIRECT mode or non O_DIRECT mode.
    ///
    /// Refer to the following for more information:
    ///
    /// * [`cuFileRead`]
    /// * [`cuFileWrite`]
    /// * [`cuFileReadAsync`]
    /// * [`cuFileWriteAsync`]
    /// * [`cuFileHandleDeregister`].
    ///
    /// # Parameters
    ///
    /// - `fh`: Valid pointer to the OS-neutral cuFile handle structure supplied by the user but populated and maintained
    /// by the cuFile runtime.
    ///
    /// # Return value
    ///
    /// - [`CUfileOpError::CU_FILE_DRIVER_NOT_INITIALIZED`]: [`CUfileOpError::CU_FILE_DRIVER_NOT_INITIALIZED`] on failure to load the driver.
    /// - [`CUfileOpError::CU_FILE_HANDLE_ALREADY_REGISTERED`]: [`CUfileOpError::CU_FILE_HANDLE_ALREADY_REGISTERED`] if the file is already registered using the same file-descriptor.
    /// - [`CUfileOpError::CU_FILE_INVALID_FILE_OPEN_FLAG`]: [`CUfileOpError::CU_FILE_INVALID_FILE_OPEN_FLAG`], if the file is opened with unsupported modes such as no `O_APPEND`,
    ///   :   `O_NOCTTY`, `O_NONBLOCK`, `O_DIRECTORY`, `O_NOFOLLOW`, `O_NOATIME`, and `O_TMPFILE`.
    /// - [`CUfileOpError::CU_FILE_INVALID_FILE_TYPE`]: [`CUfileOpError::CU_FILE_INVALID_FILE_TYPE`], if the file path is not valid, not a regular file, not a symbolic link,
    ///   or not a device file.
    /// - [`CUfileOpError::CU_FILE_INVALID_VALUE`]: [`CUfileOpError::CU_FILE_INVALID_VALUE`] if there are null or bad API arguments.
    /// - [`CUfileOpError::CU_FILE_IO_NOT_SUPPORTED`]: [`CUfileOpError::CU_FILE_IO_NOT_SUPPORTED`], if the filesystem is not supported.
    /// - [`CUfileOpError::CU_FILE_SUCCESS`]: [`CUfileOpError::CU_FILE_SUCCESS`] on successful completion.
    pub fn cuFileHandleRegister(
        fh: *mut CUfileHandle_t,
        descr: *mut CUfileDescr_t,
    ) -> CUfileError_t;
}
unsafe extern "C" {
    /// The API is used to release resources that are claimed by [`cuFileHandleRegister`].
    ///
    ///   This API should be invoked only after the application ensures there are no outstanding IO operations
    ///   with the handle. If [`cuFileHandleDeregister`] is called while IO on the file is in progress might
    ///   result in undefined behavior.
    /// * The user is still expected to close the file descriptor outside the cuFile subsystem after calling
    ///   this API using `close` system call.
    ///
    ///   Closing a file handle without calling [`cuFileHandleDeregister`] does not release the resources that
    ///   are held in the cuFile library. If this API is not called, the cuFile subsystem releases the
    ///   resources lazily or when the application exits.
    ///
    /// Refer to the following for more information:
    ///
    /// * [`cuFileRead`]
    /// * [`cuFileWrite`]
    /// * [`cuFileHandleRegister`].
    ///
    /// # Parameters
    ///
    /// - `fh`: The file handle obtained from [`cuFileHandleRegister`].
    pub fn cuFileHandleDeregister(fh: CUfileHandle_t);
}
unsafe extern "C" {
    /// Based on the memory type, this API registers existing `cuMemAlloc`’d (pinned) memory for GDS IO
    ///   operations or host memory for IO operations.
    ///
    /// Based on the memory type, this API either registers the specified GPU address or host memory address and size
    /// for use with the [`cuFileRead`] and [`cuFileWrite`] operations. The user must call [`cuFileBufDeregister`] to
    /// release the pinned memory mappings for GPU memory if needed.
    ///
    /// See the following for more information:
    ///
    /// * [`cuFileBufDeregister`].
    ///
    /// # Parameters
    ///
    /// - `bufPtr_base`: Address of device pointer. [`cuFileRead`] and [`cuFileWrite`] **must** use this `bufPtr_base` as the base address.
    /// - `flags`: Reserved for future use; must be 0.
    ///
    /// # Return value
    ///
    /// - [`CUfileOpError::CU_FILE_CUDA_DRIVER_ERROR`]: [`CUfileOpError::CU_FILE_CUDA_DRIVER_ERROR`] on CUDA-specific errors. CUresult code can be obtained using `CU_FILE_CUDA_ERR` (err).
    /// - [`CUfileOpError::CU_FILE_CUDA_MEMORY_TYPE_INVALID`]: [`CUfileOpError::CU_FILE_CUDA_MEMORY_TYPE_INVALID`], for device memory that is not allocated via `cudaMalloc` or `cuMemAlloc`.
    /// - [`CUfileOpError::CU_FILE_CUDA_POINTER_RANGE_ERROR`]: [`CUfileOpError::CU_FILE_CUDA_POINTER_RANGE_ERROR`], if the size exceeds the bounds of the allocated memory.
    /// - [`CUfileOpError::CU_FILE_GPU_MEMORY_PINNING_FAILED`]: [`CUfileOpError::CU_FILE_GPU_MEMORY_PINNING_FAILED`], if not enough pinned memory is available.
    /// - [`CUfileOpError::CU_FILE_INTERNAL_ERROR`]: [`CUfileOpError::CU_FILE_INTERNAL_ERROR`], an internal library-specific error.
    /// - [`CUfileOpError::CU_FILE_INVALID_MAPPING_SIZE`]: [`CUfileOpError::CU_FILE_INVALID_MAPPING_SIZE`], if the size exceeds the GPU resource limits.
    /// - [`CUfileOpError::CU_FILE_INVALID_VALUE`]: [`CUfileOpError::CU_FILE_INVALID_VALUE`] on a failure.
    /// - [`CUfileOpError::CU_FILE_MEMORY_ALREADY_REGISTERED`]: [`CUfileOpError::CU_FILE_MEMORY_ALREADY_REGISTERED`], if memory is already registered.
    /// - [`CUfileOpError::CU_FILE_NVFS_DRIVER_ERROR`]: [`CUfileOpError::CU_FILE_NVFS_DRIVER_ERROR`] if the nvidia-fs driver cannot handle the request.
    /// - [`CUfileOpError::CU_FILE_SUCCESS`]: [`CUfileOpError::CU_FILE_SUCCESS`] on a successful registration.
    pub fn cuFileBufRegister(
        bufPtr_base: *const ::core::ffi::c_void,
        length: size_t,
        flags: ::core::ffi::c_int,
    ) -> CUfileError_t;
}
unsafe extern "C" {
    /// Based on the memory type, this API either deregisters CUDA memory or the host memory registered using
    ///   the [`cuFileBufRegister`] API.
    ///
    /// This API deregisters memory mappings that were registered by [`cuFileBufRegister`]. Refer to [`cuFileBufRegister`] for more information.
    ///
    /// # Parameters
    ///
    /// - `bufPtr_base`: Address of device pointer to release the mappings that were provided to [`cuFileBufRegister`].
    ///
    /// # Return value
    ///
    /// - `CU_FILE_ERROR_INVALID_VALUE`: `CU_FILE_ERROR_INVALID_VALUE` on failure to find the registration for the specified memory.
    /// - [`CUfileOpError::CU_FILE_INTERNAL_ERROR`]: [`CUfileOpError::CU_FILE_INTERNAL_ERROR`], an internal library-specific error.
    /// - [`CUfileOpError::CU_FILE_MEMORY_NOT_REGISTERED`]: [`CUfileOpError::CU_FILE_MEMORY_NOT_REGISTERED`], if `bufPtr_base` was not registered.
    /// - [`CUfileOpError::CU_FILE_SUCCESS`]: [`CUfileOpError::CU_FILE_SUCCESS`] on a successful deregistration.
    pub fn cuFileBufDeregister(bufPtr_base: *const ::core::ffi::c_void) -> CUfileError_t;
}
unsafe extern "C" {
    /// Reads specified bytes from the file descriptor into the device memory or the host memory.
    ///
    /// This API reads the data from a specified file handle at a specified offset and size bytes into the GPU memory by
    /// using GDS functionality or into the host memory based on the type of memory pointer. The API works correctly for
    /// unaligned offsets and any data size, although the performance might not match the performance of aligned reads.
    /// This is a synchronous call and blocks until the IO is complete.
    ///
    /// Note
    ///
    /// For the `bufPtr_offset`, if data will be read starting exactly from the `bufPtr_base` that is registered
    /// with [`cuFileBufRegister`], `bufPtr_offset` should be set to 0. To read starting from an offset in the
    /// registered buffer range, the relative offset should be specified in the `bufPtr_offset,` and the `bufPtr_base`
    /// must remain set to the base address that was used in the [`cuFileBufRegister`] call.
    ///
    /// See the following for more information:
    ///
    /// * [`cuFileWrite`]
    /// * [`cuFileReadAsync`]
    /// * [`cuFileWriteAsync`].
    ///
    /// # Parameters
    ///
    /// - `fh`: File descriptor for the file.
    /// - `bufPtr_base`: Base address of buffer in device memory or host memory. For registered buffers, `bufPtr_base` must
    /// remain set to the base address used in the [`cuFileBufRegister`] call.
    /// - `size`: Size in bytes to read.
    /// - `file_offset`: Offset in the file to read from.
    /// - `bufPtr_offset`: Offset relative to the `bufPtr_base` pointer to read into. This parameter should be used only with registered buffers.
    pub fn cuFileRead(
        fh: CUfileHandle_t,
        bufPtr_base: *mut ::core::ffi::c_void,
        size: size_t,
        file_offset: off_t,
        bufPtr_offset: off_t,
    ) -> ssize_t;
}
unsafe extern "C" {
    /// Writes specified bytes from the device memory into the file descriptor using GDS.
    ///
    /// This API writes the data from the GPU memory or the host memory to a file specified by the file handle
    /// at a specified offset and size bytes by using GDS functionality. The API works correctly for unaligned
    /// offset and data sizes, although the performance is not on-par with aligned writes.This is a synchronous
    /// call and will block until the IO is complete.
    ///
    /// Note
    ///
    /// GDS functionality modified the standard file system metadata in SysMem. However, GDS functionality
    /// does not take any special responsibility for writing that metadata back to permanent storage. The
    /// data is not guaranteed to be present after a system crash unless the application uses an explicit
    /// `fsync(2)` call. If the file is opened with an `O_SYNC` flag, the metadata will be written to
    /// the disk before the call is complete.
    ///
    /// Refer to the note in [`cuFileRead`] for more information about `bufPtr_offset:`.
    ///
    /// Refer to the following for more information:
    ///
    /// * [`cuFileWrite`]
    /// * [`cuFileReadAsync`]
    /// * [`cuFileWriteAsync`].
    ///
    /// # Parameters
    ///
    /// - `fh`: File descriptor for the file.
    /// - `bufPtr_base`: Base address of buffer in device memory or host memory. For registered buffers, `bufPtr_base` must
    /// remain set to the base address used in the [`cuFileBufRegister`] call.
    /// - `size`: Size in bytes to which to write.
    /// - `file_offset`: Offset in the file to which to write.
    /// - `bufPtr_offset`: Offset relative to the `bufPtr_base` pointer from which to write. This parameter should be used only with registered buffers.
    pub fn cuFileWrite(
        fh: CUfileHandle_t,
        bufPtr_base: *const ::core::ffi::c_void,
        size: size_t,
        file_offset: off_t,
        bufPtr_offset: off_t,
    ) -> ssize_t;
}
unsafe extern "C" {
    /// Opens the Driver session to support GDS IO operations.
    ///
    /// * This API opens the session with the NVFS kernel driver to communicate from userspace to kernel space and calls
    ///   the GDS driver to set up the resources required to support GDS IO operations.
    /// * The API checks whether the current platform supports GDS and initializes the cuFile library.
    /// * This API loads the cuFile settings from a JSON configuration file in `/etc/cufile.JSON`.
    ///
    ///   If the JSON configuration file does not exist, the API loads the default library settings. To modify this default
    ///   config file, administrative privileges are needed. The administrator can modify it to grant cuFile access to the
    ///   specified devices and mount paths and also tune IO parameters (in KB, 4K aligned) that are based on the type of
    ///   workload. Refer to the [default config file](https://docs.nvidia.com/gpudirect-storage/configuration-guide/index.html#gds-parameters)
    ///   (`/etc/cufile.json`) for more information.
    ///
    /// # Return value
    ///
    /// - [`CUfileOpError::CU_FILE_CUDA_DRIVER_ERROR`]: [`CUfileOpError::CU_FILE_CUDA_DRIVER_ERROR`] if the CUDA driver failed to initialize. [`CUfileOpError::CU_FILE_PLATFORM_NOT_SUPPORTED`] if the
    ///   current platform is not supported by GDS.
    /// - [`CUfileOpError::CU_FILE_DRIVER_NOT_INITIALIZED`]: [`CUfileOpError::CU_FILE_DRIVER_NOT_INITIALIZED`] on a failure to open the driver.
    /// - [`CUfileOpError::CU_FILE_DRIVER_VERSION_MISMATCH`]: [`CUfileOpError::CU_FILE_DRIVER_VERSION_MISMATCH`], when there is a mismatch between the cuFile library and its kernel driver.
    /// - [`CUfileOpError::CU_FILE_NVFS_SETUP_ERROR`]: [`CUfileOpError::CU_FILE_NVFS_SETUP_ERROR`] for a cuFile-specific internal error.
    /// - [`CUfileOpError::CU_FILE_PERMISSION_DENIED`]: [`CUfileOpError::CU_FILE_PERMISSION_DENIED`] on a failure to open.
    ///
    ///   This can happen when the character device (`/dev/nvidia_fs\[0-15\]`) is restricted to certain users by an administrator,
    ///   for example, admin, where `/dev` is not exposed with read permissions in the container.
    /// - [`CUfileOpError::CU_FILE_SUCCESS`]: [`CUfileOpError::CU_FILE_SUCCESS`] on a successful open, or if the driver is already open.
    pub fn cuFileDriverOpen() -> CUfileError_t;
}
unsafe extern "C" {
    /// Closes the driver session and frees any associated resources for GDS.
    /// * This happens implicitly upon process exit.
    /// * The driver can be reopened once it is closed.
    ///
    /// * Close the GDS session and any associated memory resources. If there are buffers registered by using
    ///   [`cuFileBufRegister`], which are not unregistered, a [`cuFileDriverClose`] implicitly unregisters those
    ///   buffers. Any in-flight IO when [`cuFileDriverClose`] is in-progress will receive an error.
    ///
    /// # Return value
    ///
    /// - [`CUfileOpError::CU_FILE_DRIVER_NOT_INITIALIZED`]: [`CUfileOpError::CU_FILE_DRIVER_NOT_INITIALIZED`] on failure.
    /// - [`CUfileOpError::CU_FILE_SUCCESS`]: [`CUfileOpError::CU_FILE_SUCCESS`] on a successful close.
    pub fn cuFileDriverClose() -> CUfileError_t;
}
unsafe extern "C" {
    /// Closes the driver session and frees any associated resources for GDS.
    /// * This happens implicitly upon process exit.
    /// * The driver can be reopened once it is closed.
    ///
    /// * Close the GDS session and any associated memory resources. If there are buffers registered by using
    ///   [`cuFileBufRegister`], which are not unregistered, a [`cuFileDriverClose`] implicitly unregisters those
    ///   buffers. Any in-flight IO when [`cuFileDriverClose`] is in-progress will receive an error.
    ///
    /// # Return value
    ///
    /// - [`CUfileOpError::CU_FILE_DRIVER_NOT_INITIALIZED`]: [`CUfileOpError::CU_FILE_DRIVER_NOT_INITIALIZED`] on failure.
    /// - [`CUfileOpError::CU_FILE_SUCCESS`]: [`CUfileOpError::CU_FILE_SUCCESS`] on a successful close.
    pub fn cuFileDriverClose_v2() -> CUfileError_t;
}
unsafe extern "C" {
    pub fn cuFileUseCount() -> ::core::ffi::c_long;
}
unsafe extern "C" {
    /// The `cuFileDrvProps_t` structure can be queried with [`cuFileDriverGetProperties`] and selectively
    /// modified with `cuFileDriverSetProperties`. The structure is self-describing, and its fields are
    /// consistent with the major and minor API version parameters.
    ///
    /// * Gets the Driver session properties for GDS functionality.
    ///
    /// This API is used to get current GDS properties and nvidia-fs driver properties and functionality, such as
    /// support for SCSI, NVMe, and NVMe-OF.
    ///
    /// This API is used to get the current `nvidia-fs` drivers-specific properties such as the following:
    ///
    /// * `major_version`: the cuFile major version
    /// * `minor_version`: the cuFile minor version
    /// * `props.nvfs.dstatusflags`, which are bit flags that indicate support for the following driver features:
    ///
    ///   + `CU_FILE_EXASCALER_SUPPORTED`, a bit to check whether the DDN EXAScaler parallel filesystem solutions
    /// (based on the Lustre filesystem) client supports GDS.
    ///   + [`CUfileDriverStatusFlags::CU_FILE_WEKAFS_SUPPORTED`], a bit to check whether WekaFS supports GDS.
    /// * `Props.nvfs.dcontrolflags`, which are bit flags that indicate the current activation for driver features:
    ///
    ///   + [`CUfileDriverControlFlags::CU_FILE_USE_POLL_MODE`], when bit is set, IO uses polling mode.
    ///   + [`CUfileDriverControlFlags::CU_FILE_ALLOW_COMPAT_MODE`], if the value is 1 compatible mode is set.
    ///   > Otherwise, the compatible mode is disabled.
    /// * `Props.fflags`, which are bit flags that indicate whether the following library features are supported:
    ///
    ///   + [`CUfileFeatureFlags::CU_FILE_STREAMS_SUPPORTED`], an attribute that checks whether CUDA-streams are supported.
    ///   + [`CUfileFeatureFlags::CU_FILE_DYN_ROUTING_SUPPORTED`], an attribute that checks whether dynamic routing feature is supported.
    /// * `Props.nvfs.poll_thresh_size`, a maximum IO size, in KB and must be 4K-aligned, that is used for the POLLING mode.
    /// * `Props.nvfs.max_direct_io_size`, a maximum GDS IO size, in KB and must be 4K-aligned, that is requested by the nvidia-fs
    ///   driver to the underlying filesystem.
    /// * `Props.max_device_cache_size`, a maximum GPU buffer space per device, in KB and must be 4K-aligned. Used internally,
    ///   for example, to handle unaligned IO and optimal IO path routing. This value might be rounded down to the nearest GPU page size.
    /// * `Props.max_device_pinned_mem_size`, a maximum buffer space, in KB and must be 4K-aligned, that is pinned and
    ///   mapped to the GPU BAR space. This might be rounded down to the nearest GPU page size.
    /// * `Props.per_buffer_cache_size`, a GPU bounce buffer size, in KB, used for internal pools.
    ///
    /// # Parameters
    ///
    /// - `props`: Pointer to the cuFile Driver properties.
    ///
    /// # Return value
    ///
    /// - [`CUfileOpError::CU_FILE_DRIVER_NOT_INITIALIZED`]: [`CUfileOpError::CU_FILE_DRIVER_NOT_INITIALIZED`] on failure.
    /// - [`CUfileOpError::CU_FILE_DRIVER_VERSION_MISMATCH`]: [`CUfileOpError::CU_FILE_DRIVER_VERSION_MISMATCH`] on a driver version mismatch.
    /// - [`CUfileOpError::CU_FILE_INVALID_VALUE`]: [`CUfileOpError::CU_FILE_INVALID_VALUE`] if input is invalid.
    /// - [`CUfileOpError::CU_FILE_SUCCESS`]: [`CUfileOpError::CU_FILE_SUCCESS`] on a successful completion.
    pub fn cuFileDriverGetProperties(props: *mut CUfileDrvProps_t) -> CUfileError_t;
}
unsafe extern "C" {
    /// `cuFileDriverSetPollMode(bool poll, size_t poll_threshold_size)` API
    ///
    /// * Sets whether the Read/Write APIs use polling to complete IO operations. If poll mode is enabled, an IO
    ///   size less than or equal to the threshold value is used for polling.
    /// * The `poll_threshold_size` must be 4K aligned.
    ///
    /// This API is used in conjunction with `cuFileGetDriverProperties`. This API is used to set whether the
    /// library should use polling and the maximum IO threshold size less than or equal to which it will poll.
    ///
    /// This API overrides the default value that may be set through the JSON configuration file using the
    /// config keys `properties.poll_mode` and `properties.poll_max_size_kb` for the current process.
    ///
    /// Refer to the following for more information:
    ///
    /// [`cuFileDriverGetProperties`].
    ///
    /// # Parameters
    ///
    /// - `poll`: Boolean to indicate whether to use the poll mode.
    /// - `poll_threshold_size`: IO size to use for POLLING mode in KB. The default value is 4KB.
    ///
    /// # Return value
    ///
    /// - [`CUfileOpError::CU_FILE_DRIVER_NOT_INITIALIZED`]: [`CUfileOpError::CU_FILE_DRIVER_NOT_INITIALIZED`] on failure to load the driver.
    /// - [`CUfileOpError::CU_FILE_DRIVER_UNSUPPORTED_LIMIT`]: [`CUfileOpError::CU_FILE_DRIVER_UNSUPPORTED_LIMIT`] on failure to set with valid threshold size.
    /// - [`CUfileOpError::CU_FILE_SUCCESS`]: [`CUfileOpError::CU_FILE_SUCCESS`] on a successful completion.
    pub fn cuFileDriverSetPollMode(
        poll: bool,
        poll_threshold_size: size_t,
    ) -> CUfileError_t;
}
unsafe extern "C" {
    /// Sets the max IO size, in KB.
    ///
    ///   This parameter is used by the nvidia-fs driver as the maximum IO chunk size in which IO is issued to the
    ///   underlying filesystem. In compatible mode, this is the maximum IO chunk size that the library uses to issue POSIX read/writes.
    /// * The max direct IO size must be 4K aligned.
    ///
    /// This API is used with `cuFileGetDriverProperties` and is used to set the maximum direct IO size used by
    /// the library to specify the nvidia-fs kernel driver the maximum chunk size in which the latter can issue
    /// IO to the underlying filesystem. In compatible mode, this is the maximum IO chunk size which the library
    /// uses for issuing POSIX read/writes. This parameter is dependent on the underlying GPU hardware and system memory.
    ///
    /// This API overrides the default value that might be set through the JSON configuration file by using the
    /// `properties.max_direct_io_size_kb` config key for the current process.
    ///
    /// Refer to the following for more information:
    ///
    /// * [`cuFileDriverGetProperties`].
    ///
    /// # Parameters
    ///
    /// - `max_direct_io_size`: The maximum allowed direct IO size in KB. The default value is 16384KB. This is because typically
    /// parallel-file systems perform better with bulk read/writes.
    ///
    /// # Return value
    ///
    /// - [`CUfileOpError::CU_FILE_DRIVER_NOT_INITIALIZED`]: [`CUfileOpError::CU_FILE_DRIVER_NOT_INITIALIZED`] on failure to load the driver.
    /// - [`CUfileOpError::CU_FILE_DRIVER_UNSUPPORTED_LIMIT`]: [`CUfileOpError::CU_FILE_DRIVER_UNSUPPORTED_LIMIT`] on failure to set with valid size.
    /// - [`CUfileOpError::CU_FILE_SUCCESS`]: [`CUfileOpError::CU_FILE_SUCCESS`] on successful completion.
    pub fn cuFileDriverSetMaxDirectIOSize(max_direct_io_size: size_t) -> CUfileError_t;
}
unsafe extern "C" {
    pub fn cuFileDriverSetMaxCacheSize(max_cache_size: size_t) -> CUfileError_t;
}
unsafe extern "C" {
    /// Sets the maximum GPU buffer space, in KB, that is pinned and mapped. This value might be rounded down to the nearest GPU page size.
    /// * The max pinned size must be 4K aligned.
    /// * The default value corresponds to the maximum `PinnedMemory` or the physical memory size of the device.
    /// * This API overrides the default value that may be set by the `properties.max_device_pinned_mem_size_kb` JSON config key for the current process.
    ///
    /// This API is used with `cuFileGetDriverProperties` and is used to set an upper limit on the maximum size of
    /// GPU memory that can be pinned and mapped and is dependent on the underlying GPU hardware and system memory.
    /// This API is related to [`cuFileBufRegister`], which is used to register GPU device memory. See
    /// [`cuFileDriverGetProperties`] for more information.
    ///
    /// # Return value
    ///
    /// - [`CUfileOpError::CU_FILE_DRIVER_NOT_INITIALIZED`]: [`CUfileOpError::CU_FILE_DRIVER_NOT_INITIALIZED`] on failure to load driver.
    /// - [`CUfileOpError::CU_FILE_DRIVER_UNSUPPORTED_LIMIT`]: [`CUfileOpError::CU_FILE_DRIVER_UNSUPPORTED_LIMIT`] on failure to set with valid size.
    /// - [`CUfileOpError::CU_FILE_SUCCESS`]: [`CUfileOpError::CU_FILE_SUCCESS`] on successful completion.
    pub fn cuFileDriverSetMaxPinnedMemSize(max_pinned_size: size_t) -> CUfileError_t;
}
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum CUFILEStatus_enum {
    CUFILE_WAITING = 1,
    CUFILE_PENDING = 2,
    CUFILE_INVALID = 4,
    CUFILE_CANCELED = 8,
    CUFILE_COMPLETE = 16,
    CUFILE_TIMEOUT = 32,
    CUFILE_FAILED = 64,
}
pub use self::CUFILEStatus_enum as CUfileStatus_t;
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum cufileBatchMode {
    CUFILE_BATCH = 1,
}
pub use self::cufileBatchMode as CUfileBatchMode_t;
#[repr(C)]
#[derive(Copy, Clone)]
pub struct CUfileIOParams {
    pub mode: CUfileBatchMode_t,
    pub u: CUfileIOParams__bindgen_ty_1,
    pub fh: CUfileHandle_t,
    pub opcode: CUfileOpcode_t,
    pub cookie: *mut ::core::ffi::c_void,
}
#[repr(C)]
#[derive(Copy, Clone)]
pub union CUfileIOParams__bindgen_ty_1 {
    pub batch: CUfileIOParams__bindgen_ty_1__bindgen_ty_1,
}
#[repr(C)]
#[derive(Debug, Copy, Clone, Hash, PartialOrd, Ord, PartialEq, Eq)]
pub struct CUfileIOParams__bindgen_ty_1__bindgen_ty_1 {
    pub devPtr_base: *mut ::core::ffi::c_void,
    pub file_offset: off_t,
    pub devPtr_offset: off_t,
    pub size: size_t,
}
impl Default for CUfileIOParams__bindgen_ty_1__bindgen_ty_1 {
    fn default() -> Self {
        let mut s = ::core::mem::MaybeUninit::<Self>::uninit();
        unsafe {
            ::core::ptr::write_bytes(s.as_mut_ptr(), 0, 1);
            s.assume_init()
        }
    }
}
impl Default for CUfileIOParams__bindgen_ty_1 {
    fn default() -> Self {
        let mut s = ::core::mem::MaybeUninit::<Self>::uninit();
        unsafe {
            ::core::ptr::write_bytes(s.as_mut_ptr(), 0, 1);
            s.assume_init()
        }
    }
}
impl Default for CUfileIOParams {
    fn default() -> Self {
        let mut s = ::core::mem::MaybeUninit::<Self>::uninit();
        unsafe {
            ::core::ptr::write_bytes(s.as_mut_ptr(), 0, 1);
            s.assume_init()
        }
    }
}
pub type CUfileIOParams_t = CUfileIOParams;
#[repr(C)]
#[derive(Debug, Copy, Clone, Hash, PartialOrd, Ord, PartialEq, Eq)]
pub struct CUfileIOEvents {
    pub cookie: *mut ::core::ffi::c_void,
    pub status: CUfileStatus_t,
    pub ret: size_t,
}
impl Default for CUfileIOEvents {
    fn default() -> Self {
        let mut s = ::core::mem::MaybeUninit::<Self>::uninit();
        unsafe {
            ::core::ptr::write_bytes(s.as_mut_ptr(), 0, 1);
            s.assume_init()
        }
    }
}
pub type CUfileIOEvents_t = CUfileIOEvents;
pub type CUfileBatchHandle_t = *mut ::core::ffi::c_void;
unsafe extern "C" {
    /// This interface should be the first call in the sequence of batch I/O operation. This takes the maximum
    /// number of batch entries the caller intends to use and returns a `CUFileBatchHandle_t` which should
    /// be used by the caller for subsequent batch I/O calls.
    ///
    /// Refer to the following for more information:
    ///
    /// * [`cuFileRead`]
    /// * [`cuFileWrite`]
    /// * [`cuFileReadAsync`]
    /// * [`cuFileWriteAsync`]
    /// * [`cuFileBatchIOGetStatus`]
    /// * [`cuFileBatchIOCancel`]
    /// * [`cuFileBatchIODestroy`].
    ///
    /// # Parameters
    ///
    /// - `batch_idp`: (Output) Will be used in subsequent batch IO calls.
    ///
    /// # Return value
    ///
    /// - [`CUfileOpError::CU_FILE_INTERNAL_ERROR`]: [`CUfileOpError::CU_FILE_INTERNAL_ERROR`] on on any failures.
    /// - [`CUfileOpError::CU_FILE_SUCCESS`]: [`CUfileOpError::CU_FILE_SUCCESS`] on success.
    pub fn cuFileBatchIOSetUp(
        batch_idp: *mut CUfileBatchHandle_t,
        nr: ::core::ffi::c_uint,
    ) -> CUfileError_t;
}
unsafe extern "C" {
    /// This API will need to be used to submit a read/write operation on an array of GPU/CPU data pointers from
    ///   their respective file handle, offset, and size bytes.
    ///
    ///   Based on the type of memory pointer, the data is transferred to/from the GPU memory by using GDS or the
    ///   data is transferred to/from the CPU memory.
    ///
    ///   + This is an asynchronous call and will enqueue the operation on a `batch_id` provided by the `cuFileIOSetup`
    /// API. The operation can be monitored when using this `batch_id` through [`cuFileBatchIOGetStatus`].
    ///   + The operation can be canceled by calling [`cuFileBatchIOCancel`] or destroyed by [`cuFileBatchIODestroy`].
    /// * The entries in the [`CUfileIOParams_t`] array describe individual IOs.
    ///
    ///   The bytes transacted field is valid only when the status indicates a completion.
    /// * Operations that are enqueued with cuFile Batch APIs are FIFO ordered with respect to other work on the stream and
    ///   must be completed before continuing to the next action in the stream. Operations in each batch might be reordered with respect to each other.
    /// * The status field of individual IO operations via [`CUfileIOParams_t`] entries will have undefined values before
    ///   the entire batch is complete. This definition is subject to change.
    ///
    /// Refer to the following for more information:
    ///
    /// * [`cuFileRead`]
    /// * [`cuFileWrite`]
    /// * [`cuFileReadAsync`]
    /// * [`cuFileWriteAsync`]
    /// * [`cuFileBatchIOGetStatus`]
    /// * [`cuFileBatchIOCancel`]
    /// * [`cuFileBatchIODestroy`].
    ///
    /// # Parameters
    ///
    /// - `batch_idp`: The address of the output parameter for the newly created batch ID, which was obtained from a `cuFileBatchSetup` call.
    /// - `nr`: The number of requests for the batch request.
    /// * The value must be greater than 0 and less than or equal to `max_nr` specified in `cuFileBatchIOSetup`.
    /// - `iocbp`: The pointer contains the [`CUfileIOParams_t`] array structures of the length `nr` array.
    /// - `flags`: Reserved for future use. Should be set to 0.
    ///
    /// # Return value
    ///
    /// - [`CUfileOpError::CU_FILE_INTERNAL_ERROR`]: [`CUfileOpError::CU_FILE_INTERNAL_ERROR`] on any failures.
    /// - [`CUfileOpError::CU_FILE_SUCCESS`]: [`CUfileOpError::CU_FILE_SUCCESS`] on success.
    pub fn cuFileBatchIOSubmit(
        batch_idp: CUfileBatchHandle_t,
        nr: ::core::ffi::c_uint,
        iocbp: *mut CUfileIOParams_t,
        flags: ::core::ffi::c_uint,
    ) -> CUfileError_t;
}
unsafe extern "C" {
    /// This is a batch API to monitor the status of batch IO operations by using the `batch_id` that was returned by
    ///   [`cuFileBatchIOSubmit`]. The operation will be canceled automatically if [`cuFileBatchIOCancel`] is called and the
    ///   status will reflect `CU_FILE_CANCELED` for all canceled IO operations.
    /// * The status of each member of the batch is queried, which would not be possible with one `CUEvent`. The status
    ///   field of individual IO operations via [`CUfileIOParams_t`] entries will have undefined values before the entire
    ///   batch is completed. This definition is subject to change.
    ///
    /// Refer to the following for more information:
    ///
    /// * [`cuFileBatchIOSubmit`]
    /// * [`cuFileBatchIODestroy`].
    ///
    /// # Parameters
    ///
    /// - `batch_idp`: Obtained during setup.
    /// - `min_nr`: The minimum number of IO entries for which status is requested. The `min_nr` should be greater
    /// than or equal to zero and less than or equal to `*nr`.
    /// - `nr`: This is a pointer to max requested IO entries to poll for completion and is used as an Input/Output
    /// parameter. As an input `*nr` must be set to pass the maximum number of IO requests to poll for.
    /// As an output, `*nr` returns the number of completed I/Os.
    /// - `iocbp`: `CUFileIOEvents_t` array containing the status of completed I/Os in that batch.
    /// - `timeout`: This parameter is used to specify the amount of time to wait for in this API, even if the minimum
    /// number of requests have not completed. If the timeout hits, it is possible that the number of
    /// returned IOs can be less than `min_nr`.
    ///
    /// # Return value
    ///
    /// - `CU_FILE_ERROR_INVALID_VALUE`: `CU_FILE_ERROR_INVALID_VALUE` for an invalid batch ID.
    /// - [`CUfileOpError::CU_FILE_SUCCESS`]: [`CUfileOpError::CU_FILE_SUCCESS`] on success.
    ///
    ///   The success here refers to the completion of the API. Individual IO status and error can be obtained
    ///   by examining the returned status and error in the array iocbp.
    pub fn cuFileBatchIOGetStatus(
        batch_idp: CUfileBatchHandle_t,
        min_nr: ::core::ffi::c_uint,
        nr: *mut ::core::ffi::c_uint,
        iocbp: *mut CUfileIOEvents_t,
        timeout: *mut timespec,
    ) -> CUfileError_t;
}
unsafe extern "C" {
    /// This is a batch API to cancel an ongoing IO batch operation by using the `batch_id` that was returned
    ///   by [`cuFileBatchIOSubmit`]. This API tries to cancel an individual IO operation in the batch if
    ///   possible and provides no guarantee about canceling an ongoing operation.
    ///
    /// Refer to the following for more information:
    ///
    /// * [`cuFileBatchIOGetStatus`]
    /// * [`cuFileBatchIOSubmit`]
    /// * [`cuFileBatchIODestroy`].
    ///
    /// # Parameters
    ///
    /// - `batch_idp`: The batch ID to cancel.
    ///
    /// # Return value
    ///
    /// - `CU_FILE_ERROR_INVALID_VALUE`: `CU_FILE_ERROR_INVALID_VALUE` on any failures.
    /// - [`CUfileOpError::CU_FILE_SUCCESS`]: [`CUfileOpError::CU_FILE_SUCCESS`] on success.
    pub fn cuFileBatchIOCancel(batch_idp: CUfileBatchHandle_t) -> CUfileError_t;
}
unsafe extern "C" {
    /// This is a batch API that destroys a batch context and the resources that are allocated with `cuFileBatchIOSetup`.
    ///
    /// Refer to the following for more information:
    ///
    /// * [`cuFileBatchIOGetStatus`]
    /// * [`cuFileBatchIOSubmit`]
    /// * [`cuFileBatchIOCancel`].
    ///
    /// # Parameters
    ///
    /// - `batch_idp`: The batch handle to be destroyed.
    pub fn cuFileBatchIODestroy(batch_idp: CUfileBatchHandle_t);
}
unsafe extern "C" {
    /// Enqueues a read operation for the specified bytes from the cuFile handle into the device memory
    ///   by using GDS functionality or to the host memory based on the type of memory pointer.
    /// * If non-NULL, the action is ordered in the stream.
    /// * The current context of the caller is assumed.
    ///
    /// * This API reads the data from the specified file handle at the specified offset and size bytes
    ///   into the GPU memory using GDS functionality.
    ///
    ///   This is an asynchronous call and enqueues the operation into the specified CUDA stream and will not block
    ///   the host thread for IO completion. The operation can be waited upon using `cuStreamSynchronize(stream)`.
    /// * The `bytes_read_p` memory should be allocated with `cuMemHostAlloc/malloc/mmap` or registered with `cuMemHostRegister`.
    ///
    ///   The pointer to access that memory from the device can be obtained by using `cuMemHostGetDevicePointer`.
    /// * Operations that are enqueued with cuFile Stream APIs are FIFO ordered with respect to other work on the
    ///   stream and must be completed before continuing to the next action in the stream.
    /// * Unless otherwise specified through [`cuFileStreamRegister`] API, file offset, buffer offset or size parameter
    ///   will not be evaluated until execution time. In these scenarios, size parameters should be set to the maximum
    ///   possible I/O size at the time of submission and can be set to the actual size prior to the stream I/O execution.
    ///
    /// Refer to the following for more information:
    ///
    /// * [`cuFileRead`]
    /// * [`cuFileWrite`]
    /// * [`cuFileWriteAsync`].
    ///
    /// # Parameters
    ///
    /// - `fh`: The cuFile handle for the file.
    /// - `bufPtr_base`: The base address of the buffer in the memory into which to read.
    /// * The buffer can be allocated using either `cudaMemory`, `cudaMallocHost`, `malloc`, or `mmap`.
    /// * For registered buffers, `bufPtr_base` must remain set to the base address used in [`cuFileBufRegister`] call.
    /// - `size_p`: Pointer to size in bytes to read. If the exact size is not known at the time of I/O submission, then yo
    /// must set it to the maximum possible I/O size for that stream I/O.
    /// - `file_offset_p`: Pointer to offset in the file from which to read. Unless otherwise set using [`cuFileStreamRegister`]
    /// API, this value will not be evaluated until execution time.
    /// - `bufPtr_offset_p`: Pointer to the offset relative to the `bufPtr_base` pointer from which to write. Unless otherwise set
    /// using [`cuFileStreamRegister`] API, this value will not be evaluated until execution time.
    /// - `bytes_read_p`: Pointer to the bytes read from the specified filehandle. This pointer should be a non NULL value and
    /// `*bytes_read_p` set to 0. After successful execution of the operation in the stream, the value
    /// `*bytes_read_p` will contain either:
    ///
    /// * The number of bytes successfully read.
    /// * -1 on IO errors.
    /// * All other errors return a negative integer value of the [`CUfileOpError`] enum value.
    /// - `stream`: CUDA stream in which to enqueue the operation.
    /// * If NULL, make this operation synchronous.
    ///
    /// # Return value
    ///
    /// - `CU_FILE_CUDA_ERROR`: `CU_FILE_CUDA_ERROR` on CUDA-specific errors.
    ///
    ///   CUresult code can be obtained by using `CU_FILE_CUDA_ERR(err)`.
    /// - `CU_FILE_DRIVER_ERROR`: `CU_FILE_DRIVER_ERROR`, if the nvidia-fs driver cannot handle the request.
    /// - `CU_FILE_ERROR_INVALID_VALUE`: `CU_FILE_ERROR_INVALID_VALUE` on an input failure.
    /// - [`CUfileOpError::CU_FILE_SUCCESS`]: [`CUfileOpError::CU_FILE_SUCCESS`] on a successful submission.
    pub fn cuFileReadAsync(
        fh: CUfileHandle_t,
        bufPtr_base: *mut ::core::ffi::c_void,
        size_p: *mut size_t,
        file_offset_p: *mut off_t,
        bufPtr_offset_p: *mut off_t,
        bytes_read_p: *mut ssize_t,
        stream: CUstream,
    ) -> CUfileError_t;
}
unsafe extern "C" {
    /// Queues Write operation for the specified bytes from the device memory into the cuFile handle by using GDS.
    ///
    /// * This API writes the data from the GPU memory to a file specified by the file handle at a specified offset and
    ///   size bytes by using GDS functionality. This is an asynchronous call and enqueues the operation into the specified
    ///   CUDA stream and will not block the host thread for IO completion. The operation can be waited upon by using `cuStreamSynchronize(stream)`.
    /// * The `bytes_written` pointer should be allocated with `cuMemHostAlloc` or registered with `cuMemHostRegister`,
    ///   and the pointer to access that memory from the device can be obtained by using `cuMemHostGetDevicePointer`.
    /// * Operations that are enqueued with cuFile Stream APIs are FIFO ordered with respect to other work on the stream and
    ///   must be completed before continuing to the next action in the stream.
    /// * Unless otherwise specified through [`cuFileStreamRegister`] API, file offset, buffer offset or size parameter will
    ///   not be evaluated until execution time. In these scenarios, size parameters should be set to the maximum possible
    ///   I/O size at the time of submission and can be set to the actual size prior to the stream I/O execution.
    ///
    /// See the following for more information:
    ///
    /// * [`cuFileRead`]
    /// * [`cuFileWrite`]
    /// * [`cuFileReadAsync`].
    ///
    /// # Parameters
    ///
    /// - `fh`: The cuFile handle for the file.
    /// - `bufPtr_base`: The base address of the buffer in the memory from which to write. The buffer can be allocated using either
    /// `cudaMemory/cudaMallocHost/malloc/mmap`. For registered buffers, `bufPtr_base` must remain set to
    /// the base address used in the [`cuFileBufRegister`] call.
    /// - `size_p`: Pointer to the size in bytes to write. If the exact size is not known at the time of I/O submission,
    /// then you must set it to the maximum possible I/O size for that stream I/O.
    /// - `file_offset_p`: Pointer to the offset in the file from which to write. Unless otherwise set using [`cuFileStreamRegister`]
    /// API, this value will not be evaluated until execution time.
    /// - `bufPtr_offset_p`: Pointer to the offset relative to the `bufPtr_base` pointer from which to write. Unless otherwise set
    /// using cuFileStreamRegister API, this value will not be evaluated until execution time.
    /// - `bytes_written_p`: Pointer to the bytes written to the specified filehandle.This pointer should be a non NULL value and
    /// `*bytes_written_p` set to 0. After successful execution of the operation in the stream, the value
    /// `*bytes_written_p` will contain either:
    ///
    /// * The number of bytes successfully written.
    /// * -1 on IO errors.
    /// * All other errors will return a negative integer value of the [`CUfileOpError`] enum value.
    /// - `stream`: The CUDA stream to enqueue the operation.
    ///
    /// # Return value
    ///
    /// - `CU_FILE_CUDA_ERROR`: `CU_FILE_CUDA_ERROR` on CUDA-specific errors.
    ///
    ///   The CUresult code can be obtained by using `CU_FILE_CUDA_ERR(err)`.
    /// - `CU_FILE_DRIVER_ERROR`: `CU_FILE_DRIVER_ERROR`, if the nvidia-fs driver cannot handle the request.
    /// - `CU_FILE_ERROR_INVALID_VALUE`: `CU_FILE_ERROR_INVALID_VALUE` on an input failure.
    /// - [`CUfileOpError::CU_FILE_SUCCESS`]: [`CUfileOpError::CU_FILE_SUCCESS`] on a successful submission.
    pub fn cuFileWriteAsync(
        fh: CUfileHandle_t,
        bufPtr_base: *mut ::core::ffi::c_void,
        size_p: *mut size_t,
        file_offset_p: *mut off_t,
        bufPtr_offset_p: *mut off_t,
        bytes_written_p: *mut ssize_t,
        stream: CUstream,
    ) -> CUfileError_t;
}
unsafe extern "C" {
    /// Defines the input behavior for stream I/O APIs.
    ///
    /// This optional API registers the stream with the cuFile subsystem.
    ///
    /// This API will allocate resources to handle stream operations for cuFile.
    ///
    /// The API will synchronize on the stream before allocating resources.
    ///
    /// The stream pointer is expected to be a valid pointer.
    ///
    /// # Parameters
    ///
    /// - `stream`: CUDA stream in which to enqueue the operation. If NULL, make this operation in the default CUDA stream.
    /// - `flags`: The following are valid values.
    ///
    /// # Return value
    ///
    /// - `CU_FILE_DRIVER_ERROR`: `CU_FILE_DRIVER_ERROR` if the NVIDIA-fs driver cannot handle the request.
    /// - `CU_FILE_ERROR_INVALID_VALUE`: `CU_FILE_ERROR_INVALID_VALUE` on a invalid stream specification.
    /// - [`CUfileOpError::CU_FILE_PLATFORM_NOT_SUPPORTED`]: [`CUfileOpError::CU_FILE_PLATFORM_NOT_SUPPORTED`] on unsupported platforms.
    /// - [`CUfileOpError::CU_FILE_SUCCESS`]: [`CUfileOpError::CU_FILE_SUCCESS`] on a successful submission.
    pub fn cuFileStreamRegister(
        stream: CUstream,
        flags: ::core::ffi::c_uint,
    ) -> CUfileError_t;
}
unsafe extern "C" {
    /// This optional API deregisters the stream with the cuFile subsystem.
    ///
    /// This API will free allocated cuFile resources associated with the stream.
    ///
    /// The API will synchronize on the stream before releasing resources.
    ///
    /// The stream pointer is expected to be a valid pointer.
    ///
    /// The stream will be automatically deregistered as part of [`cuFileDriverClose`].
    ///
    /// # Parameters
    ///
    /// - `stream`: CUDA stream in which to enqueue the operation. If NULL, make this operation in the default CUDA stream.
    ///
    /// # Return value
    ///
    /// - `CU_FILE_ERROR_INVALID_VALUE`: `CU_FILE_ERROR_INVALID_VALUE` on a invalid stream specification.
    /// - [`CUfileOpError::CU_FILE_PLATFORM_NOT_SUPPORTED`]: [`CUfileOpError::CU_FILE_PLATFORM_NOT_SUPPORTED`] on unsupported platforms.
    /// - [`CUfileOpError::CU_FILE_SUCCESS`]: [`CUfileOpError::CU_FILE_SUCCESS`] on a successful submission.
    pub fn cuFileStreamDeregister(stream: CUstream) -> CUfileError_t;
}
unsafe extern "C" {
    /// Retrieves the cuFile library version.
    /// * The version is returned as (1000 \* major + 10 \* minor).
    /// * For example, cuFile 1.7.0 would be represented by 1070.
    ///
    /// This API is used to obtain the current version of the cuFile library. It may be useful sometimes for an
    /// application to expect based on the version if any specific GDS feature is present or not.
    ///
    /// # Parameters
    ///
    /// - `version`: Output argument which would contain the version number in the above format upon successful completion.
    ///
    /// # Return value
    ///
    /// - [`CUfileOpError::CU_FILE_DRIVER_VERSION_READ_ERROR`]: [`CUfileOpError::CU_FILE_DRIVER_VERSION_READ_ERROR`] if the version is not available.
    /// - [`CUfileOpError::CU_FILE_INVALID_VALUE`]: [`CUfileOpError::CU_FILE_INVALID_VALUE`] if the version parameter is null.
    /// - [`CUfileOpError::CU_FILE_SUCCESS`]: [`CUfileOpError::CU_FILE_SUCCESS`] on successful completion.
    pub fn cuFileGetVersion(version: *mut ::core::ffi::c_int) -> CUfileError_t;
}
unsafe extern "C" {
    pub fn cuFileExportPCIeTopology(
        filename: *const ::core::ffi::c_char,
    ) -> CUfileError_t;
}
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum CUFileSizeTConfigParameter_t {
    CUFILE_PARAM_PROFILE_STATS = 0,
    CUFILE_PARAM_EXECUTION_MAX_IO_QUEUE_DEPTH = 1,
    CUFILE_PARAM_EXECUTION_MAX_IO_THREADS = 2,
    CUFILE_PARAM_EXECUTION_MIN_IO_THRESHOLD_SIZE_KB = 3,
    CUFILE_PARAM_EXECUTION_MAX_REQUEST_PARALLELISM = 4,
    CUFILE_PARAM_PROPERTIES_MAX_DIRECT_IO_SIZE_KB = 5,
    CUFILE_PARAM_PROPERTIES_MAX_DEVICE_CACHE_SIZE_KB = 6,
    CUFILE_PARAM_PROPERTIES_PER_BUFFER_CACHE_SIZE_KB = 7,
    CUFILE_PARAM_PROPERTIES_MAX_DEVICE_PINNED_MEM_SIZE_KB = 8,
    CUFILE_PARAM_PROPERTIES_IO_BATCHSIZE = 9,
    CUFILE_PARAM_POLLTHRESHOLD_SIZE_KB = 10,
    CUFILE_PARAM_PROPERTIES_BATCH_IO_TIMEOUT_MS = 11,
}
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum CUFileBoolConfigParameter_t {
    CUFILE_PARAM_PROPERTIES_USE_POLL_MODE = 0,
    CUFILE_PARAM_PROPERTIES_ALLOW_COMPAT_MODE = 1,
    CUFILE_PARAM_FORCE_COMPAT_MODE = 2,
    CUFILE_PARAM_FS_MISC_API_CHECK_AGGRESSIVE = 3,
    CUFILE_PARAM_EXECUTION_PARALLEL_IO = 4,
    CUFILE_PARAM_PROFILE_NVTX = 5,
    CUFILE_PARAM_PROPERTIES_ALLOW_SYSTEM_MEMORY = 6,
    CUFILE_PARAM_USE_PCIP2PDMA = 7,
    CUFILE_PARAM_PREFER_IO_URING = 8,
    CUFILE_PARAM_FORCE_ODIRECT_MODE = 9,
    CUFILE_PARAM_SKIP_TOPOLOGY_DETECTION = 10,
    CUFILE_PARAM_STREAM_MEMOPS_BYPASS = 11,
}
#[repr(u32)]
#[derive(
    Debug,
    Copy,
    Clone,
    Hash,
    PartialOrd,
    Ord,
    PartialEq,
    Eq,
    TryFromPrimitive,
    IntoPrimitive,
)]
pub enum CUFileStringConfigParameter_t {
    CUFILE_PARAM_LOGGING_LEVEL = 0,
    CUFILE_PARAM_ENV_LOGFILE_PATH = 1,
    CUFILE_PARAM_LOG_DIR = 2,
}
unsafe extern "C" {
    /// Fetch cuFile configuration parameters in a program.
    /// * Based on the parameter type, the corresponding API can be chosen.
    /// * If the driver is open, `cuFileGetParameter*()` returns the current runtime
    ///   value for the given parameter.
    /// * If the driver is not opened yet, `cuFileGetParameter*()` returns the
    ///   currently staged value for that parameter. Staged parameter values are
    ///   cleared when the driver is opened.
    ///
    /// These APIs are used to fetch configuration parameters in a programmatic way as
    /// opposed to reading the JSON file or environment variables.
    ///
    /// # Parameters
    ///
    /// - `param`: The parameter to get.
    ///
    /// # Return value
    ///
    /// - [`CUfileOpError::CU_FILE_INVALID_VALUE`]: [`CUfileOpError::CU_FILE_INVALID_VALUE`] if the input parameter is invalid.
    /// - [`CUfileOpError::CU_FILE_SUCCESS`]: [`CUfileOpError::CU_FILE_SUCCESS`] on success.
    pub fn cuFileGetParameterSizeT(
        param: CUFileSizeTConfigParameter_t,
        value: *mut size_t,
    ) -> CUfileError_t;
}
unsafe extern "C" {
    /// Fetch cuFile configuration parameters in a program.
    /// * Based on the parameter type, the corresponding API can be chosen.
    /// * If the driver is open, `cuFileGetParameter*()` returns the current runtime
    ///   value for the given parameter.
    /// * If the driver is not opened yet, `cuFileGetParameter*()` returns the
    ///   currently staged value for that parameter. Staged parameter values are
    ///   cleared when the driver is opened.
    ///
    /// These APIs are used to fetch configuration parameters in a programmatic way as
    /// opposed to reading the JSON file or environment variables.
    ///
    /// # Parameters
    ///
    /// - `param`: The parameter to get.
    ///
    /// # Return value
    ///
    /// - [`CUfileOpError::CU_FILE_INVALID_VALUE`]: [`CUfileOpError::CU_FILE_INVALID_VALUE`] if the input parameter is invalid.
    /// - [`CUfileOpError::CU_FILE_SUCCESS`]: [`CUfileOpError::CU_FILE_SUCCESS`] on success.
    pub fn cuFileGetParameterBool(
        param: CUFileBoolConfigParameter_t,
        value: *mut bool,
    ) -> CUfileError_t;
}
unsafe extern "C" {
    /// Fetch cuFile configuration parameters in a program.
    /// * Based on the parameter type, the corresponding API can be chosen.
    /// * If the driver is open, `cuFileGetParameter*()` returns the current runtime
    ///   value for the given parameter.
    /// * If the driver is not opened yet, `cuFileGetParameter*()` returns the
    ///   currently staged value for that parameter. Staged parameter values are
    ///   cleared when the driver is opened.
    ///
    /// These APIs are used to fetch configuration parameters in a programmatic way as
    /// opposed to reading the JSON file or environment variables.
    ///
    /// # Parameters
    ///
    /// - `param`: The parameter to get.
    ///
    /// # Return value
    ///
    /// - [`CUfileOpError::CU_FILE_INVALID_VALUE`]: [`CUfileOpError::CU_FILE_INVALID_VALUE`] if the input parameter is invalid.
    /// - [`CUfileOpError::CU_FILE_SUCCESS`]: [`CUfileOpError::CU_FILE_SUCCESS`] on success.
    pub fn cuFileGetParameterString(
        param: CUFileStringConfigParameter_t,
        desc_str: *mut ::core::ffi::c_char,
        len: ::core::ffi::c_int,
    ) -> CUfileError_t;
}
unsafe extern "C" {
    /// Get both the minimum and maximum settable values for a given `size_t`
    ///   parameter in a single call.
    ///
    /// This API is used to get the range a certain `size_t` parameter supports. This
    /// can help applications decide what values they can assign to certain parameters.
    ///
    /// # Parameters
    ///
    /// - `param`: [`CUFileSizeTConfigParameter_t`] configuration parameter.
    /// - `min_value`: Pointer to store the minimum value.
    /// - `max_value`: Pointer to store the maximum value.
    ///
    /// # Return value
    ///
    /// - [`CUfileOpError::CU_FILE_INVALID_VALUE`]: [`CUfileOpError::CU_FILE_INVALID_VALUE`] if `min_value` or `max_value` is `NULL`.
    /// - [`CUfileOpError::CU_FILE_SUCCESS`]: [`CUfileOpError::CU_FILE_SUCCESS`] on success.
    pub fn cuFileGetParameterMinMaxValue(
        param: CUFileSizeTConfigParameter_t,
        min_value: *mut size_t,
        max_value: *mut size_t,
    ) -> CUfileError_t;
}
unsafe extern "C" {
    /// Allows setting of cuFile configuration parameters in a program.
    /// * Based on the parameter type, the corresponding API can be chosen.
    /// * Should be called before the driver opens and the values take effect after the driver is opened.
    /// * If the same parameter is set multiple times, only the last parameter is kept and used.
    /// * Parameter precedence (highest to lowest) is:
    ///   `cuFileSetParameter*()` (if set), then environment variable (if exists and set),
    ///   then `cufile.json`.
    ///
    /// These APIs are used to set configuration parameters in a programmatic way as
    /// opposed to relying on the JSON file or environment variables.
    ///
    /// # Parameters
    ///
    /// - `param`: The parameter to set, based on parameter type.
    ///
    /// # Return value
    ///
    /// - [`CUfileOpError::CU_FILE_DRIVER_ALREADY_OPEN`]: [`CUfileOpError::CU_FILE_DRIVER_ALREADY_OPEN`] if the driver is already open.
    /// - [`CUfileOpError::CU_FILE_INVALID_VALUE`]: [`CUfileOpError::CU_FILE_INVALID_VALUE`] if the input parameter is invalid.
    /// - [`CUfileOpError::CU_FILE_SUCCESS`]: [`CUfileOpError::CU_FILE_SUCCESS`] on success.
    pub fn cuFileSetParameterSizeT(
        param: CUFileSizeTConfigParameter_t,
        value: size_t,
    ) -> CUfileError_t;
}
unsafe extern "C" {
    /// Allows setting of cuFile configuration parameters in a program.
    /// * Based on the parameter type, the corresponding API can be chosen.
    /// * Should be called before the driver opens and the values take effect after the driver is opened.
    /// * If the same parameter is set multiple times, only the last parameter is kept and used.
    /// * Parameter precedence (highest to lowest) is:
    ///   `cuFileSetParameter*()` (if set), then environment variable (if exists and set),
    ///   then `cufile.json`.
    ///
    /// These APIs are used to set configuration parameters in a programmatic way as
    /// opposed to relying on the JSON file or environment variables.
    ///
    /// # Parameters
    ///
    /// - `param`: The parameter to set, based on parameter type.
    ///
    /// # Return value
    ///
    /// - [`CUfileOpError::CU_FILE_DRIVER_ALREADY_OPEN`]: [`CUfileOpError::CU_FILE_DRIVER_ALREADY_OPEN`] if the driver is already open.
    /// - [`CUfileOpError::CU_FILE_INVALID_VALUE`]: [`CUfileOpError::CU_FILE_INVALID_VALUE`] if the input parameter is invalid.
    /// - [`CUfileOpError::CU_FILE_SUCCESS`]: [`CUfileOpError::CU_FILE_SUCCESS`] on success.
    pub fn cuFileSetParameterBool(
        param: CUFileBoolConfigParameter_t,
        value: bool,
    ) -> CUfileError_t;
}
unsafe extern "C" {
    /// Allows setting of cuFile configuration parameters in a program.
    /// * Based on the parameter type, the corresponding API can be chosen.
    /// * Should be called before the driver opens and the values take effect after the driver is opened.
    /// * If the same parameter is set multiple times, only the last parameter is kept and used.
    /// * Parameter precedence (highest to lowest) is:
    ///   `cuFileSetParameter*()` (if set), then environment variable (if exists and set),
    ///   then `cufile.json`.
    ///
    /// These APIs are used to set configuration parameters in a programmatic way as
    /// opposed to relying on the JSON file or environment variables.
    ///
    /// # Parameters
    ///
    /// - `param`: The parameter to set, based on parameter type.
    ///
    /// # Return value
    ///
    /// - [`CUfileOpError::CU_FILE_DRIVER_ALREADY_OPEN`]: [`CUfileOpError::CU_FILE_DRIVER_ALREADY_OPEN`] if the driver is already open.
    /// - [`CUfileOpError::CU_FILE_INVALID_VALUE`]: [`CUfileOpError::CU_FILE_INVALID_VALUE`] if the input parameter is invalid.
    /// - [`CUfileOpError::CU_FILE_SUCCESS`]: [`CUfileOpError::CU_FILE_SUCCESS`] on success.
    pub fn cuFileSetParameterString(
        param: CUFileStringConfigParameter_t,
        desc_str: *const ::core::ffi::c_char,
    ) -> CUfileError_t;
}
unsafe extern "C" {
    /// Set the level of statistics collection for cuFile operations. This overrides
    /// the `cufile.json` settings for stats. Higher stats levels may impact
    /// performance. Level 0 disables statistics collection. Changes to stats level
    /// take effect for future operations. Must be called before
    /// [`cuFileStatsStart`] to take effect.
    ///
    /// # Parameters
    ///
    /// - `level`: Statistics level (0 = disabled, 1 = basic, 2 = detailed, 3 = verbose).
    pub fn cuFileSetStatsLevel(level: ::core::ffi::c_int) -> CUfileError_t;
}
unsafe extern "C" {
    /// Get the current level of statistics collection for cuFile operations. Returns
    /// the currently configured stats level (0–3) in the provided pointer.
    ///
    /// # Parameters
    ///
    /// - `level`: Pointer to store the current statistics level.
    pub fn cuFileGetStatsLevel(level: *mut ::core::ffi::c_int) -> CUfileError_t;
}
#[repr(C)]
#[derive(Debug, Default, Copy, Clone, Hash, PartialOrd, Ord, PartialEq, Eq)]
pub struct CUfileOpCounter {
    pub ok: u64,
    pub err: u64,
}
pub type CUfileOpCounter_t = CUfileOpCounter;
#[repr(C)]
#[derive(Debug, Default, Copy, Clone, Hash, PartialOrd, Ord, PartialEq, Eq)]
pub struct CUfileStatsLevel1 {
    pub read_ops: CUfileOpCounter_t,
    pub write_ops: CUfileOpCounter_t,
    pub hdl_register_ops: CUfileOpCounter_t,
    pub hdl_deregister_ops: CUfileOpCounter_t,
    pub buf_register_ops: CUfileOpCounter_t,
    pub buf_deregister_ops: CUfileOpCounter_t,
    pub read_bytes: u64,
    pub write_bytes: u64,
    pub read_bw_bytes_per_sec: u64,
    pub write_bw_bytes_per_sec: u64,
    pub read_lat_avg_us: u64,
    pub write_lat_avg_us: u64,
    pub read_ops_per_sec: u64,
    pub write_ops_per_sec: u64,
    pub read_lat_sum_us: u64,
    pub write_lat_sum_us: u64,
    pub batch_submit_ops: CUfileOpCounter_t,
    pub batch_complete_ops: CUfileOpCounter_t,
    pub batch_setup_ops: CUfileOpCounter_t,
    pub batch_cancel_ops: CUfileOpCounter_t,
    pub batch_destroy_ops: CUfileOpCounter_t,
    pub batch_enqueued_ops: CUfileOpCounter_t,
    pub batch_posix_enqueued_ops: CUfileOpCounter_t,
    pub batch_processed_ops: CUfileOpCounter_t,
    pub batch_posix_processed_ops: CUfileOpCounter_t,
    pub batch_nvfs_submit_ops: CUfileOpCounter_t,
    pub batch_p2p_submit_ops: CUfileOpCounter_t,
    pub batch_aio_submit_ops: CUfileOpCounter_t,
    pub batch_iouring_submit_ops: CUfileOpCounter_t,
    pub batch_mixed_io_submit_ops: CUfileOpCounter_t,
    pub batch_total_submit_ops: CUfileOpCounter_t,
    pub batch_read_bytes: u64,
    pub batch_write_bytes: u64,
    pub batch_read_bw_bytes: u64,
    pub batch_write_bw_bytes: u64,
    pub batch_submit_lat_avg_us: u64,
    pub batch_completion_lat_avg_us: u64,
    pub batch_submit_ops_per_sec: u64,
    pub batch_complete_ops_per_sec: u64,
    pub batch_submit_lat_sum_us: u64,
    pub batch_completion_lat_sum_us: u64,
    pub last_batch_read_bytes: u64,
    pub last_batch_write_bytes: u64,
}
pub type CUfileStatsLevel1_t = CUfileStatsLevel1;
#[repr(C)]
#[derive(Debug, Default, Copy, Clone, Hash, PartialOrd, Ord, PartialEq, Eq)]
pub struct CUfileStatsLevel2 {
    pub basic: CUfileStatsLevel1_t,
    pub read_size_kb_hist: [u64; 32usize],
    pub write_size_kb_hist: [u64; 32usize],
}
pub type CUfileStatsLevel2_t = CUfileStatsLevel2;
#[repr(C)]
#[derive(Debug, Default, Copy, Clone, Hash, PartialOrd, Ord, PartialEq, Eq)]
pub struct CUfilePerGpuStats {
    pub uuid: [::core::ffi::c_char; 16usize],
    pub read_bytes: u64,
    pub read_bw_bytes_per_sec: u64,
    pub read_utilization: u64,
    pub read_duration_us: u64,
    pub n_total_reads: u64,
    pub n_p2p_reads: u64,
    pub n_nvfs_reads: u64,
    pub n_posix_reads: u64,
    pub n_unaligned_reads: u64,
    pub n_dr_reads: u64,
    pub n_sparse_regions: u64,
    pub n_inline_regions: u64,
    pub n_reads_err: u64,
    pub writes_bytes: u64,
    pub write_bw_bytes_per_sec: u64,
    pub write_utilization: u64,
    pub write_duration_us: u64,
    pub n_total_writes: u64,
    pub n_p2p_writes: u64,
    pub n_nvfs_writes: u64,
    pub n_posix_writes: u64,
    pub n_unaligned_writes: u64,
    pub n_dr_writes: u64,
    pub n_writes_err: u64,
    pub n_mmap: u64,
    pub n_mmap_ok: u64,
    pub n_mmap_err: u64,
    pub n_mmap_free: u64,
    pub reg_bytes: u64,
}
pub type CUfilePerGpuStats_t = CUfilePerGpuStats;
#[repr(C)]
#[derive(Debug, Default, Copy, Clone, Hash, PartialOrd, Ord, PartialEq, Eq)]
pub struct CUfileStatsLevel3 {
    pub detailed: CUfileStatsLevel2_t,
    pub num_gpus: u32,
    pub per_gpu_stats: [CUfilePerGpuStats_t; 16usize],
}
pub type CUfileStatsLevel3_t = CUfileStatsLevel3;
unsafe extern "C" {
    /// Start collecting cuFile statistics. The statistics level must be set with a
    /// level greater than 0 before calling this function. Statistics counters begin
    /// accumulating from this point and can be stopped and restarted without resetting
    /// counters unless [`cuFileStatsReset`] is called.
    pub fn cuFileStatsStart() -> CUfileError_t;
}
unsafe extern "C" {
    /// Stop collecting cuFile statistics. Accumulated statistics remain available for
    /// querying until reset or driver close. Statistics counters are preserved and can
    /// still be queried. Collection can be restarted with [`cuFileStatsStart`] to
    /// continue accumulating. Use [`cuFileStatsReset`] to clear all counters.
    pub fn cuFileStatsStop() -> CUfileError_t;
}
unsafe extern "C" {
    /// Reset all cuFile statistics counters to zero. This clears all accumulated
    /// statistics data across all levels. Can be called while statistics collection is
    /// active or stopped. Useful for measuring statistics over specific time periods
    /// or operation sets.
    pub fn cuFileStatsReset() -> CUfileError_t;
}
unsafe extern "C" {
    /// Get Level 1 cuFile statistics. Level 1 provides basic I/O and operation
    /// counters including read and write operations, bytes transferred, bandwidth,
    /// latency, and batch operation statistics.
    ///
    /// # Parameters
    ///
    /// - `stats`: Pointer to [`CUfileStatsLevel1_t`] structure to be filled.
    pub fn cuFileGetStatsL1(stats: *mut CUfileStatsLevel1_t) -> CUfileError_t;
}
unsafe extern "C" {
    /// Get Level 2 cuFile statistics. Level 2 includes all Level 1 statistics plus
    /// size histograms for read and write operations. The histograms track the
    /// distribution of I/O operation sizes, useful for analyzing I/O size patterns and
    /// optimization.
    ///
    /// # Parameters
    ///
    /// - `stats`: Pointer to [`CUfileStatsLevel2_t`] structure to be filled.
    pub fn cuFileGetStatsL2(stats: *mut CUfileStatsLevel2_t) -> CUfileError_t;
}
unsafe extern "C" {
    /// Get Level 3 cuFile statistics. Level 3 includes all Level 2 statistics plus
    /// per-GPU breakdown of operations, bandwidth, utilization, and buffer
    /// registration statistics. This level provides detailed analysis of which GPUs
    /// are being used, how operations are distributed across GPUs, and the type of I/O
    /// path used (P2P, NVFS, or POSIX). The per-GPU breakdown includes operation
    /// counts, bandwidth metrics, utilization percentages, and buffer registration
    /// statistics. This level has the highest performance overhead but provides the
    /// most detailed insights for performance analysis and debugging.
    ///
    /// # Parameters
    ///
    /// - `stats`: Pointer to [`CUfileStatsLevel3_t`] structure to be filled.
    pub fn cuFileGetStatsL3(stats: *mut CUfileStatsLevel3_t) -> CUfileError_t;
}
unsafe extern "C" {
    /// Get the BAR size for a specific GPU in KB.
    ///
    /// This API is used to get the BAR size of the GPU passed in as a parameter.
    ///
    /// # Parameters
    ///
    /// - `gpuIndex`: GPU index to query.
    /// - `barSize`: Pointer to store the BAR size in KB.
    ///
    /// # Return value
    ///
    /// - [`CUfileOpError::CU_FILE_DRIVER_NOT_INITIALIZED`]: [`CUfileOpError::CU_FILE_DRIVER_NOT_INITIALIZED`] if the driver is not initialized.
    /// - [`CUfileOpError::CU_FILE_INVALID_VALUE`]: [`CUfileOpError::CU_FILE_INVALID_VALUE`] on invalid input.
    /// - [`CUfileOpError::CU_FILE_SUCCESS`]: [`CUfileOpError::CU_FILE_SUCCESS`] on success.
    pub fn cuFileGetBARSizeInKB(
        gpuIndex: ::core::ffi::c_int,
        barSize: *mut size_t,
    ) -> CUfileError_t;
}
unsafe extern "C" {
    /// Sets POSIX pool slabs for compat mode I/O.
    /// * `size_values` and `count_values` parameters are treated as a pair.
    /// * Should be set before [`cuFileDriverOpen`] is called.
    ///
    /// This API is used to set a POSIX pool array. The application can set it based on
    /// the I/O sizes it is going to be issuing and hence reduce memory fragmentation.
    ///
    /// # Parameters
    ///
    /// - `size_values`: Array of slab sizes in KB.
    /// - `count_values`: Array of slab counts.
    /// - `len`: Length of both arrays (must be the same).
    ///
    /// # Return value
    ///
    /// - [`CUfileOpError::CU_FILE_DRIVER_ALREADY_OPEN`]: [`CUfileOpError::CU_FILE_DRIVER_ALREADY_OPEN`] if the driver is already open.
    /// - [`CUfileOpError::CU_FILE_INVALID_VALUE`]: [`CUfileOpError::CU_FILE_INVALID_VALUE`] if inputs are invalid.
    /// - [`CUfileOpError::CU_FILE_SUCCESS`]: [`CUfileOpError::CU_FILE_SUCCESS`] on success.
    pub fn cuFileSetParameterPosixPoolSlabArray(
        size_values: *const size_t,
        count_values: *const size_t,
        len: ::core::ffi::c_int,
    ) -> CUfileError_t;
}
unsafe extern "C" {
    /// Get POSIX pool slab array.
    /// * `size_values` and `count_values` parameters are returned as a pair.
    ///
    /// This API is used to query values of a POSIX pool array.
    ///
    /// # Parameters
    ///
    /// - `size_values`: Buffer to receive slab sizes in KB.
    /// - `count_values`: Buffer to receive slab counts.
    /// - `len`: Buffer size (must match the actual parameter length).
    ///
    /// # Return value
    ///
    /// - [`CUfileOpError::CU_FILE_INVALID_VALUE`]: [`CUfileOpError::CU_FILE_INVALID_VALUE`] if inputs are invalid.
    /// - [`CUfileOpError::CU_FILE_SUCCESS`]: [`CUfileOpError::CU_FILE_SUCCESS`] on success.
    pub fn cuFileGetParameterPosixPoolSlabArray(
        size_values: *mut size_t,
        count_values: *mut size_t,
        len: ::core::ffi::c_int,
    ) -> CUfileError_t;
}
unsafe extern "C" {
    pub fn cuFileSetParameterGpuBounceBufferSlabArray(
        size_values: *const size_t,
        count_values: *const size_t,
        len: ::core::ffi::c_int,
    ) -> CUfileError_t;
}
unsafe extern "C" {
    pub fn cuFileGetParameterGpuBounceBufferSlabArray(
        size_values: *mut size_t,
        count_values: *mut size_t,
        len: ::core::ffi::c_int,
    ) -> CUfileError_t;
}
unsafe extern "C" {
    /// Gets the P2P flags for a specific filesystem or block device.
    ///
    /// This API is used to query what P2P modes are supported by cuFile for a certain
    /// filesystem or block device.
    ///
    /// # Parameters
    ///
    /// - `status_flag`: The filesystem/device status flag (for example,
    /// [`CUfileDriverStatusFlags::CU_FILE_LUSTRE_SUPPORTED`]).
    /// - `p2p_flags`: Pointer to store the P2P flags.
    ///
    /// # Return value
    ///
    /// - [`CUfileOpError::CU_FILE_DRIVER_NOT_INITIALIZED`]: [`CUfileOpError::CU_FILE_DRIVER_NOT_INITIALIZED`] if the cuFile driver is not initialized.
    /// - [`CUfileOpError::CU_FILE_INVALID_VALUE`]: [`CUfileOpError::CU_FILE_INVALID_VALUE`] if `p2p_flags` is `NULL`.
    /// - [`CUfileOpError::CU_FILE_SUCCESS`]: [`CUfileOpError::CU_FILE_SUCCESS`] on success.
    pub fn cuFileDriverGetP2PFlags(
        status_flag: CUfileDriverStatusFlags_t,
        p2p_flags: *mut CUfileP2PFlags_t,
    ) -> CUfileError_t;
}
unsafe extern "C" {
    /// Sets the P2P flags for a specific filesystem or block device.
    /// * The `p2p_flags` parameter should be a bitmask of the desired P2P
    ///   capabilities.
    /// * On `x86_64` platforms: only `CU_FILE_P2P_FLAG_PCI_P2PDMA` is allowed.
    /// * On `AARCH64` platforms: only `CU_FILE_P2P_FLAG_C2C` is allowed.
    /// * `CU_FILE_P2P_FLAG_NVFS` and `CU_FILE_P2P_FLAG_DMABUF` are not allowed to
    ///   be set via this API (they are managed internally by the driver).
    ///
    /// This API sets the P2P flags for a specific filesystem or block device.
    /// Currently, based on the platform, only `CU_FILE_P2P_FLAG_PCI_P2PDMA` or
    /// `CU_FILE_P2P_FLAG_C2C` is allowed. If these flags are set, then the I/O will
    /// be attempted via P2PDMA mode (if the system supports it) instead of traditional
    /// NVFS mode.
    ///
    /// # Parameters
    ///
    /// - `status_flag`: The filesystem/device status flag (for example,
    /// [`CUfileDriverStatusFlags::CU_FILE_LUSTRE_SUPPORTED`], [`CUfileDriverStatusFlags::CU_FILE_NVME_SUPPORTED`], etc.).
    /// - `p2p_flags`: The P2P flags bitmask to set.
    ///
    /// # Return value
    ///
    /// - [`CUfileOpError::CU_FILE_DRIVER_NOT_INITIALIZED`]: [`CUfileOpError::CU_FILE_DRIVER_NOT_INITIALIZED`] if the cuFile driver is not initialized.
    /// - [`CUfileOpError::CU_FILE_INVALID_VALUE`]: [`CUfileOpError::CU_FILE_INVALID_VALUE`] if `status_flag` is invalid.
    /// - `CU_FILE_P2P_FLAG_NOT_SUPPORTED`: `CU_FILE_P2P_FLAG_NOT_SUPPORTED` if `p2p_flags` contains flags not
    ///   supported on the current platform.
    /// - [`CUfileOpError::CU_FILE_SUCCESS`]: [`CUfileOpError::CU_FILE_SUCCESS`] on success.
    pub fn cuFileDriverSetP2PFlags(
        status_flag: CUfileDriverStatusFlags_t,
        p2p_flags: CUfileP2PFlags_t,
    ) -> CUfileError_t;
}
